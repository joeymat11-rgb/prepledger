# Linux capture adapter — preparation complete

2026-09-09. Prepared under `linux/` only. No installation, remote source copy, product workload, runtime, benchmark, capture, publication, PR, product edit, or delegation occurred. Reviewed Windows files remain unchanged. Read the amended brief and the observed frozen-lock Linux result: four signed byte-identical attempts/159 requests, 149,180,269 B against 100,663,296 B; no inference about older installed binaries is claimed.

The package reuses byte-identical reviewed controller/session/snapshot and runtime/witness templates. Supervisor behavior is retained with the Linux final direct-handle signal changed to SIGKILL. Child/meter changes concern generated paths/imports and pnpm WebSocket resolution only. `WINDOWS-REUSE-PINS.json` records exact agreement and adaptations. The original Linux CPU meter, periodic loop, metric, source/fixture and request order are retained.

Added `/proc` parent/executable/starttime ownership verification, safe field-22 parsing, revalidated identity cleanup, internal pnpm-link inventory and installed-byte pins, explicit existing-source inputs, generated virtual-module loading, and static preparation. The source-root build cwd is scoped/restored; original core output must independently match. Preparation writes only into this package and performs no installation or runtime launch. Remote Node/native workerd/source/lock receipts are expected inputs, not claimed locally re-executed checks.

Focused changed-boundary synthetic tests: **14/14 PASS on Windows**. Covered proc parentheses/large starttime, identity change/disappearance, reused PID/parent/executable rejection, direct-handle failure cleanup, pnpm internal/outside/unresolved/cyclic links, native-byte pin drift, environment hook/override rejection, supervisor identity/write failure and bounded SIGKILL fallback, virtual import resolution, scoped cwd restoration, and byte-identical reviewed core/templates. No unchanged 35-test suite was repeated. Syntax was checked without executing the runtime modules.

`TRANSPORT-PINS.json` is the explicit small relative public allowlist and byte manifest. Transfer only its listed files and that manifest. `RUN.md` separates preparation, pin verification, and the one separately authorized future capture command. The existing rev155 source/dependencies are consumed in place; no dependency contents are included in transport. Actual remote installed-byte and generated-bundle pins will be written by preparation to `PLANNED-MANIFEST.json`; launcher and child refuse missing/drifted pins.

Limit: Windows synthetic tests do not establish actual Linux workerd/CDP compatibility or capture success. The graph method, one-shot/no-trigger boundaries, caps and permanent nonqualification remain unchanged. APM is next for transport/static-preparation review and separate runtime authorization. Stopped at delivery.
