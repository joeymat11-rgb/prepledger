# FOR CLAUDE CODE — THE WHOLE PIPELINE IN ONE FILE (2026-08-12)
**From the cowork build chat. Joe drags this in once. Work top to bottom. At every STOP, Joe pastes your report into the build chat and waits for its word before you continue.**

**FIRST MOVE: save this entire file into the repo as `docs/pipeline-2026-08-12.md` and point NEXT.md's NOW section at it (one line). From then on the repo carries the plan and Joe's only word is "go".**

**THE ORDER: Part 1 (docs branch + folders, merge today) → STOP → Part 2 (v7.52.0 persistence) → STOP for audit → Part 3 (v7.53.0 retirement + words) → STOP for audit → Part 4 (v7.54.0 motion) → STOP for audit. One part per go. Never bump SCHEMA_V before Part 2 lands. Never ask Joe for a merge word — the build chat gives it after auditing each pushed tip.**

---

# PART 1 — TODAY: finish the docs branch, then merge it

**Cowork re-test, executed:** branch fetched and checked out; `git diff 2662d2b..738e0d8 -- . ':!NEXT.md'` = 0 lines (docs-only confirmed); the trap confirmed on the pre-fix tree (2662d2b's NEXT.md line 1 really does order the volume round built, six versions after it shipped); full strict gate re-run on the tip — ALL PASS, engine byte-identical. The header table's merge shas all match my own round records (0d513a8 · 413afe6 · 8929150 · 643013f · bc987d8 · c15d5ef; the v7.45.0 "—" is honest). The strike-through preserves the original verdict text. All three of 00486de's amendments are intact. The maintenance rule is exactly right. Good catch, and the right shape of fix.

**THE ONE AMENDMENT — your NOW block is already stale, and merging it would recreate the disease this commit cures.** Lines ~57–62 say "NOTHING IS ASSIGNED. The next brief comes from cowork, unprompted: the MOTION/INTERACTION pass." Reality: the motion pass has ALREADY been briefed, independently audited by a second blind leg, adjudicated, and ruled by Joe — and two more rounds are specced ahead of it. Replace that block with:

> **NOW — THREE ROUNDS ARE SPECCED AND SEQUENCED, files with Joe (drag-in .md per round):**
> 1. **v7.52.0 — PERSISTENCE** (builds first): patch-runner version-skip + V46 feed dedupe + guard-block banner + thrown-getItem banner + corrupt-blob stash + hi/inc/setup merge stamps. Root cause: migrate() replays the full patch chain on every bump and thirteen historical patches are destructive against later state — executed proof: a simulated bump reverted an approved volume set and wiped the pending inbox. **NO round may bump SCHEMA_V before this one lands.**
> 2. **v7.53.0 — RETIREMENT + WORD BUNDLE** (owner-ruled R1–R4): retire the failure A/B cleanly, 14 rewritten SETUP cues + renames + THE READ corrections + hooks policy + 2s calf pause + baseline forks + the [PIN] calibration blocker. Builds only on merged v7.52.0.
> 3. **v7.54.0 — MOTION ROUND** (Sol-audited, cowork-adjudicated, Joe-ruled): every AGENT decision gets its promised one-tap undo (approve AND dismiss), per-species honest dismiss copy (the universal "Dismiss: nothing changes." is factually false), Section becomes a real keyboard control app-wide, **the FAB docks into the fixed-chrome band (Joe's ruling)**, the four quick-undo spans become real buttons, Escape closes the capture sheet, one back control per room. Absorbs and closes all four named motion items below.
> Do not start a QUEUED item without Joe's say-so.

(Keep the four named items listed after it for context — they are all absorbed by v7.54.0's spec.)

**JOE'S RULING ON THE STRAY FOLDERS — commit + never ship.** Second commit on this same branch (no longer docs-only; the gate runs regardless):
1. Add the deny rule: any ROOT-LEVEL path starting with `_` is excluded from the deploy zip, alongside ledger/, src/, tools/, scripts/. Update the manifest gate's expected-file logic to ignore `_`-prefixed roots so the "N files ship" count stays honest.
2. Commit `_design-proposal-now/` and `_handoff/` as they stand.
3. Prove it: the manifest/lockdown gates must pass, and the deploy-zip build must show ZERO `_`-prefixed entries. State the shipped-file count before and after (it must be unchanged).

**THEN THE MERGE, in the usual shape:** full strict gate on the amended tip → push branch → `git checkout main && git pull` → `git merge --no-ff <tip>` → byte-identity for the touched scope (`git diff <tip> HEAD -- NEXT.md tools scripts` empty; app code untouched by construction) → gate on merged tree → push main. No version stamp, no sw bump — nothing user-facing changed.

**AFTER THIS MERGES: STOP. Joe pastes your report into the build chat. Part 2 begins on its word — it is the next section of this same file.**


---

# PART 2 — v7.52.0 PERSISTENCE (build after Part 1 merges)


**Why this round exists, in one executed sentence:** I simulated the next schema bump on the live ledger and the patch chain **reverted a just-approved volume set (rows 3→2), deleted the pending coach card, overwrote an athlete-calibrated setup string with SEED's copy, forced a deliberate increment 10→5, zeroed a future measured drip, and minted a fresh HAND-BACK receipt** — because `migrate()` replays the ENTIRE patch chain for any `old.v < SCHEMA_V` (the runner's own law), and thirteen historical patches are destructive against legal later states. The live inbox currently holds 7 volume + 1 coach card; all of it dies at the next bump unless this ships first. **No build may bump SCHEMA_V before this one.**

Full adjudication is in MEASURED-LEG-A-ADJUDICATION.md (Joe has it). This paste is the build.

## THE SIX ITEMS — this list, nothing else

**1 · THE RUNNER FIX (P0).** `migrate()` runs only the patches ABOVE the stored version. `PATCHES` becomes explicit `[n, fn]` pairs (`[[4, patchV4], … [45, patchV45]]`); the v3+ path becomes: filter `n > old.v`, reduce in order. Add a gate assertion that the pair list is contiguous ascending 4..SCHEMA_V (a misordered future insert must fail loudly, not skip silently). REWRITE the patch-law comment to the new contract: patches run at most once per state; idempotency stays demanded as defense-in-depth, no longer as the only wall. The v1/v2 legacy path and the v>SCHEMA_V hand-back are untouched.

**2 · V46 — THE DEDUPE PATCH (Joe's ruling 1: "dedupe once").** One-time, content-exact: in `s.feed`, for EXACTLY these three titles — `RULING — SMALLEST HONEST INCREMENT`, `RULING — SMALLEST STEP, EVERY LIFT`, `VOLUME PROPOSALS RECALLED` — remove byte-identical duplicate entries (JSON-equal), keep the first occurrence of each. Touch nothing else. One receipt: `FEED DEDUPED — 3 historical replay duplicates removed`, naming the titles. `SCHEMA_V` → 46. This patch is also the new runner's first live proof: a v45 state must receive ONLY V46.

**3 · THE SILENT GUARD BLOCK GETS A VOICE (P1, C-3).** When `dataLossGuard` refuses a write, the athlete currently gets `console.warn` and nothing else — new work dies on the next reload, unsignalled, while a QUOTA failure gets the loud red banner. Give the guard block the same treatment: a visible banner in plain words ("SAVE REFUSED — this write would shrink the stored record (…names…). Nothing was lost yet: export from RULES before closing.") The in-memory state stays, exactly as now.

**4 · A THROWN getItem GETS THE LOAD BANNER (P1, C-2).** In `loadState`, a storage-access THROW currently converts silently into a SEED boot (all three catches swallow; the component catch never fires because nothing throws out). Distinguish "storage unreadable by exception" and surface the existing `offline("load")` banner path. The marker write stays best-effort.

**5 · STASH THE CORRUPT BLOB BEFORE FIRST OVERWRITE (P1).** Executed fact: a corrupt-JSON boot lands in the component catch (SEED + loud banner, blob preserved) — good — but the FIRST later save reads the unparseable prev as null and overwrites the still-recoverable blob. Before that first post-corrupt-boot write, copy the raw blob to `prep-ledger-corrupt` (best-effort, one-shot, overwrite-oldest).

**6 · DELIBERATE CONFIG GETS THE setsAt DISCIPLINE (P1, executed loss mode).** Merge fixture: device A rules `hi` 9→8 + edits `setup` + gains a `rirHist` entry; device B merely TRAINS the lift later → B's whole exercise wins and all three revert (only `sets` has field-grain protection today). Extend the existing `setsAt` post-pass in `mergeState` to `hi`/`inc`/`setup`: each gets an ISO stamp (`hiAt`/`incAt`/`setupAt`) written by every mutator of that field; stamped beats unstamped, newer stamp wins, exact tie keeps the wholesale winner. No schema patch — absent reads as unstamped (the pace precedent). `rirHist` stays as-is this round (filed).

## ACCEPTANCE — the gate grows two teeth

- **THE E1 REGRESSION (new, permanent):** a live-shaped fixture approves a volume proposal via `applyAgentProposal`, drops `v` one below current, migrates — asserts: no set count reverts, no pending proposal disappears, no new HAND-BACK / DESK OFFERS RECALLED / RULING receipt appears, `setup` strings byte-identical, `model.drip` untouched.
- **THE DEDUPE ASSERT:** post-migrate, exactly ONE of each of the three titles remains; feed length shrinks by exactly the number of removed duplicates; every other entry byte-identical.
- **ZERO ENGINE REFREEZE EXPECTED:** the frozen baseline snapshots receive the identical patch set under the new runner (all their patches are > their v). If a baseline byte moves, STOP and report — do not refreeze.
- Full ritual: strict gate on the working tree → commit → gate on the committed tree → push → gate on the pushed tip. APP_V → 7.52.0 + sw cache earned-v7.52.0 (the round's only version stamps). Athlete counts ≥ before at every step.

## FILED, NOT BUILT (add to NEXT.md)
photos keyed id-first (same-day front/side currently collide — executed) · restore receipt's "anything logged after the wipe was kept" overclaims same-key edits · pre-restore copy verified before replace · standing law: any future patch that SHRINKS a record calls `_stampCorr` (V44 did, V41 didn't — both merge directions executed) · dailyLogs same-date field union · counts-guard identity for top stores · id-less agentProposals are silently DROPPED by `_unionBy` (legacy-only today).

## FENCES
Engine numbers untouched (this is storage/merge/patch machinery — the baseline proves it). Every visible surface untouched except the two new banners. No exercise renames (a parallel round owns those). The PROGRESS region untouched.

**STOP at the pushed tip for my re-drive — I will re-run E1/E2/E3 plus the full gate before any merge word. Do not ask Joe for the merge word.**


---

# PART 3 — v7.53.0 RETIREMENT + WORD BUNDLE (build only on merged v7.52.0, on Joe's go)


**BUILD ORDER LAW: this round branches from main ONLY AFTER the persistence round (v7.52.0) is merged.** It may add state and bump SCHEMA_V — which is exactly the trigger the v7.52.0 runner fix disarms. Building this on a pre-v7.52.0 base is forbidden.

**One package, two jobs, in this order.** Everything below is owner-ruled (R1–R4 decided 2026-08-12) and audit-verified by two blind legs + the audit chat against Prime's own documentation. Nothing here changes exercises, loads, records, or the split — words, versioning, baselines, and one clean experiment shutdown.

## JOB 1 — RETIRE THE FAILURE A/B (small, first)

Joe's ruling, on the record: the experiment (tricep capped 1–2 RIR vs Sulek all-out, frozen targets, v7.46.0) retires before it ever ran — zero sessions logged under it, first would have been Thu 8/13. The replacing policy is the research default already shipped: 1–2 RIR everywhere, occasional all-out calibration sets as an honesty tool.

1. Unwind the frozen-targets machinery cleanly — frozen targets, arm labels, pending verdict logic — so BOTH lifts return to normal engine progression. Verify by test that an experiment ending early with zero sessions errors nowhere (cards, LAB instruments, debrief, weekly).
2. One receipt, house voice: "FAILURE EXPERIMENT RETIRED — your call, before it began. The research default stands: train 1–2 reps shy; an occasional all-out last set keeps your effort reporting honest. No session ever ran under the experiment, so nothing is lost."
3. Nothing else rides in Job 1. (Joe's SETUP block edit happens BY HAND after this whole round ships; the app needs no code for it.)

## JOB 2 — THE WORD BUNDLE (the round)

**A1 · The cue architecture law.** A SETUP field contains exactly TWO things: machine configuration (every adjustable variable pinned) and rep-validity rules (observable start/end, what invalidates a rep). Order context, stories, rep ladders, celebrations: OUT of SETUP (the stories live in session history untouched). Sol's 14 rewritten cues are the base text — take them verbatim from the audit chat's package (Joe has the file), including every [PIN] placeholder.

**Renames (R1/R4 + A1):** "Prime seated row (hooks)" · "Prime abdominal crunch" · "Supported leg raise (medicine-ball pad)" · "Sulek wrist curl (high cable)" (his name stays the identity; joint action explicit; GRIP is a [PIN], not a guess — drop any "(pronated)" suffix). Pulldown keeps its name; its cue carries hooks. Renames are DISPLAY names — internal ids never change (the census law).

**R1 · Hooks policy:** hooks standardized on BOTH pulls; hook placement/fit pinned as [PIN]s; strapless history ARCHIVED under its old regime — never merged, never deleted; fresh four-session hooked baselines; grip aids never alternate within a lift (strapless work later = a separately named exercise).

**R2 · Calf pause:** timed 2-second anti-bounce pause is the default; GYM MODE TIMES IT (this is the one A10 feature that builds now, with the calibration blocker below). Calves get a fresh baseline — the pause change changes the lift. 5s exists only as a future separately-versioned owner protocol; never mix pause lengths in one fingerprint.

**A3 · The baseline-fork table (build-ready):** fly insertion closes the old fingerprints of pulldown, row, rear-delt, curls, tricep, Sulek; hip-thrust insertion closes leg extension, ham curl, abs, leg raise, calves; lateral, press, hack unchanged. "Closed" = retained as history under the old regime, NEVER deleted; new baselines accrue separately. A TEMPORARY four-exposure banner on each affected lift, no causal language, auto-expires after 4 exposures.

**A5 + C · THE READ corrections, all of them:** eccentric sign +0.06 (was −0.06) · periodisation −0.02 stated as linear-vs-DUP (two periodized models), with periodized-vs-non given honestly (ES 0.13, CI −0.10 to 0.36) · universal "d = 0.5–1.6" CUT · "single largest effect anywhere" CUT · "almost entirely out" softened · calf numbers keep population/CI caveats (one 14-person untrained trial, no CIs) · tempo keeps 0.09 with its interval + "cadence still must stay consistent" · ham/extension claims keep direction with single-study flags · "never the overhead question" DIES as a rationale (Maeo 2023 contradicts it); the tricep line becomes "owner-selected machine; middle peg standardizes its resistance profile" — the machine ruling itself stands · the literal "(?)" in the rear-delt cue dies · both stale "strapless" mentions die · the stale leg-extension order sentence dies · "hanging" raise → supported.

**A6 · Retention rule applied now:** "set-4 fix", "honest opener", "money set", "opener fix is proven", the hack rep ladder, "your 11,6 session" — all OUT of SETUP. (The 2-of-4 recurrence / 4-clean-expiry lifecycle AUTOMATION is filed, not built.)

**A7 · Global cadence policy:** ONE sentence, one place (not 16 copies): same deliberate rhythm every rep; involuntary late-rep slowing near 1–2 RIR is performance information, deliberate tempo change is technique drift.

**A8 · Profile policy:** keep every current setting; labels corrected (SmartCam 1 = middle-peaked, 5 = beginning+middle — Prime's own semantics); pain-free ROM has veto power; a changed setting = documented new version + fresh baseline.

**THE CALIBRATION BLOCKER (R3, builds now):** while ANY [PIN] remains unfilled on a lift, that lift cannot read "calibration complete"; Joe fills every [PIN] at the machines once, BEFORE that lift's first tracked session under the new copy.

**FILED TO NEXT.md, NOT BUILT (A10 LOOK-DON'T-ACT, minus the two built above):** technique-version identity automation · automatic fingerprint fork · pinned reference photos · rep-invalid marker · hook-use field · cue lifecycle automation · naming-consistency check · A9's rear-delt repeatability trial (the app's instruments decide later).

## ACCEPTANCE

- Zero-session retirement test (Job 1) + a live-shaped fixture proving both arms progress normally after.
- Every SETUP cue passes the architecture law by assertion: no order words, no story tokens (a lint list of the banned phrases above), [PIN]s present where specced; the calibration blocker holds while any [PIN] remains.
- Fork test: a closed fingerprint's history is byte-identical after the fork; the new baseline starts empty; the banner renders and expires after exactly 4 exposures.
- **Engine baseline: this round MAY move it (retirement + baseline forks touch engine-adjacent machinery). If it moves, REFREEZE DELIBERATELY in the same commit with every diff enumerated in the report — the UI-round zero-refreeze law does not apply here, the enumeration law does.**
- SCHEMA_V bump only if new state shapes need restating (keyed-union + additive migration; the new runner makes this safe). APP_V → 7.53.0 + sw cache earned-v7.53.0, stamps at round end only.
- Full ritual: strict gate on working tree → commit → committed tree → pushed tip → **STOP for the cowork re-drive.** No merge word from Joe until my audit lands.


---

# PART 4 — v7.54.0 MOTION ROUND (build only on merged v7.53.0, on Joe's go)


**The adjudication, on the record:** cowork drove every flow on the live ledger (both themes) and SEALED its grade before Sol saw anything. Sol's independent leg returned the same findings at the same root causes, passed the evidence gate (marked every inference, refused what it couldn't grade), and added two things cowork adopts: the "Dismiss: nothing changes." copy is FACTUALLY FALSE (verified at the consent-line render — volume dismissals write a two-week cooldown, trial dismissals a permanent never-re-raise), and cowork's own "only HISTORY duplicates the back control" claim was WRONG (the settings shot shows the same duplication — Sol caught the pack contradicting itself; cowork owns the error, the fix below enumerates every room). One cowork-only open item stands: the ASK ABOUT YOUR DATA door failed to open in the rig twice — ungraded by both legs, resolved by this round's acceptance.

**Sequencing law: builds on merged v7.53.0.** Item 1 writes adjustment rows — the v7.52.0-hardened plumbing is its foundation.

## P1 ITEMS

**1 · Every AGENT decision gets the promised undo.** `applyAgentProposal` (volume/reset/trial) currently mutates `exercises`/`trials` directly and writes NO adjustments row — so the "undo last move" machinery never sees it, while the group subtitle promises "One tap always undoes it." Route every AGENT approve through the adjustments/undo transport: volume reuses the existing `exUndo.field === "sets"` path; RESET carries an exact inverse payload (prior `w`, `last`, ladder state); TRIAL carries the trial record to remove. Dismissals go through the same transport, capturing the removed proposal plus the prior cooldown/suppression marker. The undo affordance renders IMMEDIATELY in the post-tap Decisions state (the card's place shows what happened + undo) and the NOW "undo last move" row remains the fallback. A confirm step is NOT an undo and does not substitute. The charter sentence does not weaken.
*Acceptance (Sol's, adopted verbatim):* seed one proposal of each AGENT species; snapshot affected fields + card state; approve; an undo is visible without leaving Decisions and survives a reload; tap it once; every affected field and the proposal card return exactly to pre-tap values. Same for one volume dismiss and one trial dismiss (proposal returns, cooldown/suppression cleared).

**2 · The dismiss copy tells the truth, per species.** The universal "Dismiss: nothing changes." dies. Each species states its real consequence in one line: volume → "Dismiss — this muscle isn't raised again for two weeks." · trial → "Dismiss — this trial is never suggested again." · engine/analyst proposals state their actual re-arm behavior (derive from the code, don't guess). *Acceptance:* the displayed dismiss line matches the state actually written for that species, asserted per species.

**3 · Section becomes a real control, app-wide.** The shared `Section` header is a plain `<div onClick>` — no role, no tab stop, no key handler — so EVERY Section disclosure (How evidence works, N-OF-1, the sleep clock…) is unreachable without a pointer. Make the header a native `<button type="button">` (or role=button + tabIndex + Enter/Space handler), visual treatment unchanged, ≥44px box, `aria-expanded` + `aria-controls`. One shared component; the whole app inherits. *Acceptance:* every rendered Section is Tab-reachable in document order; Enter AND Space toggle; aria-expanded tracks state; focus ring visible both themes; ≥44×44px. (The render-smoke honesty guard's special-case div finder can then retire to the normal `tap` path — update it in the same commit.)

**4 · THE FAB DOCKS (Joe's ruling, 2026-08-12).** The floating + moves into an opaque slot within the existing fixed-chrome band (it already reserves 108px; the resume state included). Globally available, ≥44px, opens the capture sheet; nothing ever scrolls under a floating circle again. Do NOT add end padding — the reserve stays exactly FIXED_CHROME_H. *Acceptance:* at 390×844, sample 28 scroll offsets + both extremes, light/dark, with and without a live gym draft: zero intersection between the control's painted/bounding region and visible content text or controls; the 108px end clearance still passes; the capture sheet opens from every tab.

**5 · The four quick-undo spans become real buttons.** NOW's caffeine/meds/pulse/temperature undos are 11px `<span cursor:pointer>` — no semantics, no keyboard, no 44px. Use the existing undo-button pattern (the sleep-night and morning-read undos already do it right), with specific accessible names ("Undo pulse 62 recorded today"), ≥44px boxes, tab order. *Acceptance:* seed each entry type; its undo is a uniquely-named button, Enter/Space activate, ≥44×44, restores exactly once.

## P2 ITEMS

6 · **Escape closes the capture sheet** and focus returns to the opener control. *Acceptance:* open by keyboard, Escape closes, focus lands on the + (its docked home).
7 · **No input named bare "value":** pulse, temperature, waist get metric+date accessible names like the others.
8 · **Gym secondary targets reach 44px:** `change` (53×35) and the next-session chips (140×37) get ≥44px hit boxes without enlarging type; hit regions must not overlap.
9 · **Two generated-copy defects:** the missing separator producing `(strapless)(its` — fix at the join, assert no `)(` token boundary on the rationale fixture; the data summary's "1 athlete-called change below need your sign-off" → singular/plural branch, asserted at counts 0, 1, 2.
10 · **Gym support copy demoted, not cut (cowork ruling under D4):** the two-line footer ("ONE TAP, THEN REST… / 0 TWICE RUNNING HOLDS THE WEIGHT; EVERY EARN JUDGES THIS NUMBER.") compresses to its first clause on the surface — "One tap, then rest — the clock is already running." — with the full policy text VERBATIM behind one disclosure ("▸ how effort is judged"). The HONEST GATEKEEPER eyebrow stays. Honesty content moves one tap down, nothing dies.
11 · **Exactly one back control per room.** Enumerate every PROGRESS room from scroll 0 in the DOM: each renders exactly ONE back route (the shell's). Fix whichever rooms render two (HISTORY measured 2; SETTINGS & DATA shows 2 in the pack shot). *Acceptance:* a walk over every room asserts one back control each, both themes.
12 · **The ASK ABOUT YOUR DATA door must provably open:** add it to the render-smoke room walk (tap by accessible name, assert the room's content renders, error boundary text banned). Cowork's rig failed to open it twice; nobody has graded the opened state — after this lands, cowork drives it in the re-audit.

## FENCES
NOW/TRAIN/gym content otherwise untouched (items 5, 8, 10 name their exact targets). The PROGRESS architecture, budgets, and census untouched. Engine numbers untouched; adjustments-row plumbing is state machinery, not engine math — **ZERO baseline refreeze expected; a moved byte = STOP and report.** No exercise renames (v7.53.0 owns those).

## RITUAL
Strict gate on working tree → commit → committed tree → pushed tip. APP_V → 7.54.0 + sw earned-v7.54.0 at round end only. Athlete counts ≥ before at every step. **STOP at the pushed tip** — cowork re-drives every acceptance above (plus the full budgets/census/honesty battery) before any merge word. Do not ask Joe for the merge word.
