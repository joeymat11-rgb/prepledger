# Caselaw — rulings mined from behavior

Numbered rulings inferred from the athlete's own decisions in the feed/queue (never asked for). Format: facts (with numbers) · ruling · principle · scope. House law stands above all: deterministic engines compute, models interpret; everything measured-or-badged; every action is a consent-gated proposal; the coach holds structural authority.

**CL-1 · Press 245 is provisional on debt.** Facts: 245×8,8,7 landed 7/23, but on debt (fix-window opened 7/22); the prior clean 8,8,7 was 7/16. Ruling: does not count / no load until repeated clean (queue: "Do NOT load until owned"). Principle: reps earned on a debt day are provisional until repeated on a clean day. Scope: PR ownership + load progression. **Confirmed in full 7/30 (run 10):** 245×8,8,7 was repeated on a clean (non-debt) day and the app banked it — feed: "PRESS 245 OWNED — 8,8,7 repeated — that is the confirmation, so 250 enters the queue at coach flag." The intervening attempts behaved exactly as the ruling predicts: 8,7,6 (7/20), a debt-day 8,8,7 (7/23) that did not bank, 8,8,5 (7/27). Fourteen days from first landing to owning it, and the only thing that changed at the end was the day being clean. This also settles a competing story in my own briefs: the repeat happened after a 7.5 h night, but his history refuses sleep as the mechanism on this lift (8,8,7 after a 4.5 h night on 7/16; 8,7,6 after a 7.5 h night on 7/20) — sleep bought the clean week, CL-1 banked the reps.

**CL-2 · Cratered self-bump reverts.** Facts: leg extension self-bumped 150→155, cratered to 9,6 (7/21). Ruling: revert to 150; 155 reopens only after 150×9,9 lands. Principle: an unearned self-bump that craters reverts to the last owned load. Scope: load progression. **Satisfied and reopened 7/31 (run 11):** the reopening condition was 150×9,9 and he put up 150 × **10,9**; the app wrote "EXTENSION 150 RE-OWNED — 9,9 — the 155 gate reopens, earned" and `q_ext155` entered the queue as DEBUT. The full arc is the cleanest vindication of this ruling on record — self-bumped 155 cratered 9,6 (7/21) → reverted to 150 → 6,9 (7/24) → 8,7 (7/28) → 10,9 (7/31) → gate reopens. Ten days from crater to earned re-entry, and the thing that closed it was an opener taken harder rather than any change in load. **The ruling's live half is now the debut itself:** 155 has one prior data point and it is a crater, so this attempt is what tells us whether the crater was the load or the unearned-ness. Carded as `sug_2026-08-01_ext155` with an explicit success threshold (≥8,8) so a normal +3.3%-load debut is not mistaken for a second crater and self-reverted.

**CL-3 · Sleep gates structural additions. — RETIRED BY THE APP, 7/29. Do not cite as live.** Facts: hack 3rd-set debut passed its gate but was deferred 2× for sleep; now runs unless a true <4.5h night. Ruling (as it stood): sleep quality can defer an already-earned progression. Principle: recovery state gates structural additions, even earned ones. Scope: session sequencing + recovery. **Struck 7/29 by the app itself**, in a feed entry titled "THE CLEAN-SLEEP GATE IS GONE": *"It was retired in the engine earlier but survived in three places that decided what you lift: the prescription desk held every lift on a short night, the set plan pulled your last set off failure, and the queue still told you sleep decided before your muscles did. None of that had evidence behind it — a short night costs about 2.85% on strength, which is smaller than your own set-to-set spread. Sleep still matters enormously, just for a different thing: at a matched deficit it decides how much of what you lose comes off muscle instead of fat. It is now on your sleep card as that, and nothing anywhere gates a rep on it."* Noted 8/1 (run 11). This is the first ruling in this file to be **overturned by a change in the system rather than by the athlete's behaviour**, and it matters beyond calves: [[CL-10]] was mined partly against CL-3's logic (the hold card that CL-10 examines was "CL-3 applied off recovery 45/100"), and my own 7/30 restraint leaned on the CL-1-not-CL-3 distinction. Those remain sound — CL-10 is about *authority*, not about whether sleep should have gated anything — but any future card that wants to defer an earned progression on recovery grounds no longer has CL-3 to stand on and must argue from the data directly. Sleep's standing in the constitution (Article VI, Nedeltcheva 2010) is untouched: it is a body-composition lever, not a rep gate.

**CL-4 · Sanctioned events carry no penance.** Facts: Wedding #1 (7/18) ran ~2,300 cal / 170 g protein, below maintenance; logged as ZERO-COMP EVENT #5, "no penance." Ruling: pre-agreed social/event meals incur no compensatory deficit. Principle: sanctioned events are not tracked at the table and not repaid. Scope: nutrition + event days.

**CL-5 · Mid-session volume adds flag for the coach.** Facts: 7/23 rear-delt → 3×/side and lateral 4th set were both user-called mid-session — two structural moves in one day. Ruling: the athlete may add volume live, but the batch flags for coach review (Monday). Principle: athlete-initiated structural changes are provisional pending coach authority. Scope: volume + coach authority.

**CL-6 · Session note overrides a contradicting logged set.** Facts: pronated 7/23 logged 40×12,12, but the session note read "Had to skip pronated today due to time." Asked which was right, the athlete ruled "Skipped" (feed ANALYST ANSWER, 7/24). Ruling: the prose note wins — the logged set is phantom and struck; structured truth carries zero phantom reps. Principle: when a set's log contradicts the day's note, the note governs until the athlete says otherwise. Scope: log integrity. **Hardened 8/1 (run 11) — second occurrence, near-identical.** `sessionLog["2026-07-31"].note` reads "Ham curl skipped this sesh for time. Abnormal" while the same session logs ham 120 × 12,12. The 7/23 case was "Had to skip pronated today due to time constraints. Not typical." against a logged 40 × 12,12 — same stated reason (time), same self-description (not typical / abnormal), same 12,12 rep shape. A third evidentiary leg is present this time that the first case lacked: the ham entry is the **only** lift in the session carrying `rir: null` / `rirSets: [null,null]`, while calves, abs, hanging, hack and extension all carry effort scores and his own ham entries on 7/24 and 7/28 both carry `rir: 2`. A phantom set apparently arrives without effort data. That is now a diagnostic worth checking on its own, ahead of the note. Because CL-6 is settled law, this was **not** re-fired as a QUESTION — it shipped as card `sug_2026-08-01_hamlog`, with `gate: approve` serving as the "until the athlete says otherwise" clause. Live stake: the app has already written "HAM CURL — TOP OF WINDOW … Repeat it and the load queues itself," so leaving the phantom in place queues 125 off a half-real baseline, which is the unearned-bump shape of [[CL-2]].

**CL-7 · An ascending rep pair is not a transposition.** Facts: extension 7/24 logged 150×6,9 (second set above the first) with NO contradicting note. Asked "real or transposed," the athlete ruled "Real" (feed ANALYST ANSWER, 7/25). Ruling: the ascending pair stands as logged — a cautious opener followed by a stronger second set is legitimate. Principle: a second set higher than the first is not, on its own, a data error; only flag a rep-shape surprise when a note or day-flag actually contradicts it. Scope: log integrity. Distinguishes [[CL-6]]: CL-6 required a note-vs-log contradiction; here there was none, so the log is trusted intact.

**CL-8 · An RIR-buffer override never launders debt into owned reps.** Facts: on 7/24 the athlete fired the "RIR DEBT BUFFER — OVERRIDDEN" call ~11 times (athlete call, this session only); the app held banking unchanged and every set still logged provisional. Ruling: the athlete owns effort/RIR on the floor, but overriding the buffer does not bank the reps — debt-day and provisional sets stay provisional. Principle: floor autonomy over effort is not banking authority; consent to push does not convert provisional reps to owned load. Scope: RIR + banking + load progression. Reinforces [[CL-1]] (debt reps are provisional until repeated clean).

**CL-9 · Estimate days are declared post-hoc, at the day level.** Facts: Wedding #2 (7/25) had `dayCtx["2026-07-25"].est` set true with note "declared estimate day," while `events[wed2].estimated` stayed false — the flag went on *after* the event, on the day, not the event, and not in advance. Two analyst pitches to pre-declare the estimate day (7/24, 7/25) were both dismissed. Ruling: the athlete owns the estimate flag and sets it retroactively after the meal is behind him; the event-level `estimated` field is not his lever. Principle: estimate days are a post-hoc, day-level declaration by the athlete — never pre-emptive, never event-level. Scope: estimate days + event accounting. Complements [[CL-4]] (sanctioned events carry no penance): CL-4 governs the no-comp math, CL-9 governs *when and where* the flag is set. **Third occurrence 8/1 (run 11), and the first without an event attached.** `dayCtx["2026-07-30"]` carries `est: true`, note "declared estimate day" — set after the fact, at the day level, on a Thursday with no wedding, refeed or entry in `events[]` behind it. The day logged 3,200 kcal / 200 g protein / 19,000 steps / sodium high. So the flag is not an event mechanism at all: it is how he marks any day whose numbers he does not trust, and he reaches for it retroactively. Two consequences for how I read him. Any `est: true` day should be treated as a *number with wide error bars*, not as a licence to discount it — the 3,200 still moved the week's mean 240 kcal and still produced a +4.0 lb scale response the next morning. And [[CL-4]]'s no-penance logic extends here by analogy but not by right: 7/30 was not a sanctioned event, so the reason not to repay it is arithmetic (one day against a measured maintenance) rather than the standing zero-comp rule. **Occurrences four and five (8/3, run 13):** `dayCtx` 7/31 and 8/1 both carry `est: true` / "declared estimate day" — set after the fact, at the day level, with no event behind either (a 4,000-calorie Friday with 14 on the alcohol line, then a 3,000 Saturday). Five occurrences, one shape, zero exceptions: the flag is simply how he marks any day whose numbers he does not trust, and he always reaches for it retroactively. Read `est: true` as "wide error bars, honestly declared" — never as missing data and never as a licence to discount the day.

**CL-10 · Consent to a hold does not bind the floor; a self-run structural debut stands, but provisional.** Facts: on 7/28 the athlete APPROVED `sug_2026-07-28_hold` ("hold the Calves 320 debut until a clean week" — itself CL-3 applied off recovery 45/100), then the same cycle ran Calves 320×10,8,7,7; the engine advanced `q_calves_inc` to ESTABLISH/done rather than reverting it, on a non-clean week (sleep debt, recovery-low proposal `recovery_2026-07-27` stood down). Ruling: approving a hold card is not the same as honoring it — the floor decision governs, and the executed debut is kept but provisional until repeated on a clean week (it does not bank as owned load). Principle: consent gates *permission the engine would otherwise defer*, not the athlete's own hands; a floor-initiated structural change is provisional-and-kept, gated for ownership, never for permission. Scope: consent vs. floor authority + load progression. Distinguishes [[CL-3]] (recovery gates the engine's *own* deferrals, e.g. the hack 3rd-set) and extends [[CL-8]]/[[CL-1]] (floor autonomy runs the load; the reps stay provisional until owned clean). Open: QUESTION 7/29 asks whether 320 is the intended new floor — grade CL-10's "kept but provisional" read against his answer. Evidence added 7/29 (run 8), on the merits rather than the calendar: 320 produced 10,8,7,7 (32 reps) against 315's 13,12,11,10 (46 reps) four days earlier — 14 reps surrendered for a 1.6% load bump — while the same session ran strong everywhere else (hack 9,9,9, ham 12,11, abs 10,12,10). So the debut is not explainable as a low-recovery day; the load itself is heavy right now. That does not change the ruling, which is about authority rather than merit, but it sharpens what "repeated on a clean week" has to look like: a rep count that closes most of the 14-rep gap, not merely a completed session. Status 7/30 (run 9): still open and still untested — 7/28 remains the only session at 320, so the "kept but provisional" clause has had no repeat to be judged on, and the QUESTION is unanswered. Do not re-fire it; carry it in the brief. Status 7/31 (run 10): still open, and now partly **unanswerable from the app's side** — the 7/31 restore left `exercises[calves]` at 315 / `lastMeta.d 2026-07-21` and `queue.q_calves` back at RECLAIM ("Needs 13,12,11,10 back · last: 12,10,9,8"), so the app is no longer showing him the 320 debut it is supposed to be provisional on. The debut itself is not lost — `sessionLog["2026-07-28"]` has 320×10,8,7,7 and the feed carries "CALVES 320 DEBUT COMPLETE · on debt — provisional" — but the question cannot be fairly graded until the lift card matches the session log. Fix the card first (tonight's #1), then re-read the answer. **CLOSED 8/1 (run 11) — confirmed on both halves, answered in deed rather than in words.** The lift card was fixed (his approval of `sug_2026-07-31_legcards` landed before he trained), and the very next lower day he ran 320 again: **11,9,7,7 on 7/31**. The app then wrote "CALVES — TARGET MET," moved `queue.q_calves` RECLAIM → RECLAIMED/done, and left `q_calves_inc` at ESTABLISH/done with `exercises[calves].w = 320`. So both clauses hold exactly as ruled: the self-run debut was **kept** (never reverted, through a restore that briefly hid it), and it was **provisional until repeated** (it banked only on the repeat, not on the debut). The QUESTION never needed a verbal answer — the floor answered it by turning up and doing it again. **What did not hold was my own grading bar, and that correction belongs here rather than in the ruling.** In run 8 I wrote that a legitimate repeat would have to close most of the 14-rep gap against 315's 46. He closed 2 of it (32 → 34 reps) and his system banked 320 anyway. The gap is a true and useful observation — 320 is genuinely heavy, roughly a quarter of the rep volume surrendered for a 1.6% load bump, and it belongs in the brief — but it was never the app's criterion and never his, and I should not have written it as though it were the test. Principle added for future rulings: **grade a ruling against the system that issues it.** Ruling closed; the rep-volume question is carried as an observation, not a condition.

**CL-11 · Approval is a moment, not a commitment — card discrete actions, never standing restraints.** Facts: two of the nine cards this loop has issued asked him to *refrain* from something rather than *do* something, and both were approved and then overridden in deed. `sug_2026-07-28_hold` ("hold the Calves 320 debut until a clean week") was approved 7/28 and the same cycle ran 320 ([[CL-10]]). `sug_2026-07-29_band_hold` ("leave the calorie band alone — that drift is your step taper") was approved 7/29 and on 8/2 he applied the app's own `AUTO-PILOT · TIGHTEN THE TARGET` proposal at the offered number (`proposals[ap_tighten_2026-08-02].resolved: true, nudge: 0`, no `dismissed` flag, mirrored in `adjustments`; `dismissProposal` would have written `dismissed: true` and an ADJUSTMENT DECLINED feed entry, and did not). Against that, every card asking for a **discrete act** was approved *and* performed: the sleep target was set and adhered to for four nights, the sync republish restored the file, the extension opener was taken harder (and wider than asked), the leg cards were fixed before he trained. Ruling: approval records agreement at the moment of reading; it does not create a standing commitment and it does not survive a later one-tap from the app itself. Principle: a card whose value depends on continued abstention is structurally weak in this system — write cards as one discrete, completable action, and put standing restraints in the brief or to the coach instead. Scope: card design + consent semantics. Extends [[CL-10]] from the training floor to the nutrition dial: same shape, different surface. **Ruled young and deliberately narrow** (n=2 on the restraint side against n=4 on the action side, Article IX) — the contrast is what carries it, not the count. It also disposes of the second half of W-1: taps are not reflexive. Two cards sat untapped on 8/2 while he was demonstrably in the app resolving a different proposal, and the auto-pilot offered him a real "Not now" that he declined to take. The gate carries information; it just does not carry *duration*.

**CL-12 · A declaration in a session note is intent, not state — the floor still does what the floor does.** Facts: the 7/30 session note reads "4th set of laterals is not going to be done any sessions until (if you determine) the massing phase" — an explicit, standing, athlete-declared programme change, written on the floor. The same session ran four sets. The next upper day, 8/3, ran **four sets again** (80 × 15,15,13,12, the fourth taken to 0 RIR), five days after the declaration. Nothing in `exercises.lateral`, `queue` or `plan` ever recorded the removal, and the app went on to queue the 85 increment off that four-set session ("LATERAL MACHINE 85 DEBUT · Earned via 80×15,15,13,12"). Ruling: a programme change declared in prose does not take effect and does not bind — it records what he was thinking at the time, and the next session governs. Principle: read a session note as a *statement of mood or intent*, not as a change of state; never adjust the model, the volume ledger, or a card on the strength of a declaration alone — wait for the behaviour. Scope: session notes + volume + programme structure. Extends [[CL-11]] one surface further out: CL-11 said his approval of *my* card is a moment rather than a commitment; CL-12 says the same of his declarations to *himself*. It also resolves the second half of [[W-2]] by making it moot — side delts stay at 8 weekly sets rather than dropping to 6, so the ≥10-hard-sets concern (Schoenfeld 2017) is unchanged rather than worsened, and this comes off the coach list. **Ruled narrow and deliberately** (Article IX): n=1 on execution, though it is a clean, pre-registered test — W-2's stated resolution condition was literally "the next upper day: three sets or four," and the answer came back four. Distinguishes [[CL-5]], which covers a volume *add* actually performed on the floor; a performed change is behaviour and stands, a declared change is not.

**CL-13 · Failure-work openers bill in progression, not in reps — and the app collects.** Facts: on 7/30 and again on 8/3 the rows opener was logged at 0 RIR (`rirHist [0,0]`). The app set `exercises.rows.holdFlag = true` and wrote "RIR 0 TWICE — opener running hot two sessions straight — load HELD until an honest session lands." While that flag stands, `progressStep` returns `{ add: 0 }` and the top-of-window branch is diverted to "TOP OF WINDOW, BUT HOT — a grind is not an earn," so **no rep count can advance the load**. Meanwhile the thing [[W-3]] was actually watching for did not happen: the 8/4 lower day held or improved on four of five comparable lifts (hack 160 × 11,11,10 vs 11,10,11 — 32 reps either way; abs 100 × 12,12,15 → 13,13,15; calves 320 × 11,9,7,7 → 11,10,7,8; hanging 6,6 → 6,5). Ruling: taking openers to failure does not necessarily cost him reps in the following session, but it does cost him the *right to progress the load*, immediately and automatically, on any lift that does it twice running. Principle: on this system the price of a hot opener is charged by the governor, not by the muscle — look for the cost in `holdFlag` and `progressStep`, not only in the next session's rep line. Scope: RIR + load progression + how to price failure work. Release condition is symmetrical and cheap: one opener logged at 1 RIR or better clears the flag on the spot ("HOLD RELEASED — honest again, loads can earn"), which is why this is a card and not a restraint ([[CL-11]]). **Ruled with two honest limits.** The 8/4 retest logged `rir: null` on every opener, so it did not reproduce the 0-RIR condition it was testing — the rep half of W-3 is answered but not cleanly demonstrated. And the extension entry from that session is missing entirely ([[W-5]]), so one of the four watched lifts has no data at all. What is *not* thin is the governor half: it is read directly out of source and out of `state.json`, not inferred from a rep pattern. Explicitly **not** [[CL-3]] returning by the back door — the governor keys on opener RIR, and the source comment at `progressStep` says so in terms: sleep "did not vanish from the engine — it moved to where it is actually earned… the night is a receipt, not a governor."

**CL-6 — MECHANISM FOUND, 8/5 (run 15). The ruling stands; my reasoning for it was wrong.** Both phantom sets were caused by a defect, not by the athlete's memory or a logging slip. Commit `f3b5afc` / `78ea9ea` (8/4 01:29): *"Gym Mode: a control labelled 'skip' was banking a set the athlete never performed"* — the lift-screen skip advanced without recording the skip, so `finish()` banked the lift **at its target reps**. That single line explains every feature of both cases at once: why pronated 7/23 and ham 7/31 both read exactly 12,12 (target reps, not performed reps), why neither carried an effort score (nobody was there to rate a set nobody did), and why the athlete's prose note was right both times. So the diagnostic I added on 8/1 — "a phantom set apparently arrives without effort data, and that is worth checking ahead of the note" — was correct *and* I had its status backwards: `rir: null` is not a clever secondary tell, it is the defect's fingerprint. **Standing lesson: when the same anomaly recurs with an identical shape, look for a defect in the writer before ruling on the athlete's memory.** Practical consequences. The ruling is unchanged and still governs (the note wins). The class is closed at source — four releases on 8/4 (v7.9.0 phantom skips, v7.10.0 un-skip + `deriveLastMeta`, v7.11.0 correction-survives-sync, v7.11.1 `reconcileLiftCaches`) — so a *third* occurrence of this exact shape should now be read as a regression rather than as CL-6 evidence. Both historical repairs are stamped and survived the round trip (`2026-07-31` at 00:09:52Z, `2026-07-23` at 00:10:07Z), and `ham.last` healed 12,12 → 10,10.

---

## Watching — candidate rulings not yet hardened

**W-1 · Does approval mean anything? (opened 7/30, run 9).** Facts: four suggestion cards have been offered and all four were APPROVED, none dismissed (`suggestionLog`: `sug_2026-07-28_sleep`, `sug_2026-07-28_hold`, `sug_2026-07-29_band_hold`, `sug_2026-07-29_ext_opener`) — and one of the four, the hold card, was overridden in deed the same cycle ([[CL-10]]). Why it is not a ruling yet: with zero dismissals on record, a 4/4 rate cannot distinguish "the cards are sharp" from "he taps approve on everything," which is precisely the thin-data situation Article IX says to flag rather than rule on. Why it matters: if approval is universal it stops being a filter, adoption stops functioning as a grade, and the only honest scoring left is whether the *predictions* came true. Resolve on the first dismissal, or on a second approve-then-override.

**W-1 update (7/31, run 10).** A fifth card was offered (`sug_2026-07-30_lateral_85`) alongside the sync card. The sync card was approved within hours; the lateral card is still untapped a day later. So the record now reads 5 offered / 5 approved / 0 dismissed / **1 ignored** — the first card that did not get a tap, and the one my own scorecard has since graded a miss. An ignore is weak evidence: it could be a considered pass, or it could simply be that the top card gets read and the rest do not. It nudges the watch toward "the gate is doing something" without settling it. Resolution conditions unchanged: an explicit dismissal, or a second approve-then-override.

**W-2 · Does a declared volume *removal* bind, and does it need the coach? (opened 7/31, run 10).** Facts: the 7/30 session note reads "4th set of laterals is not going to be done any sessions until (if you determine) the massing phase" — an explicit, standing, athlete-declared programme change made on the floor. The same session still ran four sets (14,13,12,12 at 80, the fourth taken to 0 RIR), so it is declared and not yet executed. Why it is not a ruling yet: n=1, and it is a *declaration* rather than a *behaviour* — [[CL-5]] was mined from two volume changes actually performed. Why it matters two ways. First, authority: [[CL-5]] says an athlete-initiated volume **add** stands but flags for coach review; a removal is the mirror case and there is no owned load to protect, so the ruling may be that a removal takes effect immediately and only the coach can restore it. Second, cost: at two upper days a week this takes side delts from 8 weekly sets to 6, against the ≥10 hard-sets-per-muscle prior (Schoenfeld 2017) and a Volume Ledger that already reads `delts_side` light. That is a body-composition consequence, not just a bookkeeping one, which is why it went to the brief and the coach rather than onto a card. Resolve on the next upper day: three sets or four.

**W-1 update (8/1, run 11).** Both 7/31 cards were approved, taking the record to **7 offered / 6 approved / 0 dismissed / 1 ignored**. Two things sharpen the watch without resolving it. First, `sug_2026-07-31_press250` shipped deliberately with `gate: "coach"` so that it would *not* be a one-tap — and he tapped it anyway, the feed recording "training progression noted — coach territory," with `q_press250` still sitting at DEBUT and nothing run. So the coach gate is currently **a label, not a brake**: it changes how the decision is worded in his feed, not whether he taps it. That is worth knowing before Article IX's "raise it for a human" is treated as a working safety mechanism in this app. Second, the evidence now points slightly toward the gate being real rather than reflexive: the only card he has not tapped is the one my own grading later called a miss, and both cards he approved this cycle were followed by exactly the behaviour they asked for (leg cards fixed before training; press 250 left un-run pending the flag). Resolution conditions unchanged — an explicit dismissal, or a second approve-then-override.

**W-2 update (8/1, run 11).** Unresolved and untested: 7/31 was a lower day, so no lateral work happened. The next upper day still decides it — three sets or four.

**W-3 · The 0-RIR opener — a win that is also a bill (opened 8/1, run 11).** Facts: on 7/28 every lower-day opener was logged at 2 RIR. On 7/31, after `sug_2026-07-29_ext_opener` asked for ~1 RIR on the extension alone, `rirSets[0]` came back **0** on the extension, hack, abs and hanging raise — he applied the card harder and wider than it was written. The same session produced his best numbers on record at three loads: hack 160 × 11,10,11 (from 9,9,9), abs 100 × 12,12,15 (from 10,12,10), extension 150 × 10,9 (from 8,7). The day before that, 7/30, five of nine upper lifts finished at 0 RIR. Why it is not a ruling yet: two sessions, and the effect so far is entirely positive, so there is nothing to rule *against*. Why it matters: the science floor says 1–3 RIR is sufficient stimulus and that on a deficit failure work buys recovery cost more than growth (Robinson 2024; Grgic 2021), and the cost of a session run to failure lands in the *following* session, not the one that felt great. Also relevant that the app's own RIR calibration check moved from "9% of your 1s preceded a crater" (7/27) to "17%" (7/31) — his own self-report may be drifting as the effort goes up. Resolve on the next lower day: do those three lifts hold their new numbers at 0 RIR, or give reps back? Deliberately not carded while it is winning — calling a breakout session a mistake is the fastest way to make the cards stop being believed.

**W-1 — RESOLVED 8/2 (run 12) → [[CL-11]]. Do not carry as a watch.** The record closed at **9 offered / 6 approved / 0 dismissed / 3 untapped**, and it resolved on the second of its two stated conditions: a second approve-then-override. `sug_2026-07-29_band_hold` asked him to leave the calorie band alone; he approved it 7/29 and on 8/2 applied the app's own auto-pilot tighten at the offered number. That is the same shape as [[CL-10]]'s hold card, one surface over. Both halves of the watch answer at once. Does approval carry information? **Yes** — two cards sat untapped on 8/2 while he was in the app resolving a different proposal, and the auto-pilot's own "Not now" was available and not taken, so taps are choices. Does approval bind? **No** — it records agreement in the moment and expires there. The split that carries the ruling is action-vs-restraint, not sharp-vs-reflexive: 4 of 4 discrete-action cards were performed, 2 of 2 standing-restraint cards were overridden. Filed as CL-11; the design consequence (never card a restraint) is now standing.

**W-2 update (8/2, run 12).** Still unresolved and still untested — no session at all since 7/31, so no upper day has happened. Next upper day decides it: three sets of laterals or four. Note it now sits alongside the press 250 debut on the same session, so one upper day settles both.

**W-3 update (8/2, run 12).** Still unresolved — no lower day since 7/31, so the 0-RIR bill has not been presented. Unchanged in substance, with one addition worth writing down before the test runs: the two nights either side of that session are unlogged (sleep stops 7/30) and Friday carried 14 on the alcohol line, so if the next leg session gives reps back I will not be able to attribute it cleanly to the failure work. Confounded before it is graded. Say so at the time rather than picking whichever cause is convenient.

**W-4 · Does a prediction survive its own instrument going quiet? (opened 8/2, run 12).** Facts: run 11 predicted Friday's 165.0 would unwind over three to four mornings, and named `reads[]` as the test — a field the app genuinely acts on, which was the correct lesson from runs 10 and 11. The field then went empty: no read on 8/1 or 8/2, so the prediction is neither hit nor miss, it is unobserved. Why it is not a ruling: one occurrence, and the cause is almost certainly a weekend plus two late nights shipping app versions rather than anything structural. Why it matters: it is the third variation in three runs of the same underlying error, one level further up each time — run 10 named a metric that read the wrong field, run 11 named thresholds the app does not use, and run 12 named the right field and watched it stop being populated. The candidate rule: **a prediction is only as good as the ritual that feeds its test, so when a card's grade depends on him logging something, the logging is part of the card.** Tonight's #1 is the first card written that way on purpose. Resolve on whether the reads resume and the prediction becomes gradeable, or whether data-dependence turns out to be a recurring blind spot.

**W-2 update (8/3, run 13).** Still untested — no session of any kind since 7/31, the longest training gap in the file (three days, weekend-shaped). Unchanged in substance: the next upper day decides three sets or four, and it is the same session that decides the press 250 debut, so one day answers two watches and a queue item at once.

**W-3 update (8/3, run 13).** Still unpresented, and the confound is now as bad as it can get: between the 0-RIR session and its test sit three unlogged nights, a weekend at maintenance (week mean 2,653), and Friday's 14-drink line. Pre-committed in tonight's brief, in writing, before the test: if the next leg day gives reps back, I will say the cause cannot be cleanly attributed rather than picking whichever story suits the ruling I'd prefer. If the numbers instead hold or climb on three rest days and a maintenance week, that is weak-but-real evidence the failure work was affordable this once — note it, do not generalise it.

**W-4 update (8/3, run 13).** Two more empty mornings: no read on 8/1 or 8/2, so the 165.0 unwind prediction is still unobserved, and the watch's candidate rule ("the logging is part of the card") now has three consecutive silent mornings as its evidence base. The sharpening tonight is that the athlete himself built the same diagnosis into the app — v6.3.2's commit message is literally "hold off a frozen rate, flag stale reads," with a confidence gate holding auto-pilot proposals against the rate CI (±0.89 tonight, wider than the whole band). So the system now agrees with the watch: a stale input should gate decisions, not feed them. Resolution unchanged and now imminent: the carried scale card's test lands the morning this brief publishes (reads gaining 8/3 and 8/4, forecast moving off 163.9). If the reads resume and the unwind grades, close W-4 by folding "name the ritual inside the card" into standing practice; if they do not, the watch hardens toward a ruling that data-dependence is a first-class failure mode to design every card around.

**W-2 — RESOLVED 8/4 (run 14) → [[CL-12]]. Do not carry as a watch.** It resolved on exactly the condition it was opened with: "the next upper day — three sets or four." Monday 8/3 ran **four** (80 × 15,15,13,12, the fourth at 0 RIR), five days after the declaration. So the declared removal did not bind, and both halves of the watch answer at once. Does it bind? No. Does it need the coach? Moot — side delts stay at 8 weekly sets rather than falling to 6, so the ≥10-hard-sets concern (Schoenfeld 2017) is unchanged and this comes off the coach list. One extra finding worth carrying: the *system* never saw the declaration either. Nothing in `exercises.lateral`, `queue` or `plan` recorded it, and the app went on to queue the 85 increment off that very four-set session. A floor declaration has no representation anywhere the app can act on — which is why it was right not to adjust the Volume Ledger read on the strength of it. Filed as CL-12.

**W-3 update (8/4, run 14).** Still unpresented — 8/3 was an upper day, so the next session is the lower day that tests it, and everything now lands at once: the extension 155 debut, the ham-log strike, and the 0-RIR bill. The confound is worse than when it was pre-committed, not better: between the 7/31 session and its retest sit four days, a 5.0 h night (7/31), a 2.8 h seven-night sleep debt, and Friday's 14-drink line. **The pre-commitment stands and is repeated in tonight's brief in writing, before the test: if those lower numbers give reps back, I will say the cause cannot be cleanly attributed rather than pick whichever story flatters the watch.** One piece of indirect evidence arrived in the meantime and it points the friendly way: Monday's upper day ran three days after the 0-RIR leg session and produced the best upper numbers in the file (press 250 debut 8,7,7, rows 180×9,9, laterals 15,15,13,12) with the note "Felt big and strong today." That is not the test — different muscles, different day — but a genuine failure-work hangover would be unlikely to leave an upper day looking like that.

**W-4 — RESOLVED 8/4 (run 14). Closed; the practice is adopted.** The watch asked whether a prediction survives its own instrument going quiet, and it resolved on its first stated condition: the reads resumed (164.0 on 8/3, 162.4 on 8/4) on the morning the card asked for them, and run 12's unwind prediction became gradeable — a partial: the ritual half hit, the *level* half missed (I called mid-to-high 160s opening and a mid-164s landing; it opened at 164.0 and is already at 162.4). The candidate rule earns adoption as standing practice rather than a numbered ruling, because it governs my card design rather than his behaviour: **when a prediction's test depends on him logging something, put the logging inside the card, and name the grading fields in the card text.** Honest limits on the evidence: n=1, and a man may weigh himself for reasons that have nothing to do with a card. Two sharpenings carried forward. First, name the field the app *acts on and updates live* — `state.trend` moved 163.9 → 163.5 while the `forecasts[]` row for 8/4 still prints 163.9, so the field I named lagged the field I meant, which is the fourth run in a row on that theme. Second, and new: **after a gap, the first read back is mid-unwind, not the peak** — the 7/11 precedent I anchored on had a read the very next morning, and three dark mornings meant the giveback happened unobserved.

**W-3 — RESOLVED 8/5 (run 15) → [[CL-13]]. Do not carry as a watch.** It resolved on exactly the condition it was opened with: "the next lower day — do those lifts hold their new numbers, or give reps back?" They held. Hack 160 came back 11,11,10 against 11,10,11 (thirty-two reps either way), abs 100 went 12,12,15 → 13,13,15, calves 320 went 11,9,7,7 → 11,10,7,8, and the hanging raise gave back a single rep. **No bill in reps** — and I am declining to bank that as a win, for the same reason I pre-committed to declining a clean attribution on a loss: the 8/4 session logged `rir: null` on every opener, so it did not reproduce the 0-RIR condition it was testing, and the extension entry is missing outright ([[W-5]]), so one of the four watched lifts contributed nothing. Weak-but-real evidence that the failure work was affordable this once; do not generalise it. **The finding that carried the ruling was not the one the watch was looking for.** The cost landed on rows, in a currency I was not counting: two 0-RIR openers in a row set `holdFlag`, and while it stands the lift cannot progress at all regardless of reps. Filed as CL-13, with the governor half read straight out of source and state rather than inferred, and the rep half flagged as answered-but-not-demonstrated.

**W-5 · Does an amendment reach every instrument, or only the ones somebody remembered? (opened 8/5, run 15).** Facts: on 8/4 the athlete amended four session records — un-skipping ham (10,10) and leg extension (8,8) on 8/4, and marking the two historical phantoms skipped (pronated 7/23, ham 7/31). Three of those four landed everywhere. The fourth did not land anywhere the engine reads: the feed says `RECORD AMENDED — Leg extension UN-SKIPPED on Tue 8/4 · logged 8,8`, while `sessionLog["2026-08-04"]` carries five entries with extension absent and listed in `skipped`, and `exercises.extension.lastMeta` / `ex.last` / `q_ext155` all still read 7/31. Separately, `exercises.ham` still carries `topAt: 120 / topRun: 1` against an 8/4 session of 120 × 10,10 that the app's own rule says should have reset it to 0. Why it is not a ruling yet: n=2, both on the same day, and that day shipped five releases — a system in flight is a bad place to mine a rule. Why it matters: it is the third variation in four runs of predictions dying on data rather than on being wrong, and it is the worst of the three. Runs 12 and 14 were the athlete not logging something; **this is the app losing something he did log**, which no amount of ritual on his side can fix. The candidate rule has two halves. First, *a correction is only as good as the number of caches it reaches* — `deriveLastMeta` and `reconcileLiftCaches` cover `lastMeta` and `ex.last`, and neither the progression counters (`topAt` / `topRun`) nor the merge outcome for a *removed* entry is covered. Second, and for my own practice: **before grading any card against a session, check the session log against the feed, not just against itself.** Resolve on whether the extension entry comes back with its load and whether `ham.topRun` clears — and on whether a future amendment survives without a card asking it to.

**CL-11 — EVIDENCE BASE WEAKENED, 8/6 (run 16). The ruling stands; its stated split does not carry as much as I wrote.** CL-11 was mined on 8/2 off a clean contrast: 4 of 4 **discrete-action** cards were performed, 2 of 2 **standing-restraint** cards were approved and overridden. The action side is no longer clean. `sug_2026-08-04_sleep` asked for one discrete, completable act on one named night — bed by 00:30 — was approved on 8/4, and `sleep.nights` now shows that night at **6.00 h, bed 01:20**. That is the first discrete-action card approved and not performed, and it is the strongest possible counter-example because nothing gated it: no session had to happen, no tool was needed, no app state stood in the way. Three further discrete-action cards (`extlog`, `hamcounter`, `rows_open`) sit approved and unexecuted, but all three are ~24 h old and two wait on a session that has not occurred, so they are not evidence of anything yet. **What survives unchanged:** approval carries information (three `ADJUSTMENT DECLINED` rows on 8/4, a fourth on 8/5), approval does not bind, and a card whose value is continued abstention is structurally weak. **What I am withdrawing:** the implication that a discrete framing *secures* execution. The honest revision is narrower and less flattering to card design — **a card can buy attention and agreement; it cannot buy adherence, and the action/restraint split predicts which cards are worth writing rather than which will be done.** Deliberately not re-ruled and no new number attached, per Article IX: one counter-example against four is a crack, not a refutation. Revisit when `extlog` and `hamcounter` either land or expire on the next lower day.

**W-5 update (8/6, run 16) — unresolved, and the first datapoint is the athlete rather than the app.** The watch asks whether an amendment reaches every instrument. `sug_2026-08-05_extlog` was written to answer it directly: approved on 8/5 (`ANALYST SUGGESTION APPLIED`, `adjustments.adj_msgz5bmf3i5mw`), and twenty-four hours later every field the card named reads exactly as before — `sessionLog["2026-08-04"]` holds five entries with extension in `skipped`, `exercises.extension.lastMeta.d` is still 2026-07-31, `ex.last` is still `[10,9]`, `q_ext155` is still DEBUT. `exercises.ham` likewise still reads `topAt: 120 / topRun: 1`. So the watch has learned nothing about the *merge* mechanism this run, because the re-entry it was going to test never happened. **That is itself worth recording as a limit on the watch's design:** W-5 asks a question about the app's plumbing but its only instrument is an action by the athlete, so a night of no action reads identically to a night of successful repair-then-loss. If the entry comes back and survives, the watch resolves; if it comes back and vanishes again, the watch resolves harder; if he simply never re-enters it, W-5 is unanswerable and should be closed as such rather than carried indefinitely. Deadline set on his next **lower** day — 8/3 was upper and 8/4 lower, so the next session is upper and the condition is two sessions out, not one.

**Correction to the run-11 note on the coach gate (8/6, run 16).** Run 11 recorded that `sug_2026-07-31_press250` shipped at `gate: "coach"` and drew the feed line "training progression noted — coach territory," and read that as evidence the coach gate is "a label, not a brake." The inference was too strong. `sug_2026-08-05_rows_open` shipped at `gate: "approve"` and drew **the same feed line**. So that phrasing attaches to `apply.kind === "progression"`, not to the gate field — it says nothing about whether the coach gate does anything. The underlying observation still stands on its own facts (he tapped a coach-gated card, and `q_press250` sat at DEBIT/un-run), but the feed text is not evidence for it and should not be cited as such again. Standing lesson, and it is the same one run 12 taught: **read the writer before reading the field name.**


**CL-14 — RULED, 8/7 (run 17). A blank field is not a neutral field; it is a lift that cannot move.** Established tonight from source rather than inference. `progressStep` and the lift-card renderer both key on the *opener's* RIR: `note` renders "HELD — one honest opener (RIR ≥1) releases the load", and `buildRirSets` leaves the opener null when `en.rir` is absent, so an unrated opener never appends to `rirHist`. The consequence is asymmetric and easy to miss — a **hot** opener (RIR 0) at least deepens the record and tells the governor something; a **blank** opener tells it nothing at all, so the lift is frozen in place regardless of how good the reps were. Evidence: 8/4 and 8/6 both logged with `rir: null` on every entry; rows put up 180 × 9,8 on 8/6 and `rirHist` stayed `[0,0]` with `holdFlag` true; press put up 250 × 8,8,7 — the best press in the file — and it banked as an attempt with no rating behind it. **Standing consequence for how I write cards:** when an approved card fails, check whether the *field it depends on* was populated before concluding the athlete declined. `rows_open` looked like a refusal and was a logging gap, and those need opposite cards. Related to but distinct from [[CL-11]] — this is not approval-without-adherence, it is adherence-without-instrumentation.

**CL-15 — RULED, 8/7 (run 17). The debt flag is a property of the night, not of the session — and it is a three-night mean, not last night.** Read in source (`deriveLastMeta` → `cleanAtDate`): `DEBT_LAST_H = 6.5` (last night alone), `DEBT_MEAN3_H = 7.0` (three *calendar*-consecutive nights ending last night), and `debt` is carried from cache when the date is unchanged and recomputed otherwise. Worked example on his own file: going into 8/6 the three nights were 6.17 / 6.00 / 7.58 = **6.58 h**, so Thursday's session ran on debt even though the immediately preceding night was his best in a week. **Why this matters for my voice:** it converts nine runs of "protect your sleep" from a health nag into a training consequence with a threshold, a date and a number — the athlete can see exactly which two nights cost him and exactly what clears it. Use the mechanism, not the exhortation. **Recorded quirk, deliberately not exploited:** `cleanAtDate` returns true when fewer than three calendar-consecutive nights exist, so an unlogged night *launders* the flag. The flag therefore **understates** debt when logging is patchy — the opposite of the failure mode I would have assumed, and a reason never to read a clean flag as evidence of a clean week.

**W-6 — OPENED, 8/7 (run 17). My forecasts run pessimistic, in the same direction and roughly the same size as his.** Three graded predictions now miss the same way: `press250` priced the debut at 7 opener / 7,7,6 landing and it opened at 8 and reached 8,8,7 in two sessions; `scale` said "settling near the mid-164s" and it settled ~162.5; run 11's give-back estimate was ungradeable but leaned the same way. His own PROPHET'S SCORECARD (8/5) puts his app's 7-day weight bias at **−0.78 lb, runs pessimistic**. That I share the bias is not a coincidence I can explain yet — plausibly I anchor on the last bad session or the last salted read, the same recency his instrument does. **Watch condition:** grade the next three predictions I make that carry a number, and record the sign of the miss. If all three run pessimistic again, the correction is a standing one — price debuts and give-backs at the optimistic edge of my range rather than the middle — and it belongs in case-law rather than in a watch.

**CL-16 — RULED, 8/8 (run 18). The opener governor: two rated 0s freeze the load; one rated 1 or better releases it. A card that asks for a rating must also name the target.** Read in source (`src/app.jsx` ~1394–1398 and `progressStep` ~833): `ex.rirHist` appends **only** when `en.rir != null`; if the last two entries are both `0`, `ex.holdFlag = true` and the app pushes `<LIFT> — RIR 0 TWICE · opener running hot two sessions straight — load HELD until an honest session lands`; a single opener at `rir >= 1` sets `holdFlag = false` and pushes `<LIFT> — HOLD RELEASED`. While the flag stands, `progressStep` returns `{add: 0, why: "governor hold…"}` — the lift cannot climb however good the reps look. **Worked example, and the reason this is case-law rather than a note:** on 8/7 he followed `sug_2026-08-07_rateopener` exactly — five of six openers rated, first rated session since 8/3 — and rated abs at 0, taking `rirHist` [2,2,0] → [2,0,0] and freezing abs at 100 on the best lower day in the file. **My card caused it.** "Rate set one" was a complete instruction to him and an incomplete one from me: it asked for the number and never named the target, and 0 is both a valid rating and the only rating the governor punishes. **Standing rule:** any card that asks him to log an effort score must state the effort score to log. Ask for "open at 1–2 RIR and log it," never for "rate the opener." **Corroborating same-session evidence (n=1, held lightly):** hack opened at 2 → 32 reps to 36; extension opened at 1 → 155 debut 8,9; abs opened at 0 → 41 reps to 40 and held. Direction agrees with the 1–3 RIR literature (Schoenfeld 2017; Robinson 2024; Grgic 2021), which is the prior; his own data has one session on it.

**CL-17 — RULED, 8/8 (run 18). Name a test the *next session* can actually reach.** Amends the run-11 rule ("name the test on a field the app itself acts on"). `sug_2026-08-07_rateopener` named `rows.holdFlag` as its grading field — a correct, app-acted field on an **upper**-day lift — and the next session was a **lower** day, so the card is ungradeable on its own terms four days later even though the behaviour it asked for was performed. This is the fourth consecutive run where the reasoning was sound and the *measurement* was not: run 11 graded against a threshold the app does not use, run 12 named a field that stopped being populated, run 15 shipped without a logging ask, run 17/18 named an out-of-rotation lift. **Standing rule:** before shipping, check the named field against the training split and the logging cadence — if the next session cannot populate it, either name a field that session *will* populate or state the deadline explicitly in the card ("grade on the next upper day").

**W-5 — CLOSED, 8/8 (run 18), superseded rather than answered.** The 8/4 leg-extension set eaten by the un-skip/re-skip `mergeState` race is unrecoverable: 8/4 still carries extension in `skipped`, the entry never came back, and the load is gone for good. It stopped mattering on 8/7, when he simply ran the lift at 155 and put 8,9 on the record — resolving the 155 gate the lost set would have resolved. Filed as: *a lost record can be superseded by the next honest session; do not spend cards chasing a set that the next session will overwrite anyway.* The underlying `mergeState` entry-count scoring rule is unchanged and still capable of eating a set, so the mechanism stays on the watch list even though this instance is closed.

**W-6 — NARROWED, 8/8 (run 18). The pessimism bias is a *weight-forecast* bias, not a general one.** `sug_2026-08-01_ext155` predicted extension 155 would "open near 9"; it opened at **8** — a miss in the **optimistic** direction, the first counterexample. So the three cases that opened W-6 (`press250`, `scale`, run 11's give-back) are all weight/scale or load-give-back forecasts, and his own PROPHET'S SCORECARD bias (−0.78 lb) is likewise a 7-day *weight* error. Had I applied run 17's proposed correction ("stop pricing debuts as if the load will hurt more than it does") to this debut, the prediction would have been *worse*. **Watch condition, revised:** grade the next three predictions that carry a **weight or trend** number and record the sign; keep debut/rep predictions in a separate tally. Do not issue a standing optimism correction across both classes off a common cause I cannot name.


**CL-11 — STRENGTHENED, 8/9 (run 19). The strongest instance yet: approval followed within 24 hours by the opposite action, at five times the scale.** `sug_2026-08-08_volstack` shipped at `gate: "coach"` asking him to stagger **two** upper-day set additions across two weeks. He approved it on 8/8 (`adj_msk995p60bv52`, feed row `ANALYST SUGGESTION APPLIED`). On 8/9 the feed records **five further** VOLUME +1 rows — quads via hack (now 4 sets), abs (4), biceps via curls (4), triceps (4), back via rows (3) — taking the three-day total to eight muscle groups. This is not adherence decay and it is not a logging gap ([[CL-14]]): the approval and the contradiction are both discrete, both timestamped, and both his. **What it confirms:** CL-11's core holds exactly as ruled — approval is a moment, not a commitment, and the tap carries information about agreement rather than about what will happen next. **What it adds:** the run-16 revision ("a card can buy attention and agreement; it cannot buy adherence") now has an instance where the card bought agreement and the athlete then moved *harder in the opposite direction* than the card was written to slow. A restraint card does not merely fail to bind — it can be approved and then overrun. **Standing consequence for card design:** where the restraint concerns a *count* the app tracks and can undo (`exUndo` is on file for every one of these), write the card as a discrete reversal of specific named items rather than as a standing "hold." Tonight's #1 does exactly that: undo these five, keep those three. If a discretely-framed reversal also fails, the action/restraint split ([[CL-11]]) is finished as a design heuristic and should be retired rather than re-narrowed a third time.

**W-7 · The undeclared recomp. Does a phase change get named, or does the app keep running cut machinery around a block that stopped being a cut? (opened 8/9, run 19).** Facts: last four logged days 2,800 / 2,800 / 3,000 / 2,900, mean **2,875**, against a measured maintenance of ~2,850–3,050 (engine 2,911; `learned.tdee` 2,854 with CI 2,670–3,039; straight arithmetic off the fortnight ≈ 3,040) — a deficit of approximately zero. Weekly intake means have walked 1,903 (wk 7/13) → 2,091 → 2,546 → 2,567, and mean steps have fallen 21,714 (wk 6/29) → 13,500 (wk 8/3), so both sides of the equation moved against the deficit — roughly a 740 kcal/day swing versus late June. Simultaneously protein sits at 190–200 with eight weeks at 100%, volume is up across eight muscle groups, `plan.apMode` reads `recomp`, the app's own `pivot` proposal ("is the cut done?") was **withdrawn by the app** on 8/6, and 8/7 produced the best lower day in the file. Meanwhile `phase` still reads `EASE 1`, `phaseLog` is empty, `rate.band` is still `[1, 1.4]` **in pounds** (the `rateunit` proposal's exact objection, stood down on 8/3), and the trend gauge is still waiting for a loss this week's food will not produce. **Why it is a watch and not a ruling:** n=4 days, and a run of high days after a low fortnight is a pattern this loop has misread before — run 6 killed an intake card for precisely that reason and was right to. **Why it matters:** every instrument in the app is calibrated for a deficit, so if the block has genuinely changed, the rate reads as a stall, the auto-pilot reads as a reason to tighten, and the volume additions get graded against a recovery model built for a cut. **Resolve on:** whether `phase` / `phaseLog` move, or a coach note lands, or intake returns inside the band within ~7 days. **Prediction entered for the tally:** if intake stays where it is, the measured rate falls under **0.7 lb/wk by ~8/16** and under the band's own 0.8 floor. Deliberately not carded — Article IX puts a phase pivot with the coach, never on a one-tap.

**Working constant, not a ruling — the water number now forecasts (8/9, run 19).** `waterSensitivityLb` has been carried for weeks as a caveat ("that's water, not fat"). Tonight it was used as a *forecast* for the first time and landed: predicted +1.11 lb the morning after a salted day, observed 164.5 against a 163.37 soft trend — a gap of **+1.13**. n=5, same direction every time, one instance of point-accuracy. Held as a working constant for pricing the morning after a high-sodium or alcohol day; not ruled, because n=5 with one accurate point is a coincidence away from being nothing. Relevant to [[W-6]] only insofar as it is a weight prediction that did *not* run pessimistic.

**CL-11 — RUN-19 STRENGTHENING WITHDRAWN, 8/10 (run 20). The "approved and then overrun" instance was not adherence failure; the action had no mechanism.** Run 19 filed `sug_2026-08-09_volstack5` as CL-11's strongest instance — approved 8/9, contradicted at five times the scale. Tonight I checked the affordance before re-filing it and the reading collapses. The card's own text said "each has a one-tap undo already on file." `adjustments` carries `exUndo` for exactly three items — `volpush_delts_rear` (`prev: 3`), `volpush_chest` (`prev: 3`), `volpush_hams` (`prev: 2`), all dated 8/7. The five 8/9 VOLUME +1 rows (quads/hack, abs, biceps/curls, triceps, back/rows) have **no `adjustments` entry at all**, therefore no `exUndo`, therefore no one-tap. He could not have performed the ask as written. **What this costs CL-11:** the run-19 strengthening is withdrawn in full, and with it the claim that a *discretely-framed reversal* also fails — that experiment never ran. CL-11's core is untouched and still rests on its original evidence (approval carries information; approval does not bind; a card whose value is continued abstention is structurally weak). **What it adds, and this is the sharper rule:** [[CL-14]] said *when an approved card fails, check whether the field it depends on was populated before concluding the athlete declined*. Tonight extends it one step earlier — **check that the affordance the card names actually exists before shipping, not after grading.** I asserted a button from a pattern (three pushes had `exUndo`, so all pushes must) rather than from the record. Same failure family as run 11's "name a field the app acts on" and run 18's "name a test the next session can reach", and now the fifth consecutive variation of *the reasoning was sound and the instrument was not* — except this time the broken instrument was the ask itself. **Partial counter-evidence he supplied unprompted:** `RECORD CORRECTED — unattested sets struck from Sunday's re-log` ("I didn't do the 3rd set of arms") removed the tail set from tricep, curl and rows. The counts stand; three of the sets were declined and declared. Read as: he acted on the substance through the mechanism that did exist.

**CL-15 — CONFIRMED END TO END, 8/10 (run 20). The laundering quirk is not theoretical; it reached the weekly grade.** CL-15 was ruled 8/7 from source (`cleanAtDate`: `DEBT_LAST_H 6.5`, `DEBT_MEAN3_H 7.0`, three calendar-consecutive nights, and **true** when fewer than three nights exist). Observed tonight on his own file with n=3 dark nights: `sleep.nights` stops at 8/6; 8/7, 8/8 and 8/9 are absent; every entry in the 8/9 session carries `lastMeta.debt: false`; and the 8/10 `WEEK IN REVIEW · WK 9` reports **"sleep 2/3 clean"** — a weekly grade computed off three logged nights in seven, presented alongside "the boring, winning kind of week." **What hardens:** the quirk does not merely mislead the debt flag, it propagates into the weekly summary the athlete actually reads, so missing sleep data makes the *app* sound more confident, not less. **Standing consequence for my voice:** never cite a clean debt flag, or the app's weekly sleep line, without first checking `sleep.nights` coverage for the window — and when coverage is short, say the flag is empty rather than clean. Also the reason tonight's #1 asks for *estimates flagged as estimates* rather than good nights: on this mechanism a badly-logged night is strictly more informative than a missing one.

**CL-18 — RULED, 8/10 (run 20). A set added to a lift changes what that lift is graded against, and the change is silent.** Facts: press 250 × 8,8,7 on 8/6 drew `PRESS — TARGET MET`. On 8/7 `volpush_chest` took press 3 → 4 sets (OWNER'S CALL text: *"the count changes next session; the effort ladder re-keys itself, and the trend window restarts"*). On 8/9 press ran 250 × **8,8,7,4** with the opener rated 1 on a (laundered) clean day, and the feed carries **no press row of any kind**, while rows, tricep, sulek and lateral each drew one from the same session. Source reads `tgtMet = en.tgt && en.tgt.every((t,i) => (r[i] ?? 0) >= t)` (src/app.jsx ~1483) with the push at ~1517, so a fourth target slot that the fourth set misses fails the whole conjunction. **Why this is ruled narrowly and the card shipped at low confidence:** `en.tgt` is **not persisted** in `sessionLog` entries — undefined on both 8/6 and 8/9 — so the target array cannot be reconstructed from state and the mechanism is inferred, not verified. What is *ruled* is the observable, not the mechanism: **the best press in the file scored nothing, the only variable that changed was the set count, and a volume push therefore carries a hidden scoring cost on the lift it lands on.** Withdrawal clause entered with the card: if the next upper day produces a press row with four sets logged and no load change, the inference is wrong and CL-18 reverts to an observation. **General form worth carrying regardless of how it resolves:** a volume change is never only a volume change — it re-keys the instrument that judges the lift, so a push and an ownership attempt should not land in the same session. That half was predicted on 8/9 and is confirmed.

**Working constant, second confirmation (8/10, run 20).** `waterSensitivityLb` predicted +1.11 lb the morning after Saturday's high-sodium day; 163.37 + 1.11 = 164.48 against an observed **164.5**. n=6, same direction every time, two consecutive point-accurate uses. Still held as a working constant rather than ruled, but it is now the single most reliable number I carry, and it is a weight forecast that has not run pessimistic — which continues to narrow [[W-6]].

**W-7 update (8/10, run 20) — unresolved, and both sides of the question hardened.** The watch asks whether the undeclared recomp gets named. Neither resolution condition fired: `phase` still reads `EASE 1`, `plan.phaseLog` is still `[]`, `rate.band` is still `[1, 1.4]` in pounds, no coach note landed, and intake did not return inside the band — the last four logged days (8/5–8/8) are 2,800 / 2,800 / 3,000 / 2,900, mean **2,875**, with 8/9 unlogged. What *did* move is the maintenance estimate, in the direction that makes the read stronger: `learned.tdee` came up to **2,886 (2,701–3,070)** on 8/10 from 2,854, against the engine's **2,911** — two independent fits now 25 kcal apart, where a fortnight ago I was calling the rise an estimate-day artifact. Meanwhile the app's own `WEEK IN REVIEW` reports ~1.15 lb/wk against a band of 0.98–1.14 while the engine reads 0.97 — the two disagree on window length and *both* are extrapolating from a clean read four days old, so the watch cannot be resolved on rate evidence until the morning reads resume. **Deadline held rather than extended:** original resolve-by was ~7 days from 8/9. Restated per Article IX as coach territory, brief only, never carded.

**CL-19 — RULED, 8/11 (run 21). He is not skipping the scale. He is weighing at the wrong end of the day, and the ledger charges him twice for it.** Facts: `reads` 8/8 = 163.1 and 8/10 = 164, **both carrying `offWindow: true`** and the note *"evening read — set aside"*; 8/9 = 164.5 is in-window but salt-flagged. On **8/10 the same day carries both** a `MORNING READ MISSED` feed row ("the trend carries. Today it cost 0.02 lb/wk of rate precision") **and** a discarded evening read. So on the day I would have scored him as non-compliant, he stepped on the scale and received a penalty plus nothing. Last clean morning read: **8/6 (162.8)** — five days of every rate figure extrapolating off one morning, which is also why his app says 1.15 lb/wk and the engine says 0.80. **The ruling:** a missing read and a discarded read look identical in the trend and are opposite in what they mean about the athlete, and I have no excuse for conflating them — the `offWindow` flag was in the file the whole time. **Standing consequence:** before shipping any logging card, check whether the entry is *absent* or *present-and-rejected*, and card the actual defect. Three runs of compliance cards were aimed at a timing problem. Supersedes the framing (not the substance) of the 8/10 "log the three nights" headline.

**CL-20 — RULED, 8/11 (run 21). The app banks a load on one sighting when the gain clears two standard errors of that lift's own spread; the two-sighting rule is not unconditional.** Facts: `sug_2026-08-05_hamcounter` argued that clearing `ham.topRun` would make 125 "wait for two real sightings, the way the rule intends." `topRun` did clear (now 0) — and 125 banked on **one** sighting anyway, on 8/10: *"120×12,12,11 — 4 reps clear of last time, and two standard errors of your own measured spread is 2.7. That is outside the noise, so it banks on one sighting."* Compare the same day's `ABS — TOP OF WINDOW, PROVISIONAL`: 14,13,14 was only 1 rep up against a ±1.28-rep spread, so it did **not** bank and queued a repeat instead. Both behaviours are the same rule with the effect size as the discriminator. **The ruling:** the two-sighting requirement is a noise-floor test, not a counting test — a gain large relative to that lift's own measured spread earns immediately. **Consequence:** never argue "it needs a second sighting" without first checking the lift's spread and the size of the gain. My model of the earn logic was wrong for at least three runs and the outcome only looked right by accident.

**CL-21 — RULED, 8/11 (run 21). A card whose `apply.kind` is `note` or `progression` can never carry an Approve button — this is structural at the render, not a bug.** Facts: `src/app.jsx` computes `does` as non-null only when `apply.kind` is `protein`, `sleep` or `dietbreak` **and** `conf === "high"`; `approve()` calls `applySuggestion` only `if (this.does)`, otherwise `noteSuggestion`, and `approveLabel()` returns "Noted". `basis` likewise renders "measured" only at high confidence and "speculation" otherwise. This is R20a — the Voice Law — enforced in code: the apply control *derives from* the "Approving does:" line. **The ruling:** every `note` and `progression` card I write is an observation he reads and acts on by hand, and I must write it that way. **This retroactively explains the volunwind failure** (8/10 feed, verbatim: *"The card you approved on 8/09 said this and did nothing (its apply was a note)"*) — the missing undo affordance was the symptom; the real defect was that I wrote an instruction expecting a tap to enact it. Standing rule, replacing the 8/10 formulation: **never phrase a card as though approving performs it unless `apply.kind` is protein/sleep/dietbreak at high confidence.** State the act plainly and let him do it.

**W-6 update (8/11, run 21) — the weight tally cannot be scored, and that is itself the finding.** The 162.8–163.4 clean-read forecast has now gone five days without a gradeable read (see [[CL-19]]). The +1.11 lb water constant was not exercised this run. The sub-0.7 lb/wk rate forecast is tracking: 0.97 → **0.80** in one run against a ~8/16 deadline, with a plainly dietary cause (four logged days at mean 2,875 against fits of 2,810 and 2,886). Deadline held, not extended.

**W-7 update (8/11, run 21) — unresolved, and the rate side moved decisively toward the recomp read.** `phase` still `EASE 1`, `plan.phaseLog` still `[]`, `rate.band` still `[1, 1.4]` in pounds — no resolution condition fired. But the measured rate has now fallen to **0.80 lb/wk, exactly `rate.floor`**, and 0.49%/wk sits *below* the 0.5–0.7% band that protects lean (Garthe 2011) — the first run where the risk is losing too slowly rather than too fast. Meanwhile the loads did this in one session: hack 180 × 9,9,10, extension 155 × 9,9, abs topping window at 100, calves 320 met, ham earning 125. **Weight flat, five lifts advancing, protein at target every week — that is the empirical signature of a recomp, arriving from the training side while the declaration stays absent.** Still coach territory under Article IX, still brief-only, still never carded. Deadline held at ~7 days from 8/9.


**CL-22 — RULED, 8/12 (run 22). A record the merge cannot key does not exist. Read the merge dictionary before believing a feed row.** Facts: the 8/11 feed carries `TRIAL STARTED — THE FAILURE A/B — TRICEP CAPPED vs SULEK ALL-OUT` with the text *"designed by your analyst for a pattern in YOUR data, consented by you"*, and `state.trials` is `[]`. Mechanism read end to end in source, not inferred. `approveAgentProposal` (src/app.jsx ~8877–8880) writes `const rec = ap.custom ? { custom: ap.custom, started: tISO } : { tplId: ap.tplId, started: tISO }` then `s.trials = [...(s.trials || []), rec]` and unshifts the feed row in the same breath — so both are written together and only one survives. `MERGE_ARR.trials = (t) => t && (t.id || t.d)` (~10880); the trial record carries neither field. `_unionBy`'s adder is `const k = keyOf(x); if (k == null) return;` (~10706) — **an entry whose key comes back null is silently dropped, on every merge, in both directions.** The feed survives because `MERGE_MULTI.feed` keys on the whole JSON and every feed line carries `d`. **The ruling:** in this app a record's survival is a property of the merge dictionary, not of the code that wrote it, and a feed row is evidence that a handler *ran*, never that its record *persisted*. **Scope, and it is wider than the one trial:** the shape has no `id` or `d` in either branch, so *template* trials fail identically to custom ones; the decline path (~8891) writes `{ custom, declined: true }` / `{ tplId, declined: true }` with the same defect, so the comment above it — *"the decline is RECORDED in s.trials, so a pre-filed trial card never refiles"* — is false across a sync; and the re-offer guard at ~8490 (`seenAB`) reads the same empty list, so the proposal can come back and re-tapping writes the identical unkeyable record. **Live consequences:** `abU1` (~896) and `abT9` (~5524) look the trial up to decide which arm rides today's protocol, so with `trials` empty the A/B assigns nothing and collects nothing while the athlete pays the protocol's cost. **Why this is ruled at high confidence where [[CL-18]] was shipped at low:** CL-18's `en.tgt` is not persisted so its mechanism could not be reconstructed from state; here both halves are visible — the writer in source and the absence in the file. **Standing consequence for my practice, and it generalises three earlier findings ([[CL-6]] the un-skip phantoms, [[W-5]] the eaten extension set, [[CL-18]] the silent re-key):** before treating a feed row as a fact about state, check that the record behind it has a merge key. All four are the same failure at different depths — *the app said it happened and the ledger does not carry it* — and this is the first one where the reason is a two-line dictionary entry rather than a race. **Withdrawal clause entered with the card:** if the 8/13 upper day prescribes an arm for tricep or sulek, the reading is wrong and CL-22 reverts to an observation.

**W-6 — WEAKENED, 8/12 (run 22). The weight tally has its first in-class counterexample, and it runs optimistic.** W-6 was opened 8/7 on three pessimistic misses and narrowed 8/8 to *weight and trend forecasts only*. `sug_2026-08-11_morningread` priced the returning clean read at **162.8–163.6**; it came in at **164.2** — a miss of 0.6–1.4 lb in the **optimistic** direction, in exactly the class W-6 reserves for itself. Two confounds recorded before leaning on it: the read follows a 19,500-step day with sodium unlogged, and a single read after five dark mornings is mid-unwind rather than a level (the run-14 lesson). Against it, the two point-accurate uses of the +1.11 lb water constant (8/9, 8/10) were also weight forecasts that did not run pessimistic. **Standing of the watch:** the original three cases stand, but the claim "my weight forecasts run pessimistic" is now 3 pessimistic / 1 optimistic / 2 accurate within its own class, which is a scatter rather than a bias. **Revised watch condition:** keep grading weight forecasts and recording the sign, but stop treating a pessimistic lean as the expected error — and do not apply a standing optimism correction, which run 18 already showed would have made a prediction worse. Resolve toward closure-as-noise unless the next three weight calls miss the same way.

**W-7 update (8/12, run 22) — no declaration, and the behaviour corrected itself instead.** Neither stated resolution condition fired on the declaration side: `phase` still reads `EASE 1`, `plan.phaseLog` is still `[]`, `rate.band` is still `[1, 1.4]` in pounds, no coach note. But the third condition — *intake returns inside the band within ~7 days* — has substantially fired: after four days at a mean of 2,875 (8/5–8/8, a deficit of approximately zero), the last three logged days read **2,350 / 2,231 / 2,279** on **14,700 / 19,500 / 17,500** steps against a 13,500 mean the week before. Both sides of the equation moved back, and they moved back *before* his app's 8/11 `RATE FLOOR TRIPPED` row asked for it. So the undeclared recomp looks less like a phase he entered and more like a fortnight he drifted through and pulled out of. **What that costs the watch:** its sharpest form — "the app is running cut machinery around a block that stopped being a cut" — loses its premise if the block is a cut again by Friday. **What survives regardless, and it is the part worth keeping:** the instruments disagreed loudly while it happened (three different rate figures on one day: forecasts 1.15 stale since 8/10, week-in-review 0.4 and 0.3, engine 0.62), and none of them could see the correction for the same reason — every one of them is anchored to a trend built before the clean reads came back. Deadline held at ~8/16 as originally set; grade it then on whether the rate recovers without a declaration ever being needed. Still coach territory under Article IX, still brief-only, still never carded.


**CL-23 — RULED, 8/13 (run 23). A rate computed over a fixed window cannot report a change younger than the window; treat a rate that disagrees with present intake as an instrument lag until the window turns over, and say which it is.** Facts: 8/9–8/12 ran 2,350 / 2,231 / 2,279 / 2,249 (mean **2,277**) on 14,700 / 19,500 / 17,500 / 19,000 steps (mean **17,675**, against 13,500 the prior week). Against maintenance fits of 2,660 (engine) and 2,752 (`learned.tdee`) that is a **380–475 kcal/day deficit ≈ 0.8–0.95 lb/wk**. The measured rate over the same days went the **opposite** way — 0.62 → **0.36 lb/wk (0.22%/wk)** — because the 14-day fit still carries the 8/5–8/8 maintenance stretch (2,800 / 2,800 / 3,000 / 2,900) at full weight: four deficit days averaged against ten that were not. **The ruling:** when present intake and measured rate disagree, compute how much of the window predates the change *before* reaching for a physiological explanation, and state the arithmetic so he can check it. **Consequence for practice:** the same lag explains, in one sentence, why every gauge in this app was wrong in the same direction during run 22's *RATE FLOOR TRIPPED* episode — the app's forecasts row, its week-in-review figures and my engine were all anchored to a trend built before the correction. That was diagnosed there as four instruments disagreeing; it is better understood as **one shared property of windowed estimators**, and it will recur every time he changes behaviour sharply. **Guard against the obvious abuse:** this ruling makes it easy to explain away any inconvenient stall as "the window." So it ships with a falsifier attached to a date — by **8/17** the window is mostly deficit days, and if the rate is still under 0.6 lb/wk then, the lag reading is wrong and the food log is the thing to doubt. Related: the maintenance fit inherits the same contamination in the other direction — `learned.tdee` fell **2,886 → 2,793 → 2,752** in two days purely because the weight rose, so a windowed rate and a weight-derived TDEE can be wrong *together* and confirm each other while doing it.

**CL-24 — RULED, 8/13 (run 23). A measured personal coefficient is worth more than the textbook it came from, and a blank field is what destroys it.** Facts: his water coefficient reads **+1.11 lb next-day after a high-sodium or alcohol day (n=4–5)** and has now priced two reads to the ounce — 8/9 predicted 164.48 against an actual 164.5, and again on 8/10. On 8/12 the scale rose 164.2 → **164.7** on a day he ate ~2,277 and walked 19,000 steps, and the sodium column for that day is **blank**, as it is for 8/9 and 8/10. With the field filled, that read either classifies as water and gets set aside under Article III's soft-damping, or it does not and becomes a real finding. Blank, it can be neither, and an unclassifiable stall is precisely the input his app's own `RATE FLOOR` rule reads before asking for a calorie trim. **The ruling:** when a personal coefficient exists and is calibrated, the highest-leverage ask is almost never a behaviour change — it is the two-second log entry that lets the coefficient run. **Consequence:** rank an instrumentation card **above** a training card when the missing field gates the interpretation of the week rather than of one lift, which is why `sodiumlog` outranked `press4th` and `trialghost` tonight despite both of those expiring within hours. **General form, and it is the fourth member of a family** ([[CL-19]] the evening reads, [[CL-15]] the laundered debt flag, [[CL-22]] the unkeyable trial): *the analysis is not limited by what the body did, it is limited by which fields survived* — and three of the four were fixable by him in under a minute.

**W-6 update (8/13, run 23) — no new gradeable weight call, but the open rate forecast is running optimistic again.** Run 22's *rate back over 0.8 lb/wk by 8/17* is at **0.36** and falling, having been 0.62 when written. Not yet due, so not scored. Recorded here because the sign matters to this watch: it was chosen deliberately as a *faster*-loss call so it could falsify the pessimism story, and it is instead lining up to be the **second in-class optimistic miss**, after the 162.8–163.6 call that came in at 164.2. If it lands wrong on 8/17, W-6's tally becomes 3 pessimistic / 2 optimistic / 2 accurate, which is a scatter with no usable bias and should be **closed as noise** rather than narrowed a fourth time. Deadline held at 8/17. New call entered alongside it, deliberately two-sided and cheap to score: if 8/12 is logged high-sodium, the trend reads **≤163.6 by Saturday 8/15**.

**W-7 update (8/13, run 23) — the app named it first, and that changes the watch's status without resolving it.** The 8/13 feed opens with `LAB LIVE — THE REGIME DETECTOR: lifts rising and fat still falling — both terms improving at once`, i.e. **his own app called the recomp** on the same morning my engine reads `plateau` on the same numbers. Neither declaration condition has fired: `phase` is still `EASE 1`, `plan.phaseLog` is still `[]`, `rate.band` is still `[1, 1.4]` in pounds. But the empirical side is now unambiguous from the training direction — hack 180 × 9,9,10, extension 155 × 9,9, abs 100 and rows 180 both off their holds, ham 120 × 12,12,11 earning 125, press moving 250, all on a trend that has moved 163.6 → 163.99 in a fortnight. **What this costs the watch:** its sharpest form was *the app is running cut machinery around a block that stopped being a cut*, and the app has now said the second half of that out loud, so the remaining gap is purely between the app's **read** and the app's **plan**, not between the app and me. **What it does not resolve:** the third condition from run 22 — intake back inside the band — did fire, and yet the rate fell anyway, so "is this a recomp or a lagging window?" ([[CL-23]]) is genuinely undecided and the two readings predict opposite things on 8/17. That is the cleanest test this watch has ever had and it arrives on the existing deadline. Grade it then. Still coach territory under Article IX, still brief-only, still never carded — and the honest reason to keep it uncarded is that a DEXA answers it in one morning and the scale cannot answer it in a month.

**CL-22 — OVERTURNED, 8/14 (run 24). The empty `trials` array was not a merge-key defect. He retired the trial himself.** Facts: [[CL-22]] was ruled on 8/12 off the observation that `trials` stayed `[]` after a `TRIAL STARTED — THE FAILURE A/B — TRICEP CAPPED vs SULEK ALL-OUT` feed row on 8/11, and it read that as an entry the sync could not key and was silently dropping — filed as the fourth member of the [[CL-24]] "which fields survived" family. The 8/13 feed carries the actual explanation: `FAILURE EXPERIMENT RETIRED — Your call, before it began. The research default stands: train 1–2 reps shy... No session ever ran under the experiment, so nothing is lost.` The array was empty because the trial was cancelled, not because the write failed. **The ruling, and it is against me:** I had a feed row announcing a start, an array that stayed empty, and I reached for an instrument defect when the ordinary reading — *the athlete changed his mind* — was available and cheaper. The prior ruling's own withdrawal clause is hereby exercised. **Consequence for practice:** before diagnosing a persistence bug from an empty collection, read forward in `feed` for a cancellation, a retirement or a recall. Three of the last four instrument findings ([[CL-19]] evening reads, [[CL-15]] laundered debt, [[CL-24]] blank sodium) were real; this one was me pattern-matching onto the family I had just built, which is the specific failure mode of a memory that compounds — **a fresh ruling makes the next observation look like more of itself.** [[CL-24]]'s general form survives intact; this was never one of its members.

**CL-25 — RULED, 8/14 (run 24). A gate checked when a proposal is written is not a gate on the day it is applied.** Facts: proposal `volpush_delts_side_2026-08-10` was drafted **8/10** and its stated authority is *"regime FREE confirmed a week apart, lifts not falling while fat clearly falls, recovery GREEN, **the 3-night sleep mean clean**, and no other volume move this week."* It applied on **8/13** (`adj_msrldyrh0gzbl`, `lateral` 4 → 5 sets, 8 → 10 weekly). The three nights behind 8/13 are 7.67 (8/11), **5.33** (8/12, bed 02:30) and 8/13 unlogged — a seven-night debt that went 0 → 1.7 h on that single night. The sleep condition that authorised the set **would not have passed on the day the set arrived**, and the proposal text still displayed it in the present tense as if it had. **The ruling:** a recovery gate is a statement about a moment, and a proposal that survives three days carries the gate's *wording* without its *warrant*. Where a proposal's authority is a recovery read, the read must be re-taken at apply time or the claim must be re-dated in the copy. **Consequence for practice:** point at the input, never the output ([[consent-hygiene]]) — this is a note for whoever owns the volume ledger, not a card for Joe, and one set of laterals is not worth an undo. **Why it is worth a ruling anyway:** the same drafting-to-apply gap will recur on gates that matter more than three minutes of side delts, and this is the first case where the stale side of a stale gate is *recovery* rather than a stale prescription string. Related to the `targets.progression` field that has now displayed an already-enacted instruction for four consecutive runs — same defect class, opposite direction: **one shows an old instruction as current, the other shows an old condition as current.**

**W-6 update (8/14, run 24) — the optimistic-miss watch gains a void, not a hit, and I am declining to score it as one.** Run 23's two-sided call was *if 8/12 is logged high-sodium, the trend gives back 0.5–1.1 lb inside two mornings and reads ≤163.6 by Saturday 8/15.* The scale did exactly the predicted thing — 164.7 → **163.3**, a 1.4 lb overnight fall, soft trend 163.99 → **163.81**, app trend **163.7** — and the **antecedent never happened**: 8/12's sodium is still blank, as is every other field for 8/13. A conditional whose condition was never met is **void**, and the temptation to bank the consequent as a win is exactly the bias this watch exists to catch. Scored void. Run 22's *rate back over 0.8 lb/wk by 8/17* now reads **0.16 lb/wk (0.1%/wk)**, down from 0.36 and 0.62; deadline held at 8/17, and it is lining up hard as the second in-class optimistic miss. New call entered, chosen because it is scoreable **tomorrow** off a field he already fills reliably: the next clean morning read lands **between 163.0 and 164.0**; at or above 164.5 the water story for Wednesday's rise is wrong and three nights of briefs were built on it.

**W-7 update (8/14, run 24) — no movement, and the 8/17 test is now at risk of being unanswerable.** No session since 8/10, so the training side of the recomp read gained nothing this run; `phase` still `EASE 1`, `plan.phaseLog` still `[]`, `rate.band` still `[1, 1.4]`, `q_dexa` still `UNBOOKED`. The material change is to the **test**, not the watch: [[CL-23]] shipped with a falsifier dated 8/17 — *if the rate is still under 0.6 with a window mostly full of deficit days, the deficit is not what I think and the food log is wrong* — and **8/13 now sits inside that window with no food entry at all**. A window with a hole in it cannot separate "the body did not respond" from "the intake was never recorded," which means the cleanest test this watch has ever had can be spoiled by a blank row rather than answered by a number. That is the whole reason `fillthursday` outranked a same-day training card tonight, and it is [[CL-24]] operating one level up: *the analysis is not limited by what the body did, it is limited by which fields survived* — this time the field that decides a forecast rather than a morning.


**CL-26 — RULED, 8/15 (run 25). A measured coefficient licenses a prediction from a *logged* antecedent. It never licenses inferring the antecedent from the size of the effect.** Facts: the water constant reads **+1.1 lb the morning after a high-sodium or alcohol day**, calibrated at n=4 on days where the `sodium` field is **filled in**, and it has priced three mornings correctly (8/9 predicted 164.48 / actual 164.5; again 8/10; and the 8/13 fall). On 8/13 the read fell 164.7 → **163.3**, a −1.4 lb move of exactly coefficient size, and **8/12's sodium is blank** — as is 8/13's. Run 24 read the magnitude, recognised it, and concluded backwards to a salty Wednesday that was never recorded, then built a brief and a forecast on it. **8/14 came in at 164.7**, rebounding the full 1.4 lb, and the two-sided clause shipped with that forecast (*at or above 164.5 the water story is wrong*) fired. **The ruling:** this is affirming the consequent with a number attached, and the number makes it feel rigorous. The plain reading was available the entire time and cheaper — reads went 164.2 / 164.7 / 163.3 / 164.7 against a **±1.5 lb/day noise floor**, so both the fall and the rebound sit inside noise and neither required an explanation at all. **Standing consequence:** before applying any personal coefficient to a read, check that its antecedent field is *populated for that day*. If the field is blank, the read is unclassified — say "unclassified," soft-damp it under Article III, and do not narrate it. Article III requires never going dark; it does not require inventing a cause. **Relationship to [[CL-22]]:** same family (reaching past the ordinary explanation) but a distinct mechanism — CL-22 was preferring an exotic cause over a mundane one, this is running a valid conditional in the wrong direction. **Relationship to [[CL-24]]:** CL-24 ruled that a blank field destroys a calibrated coefficient. That was right, and this is its sharper corollary — the blank field does not merely cost the coefficient's use, it *invites* the coefficient's misuse, because the effect is visible and only the cause is missing. **Narrowing of [[CL-24]] in the same breath, and it is against that ruling's ranking rule:** CL-24 put an instrumentation card above two training cards on the theory that the missing field "gates the interpretation of the week." Two runs on, the week's interpretation turned on the **trend**, which is a smooth over many reads and which single-day water noise washes out of. Sodium gates the interpretation of a *morning*, not a *week*. Rank it accordingly.

**CL-23 — MECHANISM STANDS, THIS WEEK'S USE WITHDRAWN, 8/15 (run 25).** The ruling — *a rate computed over a fixed window cannot report a change younger than the window* — is sound and keeps its general force. What is withdrawn is its use as the explanation for **this** stall. The falsifier was dated 8/17 and read *if the rate is still under 0.6 lb/wk with a window mostly full of deficit days, the deficit is not what I think and the food log is wrong somewhere*. On 8/15 the rate is **−0.06 lb/wk (−0.04%/wk)**, having gone 0.62 → 0.36 → 0.16 → −0.06 over four runs, and `dailyLogs` gained the 8/13 backfill so the window has no hole in it and the test is answerable. **Spent two days early and against myself**, deliberately: a ruling that ships with a dated escape hatch has to be willing to use it before the date flatters it. **The distinction that decided it:** a windowed slope can be dragged by stale interior points, but it cannot make the **raw endpoint reads** climb — and 164.2 / 164.7 / 163.3 / 164.7 are climbing against a soft trend that has gone 163.37 → **164.07** in five days. **Standing consequence:** when invoking a window artefact, check the raw endpoints separately from the slope. If the endpoints agree with the slope's story, the artefact reading is available; if they contradict it, the artefact is not doing the work you think. **Second-order finding worth as much as the ruling:** both maintenance fits are derived *from* the weight series, so a flat scale drags them down toward intake — `learned.tdee` has gone **2,886 → 2,793 → 2,752 → 2,708** in four days and, left alone, will converge on his logged 2,263 and declare the deficit gone. That is the estimator eating its own output. Named in advance in the brief precisely so that when it happens it cannot be mistaken for a metabolic finding; input-side, no second number, per the consent-hygiene law.

**CL-25 — STRENGTHENED, 8/15 (run 25). Second instance in two days, and this one lands on an engine-owned number.** Facts: `steppush_2026-08-10` was drafted **8/10** on the premise *UNDER THE CORRIDOR — STEPS FIRST*, when his step baseline was ~13,500/wk. It applied on **8/14** (`adj_mst73yoz09xsp`, `stepDelta: 1000`, target → ~16,500), by which point 8/9–8/13 had averaged **17,540** steps — already above the target it was raising him to. Same shape as the 8/13 side-delt set whose sleep gate had gone stale, and the same shape as run 22's *don't take the trim on top of a correction you already ran*. **What the second instance adds:** the first case could be dismissed as three minutes of laterals; this one moves a **derived target** the athlete is measured against, which is exactly the class the consent-hygiene law protects. So CL-25 is not a curiosity about volume proposals — it is a property of the proposal pipeline generally, and it should be assumed live on any proposal that survives more than a day between draft and apply. **Handling, and it is the part to copy:** because the step target is engine-owned, this got **no card and no competing number** — one brief paragraph naming which input the proposal is working from (a step baseline four days stale) and nothing else. Point at the input, never the output.

**W-6 — CLOSED, 8/15 (run 25), as noise.** Run 23 named the closing condition in advance: if the 8/17 rate call landed wrong, the tally becomes a scatter and the watch closes rather than being narrowed a fourth time. It landed wrong (rate **−0.06 lb/wk**, called early), and run 24's 163.0–164.0 read call missed in the same optimistic direction at **164.7**. **Final in-class tally: 3 pessimistic / 3 optimistic / 2 accurate.** No usable bias; run 18 already demonstrated that applying a standing correction made a prediction worse; and grading the *sign* of every weight call has stopped returning anything. **What survives the closure:** keep scoring weight forecasts as hits, misses and voids — that discipline caught tonight's error and is the reason the brief opened with it. Stop treating the *direction* of a weight miss as evidence about my own bias. Closed.

**W-8 — OPENED, 8/15 (run 25). Eleven lifts lose their comparison baseline on 8/17, at the exact moment the training record is the only evidence left for the recomp reading.** Facts: `insertions` reads `{ fly: "2026-08-17", hipthrust: "2026-08-17" }` and the feed carries eleven `seam:*` rows dated 8/17 — **lower:** calves, hanging, abs, ham, extension (behind the hip thrust); **upper:** sulek, tricep, curl, rearDelt, rows, pulldown (behind the machine fly) — each stating *"History stays on the record under the old order; the instruments start a fresh four-session baseline rather than comparing across the change."* **Press, hack, lateral and pronated are not seamed.** Why this is a watch and not a ruling: it is his programme decision, correctly implemented, and nothing about it needs undoing. The risk is entirely in **how I read the next four sessions**. Two specific failure modes to guard against: (i) a fresh baseline starting low will look like a regression on eleven lifts at once, and reading that as fatigue or under-recovery would be the same error as [[CL-15]]'s laundered debt flag — an artefact of which records exist, dressed as a fact about the body; (ii) [[W-7]] currently rests on *rising loads against a flat scale*, and most of that surface goes quiet for four sessions, so the recomp-versus-under-logging question loses its second line of evidence a week after losing its first. **Consequences already taken:** tomorrow (Sun 8/16) is the last upper session under the old baseline and press — unseamed — is carded on it; the DEXA moved from standing wish to headline card precisely because both non-scale lines of evidence are now compromised. **Resolve by ~8/31**, four sessions in, on whether the seamed lifts' fresh baselines were read correctly as re-anchors rather than as declines.

**W-9 — OPENED, 8/16 (run 26). The `noted` gate may carry as much information as the `approve` gate, which would put [[CL-11]] on the wrong axis.** Facts: all three of run 24's cards were decided `noted` — not approved — and all three converted inside 48 hours without a second ask. `fillthursday` → `dailyLogs` gained **8/13 at 2,208 / 160 g / 17,000**; `ham125rung` → ham ran **125 × 11,9,9 at 1 RIR** on 8/14 with the thigh pad pinned the same day (`ham.setupAt` 8/14T16:46); `bedtime` → 8/13 came back at **9.08 h** after the 5.33 h night. Run 23's noted cards behaved the same way. Meanwhile the *approve*-gated `sug_2026-08-04_sleep` was approved and not performed, and three more approved discrete-action cards sat unexecuted long enough to be flagged in the 8/6 running-tally note. **Why this is a watch and not a ruling:** two runs is not a sample, the noted cards that converted were all cheap single acts with an obvious next occasion (a blank day, a machine to check, a bedtime), and the approved cards that stalled were not — so the confound is plausibly *cost and occasion*, not the gate. **What CL-11 would become if this holds:** the informative variable is not approve-versus-dismiss but whether the card names one cheap act with a specific next occasion; the tap is a report on the card's clarity rather than a commitment. **Resolve by ~8/30**, once run 25's three cards and tonight's two have been decided and had a chance to land — specifically, compare conversion rates of `noted` against `approve` on cards matched for cost. Until then [[CL-11]] stands unamended.

**CL-18 — OVERTURNED, 8/17 (run 27), on its own withdrawal clause.** CL-18 ruled that a set added to a lift silently re-keys what that lift is graded against, mined from press 250 × 8,8,7,4 on 8/9 drawing **no feed row of any kind** while four other lifts from the same session each drew one — the only variable that had changed being the set count. It shipped at low confidence with a stated clause: *if the next upper day produces a press row with four sets logged and no load change, the inference is wrong and CL-18 reverts to an observation.* On **8/16** press ran **250 × 8,9,8,4**, fourth set rated 1 RIR, no load change, and the feed carries **PRESS — TARGET MET**. The clause fired; the ruling is withdrawn. **What replaces it is better than the inference and it is in the app's own words.** The same session's feed carries three rows of the form *"<LIFT> — NEW SET, BANKS WHAT IT GIVES · a slot the volume push just created has no line to miss; what it gives today IS the line, and the anchor machinery owns it from the next session"* — on tricep, pulldown and lateral. So the app has **explicit handling for a newly-created slot**, which is the opposite of the silent conjunction failure CL-18 described. **Why 8/9's press drew nothing is now unknown, and it is recorded as unknown.** The available guesses (the new-slot machinery post-dating 8/9; a target array that only re-keyed on the second four-set session) are guesses, `en.tgt` is still not persisted so none is checkable from state, and [[CL-22]] is the standing lesson about what happens when I substitute a fresh mechanism for an absent one. **What survives, and it survives on other evidence:** the general form entered alongside CL-18 on 8/9 — *a volume change is never only a volume change; a push and an ownership attempt should not land in the same session* — was confirmed independently by the volunwind arc and does not depend on the press observation. **The part worth copying is procedural.** This is the second ruling in four runs ([[CL-22]] 8/14, this one) to be overturned by a clause it shipped with, and the third ([[CL-23]] 8/15) to have a dated falsifier spent against itself early. A ruling that ships without a stated way to die is not being ruled, it is being asserted. Keep attaching the clause.

**W-7 — CLOSED, 8/17 (run 27), on its own deadline, and answered by arithmetic rather than by a declaration.** The watch asked whether an undeclared recomp gets named, or whether the app keeps running cut machinery around a block that stopped being a cut. Deadline was ~7 days from 8/9, held at ~8/16 through four updates. **The declaration side never fired and now never will need to:** `phase` still reads `EASE 1`, `plan.phaseLog` is still `[]`, `rate.band` is still `[1, 1.4]` in pounds, no coach note landed. **The empirical side resolved on 8/15's log.** Seven days 8/9–8/15 read 2,350 / 2,231 / 2,279 / 2,249 / 2,208 / **2,800** / **3,200**, mean **2,474**, against an engine maintenance of **2,653** — a deficit of **179 kcal/day ≈ 0.36 lb/wk** against a measured rate of **0.3 lb/wk**. Those agree, for the first time in the file. **So the premise the watch was built on is gone: nothing needed explaining.** The block is a cut on weekdays (mean 2,242) and not on weekends (mean 3,000), and the weekly average is what every instrument in the app actually sees. **What is NOT concluded, and the distinction matters:** the recomp reading is not refuted. Six lifts advanced in one session on a flat scale, protein has held its window for eleven-plus weeks, and at roughly maintenance with that protein and that volume a slow recomp is exactly what should happen — it is the *good* outcome, not a failure. What is refuted is the need for a phase change to explain a flat scale. **The composition question hands off intact to the DEXA card**, which is where Article IX put it from the start and which is why it was right never to card a pivot. **Standing lesson:** before opening a watch on a physiological explanation, close the arithmetic first. This watch ran nine days and four updates on a gap that two logged days closed.

**W-8 update (8/17, run 27) — live from today, nothing to grade, and the guard is now the first line of the next four reads.** The eleven `seam:*` rows are dated today and tonight is the first session behind them: **calves, hanging, abs, ham and extension** all start fresh four-session baselines behind the hip thrust. Two things changed since the watch opened. First, **the machine fly debuted a day early** — it ran 8/16 at **90 × 8,10** and the feed adopted 90 as the working load, so the six upper lifts behind it (sulek, tricep, curl, rearDelt, rows, pulldown) are seamed from today against a lift that already has a number. Second, the reason the watch was opened has weakened: it was opened partly because *rising loads on a flat scale* was [[W-7]]'s second line of evidence and the seam would silence it — W-7 is now closed on arithmetic, so the seam costs less than it did on 8/15. **The reading risk is unchanged and it is entirely mine:** five lower lifts starting low tonight is a re-anchor, not a decline, and calling it fatigue would be [[CL-15]]'s error in a new suit — an artefact of which records exist, dressed as a fact about the body. Resolve ~8/31, four sessions in.

**W-9 update (8/17, run 27) — a new shape, and it points the same way: a card converted with no decision ever recorded.** The watch asks whether the `noted` gate carries as much information as `approve`, which would put [[CL-11]] on the wrong axis. Tonight adds a case that fits neither gate. Run 26's card #1 asked him to log Saturday; `dailyLogs` gained **8/15 at 3,200 / 160 g / 16,000**. And `suggestionLog` still stops at the three 8/14 cards — run 25's three and run 26's two are all undecided across two syncs, with **`sync.last` now reading 8/16 and `status: synced`**, so run 26's "sync latency" reading no longer covers it. **The card was performed without being tapped at all.** Against that, run 26's card #2 (hip thrust load) was equally untapped and equally cheap and was **not** performed. So the tap is looking less like a gate on anything and more like a report on whether the ask was clear and had an obvious next occasion — which is exactly the alternative W-9 was opened to test, arriving from an unexpected direction. **Not resolving on this.** Two of the five undecided cards have now had their conditions land, the sample is small, and the honest confound is unchanged (cheap-with-an-occasion versus expensive-without-one). Resolve ~8/30 as set, on cards matched for cost. [[CL-11]] stands unamended.

**Working practice, recorded 8/17 (run 27) — the carry-the-sid rule depends on a file the engine destroys.** Run 23 established: *decided → fresh sid; undecided → carry the sid.* Tonight the hip-thrust card was undecided and should have been carried, and its sid was unrecoverable because `node tools/analyst-engine.js --write` overwrites `ledger/suggestions.json` at the start of every run, before I read it, and `suggestionLog` had not caught up. Shipped with a fresh sid and the duplication noted in the scorecard rather than left as a surprise. **Consequence:** when a card is expected to survive undecided into the next run, write its sid into the scorecard entry that night, not into the file the engine owns. Same family as [[CL-24]] — *the analysis is limited by which fields survived* — with the novelty that this time the field is destroyed by my own tooling rather than never written.

**W-10 — OPENED, 8/18 (run 28). Retrospective single-field asks convert; prospective do-it-at-the-gym asks do not. This may be a better axis than [[CL-11]]'s discrete-action / standing-restraint split.** Facts: three logging cards in a row have converted inside 24 h — `fillthursday` (8/14 → `dailyLogs` gained 8/13), log-Saturday (8/16 → gained 8/15 at 3,200), `logsunday` (8/17 → gained 8/16 at 2,122 plus the sleep night). All three asked him to **put a named past day on the record**: one screen, verifiable, done at home, no occasion required. Over the same stretch, every ask that named a **future action at the gym** has failed to convert — `hipthrustload` twice (8/16 and 8/17; `hipthrust.w` still `null`), `press4th` three times before its condition finally arrived on its own. [[CL-11]] reads that split as *discrete action versus standing restraint*, but all of these are discrete actions; what separates them is **retrospective-and-verifiable versus prospective-and-situational**. Candidate ruling: an ask that can be completed the moment it is read converts; an ask that must wait for an occasion decays, and each day of waiting costs it. **Honest confound, and it is large: the prospective asks may simply never have had their occasion.** Monday's lower session was never logged, so the hip thrust's day did not arrive at all, and 8/9's press card expired on a rest day. **Resolution condition, dated:** Friday 8/21 is a lower day and the hip thrust debuts on it. If `hipthrust.w` gains a value before Friday, the axis is wrong and this closes as noise. If Friday arrives with the load still `null` while a same-period logging ask (`fillmonday`) converts, the axis holds and W-10 hardens against [[CL-11]]. If Friday's session does not happen either, the watch is **void, not confirmed** — say void.

**[[CL-25]] — THIRD INSTANCE, 8/18 (run 28), and the widest yet on an engine-owned number.** The ruling is *a gate checked when a proposal is written is not a gate on the day it is applied.* Facts: the app fired `STEP TARGET RAISED — UNDER THE CORRIDOR — STEPS FIRST — +1,000 → ~16,500` on **8/14** and again on **8/17**, four days apart, both times prescribing a step correction he had already exceeded — the 8/10–8/16 weekly block reads **17,643 steps/day**. Alongside it, `agentProposals` still carries the unresolved **29 July** proposal *"Adjust calorie band to 1,950–2,050 for current step average"*, now **twenty days** old, whose entire argument is that steps had fallen ~1,000 **below** a 16,237 baseline; they are ~1,400 **above** it. Both sit on engine-owned numbers, so the handling is fixed by the consent-hygiene law: **point at the input, never the output** — one brief line each, no card, no competing number. Scope: treat draft-to-apply staleness as live on any proposal older than a day, and as near-certain on anything older than a week.

**Working constant — the maintenance fit tracks the log, not the body. Third confirmation, 8/18 (run 28), and every one of them called in advance.** `learned.tdee`: 2,886 → 2,793 → 2,752 → 2,708 → 2,646 (sliding, 8/11–8/15) → **2,653** (halted 8/16, the run after two high days entered the 20-day mean) → **2,630** (falling again 8/17, the run after Sunday's 2,122 entered). Run 25 named the mechanism before the first turn — *the estimator eats its own output; a flat scale drags the fit toward intake and it will eventually announce the deficit gone.* It has now moved three times, each time in the direction the log moved and never in the direction the body moved. **Standing handling: read it as a ceiling that drifts, never as a measurement, and never quote its movement as evidence about metabolism.** The reason this is usable at all is that each move was predicted before it happened; a post-hoc reading of the same series would be indistinguishable from noise.

**Working practice, recorded 8/18 (run 28) — a two-sided forecast should sit on an axis that is not already carrying one.** Two weight forecasts are in flight (≤163.4 by 8/22, ≤163.8 by 8/21) and both are being decided by the same few reads, so a third would have added no information and three shared failure modes. Tonight's forecast was filed on **intake** instead (Fri and Sat 8/21–8/22 each above 2,600, falsified by both under 2,500). Companion to the run-27 lesson that a two-sided test must **exhaust** the space rather than merely oppose itself: it must also be **independent** of the tests already open, or the tally measures one thing three times.


**CL-27 — RULED, 8/19 (run 29). My own trend eats reads the app has explicitly set aside. `offWindow` is a field the engine has never read, and it moved tonight's headline number.** Facts: the 8/18 read is `{"d":"2026-08-18","w":163.2,"sealed":false,"note":"evening read — set aside","offWindow":true}` and the feed carries **five** `EVENING READ — SET ASIDE` rows on that date. His app's trend held at **164.1**; my soft trend went 164.15 → **163.87** and my rate 0.35 → **0.39 lb/wk**. **Mechanism read in source, not inferred** (`tools/analyst-engine.js`, `reliability()`, ~ll. 40–50): the function down-weights `r.sealed` (×0.4), prior-day sodium high/med, prior-day alcohol ≥4 units, a creatine load window, temp ≥99 °F, a prior night under 6 h, and any note matching `/(salt|alcohol|water|spike|refeed|event|wedding)/i`. **`offWindow` appears nowhere in it, and the string "evening read — set aside" matches none of those seven words** — so the read entered the EWMA at `rel = 1`, full weight, and pulled the trend 0.28 lb. **Second instance in the file:** 8/10 is also `offWindow: true` with an evening note and also entered clean; it sits inside the 14-day rate window, so it has been quietly contributing since. **The ruling:** a read the app has quarantined is not evidence, and my engine must not be able to reach a conclusion his app has already refused to reach. Where the app and I disagree about a *number*, the first question is whether we disagree about which *reads* exist — not about the body. **Why this is ruled at high confidence where [[CL-18]] shipped at low:** both halves are visible, which is the standard [[CL-22]] set — the writer is in source and the effect is in the file, and the prediction is checkable tomorrow (a clean 8/19 morning read should pull the two trends back together; if they stay apart, the diagnosis is incomplete). **Withdrawal clause:** if the 8/19 and 8/20 morning reads land and my soft trend does *not* converge back toward his, `offWindow` is not the whole story and CL-27 reverts to an observation. **Standing consequence, and it is a duty rather than a preference: until the engine reads `offWindow`, every trend and rate figure I quote must be reconciled against `state.trend` before it goes in a brief, and any divergence gets named to Joe in plain words before any conclusion is drawn from it.** Tonight's brief opens with exactly that, ahead of the good news, because the good news would otherwise have been read off a number I manufactured. **Family:** this is the fifth member of [[CL-24]]'s line — *the analysis is not limited by what the body did, it is limited by which fields survived* — and the second where the failure is on my side of the glass rather than his ([[the carry-the-sid rule]], run 27, was the first). The novelty is that this one does not lose a field, it **silently promotes a field the app had already demoted**, which is more dangerous: a missing input goes quiet, a mis-weighted input speaks confidently.

**[[CL-25]] — FOURTH INSTANCE, 8/19 (run 29).** The ruling is *a gate checked when a proposal is written is not a gate on the day it is applied.* `dailyLogs` records **22,000 steps on 8/17**, the highest single day in the file, and the 8/10–8/16 block averaged 17,643 — against a step target his app raised to ~16,500 on **8/14** and again on **8/17**, both times prescribing a correction he had already cleared. The **29 July** `agentProposals` entry is now **twenty-one days** unresolved and its stated argument — steps ~1,000 *below* a 16,237 baseline — is backwards by roughly **2,400**. Handling unchanged and fixed by the consent-hygiene law: engine-owned number, **no card, no competing figure**, one brief line naming the stale input. Recording the fourth instance mainly to close the question of whether this is a curiosity: four cases across volume, step target and calorie band in nine days makes draft-to-apply staleness a **property of the proposal pipeline**, and any proposal older than a day should now be *assumed* stale rather than checked for staleness.

**[[W-8]] update (8/19, run 29) — the seam has now waited two days and collected nothing; the guard stays up and gets its first real test Friday.** Eleven `seam:*` rows are dated 8/17; `sessionLog` ends **8/16**. No session ran on 8/17 or 8/18, so the fresh four-session baselines on calves, hanging, abs, ham and extension (and the six upper lifts behind the fly) still have **zero** sessions behind them. The risk window has now slid twice. Nothing gradeable, nothing changed in the watch — but the practical consequence is worth naming: Friday 8/21 will present **five lower lifts opening low on the same session as a hip thrust debuting at a load that may be a guess**, which is the single most misreadable session in the file. The rank-2 card exists to remove one of those two unknowns before it lands. Resolve ~8/31, four sessions in.

**[[W-10]] update (8/19, run 29) — the retrospective side has gained a fourth conversion; the prospective side still has not had its occasion, and the void branch is now the likeliest outcome.** The watch asks whether *retrospective-and-verifiable versus prospective-and-situational* is a better axis than [[CL-11]]'s discrete-action / standing-restraint split. Retrospective: `fillmonday` converted inside 24 h and **over-delivered**, bringing 8/18 in as well as the 8/17 it asked for — four for four. Prospective: `hipthrustload` is now **three** issues deep with `hipthrust.w` still `null`. **But the confound has grown, not shrunk:** no session has run since 8/16, so the hip thrust's occasion has not merely been declined, it has not arrived on any of the three days the card has been live. That is precisely the branch run 28 pre-committed to calling **void**, and I am flagging now — before Friday, so it cannot be rationalised after — that if Friday 8/21's session does not happen, this watch is void and says nothing about the axis. It resolves as evidence **only** if a session runs and the load is still null. Deadline held at Friday 8/21.

**[[W-9]] update (8/19, run 29) — ten cards undecided, four conversions without a tap, and the sync channel is no longer a plausible explanation.** `suggestionLog` still stops at the three 8/14 cards and `priorDecisions` still carries the same **10** entries it carried on 8/17 and 8/18. Ten cards are now undecided (3 from 8/15, 2 from 8/16, 3 from 8/17, 2 from 8/18), and **four** of them have had their conditions land in behaviour with no decision ever recorded. Meanwhile `sync.last` reads **8/16** against a file carrying 8/18 logs — it healed to 8/16 on 8/17 after seventeen runs stuck at 8/09 and has gone stale again, so the decision channel and the data channel are demonstrably not the same channel: **his data is arriving and his taps are not.** That is a materially different reading from "the tap reports clarity rather than gating anything" — it may be that the tap is simply not being written back. **Not resolving on it**, because I cannot distinguish "not tapped" from "tapped and not synced" from inside the file, and that distinction is the whole watch. Recorded as the sharpest form the question has taken. Deadline moved out to ~9/05 on that ground; [[CL-11]] stands unamended.


**[[CL-27]] — CLAUSE CHECKED, RULING STANDS, 8/20 (run 30).** The withdrawal clause shipped with the ruling: *if the 8/19 and 8/20 morning reads land and my soft trend does not converge back toward his, `offWindow` is not the whole story.* The 8/19 morning read landed (**162.4**, no `offWindow`) and the trends converged — his 164.1 → 163.7, mine 163.87 → 163.6, gap 0.23 → **0.10** — so the clause did not fire and the diagnosis holds: the divergence was the evening read, nothing else. One detail worth keeping: the app's own note on the new read (*"spike — damped in trend"*) happens to match my engine's note regex, so both instruments down-weighted the same read for once — agreement by coincidence of vocabulary, not by shared design, which is exactly why the standing duty (reconcile every quoted figure against `state.trend` until the engine reads `offWindow`) stays in force even on nights the numbers agree.

**Working practice, recorded 8/20 (run 30) — a forecast is graded at its deadline; "dead" before the deadline is itself a prediction, and this one missed inside 48 hours.** ≤163.4-by-8/22 was called *almost certainly dead* in run 28's brief at 164.1 and graded flat **dead** in run 29. Tonight the trend reads 163.7 and the call needs 0.3 lb with two days left — alive. The hedged form was defensible; the flat grade was not, because a single damped morning read was always capable of moving the trend 0.4. Distinction to keep: *dying* is a status report and may be said early; *dead* is a grade and happens on the date. Companion to the run-27 clause discipline (a ruling ships with a way to die) — a forecast's death also has a date, and it is written on the forecast.

**[[W-8]] update (8/20, run 30) — three days, zero sessions collected; tomorrow stacks everything on one page.** `sessionLog` still ends 8/16, so the eleven seamed lifts still have nothing behind them and Friday now presents five lower lifts opening on fresh baselines **plus** a hip thrust debuting at a load that is still `null`. The rank-2 card exists to remove the one removable unknown. The guard is unchanged and it is the first line of tomorrow's reading: **fresh baselines, not declines.** Resolve ~8/31.

**[[W-9]] update (8/20, run 30) — fifth conversion without a tap, and the freeze is now four runs old.** `morningread` converted inside 24 h (the 8/19 read exists) with no decision recorded; `priorDecisions` has carried the same 10 entries since 8/17; `suggestionLog` stops at 8/14; **twelve** cards now undecided. `sync.last` still reads 8/16 against a file carrying an 8/19 read, so the data channel keeps delivering while the decision channel stays dark — the sharpest form of the watch is unchanged and still not resolvable from inside the file. Deadline ~9/05 stands.

**[[W-10]] update (8/20, run 30) — decision day is tomorrow and the branches are pre-committed.** Retrospective side now five for five inside 24 h (morningread joins fillthursday, log-Saturday, logsunday, fillmonday). Prospective side (`hipthrustload`) is four issues deep with the load still `null` and, crucially, still zero occasions — no session has run on any day any issue was live. Tomorrow: load gains a value before the session → axis wrong, close as noise; session runs with the load `null` while fillwednesday converts → axis holds and W-10 hardens against [[CL-11]]; no session → **void, not confirmed — say void.** Nothing graded tonight.


**[[CL-28]] (8/21, run 31) — the hold attaches to 0-RIR openers that *miss*, not to 0-RIR openers as such.** Facts: abs froze on 8/8 after opening at a rated 0 and stalling, and releasing it required the 1-RIR opener the 8/8 card prescribed. Rows opened at a rated **0** on 8/20 — first set 10 at RIR 0 — and did **not** freeze: the feed carries PRIME SEATED ROW (HOOKS) — TARGET MET, `rows.holdFlag` absent, 180 adopted clean off 10,11. One lift froze on 0-and-stall; another sailed on 0-and-hit. Ruling: the app's hold mechanism keys on the *combination* (0 RIR opener + failure to convert), not on the opener rating alone; run-18's broader reading ("a rated 0 is what froze abs") was true of that instance but overstated the mechanism. Principle: opener discipline at 1–2 RIR stays the standing recommendation — it buys attribution and repeatability — but future cards must state the freeze risk in the narrow form, because an athlete who opens hot and converts has not broken the rule the app actually enforces. Scope: app hold logic as observed on two instances (n=2, one each way); revisit only if a 0-RIR *hit* ever freezes or a 0-RIR *miss* ever passes.


**Working practice (run 30) — CONFIRMED IN FULL, 8/23 (run 32): "dead is a grade; grades happen on the date" just decided a verdict.** The ≤163.4-by-8/22 forecast ran the complete arc: filed 8/15 · *almost certainly dead* (run 28, defensible hedge) · graded flat **dead** at 164.1 (run 29, the error) · revived (run 30) · **HIT at its deadline at 163.0, margin 0.4**. Had the run-29 grade stood, the tally would carry a miss on a call that finished a winner — the premature verdict was not merely early, it was *wrong*, and only deadline discipline caught it. Standing form unchanged: *dying* may be said any night as a status report; *dead* is a grade and has a date, and the date is written on the forecast. This entry exists so the practice is cited as paid-for, not aspirational.

**CL-29 — RULED, 8/23 (run 32). A session-gated card converts in occasions, not days; count its latency accordingly.** Facts: `hipthrustload` was issued five times (8/16 → 8/20 sid chain, recorded run by run in the scorecard) and looked like the prospective genre's standing failure — until the occasion ledger is consulted: **no session ran on any day any issue was live** (sessionLog: 8/16 upper, then nothing until 8/21). The first session that could carry the ask arrived 8/21 — and the ask was done *before it*: `hipthrust.w` set to 90 at 19:02:42, setup and all three pins stamped 19:02–19:03, session logged 19:44, debut 90 × 7,8,7 at a 2-RIR opener. **Five issues, zero occasions; one occasion, one conversion.** The ruling: a card whose condition can only fire on a session day has its latency measured in *sessions elapsed while live*, not calendar days or re-issue counts; re-issues across occasionless days are the card waiting, not the athlete declining. Consequence for [[W-10]]: **closed on its pre-committed branch as noise** — the retrospective-vs-prospective axis dissolved once occasions were counted (retrospective cards convert on *days*, which arrive daily; session cards convert on *sessions*, which that week arrived never); [[CL-11]] stands unamended. Honest wrinkle kept in the record: the card's letter said "before you leave the house" and the stamps say "at the machine, forty minutes ahead" — the letter converted late, the guarded risk (an unchosen debut number, hack-170 style) never occurred. Scope: latency accounting and void-branch design for all future training-day cards; the behavioural claim rests on n=1 conversion, the counting claim on the full five-issue, six-retrospective-conversion record behind it.

**Working practice, recorded 8/23 (run 32) — a forecast that grades on athlete-logged inputs carries a blank branch, or it was never fully filed.** Second instance of run 27's exhaustiveness lesson, and the sharper one: the weekend bet (Fri+Sat each >2,600, falsified by both under 2,500) reached its due date with **both legs blank** — no 8/21 or 8/22 food exists — and the filing had said "gradeable Sunday" as if the days' existence were certain, on the athlete's *single most-missed day* (Saturday: 8 of 17 lifetime misses; the blank branch was the modal outcome and the design ignored it). Handling now fixed at filing time for all future intake/log forecasts: name the blank branch and an expiry when the forecast is filed. This one: grade slides to the day both legs fill, **hard expiry 8/30, VOID if still blank** — void is not a win, not a loss, and not silence; it files as a design lesson against the forecaster.


**CL-30 — RULED, 8/28 (run 33). A card-conversion prediction is a log-dependent forecast and carries a blank branch at filing, like any other.** Facts: run 32's gradeable call — "fills inside 24 h, read tomorrow" — rode a seven-for-seven retrospective streak and missed completely: the file recorded zero athlete inputs for six days (reads end 8/21, food 8/20, nights 8/20, sessions 8/21), so no contact-day ever arrived for the cards to convert on. The irony that rules it: the same run 32 filed the working practice "a forecast that grades on athlete-logged inputs carries a blank branch, or it was never fully filed" — for intake forecasts — and in the same breath filed a card-conversion bet without one. The ruling extends the practice to its last hiding place: **every prediction whose grading path runs through the athlete's own logging — intake bets, card conversions, read-gated watches — names its blank branch and expiry when it is filed.** Mechanism note that sharpens [[CL-29]] rather than amending it: CL-29 counts session-card latency in occasions; tonight shows *retrospective* cards have occasions too — contact-days, days the athlete touches the file at all. A dark file stalls both genres at once, and non-conversion during a contact gap grades the gap (and the forecaster's missing branch), not the genre. The seven-for-seven record is not broken evidence about Joe; the miss files against my filing discipline. Scope: forecast/filing hygiene for all future predictions; the genre's behavioural record resumes intact on the next contact-day. Withdrawal clause: if it emerges the fills were entered and lost (a sync defect, not a contact gap), the miss re-files from me to the instrument and CL-30's mechanism half reverts to observation — checkable against `sync.last`, which healed to 8/23 and carried data cleanly, which is why the contact-gap reading stands at high confidence tonight.

**[[W-9]] update (8/28, run 33) — the sync stamp healed a full week and carried no taps; "not tapped" strengthens over "tapped and not synced."** `sync.last` moved 8/16 → 8/23 with status "synced", delivering the 8/21 session, the 8/21 read and the 8/19–8/20 logs — while `suggestionLog` still ends at the three 8/14 cards (24 entries) and `priorDecisions` holds the same 10. A demonstrably working pipe moved everything except decisions. Nineteen cards now undecided. The watch's sharpest form updates: the decision channel is dark *through* a healthy data channel, which points at the tap itself (not made, or not written locally) rather than transport. Still not resolvable from inside the file; deadline ~9/05 stands. One wrinkle for fairness: the athlete has not opened a contact-day since 8/21, so the three 8/23 cards specifically have had no occasion to be tapped ([[CL-30]]'s contact-day logic) — the nineteen-count is real, but the freeze's clean test remains the sixteen that pre-date the gap.

**[[W-11]] update (8/28, run 33) — on script, ungraded, blank branch now explicit.** Every read that exists supports the fat-not-water verdict: giveback +0.6 printed 8/21 as called, trend fell anyway to a record 162.9 (his; mine 163.05, reconciled, gap 0.15). But reads stop 8/21 and the frame said "through ~8/28," so per deadline discipline there is no early grade on a carried trend. Filed tonight: grades on the first read to land; **no read by 9/01 → VOID** (run-32 practice, now CL-30). Mid-162s on the next read = confirmed; low 163s = giveback larger than taped, frame intact; above 163.5 = the whoosh was mostly water and I say so.

**[[W-8]] update (8/28, run 33) — occasions elapsed: still zero; resolve slides to ~9/07.** No session since 8/21, so the four fresh baselines (ham, abs, hanging, calves) and hip thrust's first real target remain unopened, undeclined, uncounted ([[CL-29]]). The comeback session now stacks four baselines + a debut target + a week of layoff rust on one sheet — pre-framed in the brief as baselines-not-declines with 1–2 RIR openers ([[CL-28]]'s narrow form: the freeze risk attaches to 0-RIR *misses*, and a rusty opener is where those live). rearDelt's third-skip watch rides the next upper occasion, likewise uncounted through the gap.
