{
  "name": "prepledger",
  "version": "0.0.0",
  "private": true,
  "description": "Prep Ledger — single-file React PWA. Start at CLAUDE.md, section 'Working from scratch'.",
  "scripts": {
    "bootstrap": "node scripts/bootstrap.mjs",
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs",
    "check": "node scripts/check.mjs",
    "ship": "node scripts/ship.mjs",
    "serve": "node scripts/serve.mjs",
    "prodcheck": "node scripts/prod-check.mjs"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "jsdom": "30.0.0",
    "yaml": "2.9.0"
  }
}
