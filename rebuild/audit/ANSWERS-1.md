# PROCESS AUDIT 1 — cowork's answers (2026-09-04). Every executable finding was re-run; judgment findings are marked as such.

Astra's disclosure (it inherited its own T3 context; not a fresh reviewer) is noted and accepted; F8 and F9 go to Sol as tie-breaker
per the brief, after Sol's blind T3 read returns (sending them now would unblind it).

| F | Verdict by execution | What changes |
|---|---|---|
| F1 30-day device soak scheduled too late | JUDGMENT — AGREED. The sheet orders the soak to start as soon as a durable-store stub exists; the roadmap had it in M4. | ROADMAP v1.3: a minimal installed-PWA storage soak on the owner's phone starts in M3's first week, in parallel with M2 — a stub app that writes an outbox, is left untouched, and is read back after 30 idle days with the device/OS, start date and pressure protocol recorded. Owner supplies the phone. |
| F2 privacy/backup/cap need acceptance gates before the real port | JUDGMENT — AGREED. The backend packet names features, not proofs. | ROADMAP v1.3 M3 definition of done gains: two-athlete isolation test on the real backend; restore into an isolated account; recorded loss/recovery objectives; key-rotation + account-recovery drill; import checkpoint + rollback rehearsal; privacy-safe error telemetry; an OBSERVED spending alert (what "$5" actually stops, written down). |
| F3 no owner/gate for the absent policy/progression/protocol families | JUDGMENT — AGREED. "29 RED-first against absent families" cannot be beta-ready by silence. | ROADMAP v1.3: contract-to-module-to-milestone table; beta's required-family inventory (policy + progression adapters required by M4; source-ingestion protocol: owner to rule whether beta needs it). |
| F4 unsigned receipt advances W; canonical collision; durability without restart | CONFIRMED by reading rebuild/client/sync.cjs deliverReceipts (accepts any integer seq; no signature). Already on the v4 list from the T3 CLAUDE report (6e) and T2 (6a) — the audit adds an executed witness. | Suite v4: receipt authentication law (receipts carry the authority signature or are admitted only for verified op_ids); canonical-v2; restart-from-serialized-store durability law (rig187 promoted). |
| F5 the census can preserve wrong behaviour and miss broken writers | CONFIRMED by cowork's own bite B on module 1: the FIX-4c line flipped and no blob noticed. RECON §5 already lists the second gate. | The second gate (engine-test.jsx ≈2,600 assertions + sync-laws + engine-surface baseline re-pointed at the module) is REQUIRED before M2 closes, not "ideally". A same-day-decrease fixture enters v4. |
| F6 rebuild branch lacks main's CDN denylist; strict gate is output-parity, not a frozen-path hash | CONFIRMED: `git merge-base --is-ancestor cbe86e3 HEAD` → 1; site-manifest.mjs on the branch has no rebuild/ exclusion. | Claude Code merges origin/main into rebuild/t2-client-core now (brings cbe86e3 AND the frozen v7.56.0 app; the branch then tracks the frozen main). A frozen-path diff check (src/, app.js, index.html vs the brief's base) is added to the rebuild gate in v4. |
| F7 gate-artifact step trusts `pass:true`, ignores passed/total | CONFIRMED: run.cjs:58 checked `g.result.pass` only. FIXED in suite v3.2 (this handoff): `passed === total > 0` is required; proven — setting passed=0 on one gate now turns step 8 BAD and the suite INCONSISTENT. | v3.2 committed by Claude Code. |
| F8 bake-off: dead mutant scored as a tie; seams×2 as the deciding metric; time not normalised | PARTLY CONFIRMED. Time: correct — the scorecard's "13 min" was CLAUDE's build time only; its report also lists ~30 min reading. Astra's 18 included reading. The scorecard is amended (cost row: CLAUDE ≈43 min incl. reading; ASTRA ≈18 incl. reading). Dead mutant: it was scored as "not evidence against Astra", not as evidence for it — but the audit is right that it is not evidence for the tenth family either; the M2 re-test replaces ineffective mutants until both products show the same wrong behaviour. Seams metric: pre-registered before either build; changing it after the fact would be worse than keeping it. Going forward (M2 re-test): seams classified by violated boundary and severity, omissions scored separately. | To Sol as tie-breaker with the scorecard and this answer. The owner's ruling stands (it was provisional by design: "same scorecard on module 1"). |
| F9 roles by evidence and authority, not brand or PC location; the second blind review was dropped | AGREED in substance. The pre-registration promised two blind reviewers; Grok was retired before the T3 pack went out, so the plan changed and must be recorded as changed (done, ROADMAP v1.3). The integrator seat is a role (secrets, private golden custody, releases) with a reproducible runbook — the model in it can change after an exercised handoff. | ROADMAP v1.3 records the changed review plan; a backup-integrator rehearsal is queued for M3. Grok stays retired unless a targeted call is warranted. |
| F10 repo text vs current state; owner as the integration bus | CONFIRMED: AGENTS.md still said a0009c3 (fixed in this handoff); CLAUDE.md/NEXT.md/HANDOFF.md carry stale numbers. | AGENTS.md rewritten (fe516c1; golden pin policy; ONE report + ONE PR link per builder; ROADMAP §Now = the task index). Builders' reports are fetched from the PR by cowork — the owner no longer relays builder reports at all. Stale NEXT.md/CLAUDE.md sections are demoted with a dated marker in the next docs refresh (not this handoff — frozen-app files). |

Also from the audit, accepted without argument: publish engineering effort and calendar dependencies separately; a quiet scheduled PR/blocker monitor may notify but never approve; recover the three missing ratified documents from the owner's copies and commit them.

Two cowork defects surfaced tonight, owned here: (a) suite v3.1 broke rig185 (a0009c3 has no _setsAtTime → blank INFO row) — v3.2 runs rig185 on the FROZEN engine with the corrected rename shape and expects PASS, PASS, and skips cleanly on a pre-FIX-4 engine; (b) the T3 scorecard's cost row compared unlike quantities (fixed above).
