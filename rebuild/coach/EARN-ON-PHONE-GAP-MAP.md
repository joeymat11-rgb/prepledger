# Load progression on the phone: gap map
Astra for PM4, 2026-09-19; named reading assignment under DECISIONS:412 and :569(3).
Read HEAD: 1d4f48f466a53bdb776b881cc49d6dc97a0d543f; branch rebuild/c-earn-on-phone-map.
No product change, implementation selection, queue priority, or engine-path authorization.
All workout values, dates, identities and outputs in the drives below are SYNTHETIC.

## 1. Was this gap decided or did it just happen?

**Finding: a deliberately bounded reader surface, with a planned but unfinished writer join.**
It was written down and explicitly left open. I found no ruling deferring native load progression to a named later delivery step, nor authority to call it a permanent training rule.
The evidence does not establish who lost ownership of the open item; it does establish that accepting the reader packages did not close it.

| Record | What it actually commits to |
|---|---|
| Broad plan | rebuild/ROADMAP.md:95-99 assigns gym logging, proposals/consent and progression adapters to M4. rebuild/m4/BRIEF-OWNER-WORKOUT.md:264 still names "Full UI and lossless fact-to-engine/writer join/D43 closure"; :318 calls mixed loads/unknown effort a real writer/UI proof obligation. |
| Early rich-fact proposal, Astra, September 9-10 | rebuild/m4/spec/PERFORMED-ENGINE-v1.md:19-23 separates workoutFacts from sessionLog: "An acknowledged workout fact is not consent to a plan change." :23 says reopening/sync/correction/replay "MUST NOT call completeSession again" (backticks omitted); transitions belong in the authorized operation/consent/plan-transaction path. :32 names the writer join; :77 says the effect protocol remains required. These paragraphs were proposals, not blanket accepted authority. |
| Owner-approved repairs | DECISIONS:60 (September 6) approves D41/D43 repair directions, not arbitrary code. DECISIONS:85 (cowork, September 10) accepts PERFORMED-ENGINE sections 0 and 6 only; sections 1-5 "remain PROPOSED". :86 receipts and :87 integrates M2-LOAD-WRITES, expressly "Not qualified ... hosted, phone" or rich-input sections 1-5. |
| What LOAD-WRITES proves | rebuild/m4/spec/PERFORMED-ENGINE-v1.md:97-110 moves canonical earnWalk and repairs queued vector adoption, reset vector translation, and persisted entered loads. rebuild/m4/spec/load-write-cases.cjs:10-42 directly calls completeSession/applyAgentProposal/mergeState; load-write-direct.cjs:23-42 compares those outputs and mutants. This is writer correctness, not a native Finish caller. D43 stores the performed load; it does not itself wire native weight adoption. |
| Slice promise versus bounded delivery | rebuild/slice/PLAN-SLICE-v1.md:8 says "Sets are real engine writes (writers.cjs), not simulated." DECISIONS:90/:92 ratify the slice with A0 = native next-target host plus required carriers. :88 explicitly defers hosted sync/two-phone/native host, not load progression. :102 (cowork, September 11) accepts A2 Start/Set/Finish through capture and client, with no engine or m4 edits. The plan's writer sentence is not fulfilled by recording a session-close. |
| NATIVE-NEXT-TARGETS / NATIVE-CARRIERS | rebuild/m4/workout/engine-runtime.cjs:2-8 calls itself "static binding-only prescription runtime": "no completion or adaptive writer escapes or executes through this surface." rebuild/m4/spec/NATIVE-CARRIERS-THEME.md:123-131 says "every completion/adaptive writer body is unchanged." DECISIONS:93 accepts that theme; :95/:96 receipt it; :97 integrates it. :98 assembles A0. This knowingly delivers native reads, not native earn writes. |
| B-NTC names the missing owner of the work | rebuild/lanes/b/BRIEF-B-NTC-NATIVE-TREND-CONTEXT.md:1289-1299 records permanent DEBUT with w:null. Open item O1 at :1642-1646 asks "what writes w for a native athlete, and when" and says it "is not B-NTC's and is not answered here" (backticks omitted). DECISIONS:103/:108/:109/:122 commission/accept the trend-context provider; :141/:142/:144 accept/integrate it on September 12. No native load writer is assigned by those lines. |
| Later import keeps the boundary | rebuild/m4/import/replay-core.cjs:178-188: "Their admission is not consent to invoke completeSession/plan effects." Source replay restores native observations separately. S8's theme/receipt/merge, DECISIONS:525/:528/:529, seals real import shape; :527 expressly changes no engine byte. |
| A real, narrower deferral | rebuild/lanes/pm/CRITICAL-PATH-2026-09-15.md:387-388 proposes deferring memory M07/M10 (issuance -> decision -> plan consequence). Joe chooses Q6 Option A in DECISIONS:432 (September 15); its interpretation explicitly defers M07/M08/M10 for his trial. That is coaching-memory scope, not an explicit native earn/adoption deferral or assignment. |
| Related unfinished effect join | rebuild/lanes/astra/MEMORY-LOCAL-DECISION-PM-DIRECTION.md:9-15 requires one durable decision/effect, replay at its source frontier, and Today/gym sharing the restored programme; :19-24 is contract work before runtime release. It is not evidence that native Finish invokes those effects. |

Thus "never specified anywhere" is false; "the owner deliberately chose a phone that never advances loads" is unsupported.
The named surviving gap is B-NTC O1, plus the larger fact-to-writer/effect join; no named load-progression successor/date was located.
The critical-path scout's sample-athlete finding (:12-16) was another wiring gap, not an explanation that closes O1.
PM queue placement and a fresh owner word for an engine-path change remain outside this assignment.

## 2. What exists already

Here "old" means scalar entries in sessionLog; "new" means typed workoutFacts interpreted from native Start/Set/Skip/Close/corrections.
The prior reports distinguish engine rules from phone reach: rebuild/coach/COACHING-POLICY-AS-BUILT.md:53-57, COACHING-POLICY-AS-BUILT-CHECK.md:342-369, and COACHING-POLICY-PHONE-REACH-ASTRA.md:5-9.
This inventory covers the standing paths relevant to performed work and load/sets, not historical one-shot migration patches.

| Existing function/path and source | What it can change; what history it reads |
|---|---|
| completeSession, rebuild/engine/writers.cjs:203-239 | Takes OLD scalar entry arguments, clones state, writes lastMeta/rirHist; two zero-reserve openers set holdFlag and a later opener >=1 clears it. It is not a typed native-facts writer. |
| Completion branches, writers.cjs:244-328 | eraFresh retires previous-era standards; completed debut/unlock consumes queue, adopts newW/newWSets, stamps load and can land pendingThird as three sets. Owned/reclaim standards can queue debuts; old ladder branch returns early. These branches use supplied entries and stored standards/queue; eraFresh can see native facts (plan.cjs:304-318). |
| Adoption and earn, writers.cjs:331-401 | First numeric completion adopts w; an ordinary different numeric load changes w, shifts prescribed wSets, merges an off-ladder rung and resets sightings. Calls atTopOfWindow -> earnWalk; off-top resets topRun. Writes date-keyed sessionLog and feed (:401-420). Own/reclaim/queued-debut early returns mean adoption is not universal for every branch. |
| earnWalk, rebuild/engine/earn.cjs:11-100 | Updates topAt/topRun, including no-next-load sightings; checks opener/hold, same-load previous reps and noise; queues automatic DEBUT or PROPOSED offers. Reads supplied old-shaped en/r/prevMeta and state; typicalError can consume new facts, but earnWalk itself does not interpret a typed native entry or deduplicate repeated Finish calls. |
| Arithmetic dependencies | rebuild/engine/progression.cjs:346-404 defines loadRungs/nextLoad/prevLoad/snapLoad/deloadLoad; :436-493 supplies progressionSetCount/atTopOfWindow; :553-603 supplies typicalError/beatsNoise. These are readers, not durable transitions. Native-aware tenure and noise support do not authorize running the writer. |
| deriveSighting/_deriveSightingFull | progression.cjs:608-653 calls _loadTenure (:188-212): OLD log when no workoutFacts, NEW causally ordered rows when attached. It derives an unspent, era/tenure-bounded top run with feed receipts as spend markers. Important existing asset: it can already derive native sightings; see the executed output below. No phone caller persists that result. |
| reconcileSightings / joint mint | rebuild/engine/migrate.cjs:79-92 stores derived sightings. migrate exit :2118 does not request mint; merge exit rebuild/engine/merge.cjs:1096 does. _mintJointEarn (migrate.cjs:35-74) still fetches entries by date from OLD sessionLog (:51), requires two provisional/no-next-load receipts and calls the same earnWalk (:62). Native-aware derivation alone does not make this a native joint mint. |
| Existing imported-load reconciliation | migrate.cjs:1904-1960 reconcileLiftCaches and :1964-2061 reconcileCorrectedLoads read OLD deriveLastMeta/sessionLog, not native Set edit records. The latter adopts a qualifying latest load correction with wCorrAt, respecting later athlete authority, updates rungs/load/last/sightings and deduplicates its receipt. A native edit_op_id is not that old correction stamp. |
| sweepStalls -> liftCall | writers.cjs:1534-1546 creates an agent reset OFFER, not a reset. rebuild/engine/sleep.cjs:50-76 reads OLD sessionLog for the last-five/stall comparisons, even when workoutFacts exists; :80 onward adds recovery context. No new native reset offer follows just from logging on the phone. |
| Volume/adaptive offer producers | writers.cjs:1329 volumePush, :1449-1530 sweepVolume, :1615 runAdaptive (earned volume :1899-1901; owner-requested :1954-1956); sweepLab :1565-1568 invokes stalls/ladders/volume. sweepVolume's age gate uses OLD sessionLog (:1461), as do muscleVolume/volumeConversion (volume.cjs:38-41,220-221); liftTrend has a NEW branch (progression.cjs:682 onward). They are not a fully native-adapted Finish pipeline. |
| Accepted proposal effects | writers.cjs:2314-2361 applyAgentProposal changes/stamps sets or resets w and translates wSets; it reads the offered body and current plan/budget, not native completed facts. applyProposal :2183-2209 changes sets or ladder/load; undoAdjustment :2444-2446 restores recorded set count. sweepLadders :129-149/proposeLadder (progression.cjs:310) infer equipment offers from OLD history. These distinct proposal stores/effects are not all handled by completeSession. |
| Consent and card consumption | writers.cjs:187-198 takeProposedDebut changes PROPOSED to DEBUT and supersedes lower offers. today.cjs:53-59 pickStructural excludes PROPOSED, selects a main plus coApproved riders; genSession :95-115 uses a selected lift's queue.newW or exercise.w, and supports pendingThird/unlock. Completion :261-268 later marks it ESTABLISH/done. Queue selection has a measured caveat in section 3. |

Bare engine filenames in this section mean rebuild/engine/.
What the accepted NEW reader already supplies:

- rebuild/engine/performed.cjs:34-72 validates completed typed entries, original/current values, source/edit IDs, slot statuses and effort variants.
- :145-187 combines OLD history with ordered NEW sessions; mixed history requires explicit baseline/import mapping, not date sorting as native causality.
- :218-253 derives the contiguous ORIGINAL performed prefix and compares each corresponding actual load to the current prescribed vector; gaps do not compact, added sets do not become original positions.
- :77-99 preserves exact/at-least/unknown effort; progression.cjs:37-50 uses original terminal/opener effort for the rep budget.
- progression.cjs:110-160 reads governing last/meta from NEW facts and finds a same-era, non-rushed, same-load anchor, with the fallback described in section 4; :261-307 constructs targets.
- Native whole-work scoring and repeat pairing use actual loads, not a substituted prescription (performed.cjs:131-143; progression.cjs:553-590,656-673).
- The factual projector's progression_eligible:false is not a ban on these qualified readers; it means facts alone grant no eligibility (rebuild/m4/workout/engine-history.cjs:127-135; performed.cjs:163-165).

## 3. The native Finish seam and options

Finish is rebuild/m3/w7-preview/today/gym-model.mjs:538-546.
It prepares continuation, collects Start and completed-slot operation parents, then sends executeResumedWorkout with resumeId, action:'close', and input {session_start_op_id, completion_kind:'normal', causal_parents}.
rebuild/m3/w6/public-client.mjs:371-387 checks the prepared context and that normal Close has no unfinished slot, then calls bridge.execute('workout', args).
rebuild/m4/workout/commands.cjs:42-72 maps this to class session/kind session-close; payload completion_kind is stored beside the existing Start capture, Set load/reps/reserve and Skip facts.
The bridge stages against the loaded generation and returns durable success after repository.commit (rebuild/m3/w6/bridge.mjs:17-49); Finish clears its saved UI state only after acknowledged:true.
It writes neither exercise.w/topRun/holdFlag nor an engine debut/volume/reset queue entry.

The read return path is engine-history.cjs:124-135 -> source-projection.cjs:50-58 -> engine-capture.cjs:39-52, all under rebuild/m4/workout/.
It attaches ephemeral workoutFacts beside the accepted state and calls genSession on a clone; it does not adopt actual load into that state.
The browser mirrors the same five-reader surface (rebuild/m3/w6/host/engine-runtime-host.cjs:54,97-114).
For a stored sighting/earn, the missing join must evaluate the actual engine transition (completeSession/earnWalk or a reviewed native-aware equivalent) at the completed factual basis and commit its effects through the existing durable command/plan machinery.
The existing primitives are not a drop-in native writer: the OLD completion signature/date-keyed log loses per-set source identity, and replay is expressly excluded (PERFORMED-ENGINE-v1.md:22-23; BRIEF-OWNER-WORKOUT.md:641).
Client consent primitives exist (rebuild/client/index.cjs:349-371 respond; :135-136 consent-linked accepted plan; rebuild/client/plan.cjs:2-10 projection), but no located Finish adapter joins them to those engine effects.

**Sealed-file key check, not a permission grant.** S = key present in product of rebuild/m4/spec/acceptance-s8-real-shape.json; U = absent from both product and executionPins.
DECISIONS:528/:529 receipts/seals S8; :527 records its no-engine-byte scope.

- S: engine earn (:221), merge (:241), migrate (:246), performed (:256), progression (:271), today (:286), writers (:296).
- S: m3/w6/host/engine-runtime-host.cjs (:436), workout-host.mjs (:481), local/source-admission.mjs (:521), local/today-bindings.mjs (:531).
- S: m3/w7-preview/today/build.mjs (:686), gym-host.mjs (:701), today-entry.mjs (:791); m4/import/replay-core.cjs (:846); m4/workout/engine-runtime.cjs (:996), engine-history.cjs (:1241).
- U: m4/workout/{source-projection,engine-capture,commands,schema}.cjs; m3/w7-preview/today/gym-model.mjs; m3/w6/{public-client,bridge,repository}.mjs and t2-stage.cjs; client/{index,plan,face}.cjs.
All abbreviated paths in this list start with rebuild/. U does not mean ungoverned: other contracts/build pins still exist.

Options only; these are prospective touched surfaces and obligations, not a chosen file-level specification.

| Option | Likely files/surfaces (S/U above) | Stored data and undo | Second device / re-import | Athlete's tap |
|---|---|---|---|---|
| Derive sightings/next load whenever building a card | S engine/{progression,earn,today,performed}, runtime + browser mirror; U source-projection/engine-capture; possibly S engine-history; tests/build and successor pins as needed. Existing deriveSighting is reusable groundwork, not the whole earn. | No new mutable sighting/load cache in this variant. Future reads can be reverted/recomputed; already captured prescriptions remain facts. Durable consent/spend evidence is still needed for PROPOSED acceptance, overrides and applied changes. | Same authenticated facts, causal order, imported baseline and effect/spend history must derive the same offer; deduplicate sources, preserve completed debuts, and avoid re-earning after import. This is not yet proved. | Automatic DEBUT can become eligible without an offer tap under the existing rule; PROPOSED cannot. A stateless calculation cannot invent that tap or its durable identity. |
| Run writer at Finish through current command path | U gym-model, public-client, commands/schema, t2-stage/client plan APIs; S runtime/mirror and engine/writers/earn if exposed/adapted; S today-bindings/source-admission for reconstruction; potentially S workout-host and build. Existing bridge/repository are the commit layer, not necessarily files needing edits. | Adds durable transition/receipt/queue/plan state. One atomic, basis-bound effect identity is needed for retry/crash safety; plain completeSession twice is not safe. Undo would need a durable compensating effect; deleting Close or reversing a cache is not an established undo. | One completed Start/effect must not earn twice on two devices or after source re-import; source-bound effects must survive replay at the right frontier and preserve later edits. Existing OLD merge laws alone do not prove this native join. | Finish confirms recording. It cannot also mean yes to every reset, extra set or early/two-rung offer. Those need their corresponding explicit acceptance; automatic earn must preserve its distinct status. |
| Replay at load/open/import | S source-admission, today-bindings, replay-core, engine-history, engine migrate/merge/earn/writers; S runtime/mirror if enlarged; U source-projection/capture; host/build as required. | Pure projection is the first option moved to load time. Persistent re-execution needs stable effect/spend/consent identity and an undo protocol; replaying completeSession over observations is expressly not authorized. | Reopen, overlap, corrections, source swaps and re-import must reproduce prior effects once, not mint them anew; OLD joint-mint receipt/date logic is insufficient for native operations. | Loading or admitting facts is no tap. Reproduce a previously authorized effect/automatic state or retain an offer; do not convert PROPOSED to consent. |

**Existing consent rule:** earn.cjs:91-97 records Joe's August 10 ruling: one top-window sighting with terminal reserve >=2 may earn "as a PROPOSAL"; "only offers".
No separate ruling of that original one-sighting policy was found in DECISIONS; its source attribution is rebuild/engine/earn.cjs:91. DECISIONS:85/:87 preserve the canonical earn implementation.
earn.cjs:55-64 preserves the owner-ruled hot-opener exception as a tapped offer; :66-88 automatically queues the confirmed single rung, while the two-rung alternative stays PROPOSED.
takeProposedDebut requires accepting the offer; automatic DEBUT still waits for the structural slot.
Set increases/reset offers need their own consent (writers.cjs:2351-2354); DECISIONS:152, amended :157, explicitly requires a proposed, never automatic, starter-volume increase.

**Measured consent-selection caveat:** pickStructural excludes PROPOSED, but genSession's later find at rebuild/engine/today.cjs:97 does not repeat that exclusion.
earn.cjs:80 appends a two-rung PROPOSED before :88 appends its automatic single-rung DEBUT.
A synthetic pre-existing queue in that order selected the automatic structural item yet prescribed the untapped offer's weight:
```text
synthetic/proposed-before-automatic {"selectedStructural":"synthetic-automatic","cardWeight":110,"states":["PROPOSED","DEBUT"]}
```
Both runtimes agreed. This was a constructed queue, not a claim that native Finish minted it, nor a verified phone tap flow.
Any option must preserve the intended distinction and account for this existing discrepancy; no fix is proposed here.

## 4. Fresh lifts and logged loads

**Failure to adopt is a join gap, not a located rule to ignore the athlete's load forever.**
The clean constructor intentionally starts w:null (rebuild/m4/workout/athlete-state.cjs:134-136).
The card explicitly expects the first completed load to arrive through completeSession's adoption branch (rebuild/engine/today.cjs:103-112).
That branch says "First completed load ... is the working load now" (writers.cjs:338-340); the ordinary changed-load branch says "Reality outranks the filed ladder" (:331-349).
OLD corrected-load reconciliation also follows the record under its stamp/recency guards (migrate.cjs:1986-2058). None of these is called for a native recorded/edited load.

**Different-load sets are neither universally ignored nor universally treated as matching.**
rebuild/engine/performed.cjs:244-253 requires exact per-position pound/configuration matches; progression.cjs:80-85 applies that to current prescribed loads.
progressAnchor :151-158 skips a mismatch when selecting a same-load anchor; _loadTenure :199-201 ends that tenure at a mismatch.
But governingLast :120-124 takes the latest original performed prefix regardless of load; progressAnchor :146,:160 returns that base if no eligible match exists.
Therefore a heavier or mixed-load session can supply reps at the unchanged prescription through fallback. It was not deemed equal by the load comparator.
Even with an older matching anchor, progressStep :37-46 takes the LATEST factual effort, without that same-load filter.
Whole-work scoring and noise pairing have their own actual-load rules; these sets remain recorded and usable by those consumers.
Bare engine filenames here mean rebuild/engine/; PERFORMED-ENGINE-v1.md and BRIEF-OWNER-WORKOUT.md above mean rebuild/m4/spec/ and rebuild/m4/ respectively.

**Fresh execution, synthetic only.** Two sequential Node stdin invocations, no suite/build.
Each command set MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York on separate PowerShell lines.
Executable: C:\Users\joeym\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe.
The driver below constructs typed completed records at the reader boundary; it does not forge an authenticated operation or run IndexedDB/Finish.
The first driver asserts identical cards from both actual runtime surfaces and unchanged input bytes for every row.
E is a diagnostic composition of the SAME factories solely to inspect internal reader results; those extra readers are not exposed by the phone facade.
All quantities/dates in this driver are synthetic; the six sessions fall within a three-week observation window.
```js
'use strict';
const assert = require('node:assert/strict');
const {createCleanInitState}=require('./rebuild/m4/workout/athlete-state.cjs');
const Accepted=require('./rebuild/m4/workout/engine-runtime.cjs');
const Host=require('./rebuild/m3/w6/host/engine-runtime-host.cjs');
const clock={today:()=> '2026-09-03',nowISO:()=> '2026-09-03T16:00:00.000Z',nowMs:()=>Date.parse('2026-09-03T16:00:00Z'),hour:()=>12};
const nativeTrendContext=req=>({...req,hard:false,rushed:false,debt:false});
const deps={clock,nativeTrendContext};
const A=Accepted.createEngineRuntime(deps),H=Host.createEngineRuntime(deps);
const E={exById:(s,id)=>s.exercises.find(e=>e.id===id),HISTORY:Accepted.absentProvider('HISTORY'),ROLLUPS:Accepted.absentProvider('ROLLUPS')};
for(const name of Accepted.COMPOSITION.modules)Object.assign(E,require('./rebuild/engine/'+name+'.cjs')(E,name==='performed'?{nativeTrendContext}:{clock,ids:{fresh:()=>{throw Error('no mint');}},drafts:{length:0,key:()=>null}}));
const base=createCleanInitState({setup:{athlete_label:'Synthetic only',split:{from:'2026-08-01',map:Object.fromEntries(Array.from({length:7},(_,i)=>[String(i),'U']))},exercises:[{id:'synthetic-press',n:'Synthetic press',mg:'chest',day:'U',sets:2,hi:12,inc:5,steps:[90,95,100,105,110,115]}],priority_muscles:[]}});
const load=value=>({value,unit:'lb'}),exact=value=>({tag:'exact',value,unit:'rep'});
function session(i,weights,reps,reserve=[2,0],edited=false){
 const start='synthetic-start-'+i, d=new Date(Date.UTC(2026,7,14+i*3)).toISOString().slice(0,10);
 const entry={profile:'earned/performed-lift/v2',start_op_id:start,lift_lineage_id:'synthetic-press',completion:{op_id:'synthetic-close-'+i,kind:'normal',status:'stored-on-this-device'},slots:weights.map((w,j)=>{
 const key=JSON.stringify(['synthetic-press',j+1]),v={load:load(w),reps:{value:reps[j],unit:'rep'},reserve:exact(reserve[j])};
 return {position:j+1,logical_set_slot:key,origin:'original',state:'performed',prescribed_load:{state:'specified',source:load(100)},prescribed_effort:{state:'specified',target:j===0?2:0},fact:{source_op_id:'synthetic-set-'+i+'-'+j,logical_set_slot:key,lift_lineage_id:'synthetic-press',included:true,source_status:'stored-on-this-device',current_status:'stored-on-this-device',edit_op_ids:edited?['synthetic-edit-'+i+'-'+j]:[],issues:[],original:edited?{...structuredClone(v),load:load(100)}:structuredClone(v),current:v}};
 })};
 return {start_op_id:start,effective:{local_date:d},record:{entries:[entry]}};
}
function run(label,sessions,{working=100}={}){
 const s=structuredClone(base);s.exercises[0].w=working;
 s.workoutFacts={profile:'earned/workout-facts/v1',source_revision:1,sessions,order:{profile:'earned/workout-order/v1',frontier:sessions.length*4,start_ids:sessions.map(x=>x.start_op_id)}};
 const before=JSON.stringify(s),a=A.genSession(s,clock.today(),{}),h=H.genSession(s,clock.today(),{});
 assert.deepEqual(h,a);const ex=s.exercises[0],card=h.ex[0],last=sessions.at(-1)?.record.entries[0];
 const out={w:card.w,reps:card.tgt,baselineAsk:!!card.baselineAsk,anchor:E.progressAnchor(ex,s),latestMatches:last?E.performedLoadMatches(last,[working,working]):null,step:E.progressStep(ex,s).add,topRun:ex.topRun??null,queue:s.queue.length};
 assert.equal(JSON.stringify(s),before);
 console.log(label+' '+JSON.stringify(out));return out;
}
run('synthetic/no-history',[]);
run('synthetic/matching-only',[session(1,[100,100],[8,7])]);
run('synthetic/heavier-only',[session(1,[110,110],[11,9])]);
run('synthetic/older-match-latest-heavier',[session(1,[100,100],[8,7]),session(2,[110,110],[11,9])]);
run('synthetic/heavier-effort2',[session(1,[100,100],[8,7]),session(2,[110,110],[11,9],[2,2])]);
run('synthetic/mixed-load-only',[session(1,[100,110],[11,9])]);
run('synthetic/corrected-heavier-only',[session(1,[110,110],[11,9],[2,0],true)]);
run('synthetic/fresh-after-two',[session(1,[100,100],[12,11]),session(2,[100,100],[12,11])],{working:null});
run('synthetic/top-once',[session(1,[100,100],[12,11])]);
run('synthetic/top-twice',[session(1,[100,100],[12,11]),session(2,[100,100],[12,11])]);
run('synthetic/six-sessions-three-weeks',Array.from({length:6},(_,i)=>session(i+1,[100,100],[12,11])));
console.log('synthetic/runtime-parity-and-input-immutability PASS');
```

Executed stdout (null topRun means absent, not a stored zero):
```text
synthetic/no-history {"w":100,"reps":[10,10],"baselineAsk":false,"anchor":[],"latestMatches":null,"step":1,"topRun":null,"queue":0}
synthetic/matching-only {"w":100,"reps":[8,8],"baselineAsk":false,"anchor":[8,7],"latestMatches":true,"step":1,"topRun":null,"queue":0}
synthetic/heavier-only {"w":100,"reps":[11,10],"baselineAsk":false,"anchor":[11,9],"latestMatches":false,"step":1,"topRun":null,"queue":0}
synthetic/older-match-latest-heavier {"w":100,"reps":[8,8],"baselineAsk":false,"anchor":[8,7],"latestMatches":false,"step":1,"topRun":null,"queue":0}
synthetic/heavier-effort2 {"w":100,"reps":[9,8],"baselineAsk":false,"anchor":[8,7],"latestMatches":false,"step":2,"topRun":null,"queue":0}
synthetic/mixed-load-only {"w":100,"reps":[11,10],"baselineAsk":false,"anchor":[11,9],"latestMatches":false,"step":1,"topRun":null,"queue":0}
synthetic/corrected-heavier-only {"w":100,"reps":[11,10],"baselineAsk":false,"anchor":[11,9],"latestMatches":false,"step":1,"topRun":null,"queue":0}
synthetic/fresh-after-two {"w":null,"reps":[0,0],"baselineAsk":true,"anchor":[12,11],"latestMatches":false,"step":1,"topRun":null,"queue":0}
synthetic/top-once {"w":100,"reps":[12,11],"baselineAsk":false,"anchor":[12,11],"latestMatches":true,"step":1,"topRun":null,"queue":0}
synthetic/top-twice {"w":100,"reps":[12,11],"baselineAsk":false,"anchor":[12,11],"latestMatches":true,"step":1,"topRun":null,"queue":0}
synthetic/six-sessions-three-weeks {"w":100,"reps":[12,11],"baselineAsk":false,"anchor":[12,11],"latestMatches":true,"step":1,"topRun":null,"queue":0}
synthetic/runtime-parity-and-input-immutability PASS
```

The second invocation reused the setup, attached the same two synthetic top sessions, and called E.deriveSighting(s,s.exercises[0]); state bytes stayed identical:
```text
synthetic/native-derived-sightings {"derived":{"topAt":100,"topRun":2},"stored":null,"queue":0}
```
It then supplied synthetic proposed-newW=110 followed by automatic-newW=105 for that same lift; section 3 quotes that output.
The derivation proves an existing reader capability, not native mint/replay qualification.

## 5. What the owner would see today

Assuming no separate programme edit or new import during those three weeks:

1. Your sets and Finish are saved, and the app can show what you actually lifted (section 3; rebuild/m3/w7-preview/today/gym-model.mjs:538-546 and :258-283).
2. For a lift with a working weight already on file, rep targets can respond to your logs, but repeatedly reaching the top does not make these phone sessions earn a heavier weight (sections 2 and 4).
3. If you log or correct a different weight, the saved result can show it while the next card still uses the old working weight (section 4; rebuild/coach/COACHING-POLICY-PHONE-REACH-ASTRA.md:9,:42).
4. For a fresh lift with no working weight, the card keeps asking you to find one even after completed sessions (section 4; rebuild/engine/today.cjs:103-112).
5. A weight or extra set already queued can still appear, and an import can change that queue, so a changed card alone does not prove your phone sessions earned it (section 2; rebuild/coach/COACHING-POLICY-PHONE-REACH-ASTRA.md:19-28,:45-48).

## What I did not verify

No physical phone, deployed/cached build, athlete data, actual three-week use, browser/IndexedDB Finish flow, second-device convergence, or full sealed-import admission was exercised here.
No conformance/private gate, whole Today step, rig187, build, installation, network action or coaching-science review was run.
The LOAD-WRITES/package/test descriptions are source evidence and ledger attribution, not fresh gate PASS claims.
The synthetic corrected entry tests the current-fact reader, not the actual edit command; the synthetic queue tests card selection, not offer generation or a tap.
The options require their own exact scope, owner authority and reviewed effect/undo/convergence rules; existing primitives do not establish those outcomes.
I searched tracked public rebuild material and bounded DECISIONS windows for the named packages, completion/adaptive/working-weight/load/sighting and memory-effect terms; no unnamed chat or other worktree was inspected.
Protected/private paths and auth files were not opened; no product file was changed, no commit was made, and no data was deleted.

## Final worktree checks

Only this new, untracked document is changed; git diff --stat has empty stdout because it excludes untracked files.
Git also warned that the global ignore file was inaccessible; the status below is the returned stdout.
ASCII and fewer than 260 lines verified; no tracked diff; git diff --check returned no findings.

```text
$ git status --porcelain
?? rebuild/coach/EARN-ON-PHONE-GAP-MAP.md
$ git diff --stat
```

