# NATIVE-LOAD specification
Author: Astra (Codex), a named specification assignment under DECISIONS:412, :569 point 3 and :613; highest effort, engine tier; PAPER ONLY, no product, engine or test byte changed
Measured head: 686010d5963a4afbf37cf54539cc23076e0e3554

## Numbered invariants
1. I1 FACTS: Log, Finish, open and replay save/read facts; only a separately accepted, exact issued choice authorizes a native load effect.
2. I2 BASIS: a yes binds lineage, source coverage, completion/set/edit identities, plan, technique, load basis, offered vector and reason; changed evidence refuses, never substitutes.
3. I3 LANDING: adoption establishes a working load by name; an earn queues a target, lands only on its later qualified debut Close, and never changes an active capture.
4. I4 TRUTH: accepted response plus issuance and later immutable Close are durable truth; every projection reconstructs at source frontiers; semantic evidence is spent once across imports/devices.
5. I5 HONEST INPUT: reuse engine predicates unchanged, keep rich facts, and refuse unresolved inputs by name; no invented training number or scalar approximation.
6. I6 PRESERVATION: preserve imported legacy decisions and the separately accepted repair; N01 inherits EPP-R1 through EPP-R7.
7. I7 DELIVERY: accepted runtime, browser runtime and durable entry must agree through the real next card and later landing; a receipt alone proves neither.

## Scope and evidence convention
This is proposed normative paper, not a build authorization. Joe has chosen timing, not A/B/C or engine scope. D is excluded.
READ authority, selected individually from refs/remotes/origin/rebuild/t2-client-core: rebuild/DECISIONS.md:89,590,615,631,633,634. No fetch. :631 places earning after S9 and the look, before trial; :634 leaves the option unanswered. Repair acceptance and S9 completion are NOT established by this worktree.
READ whole first: rebuild/coach/EARN-ON-PHONE-OPTIONS.md, EARN-ON-PHONE-OPTIONS-CHECK.md and EARN-ON-PHONE-GAP-MAP.md. Their earlier executions are not this paper's measurements. NEXT.md and rebuild/ROADMAP.md were read; the named assignment and current rulings supersede historical roles/queues.
All paths below are repository-relative. E/ means rebuild/engine/; W/ means rebuild/m4/workout/; L/ means rebuild/m3/w6/local/; T/ means rebuild/m3/w7-preview/today/. READ citations refer to the measured head unless explicitly labeled remote authority. PROPOSED contracts below are not existing behavior or measured PASS claims.
Every fixture value, date, count of synthetic sessions, load, rep, rating and numerical fixture output is INVENTED. Source constants, line numbers, file/row counts and hashes are identified separately. No delivery duration is estimated.
Probe folder P: C:/Users/joeym/AppData/Local/Temp/native-load-spec-astra-686010d5-20260921. Retain it for the disagreeing reviewer; the probe index below names its files.

## A. What exists at this head
| READ boundary | Existing behavior; consequence |
|---|---|
| W/engine-runtime.cjs:38-40,53-78 | Privately composes twelve factories; exposes genSession(s,iso,slp), rirPlan(s,ex,slp), dayWeather(s,iso), cleanAtDate(s,iso), sessionMembership(s,iso). No native load evaluator/transition; completeSession and earnWalk do not escape. |
| rebuild/m3/w6/host/engine-runtime-host.cjs:44-78,97-122 | Same five forwarders, literal browser requires; missing history providers throw. A factory in the composition is not a callable capability. |
| E/performed.cjs:26-32,84-99,145-187,225-253 | Typed original-position prefix, current actual loads and original/current edit provenance; exact reserve is only 0/1/2, at_least is 3. Causal native order plus explicit legacy mapping; no date-keyed native log. |
| E/progression.cjs:80-85,359-370,436-491,553-604,608-653 | Exact load matching, next rung/increment, original progression prefix, canonical noise and sightings. deriveSighting catches errors and may return caches (:649): a caller must establish input validity before treating its answer as evidence. |
| E/earn.cjs:11-100; E/writers.cjs:234-239,261-268,338-349 | Canonical scalar candidate walk and vector translation; governor is inline, not reusable as a reader; old adoption and old landing are separate writer branches. Native Finish reaches none of them. |

| READ response boundary | What exists; missing join |
|---|---|
| rebuild/client/index.cjs:93-100,349-371 | respond(proposalId,answer,issuance) atomically records class plan/kind proposal-response; issuance fields producer/body/reason/revision/source/moment, digest comparison. This checks shape/digest, not engine execution or source currentness. A caller can hash its own body. |
| rebuild/client/index.cjs:135-139,343 | Accepted-plan cache requires a consent transaction; generic decision also records an answer. Neither is a native programme-effect fold. Do not expose generic decision as this feature. |
| rebuild/m3/w6/t2-stage.cjs:9,55-87 | Command set omits respond; generic dispatch supplies one args object, incompatible with respond's positional signature. |
| L/local-client.mjs:49,188-228,347-369 | Same omission; real load/stage/validate/commit bridge with lease, integrity and sidecar checks. Add a guarded capability, not an unguarded command-name entry. |

| READ import boundary | Existing admission |
|---|---|
| rebuild/m4/import/replay-core.cjs:39-79 | Reproduces migrate and optional mergeState on source copies; merge can mint legacy joint earns. Retain this legacy behavior. Never merge a native-derived programme cache back into source material. |
| Same :168-188,248-250 | Recognized native operations are observations only; unknown accepted engine context refuses. No accepted native-load response family. |
| L/source-admission.mjs:497-526,596-743,769-801 | Shipped local admission dispatches families, reconstructs typed workout facts and correspondence; unknown plan operations get LOCAL_SOURCE_EFFECT_UNMAPPED. Both this path and portable replay need the SAME new family. |
| W/source-projection.cjs:35-75; L/today-bindings.mjs:445-508 | Registered projection identity and exact basis gate capture; local imported order attaches at the engine seam. A new fold must run before registration, preserving this ownership and baseline reference relationship. |

| READ Finish to durable put | Existing output |
|---|---|
| T/gym-model.mjs:538-546 | prepareWorkoutContinuation, then executeResumedWorkout(close, normal, Start/slot parents); reports success only after acknowledged. |
| rebuild/m3/w6/public-client.mjs:371-389 | Requires live prepared resume context, matching Start, allowed action, no unfinished slot for normal Close; calls bridge.execute('workout',args). |
| W/commands.cjs:42-72 | Produces class session/kind session-close, completion_kind and Start identity; no load authorization. |
| rebuild/m3/w6/bridge.mjs:17-49 | Loads generation, stages and verifies prepared batch; publishes Saved only after repository.commit returns. |
| rebuild/m3/w6/repository.mjs:215-261,290-307 | Transaction checks expected head and synchronous validator before store.put(record,'active'); completion, not a request event, acknowledges durability. |

MEASURED P/readers.cjs at this head: both actual runtime facades agree and inputs remain unchanged in every retained driver case. INVENTED two tops at 100 leave card 100, no stored topRun and empty queue; fresh load remains null/baselineAsk. This is a reader-boundary drive, not Finish/IndexedDB proof.
MEASURED P/seams.cjs: INVENTED deriveSighting returns topRun 2 without writing it; pre-counting the current completion gives an unjustified ordinary candidate. Existing vector [100,95] produces canonical candidate vectors [110,105] and [105,100]. The typed bound remains at_least 3 while a scratch coercion token reaches canonical earnWalk and prints "at least 3". No durable yes or new effect was implemented.

## B. Common design: closed inputs, one source fold
The common capability is named earned/native-load/v1. It is enabled only by the owner's scoped word, the accepted brief and selected sealed engine revision. All A/B/C use it. No Finish side effect, automatic acceptance, numerical caller override, mutable top-run store or duplicate sessionLog is added.
The contract uses strict plain JSON data: own enumerable data properties only, no getters/custom prototypes/extras, finite new load quantities with units, no negative zero, no omitted array positions. Verbatim engine candidate and prior field images retain their existing formats. Keep null distinct from absent. Operation authentication uses existing codecs; semantic equality compares the validated full data, not merely the client's shortened proposal digest (I2/I4; cutting this admits a substituted body).

### Functions and capability custody
| PROPOSED exact signature | Input/output, clause and cut counterexample |
|---|---|
| evaluateNativeLoad(state, request) -> Evaluation | FC01 pure reader. request={lift_lineage_id,completion_op_id,intent,basis}; intent='check' or {compensate:spend_id}. state is owned engine state plus registered workoutFacts at basis; host alone constructs it. Returns {profile:'earned/native-load/v1',status:'offer'|'refused',basis,offers,refusal}; offers=[] on refusal, refusal=null on success. Never modifies state. I1/I2/I5: without it the UI invents a load. |
| applyNativeLoadDecision(state, decision, context) -> Transition | FC01 pure transition, no disk and no current clock. context={event,basis,spent,authority,completion}; event='accept'|'close'; completion=null except close. decision is the exact validated issuance body below, plus its accepted response reference in context.authority. Returns {status:'applied'|'unchanged'|'refused',state,effect,refusal}; refused/unchanged retain input state by value. I3/I4: without it a receipt never reaches the card. |
| updateOpenerHold(ex,en,push) -> undefined | Canonical existing E/writers.cjs:234-239, PURE MOVE into a helper in that SAME factory, exported only to private E. Existing completeSession calls it once at the original site. Native evaluator/fold uses it on isolated ex only. I5: cutting reuse creates a second governor or permanent hold. This additional engine hunk needs Joe's word; it is not the separate repair. |
| nativeEffort(value) -> primitive or private token | FC01 internal adapter. Exact typed value returns its existing number; unavailable terminal returns null; unavailable opener refuses. A registered bound becomes a frozen token: numeric coercion returns its retained lower bound, string coercion calls effortText; never JSON-serialize or return the token. Use effortIs for named decisions and prove token comparisons agree. I5: cutting it invents exact 3 or discards a valid bound. |
| foldNativeLoad({base,generation,frontier,workoutFacts,sourceCoverage,engine}) -> Fold | FC03 pure shared validator/orchestrator; base is immutable admitted source/clean init, not yesterday's folded state. Returns {status:'ready'|'refused',state,effects,spent,issues,coverage}; refused state=null for new prescription, facts retained. I4/I7: without it reopen loses the accepted target or applies it twice. |
| createNativeLoadHost(scope) -> {project,check,respond,close} | FC08 trusted installation binding. project() -> authenticated Fold plus source basis; check({lift_lineage_id,completion_op_id,intent}) -> immutable display offer plus opaque handle or refusal; respond({handle,proposal_id,answer}) -> durable result; answer='accept'|'decline'; close() invalidates held handles. I1/I2/I7: cutting it leaves a naked forged/stale issuance path or Today's old weight beside the gym's new one. |
No further exported helper, effect store, file or API is necessary to these invariants. Internal decomposition must cite one of them and its counterexample in the builder report.

### Basis and exact durable decision shape
Use existing schema_version 1 plan/proposal-response envelope and existing respond, not a second consent record. Accept payload is exactly {proposal_id,answer:'accept',issuance}. Native decline/Not now and cancel only dismiss the view, writing NO operation and spending nothing; the saved workout remains. A later manual check can offer again, with a new yes required. This deliberately avoids persisting an unclassifiable naked decline: current respond cannot carry issuance on decline (index.cjs:356-369), and the unaccepted offer has no durable producer record. Historical responses retain their existing admission rules.
Issuance is exactly {producer,body,reason,revision,source,moment}. producer='earned/native-load/v1'; body=Decision below; reason is the engine-produced native explanation; revision identifies the sealed producer bytes; source identifies the authenticated source cut; moment is issuance time, retained for audit, never priority or expiry. Reuse proposalDigest on this exact constructed field order. Host-held issuance is immutable; the UI receives no editable issuance input.
| Decision field (all required; null where stated) | Exact meaning, invariant; counterexample if cut |
|---|---|
| profile | 'earned/native-load-decision/v1'; I4, otherwise an unrelated proposal is replayed as load. |
| kind | 'adopt-baseline'|'adopt-observed'|'earn'|'compensate'; I3, otherwise accepting an observed load is falsely reported as earning. |
| lift_lineage_id | Stable lineage resolved through authenticated lift correspondence, never display name; I2/I4, rename/import spends again. |
| basis | Basis record below; I2, later edits accept an old target. |
| evidence | Ordered array of {start:Ref,close:Ref,sets:[{slot,position,origin,state,original,edits,current}]} from the exact completed input. slot is logical_set_slot; position/origin/state are the typed slot's fields; original is its fact/skip Ref or null when absent; edits is ordered [Ref]; current is typed resolved load/reps/reserve or null. Originals remain in ops. Include every original and added slot; no compaction. I2/I5, a corrected/missing set becomes a different unnoticed offer. |
| base_load | {scalar:LoadOrNull,vector:[LoadOrNull],fields:FieldImage}; FieldImage has exactly w,wSets,wAt,last,lastMeta,own,std,topAt,topRun, each {present:boolean,value}, with value=null when absent. Load is existing {value,unit:'lb'} or typed configuration, never an inferred magnitude; vector null positions mean not prescribed. I3/I4, replay increments the already advanced vector or cannot undo absence. |
| target_load | {scalar:LoadOrNull,vector:[LoadOrNull]}; adoption/earn require numeric Load throughout, exactly observed/canonical and complete for captured set count. Only compensation may restore original null/unprescribed positions, with the target effect's prior FieldImage. I2/I3, one set advances while another remains old or undo invents zero. |
| candidate | null for adoption/compensation; for earn {kind:'debut',newW,newWSets,state,t,gate,rule} copied from the selected scratch candidate before native consent wording. I5, a caller substitutes a different rung or loses which alternative was chosen. |
| reason_key | 'baseline'|'observed-load'|'canonical-earn'|'compensation'; numerical explanation lives in issuance.reason, bound with candidate/typed evidence. I2/I3, one-rung and two-rung alternatives share an ambiguous yes. |
| spend_id | Semantic tuple encoded as a string, defined below; I4, different operation IDs spend the same work twice. |
| consumes | Canonically sorted unique semantic evidence roots; I4, overlapping but unequal evidence arrays mint two earns. |
| compensates | null except compensation: prior spend_id; I4, undo revives the old yes or erases the workout. |
Basis is exactly {athlete_id,source,coverage,order,plan,technique,load_basis,effect_frontier}.
source is the existing full authenticated source basis (null-import W/log_digest/selection_id, or the qualified local source basis and order map); do not coerce either into the other. coverage is the sorted array of {op_id,commitment,disposition,source_member} for the complete consumed cut; source_member=null for original native ops, otherwise the immutable mapped member reference. Include the accepted prefix and authenticated local tail, edits, decisions and relevant context facts, never just W.
order is the verified workout order plus causal cut identities; plan is {plan_basis,input_basis,programme_sha256,capture_sha256,structural_queue_sha256}; technique is the exact reset-fork/era basis returned by existing readers; load_basis is {authority_refs,tenure_start,sets,prefix,hi,steps,inc,w,wSets}, preserving missing fields explicitly; effect_frontier is the sorted previously folded semantic effect IDs and their response/close references.
Every basis field holds I2/I4: remove source/coverage/order and a late or overlapping log can pass; remove plan/capture/queue and a changed prescription can pass; remove technique/load and old-era work earns at a new load; remove effect_frontier and an old yes reappears. Ref={op_id,commitment}; a numeric receipt position alone is never a Ref.
spend_id is the canonical encoding of ['native-load',lift lineage root,load authority root,technique basis,consumes]. It excludes device, response ID, issuance moment, transport revision, alias name and offered alternative. Semantic roots identify the original completed Start/lift/Close, or a PROVEN equivalent immutable source member, never its edit ID. Editing a fact or importing it under another name cannot replenish it. If equivalence is unproved, refuse SOURCE_OVERLAP; do not deduplicate equal-looking weights.
For earning, consumes contains all unspent qualifying completion roots that support this run and any completion used as the noise comparator; the current completion occurs once. Adoption consumes its observed completion for load authorization and starts a new tenure after it. Compensation consumes no new training; it permanently cancels the targeted effect without refunding its evidence. Check intersection as well as spend_id equality: overlapping support with different IDs is still a conflict.
Transition.effect is null on refusal; otherwise exactly {kind,spend_id,response_refs,close_ref,base_load,target_load}. Projected kinds are 'adopted' (I3: missing baseline), 'queued' (I3: premature w), 'landed' (I3: no later working load) and 'compensated' (I4: old yes revived); they are NOT separately durable records. close_ref=null unless landed. context.authority={response_refs,issuance,source_cut} is supplied only after FC03 verifies those actual responses; context.spent is the derived array of {spend_id,consumes,response_refs,close_ref,cancelled_by}, with nullable close_ref/cancelled_by. context.completion is null or {start:Ref,close:Ref,capture,entry,source_basis}, using the immutable Start capture and typed completed lift. These transport fields hold I2-I4; removing them loses the link from consent to the exact later completion. Fold.effects/spent are canonical arrays of those same records, not independently loaded caches.

### Evaluation algorithm, including the scalar seam
1. Authenticate/resolve outside FC01; inside, validate shape, typed entries, exact claimed cut and unique lift. Require a normal immutable Close and all original slots resolved. Use performedHistoryRows/order; keep date solely for unchanged engine date/era readers. Any source/order/basis uncertainty refuses before deriveSighting can catch it and return cached counters.
2. Resolve the current authorised plan and the completed Start's capture. If deliberate load, set-count, technique or governing plan identity changed since that completion, refuse the applicable code below; an older completion cannot silently replace a newer athlete choice. An equipment-only steps/inc change may be used by a NEW check when load/vector/count/window/technique remain identical; it invalidates any already issued offer and lets unchanged nextLoad price banked work (N18). Preserve log/edit facts. Existing active legacy debut/unlock/own/reclaim/ladder/pendingThird branches are not ordinary native earning: return LEGACY_PENDING with the named queue/branch reference; their legacy rules remain intact. A Close just used to land a native debut returns DEBUT_LANDED, not a second earn from that same invocation; its facts remain available to later unchanged readers.
3. Obtain actual original-position vector with typed readers; performedLine defines prefix/gaps/added positions; performedLoadMatches compares it against the full authorised vector. First numeric baseline or different numeric observed load is an ADOPTION choice, never passed to earnWalk under the card's weight. Adoption requires a complete usable observed original vector; reps/effort are retained, not required to top the window. Do not infer equipment rungs from an edit.
4. For earning at the matching vector, build two ISOLATED views of the same source cut: through current completion for error/read context, and immediately before it for run, prior comparable line and eraFresh. Use the contiguous native suffix AFTER the last semantic spend/authority boundary for sightings, not a filter deleting individual off-load/off-top rows. Keep full typed history for canonical error estimation, and retain legacy baseline/order identity. Do not forge dated EARNED feed rows as native spend markers. Reset scratch topAt/topRun to null/0 before deriving, so swallowed errors cannot reuse imported caches; validated legacy spend receipts still retain their original meaning. If a date-sensitive legacy reader cannot represent the witnessed source cut, refuse ORDER_RULE_UNREPRESENTABLE rather than changing dates or reimplementing its rule.
5. Use deriveSighting on the before-current view. Use progressionSetCount and atTopOfWindow on the proper completion plan/prefix, eraFresh and the typed actual-load/pair readers unchanged. A non-top inside the progression prefix breaks the run. Do not count a short line as a shorter prescription. No second threshold test, error formula or next-rung arithmetic is introduced. Ensure comparison is the immediately prior comparable same-tenure line; an intervening off-load line ends tenure.
6. Derive the governor by replaying updateOpenerHold on an isolated lift seeded with the admitted legacy governor history, then the ordered native original openers. Use known exact/bounded values only; unknown does not become zero. Fold projects only the resulting holdFlag into engine state; tokens and scratch rirHist never escape. Do not reuse rirPlan's majority-of-openers heuristic as the two-grind governor: those are different rules (E/writers.cjs:803,816-838 versus :234-239).
7. If top and not eraFresh, call unchanged earnWalk ONCE on scratch, with pre-current run, actual scalar basis, performedLine reps, original-position effort adapter and the comparable prior line. Keep full typed facts attached for typicalError; keep original terminal effort, excluding added positions. Return every resulting eligible native candidate as a separate choice, including ordinary DEBUT as an OFFER requiring yes. No scratch queue, receipt, cache or automatic effect reaches state. If nextLoad is null, retain sightings in facts and return NO_NEXT_LOAD; later equipment information can make the same unspent work eligible without re-logging.
8. Candidate vector is earnWalk.newWSets verbatim, or its scalar repeated across the unchanged original layout when no vector exists. Validate finite quantities, length and full correspondence; refuse instead of repairing a vector. Display the chosen full vector and native yes-required explanation. Old scratch rule/gate text claiming "automatically" stays evidence only and must not be shown as this native feature's promise.
9. Native explanation is produced in FC01 from the canonical candidate and typed facts. It names baseline/adoption or earning, source workouts, current/target load per set and the canonical reason; preserves "at least"/unknown. It adds no training claim. The displayed reason and vector are part of the issuance checked at yes.
10. Unknown opener refuses EFFORT_UNRESOLVED. Unknown/removed terminal cannot justify early/two-rung branches; canonical null terminal may still yield a confirmed ordinary candidate with a known honest opener. A valid at_least 3 token preserves all registered predicate results and wording; any new effort tag/coercion use outside the measured comparison set refuses EFFORT_BRIDGE_UNPROVEN until separately proved. No exact 3 object is constructed.

### Full vector obligation and SEPARATE LIMITED-SCALAR slice
Specified positive vector case: established numeric scalar w with a complete wSets, matching actual vector and unchanged set basis; canonical earnWalk already translates every position. N11 must prove the translated vector is captured and later lands, not merely returned as an offer.
UNRESOLVED GENERAL PART: a fresh unequal vector or a different unequal observed shape has no existing unique scalar-w adoption rule. E/writers.cjs:338-349 takes one scalar; choosing opener, mean, maximum or a shift of the old shape would add policy or discard actual facts. Return VECTOR_ADOPTION_UNDEFINED, keep all facts and show the limitation. This part STOPS pending Q1; it is not a general-vector delivery claim.
LIMITED-SCALAR is a separate deliverable Joe must accept BY THAT NAME if the general part remains unresolved: numeric equal-load original sets only for adoption AND earning; unchanged set-count/technique basis; typed effort still supported as above. Display "Weight checks currently require the same weight on every original set. Your different weights are saved." Unequal vectors return SCALAR_SLICE_ONLY, never average them. General known-vector earning remains separately specified and cannot be advertised as shipped by a scalar-only pass.
Changed original set counts whose historical count cannot be represented by unchanged progressionSetCount/_setsAtTime refuse SET_COUNT_BASIS_UNPROVEN. Added sets remain recorded and outside the original prefix; a skipped/removed original cannot be replaced by one. Date-based eraFresh cannot distinguish successive same-day Starts in a new technique era: P/seams.cjs measures this; return ERA_ORDER_BRIDGE_UNPROVEN, never fabricate different dates. Same-day Starts outside that ambiguity remain distinct and follow proven source order.

### Apply and fold
At accept, the host re-evaluates against the freshly loaded generation, checks the ENTIRE issued body/reason/producer and semantic source basis, then commits that issuance plus answer. Stale means STALE_OFFER; even a newly valid larger/smaller number requires another displayed offer and yes. Transport-only revision changes may pass only if consumed semantic coverage/basis are exactly equal and the commit fences bind the new generation.
applyNativeLoadDecision accepts no caller quantities. At accept/adoption it copies target scalar/vector into w/wSets, stamps wAt from the accepted issuance moment, clears native run/anchor caches and begins a new authority/tenure. The equipment list is not rewritten: observing a weight is not a new equipment-setting decision. This is a declared native consent/adoption transition, not old completeSession recast as facts.
At accept/earn it leaves w/wSets and active captures unchanged and appends ONE native-owned queue item: {id:spend_id,kind:'debut',exId,newW,newWSets,state:'DEBUT',done:false,t,gate,rule,native_load_spend:spend_id}. newW/newWSets equal the selected candidate; t/gate/rule use the issued native explanation. No coApproved is invented. Reject an existing unresolved same-lift structural entry; preserve imported queue entries and their order. Native entries follow established causal acceptance order, with independent equal-priority entries canonically ordered by semantic ID for deterministic presentation, not asserted chronology.
For each later Close, reconstruct programme at that Start's source frontier, use existing pickStructural/genSession and the captured prescription to identify the SAME selected native queue entry. Require acceptance before Start by proven causality; normal Close, complete original positions, actual vector exactly matching accepted target, same lift/layout/technique, no unresolved edits and no intervening superseding plan authority. An already-started card cannot be relabeled a debut. A rep miss at the exact accepted load does not invent a new earning threshold: landing requires completed original work, not another top-window earn.
At qualified landing, mark that exact queue item done/ESTABLISH; assign its stored target w/wSets, wAt from the immutable Close effective tuple, last from performedLine, own=false,std=null, and close-bound native authority. Do not call completeSession or select by lift alone. No pendingThird/volume/reset transition is smuggled in. Fold deduplicates the landing by (spend_id,Close Ref); a second candidate Close cannot establish the same debut again.
At compensation, evaluator offers the prior field image only if no later Start captured the accepted effect, no landing and no intervening load/technique/plan decision depends on it. On its own yes, restore that image as a NEW authority, retire only the targeted native queue entry and keep its spend tombstone in the reconstructed index. After subsequent training/landing return COMPENSATION_DESCENDANTS; a fresh plan choice belongs to its owning writer. No fact or old response is deleted.
FC03 runs on EVERY current projection: check; pre-commit respond; post-commit refresh; new capture; cold boot/cache rebuild; local source admission/reopen/rollback; portable import replay. It authenticates operation membership via the existing local identity or accepted source proof, validates each historical issuance at its ORIGINAL cut using the named producer revision, and folds accepted effects forward. An unavailable historical revision refuses PRODUCER_REVISION_UNAVAILABLE; it never re-prices an old yes under new code.
Read initial admitted source programme/clean init, immutable accepted and authenticated local operations, disposition/rejection records, source correspondence/order, typed workout facts at each cut, and native response issuances. Do not read a derived sidecar as authority. Reconstruct load/vector/authority, native queue and landing, governor view, spend/cancellation index and issues together. No new persisted topRun, effect receipt collection or plan cache is authoritative.
Distinct-lift independent effects commute; same-lift dependencies follow witnessed causal/source order. Identical semantic decisions with different response IDs coalesce to one effect; retain all response refs. Incompatible overlapping spends, accepts versus incomparable plan edits, or ambiguous source overlap produce EFFECT_CONFLICT/SOURCE_OVERLAP, never a last-clock winner. Keep accepted history and facts; mark affected new prescription unavailable and report both refs. Other fact saves remain available. A later explicit plan authority wins only when causality and ownership prove it supersedes the old effect; replay cannot override it.
After an accepted basis is corrected, reproduce the acceptance from its historical original cut, retain its effect/history and mark BASIS_REPAIR_REQUIRED; never silently change the issued vector. Refuse further load authorization/landing on that disputed basis until an eligible compensating choice or the owning plan repair resolves it. A cache showing the old accepted load is labeled disputed, not current advice. This is the N08 outcome, not a hidden rollback.

### Guarded durable command and import family
FC08 owns check handles and the installation's existing session/observation guards. It loads/authenticates through the same host bindings used for workout history; constructs the fold and evaluation from that generation; retains complete issuance and basis behind an opaque identity handle. Refuse detached host, wrong athlete/source, cloned handle or wrong proposal ID before staging.
FC07 adds optional trusted native-load capability configuration at construction, not an option supplied to execute(). FC06 handles 'respond' explicitly as client.respond(args.proposalId,'accept',ownedIssuance), after the trusted capability validates against that exact staged generation; only accept reaches this command. Host decline returns {acknowledged:false,dismissed:true} without staging. Without that capability, 'respond' refuses NATIVE_LOAD_CAPABILITY_REQUIRED. Merely adding respond to either generic command Set is a defect.
FC07 extends the existing pending-stage and synchronous validateCommit chain with the evaluated basis, namespace, session/observation epochs, selected source and exact prepared response payload. The bridge's generation compare-and-swap and validator immediately before put enforce currentness. A retry revalidates the same issuance; it cannot replace it. No async work inside the IDB validator; all expensive proof runs before it with its result fenced to this generation. Existing lease/integrity/batch checks remain conjuncts, never replaced.
Return acknowledged:true only after the existing durable commit. If the acknowledgement is lost, read the authenticated operation log for that exact issuance/spend; report already-saved only if found and folded. Do not mint another response to simulate recovery. UI projection failure after a saved response says "Your choice is saved; the next card could not be checked" and retains its durable status.
FC08 decorates the existing null registrar injected at L/today-bindings.mjs:500: register({generation,state,workoutFacts}) runs FC03 first and passes its reconstructed state to the real registrar; workoutInput remains the real registrar's identity-gated reader. Imported source family folds before its registered accepted_state is published. Both new captures and Today read that same projection. Do not mutate a captured Start or bypass W/engine-capture.cjs's correspondence guard. This avoids a new workout-host or source-projection file hunk.
FA02's refresh obtains host.project(), hands the authenticated result to the existing model.adoptBasis, then rebases the gym host for NEW preparation and refreshes its summary; an existing Start still resumes its original capture. Run this after the existing adoption chain as well as after each relevant save/reopen, with stale-generation refusal rather than painting a stale programme. READ T/today-app.cjs:2490-2502 already invokes workout.refresh after adoption; T/today-model.cjs:412-426 supplies adoptBasis. The fold always reloads its immutable source base, never the state just handed to adoptBasis. This join is required in A, B and C, independent of their entry timing.
The shared family is 'native-load', under existing class plan/proposal-response, owned by its producer/profile or a matching retained native proposal ID; it recognizes malformed native accepts and refuses NATIVE_LOAD_RECORD_INVALID rather than dropping them. Unrelated/unclassifiable plan records retain their existing refusal, not a catch-all no-effect admission. Missing issuance on a native accept is never consent; this feature creates no durable naked decline that would become unmapped on import.
FC09 dispatches this family before its unknown-plan catch at :526 and invokes FC03 after facts/correspondence are available, at each source cut. FC10 adds the same dispatch before ACCEPTED_ENGINE_CONTEXT_UNMAPPED and folds before state/coverage hashes/registration. Output coverage records response refs, issue cut, consumes and landing refs; no observation-only ID is reclassified as an acceptance. Reject effect-bearing native caches masquerading as legacy input when coverage is unproved (SOURCE_OVERLAP).
Repeated import, both delivery orders and rollback/reopen must reproduce the same source fold or the same named conflict. The current separate refusal of a second different history source is not widened by this specification. Tests may exercise synthetic portable overlap without shipping a second-source feature. No hosted transport, new import format, coach or second-device UI is commissioned.

### Runtime parity and one alternative
FC04 appends native-load after writers in MODULES; adds evaluateNativeLoad/applyNativeLoadDecision forwarders and changes COMPOSITION's reader-only claim to a pure native-load capability. FC05 adds the identical ordered literal require and exact forwarders. Neither exposes E, earnWalk, updateOpenerHold, completeSession, seed, migrate or merge. The new transition returns a copy; it is not a disk writer.
N20 drives both actual compositions with the same typed inputs through offer/refusal, adoption, accepted target, structural capture and later landing; compare semantic values and unchanged input bytes. Also drive the shipped bundle's host capability. Equality of two refusals or identical module-name lists is insufficient.
ALTERNATIVE: add a durable derived programme/effect snapshot beside each accepted response. It might shorten replay reads, but creates a second truth requiring atomic snapshot/response/Close writes, invalidation on every edit/import and new recovery proof. No latency measurement justifies it. Reject it under I4; the existing disposable derived cache may accelerate only after equality with the same immutable source fold is established.

### Named outcomes and edge table
All new refusal suffixes below expand to NATIVE_LOAD_<suffix>. Refusal is exactly {code,refs,field}; refs are authenticated operation/source references or [], field is the implicated input field or null. Evaluator failure leaves offers=[]; host write failure leaves acknowledged:false. Existing storage/lease/source-reader codes retain their original names and copy. An unexpected exception maps to NATIVE_LOAD_EVALUATION_FAILED, without leaking its message or facts.
| Input or event | Required outcome, facts retained | Clause |
|---|---|---|
| Unowned handle, wrong athlete/source or caller issuance | CAPABILITY_REQUIRED or SCOPE_MISMATCH; invalid record -> RECORD_INVALID; unknown lift -> LIFT_UNRESOLVED. | I1/I2 |
| No normal completed workout; unresolved original slot | COMPLETION_REQUIRED or PREFIX_UNRESOLVED; no numeric baseline. | I1/I5 |
| Below window / incomplete top run | WINDOW_NOT_TOP or PROVISIONAL; no effect; retain legitimate unspent sightings. | I5 |
| Hot opener/held and no canonical exception | HELD_OR_HOT; legitimate canonical tapped exception remains separately offerable. | I5 |
| Missing next equipment load | NO_NEXT_LOAD; no invented increment; retained sightings can later qualify. | I5 |
| Changed working-weight/plan authority | STALE_OFFER at confirm; PLAN_CHANGED at new check against old completion; never overwrite the new choice. | I2/I4 |
| Edit to logged load/reps before accept | STALE_OFFER; explicit new check re-evaluates current facts with original and edit refs. | I2/I5 |
| Correction after accept | BASIS_REPAIR_REQUIRED; accepted historical choice remains recorded, new advice withheld until explicit repair. | I2/I4 |
| Different actual scalar / fresh scalar | Separate adopt-observed / adopt-baseline offer; neither earns; only its yes establishes load. | I3/I5 |
| Unequal first/different shape | VECTOR_ADOPTION_UNDEFINED in general contract; SCALAR_SLICE_ONLY in the separately approved limited slice. | I5 |
| Changed original count not representable by existing historical count reader | SET_COUNT_BASIS_UNPROVEN; never pad, compact, average or count added work as original. | I5 |
| Unknown/bounded effort | EFFORT_UNRESOLVED/EFFORT_BRIDGE_UNPROVEN where needed; ordinary exact/bounded supported cases keep typed meaning. | I5 |
| Backdated or same-day work | Follow witnessed order and captured basis; if date-sensitive rule cannot represent it, ORDER_RULE_UNREPRESENTABLE or ERA_ORDER_BRIDGE_UNPROVEN. No date sorting repair. | I2/I5 |
| Duplicate root with compatible equivalent accept | Unchanged semantic effect, union response refs; already-saved recovery needs a real durable response. | I4 |
| Incompatible accepts, overlapping evidence or unmapped source alias | EFFECT_CONFLICT or SOURCE_OVERLAP; affected new prescription unavailable; retain every operation. | I4 |
| Existing legacy structural/special branch | LEGACY_PENDING with reference; do not supersede it, auto-tap it or silently make native earning own its landing. | I6 |
| New plan supersedes accepted native target | Keep historical yes, retire its projected target only with proved later authority; incomparable edit -> EFFECT_CONFLICT. | I2/I4 |
| Later Close at different vector, wrong capture or edited debut | DEBUT_BASIS_UNPROVEN; Close stays saved, target stays pending/disputed, w does not land. | I3/I5 |
| Just-qualified native debut Close | DEBUT_LANDED reports its existing landing, no new offer from that invocation; later readers keep its observations. | I3/I5 |
| Decline/cancel/failed put | No native authorisation, no spend; fact save remains true. | I1/I4 |
| Undo with descendant training/plan | COMPENSATION_DESCENDANTS; before such descendants offer exact compensating choice with its own yes. | I4 |
| Missing historical engine/cut | PRODUCER_REVISION_UNAVAILABLE or SOURCE_FRONTIER_UNPROVEN; no reinterpretation under current code. | I2/I4 |
| Other typed/capture correspondence failure | Preserve existing named reader refusal; qualify it in the UI as a weight-check limitation, never a lost-workout message. | I5/I7 |

## C. Files, custody and exact engine scope
READ/MEASURED S8 key membership: P/custody.json, enumerated from rebuild/m4/spec/acceptance-s8-real-shape.json at this head. P means product; X means execution pin; F means absent from BOTH, not free of other contracts. New file line numbers do not exist yet. The selected child must remeasure against its actual parent. Classes below are PROPOSED hunk classes, not changes made here.
| ID and exact path | Engine; S8 custody | Hunk class, scope, invariant and counterexample if cut |
|---|---|---|
| FC01 rebuild/engine/native-load.cjs | YES; new F | NEW LOGIC: evaluate/apply, typed scalar bridge, native explanation, strict body validation; I1-I5; missing native load authority/transition. No copied earn/noise/tenure rule. |
| FC03 rebuild/m4/workout/native-load-effects.cjs | no; new F | NEW LOGIC: one shared source-frontier fold/family, source validation, spend/conflict/close orchestration; I2/I4/I7; reload/import loses or doubles yes. |
| FC04 rebuild/m4/workout/engine-runtime.cjs | no; P:996 | DECLARED STATEMENT REWRITE: module/exposed lists, two forwarders and truthful capability description; I7; accepted runtime lacks engine capability. |
| FC05 rebuild/m3/w6/host/engine-runtime-host.cjs | no; P:436 | DECLARED STATEMENT REWRITE: literal native-load require, matching lists/forwarders; I7; phone differs from accepted runtime. |
| FC06 rebuild/m3/w6/t2-stage.cjs | no; F | NEW LOGIC: guarded positional respond dispatch and exact preimage proof; I1/I2; valid-looking caller body reaches disk. |
| FC07 rebuild/m3/w6/local/local-client.mjs | no; P:506 | NEW LOGIC: trusted capability construction, staged command and synchronous commit guards; I1/I2/I4; stale yes survives a raced commit. |
| FC08 rebuild/m3/w6/local/today-bindings.mjs | no; P:531 | NEW LOGIC: authenticated host/project, source fold before registrar, scope/epoch wiring and runtime forwards; I2/I4/I7; true facts feed a stale programme or raw issuance. |
| FC09 rebuild/m3/w6/local/source-admission.mjs | no; P:521 | NEW LOGIC: shared family before :526, historical cuts and folded output before :781; I4/I7; import either refuses every yes or drops it. |
| FC10 rebuild/m4/import/replay-core.cjs | no; P:846 | NEW LOGIC: same response family beside :178-182 and fold before :246; I4/I6; portable replay mints consent from observations or loses accepted effects. |
| FC11 rebuild/m3/w7-preview/today/build.mjs | no; P:686 | NO EDIT REQUIRED on measured design. READ :420-446 permits additional reachable first-party modules. FC12/N20 checks their real bundled reach; existing build emits changed input hashes. An extra required-input string holds no additional clause. Reassess only if actual parent changes this contract. |
| FC12 rebuild/m4/spec/native-load-options.test.cjs | no; new F | NEW LOGIC: N01-N20, exact child carriers of changed surface assertions and scoped mutants; I1-I7; source-string/refusal equality masquerades as delivery. |
| FC13 .github/workflows/rebuild.yml | no; P:201 | DECLARED STATEMENT REWRITE: run FC12 plus chosen FA03 and carry surface row through child, select successor package inside its postimage; I7; accepted evidence never runs in CI. |
| FG01 rebuild/engine/writers.cjs | YES; P:296 | PURE MOVE of :234-239 into updateOpenerHold; DECLARED STATEMENT REWRITE of old site to call and return-table export; I5/I6; copied governor or regression of old completion. Newly discovered addition, not FC02b repair authorization. |
| FA01 rebuild/m3/w7-preview/today/next-load-panel.mjs | no; absent F | NOT CREATED. The small view/controller is local to FA02; another module boundary holds no additional clause. Its old file ID denotes that view responsibility, not an extra file. |
| FA02 rebuild/m3/w7-preview/today/today-entry.mjs | no; P:791 | NEW LOGIC: local view/controller, common projection refresh and chosen entry hook; I1/I2/I3/I7; no reachable yes or mismatched Today/gym. |
| FA03 rebuild/m3/w7-preview/today/native-load-panel.test.mjs | no; new F | NEW LOGIC: A01-A02 and selected B/C rows at DOM plus durable host; I1/I7; hidden API passes while phone cannot use it. |
| FB01 rebuild/m3/w7-preview/today/gym-model.mjs | no; F | B ONLY, NEW LOGIC: notification after acknowledged Close, separate offer-failure result; I1/I7; calculation hides a saved Finish. |
| FB02 rebuild/m3/w7-preview/today/gym-host.mjs | no; P:701 | NO EDIT REQUIRED. :76-86 already forwards the handle from the installation; FC08 supplies capability, FA02 connects it. A duplicate wrapper holds no clause. |
All retained component files have a cut counterexample. Proposed count, excluding unbound seal administration R: common 12 (FC01,FC03-FC10,FC12-FC13,FG01), A/C 14, B 15. These beat the options paper's incremental ceilings of 15/17/15. S8 product touches are common 8, A/C 9, B 9; no touched component is X-only. Counts are design enumeration, not measured implementation size or delivery time.
MEASURED scratch hunk only: P/prepare-probes.cjs extracts SIX unchanged governor source lines (:234-239); P/writers.before.cjs and P/writers.after.cjs differ by 11 additions/7 deletions (git --no-index --numstat). P/governor-move.json asserts exact original body retained and :227 unchanged. Copies escape non-ASCII characters identically before/after; line counts are unaffected. This is a feasibility diff, not a full implementation or repair diff. No other product hunk was prototyped or counted; builder must publish its own actual diff counts, not these as a budget.

ENGINE BYTES FOR JOE'S WORD, separately and exhaustively for this design:
1. ALL bytes of the NEW E/native-load.cjs: native evaluation/typed adapter/yes-only adoption and earned target/qualified native landing/compensation. Its source byte and line count are UNMEASURED because it is not built; the named behaviors and allowed inputs above delimit the authorization sought.
2. E/writers.cjs: extract only existing :234-239 to updateOpenerHold(ex,en,push), replace that original block with updateOpenerHold(ex,en,push), add its private-composition return-table member. Wrapper/call/export plus the unchanged moved statements are the whole requested hunk; no threshold or control-flow change in completeSession. The generator preserves :227.
NO other existing E/ file is in scope. In particular E/earn.cjs, performed.cjs, progression.cjs, today.cjs, merge.cjs, migrate.cjs and constants.cjs retain their accepted bytes. If code cannot meet the contract with that boundary, STOP the affected part and amend the exact scope before a product edit. The owner's repair word covers neither item above.
FC02/FC02b are inherited accepted-parent behavior, not work owned here. N01 consumes their accepted EPP-R1-R7 witnesses without repeating their repair design. New native queue truth never overrides a legitimate imported legacy decision or treats PROPOSED as accepted.
R is deliberately not a fabricated total: PM must name the successor's package JSON under rebuild/lanes/b/tooling/packages/, brief, builder report, acceptance/review artifacts, receipt, exact inherited carrier/re-pin files and any runner registration. READ S8 example: rebuild/lanes/b/tooling/packages/S8.json:6-17,29-51; runner b-package.cjs is P:301 and X:1601 in S8. FC13's selected child must be in that closed manifest. This paper creates none of R and does not authorize writing frozen parent artifacts.

## D. Red-first rows, never deleted
PROPOSED row families, not executed new tests. Every N row runs in FC12; UI families in FA03. All numerical inputs a builder chooses are INVENTED, declared in the fixture, or explicitly read engine constants. Start with existing command/card behavior as the missing positive-effect red, then test the new API; a missing-function exception alone is not that witness. For inherited green preservation, the planted scoped defect must turn the row red. Keep every parameterized variant and original row name in child carriers.
| Row; clause | Input and REQUIRED output | Planted defect that must be RED; driven boundary |
|---|---|---|
| N01 REPAIR-PRESERVED; I6 | Accepted repaired parent with actual canonical producer order; inherited EPP-R1-R7 all hold in both compositions, including tap/reversed-order and separate half-repair negative controls. | Independently remove each accepted clause in scratch; actual producer -> selected card -> old writer storage -> following card. Never infer from a constructed queue only. |
| N02 WINDOW-NOT-CARD; I5 | Complete matching native lines meeting a lower card but missing the window -> WINDOW_NOT_TOP; prefix miss breaks run; legitimate original progression prefix tops -> offer then yes -> real higher card. | Use card targets as earn criterion or count added/gapped positions; typed projector -> evaluator -> durable yes -> capture, with positive control. |
| N03 NOISE-AND-EARLY; I1/I5 | First ordinary top -> PROVISIONAL; canonical clear-margin and terminal-qualified early cases -> same candidates as unchanged earnWalk, still no target before yes. | Replace noise predicate or auto-apply scratch queue; evaluator and committed next capture, thresholds from engine source. |
| N04 REPEAT-AND-HOLD; I5 | Repeated tops with honest opener -> ordinary offer; hot/held -> proper refusal or canonical hot-terminal tapped exception; known honest original opener releases hold. | Pre-count current top, ignore governor, use terminal as opener or implement a new hold heuristic; canonical helper/evaluator plus card effort and accepted effect. |
| N05 YES-OR-NOTHING; I1/I2/I3 | Log/Finish/check/open/Start/decline/cancel have no NEW native target; wrong/forged/stale yes refuses; correct exact yes durably creates one target, w remains base until debut. | Bypass trusted issuer, auto-accept at Finish or assign w on earn accept; actual stage/client/repository and reopened capture. |
| N06 COMMIT-RESTART; I3/I4 | Abort before put -> no response; completed put then lost ack/restart -> same response/target; later matching debut Close -> one landing surviving cold rebuild. | Publish before commit, trust sidecar or omit response from fold; encrypted disposable repository with kill/abort/ack seams, then real card and state. |
| N07 EDITED-PLAN-WINS; I2/I4 | Offer then deliberate newer working-load/technique/plan edit -> STALE_OFFER; imported old yes never overrides proved later authority; incomparable edit -> EFFECT_CONFLICT. | Compare only lift ID/value or sort wAt clocks; source fold and guarded commit, including both import orders. |
| N08 EDITED-FACT; I2/I4/I5 | Correct actual load/reps before yes -> stale; after yes -> historical effect retained, BASIS_REPAIR_REQUIRED, new advice withheld; eligible pre-training compensation works. | Recompute old issuance under corrected facts, drop original or fail to invalidate; real workout edit API, response, rebuild/import projection. |
| N09 ACTUAL-LOAD; I3/I5 | Different equal actual load saves as entered; not a repeat at card load; adopt-observed displayed by name; yes establishes exact observed vector and next card. | Substitute prescription for actual, auto-adopt on Close or label adoption earned; native Set/Close -> host -> durable response -> reopened capture. |
| N10 FRESH-BASELINE; I3/I5 | Null working load + completed usable equal numeric sets -> adopt-baseline; yes removes baselineAsk; blanks/skips/configuration without magnitude refuse, never zero. | Number(null), inferred increment or dropped adoption effect; clean-init -> real Set/Close -> baseline yes -> next capture. |
| N11 VECTOR-PREFIX; I3/I5 | Known established unequal vector earns canonical translated vector, captures every position and lands identically; gaps/added/removed/count changes get specified prefix/refusal. Fresh unequal adoption -> VECTOR_ADOPTION_UNDEFINED. | Average, take first weight, drop newWSets, compact slots or include added terminal; typed facts -> actual capture -> Close -> reconstructed state. LIMITED-SCALAR has a separate SCALAR_SLICE_ONLY variant; its refusal never counts as the general positive vector pass. |
| N12 EFFORT-VARIANTS; I5 | Exact 0/1/2, at_least 3 and all unavailable tags through hot/early/two-rung branches; known bound preserves text and canonical candidates; unknown opener refuses; unknown terminal retains only justified ordinary candidate. | Cast bound to exact, null to zero, use added-slot effort or let private token serialize; typed validator -> unchanged earnWalk adapter -> issuance -> next card. |
| N13 LATE-AND-SAME-DAY; I2/I5 | Two distinct same-date Starts survive; proven order in unchanged era gives correct offer; era-date/count ambiguity refuses by name; late source admission keeps capture basis. | Key by local date, reorder backdate, or invent era dates; actual operation log, order projector and source fold, with positive ordinary-era control. |
| N14 IMPORT-JOINT; I4/I6 | Real synthetic import preparation with legacy provisional history plus native facts/response, both orderings and repeat import -> same programme or same explicit overlap refusal; native-only positive accepted response survives import. | Feed native cache to legacy merge, drop response family or count shared legacy/native member twice; actual preparation AND local/portable admission, not merge alone. |
| N15 SPEND-ONCE; I4 | Duplicate delivery; equal accepts with distinct response IDs; partially overlapping support; renamed lift; same completion counted before earn -> one semantic effect or named conflict, never two. | Dedup only op ID, include edit ID in spend root, or feed post-current count to earnWalk; authenticated combined generations -> fold -> card/landing. |
| N16 DECLINE-UNDO; I1/I4 | Decline/cancel leave workout facts intact and write no response; accepted pre-training change -> compensating offer and yes restore prior plan, old yes stays spent; later-training undo refuses by name. | Persist an unmapped naked decline, delete history, refund evidence, silently undo or apply old yes after compensation; actual dismissal, import, response commit and cold fold. |
| N17 STRUCTURAL-AND-CAPTURE; I3/I6 | Competing accepted lifts and inherited coApproved riders retain canonical selection; only selected native target enters new capture; pre-accept active Start stays fixed; qualified selected Close lands exact target. | Mark every accepted lift active, mint coApproved or land first same-lift queue entry; existing picker/capture, resume and durable Close. Ambiguous legacy mapping keeps existing refusal. |
| N18 NO-NEXT-RUNG; I4/I5 | Maxed/missing rung retains top facts, returns NO_NEXT_LOAD; later actual equipment choice via existing authority -> same evidence usable once; repeated import/reload never increments again. | Invent fallback increment, clear banked facts or price from prior target; evaluator and real post-accept card/landing. Equipment-policy changes themselves are not authored by this feature. |
| N19 ERA-AND-TENURE; I2/I4/I5 | First changed-technique completion banks no earn; old-load excursion ends tenure; adoption opens new tenure after observed completion; rename preserves semantic spend. | Reuse old-era counters/noise, filter off-load evidence out before finding tenure, or key spend by display name; actual typed history and engine readers through host. |
| N20 HOST-PARITY; I7 | Both runtime compositions and actual shipped host return equal valid/refused offers, accepted effects, target capture and landing; owned input bytes unchanged; forbidden imports absent from runtime graph. | Omit browser forwarder/fold, expose completeSession, return two equal refusals or leave Today on old model basis; actual runtimes plus built browser entry on synthetic durable store. |

Existing rows that CHANGE their stated expectation, by READ file/line: rebuild/m3/w6/host/test/engine-equivalence.test.cjs:24-30 retains list/order equality but literal module count at :27 becomes 13; its :34-74 provider/refusal/positive-card rows all remain. That file is BOTH S8 product :451 and execution pin :1625. Carry the exact source under FC12's pinned child transform and route CI's :171 invocation to that carrier; do not delete the original test or silently skip its failure.
The original W/test/native-next-targets.test.cjs:35-48 assembly row's :41 module list and :43 two-reader surface are superseded by the child surface (existing predecessor carrier rebuild/m4/spec/b-ntc-native-next-targets.test.cjs:8-12 already demonstrates a single exact assertion rewrite). Preserve every behavioral/refusal row. Ancestor source/inventory pins (rebuild/m4/spec/native-carriers-source.cjs:89; load-write.test.cjs:7) must report parent mismatch and be covered by declared child construction, not edited to accept arbitrary new files. PM names the actually selected chain's carrier files in R; this paper does not pretend the historical two/four-reader carrier is the current five-reader acceptance.
No progression law, golden, existing physiological expected value, tap-control result or old completion behavior is specified to change. That is a proposed preservation requirement, NOT a conformance/golden equality measurement from this assignment. New native positive effects are departures from the reader-only surface and require explicit child coverage.

## E. Three thin, separable entries
Shared view responsibility FA01 lives inside FA02: a visible Check next weight action beside the workout entry; lift name, evidence, all offered set loads, reason and separate adoption label; one yes per chosen alternative, decline, close and retry. No bulk yes, automatic substitution or editable numerical proposal. Keep keyboard focus, accessible labels and the existing look's typography/buttons. Code paths come from READ T/today-entry.mjs:183-219,384-395, not a new screen router.
| Entry | Athlete sees; files/rows | Split/look dependency |
|---|---|---|
| A: explicit check | Finish remains Saved. Check next weight opens the shared choice/refusal; yes updates the future eligible card and survives restart. Common + FA02/FA03; FA01 is local code, no file. A01 FIND-AND-USE: visible button -> real offer -> durable yes -> higher eligible card; hide handler or omit projection to turn red. A02 REQUEST-REPEAT: repeated check no writes; intervening edit -> STALE_OFFER; drop basis comparison to turn red. Drive DOM, real host and repository. | Can build before Today split seals: compose a small sibling control in the existing entry/mount lifecycle, no today-app/today-lanes edit. It must survive render/back/reopen and teardown; a control appended once then erased by render fails A01. Look supplies visual rules, not an engine prerequisite. Smallest entry before the split is A. |
| B: after Finish, with A recovery | FIRST show Saved; after acknowledged normal Close notify the shared controller to check completed lifts. Each choice still needs yes; A's button recovers unanswered choices. A files + FB01, no FB02 edit. B01 CLOSE-RECOVERY: kill after committed Close/before paint -> manual check derives same eligible choice; delete recovery to turn red. B02 SAVED-IS-SAVED: evaluator throws/refuses or double Finish -> workout remains durably saved with no duplicate effect; fold offer failure into save result to turn red. | Can build before split using FB01's after-ack callback at :544-546 and FA02's existing model construction; READ FB02 already forwards host handle. Callback failure is caught after save, cannot reject the Finish promise or cancel facts. Its trigger needs an eligible new Close, not any refresh. |
| C: before a fresh Start | Finish remains Saved; on next workout open show due choice before preparing a new Start. Continue at current plan/decline is explicit and is not yes. Resume retains original capture and labels any choice as applying later. Common + FA02/FA03; no new router file. C01 OPEN-TO-START: offer -> yes -> refreshed eligible capture -> Start; stale capture reuse turns red. C02 OPEN-IS-READ: repeated open/reload/import with no yes stores no load response; automatic accept turns red. C03 RESUME-FIXED: accepted target while older Start active -> resume old capture; replace it to turn red. | Can build before split by wrapping the existing entry.open at :217-219 before mountGym, while using gym.read to distinguish new/resume. If the selected look removes that seam, this precise entry cannot integrate until an accepted replacement seam is provided; A remains the smallest pre-split entry. Never take custody of sealed today-app merely to force C in. |
The mount implementation must use the entry's actual open/refresh/dispose lifecycle, not a MutationObserver, timer or DOM selector guessed from the future split. B does not subscribe to generic refresh as a Finish substitute. No entry releases before S9, look, engine gate and its seal child, regardless of whether its isolated code can start earlier.
Concrete pre-split mount: FA02 creates an owned check region and a gym child container inside the supplied phone element, then passes ONLY that child container to mountGym. READ T/gym-app.mjs:202 replaces its supplied container, so normal gym repaint cannot erase the check control. Existing back/check-in callbacks retire the view; boot teardown invalidates its handles. B's after-ack notification records the Close reference in the entry, then its back callback lets the existing Saved/return flow complete before showing the shared offer; recovery does not depend on that volatile notification. C's wrapper gates only fresh entry, then mounts the same gym child after the choice/continue action. These are proposed DOM integrations; A/B/C rows must prove the actual look and lifecycle, which this paper did not run.

## F. Build and acceptance order
1. PM accepts this paper after the independent Claude disagreement review, settles the named scope questions and prepares a brief with exact parent/child/custody and chosen A/B/C. Joe's own words select entry and engine scope; LIMITED-SCALAR needs its own named acceptance if used. No current option/engine word is implied by timing :631 or spending :634.
2. The hour that scoped word and brief exist: builder may write red-first FC12 fixtures in OS temp/disposable repositories, common new files, exact governor extraction and parallelizable bindings on the assigned base. Keep work uncommitted if the brief says so. Neither S9's unfinished seal nor the look is needed to reason about pure synthetic inputs. Do not claim acceptance on this unrepaired evidence head.
3. Before integration of any native engine effect, consume the separately ACCEPTED repaired parent, or the accepted repair carried into the selected child. Re-run N01 against that composition. The helper extraction must preserve the accepted repair hunk, and its legacy completion output must remain equal. This paper does not reopen the repair decision or waive its owed oracle/sensitivity checks.
4. Implement FC01 plus shared FC03 fold with positive adoption/earn/landing first; then guarded stage/client and both runtime compositions; then null/imported projection and both import paths. Prove N02-N20 with scoped red mutants at their real boundaries. Stage/respond alone is not a vertical result. Do not implement a threshold, tolerance, age, cap or increment not already in the engine; STOP and name a new-number question if one becomes necessary.
5. Add A's entry and positive durable card witness. B adds only the post-save trigger/recovery witnesses. C adds only open/new-Start/resume witnesses. Obtain the look's accepted visual treatment; no Today split dependency is introduced by this design. A replacement owner-selected entry that needs split files waits for that split's accepted seal/custody, not merely its unreviewed branch.
6. Full ENGINE tier: unchanged-law conformance and selftest, actual legacy/port/sensitivity and second-gate obligations on the accepted candidate, scoped new mutants, both runtime/bundle proof, independent engine review, PM reads every built line, CI on both supported operating systems. Restricted/private proof remains with its authorized custodian in its allowed environment; missing evidence is named debt, never a PASS or a reason to edit a frozen law. Do not run the whole Today step concurrently with other hands.
7. Reseal child declares exact repaired parent and accepted repair provenance; Joe's engine/entry/limitation words; all new/moved engine statements and no numerical-policy changes; typed/vector/late/legacy refusals; all component and R files with pre/post hashes; product versus execution pins; changed assertion carriers and unchanged behavioral rows; closed runtime capability and browser module graph; source-family/producer revision; every N/entry row and its red witness; actual gate results/debts and CI postimage. No parent artifact is rewritten to hide drift.
8. Release sequence remains S9 seal AND look, then accepted/sealed native earning, then trial. Before calling it phone delivery, witness the chosen entry on the phone: save -> offer -> yes -> kill/reopen -> actual eligible card -> later qualified Close -> persisted working load. B additionally witnesses crash-after-save recovery; C witnesses old active capture. A is not silently substituted for B if the window slips; PM returns the concrete fallback to Joe.

## G. For Joe (plain words)
A: Say yes to a button that checks your saved workout and offers the next weight for your separate agreement; you may forget to press it.
B: Say yes to that same check after Finish, with the button there if the offer is missed; the app must still tell you your workout saved if the check fails.
C: Say yes to seeing the offer when you next open a workout, before Start; an old workout must keep the weights it started with.
Each choice includes the engine work that decides the offer, saves your agreement and puts the agreed weight on the right later card.
Confirming a weight you already lifted is called setting your working weight, not earning a heavier one.
Agreeing to an earned weight queues it; your working weight changes when you later complete that agreed debut.
An edited workout or conflicting copy can leave the check waiting with a reason; your saved sets stay there.
A lift waiting on an older kind of change can also stay waiting; this work does not take that older change over.
If the full different-weights-per-set work is not settled, Limited Scalar needs your separate yes: it checks only lifts using the same weight on every original set.
That limitation can leave a lift waiting even though you trained it; it must never guess an average or hide the reason.
The separate fault repair you already ordered is not being asked again.

## H. Open questions for PM (recommendations, not delegated owner authority)
1. Full vector adoption or LIMITED-SCALAR for the first delivery? Recommend return the explicit limitation to Joe if no lossless scalar/vector adoption rule is accepted; do not pick the first/mean/max load. READ E/writers.cjs:338-349 has scalar input; MEASURED P/seams.cjs proves established-vector translation only, not fresh-vector adoption. General vector positive N11 remains owed.
2. Include the governor extraction in Joe's engine scope? Recommend YES, only FG01's measured unchanged-body move/call/export; otherwise stop native governor qualification rather than duplicate it. MEASURED 6 moved lines, scratch diff +11/-7; READ inline original :234-239. PM cannot approve this engine hunk for Joe.
3. Which exact repaired parent, successor child and carried assertion files? Recommend the accepted repair on/after S9, with the new engine scope sealed in its own named child and the current five-reader carrier explicitly superseded. MEASURED S8 custody only; READ remote :633-634 reports ongoing repair/S9/split work. Neither their final heads nor a successor seal were measured here. Bind R before builders edit existing files.
4. Assign successors for LEGACY_PENDING, changed original count and same-day new-era ambiguity? Recommend keep named refusals and report them as trial scope risks, not promise universal native earning. READ writer special branches :244-328 and date-based progression count/era readers; MEASURED same-day eraFresh=true and derived run=0 despite two distinct INVENTED Starts. No new date/order/threshold rule is authorized here.
5. Accept conflict/compensation coverage and historical producer retention as written? Recommend refuse incomparable effects and undo after descendant training; retain the original issuance interpretation by revision, never latest-clock choice. READ source replay :139-148,178-188 lacks this family today; these are specified new proof obligations, NOT measured convergence. If the accepted source APIs cannot reconstruct historical cuts losslessly, stop that source lane as SOURCE_FRONTIER_UNPROVEN before claiming import support.

## I. Probe custody, limitations and final measurements
P/read-lines.ps1 is the retained explicit-path excerpt reader; per-file excerpts are P/<path-with-separators-replaced>-<first>-<last>.txt. Read snapshots escape non-ASCII source characters as Unicode escapes, preserving line identities. P/prepare-probes.cjs is the reproducible driver extractor, explicit S8 manifest query and governor scratch construction; P/custody.json and P/governor-move.json are its outputs. P/writers.before.cjs, P/writers.after.cjs and P/governor.cjs are scratch-only copies; no worktree engine copy changed.
P/retain-evidence.ps1 and its authority-<line>.txt outputs retain the individually selected remote ledger lines; P/diff-counts.txt retains the exact scoped scratch diff count; P/probe-index.txt lists the folder file by file. P/measure-spec.ps1 and P/spec-measurement.json retain the final document checks. No probe or changed copy lives in the worktree.
P/readers.cjs retains the full INVENTED driver from the gap map with explicit measured-root requires; P/synthetic.cjs exports its constructor; P/readers.out.txt is this head's execution. P/seams.cjs and P/seams.out.txt retain the vector, pre-count, bound/governor and same-day-era probes. P/seams-first-attempt.* retains a failed diagnostic that tried invalid exact-3 typed effort; P/seams-second-attempt.* retains the missing invented lift-name fixture failure. The final run corrected the fixtures, not engine code. These failures are not gate failures or hidden passes.
Commands use exactly C:/Users/joeym/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe, one Node process at a time. Each run sets MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York on separate PowerShell lines. No test suite, build, seal, receipt generator, npm install, network action, protected source/data or physical phone was exercised. No tracked file was edited, no commit/push/fetch/check-out/reset/stash/clean was run and nothing was deleted.
NOT MEASURED: new evaluate/apply/fold implementation, guarded durable respond, actual native Close/yes/landing, browser bundle parity with the new capability, IDB crash recovery, import convergence, semantic conflict repair, compensation after training, full vector adoption, all gates/goldens, final repair/S9 acceptance, selected child pins, look/split compatibility at its eventual head, performance, delivery duration or frequency of any real athlete's trigger. The probes above establish existing seams and one adapter feasibility, not acceptance of N01-N20.
MEASURED LAST: 277 lines; ASCII-only bytes; LF-only line endings with a final LF. Owner section: 11 lines. Verified by P/measure-spec.ps1 after all substantive edits; detailed result and file SHA-256 in P/spec-measurement.json. Final scoped git status and diff-stat stdout are pasted in the handoff; no commit.
