# NATIVE-LOAD-SPEC R9.2 Review L3
Reviewer: Astra (Codex), commissioned by the Claude Opus 5.5 PM; paper, engine tier; round 3
VERDICT: REJECT

B-R9-1 is resolved. B-R9-5's unconditional Undo contradiction is resolved, but the replacement recovery has a remaining queue/capture trap, B-R9-6. No product, trial or release acceptance follows.

## Scope and evidence

- Target: earned-astra-96/rebuild/coach/NATIVE-LOAD-SPEC.md, uncommitted R9.2; SHA256 a41c87933b99796870693d6b58813ec72269593b32e0dfd8d4baf536403771b0, unchanged on recheck. Spec line citations below are R9.2.
- Read own R9 L2 and build L7 reviews, and origin/rebuild/t2-client-core DECISIONS:784,785,793,794 through the explicit path. No other reviewer's report was read. This is an independent commissioned review, not a project ruling.
- Requested git -C earned-astra-96 diff could not resolve its Git metadata here. Used this worktree's metadata with --work-tree pointing to earned-astra-96 and explicit-path diff b849508 -- rebuild/coach/NATIVE-LOAD-SPEC.md. No index or checkout change.
- E = rebuild/engine; W = rebuild/m4/workout; L = rebuild/m3/w6/local. Public reader/capture bytes came from earned-astra-96. Durable shape, compensation and admission excerpts came from explicit paths at 0e4fc74.
- Two contained probe runs completed: correspondence/nextLoad/held-card/compensation traces, then earn residuals/FC16 compatibility. An initial attempt stopped on an unprovided memoOnState dependency; after inspecting today.cjs:649, the rerun supplied its identity callback. No product condition changed.
- All fixture quantities are invented. Literal paper predicates are identified below; they do not establish a working R9.2 fold, host, panel or durable transition.

## Findings with evidence

### B-R9-6 BLOCKING: a held pending target prevents the training needed for exit (b)

Invariant: :158/:277 promise a held lift's new card is not_prescribed, the day remains capturable, and a later adoption is reachable even when descendants bar Undo.
Input sequence: accept genuine Q105 at base100; a later Start captures Q105, but its completed actual loads are100, so :152/:205 do not land it and Q stays pending. Then an ordinary correction to its accepted evidence causes BASIS_REPAIR_REQUIRED. Descendant training bars Undo (:154/:208). The completed descendant predates the hold, so it cannot provide exit (b)'s required later completion. This sequence needs no fabricated record, import or direct store edit.
Specified projection: :161 retains the effect/history; :158 sets w=null and wSets=null, retiring the held native entry only when recovery adoption is accepted. There is no clause suppressing that pending entry from structural selection while held.
Measured boundary: with that exact projected state and one other ordinary lift, unchanged E/today.cjs:55-58 selects Q; :98 selects q.newW even though :103 sets baselineAsk=true. Output is {w:105,baselineAsk:true,isDebutNow:true}. Unchanged W/engine-capture.cjs:64 then throws ENGINE_CAPTURE_BASELINE_UNPROVEN for the whole capture. Thus there is no later Start/Close to reach (b), and the other lift's day is refused too.
Control: same invented state with Q absent only from the input projection gives {w:null,baselineAsk:true,isDebutNow:false}; actual capture succeeds with load.state=not_prescribed. Nothing was deleted from any stored history. The readers used explicit synthetic day/membership/last/meta/effort callbacks; the actual structural picker, genSession and capture guard were unchanged.
Required amendment: specify how held entries are excluded from active structural selection in the registered projection while retaining their records, spends and pending history. Their numeric loads must not reach a held card or consume another lift's structural slot. Keep the baseline capture and descendant guards intact. Extend N27 with this unlanded-descendant case, including cold replay and both lifts' actual capture; its present descendant row covers an applied adoption with no pending Q.

### B-R9-1 CLOSED on paper: kind-aware correspondence preserves genuine images

Executed literal c1-c3 checks accept present-null w and absent w as adopt-baseline, numeric scalar100, present-null wSets at scalar100, and established vector[100,95,90]. They reject a presence mismatch and a present non-array wSets. Unchanged nextLoad returns105 for decoded {w:100,inc:5}; undecoded wrappers return null, as before.
The amended :156 now matches E/native-load.cjs:109,115,122-128 and W/native-load-effects.cjs:218,232-233 at0e4fc74. Baseline scalar/vector project to null while the FieldImage retains absence. No v1 record rewrite is needed. Actual R1/R2 record application and baseline restoration remain implementation proof, not this paper result.

### B-R9-5 CLOSED as a guard contradiction; exit (b) still needs B-R9-6

The literal descendant trace now has Undo unavailable, without promising otherwise. A later complete supported performance can offer adoption only after every holding record; no yes leaves projected load null, and a later explicit yes adopts exactly the observed vector. Incomparable/earlier completion does not clear the hold. :158/:161 make that a new authority, retain prior consent/spends and retire held native entries.
RETIRE/RESTORE now classify from the recorded load shape (:156), not the current dispute state. The constructed RESTORE base105/target100 remains RESTORE after a later dispute; equal base/target remains RETIRE and writes no w/wSets. No recovery clause authorizes a numerical caller override, silent increase or Undo after descendants. These are paper traces, not executed native-load transitions.
Scope caveat: :158's universal promise exceeds :144-145. A held established vector followed by a fresh unequal performance still yields VECTOR_ADOPTION_UNDEFINED (or SCALAR_SLICE_ONLY), even though the athlete trained. Qualify the recovery promise by the supported adoption domain; do not invent a scalar or weaken the existing vector refusal. This is D-R9-RECOVERY-SCOPE below.

## Commissioned checks

1. The twelve L7 variants and forged adoption retain refusal clauses for R1 and R2. This is a paper mapping, not 26 executed fold cases:

| L7 input | First refusing field/clause |
| --- | --- |
| 1 body125; 2 bad programme; 3 bad queue; 5 body125 plus rename | target_load, DERIVABLE: authentic F0 rung is105 |
| 4 consumed Start missing from coverage | basis.coverage, S5 |
| 6 empty evidence | evidence, S4 |
| 7 other lift with original consumes | lift_lineage_id, S1 |
| 8 consumed Start absent; 9 Start and its evidence/facts absent | consumes, S3 |
| 10 arbitrary spend | spend_id, S2 |
| 11 forged source | basis.source, S7 |
| 12 absent Start in order | basis.order, S6 |
| Actual105 replaced by adoption110 | target_load, S8 |

Genuine recorded105 retains its captured loads, historical edit prefix and recorded equipment after rename/window/cache edits (:155-156, DECISIONS:793). The decoded candidate remains105 and the common baseline rejection is gone. Current window rules govern NEW earn checks only. N25 case7's generic expected lift label still says fx-press although the forged body says fx-row; pin affected-lift attribution in the build.
Newly constructed false record family: actual original loads100 with reps[10,9,7], authentic matching evidence/coverage/source and a non-reproducible cut, but forged PROPOSED105. Unchanged atTopOfWindow is false and nextLoad is105. S1-S8 plus DERIVABLE do not establish top eligibility. This repeats the L2 eligibility witness as a regression; it is covered by :157's named residual, not a new closed guarantee.
Rerun residual controls: hot opener with a terminal at_least3 and one prior sighting produces only PROPOSED105, while forged110 is derivable; one ordinary top with effort[2,1,1] produces no candidate, while forged105 is derivable. These execute unchanged earnWalk with explicit synthetic noise callbacks, not a substituted noise formula claimed canonical.

2. The threat boundary is fair: direct modification of the athlete's own encrypted store is excluded; app-produced records, ordinary corrections and crafted incoming records remain in scope. :157 gates external admission; :164-166 bind local consent to the guarded host and exact staged generation. B-R9-6 is an ordinary-edit failure inside that boundary, not an excluded attack.

3. FC16 is additive to format readers. With only its specified :91 replacement made in memory, measured source_json is {value:14,unit:'rep',window_hi:12}; unchanged capture.read and engine-capture.readLayout accept it. An extra OUTER reps-cell key still refuses WORKOUT_CAPTURE_INVALID. Baseline not_prescribed remains unchanged and invalid hi omits the key. The old producer reconstructing the new capture refuses ENGINE_CAPTURE_ORIGINAL_DISAGREEMENT; the matching amended producer agrees. Keep version binding and child pins; do not weaken full-capture equality. :127 blanket-refuses every NEW earn from a pre-FC16 completion, including apparently equal windows, so the ambiguous legacy cases cannot offer. Adoption is exempt. FC16 sharing FC01/FC03's seal is now explicit at:228.

4. RESIDUAL is correctly widened to equipment, eligibility, ordering/context and issuance. S1-S8/DERIVABLE alone are insufficient. L/today-bindings.mjs:644-656 at0e4fc74 statically refuses nonempty imported-source collections before the fold; L7 records the real host refusal with zero writes. :157 also binds future hosted sync to refusal. This supports the stated current local-host boundary; it is not a fresh deployed-ingress audit or proof that arbitrary authenticated fold inputs are safe. Keep FC09/FC10 closed until verified issuance or equivalent complete historical proof and the separate ruling. Admission proof remains a delivery debt.

5. Remaining earlier-section conflict: :158's claimed genSession consequence contradicts the retained queued effect and unchanged reader/capture behavior, B-R9-6. Its universal recovery wording also needs the vector qualification above. :386 preserves B0/G/H0 as historical branch text and records the answered questions. Earlier positive import rows stay future obligations behind :157, not completed claims.

## BLOCKING

- B-R9-6: define held queue projection and prove new capture/recovery with an unlanded captured descendant, without lowering COMPENSATION_DESCENDANTS or ENGINE_CAPTURE_BASELINE_UNPROVEN.

## NAMED DEBTS

- D-R9-RECOVERY-SCOPE: qualify NO TRAP by :144-145's supported adoption domain; show the existing refusal for later unequal work. General vector adoption still needs its separate policy/implementation; no mean, opener or maximum substitution.
- D-R9-ADMISSION: N28 at every real foreign path; guarded local provenance; original-cut/issuer proof covering eligibility and context before import/sync opens. A matching digest, device ID or equipment anchor is insufficient.
- D-R9-CAPTURE: actual-host producer binding, active pre-FC16 resume, mixed/invalid/missing window fields, child pins and FC16/FC01/FC03 same-seal proof.
- D-R9-EDIT/RECOVERY: correction/removal after accept, eligible compensation, descendant refusal, later adoption and repeated holds through fold/host/panel/restart; retain facts, consent, spends and tombstones.
- D-R9-DELIVERY: implemented R9.2, N25 R1/R2 matrix and N26-N28, real durable card/landing, scoped regressions, bundle/phone proof, successor seal and exact-head Windows/Linux CI. No product acceptance follows this paper review.

## Containment

Only positively selected public progression/earn/today factories and capture/engine-capture modules executed, after dependency inspection. VM contexts exposed no require, filesystem or child-process access; unexpected dependency callbacks refused. Only FC16's named line changed, in memory. No forbidden module, private/legacy file, auth file, prohibited scratch, suite/generator or receipt ran. No install, node_modules change, tracked edit, ledger/status write, commit, push, fetch or deletion. One supplied node.exe at a time; MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York each set on its own line before every probe. Exactly this new file was written; all probes stayed in memory. This report was written early and extended.

## Final scoped commands and outputs

```text
git status --porcelain -- rebuild/coach/NATIVE-LOAD-SPEC.md rebuild/engine/progression.cjs rebuild/engine/earn.cjs rebuild/engine/today.cjs rebuild/m4/workout/capture.cjs rebuild/m4/workout/engine-capture.cjs rebuild/lanes/astra/reviews/NATIVE-LOAD-SPEC-R9-REVIEW-L3.md
warning: unable to access 'C:\Users\joeym/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\joeym/.config/git/ignore': Permission denied
?? rebuild/lanes/astra/reviews/NATIVE-LOAD-SPEC-R9-REVIEW-L3.md
```

```text
git diff --stat -- rebuild/coach/NATIVE-LOAD-SPEC.md rebuild/engine/progression.cjs rebuild/engine/earn.cjs rebuild/engine/today.cjs rebuild/m4/workout/capture.cjs rebuild/m4/workout/engine-capture.cjs rebuild/lanes/astra/reviews/NATIVE-LOAD-SPEC-R9-REVIEW-L3.md
```
The diff command returned no output.
