# First-use setup through the actual owner page

Temporary PM September11. Speculative implementation under the existing fresh-S2 authorization, subject to independent review before integration. Create isolated work/astra-first-use-setup, codex/astra-first-use-setup froma157d3dca009205b0e796f9a73f97e10256a0d83. This base's navigation delta is under separate review; do not self-accept it or modify its files. Read NEXT/ROADMAP, current product goal/decisions and accepted local setup contracts/reports. PM checkpoint supplies paths and exact evidence. No frozen personal instrument/ledger reads or edits.

Concrete missing first-use path: today-entry.mjs owner boot correctly distinguishes LOCAL_FIRST_RUN/setup-required from damaged restore-required, but the fresh page only displays a dead-end message. Real browser proofs currently enroll via a test helper. Implement a human-usable setup path on the real owner page through the already independently accepted client.enrollSetup({setup}) API; no test-only initialization or browser console step. Existing owner policy/history are not a starter profile.

Use the existing approved paper/ink/Instrument components and form conventions, one main action, progressive disclosure and a concise review before saving. This is bounded setup of an existing routine, not the unapproved Dad six-screen redesign and not automatic programme prescription. The user supplies their display name, current upper/lower/rest schedule, exercises/sets/rep ceiling and actual equipment increments/available loads, plus optional explicitly selected priority muscles. Do not preselect a fabricated routine, load, training frequency, preference, nutrition goal or body measurement. Current local start date may be shown for explicit confirmation. Present physical quantities in plain language, not internal keys or JSON. Preserve entered form values on validation/retry, native keyboard/labels/error focus, and clear committed success.

First inspect identity/metadata dependencies: exercises are not arbitrary names if the existing source classifies their stable ids. Reuse a demonstrated source-owned public catalogue/mapping where one exists; do not import seed.cjs or copy Joe's old exercise/load list, manufacture generic ids that silently lose compound/muscle semantics, or invent muscle classification. If no adequate existing catalogue is present, make the specific minimal supported identity mapping concrete and report the exact source evidence to PM before adopting new prescription semantics. An explicit regular equipment ladder may be expanded arithmetically only from the user's supplied range/increment; do not assume a rack. Keep constructor's validated closed setup schema and initial loads null. No nutritional prescription or scientific rule is created here. Unsupported routine shapes must be stated truthfully, not silently converted to upper/lower.

Allowed product ownership: NEW files under rebuild/m3/w7-preview/today/setup/; narrowly wire today-entry.mjs's fresh-only branch and build.mjs if necessary. Do not edit today-app.cjs/screens.template.html (navigation owned elsewhere), engine, immutable constructor, local-client/setup/lease schema or qualification pins. Reuse exported local client/calendar contracts with normal bundler boundaries. Damaged/partial installations must never see a reenroll/overwrite shortcut; a configured record must not be rewritten; duplicate submission/postcommit failure follows accepted fail-closed client state. Successful enrollment discards caller setup, opens Today from stored authority and starts no workout or fake observation by itself. Keep separate demo database behavior.

Prove actual built-browser visible fresh setup -> review -> confirm -> authoritative stored setup -> default Today -> visible first workout/log -> reload preserved same setup/records. Synthetic explicit user-entered programme only. Add focused validation, cancellation, duplicate submission, real precommit/postcommit failures and damaged-store exclusion using existing primitives; reuse accepted low-level44/109 and existing calendar evidence instead of repeating custody tests. Check390x844 appearance; retain screenshot for PM/owner review, but no phone-ready claim. No final qualification/full campaign or edits to other workers' source. One source/test candidate plus one report with exact limitations and any mapping decision still needing PM disposition. No publish, deploy, private data, installs, main/actual integration-branch merge or law edits.

This adds necessary first-use access while qualifier and navigation reviews proceed; it does not move the full product goal to manual logging alone. Nutrition/phase inputs, adaptive prescriptions and actual phone/recovery acceptance remain tracked in the current delivery checkpoint and must follow their qualified paths.
