# Authoritative initial setup — build assignment

Temporary PM September 11. Implement the immediately buildable plumbing slice from work/astra-fresh-owner-plan/rebuild/lanes/c/ASTRA-FRESH-OWNER-PLAN.md at68a2412. This is part of fresh S2 owner use under DECISIONS:100, not a reduced product goal. No approval of the pending Dad mock or new prescription policy is implied.

Builder owns work/astra-initial-setup, branch codex/astra-initial-setup, from71fb2f1 (C4 plus scoped-reviewed B-NTC; its package support is still pending, never call it integrated). Own only rebuild/m3/w6/local/local-client.mjs, new local/initial-setup.mjs, local/today-bindings.mjs, focused local tests and ONE builder report. Keep B-NTC qualified provider and its reviewed bindings unchanged. If another exact local file is necessary to preserve setup during staged writes, report why and make the minimal change within local/; no engine, client-core, frozen-law, fixture, UI or workflow edits. Other workers own package/tooling.

Implement the report's authoritative setup requirements and meaningful focused tests: separate immutable versioned setup document sealed in the enrollment generation; explicit setup-enrollment API validating through createCleanInitState; preserve exact supplied values, identity and constructor/profile; recover the same basis without derived cache; preserve setup through actual weigh-in/workout/check-in writes and synthetic copy/reopen; reject wrong identity/profile, malformed/missing authority, second replacement and failed enrollment. Never silently infer setup from arbitrary old cleanInit/cache or a demo. Existing no-setup installations remain explicitly unconfigured with a named result; no destructive reenrollment. All partial key/store/marker loss protection remains.

The setup document records the explicitly supplied initial programme; this slice does not claim those values were generated by a qualified coaching supplier, accepted by the owner, or scientifically optimal. It adds no fabricated defaults, history, nutrition target or personal fact. Read the report's dependent calendar/baseline/onboarding sections so APIs support those next joins without implementing their policies here.

Use meaningful synthetic end-to-end tests through existing C4/C1 APIs, inspect actual persistence across derived loss and all three writes. Failure injection must establish no false success. Run affected existing local enrollment/Today composition tests once, proportionate to changed behavior. No full engine gate or phone-readiness claim. Existing locked dependencies only, no private input/export/import, install, push, integration merge, deployment, purchases or protected soak.

Commit product/test changes and ONE factual report on this candidate branch. Return exact source commit, counts, evidence limitations and public API needed by the next owner-entry builder. A different reviewer and integrator will handle acceptance and integration.
