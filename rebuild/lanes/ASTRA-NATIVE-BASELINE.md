# Native first-workout baseline — candidate implementation

Temporary PM September 11. This directly unblocks useful fresh S2 progression for Joe and Dad. Read NEXT.md, ROADMAP.md, current LANES/DECISIONS and report68a2412 at work/astra-fresh-owner-plan/rebuild/lanes/c/ASTRA-FRESH-OWNER-PLAN.md. Old memory work is resolved; do not reopen it.

## Verified defect and intended behavior

Real C4 local-era/gym execution with explicit clean-init w:null records and closes40 lb x10 with exact2 effort, but day+3 still shows Find a working load, both at7b1678a and071-style B-NTC candidate0876043. Native facts are preserved; the initial exercise baseline never adopts them. Integrated writers.cjs:335–340 already describes the accepted legacy behavior: the first completed numeric entry supplies working load; blank does not consume adoption. Native genSession checks e.w==null before targets. Replaying the legacy completeSession writer or inventing a legacy record is not an acceptable native join.

Create the smallest actual source-owned native baseline projection that preserves known working-load/per-slot/configuration semantics, obtains evidence from the existing immutable performed original-slot records and their accepted causal order, and lets the existing prescription functions operate on the athlete's recorded starting point. The immutable initial setup remains unchanged; the current derived basis can be reconstructed from setup plus facts after cache loss. Do not choose a mean/min/max/last-set baseline for unequal loads or turn a configured key into pounds. Inspect the actual accepted native-load/slot contracts and existing qualification functions first. If a material case has no established mapping, expose the exact unresolved input/confirmation requirement rather than silently treating the observation as a recommendation. Preserve valid mixed/per-slot facts and state explicitly what proposal or confirmation is required to use them; do not discard them to pass a uniform-load test.

## Ownership and working method

Separate candidate work/astra-native-baseline, branch codex/astra-native-baseline, from71fb2f1. Exclusive new baseline module/tests under rebuild/m4/workout, plus narrowly necessary edits to source-projection.cjs/workout-host.mjs, or engine/{performed.cjs,progression.cjs,today.cjs} if the existing source-owned contract demands that placement. Before editing, write a concrete behavior/delta BRIEF under rebuild/lanes/b/native-baseline/ with exact function/file selection and send PM that selection. Speculative authoring is permitted by LANES; no merge or accepted scientific-policy claim before reviewed brief/package. Do not change today-bindings.mjs (initial-setup builder owns it), tooling/spec package runners (different builder), frozen client/core/laws/oracle, UI, fixtures or old app. Preserve B1/B2 pending files by using only this candidate worktree.

## Focused acceptance targets

Use the actual C4 store and default B-NTC provider: clean explicit setup, first completed original lift with entered working loads, close/reopen, second same-group workout and a further progression decision. Show it leaves debut, targets derive from recorded facts using the actual existing engine rules, initial setup is unchanged, and the same result rebuilds with derived cache absent. Tests must start w:null; preloading w to make the journey pass is forbidden. Distinguish factual baseline adoption from new programme/prescription choices.

Meaningful adversarial cases: incomplete and early-close lifts, skipped/removed/added slots, undo or amended contributing load, differing loads between original slots, configuration-vs-numeric values, technique/lineage mismatch, unordered/concurrent sessions and future-dated records. Use accepted qualification/refusal behavior; don't create new certainty or silently lose facts. Existing nonfresh/native and legacy behavior must remain equivalent outside the deliberate baseline change. Add focused tests and a real host-composition journey, plus necessary drift/bite control proving removal of the actual baseline join fails the fresh case. No broad full run until the candidate and governing brief have independent review.

Commit implementation, tests, brief and ONE factual report. Return exact source, observed before/after behavior and unresolved policy/package obligations. No self-acceptance, private reads/export/import, install, push, integration merge, deploy, purchases or protected soak. Use existing Node and locked dependencies. Root will arrange a different reviewer and proper engine-tier qualification if engine/prescription semantics change.
