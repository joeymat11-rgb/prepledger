# Correct two independently reproduced native-scale defects

Temporary Astra PM, September 11. Read NEXT.md/ROADMAP.md, LANES, ASTRA-NATIVE-SCALE-FEEDBACK.md, candidate CONTRACT.md/report and the independent review in BASE/astra-native-scale-review/rebuild/lanes/c/ASTRA-NATIVE-SCALE-REVIEW.txt when available. BASE=C:/Users/joeym/Documents/Codex/2026-09-04/read-rebuild-t3-brief-md-and/work. Create BASE/astra-native-scale-correction on codex/astra-native-scale-correction from exact95c6a5423cf4de364e24fc24f8b1754a49dcbc91. Distinct correction builder; the same nonauthor reviewer will verify your delta and a third person integrates. You own only rebuild/m4/nutrition/scale-feedback.mjs, its scale-feedback.test.mjs, a necessary concise contract correction and ONE report rebuild/lanes/c/ASTRA-NATIVE-SCALE-CORRECTION-REPORT.txt. No other product, policy or test edits.

Independent reviewer has already reproduced two concrete defects through actual C4-produced operations; root inspected the responsible code. First, the returned workoutHistory retains caller operation references, so recursive freeze changes a previously mutable caller generation's session Start and causal_parents to frozen objects. Input JSON equality missed this observable mutation. Ensure the immutable returned view owns all references it freezes and never changes the caller's objects, property descriptors/extensibility or subsequent legitimate use. Keep output immutable without an unnecessary duplicate of the whole repository generation. Demonstrate the original failure on actual generated history and that mutating caller-owned data after the fixed projection is allowed without changing the returned snapshot.

Second, actual same-device Close effective08:00 precedes a subsequently recorded reading effective07:00. At asOf07:30 the candidate returns baseline170 with no exclusion; at asOf10:00 it correctly returns SCALE_TRAINING_CHRONOLOGY_REQUIRED. The contradiction check's time<=cutoff erases already-known contradictory predecessor evidence. This is current resolved evidence filtered for effective contribution, not a historical receipt snapshot. Preserve known chronology contradictions even when the contradictory predecessor carries an effective instant beyond the calculation cutoff. A genuinely later completed workout which does not precede the reading must still NOT retroactively exclude an earlier eligible reading. Future readings remain excluded, and no irrelevant future workout may blanket-disable useful feedback. Preserve accepted/local distinctions, removal/correction semantics and explicit unresolved mapping; do not invent a new scientific rule or chronological winner.

Reproduce both failures against exact95c6a54, add meaningful regression controls (not only serialized equality), make the minimum correction and run the19 original scale tests plus affected existing joined cases. Preserve every original assertion; do not edit shared legacy arithmetic or claims about a full gate. Inspect the final independent report when it lands; route any additional substantive finding to PM rather than expanding ownership. Retain exact commands, actual before/after verdicts and gaps. Use the retained Node24.19 runtime/dependencies and documented EARNED_READING_W6_ROOT/EARNED_SOURCE_R1_ROOT, no installs. No frozen app/laws/private data, existing Claude trees, other branches, remote push/PR/merge/deploy or full/private campaign. Return clean exact candidate/report commit; no self-acceptance or additional agents.
