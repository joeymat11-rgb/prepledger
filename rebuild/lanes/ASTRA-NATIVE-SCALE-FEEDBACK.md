# Native scale feedback — bounded implementation

Temporary Astra PM September 11. Read NEXT, ROADMAP, LANES and the owner product goal. Implement stage1 of BASE/astra-fresh-nutrition-plan/rebuild/lanes/c/ASTRA-FRESH-NUTRITION-PLAN.md at55efbc769f5bd699244546ecafb30b9bd2f3faa7. BASE=C:/Users/joeym/Documents/Codex/2026-09-04/read-rebuild-t3-brief-md-and/work. This moves useful nutrition toward first use; it does not replace the still-required four-field daily plan, personal goals, recovery or progression.

Create BASE/astra-native-scale-feedback on codex/astra-native-scale-feedback from immutable combined source ca57649b3cc9189dca8d006ceb6bac17c842bea6. Current BNTC qualification source/policy413a54e and phone/setup worktrees are separate and must remain untouched. Own new rebuild/m4/nutrition/ files and their focused tests. For a necessary shared arithmetic extraction, this brief explicitly licenses only the scale calculation part of rebuild/engine/energy.cjs, the scale update arithmetic in rebuild/engine/writers.cjs and one shared scale helper in rebuild/engine/. Preserve all legacy public outputs and calls by delegation; no new coefficient, gate, uncertainty rule, model, default or reference-data change. Do not change frozen laws, qualification machinery, runtime exports, host/Today, storage schemas or initialSetup. Those are a later explicit integration assignment.

Deliver executable projectScaleFeedback from real existing reading-projector and native workout-history output, with a concise public contract in the owned folder. Reuse existing reading-history correction/tombstone/conflict semantics and original/effective time/offset context. Resolve actual supported native inputs by inspecting current host/read interpreter; never use Today presentation rows as proof, caller-supplied eligibility booleans, fabricated sessionLog, fake blackout or a fake lean-mass model. Any genuinely absent mapping must be named and demonstrated with an actual producer vector, not hidden behind an all-unavailable implementation. Return original observations and explicit exclusions/requirements alongside genuine calculable feedback.

The first eligible actual reading seeds baseline/smoothed weight; one reading never proves rate. Subsequent eligible readings use the exact existing damped update. Factor/reuse currentRate's scale regression, sample threshold and uncertainty without needing model.drip; preserve legacy fat-rate behavior. A missing model must not prevent qualified scale-only output. No eligible reading means no invented baseline. Body-fat/fat-loss estimate remains distinct from scale change.

Publish the exact native mapping in the contract: before-noon/pre-training eligibility at the reading's effective context, actual native event establishing training, missing/ambiguous chronology, source-qualified sealed/blackout context, actual as-of exclusion, same-day repeat selection, accepted/local layers and correction ordering. Reproduce actual established semantics where they exist. If a behavior has no governing rule and would change prescriptions, isolate that specific unresolved decision with a red vector and continue the unaffected implementation; do not invent it. Reopen/cache loss recomputes from resolved facts. No durable legacy receipts or new writes during projection.

Acceptance: actual produced empty/first eligible/first late then eligible vectors; proven and ambiguous prior-training order; repeat date; correction, removal and reclassification where existing schema supports it; unresolved/rejected records; first/later/all readings removed; future/as-of; offset/DST boundaries; stable reopen/cache loss; qualified existing-threshold scale rate with no body model. Include exact legacy configured-state parity and current missing-model failure before the fix. Test meaningful source outcomes and errors, not copied implementation. Retained local dependencies only, no installs or full/private engine campaign. This is a candidate requiring distinct review and eventual full engine qualification, never an issued package receipt or phone-ready claim.

Return clean code/contract/tests and ONE factual report rebuild/lanes/c/ASTRA-NATIVE-SCALE-FEEDBACK-REPORT.txt with exact parent/diff, executed verdicts, source-backed semantics and any concrete wiring/rule gap. No push, PR, merge, deployment, private data, purchases or other branches. Root will assign an independent reviewer and wiring after source is stable; do not create your own review chain.
