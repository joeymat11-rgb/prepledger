# Correct stale visible scale evidence after a real source change

Temporary Astra PM September12. Read NEXT/ROADMAP/LANES, ASTRA-NATIVE-SCALE-TODAY.md and candidate report. BASE=C:/Users/joeym/Documents/Codex/2026-09-04/read-rebuild-t3-brief-md-and/work. Builder qualification_delta_review (not independent reviewer phone_release_review) creates BASE/astra-native-scale-today-correction/codex/astra-native-scale-today-correction at exactde90349fea7cc68ebada5a2d6469e244e5d7b14b. Native scale author currently independently reviews your form; do not alter that tree or await it unnecessarily.

Independent reviewer executed75+real Chrome and reproduced actual-page stale evidence: open Why with baseline170/count1; another real shared reading host saves180 on same date; await page.refresh(). Model now baseline null/count0 with resolution required, but visible Why still shows170/count1 because redraw handles only screen===today. Exact runnable retained .tmp/scale-cache-review.mjs in BASE/astra-native-scale-today-review; preserve original script/tree. Read final review when it arrives and coordinate other concrete findings before completing correction.

Reproduce source-before witness, then fix existing page refresh/render ownership so open read-only evidence views refresh or visibly withdraw stale quantities after source changes, refusals and calendar/context loss. Preserve active workout/check-in/weight drafts and the accepted retained-day behavior; do not solve by forcing every screen to Today or refreshing editors destructively. Scope only narrow today-entry/today-app lifecycle regions as actually necessary and owned appended local-scale-feedback tests (a separate small lifecycle test file if clearer), plus ONE report rebuild/lanes/c/ASTRA-NATIVE-SCALE-TODAY-CORRECTION-REPORT.txt. No calculation/host protocol/store/schema/source-pin changes or nutrition form integration. If source indicates another file is necessary request the exact region.

Verify actual source mutation while Why open including normal weight, conflicting same-day read, correction/removal and refusal where same path matters; confirm next screen/back remains correct and no async redraw after closed page or editor loss. Preserve original tests; replay original independent witness on corrected source and run only affected scale/owner lifecycle checks and real relevant browser flow. Do not repeat unrelated full/private campaign or edit known stale pin/count tests. Same independent reviewer validates delta, root accepts, distinct integrator follows. No installs, other-tree edits, extra agents, private data, push/PR/merge/deploy or self-acceptance.
