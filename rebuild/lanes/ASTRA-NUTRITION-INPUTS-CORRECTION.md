# Correct nutrition reviewed-commit execution ownership

Temporary Astra PM September11. Read NEXT/ROADMAP/LANES, ASTRA-NUTRITION-INPUTS.md/bab83aa, original candidate contract/report and independent input review when committed. BASE=C:/Users/joeym/Documents/Codex/2026-09-04/read-rebuild-t3-brief-md-and/work. Original input author qualification_delta_review owns this correction. Create BASE/astra-nutrition-inputs-correction on codex/astra-nutrition-inputs-correction at exact938913671d9dbd352f90adee963e4aef02cee43d. Own only inputs-boundary.cjs, narrow local-client.mjs execution/guard composition, the existing new nutrition-inputs.test.mjs and a necessary exact contract clarification, plus ONE report rebuild/lanes/c/ASTRA-NUTRITION-INPUTS-CORRECTION-REPORT.txt. No other source or old tests.

Independent reviewer phone_release_review has reproduced through actual Today forwarding APIs: prepareP; start era.commitNutritionInputs(P) without await; immediately call era.client.execute('nutritionInputs', same proposal). Raw execute returns acknowledged:true/Saved and commits one note; intended reviewed call returns NUTRITION_INPUT_STALE/Entry not saved. Global active preparation is borrowed by a separately queued raw call. Root inspected inputs-boundary active lifecycle and local-client execute composition; this is a required correction, not merely a changed test expectation. Do not allow a matching payload alone to transfer the prepared command's execution ownership.

Bind authorization to the specific private reviewed invocation/attempt, while reusing the existing C1 bridge, actual revision/token/context/lease checks and commit-before-success machinery. A private execution route or equivalent properly scoped binding may be used; do not add a new store, transport, global lock framework or bypass existing failure normalization. Public raw nutrition execute must never acquire another in-flight preparation, whether its payload matches or differs. The legitimate reviewed call must truthfully report its actual committed result, exactly one note, no phantom sequence use or false not-saved result from its own race. Preserve idempotent retry and stale review after genuine unrelated writes; serializing away all normal workout/weight usage is not acceptable.

Reproduce the original independent real-host race at938913 first. Add meaningful deterministic concurrent controls for matching and different raw requests before/during an intended reviewed commit, repeated legitimate commit, actual unrelated weight write/currentness and existing quota/postcommit failure paths. Preserve original10 input assertions; run those plus necessary affected existing local-client/setup checks. Avoid new broad C4/page-pin/full/private campaigns. The known inherited page-pin failure remains documented unchanged. Read the final independent report; route any additional substantive issue to PM before widening scope. Review will return to the same nonauthor reviewer after your exact final candidate.

Scale Today builder now owns localized public-client/host/today-bindings/Today UI in its separate tree; it does NOT absorb938913. Keep this correction confined to inputs-boundary/local-client as above, report any unavoidable overlap first. Do not change typed wire/plan meanings, schema/lease/authority validators, physiological rules, initialSetup, crypto/repository, source-policy pins/workflows/frozen laws, private data or other trees. Retained dependencies only, no installs, extra agents, self-review, push/PR/actual integration merge/deploy or soak. Return clean exact candidate/report with before/after executed witnesses and remaining limitations.
