# Correct the owner browser proof's acknowledgment waits

Temporary PM September11. Small priority correction to owner-entry candidated05876fbc01765f7958f6e5a1a58e06e9a53f263, independent /root/ntc_join_review. Use NEW isolated work/astra-owner-browser-fix, codex/astra-owner-browser-fix fromd05876f. Preserve current B1 UNKNOWN work at a safe point; no discard or cross-branch copy. Read the eventual independent report once committed; current exact reviewer findings are below. Source authoring may proceed before its report finalization.

The reviewer independently reproduced with retained real Playwright that waitForFunction(async()=>false) returns false immediately. New test local-owner-browser.mjs uses async predicates at lines45/51, so those calls do not establish the intended observed-save truth. Additionally, the second predicate some(op.kind==='fact') already matches the earlier weight fact, not specifically the new recovery check-in. Both are test-control defects; independent correctly awaited default boot/midnight/draft/close/reload proofs passed, and no product defect was found.

Change ONLY rebuild/m3/w6/test/local-owner-browser.mjs and ONE builder correction report. Use bounded, genuinely awaited truth polling over the real public client state, with an explicit timeout/failure; do not use an arbitrary sleep or weaken expected counts. Require the actual newly written session-set fact/operation after the set action and the actual new recovery profile earned/recovery-checkin/v1 with the intended Moderate answer/new identity after check-in. An existing weight fact must not satisfy the recovery fence. Preserve operation identity/count and reload assertions, no production source changes.

Execute the corrected actual built-browser journey and a meaningful false/timeout control proving an absent expected new operation cannot advance the harness. Retain exact public saved facts, no hidden source bypass. Reuse unaffected builder241/lifecycle15 and independent120/product/browser evidence; no broad rerun is required. Return exact commit and focused report, then the same independent reviewer verifies this delta. No new install, private data, push, merge, deployment, full campaign or other worker edits.
