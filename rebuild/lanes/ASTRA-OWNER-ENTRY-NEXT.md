# Next owner entry/calendar join — ready after setup review

PM September 11. This is the next concrete build assignment, not a ready-to-use claim. Depend on the accepted version of initial-setup candidate3f1d172 and B-NTC composition; preserve any corrections from independent review. Before dispatch, PM gives a separate branch based on the reconciled candidate and exclusive Today/local-clock ownership. Initial-setup author/reviewer must no longer be editing overlapping local/today-bindings.mjs.

Default shipped boot must read the actual authoritative initial setup and current local date, then create Today/gym/check-in on the existing one-store installation. It must never silently supply createBasisState or SYNTHETIC_DAY. Unconfigured startup returns a visible setup-required state with no fabricated athlete/history/targets and no misleading functional logging; damaged storage keeps the existing restore-required behavior. Explicit demo mode retains the fictional example in a distinct database/namespace and is visibly labeled. Switching modes cannot open the other mode's data.

Use initialSetup().basisState as immutable starting state and the actual store projections for subsequent observations; do not present stale snapshot histories as current truth. Preserve the baseline-projection candidate as a separate later join if it is not yet accepted. Empty recovery/nutrition data remains unknown, never demo GREEN/100 or demo calorie numbers. Preserve all approved visual tokens and interaction hierarchy; no new aesthetic or unsupported coaching promise.

Current calendar requires real date AND truthful timestamps/offsets. Today/local fixed13:00Z, -05:00 and monotonic0 adapters are explicit test/demo tools, not the owner default. Introduce a coordinated actual-clock mode that retains explicit injected clocks/day controls for existing tests and does not silently override a supplied provider. The same sampled operation context must agree on local date, timestamp, offset and real permission-time checks. Respect C4 named clock mismatches and one installation per page. Reopen/foreground/local-day change and before-write checks must avoid stale-day recording; retain unsaved drafts and original active Start identity. Prior-day session handling must use the existing explicit unfinished-session resolution path, not retarget an old performed set or silently discard its draft.

Likely exclusive files: today-entry.mjs, today-model.cjs and Today host wrappers; local/today-bindings.mjs and a focused local/calendar.mjs; affected Today/local tests; ONE builder report. No frozen engine/client/law/fixture edits or custom prescription policy. Cite exact ownership changes in the eventual builder branch. Do not implement Dad's pending six-screen design as if approved. Existing-programme versus proposed-starter choice remains a separate onboarding question; owner-entry plumbing can be built against explicit setup without inventing either person's inputs.

Acceptance: actual default boot with a synthetically enrolled real setup and no basis/day injection, app label/date from that setup/current clock, no fixture bleed; fresh and damaged/unconfigured cases distinct; real weigh-in/set/check-in stamps coherent;23:59→00:01, next-day reopen, DST/offset change and UTC+13; explicit test clock behavior unchanged; stale handles, original active-session identity and typed draft survival; demo/owner storage isolation. Independent source/execution review, affected CI both OS and actual browser journey required. Actual iPhone save/restart/next-day proof remains outstanding until owner executes it on the identified reviewed hosted build. No deploy, private import/export or phone readiness claim from local tests.
