# Prevent rebuild branches from publishing the frozen instrument preview

Temporary PM September11. Bounded deployment-workflow correction, LOCAL preparation only. Current deploy.yml preview job publishes the frozen app on every non-main push, even docs-only Earned rebuild work. This is a verified side effect obstructing publication of real reviewed authority/CI branches; slice-host.yml is the separate authorized hosting path for the rebuilt app. Root may assign ordinary reversible local fixes; no external publication is authorized here.

Create isolated work/astra-rebuild-publication-guard, codex/astra-rebuild-publication-guard from actual accepted integration7b1678a4a8449053000d52a0a8e9e070f32a240e. Read NEXT/ROADMAP, deploy.yml entire workflow and slice-host.yml trigger. Change ONLY .github/workflows/deploy.yml plus ONE builder report under rebuild/lanes/c/. Explicit scope licence for this named workflow; do not edit any frozen app/source/script/data or other workflow. Preserve all test and production jobs byte-for-byte; preserve ordinary non-rebuild feature preview behavior.

Add the smallest job-level preview condition excluding refs/heads/rebuild/ and refs/heads/codex/astra- (these are this project's existing rebuilt-app branches). Keep its current non-main / non-PR restrictions. Do not exclude all codex branches, disable CI, skip checks by commit-message marker, alter Netlify token/site lookup or run the deployment to test. Update only directly misleading comments to describe the dedicated rebuilt-app path. No workflow_dispatch expansion, new action/package dependency, secret, remote setting or trusted author shortcut.

Validate actual condition/syntax through available tooling; exercise a branch/event matrix showing main push/dispatch still reaches only original production after unchanged test dependency, pull requests never deploy, rebuild and codex/astra authority/product pushes still run the original tests but cannot reach frozen preview, and an unrelated feat/ or codex/ branch retains its prior preview behavior. Include prefix near-misses and workflow_dispatch cases, with meaningful original-versus-corrected result. Verify production/test bodies and dedicated slice-host bytes unchanged. Do not invent a GitHub CI success; local/source validation scope only. No install or broad engine tests.

Return exact committed candidate and concise report for one independent reviewer. Root prepares publication only after review and concrete real authority/source policy are ready, preserving no-main/no-production/no-private/no-protected-soak boundaries. You must not push this guard, open a remote PR, merge actual integration, create a Netlify draft or deploy anything.
