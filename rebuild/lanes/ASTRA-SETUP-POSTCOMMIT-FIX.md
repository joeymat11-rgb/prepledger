# Initial setup post-commit state correction

Builder assignment, temporary PM September11. Exact reviewe9d39b8f806142a18fc244ffbbfe981e0be6f39f in work/astra-initial-setup-review/rebuild/lanes/c/ASTRA-INITIAL-SETUP-REVIEW.txt is authoritative for the required delta. Read it, NEXT.md, ROADMAP.md and ASTRA-INITIAL-SETUP-BUILD.md. Candidate base3f1d172; separate work/astra-setup-postcommit-fix, branch codex/astra-setup-postcommit-fix. Own only local-client.mjs, focused local-initial-setup tests and ONE updated builder report. No other builder edits this branch.

Fix the same-client false first-run after repository.initialize has already committed, but key persistence or enrollment-marker write fails. Preserve meaningful state18 refusal and align status(), boot(), initialSetup()/host access and reenrollment protection with fresh-factory behavior. Track actual initialized state; do not claim configured/success, retry enrollment over committed data, or erase anything. Pre-commit validation/quota failure must remain honestly fresh and permit a valid retry. Keep existing ALREADY_INITIALIZED/concurrent protections. Distinguish merely attempted initialization from confirmed durable commit; respect repository contract for ambiguous exceptions.

Use review's executed fault controls for key failure and marker failure after durable generation, both same-client and fresh-factory outcomes, then attempted second submission cannot replace setup. Also delayed successful marker emits no early saved success, and precommit quota failure remains first-run/retryable. Existing109-test evidence is reusable outside changed error paths; run focused tests plus affected local-client suite once. No broad engine/Today campaign unless concrete changed behavior warrants it.

Commit the minimal fix, meaningful regression tests and one factual report. Return exact commit, controls and remaining review/CI/integration obligations. No self-acceptance, private data, install, push, merge, deployment, purchase or protected soak.
