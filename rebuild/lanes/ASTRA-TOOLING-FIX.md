# B-NTC prerequisite: truthful child execution accounting

Owner-authorized Astra continuation; root PM assignment. Independent reviewer report: work/astra-tooling-review/rebuild/lanes/b/reviews/TOOLING-REVIEW-ASTRA-r5.md at b148622da0937fc13529e9aa3b0fb80c86fe4028. Target tooling7748880701ef62246c8362c43f760ee10f86c2cc. This brief is a corrective implementation task, not acceptance of that candidate.

Separate author creates codex/astra-tooling-fix under work/astra-tooling-fix from7748880. Do not alter original Claude or independent review worktrees. Exclusive author scope: rebuild/lanes/b/tooling runner, meaningful focused regression tests and one report, and runner-pin updates in its six package specs only where mechanical. No product engine/runtime/host files, frozen laws/oracles, shared STATUS/DECISIONS, actual receipts or accepted artifacts.

1. Derive executable targets from the actual Node command mode. Bare-script children must not count unused trailing positional arguments as scripts executed; reject ambiguous additional file targets rather than infer execution. Preserve multiple explicitly executed file targets in supported --test mode. The exact executed-target definition must govern validation, source pins, ownChildren, inherited coverage and moved coverage; avoid divergent parallel lists.
2. Reject role:new for files already in either the parent PRODUCT map or parent executionPins. Preserve valid carried/edited versus superseded-by-child distinctions and exact pre-images.
3. Add only the requested exact child-test roots rebuild/m3/w6/host/test/ and rebuild/m3/w7-preview/today/test/. Preserve traversal/absolute-path/argument/extension/allowlist controls. No broad root exemption.
4. Keep MOVES_RULING null for now. Exact B-NTC successor mapping and authority will be supplied separately after construction/review; this task must not invent acceptance or unblock every coverage move. No weakening full gate/private readiness/receipt ancestry/envelope or existing child-failure behavior.

Required focused evidence: bare-script first-pass/second-throw forgery refuses; true multiple --test execution both passes good children and fails a throwing child; direct own passing/failing cases discriminate; inherited trailing-argument forgery cannot claim originals covered; role:new rejects parent product and parent execution files while legitimate modes pass; intended roots admitted and traversal/foreign roots refuse. Tests should exercise the actual runner logic and actual Node execution, not duplicate an idealized parser. Reuse unaffected reviewer r4/r5 evidence; no broad full campaign. Report actual expected refusals distinctly from product failures.

No installs, private fixture reads, pushes, merges into integration, deployment or fabricated receipts. Commit a bounded candidate and one report, return exact HEAD/checks to PM and original independent reviewer. Independent review must accept before engine builder relies on this tooling.
