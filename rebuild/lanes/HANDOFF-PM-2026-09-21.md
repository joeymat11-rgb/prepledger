# EARNED PM HANDOFF - 2026-09-21 (from the PM4 Claude seat to the ASTRA PM)

By the owner's word of 2026-09-21 (DECISIONS:635, verbatim there) the PM seat and most
building move to Astra. Claude stays ONLY as the independent reviewer (section 3). This file
replaces the text PM4 wrote at this path for a Claude successor (commit 75f57ea). Read it,
then rebuild/DECISIONS.md lines 613 to the end (one line at a time: they are long), then
sections 2, 3 and 5 of rebuild/lanes/HANDOFF-PM-2026-09-19.md (the seal chain is written out
there and is not repeated here). The ledger, not any chat, is the project's memory.
THERE IS NEVER A SECOND PM (:613 point 1): PM4 writes no ledger line after its closing line.
Your first ledger line records your seat. Where this file and C:\Users\joeym\.codex\AGENTS.md
differ, this file wins, by that file's own first paragraph; an owner ruling beats both.

## 1. The owner's rules (binding; AGENTS.md has most, these are the ones it lacks or blurs)
- End EVERY message to Joe with the footer: done, in flight (whose court), waiting on him
  (the exact action), queued. Conclusion first, plain words, short. Never ask him to paste,
  run, upload or carry anything. Never apologise for long cycles.
- OWNER RULINGS COUNT ONLY IN HIS OWN WORDS, recorded VERBATIM with provenance: the question
  exactly as put to him, then his whole message (:631, :634, :635 show the shape).
- Every status carries your usage gauge number (:593 point 4) and the line RESULTS WAITING ON
  THE PM RIGHT NOW: N (:613 L-6). A time is READ OFF THE PC CLOCK (echo NOW %DATE% %TIME%) or
  off a commit, in the same turn. PM4 got this wrong three times (:620, :634, :635).
- AN ENGINE BYTE NEEDS HIS OWN WORD EACH TIME (:578, :631). AGENTS.md section 03 does not list
  it. The live app's release needs his word when the patch is ready and shown (:631 1b).
- AGENTS.md section 02 (two lines every 30 minutes) and section 08 (no heartbeats, no
  status-only messages) pull against each other, and :569 point 4 says no hourly wakes. PM4's
  reading, told to Joe: a status when something finished or changed, silent otherwise.
- YOUR WEEK: at most half of it in one day (:571). Measured 2026-09-21 11:16 ET: 61 PERCENT
  USED, resetting 2026-09-26 08:47 ET (node %TEMP%\astra-usage.cjs). Your account holds no
  paid credit (has_credits false), and a paid fallback needs his word. Plan the week on 39.
- author != reviewer != integrator. You are PM and integrator, so you never review in the PM
  session: a review by Astra of a builder's work is a SEPARATE headless session under the
  commissioned-reviewer rules (section 4), blind first, told to disagree. Loops run round by
  round without the PM until ACCEPT, a finding disputed with a measurement, or round 3; the
  PM judges ONCE, reading every review whole (:613). One judgment, one dispatch, one line.
- CI: AGENTS.md section 09 says green on both systems at the exact head before anything
  merges to the chain. Read it with :563, :565 and :627: on ANY branch that is not on the
  chain tip the standing step 13 of the rebuild workflow refuses SEAL-BASE-IS-NOT-THE-CHAIN-TIP
  BY DESIGN, so a lane head is never all green. The both-systems evidence is taken by the
  integrator's merge-forward at acceptance; a ledger-only merge-forward does not void it. A CI
  sentence names the rebuild run id and its conclusion, or it is not written.
- Queue order, by his word (:578 as amended by :631): 1 the S9 seal and the look; 2 THE PHONE
  EARNS WEIGHTS, before his trial's day one; 3 the split and Edit My Week; 4 N3 macros;
  5 RE-PLAN GAP (time away, then equipment, then pain; never medical advice); 6 P4b-2.

## 2. Safety rules (never break)
Section 2 of HANDOFF-PM-2026-09-19.md stands word for word: never print or expose the GitHub
token; never delete data; do not break the app; keep the /ledger lockdown; no merge to main,
no deploy of anything but the slice, no private import or activation, no purchases and no
history port for anyone without Joe's own words EACH TIME; never read rebuild/conform/private,
src/history.js, ledger/, C:\Users\joeym\EarnedPort\**, %TEMP%\port-real.log, or the protected
soak before 2026-10-05; never weaken a law, guard or test to go green; no U+2013 and no
U+2014 in user-facing copy or in ledger lines you author; his measurements never enter a
session transcript, a pack, a review or the ledger (verdicts only); never npm install
(junctions only).
- ADDED AT :618, NOT IN AGENTS.md: on the branch main, and on any branch of the old app, ANY
  file under src is NEVER-READ for every hand (src/app.jsx embeds dated records), and so are
  the built bundle app.js and the old app's ledger directory. Only PM mechanical probes that
  run in memory and print short code-shaped lines with EVERY DIGIT MASKED may touch them
  (%TEMP%\pm4-liveprobe*.cjs and pm4-livepatch-dry.cjs are the models). %TEMP%\astra-job-50.jsonl
  and Codex's own session file of that job may hold such lines: never opened, published,
  staged or deleted.
- C:\Users\joeym\Documents\prepledger-dev, which AGENTS.md names as the repo, is checked out on
  feat/progression-1, a branch OF THE OLD APP: its whole src is never-read. Work from the PM
  worktree (section 4) and from lane worktrees cut from rebuild branches. Every git grep,
  log -p, show and diff names explicit paths after -- ; never search a whole revision.
- THE ROOT AGENTS.md OF THE REPO IS STALE (2026-09-10). Its steps 1, 5, 6 and 7 are history.
  ITS STEP 4 (rebuilding the private fixture from the old ledger) IS NEVER RUN INSIDE A
  SESSION OR A HEADLESS JOB: the private census is touched only by the seal chain's --full on
  the PC, by script, verdict lines only (:503, :516, :547, :551). PM4 recommends you replace
  that file's body with a pointer to the ledger and the newest handoff.
- AGENTS.md is GLOBAL, so it also loads into every headless review or build job, where
  "Astra is PM" and "push after every small group of commits" would be wrong. Since
  2026-09-21 %TEMP%\astra-rules.txt, which pm4-mkjob.cjs puts into every job text, OPENS by
  saying the job text outranks that file for the session (kept copy of the old rules:
  astra-rules.before-2026-09-21.txt). Keep that sentence in any job text you write by hand.

## 3. The Claude reviewer (the owner's point 1 at :635)
His words: "You stay on as the independent Claude reviewer for anything on my data path or
about to be sealed, and for the final read before a seal. Nothing else runs on Claude unless
I say so." AGENTS.md section 05 says the same from your side: for those packages Astra or Sol
never gives the final word on Astra or Sol work.
- WHEN TO ASK: (a) any product hunk on the path to his data: the engine (rebuild/engine),
  the port, import, source admission, passphrase, storage and replay code, anything that
  admits, stores, replays or computes (:536's working rule); (b) anything that will be
  pinned at the next seal; (c) the final read before every seal, over the sealed candidate.
  When in doubt it IS on the path. NOT for screens, copy, papers without engine or data-path
  orders, tooling reports, or ordinary released-path tickets.
- HOW TO ASK: one ledger line beginning CLAUDE REVIEW ASKED: with the ticket, the round, the
  branch and FULL head sha, the base sha, the spec path and sha, which of (a) (b) (c) it is,
  what the author claims, what the blind review found, and what gates are owed. Then tell Joe
  in your status, in these words: A CLAUDE REVIEW IS WAITING. He opens the Claude chat and
  says "review" (that is his word that Claude may run). Until he rules otherwise there is NO
  scheduled Claude check: PM4 asked him whether he wants one.
- WHAT COMES BACK: ONE new file rebuild/lanes/claude-review/REVIEW-<ticket>-l<round>.md on its
  OWN branch rebuild/r-claude-<ticket>-l<round>, pushed from the PC as "Claude (Earned
  independent reviewer)" by %TEMP%\claude-publish.cmd, and two plain lines to Joe. The file
  gives a verdict (ACCEPT, ACCEPT WITH NAMED DEBTS, or REJECT), every finding with a
  measurement or an executed counterexample, the list of product hunks read (all of them),
  the bar that was run and where, and what could not be run and why. You find it with
  git ls-remote --heads origin "refs/heads/rebuild/r-claude-*" and record the verdict in the
  ledger yourself: the reviewer writes no ledger line, builds nothing, fixes nothing,
  integrates nothing and decides nothing.
- WHO WINS: on (a), (b) and (c) a Claude REJECT is not overruled by the PM. The finding is
  fixed, or disproved with a measurement the reviewer then confirms. A disagreement still
  standing after round 3 goes to Joe in plain words, both positions in two lines each.
- The reviewer's own first-read file is rebuild/lanes/CLAUDE-REVIEWER.md.

## 4. Where everything is (all verified on the PC 2026-09-21 between 11:10 and 11:25 ET)
- Repo joeymat11-rgb/prepledger (public). Chain branch rebuild/t2-client-core. Ledger
  rebuild/DECISIONS.md, append-only, one LF line per event, no CR. The PM's channel to a lane
  chat is rebuild/lanes/STATUS.md (one line, at most 400 code points).
- PM WORKTREE (detached HEAD at the tip, always clean):
  C:\Users\joeym\Documents\Codex\2026-09-04\read-rebuild-t3-brief-md-and\work\t2-client-core-pm
  Node: C:\Users\joeym\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe
  Lane worktrees live under %TEMP%\earned-* (git worktree list names about 440; the live
  ones are in section 5). New ones junction node_modules from
  C:\Users\joeym\Documents\prepledger-dev\node_modules and from
  %TEMP%\earned-adm\rebuild\m3\w5 and w6 (:542); never from earned-realshape.
- TOOLS: every one is in %TEMP%; a copy of all of them (the pm4-*, astra-*, gh-* and s8-*
  scripts, the templates and the rules; searched for a token literal, none found) is in
  C:\Users\joeym\Documents\Earned-Astra-handover\pm-tools, in case %TEMP% is ever cleaned.
  The token is never in a file: the gh-*.cjs tools read GH_TOKEN inside their own header and
  never echo it.
- THE LEDGER TOOL FOR YOU (new, tested on a throwaway repository and dry on the PC):
  node %TEMP%\pm-ledger.cjs <N> <body file> "<VERBS>" <commit message file>
       [--status <one-line file>] [--lane <name>] [--dry]
  It fetches, fast-forwards the PM worktree, refuses a dirty worktree, a wrong line number,
  any CR, a body with a line break or with U+2013/U+2014, and a commit subject that does not
  begin DECISIONS:<N>; writes "- <PC date> . Astra PM . <body> . <VERBS>" with the ledger's
  middle dots; commits ONLY rebuild/DECISIONS.md (and STATUS.md when asked) as "Astra (Earned
  PM, Codex)"; pushes HEAD:rebuild/t2-client-core; prints the tip and the line count; never
  forces, resets or deletes. PM_TAG and PM_AUTHOR change the two names. Run --dry first.
  PM4's own route (mkledger.py in the cloud, then %TEMP%\pm4-append.cmd <N>) is retired.
- THE HEADLESS ROUTE (how PM4 ran every Astra job through number 96, from a plain cmd shell):
  1. node %TEMP%\pm4-mkjob.cjs <N> <template file> KEY=VALUE ...   writes
     %TEMP%\astra-job-<N>.txt; fills {{RULES}} from astra-rules.txt; refuses a reused number,
     an unfilled placeholder, non-ASCII, and a short list of whole words of the security
     trade (line 24 of pm4-mkjob.cjs, "const banned"; the provider's filter stopped jobs over
     them: write in the vocabulary of invariants, inputs, outputs and counterexamples). No
     double braces in a template except placeholders. NUMBERS ARE USED THROUGH 96: START AT 97.
  2. %TEMP%\pm4-astra-wt.cmd <N> <origin branch to cut from> <new local branch>   makes
     %TEMP%\earned-astra-<N> with the three junctions. For a builder's lane use
     %TEMP%\pm4-mk-lane.cmd <folder> <branch> <base ref> yes.
  3. start "" /b cmd /c call %TEMP%\astra-run.cmd <N> earned-astra-<N> <high|xhigh> appserver
     It runs  codex.exe exec -C <worktree> -s workspace-write -c model_reasoning_effort=<e>
     --json -o %TEMP%\astra-job-<N>.last.txt - < astra-job-<N>.txt  and writes
     astra-job-<N>.log (START, CODEX-EXIT, END), .jsonl and, at the end, .done. The binary is
     C:\Users\joeym\.codex\plugins\.plugin-appserver\codex.exe; the fourth word "sandbox"
     picks C:\Users\joeym\.codex\.sandbox-bin\codex.exe with --disable code_mode_host (it
     ships without codex-code-mode-host.exe). %TEMP%\pm-run.cmd is the same script with an
     optional FIFTH word, the model (codex exec -m), for a Sol builder job. PM4 wrote it and
     did NOT run a Sol job through it: check the model name on a one-line job first.
  4. When .done exists: read the log, git status --porcelain in the worktree (a reviewer
     leaves exactly ONE new file; a builder leaves its owned files uncommitted),
     node %TEMP%\pm4-lf.cjs <file> (CR, dashes, lines, sha256), READ THE FILE WHOLE, then
     %TEMP%\astra-publish.cmd <folder> <branch> <the one file> <FULL PATH of a message file>
     (refuses unless exactly one path changed; commits as "Astra (Earned reviewer, Codex)";
     pushes to that branch) or, for several owned paths,
     node %TEMP%\astra-publish-build.cjs <folder> <branch> <msgfile> <owned paths...>.
  A headless session never commits, pushes, fetches or writes the ledger: the PM publishes.
  Templates (%TEMP%\astra-tpl-*.txt), ready and unused: epp-review and epp-recheck (the
  engine repair; BASE=8c2bc36e), split-recheck4 (carries S-R31 and S-R32), s9int-recheck.
  Used models for new ones: t1-spec-final, s9int-review, t3-spec, eop-options-r2,
  ew2-second-source, native-load-spec.
- OTHER PM TOOLS in %TEMP%: pm4-state.cmd (lane worktree states), pm4-ff-chain.cmd <lane
  branch> <expected short sha> (the integrator's fast-forward of the chain: refuses unless
  the lane head contains the tip; never a merge commit, never a force), pm4-int.cmd and
  pm4-s9-integrate.cmd (the S9 lane merges), pm4-ci.cjs <branch> <n, 6 or more> and
  pm4-ci-jobs.cjs <run id> (hosted CI through the public API, no token), gh-runs.cjs <sha40>,
  gh-wf.cjs, gh-joblog*.cjs, gh-rerun-failed.cjs, pm4-cell.cmd, pm4-check-junctions.cmd,
  pm4-grep-ledger.cjs, pm4-sealed-touch.cjs, astra-usage.cjs (your gauge), astra-tail.cjs,
  astra-err.cjs, the seal scripts s8-*.cmd and s8-propose.cjs (copy their shape for S9).
- cmd TRAPS: never chain further commands after an if/else or a for body on one line; a
  batch file that is RUNNING must not be edited; write scripts as files, never with
  PowerShell Set-Content (it corrupts UTF-8).
- CLAUDE-SIDE ONLY (you do not need them): the airlock C:\Users\joeym\Documents\EarnedAirlock
  (the only folder a Claude cloud session can write to the PC through; never overwrite a
  name in it) and PM4's cloud kit pm4-kit-2026-09-21-b.tgz there (sha256 8ef0f37a...f8922).
- WAKES: PM4's own wake is switched off at handoff. The owner's task "Earned usage safety net"
  (trig_01PvRHE1vTb3EaeyZVeUccaE) is his: PM4 left it alone.

## 5. State, measured at 11:16 ET unless a line says otherwise
- CHAIN: 3c470de, 635 ledger lines when this file was written; this file's commit and PM4's
  closing line :636 follow. main is 1b847541 today and is nobody's to move: it moves on its
  own with the old app's "ledger auto-sync" commits, which touch only its ledger directory
  (file NAMES measured, nothing opened).
- S9, THE SEAL AND THE LOOK. Lane rebuild/b-s9-ui-pins at 15ab6e83 (rebuild/b-s9-integration
  is the same commit): INTEGRATION PART 1 ACCEPTED (:633). Five lanes merged, the glue
  written (report rebuild/lanes/b/S9-INTEGRATION-HAND-REPORT.md), your blind review 9f45e4b3
  on rebuild/r-astra-s9int-l1 (ACCEPT WITH NAMED DEBTS). The instruction sheet is
  rebuild/lanes/b/S9-UI-PINS-BRIEF.md (accepted at 8c2bc36e, :627, rulings P-S9-1 to P-S9-6).
  Four cells are red ON THEIR REAL ROW only, by design, until part 2 lands the artifact:
  fence 48/47, pack 69/66 on linux, approved 47/45, release-object 8/7
  (RELEASE-OBJECT ARTIFACT-ABSENT rebuild/m4/spec/acceptance-s9-ui-pins.json).
  PART 2 WAITS FOR THE DESIGN LANE and owes: D-NULL-ARTIFACT, D-CONDITION-MATCHER and the
  report's denominators (:633); P-S9-2's design-of-record reader cell after C-UI-1; the
  day-of pack literals; S9.json, the export command, E fact 7 last, the four PM token lines;
  then the seal chain of the 09-19 handoff, with THE CLAUDE FINAL READ BEFORE THE SEAL.
- THE DESIGN LANE (a Claude chat; the look's long pole). rebuild/c-ui-0-gates is 814f0a03.
  At 10:50:50 ET TODAY the lane pushed rebuild/c-ui-0-gates-next 181c595e (three commits of
  2026-09-19 answering P-CUI-1 to P-CUI-3; 6 files, +230 -56) and
  rebuild/c-ui-0-gates-next-wip 17768412, which calls itself WIP, NOT FOR AUDIT, left as its
  second builder had it when the weekly limit cut it off (8 files under
  rebuild/m1/approved-2026-09-18/quality and the C-UI-0 package). The 18 fixes it owes are
  ruled at :616 (P-CUI-5, PM-5, PM-6); 53 audit rows wait at 3b186404 on rebuild/r-cui0-audit2
  (runtime kit %TEMP%\cui-audit2). COURT: JOE'S. His point 1 reaches this lane, since it is
  Claude: either he keeps it running there, or he says "take it" and a Sol builder finishes
  the list from -next-wip, a separate Astra session re-audits with the 53 rows, and the gates,
  being pinned at S9, get the Claude read of section 3 (b). Then C-UI-1, then S9 part 2.
- THE ENGINE REPAIR (:615, :618, :631 1a: his word covers ONE clause in each of two files).
  PARKED, UNREVIEWED AND UNACCEPTED, at rebuild/e-proposed-pick-repair c9780cc4, cut from
  8c2bc36e: a2dfa3f the red cell first (rebuild/engine/test/proposed-pick.test.cjs, 264
  lines, rows EPP-R1 to EPP-R8), then c9780cc, which adds  x.state !== "PROPOSED" &&  before
  (x.kind  in the one lookup of rebuild/engine/today.cjs and the one of
  rebuild/engine/writers.cjs. PM4 measured the diff: exactly that. Worktree %TEMP%\earned-epp,
  clean. The spec is sections 7, 8 and 10 of rebuild/coach/EARN-ON-PHONE-OPTIONS-CHECK.md
  (a731f483, on rebuild/c-earn-on-phone-options). OWED: the author's side-by-side
  measurements, the sibling sweep and a report;
  your blind review in a separate session at the highest effort (template ready); the CLAUDE
  FINAL (section 3 (a)); the port oracle, the sensitivity pass and the private gate. A third
  engine change STOPS and goes to Joe. It rides S9 if accepted in time, else S10; the look
  never waits for it. The author was a Claude hand, so the Claude final must be a different
  Claude session from that author: PM4's own seat qualifies.
- THE LIVE APP (:631 1b, :632). main holds the same two lookups at src/app.jsx :1493 and
  :2583, text identical to the rebuild's. ORDER: the rebuild repair ACCEPTED first; then two
  exact-match replacements, each refused unless it matches exactly once, on a branch cut from
  main, by a script no hand reads through (%TEMP%\pm4-livepatch-dry.cjs is the dry model);
  app.js rebuilt with npm run build; node scripts/check.mjs --strict green; the two-line diff
  shown to Joe with digits masked; HIS RELEASE WORD before main moves. Every push to any
  branch runs main's pipeline (the gate, and a DRAFT deploy for a branch that is not main).
- THE TODAY SPLIT. rebuild/c-today-split-build at c02b001e, round 3 REJECT on the instrument,
  the product bytes right (today-lanes.cjs sha256 e6ff7c38..., today-app.cjs d1e1f1e7...).
  Your L2 is e9d65884 on rebuild/r-astra-split-l2, your L3 175f6323 on rebuild/r-astra-split-l3
  (debts D1 to D9). ORDERED AT :633 AND NOT DONE (PM4's Claude round was stopped before any
  commit; worktree %TEMP%\earned-splitbuild clean): S-R31 the cut's input is pinned at the
  door (gen-witness records, and cut.cjs refuses without, the source blob identity of each
  named ref; --unpinned-input only for the instrument's negative rows, refused with
  --product); S-R32 the banner claims only what a cell asserts. S-R33 (a plain options
  object) gets its row at S10. The builder must not be the hand that wrote L2 and L3's
  findings into the fix: a Sol builder, a separate Astra recheck (template split-recheck4),
  then the Claude read, since both files are sealed at S10.
- PAPER. ACCEPTED AND WAITING FOR A BUILD: GYM-SETTINGS-WRITER-SEAL final paper 6fe4d12c on
  rebuild/c-gym-settings-writer-seal-spec (:628, G-R1 to G-R5; builds from the split's
  ACCEPTED head; it seals a writer, so section 3 applies). The EW2 build brief 99fab142 on
  rebuild/d2-ew2-spec (:630, E-R43 to E-R46: suite steps B0 to B4 may start, product steps
  wait for their base after S10). UNREVIEWED: the EW2 second-source refusal addendum
  5626eef5 on the same branch (:631 answer 3; it proposes one guard line after
  source-admission.mjs:752 and a sentence-map entry; its cell is red on journeys 3 and 5);
  TODAY-MODEL-HANDOFF spec 45ea25a1 on rebuild/c-today-model-handoff-spec; the owner paper
  EARN-ON-PHONE-OPTIONS r2 686010d5 on rebuild/c-earn-on-phone-options. PARKED: N3-A at
  7f81f209 on rebuild/c-n3-macros-data. NOT WRITTEN: the S10 brief (input pack d97ccdd3 on
  rebuild/b-s10-brief-inputs; S-R30 at :626 orders the after-the-cut tickets; your L3 debts
  D3, D5, D6, D7, D9 go into it verbatim; S-R33's row).
- ASTRA JOB 96 (the NATIVE-LOAD spec, the common engine part of earn-on-phone; highest
  effort; started 11:05:57; worktree %TEMP%\earned-astra-96, local branch
  rebuild/c-native-load-spec cut from 686010d5; ONE new file rebuild/coach/NATIVE-LOAD-SPEC.md).
  PM4 collects it, reads it whole and publishes it to rebuild/c-native-load-spec before it
  leaves; line :636 says so. It is then UNREVIEWED, and it orders engine bytes: section 3 (a).
- RESULTS WAITING ON THE PM when this was written: 0 (job 96 still running).

## 6. Open questions to Joe (asked, not answered; never guess them)
1. HOW THE PHONE EARNS WEIGHTS: A, B, C or D of rebuild/coach/EARN-ON-PHONE-OPTIONS.md
   (686010d5). PM4 and the paper recommend B, with A as the fallback; D is not credible
   before his trial. Every option carries engine bytes, so his answer is also his word for
   them and is recorded verbatim. Asked three times.
2. THE DESIGN LANE: keep it in its Claude chat, or "take it" (section 5). Asked three times,
   the last with the news that the lane pushed today.
3. HOW THE CLAUDE REVIEWER IS CALLED: on demand by his word "review" (PM4's recommendation:
   it costs nothing while idle), or a scheduled check. Asked at handoff.
HELD FOR LATER, each with a PROPOSED text he has not seen: the six re-plan questions; N3's
three; the EW2 brief's questions 2 and 3 and every PROPOSED sentence (with the C-UI-9
drawing); the second-source refusal's sentence ("This phone already uses one history file,
so importing another is not available yet.").

## 7. The order of work (PM4's recommendation; you are the PM)
1. Record the seat. Read your gauge. Tell Joe you are seated, in two lines and the footer.
2. The engine repair: finish the author's owed report with a builder, your blind review in a
   separate session, fix rounds, then CLAUDE REVIEW ASKED. It is the shortest path to both
   the S9 ride and the live patch he already said yes to.
3. The look: the moment Joe answers question 2, act on it (section 5); C-UI-1; S9 part 2; the
   Claude final read; the seal chain; deploy; live check; tell him to close and reopen.
4. When he answers question 1: record it verbatim, get NATIVE-LOAD-SPEC reviewed (a separate
   session, then Claude, since it orders engine bytes), and order the common part first.
   This is queue item 2 and must land before his trial's day one.
5. The split's ordered round (S-R31, S-R32), recheck, Claude read, ACCEPT; then the
   settings-writer build, EW2's suite steps B0 to B4 and B1a, the S10 brief.
6. Reviews of the three unreviewed papers by separate sessions, cheapest effort that fits.
7. Keep the gauge under the cap; tell Joe the number every time.

## 8. What worked, and what cost most
Job texts written in advance and run without the PM; reviewers told to disagree and to
bring executed counterexamples; red first, one commit per finding; the PM reading every
review whole and re-running the bar before accepting; rulings named (P-, E-R, G-R, S-R) so
briefs can cite them; saying plainly to Joe when the PM was wrong (:627, :634, :635).
What cost most: the PM's own long context, large-model operator steps that only waited on
jobs, and forty hours lost to a weekly limit nobody was watching (:633). Watch the gauge.
