# Launch integration: stable validation window

State: PREPARING, not active. PM will publish a separate explicit START with the final immutable integration tip and notify every active lane. Do not infer a freeze from this preparation document.

## Why

B's unchanged H3 run at C2b9b09a and both OS jobs in rebuild34738510266 all refused `SEAL-BASE-IS-NOT-THE-CHAIN-TIP` before suite/census execution. Integration had advanced from the candidate's ancestor8f992ae to f050f4a through coordination commits. DECISIONS145 checks current integration ancestry even for this screens candidate; status commits can invalidate the check. No engine-law failure or private census PASS follows from that refusal.

## Preparation before START

1. C completes the independently found unattributed-prefix guard fix and preserves the old2b9b09a evidence. D publishes its separate preflight correction under200; D2 independently reviews the relevant exact heads and produces actionable findings. All required code/bar/custody changes are disclosed before final composition.
2. Publish the shared review/routing/status documents needed for the handoff. Record exact source heads and preserved reports. B's issuer-only tooling198 remains separate from this launch candidate and must not be silently included.
3. PM chooses the final shared integration tip after those preparation documents. The published START commit itself becomes the frozen tip, identified by its resulting full SHA in direct task messages. C absorbs that real tip and any separately reviewed preflight changes licensed for composition into one new candidate. No stale ref, pin rewrite or parser exception substitutes for ancestry.

## While active

- All lanes and PM pause pushes to `rebuild/t2-client-core`, including STATUS, REQUESTS, DECISIONS and other docs. Read-only fetches are allowed; never hide unexpected remote movement by deliberately keeping an old local tracking ref. Report unexpected movement and replan the window.
- Continue work and publish evidence only on each lane's own branches. Send direct task messages for coordination; queue shared-doc updates until the window closes. Before publication, integration must match the recorded START tip T before and after the required runs. The only expected transition is the authorized fast-forward from T to validated final commit M; afterward verify M while all other publication remains paused.
- C publishes the final composed candidate on its own branch. D2 checks the exact candidate and any composition effects; B alone executes the unchanged private-reading H3 gate196. Existing complete Windows/Ubuntu rebuild CI must pass at that exact candidate; a skipped Today step or separate green pipeline is insufficient.
- Mechanical preflight runs with full custody and only an actually verified exact-head CI run. Its corrected source classifier must be independently reviewed; an older FAIL cannot be renamed PASS. P1 build/render attribution controls, fresh browser evidence and required real profile kills remain.
- If code changes or the shared ref moves, preserve the previous evidence and name the successor that needs retesting. Do not repeatedly rerun an unchanged stale candidate or waive the guard.

## Judgment and integration

PM judges the named complete evidence. During the window, any PM-authored approval document is committed/published on a separate PM approval branch so it does not move the frozen shared tip. A separately appointed integrator, neither author nor reviewer, composes that PM approval with the reviewed candidate in its own worktree/branch.

The integrator composes final commit M, including PM's conditional approval and the integration ledger entry, on its own branch BEFORE final gate/CI/preflight. It checks that exact tree and requests B's required exact-tree gate service, without reading private fixtures itself. Required exact-head both-OS CI must pass for M; check any resulting source differences, not only an earlier report. PM's final direct authorization then names M and its completed evidence. Run IDs learned afterward stay on separate evidence branches/direct messages so recording them does not change M and restart the exact-head cycle.

After confirming the real remote still equals T, only the specifically authorized integrator fast-forwards integration to the already validated M. Do not create another merge, squash, rebase or documentation commit after validating M. A different commit needs its own required exact-head evidence. The ancestry check under145 is satisfied both while integration is ancestor T and after integration equals M; no frozen/stale-ref exception is required. Keep other shared publication paused while the integration push's run is verified at M, then PM explicitly ends the window and reconciles queued docs. An old candidate rerun after integration advances can legitimately refuse ancestry; preserve its original evidence. No main push or relay deployment is licensed.

This is a coordination procedure under193/194/145, not a gate-supersession token, a machine freeze exception, code approval or current permission to merge. Existing engine receipt/pin/FULL rules remain unchanged for future engine packages.
