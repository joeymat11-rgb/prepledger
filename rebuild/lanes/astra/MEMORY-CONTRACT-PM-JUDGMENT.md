# Memory local contract: provider direction and finite corrections

2026-09-13 · Astra PM · Under202/204/238. Select E1290ce5f020542a223e430f0ed728c055da88ee8's proposed single local decision/controller direction for the incremental pin/CI map. Runtime remains held. PM read/rehashed its36-line19217-byte contract SHA256438b62b6f474b39983b12f992647c61be674ff80edc5f8ce537b531a35f95dd3 and an independent public-code planning audit; this is design evidence only.
The contract materially closes the earlier join: one authenticated schema2 local event; actual selected sweep-issued ladder and complete existing writer effect; authenticated derived legacy-local views; a single ordered provider for null and selected import; actual idle-host refresh preserving active/uncertain workouts and drafts. No hosted authority edits, second applier or null-only full completion. G1 prerequisites and final implementation release remain.

E makes one short amendment to the existing contract, not another general audit:
1. Close the wire grammar. Specify exact decision/handle identity inputs and domain separators; the execution clock/ID transcript, source/frontier/revision/semantic-target shapes, all digest inputs and canonical lossless encoding must be unambiguous. Derive the engine confirmation stamps and T2 effective envelope from the same trusted confirmation sample, preserving separate issue-time identity. No caller clock/id/body/effect override and no regenerated historical transcript.
2. Name three states: S0 is the original authenticated prefix before issuance; Soffer is a clone of S0 containing exactly the selected real sweep-produced proposal, excluding all other newly swept offers; Sresult is actual applyProposal(Soffer,...). Store before/after digests and complete effect for S0 -> Sresult, including creation/resolution of the selected proposal and the writer's adjustment/feed consequences. Before/after comparison must not accidentally use Soffer as the replay base. Preserve every unrelated original field/row and fail on extra effect rather than discard it.
3. Put the committed-retry branch first after current installation/context authentication and authenticated-op lookup. An existing matching decision returns its current restored status, including retained/unresolved when applicable, without requiring the old pending map, historical session epoch or unchanged original prefix. Preserve that historical epoch and original timestamps. Only an uncommitted confirmation requires the live private handle/session/prefix/offer checks. Same ID with a different body still refuses; no duplicate effect or automatic reapplication after an unavailable verification read.
Also change the sentence claiming existing Q commitments include decisions/effects to a requirement of the future extension. D's current S3 implementation does not provide that capability. The named prepareDecisionBasis/restoreCurrentProgramme/validateDecisionCandidate and private ladder capability are prospective, never existing exports or permission to bypass current guards.

Provider custody is selected for the future memory package: D will be the one provider author after its S3 R1–R10 correction and independent acceptance, for m3/w6/local/source-admission.mjs and m4/import/{engine-provider.cjs,browser-replay.mjs,local-source-profile.cjs}. This is a reservation, not a change to current D correction scope or priority. The final base must be the accepted repaired D successor. m4/import/replay-core.cjs stays conditional on a demonstrated shared-fold need and a later exact grant; no parallel memory projector.
E retains the proposed seven shared source paths: coach/{decision-commands.cjs,decision-history.cjs}, client/{index.cjs,face.cjs}, m3/w6/{t2-stage.cjs,public-client.mjs,local/host-bindings.mjs}, plus its named tests only after release. C remains sole actual coach/Today consumer author. client/plan.cjs,client/ops.cjs,local/local-client.mjs and authority/* remain outside the selected implementation subset. Any actual protected host lifecycle export needed must be named before code; do not silently widen C scope.
B is assigned only an incremental public metadata/pin/CI map while its exactcc6a1315 review candidate remains frozen: start with prior73eee8f38-path map and E1290ce5's precise delta. Cover the four proposed D provider paths, T=today/{today-model.cjs,today-entry.mjs}, new T/test/memory-check.mjs, proposed provider/W6/Today test edits and decision mutation mode, plus changed transitive consumers that the concrete join actually reaches. Full paths are under rebuild/m3/w7-preview for T and under rebuild for others. Name direct/carried/current-product/diagnostic pins, exact later successor route and literal public/test/browser/current bothOS commands; no new source/profile/pin/workflow edits or gate execution. Separate the current inspectable D prototype from the required accepted future base; do not manufacture future post-image hashes. The current B1+B2 ER/D2 review remains first, and this map is docs-only in B's own tree/branch. No repeat whole38-path audit.
E may supply its exact amended contract and B may map the already selected path delta in parallel; the three grammar/protocol corrections do not require a new broad discovery round. Once those and the actual provider/host source boundaries are closed, PM can release the finite implementation under the existing post-N2 order. Source-only assurance is not a runtime PASS.
Required complete memory proof stays real eligible issuance -> displayed exact reason/change -> bound yes -> one durable operation/complete effect -> actual Today/gym changed prescription -> close/reopen -> same historical reason/status -> later saved workout preserved, for both null and selected imported sources. Preserve no-offer/decline/refusal/race/failure/forgery/session/duplicate/rollback controls and own assertion-killed mutants. Separate reviews, B's actual final gate/bothOS evidence, third integration and owner/private/phone controls remain. No new lane, science, S3/P1 prerequisite, runtime execution or release permission is granted.
