# E308: atomic read-only storage-head publication

PM314, 2026-09-13, under193/266/308/312. Grant the concrete repository boundary repair; do not substitute an unproved single-writer policy or another final load.

1. E reports original150 baseline passing and69 new controls with67 passing/two real assertion failures: revision and same-revision ciphertext/token can change after the final load captured its record and before it returns. These are author-reported results, not PM/independent executions. Preserve an immutable local RED checkpoint with both exact assertions and original outputs before the repair, then name both red and corrected sources at handoff. PM read the full315-line accepted-M repository: readRecords resolves only on transaction completion200-213; loadActive265-268 then awaits unseal188-198, including key/decrypt awaits. Thus load is authenticated snapshot evidence, not an atomic check at a later local publication.
2. Add ONLY rebuild/m3/w6/repository.mjs to E's writable source paths, in the same fresh308 tree/base. Its accepted-M preimage is17378 bytes,SHA2564d8696382792d05585174652ac23b6589be9269d92eed0de37c8980d296a8912, already in the original50 public input inventory. Implement one additive trusted internal read-only current-head callback capability and expose it on the existing repository return object for the308 client call. Existing initialize/commit/load/recovery/import/adoption/seal/token/record validation semantics and writers remain unchanged; reuse existing validRecord/token checks rather than another encoding. The two existing test entries and <=35-line E report remain the only other paths: five changed files total. No dependency/loader/fixture/workflow/pin/manifest/browser execution expansion.
3. The capability snapshots/validates expected namespace-bound revision and full token before asynchronous work. Open an actual readonly transaction on the existing generations object store, get active, and inside that request's onsuccess validate the record and compare exact revision+token. Missing/malformed/stale/same-revision-different-ciphertext heads refuse without invoking publication. Do not expose the raw database/store handle, weaken authentication or use a readwrite transaction/no-op generation commit. Earlier actual authentication and complete-history proof remain required; equality to that authenticated full token carries the proof into this final boundary.
4. Within that SAME synchronous request callback, the client rechecks all private context/session/observation/currentness/queue/uncertainty/preparation fences and the exact-true trusted currentness callback, then rechecks private fences after that callback. Publish and retire/fence the obsolete idle handle there with no intervening await, Promise continuation, microtask, decryption, secondary read, event dispatch or deferred publication. The repository callback is trusted internal client code; do not confuse its result with the nested312 publisher's required undefined return. Preserve all312 publisher-contract/unknown-result defenses. Enqueued work or changed guards while waiting for the transaction must still refuse.
5. The storage ordering basis is [IndexedDB transaction scheduling, section2.7.2](https://w3c.github.io/IndexedDB/#transaction-scheduling): overlapping readwrite transactions cannot start while earlier overlapping transactions remain unfinished; earlier writes finish before this read transaction starts. PM infers that the synchronous active-record comparison/publication in that live readonly transaction supplies the needed ordering point. This is not evidence of a completed browser run, permanent freshness after that point, or atomic durability of an in-memory callback. Future candidate prepare/Start must retain their existing generation checks.
6. Track whether publication was actually invoked/completed. A setup/request/transaction refusal before invocation preserves the old usable state. Settle successful repository completion only on the transaction's complete event. Abort/error/close or callback failure after a publication attempt produces an explicit distinguishable unknown-publication result, never a no-effect refusal or false success. Preserve already-performed old-handle retirement/fencing; do not revive an old handle because a later transaction event failed. Keep sticky replacement uncertainty and forbid automatic refresh retry; no durable receipt/write is created. Do not claim a storage abort rolls back local references. The later real consumer must handle this outcome before activation.
7. Repair and retain BOTH original failing assertions; do not move the pause to hide them. In the same existing real-client tests, exercise independent connections/handles to the same actual fake-indexeddb database: earlier committed revision/token change refuses; a conflicting write scheduled after the read transaction waits until its callback/transaction finishes; publisher sees the expected record at the ordering point. Prove the client rechecks queued/context changes while awaiting the final transaction. Include missing/malformed head, transaction-begin/read failure and abort before/after publication where the real test adapter permits deterministic control. A failure injected after publication must prove unknown classification and no old-handle revival. No simulated replacement repository or monkey-patched successful guard counts as these positives.
8. Prove no put/delete/clear/readwrite transaction from the new method and unchanged active/previous/adoption generation, full collections/ops/outbox on a no-competitor successful replacement. Separate the competitor's deliberate write from the method's own effects. Preserve existing command/retry semantics and original150 baseline assertions. Run the same two licensed test entries plus focused cases and meaningful assertion-breaking reversals/restorations; source/dependency identities and all failures remain in the final evidence. A new required supporting path still comes to PM before expansion.
9. Continue at XHIGH now; no extra planning or owner decision. D2/MAX independently reviews the final exact five-file candidate with308/312/314 and source before E's author report. Additive repository scope still needs review and later combined regression/pins/CI/browser/phone applicability; it does not admit full memory, source/provider/controller work, private gates, import or deployment. B tooling work remains separate and active.
