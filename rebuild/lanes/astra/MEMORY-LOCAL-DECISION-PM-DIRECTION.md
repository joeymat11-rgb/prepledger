# Coaching memory G2: selected local decision contract

2026-09-13 · Astra PM · Design selection and finite contract assignment under202/203/204. This selects a technical route for the already-approved memory outcome; it releases no runtime, pin, gate or data work. All abbreviated product paths below are relative to rebuild/.
PM read the delivered C a3a10a3 consent note, B73eee8f pin map and E239ca308 G2 proposal, independently checked their Git hashes and the relevant current public staged/client/Today/source interfaces. An independent public-code planning audit agrees on the local route and identifies the remaining restoration seam. Source inspection is not executed product proof.

## Selected route

Use a closed `earned/local-coaching-decision/v1` profile through the existing schema2 staged local route. It represents consent and an engine-permitted effect saved on this installation. Do not impersonate hosted authority, mint accepted-log receipts, advance hosted W or relax LOCAL_ERA_NO_INBOUND. Keep authority/{validate,issue,admit,plan,reduce}.cjs unchanged for this phase; the old producer-only assumption is withdrawn.
One trusted local coordinator obtains the real existing sweepLadders issuance, its immutable identity/body/reason, and invokes the existing applyProposal on the exact validated state. The model supplies a handle and explicit confirmation only; coach/memory consumers never call the writer independently, choose quantities, fabricate reason/effect/authority or add an adaptation rule. Use a public eligible exercise whose actual next prescription changes; all-refusal cannot satisfy M10/J6.
One authenticated decision operation is durable truth. Bind its closed profile, installation/session, proposal/decision identity, exact source/engine revision, observed causal frontier and programme basis, canonical offer/reason digest, explicit yes and the engine-produced effect. Preserve units, unknown/missing/null values and all actual writer consequences, including resolution/audit fields. Encode the minimal complete effect, not a copied whole athlete-history snapshot. Historic reason omissions remain unknown.
The coordinator prepares an immutable offer against the real loaded revision/context. At confirmation, it revalidates identity, exact body/digest, current source frontier and programme basis; a changed offer/basis requires a new review and a new yes. Stage the operation, sequence/outbox and any derived compatibility data in one candidate, commit through the existing sealed-generation protocol, and publish success only after durable completion. No respond-then-recordIssuance second write, Promise producer, asynchronous Store.transaction callback or synchronous-stage bypass.
An identical retry verifies the current installation/context and committed decision identity, returns its existing current result and causes no second effect. A lost race without that committed decision refuses stale confirmation. An abort leaves the prior generation intact. A commit followed by an unavailable verification read retains the committed fact and reports verification unavailable; it must not retry application. Preserve session/lease/currentness checks around awaits and the actual revision compare-and-swap.
Derive legacy answers/model.issuances/issuedInstance and new decision history from the same authenticated local decision. A rebuildable cache is not another truth source. Preserve old records unchanged and distinguish local confirmation from hosted acceptance; matching a local id must never unlock an unrelated hosted unissued offer. A refusal or ordinary decline does not manufacture application or silently create memory; separately confirmed declined-reason memory retains its existing optional contract.
Restore the complete effect at its recorded source frontier before subsequent native workout facts. Do not overlay an old snapped working weight onto newer training, replay a prior workout twice, recompute a historical reason with today's engine or create a second memory-specific source projector. Reuse the accepted S3 production provider's ownership/order/currentness design. A first bounded null-selection/W=0 witness may establish that phase; completed Memory v1 also requires S3 selected-source plus later-native-operation composition. Memory is not a prerequisite for S3/P1.
Today and gym must share that canonical restored programme. The current today-model.cjs:166–183 has no decision stage; today-entry.mjs:160/164 captures state when constructing gym hosts. A model refresh alone is insufficient. After a confirmed change, retire only eligible idle preparation and rebuild from the new basis, preserving active workouts/drafts and their established currentness policy. Prove this through actual host creation, later session and reopen.

## One finite contract task, then exact runtime release

E is assigned one <=60-line `rebuild/lanes/e/MEMORY-LOCAL-DECISION-CONTRACT.md`, plus necessary index changes in its existing two briefs. This is not another general gap audit. Provide the concrete canonical operation/effect shape, real trusted coordinator entry and its stage/validation protocol, one source-order restoration insertion point and host-refresh lifecycle, exact author/path subset and literal required test/browser/mutant map. Name the existing interfaces that each step actually calls; show the positive ladder join and both source families. If one seam is not provided by current S3 core, name the exact missing exported interface/caller rather than inventing an adapter or returning a broad dependency list.
For that document, provisional shared author is E: coach/decision-commands.cjs and the minimum necessary client/{index,plan,face}.cjs plus m3/w6/{t2-stage.cjs,public-client.mjs,local/host-bindings.mjs}. This transfers design ownership of the joined coordinator from the unspecified PM-author slot to E; it is not permission to edit those runtime files. Retain C as the sole coach/Today consumer author and B as protected source/pin/successor owner. Do not use local-client whitelist changes or any authority edit without demonstrating necessity and obtaining a new exact scope.
C's known consumer set is coach/{tools.cjs,local-world.mjs,wave1-tools.cjs} plus today/{today-model.cjs,today-entry.mjs}; actual build/template/contract and tests follow the measured dependency closure. Today entry/model extend E/B's earlier38-path inventory. Entry is B-NTC-pinned; no lazy-call or replacement-host workaround may evade that custody or the shared source basis. The chosen provider insertion and these two paths require B's precise incremental pin/CI route before runtime release. B's current engine/N2 work retains priority; do not commission another full38-path audit.
E may make targeted immutable-public-code reads of those exact shared/consumer files, C's current bfc2935 versions, their existing named tests below, the public code in engine/{writers.cjs,progression.cjs} and its named synthetic ladder witness engine/test/writers-differential.cjs (no loading or following seed imports), and the eight D core runtime files at ebc4c4e: m3/w6/local/{source-admission.mjs,source-commit.mjs,source-platform.mjs}, m4/import/{local-source-profile.cjs,local-source-order.cjs,replay-core.cjs,browser-replay.mjs,engine-provider.cjs}. Read D's current S3 core report and accepted230 contract/context/provider documents to align ownership; no runtime import/execution, transitive private/seed/history/retained read, other worktree, provider invocation or new product-file edit is granted. Report any additional necessary public path by exact name.
Preserve and map current coach tiers/wave1/no-dashes, client public contract tests, m3/w6/test/{local-client.test.mjs,local-host-journey.test.mjs,public-client.test.mjs}, current Today14/A0/local journey and new decision-memory/memory-journey coverage. Require real eligible engine issuance -> displayed exact reason/change -> bound yes -> one durable decision/effect -> actual Today/gym prescription -> close/reopen -> same status/reason and applicable prescription, followed by a later workout that retains its newer result. Include both source families, no-offer/decline/stale/forged/malformed/failed commit/read/current-session/race/duplicate retry controls and assertion-failing mutants; no planted consent provider or all-refusal success.
Existing G1 prerequisites and final G3 exact pin/CI route remain. PM will release the precise runtime subset only after E's concrete shared contract and B's incremental protected-path route close the actual restoration seam; C execution still follows N2. Separate D2 implementation review, MAX for engine-affecting successor, actual final bothOS/current gate and third integration remain. No new lane, scientific rule, owner question, public/private execution, acceptance or release grant is created here.
