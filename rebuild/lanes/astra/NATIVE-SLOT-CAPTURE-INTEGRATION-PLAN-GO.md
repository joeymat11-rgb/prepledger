# Prepare the exact original-capture integration

PM404 under193/196/306/320/330/344/354/384/386/398/402. Existing B at **Astra/MAX** prepares one finite source-only integration plan while ER completes already-authorized reconciliation. ER independent-first `dd4ae18da24a614d468ced172cdf5cc89766d402` reports no blocking finding for the bounded standalone capability; its complete final review and PM judgment are still pending. No acceptance, product edit or runtime execution follows from this planning grant.

1. Use the existing owned fresh `work/pm-caretaker/native-slot-prototype-b` workspace, preserving immutable source `2592f091f9cd2956f5f6c442d36c8763e247cff6` and handoff `9cc78e7701957569fd603b9013d2ac454dacb2dd`. On a new own `codex/astra-native-slot-capture-plan` branch from the preserved handoff, write only the two plan files named below and necessary own ignored source/metadata scratch. Check for any outstanding own work first and preserve it; no reset or other actor's workspace operation. Every command explicitly uses this root. No new task, second writer, tool install or download.
2. Read NEXT/required current lane context, this entire grant and [input manifest](NATIVE-SLOT-CAPTURE-INTEGRATION-PLAN-INPUTS.json), SHA256 `bbd936dc2baa48144e3628c19a1dbc97ce455c8741bb79836513e9aa64a572db`. Source/bar references are finite and static. The actual late-observer/resource refutation at `d05ef3e9005ced9505250cd4e67f654910a9aacc` remains rejected; its R2 parser logic and original privacy/census requirements must be retained. The prototype is a separately reviewed capability, not an accepted original capture implementation. Read ER's full immutable first assessment for the actual limits; do not reopen its completed runtime experiment or interfere with its reconciliation.
3. Produce **one concrete next implementation plan**, not another alternatives/literature audit. Trace the actual named helper/operator/driver call sites, original fixed child argv and process graph. Show exactly where the parent creates/retains/reads/closes its authoritative view, how the approved original child and actual writer receive the descriptor, and how no unrelated or unfinished writer can produce an accepted result. Account for normal exit, early error/signal, late/extra progress and failed later diagnostic IO. Separate declared writer closure from arbitrary descendant discovery; identify any remaining actual source edge needed to establish the original writer set.
4. Specify the minimal loading and artifact-custody mechanism for the original invocation. Existing standalone lexical containment alone does not establish real file identity, symlink/junction safety or arbitrary loader authenticity. Name exact source/build/binary/runtime inputs and checks, the bounded transport through the existing invocation, private-directory/access requirements and failure behavior. Preserve the original child command, caller-controlled object state, return/throw, assertions, first failure, census and natural exit. No Promise/resource/scheduler/emitter hook, private payload/hash capture, fallback local state, forced termination or arbitrary command becomes authorized.
5. Name exact prospective read/write paths and function sites, baseline/source identities, dependency and profile/pin impacts. Preserve original112 product keys,26 literal registered argv,24 D-ids,9+3+7 duties, native expectations/M/U and amendment bindings; current helper.post-only rule may not silently expand. If a necessary new auxiliary/build/capability pin cannot fit the present rules, spell out that concrete change for PM before implementation. A diagnostic category/count is never exact private field-delta admission. Reuse accepted R2/caps/whitelist/ACL/drainage behavior; disclose every necessary interaction.
6. Separate the smallest Windows original-capture implementation from still-required platform/current-CI and third-integrator obligations. Explain the actual existing Ubuntu/Windows applicability of this test-only native dependency; do not assume that Windows prototype success proves Ubuntu or author a new workflow. Read the pinned existing .github/workflows/rebuild.yml statically for this route; no workflow edit or CI dispatch. Name any additional exact registration/build/consumer source needed before opening an unlisted path. Keep E's accepted build support and C400 coach delta as separately identified composition inputs for the later single I assembly, not a reason to merge all unaccepted A or repeat earlier tests.
7. Propose the smallest meaningful preregistered public synthetic proof for that implementation: actual helper/operator and unchanged original argv under invented closure; missing/extra/partial/late state despite failed IO; Promise/resource/object-state noninterference and first-failure parity; strict existing TAP and private output boundaries; loading/custody/writer/refusal controls; exact guard reversals only for affected claims. Identify which existing evidence remains applicable and what genuinely needs a new run. No build, module load, test, browser, original native/H3/FULL/CI or private/history/seed/fixture access is licensed now.
8. Deliver `rebuild/lanes/b/NATIVE-SLOT-CAPTURE-INTEGRATION-PLAN.md` (<=45 lines) and companion JSON with concrete interfaces, complete prospective path/input list, retained obligations, actual unresolved prerequisites and one recommended next GO. If a new actual source path is essential, ask PM once with its purpose; keep independent plan work moving. Do not ask the owner to approve an abstract approach. PM supplies any settled source permission and judges the final exact scope after ER completes. No new scientific policy, data import, release or acceptance is requested here.
9. Hand off immutable head/branch/report/hash identities promptly; then LOW/paused320. This task ends at the actionable plan. No implementation, product/profile changes, execution, shared push, new model, paid API, account/reset/deploy action. Preserve all previous source/evidence, use existing Codex usage, Astra only. A new capacity failure preserves work and stops without a retry/model-switch loop.
