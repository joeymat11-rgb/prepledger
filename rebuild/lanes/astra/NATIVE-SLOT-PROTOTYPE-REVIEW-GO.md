# Independent standalone native-slot review

PM402 under193/306/320/384/386/398. Existing Engine reviewer uses **Astra/MAX**. This authorizes a finite independent source and executable review of candidate `2592f091f9cd2956f5f6c442d36c8763e247cff6`, base `126fac49474ab2ba8e3dccf7ceb0707ddf65d113`. It does not authorize original capture integration, private/native gates or acceptance.

1. Use fresh own `work/pm-caretaker/native-slot-prototype-review-er`, branch `codex/astra-native-slot-prototype-review-er`, at the exact candidate. Complete2373-entry index before hydrating only the five licensed files and public instructions; every command explicitly uses the own root. Preserve previous review trees. The input-only [manifest](NATIVE-SLOT-PROTOTYPE-REVIEW-INPUTS.json), SHA256 `56423671e38fadda78358b2f1d7bb02549e8e593cff7b4aa93161b1d4145411f`, pins source and instruction bytes.
2. Remain source-first: read NEXT, required AGENTS/lane/PM context from the source checkpoint and the specifically pinned current GO/input/gate records. Required role rulings193/306/320 and owner398 are unchanged: sole PM judge, separate author/reviewer/integrator, Astra only, no private or release access. This GO supplies that current coordination authority. Do not open current STATUS/REQUESTS, outcome-bearing PM402 ledger prose, or any new B report/INDEX/SOURCES/MAPPING/INPUT-CUSTODY/raw result before your immutable independent first assessment. Historical ER368/B376 source conclusions are licensed context; preserve those known refutations without repeating their experiments.
3. Inspect all five exact files and the complete original384 bar before running. Independently assess the C Node-API handle/reference/finalizer/environment cleanup boundary, mapping lifetime/metadata/alignment/interlocked state, scalar argument validation, safe closure and ownership, loader refusal and the actual driver process/writer/outcome boundary. Distinguish logical view/header length from exact section allocation and declared writer closure from arbitrary descendants. Identify dependencies and assumptions; do not equate a source claim with proof. Read only directly needed primary Node/Windows definitions within384/386 pins; no broad Node or compiler audit.
4. You may acquire and use exactly the portable Zig0.15.2 and Node22.23.2 runtime/header/library artifacts in the original manifest, under your own ignored scratch. Reuse your own already verified matching copies if available; otherwise retrieve each exact primary URL once, check bytes/hashes and safe unique extraction before use. Follow the build's expected scratch layout; all tools/caches/temp/binaries remain in the fresh own root. No package/lifecycle/system install, administrator action, global configuration, other actor's workspace operation, other toolchain, WSL, remote or CI execution. Standard pinned toolchain/OS behavior outside the directly inspected contracts remains an explicit trust boundary.
5. If source inspection does not identify an execution-blocking defect, build the exact unchanged candidate plus its explicitly incompatible version/size fixtures and run the delivered standalone test once. Record exact source/head/runtime/compiler/header/library/binary/argv/environment/output identities, every raw stdout/stderr/exit/census and the actual nested process graph. The reviewer performs a new independent reproduction, not a replay of author result files. Add only a few distinct invented-data JS/process controls for material gaps you identify; no second binding or product implementation. Explicitly assess two-record completion, partial/extra/out-of-order sticky refusal, late writes with unavailable diagnostics, parent lifetime, forged/closed/non-owner/mismatched/missing setup, cleanup and local/stale-state negative controls. State4 without the required child outcome cannot be accepted as completion. Preserve each intentional failing child as an actual failure within its control.
6. Preserve any setup/build/test failure and stop on a decisive source/behavior defect with a concrete report to PM; do not silently patch candidate source or repeat a known-refuted cohort. A repeated run needs a new change, failure diagnosis or unresolved evidence question. Do not turn original capture/noninterference/private/other-platform obligations into new proof claims for this Windows standalone test. No original helper/operator, engine/m3/m4/Today/client/gym, private/history/seed/fixture/golden, H3/FULL/native/CI, import or app execution is licensed.
7. Commit and send an immutable independent first assessment, including actual performed source and execution evidence and precise limits, **before** opening B's new outcome records. Then you are authorized, without another permission cycle, to read the full final handoff `9cc78e7701957569fd603b9013d2ac454dacb2dd`: complete report and all five authored summary/custody records plus every mapped public evidence copy. Reconcile all material claims, source identities, builds and actual nested outcomes, record the limits of tool/archive and historical setup assertions, verify the complete mapped copy identities, and preserve the independent first verdict. Never relabel an old failed or unavailable result or claim that metadata hashes independently reproduce tool execution.
8. Write only `rebuild/lanes/b/reviews/NATIVE-SLOT-PROTOTYPE-REVIEW.md` (<=60 lines), necessary first/final assessment, reconciliation, source/custody/raw evidence and bounded review-only JS controls under `rebuild/lanes/b/reviews/native-slot-prototype-review/`, and your own ignored scratch. Do not change the five candidate inputs, old verdicts or shared policy. Do not commit binaries, archives or SDK/cache trees. Preserve verbatim raw evidence with scoped attributes and exact staged Git-blob hashes, without filters rewriting it.
9. Deliver the full exact branch/head/report/hash inventory and complete findings immediately; PM is sole acceptance judge. B is finished/LOW, so any needed author repair returns to B by a named PM scope. On completion return LOW/paused320; on a new provider refusal preserve state and pause without a retry/model-switch loop. Existing Codex usage only, **Astra only**, no spending/account/reset/deploy action. An accepted standalone capability still needs a separately licensed original integration/noninterference/writer-set/private reduction/other-platform path.
