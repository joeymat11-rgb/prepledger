# S9 public child execution-boundary inventory

Status: STATIC, non-authorizing inventory. No child, repository module, exporter, or package runner ran.
Candidate: `a23c079b1bda934257cce944fc2763d914636956`.
Canonical PROPOSED spec SHA-256: `81dab6835a7c32a5ce8a54559939cc30ca31a3abb4b5d669ba0fb142a5c70587`.
Declared surface: 33 child groups, 83 regular Git target blobs. Python font remains separate.
Method: read the exact 83 public targets and only named public helpers needed to identify immediate effects.
The protected five, forbidden private census, old root app, ledger, soak, quarantine, and logs were not opened.
This is an immediate-boundary map, not a claim that every transitive call is contained.

## Strongest synthetic/in-process candidates
- `measure-hermetic`, `a0-journeys`, `d-import-retract`, `d-replay-measure`, `d-capture-start`,
  `food-live-save`, `w6-host-seams`, `b-lom`, `passphrase-normalize`, and `f2-land` use synthetic
  state/DOM/fake storage or pure public helpers; their declared targets show no subprocess or file write.
- `reference-closure` is public source/file reading, not athlete-state execution.
- `today-carry`, `d-port-admission`, and `d-real-shape` inspect public source/fixture behavior; keep them
  read-only and synthetic, but do not infer whole transitive containment from these targets alone.

## Public build, temporary-file, Git, or subprocess boundaries
- `today-17` invokes public Today build/server helpers, generated `dist/app.js`, temp files, local HTTP,
  JSDOM, and a child Node row. This is generated output, not permission to read root `app.js`.
- `s4-real-day` shells to `git show`; `d-plan-edit` shells to Git and runs public browser build code.
- `m4-import-production`, `w6-local-source`, and `w6-local-import` spawn the public port path into temp
  output. The first two use invented state; `w6-local-import` reads the public frozen preimage fixture.
- Those three include bounded stdout/stderr tails in assertion failures; logs need path/data review.
- `d-admission-swap`, `w7-import`, `d-replay-all`, and `release-object` write only authored temp/build
  fixtures by the inspected targets, but their build/import helpers still need isolated output roots.
- `sealed-inventory-fence` shells to Git, reads the chain ref, and creates throwaway Git repositories.
  It needs full history and the exact remote-tracking ref; its Git failure object can carry command output.
- `ui-pack-pins` runs Git plus Windows `whoami.exe`/`icacls.exe` controls and temp fixtures. Failed
  assertions concatenate subprocess stdout/stderr, so OS identity/path output needs bounded handling.

## Protected-source and reconstruction boundaries
- The five `s9-sup-*` children use public `native-carriers-source.cjs`, Git blobs, dynamic compilation,
  and disk modules. Inherited/defect/writers/second-gate paths approach or execute protected engine files.
- `s9-engine-files-differential` hashes every tracked engine file and prints public path/hash prefixes;
  it necessarily reads the protected five and needs the exact existing five-file authorization at runtime.
- `m4-import` and `m4-import-production` reach the public engine-provider/import composition; target text
  names protected engine boundaries, so static closure does not authorize their module execution.
- `w7-import` page-bundle and `d-replay-all` writer enumeration call public browser builders whose graph
  includes engine composition. Keep build output isolated and treat protected transitives as a hard stop.
- No declared target text names `rebuild/conform/private`; several supersession rows consume a public
  census/SEED projection. A later runner private-census phase is separate and still requires authority.
- Public legacy reconstruction is present in import/port rows through public wrappers: invented bundles
  for `m4-import-production`/`w6-local-source`, public frozen preimage for `w6-local-import`. No owner input.

## Required execution envelope still owed
1. Accept the directory-reference source-closure helper correction before any renewed actual closure run.
2. Bind exact helper/spec/head hashes; retain all 83 targets, 512 cap, alias/link/out-of-root/forbidden rules.
3. Run children serially in an isolated checkout with pinned Node/date/TZ and existing locked dependencies.
4. Sanitize environment and disable external network; permit only explicit localhost/temp/Git needs above.
5. Bound/redact child stdout/stderr before shared logs; never print protected source, private data, or secrets.
6. Permission 766 already covers reviewed bounded read/hash/static checks of the protected five. Engine/runtime
   execution and any new private-census scope remain separate; helper changes require review, not renewed owner words.
7. Measure exact needles and zero skips per group, then run both-OS exact-head CI before package acceptance.

Exporter boundary is reused from PM inventory `S9-EXPORT-PUBLIC-EFFECT-INVENTORY-2026-09-22.md`:
the reviewed prefix imports actual repository modules and `proposed()` reads repository inputs. `Reference.create`
is excluded by that prefix cut and remains a future full-`--ci` boundary. This report does not re-audit it.
