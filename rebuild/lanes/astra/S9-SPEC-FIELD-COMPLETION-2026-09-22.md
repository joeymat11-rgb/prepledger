# S9 nonfinal field and completion procedure

Status: OFF-REPO STATIC PROPOSAL. It is not an accepted package, artifact, token, or closure result.
Candidate inspected: `80082fae7e61c2d3c4a1c8a67bf77927e3ca9a50`.
Chosen preserved sourceBase: `0cd07be7cf967dfbfea8c84947ba8477f58cfb5f`.
Draft: `S9-PROPOSED-NONFINAL.json`; canonical payload is its `proposedSpec` property.

## Established now
- Status is `PROPOSED`; `brief.acceptedLedgerLine` is null; `authorizations` is empty.
- Parent is S8; its public artifact/review identities and current public source hashes are measured.
- PM reports reviewed helper `1528b20` Stage 1 PASS: 227 parent pins including five protected identities.
- The five protected entries use provisional S8 parent-pin references; this author read no protected bytes.
- Current staged product roles: 255 total = 21 edited + 201 carried + 2 released +
  1 superseded-by-child + 21 new + 9 pinned-unchanged.
- Releases are exactly `today/preview.css` and `today/build.mjs`, each with parent pre and null post.
- All 25 S8 parent child names are retained; six use S9 mirror name/argv substitutions.
- Eight current groups are added; `d-real-shape` also carries both layout targets in its existing group.
- The 33 groups and 83 JS argv targets are planning inputs, not final measured package totals.
- Every child is exactly `{name, argv, needle}` and every needle is null.
- Font transport stays in its independent two-OS Python job and is absent from child argv.
- No deferred product/reference promotion is present.
- Both layout-v2 cells are mechanically `pinned-unchanged`: sourceBase and candidate bytes are equal.

## Deliberately unfinished
- PM-owned receipt, authority, review, release and supersession prose; no ledger text was copied.
- Final accepted brief citation, child needles, counts, pack/approved literals and combined workflow post.
- Final post-assembly public pins, five protected parent-pin substitutions, executed closure and artifacts.
- Final runner-hash verification; PM reports all seven ancestor pins already match. Repin only after an actual runner delta.

## Safe next sequence for the PM
1. Verify the frozen scratch hash with `Get-FileHash -Algorithm SHA256 -LiteralPath <draft>`.
2. Parse the envelope; serialize only `.proposedSpec` as UTF-8 without BOM to
   `rebuild/lanes/b/tooling/packages/S9.json`; do not copy planning metadata into the spec.
3. Commit that immutable PROPOSED file alone, then update only the reviewed helper's CANDIDATE
   constant. Independently review both committed deltas before invoking the modified helper.
4. After that review, reuse the exact recorded `1528b20` invocation; this worktree lacks the helper.
   Expect Stage 1 PASS and Stage 2 source closure to replace `S9_SPEC_ABSENT`; do not call `spec()`,
   the exporter, children, product modules, or protected modules merely to obtain closure.
5. Complete PM-owned pack/approved literals and authority fields, then remeasure every public raw
   SHA-256. Replace the five provisional protected references only from the reviewed 227-pin result.
6. Run each final child argv with pinned Node/date/TZ and write its observed exact needle; Python
   font evidence remains its separate Windows/Linux workflow job.
7. Verify the integrated PM-A1 comment at runner lines 1250-1264 and its final SHA-256; do not edit
   or repin H3 and S3-S8 unless the runner bytes actually change.
8. Recompute roles/counts/closure and run `node rebuild/lanes/b/tooling/b-package.cjs --ci --package S9`.
9. Serialize the recomputed artifact plus PENDING review envelope by the PM's reviewed procedure;
    any input change requires re-proposal. Both-OS exact-head CI and independent review remain owed.
