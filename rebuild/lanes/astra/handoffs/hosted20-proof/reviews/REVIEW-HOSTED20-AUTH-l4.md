# REVIEW-HOSTED20-AUTH-l4 (round 4: authorization and activation; independent reviewer claude-fable-5-1, 2026-09-23)
Worktree C:\Users\joeym\AppData\Local\Temp\earned-h20 at HEAD 07ea6f81 (= fe9f14b5 + the packet files and three reviews only; no source change), uncommitted changes as listed by the author.
Hashes verified (Get-FileHash): AUTHORIZATION.json 0c60030a; HOSTED20-INVENTORY.json 15042fbc (unchanged); HOSTED20-RUNNER.cjs.txt 7a2f6f2b (unchanged); HOSTED20-WORKFLOW.yml.txt ea40e27a = .github/workflows/s9-hosted20-proof.yml ea40e27a; SELFTEST.cjs.txt e78a8c33; SELFTEST-RESULT.json 78ef0ce6; AUTHORIZATION-REPORT.md f5b3fc30; run/ copies: authorization 0c60030a, inventory 15042fbc, runner 7a2f6f2b (byte-identical to the packet).
Nothing built, committed, pushed or dispatched by me. No real group, engine or repository module executed. Forbidden paths not opened (soak.yml not read).

## Verdict: ACCEPT WITH NAMED DEBTS (two provenance/operational notes, D-A1 and D-A2; no control weakened)

## 1. AUTHORIZATION.json (0c60030a)
- Read `origin/rebuild/t2-client-core` (ec424456) rebuild/DECISIONS.md lines 782-786 via git show. :784 is the owner entry; his message's first sentence is exactly "I approve the hosted 19-group run." (verbatim; the remaining sentences answer 2a-2d and Codex/agent questions). :785 is the PM reading: 19 groups as packet 07ea6f8 scopes them, b-lom HELD, no other permission inferred.
- Executed through the runner's own loadInventory under the three real pins (my syn-selftest4.cjs): status REVIEWED_AND_AUTHORIZED; 19 AUTHORIZED, held set == {b-lom}, 20 groups; grants == exactly the union of the 19 authorized groups' ownerPermissions (EXECUTE 19, CONSUME 18 without m4-import, APP_JSX only s9-sup-source-carriers); no grant serves only the held group; authorizedBy == {rebuild/DECISIONS.md, 784, "I approve the hosted 19-group run."}. Under the three pins a probe of the real inventory against a synthetic island now fails only on the island (STOP HEAD_NOT_CURRENT), never on an authorization code. A forged copy with b-lom AUTHORIZED STOPs on the pin (AUTHORIZATION_SHA256_NOT_PINNED) and, pinned to its own bytes, on SPLIT_GROUP_NOT_HELD. Inventory 15042fbc unchanged; 07ea6f8 vs fe9f14b5 diff = packet files only, so the source island (refs.current fe9f14b5) is the one Joe's grant names.

## 2. Workflow .github/workflows/s9-hosted20-proof.yml (ea40e27a)
- Diffed against my l3 copy (round2/workflow3.yml, sha 84ec3ba4): changes are the header comments, `on: push: branches: [rebuild/p-s9-hosted20-run]` (no workflow_dispatch/pull_request/schedule/tags/paths), job `if:` on event_name == push and ref == refs/heads/rebuild/p-s9-hosted20-run, a first step asserting GITHUB_EVENT_NAME/GITHUB_REF/GITHUB_REF_TYPE, the three pins set to 15042fbc / 7a2f6f2b / 0c60030a (equal to the run/ bytes, checked by my r4_workflow_pins_equal_run_copies), the control path rebuild/lanes/astra/handoffs/hosted20-proof/run/, and one comment line. concurrency group s9-hosted20-proof with cancel-in-progress: false unchanged; every other step byte-identical to what I accepted in l3.
- Move to run/ verified statically: fe9f14b5/HEAD rebuild/lanes/b/tooling/b-package.cjs:96 `TOOLING = 'rebuild/lanes/b/tooling'`, :2596 `function fidelity(s, sealed)`, :2598 `git diff --name-only sourceBase HEAD -- rebuild/engine rebuild/conform rebuild/m4/spec TOOLING`, :2604 asserts UNLISTED-SOURCE-CHANGE on anything not in the product/tooling allowlists. A new file under rebuild/lanes/b/tooling would fail rebuild.yml's package step; rebuild/lanes/astra/handoffs/hosted20-proof/run/ and .github/workflows are outside that diff scope.

## 3. Regressions
- Author's SELFTEST.cjs.txt (e78a8c33) run from my scratch against run/ copies and the live workflow: exit 0, 66/66 PASS, hashes identical to SELFTEST-RESULT.json (runner 7a2f6f2b, inventory 15042fbc, authorization 0c60030a). New W1/W2/A5/A6/A7/A8 read as described.
- My suite re-pointed at run/ (syn-selftest4.cjs): 72/72 PASS = the 65 round-3 checks (l3's "still NOT_AUTHORIZED" case necessarily replaced by the acceptance case above) plus 8 round-4 checks. Lazy-fetch red control, post-seal unreadability, exact materialization, forged TAP, secret containment, HELD/budget and D3b pin behaviours all unchanged. Run dir syn-1790134074197.

## 4. Scope: src/app.jsx in the shared island
- Measured statically over the 60 argv tests and all 223 literal-closure files (protected five excluded from every grep; src/ never opened): every mention of src/app.jsx or fe516c1 outside s9-sup-source-carriers is a comment or provenance string (engine "Copied from frozen src/app.jsx" headers, legacy-fixture.cjs:5, source-admission.mjs:272/336/715, second-gate.test.cjs:13/146 which checks the literal `sourceCommit:"fe516c1"` inside gate text), or a path-string in an exclusion list (build.mjs:86 and page-bundle.test.mjs:60 name src/history.js, not app.jsx). The one executable reference, package.test.cjs:372, requests "/src/app.jsx" from the today dev server and asserts 404 "Not found"; it never opens the file and its outcome does not depend on the file's presence. No fs read, readdir, glob or git show over `src/` exists in any group; the only readdir walks root at rebuild/** (13 sites, l1). Inventory agrees: the gated entry lists groups [s9-sup-source-carriers] and no other group's evaluatedReads name it.
- Recommendation: ACCEPT the shared presence. Presence is not a read; no other group can reach the file without changing pinned test bytes; the blob is identical at current and fe516c1 and Joe's grant names it. A per-group hydration would need the runner to reshape the island between groups (materialize/unmaterialize plus a changing object inventory), a new control surface that would itself need review. Not worth it for a file no other group reads.

## 5. Other workflows on the push
Author's static claim (pipeline, rebuild, shared-preflight run; slice-host and prod-check do not; soak.yml not read) is consistent with fe9f14b5:.github/workflows listing and with the PM's separate run-list confirmation that soak never starts on rebuild/** pushes. I did not open soak.yml. Those sibling workflows do full checkouts on every rebuild/** push already; this push adds nothing new to them (run/ and the workflow are outside b-package's fidelity scope and the fence artifacts, per the author; fidelity scope verified above).

## Named debts
- D-A1 (provenance) authorizedBy.decisionsLine 784 is meaningful only against the chain ledger (origin/rebuild/t2-client-core, DECISIONS.md blob 1117f9d7 at ec424456); the run branch's own rebuild/DECISIONS.md (blob b12b2ee8 at 07ea6f8) has a different line 784. The schema cannot carry the ledger commit; the PM should record "DECISIONS:784 at ec424456" next to the receipt triple in the ledger entry. Also the quote is the first sentence of a longer message; :785 is the PM's reading of the rest, so the excerpt is faithful.
- D-A2 (operational) one push = one run and the job is red by design (HELD_INCOMPLETE, exit 2) even on full success; the sibling pipeline/rebuild/shared-preflight jobs will also run on the same push. The PM must push exactly once (`X:refs/heads/rebuild/p-s9-hosted20-run`), never re-push, and read the receipt's verdict rather than the job colour.

## Read / ran
Read: AUTHORIZATION.json, workflow (full, diffed vs l3), runner (unchanged sha), SELFTEST.cjs.txt diff vs round 3, SELFTEST-RESULT.json, AUTHORIZATION-REPORT.md (last), chain DECISIONS.md:782-786, b-package.cjs :96 and :2590-2612, package.test.cjs :363-386 (public). Ran (earned-h20-review): hash4.ps1, hash4b.ps1, bpkg.cjs, run-selftest4.ps1 (author's suite), syn-selftest4.cjs (mine), grep-reads.cjs closure grep for app.jsx/fe516c1/src references.
