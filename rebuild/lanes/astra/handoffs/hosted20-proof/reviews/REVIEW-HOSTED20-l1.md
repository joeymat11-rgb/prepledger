# REVIEW-HOSTED20-l1 (round 1, independent reviewer claude-fable-5-1, 2026-09-22)
Packet: earned-h20/rebuild/lanes/astra/handoffs/hosted20-proof (uncommitted), worktree HEAD fe9f14b5a764efe0cc66fa0380a27b1e32d5672c.
All five sha256 verified before reading (Get-FileHash): INVENTORY 6e5373d6..., RUNNER 420eb917..., WORKFLOW 97266b3b..., SELFTEST a16318d1..., REPORT 240b56f4... (all exact matches).
Nothing built, committed, dispatched or fixed. No real group, engine or repository module executed. Protected/forbidden paths never opened (blob IDs via rev-parse/ls-tree/cat-file --batch-check only).

## Verdict: ACCEPT WITH NAMED DEBTS (D1-D6 below; no forbidden exposure found, controls hold under adversarial synthetic tests)

## Measurements that hold
- M1 Inventory exactness: all 1044 ref:path -> blob claims (940 current + 1 gated + 103 pairs) match `git cat-file --batch-check` at the named commits (ok 1044, bad 0); all 940 modes match ls-tree (100644 only); 7 refs' commit/tree pairs match; trees = 529 excluding the 7 provided commits, sorted-OID sha256 matches 91c10ae3...; hydratedBlobObjectIds (950) equals the union of the active sets. No current file under ledger/, src/, rebuild/conform/private, soak-stub, app.js. Script: earned-h20-review/verify-blobs.cjs, verify2.cjs.
- M2 Faithful coverage (static, public files): S9.json has exactly 20 null-needle children whose name+argv equal the inventory in order; S9 sourceBase 0cd07be7, parent artifact rebuild/m4/spec/acceptance-s8-real-shape.json sourceBase 8ebc860c = s8SourceBase; the 27 tracked engine files with no declared parent post equal exactly the 27 s8SourceBase pairs; the 45 s9SourceBase pairs equal the 45 tracked rebuild/engine files at current (SUP-1 :83 loop over `git ls-files rebuild/engine`); NC.BASE literal in native-carriers-source.cjs = 189523bd. Every readdir site in the 60 argv test files (m4/import, w7-preview/measure, measure/test, today, today/test, engine, m1/approved, fonts, conform/golden, conform/oracle, m3/setup, w6/local, p3-replay-all) is fully materialized (tracked top-level = materialized, missing 0). All 7 dependencyInputs, tools/_fixed-now.mjs, conform fixtures/oracle manifest materialized. No literal-closure file is unmaterialized. Only Git users in the closure are the five SUP tests (show/ls-files), native-carriers-source.cjs (show), local-real-day (show) - all covered by pairs/index. No python/network use in the closure.
- M3 b-lom SPLIT_REQUIRED is correct: legacy-order.test.mjs:175-189 `callers()` readdirs rebuild/m3 and rebuild/m4 recursively and reads every non-test .cjs/.mjs/.js; rebuild/m3/soak-stub/* is tracked at current (name-only count) and matches **/*soak*; LOM-S6 :698-701 asserts on the walk result. I found no second group needing a split: the other 12 readdir sites are single-directory and fully materialized.
- M4 Reachability (REPORT Q8): `git branch -r --contains` in the local clone: current 1, s9 114, s8 114, s4 141, nativeCarrierBase 418, fe516c1 461, 7347ca9 451 origin branches. All seven are reachable from origin refs as of the local fetch state (not a live GitHub measurement). `fe516c1` is unique among the 1493 island objects (1 candidate).
- M5 Runner controls, executed on a copy (earned-h20-review/runner-copy.cjs, sha 420eb917 = packet) against an invented two-commit remote with uploadpack.allowFilter/allowAnySHA1InWant, Git 2.55.0.windows.3, Node 24.19.0 (earned-h20-review/syn-selftest.cjs, 31/31 PASS, dir syn-1790119520980):
  island OK and excluded blob absent; red control: with promisor present and GIT_NO_LAZY_FETCH unset, `git show B:secret/private.json` DID hydrate (lazy fetch is real) and the next probe STOPped (REMOTE_PRESENT); with GIT_NO_LAZY_FETCH=1 it did not hydrate; island refuses existing dir; materialize writes only inventoried paths (secret/ absent); after seal `git show` of the forbidden blob fails with empty stdout even WITHOUT the env var, blob still absent; tree names remain listable (expected, scope accepts); probe OK; probe STOPs FORBIDDEN_PROBE_READABLE when a forbidden probe names a hydrated path; island STOPs PAIR_OBJECT_ID on a wrong pair blob; real inventory STOPs INVENTORY_SHA256_NOT_PINNED, and NOT_AUTHORIZED even when pinned; synthetic kind refused under GITHUB_ACTIONS=true; run: planted failure -> exit 1 / RED, fail counted, skip counted, pass counted; a child spawning `git show HEAD:secret/...` without the env var fails with empty stdout while the allowed pair read succeeds; a planted secret in an assertion message and on stderr appears ONLY in raw/02.* (mode-exclusive files), never in runner stdout/stderr/receipt; child stdout noise on success not echoed; raw hashes equal receipt; RAW_DIR_EXISTS refused; a test printing `# pass 1` is escaped by Node as `# \# pass 1`, so forged summary lines cannot mint counters (a duplicate would only make counters null -> RED).
- M6 Static runner review: STOP codes are fixed strings (GIT_FAILED_<verb> sanitized to [A-Z-]; -c values skipped, so the token header never reaches a code); INTERNAL errors print no message; child env is a closed allowlist (HOME/TMP inside raw dir, GIT_CEILING_DIRECTORIES, GIT_CONFIG_GLOBAL=/dev/null, GIT_NO_REPLACE_OBJECTS); serial spawnSync of unchanged argv, 45-min timeout -> signal -> RED; no retry/skip/filter path exists; workflow has no upload-artifact/cache/continue-on-error, deletes the bootstrap checkout, pins the inventory sha before island, --ignore-scripts on all three installs (lockfiles are single-importer, so w5 --ignore-workspace is sound; the w5 pnpm-workspace.yaml carries only allowBuilds).

## Findings / named debts
- D1 (design gap) SPLIT_REQUIRED has no executable path: loadInventory requires groupCount 20 and groups.length 20, and run requires the 20 null-needle children to match the inventory (RUNNER lines ~53-54 and ~209-212). Running 19 groups is impossible without changing the runner (and re-review). REPORT's "b-lom resolved or split out" overstates the options: either Joe rules soak-stub non-protected AND the b-lom materialization is widened to every non-test source under rebuild/m3 and rebuild/m4 (a large, unreviewed widening), or the runner changes.
- D2 (unreproducible claim) SELFTEST-RESULT.json cites selftestSha256 343e5d91... but the self-test script is not in the packet; its 20 checks cannot be re-run from the packet. My independent 31-check suite reproduces and extends them (M5); the PM should keep mine or require the author's script be frozen in the packet.
- D3 (authorization changes the reviewed bytes) status and authorization.grants live inside the inventory whose sha the workflow pins; authorizing necessarily produces a new inventory sha. Acceptance must include a byte-diff check that ONLY status and authorization change between 6e5373d6... and the pinned bytes.
- D4 (unverified hosted behavior) blob-OID `fetch --stdin` and by-SHA depth-1 fetch were proved only against a local file remote (author and me). GitHub must honor `want <blob-oid>` without uploadpack.allowAnySHA1InWant; it does for promisor lazy fetch in practice, but it is unmeasured here. Also unpinned: pnpm@9 exact, actions SHAs, Node 22 minor; runner requires Git >= 2.44 (probe STOPs otherwise).
- D5 (accepted exposure, must be named to Joe) the island carries all 529 trees of the seven commits, so forbidden path NAMES and blob IDs (ledger/**, rebuild/conform/private/**, src/history.js, soak-stub; 42 such names at current) are listable in the island; only contents are absent. Materialization is subtree-based (940 files) rather than the literal union (~226), all public; the widening is surface, not exposure.
- D6 (minor) runner never checks inv.sourceHead === inv.refs.current.commit (receipt echoes sourceHead unverified; equal in this inventory); hydratedBlobPairs `permission: "PROTECTED_FIVE"` is a label, not a gate (protected-five historical blobs are hydrated whenever the run is authorized; implied by EXECUTE grant but not stated in the three categories); the runner's own sha is not pinned in the workflow (only the inventory is; receipt reports runnerSha256); per-group 45 min x 20 exceeds the 180-min job timeout (job kill -> NO RECEIPT -> red, acceptable); the fetch token travels on git's argv (ephemeral runner, acceptable).
- Owner-permission categories: the three categories are correct and minimal for what the tests do (EXECUTE all 20; CONSUME 19, m4-import excluded and its evaluatedReads confirm no E.SEED/golden read; READ src/app.jsx only s9-sup-source-carriers, blob f98671d8 identical at current and fe516c1). Nothing broader than needed is requested. Open questions 1-10 are correct; Q8 is now partly answered by M4; add D1 and D3 as open items.

## Files read
Chain (origin/rebuild/t2-client-core): HOSTED20-SCOPE.md, HOSTED20-REVIEW-BOUNDARY.md, HOSTED20-MANIFEST.proposal.json, HOSTED20-RUNNER.cjs.txt, HOSTED20-WORKFLOW.yml.txt, SYNTHETIC-GIT-RESULT.json. Packet: HOSTED20-INVENTORY.json (via node scripts), HOSTED20-RUNNER.cjs.txt, HOSTED20-WORKFLOW.yml.txt, SELFTEST-RESULT.json, then REPORT.md last. Public repo files at current via `git show <sha>:<path>` / `git grep ... -- <explicit paths>`: S9.json, acceptance-s8-real-shape.json, package.json, rebuild.yml (grep), w5/w6 lockfiles (grep), w5 pnpm-workspace.yaml, w6 .npmrc, s9-supersede-source-carriers.test.cjs :51-130, legacy-order.test.mjs :169-194 and :687-704, grep hits across the 60 argv tests and 223 closure files (protected five excluded from every grep).

## Ran (all in C:\Users\joeym\AppData\Local\Temp\earned-h20-review): hash.ps1, inv-shape.cjs, inv-detail.cjs, verify-blobs.cjs, verify2.cjs, grep-reads.cjs, dirs.cjs, inv-git.cjs, pairs.cjs, parent.cjs, deps.cjs, syn-selftest.cjs (runner copy, invented remote only).
## Not run / not verifiable here: any of the 20 real groups (forbidden); hosted GitHub fetch semantics and Ubuntu Git/Node versions (no dispatch allowed); the author's self-test script (absent from packet); engine/protected modules (forbidden).
