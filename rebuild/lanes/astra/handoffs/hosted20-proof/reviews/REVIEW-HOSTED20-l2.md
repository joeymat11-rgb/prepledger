# REVIEW-HOSTED20-l2 (round 2, independent reviewer claude-fable-5-1, 2026-09-22)
Packet: earned-h20/rebuild/lanes/astra/handoffs/hosted20-proof (7 files, still uncommitted `??`), worktree HEAD fe9f14b5a764efe0cc66fa0380a27b1e32d5672c.
All seven sha256 verified first (Get-FileHash): AUTHORIZATION 9811ee1d, INVENTORY 15042fbc, RUNNER 98b6f358, WORKFLOW d73a0c67, SELFTEST f170ee69, SELFTEST-RESULT ef515c40, REPORT 8b47693b - all exact.
Nothing built, committed, dispatched or fixed. No real group, engine or repository module executed. Forbidden paths never opened (soak.yml not opened by me).

## Verdict: ACCEPT WITH NAMED DEBTS (D1, D2, D4, D6 closed; D3 closed in the runner but one workflow pin still missing; D5 correctly named to Joe)

## 1. Debts, each attacked
- D1 closed. Runner lines 66-75: a group may be HELD only when the inventory marks it SPLIT_REQUIRED (HELD_ONLY_FOR_SPLIT_REQUIRED), a SPLIT_REQUIRED group can never be AUTHORIZED (SPLIT_GROUP_NOT_HELD), every inventory group must be decided (AUTHORIZATION_GROUP_SET, GROUP_DECISION), all-held is refused (NOTHING_AUTHORIZED). Verdict order RED > HELD_INCOMPLETE(exit 2) > PASS; PASS requires held === 0 (line 296). Executed: my held run (two groups marked SPLIT_REQUIRED, all others green) gave exit 2, verdict HELD_INCOMPLETE, heldCount 2, no raw files for held indices, the other groups ran in order; marking a non-split group HELD in the authorization STOPped; marking every group HELD STOPped. Only b-lom carries SPLIT_REQUIRED in the real inventory (re-checked). The workflow's `set -euo pipefail` turns exit 2 into a failed job.
- D2 closed. Ran the packet's SELFTEST.cjs.txt byte-for-byte from my scratch (round2/selftest2.cjs, sha f170ee69) against the packet runner/inventory/authorization copies: exit 0, 52/52 PASS, git 2.55.0.windows.3, node v24.19.0, runnerSha256 98b6f358, realInventorySha256 15042fbc, realAuthorizationSha256 9811ee1d - identical to SELFTEST-RESULT.json. Its A/B/C/D/E/F/G/H/I/J sections are sound; the red control (D4) proves the lazy-fetch check has teeth.
- D3 mostly closed. Inventory carries no top-level status or authorization (checked; runner refuses one that does, AUTHORIZATION_INSIDE_INVENTORY, executed by the author's F9). AUTHORIZATION.json must name the exact inventory sha256 (executed: other sha -> AUTHORIZATION_NAMES_OTHER_INVENTORY), say REVIEWED_AND_AUTHORIZED, use the right kind, decide every group with exactly AUTHORIZED/HELD (PENDING, lowercase, phantom names, non-string grants all STOP in my suite). Partly decided is impossible. RESIDUAL D3b: the workflow pins the inventory and the runner (both checked by the runner, RUNNER_SHA256_NOT_PINNED executed) but NOT AUTHORIZATION.json; its sha256 only appears in the receipt after the fact. Anyone who can write the proof branch can swap a fully-granting authorization without any pin failing (they could also edit the workflow pins, so this is accountability, not a sandbox). Also no field records who authorized or Joe's words. Author's own Q9 asks this; answer: yes, add HOSTED20_AUTHORIZATION_SHA256 to the workflow env and to the runner's checks, and the PM records the expected receipt triple (inventory, authorization, runner).
- D4 honest. actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 is present in fe9f14b5:.github/workflows/shared-preflight.yml (I read only its uses:/node-version lines); rebuild.yml uses checkout@v4/setup-node@v4 unpinned; no packageManager field, no engines except w6 ">=22"; both pnpm lockfiles lockfileVersion 9.0. OPEN_PIN_1..4 are stated as open, no SHA invented. Not re-verified upstream (author says so).
- D6 closed. SOURCE_HEAD_NOT_CURRENT (author E4, and the inventory's sourceHead == refs.current). PROTECTED_FIVE_HYDRATION_NOT_GRANTED: the 12 historical PROTECTED_FIVE pairs and the 5 current files flagged protected (exactly index/merge/migrate/oracle-shim/seed.cjs, checked) require EXECUTE (author F8 executed). Budget: PER_GROUP 45 min inside a 300-min budget, timeout min(45, left) with SIGKILL, TIMEOUT and NOT_RUN_BUDGET_EXHAUSTED rows are RED; executed by the author (J1: PASS, TIMEOUT, NOT_RUN) and by me (budget 1 ms: every group NOT_RUN/TIMEOUT, RED, nothing spawned). The env override is gated to the synthetic kind (static check). Job timeout 355 > 300.
- D5 is named to Joe in plain words in REPORT.md (names and blob IDs listable, contents never downloaded). Correct.

## 2. Inventory content unchanged
No copy of 6e5373d6 kept, so I re-derived: refs (7 commit/tree pairs), trees 529 + sorted-OID sha 91c10ae3, 103 pairs, 940 current files, 1 gated file, 950 blob IDs, forbiddenProbes (25 ref:paths, 6 worktree paths), dependencyInputs/Trees, group names/argv/status/ownerPermissions - all equal to the values I recorded in REVIEW-l1. Re-ran every round-1 script on 15042fbc: 1044/1044 blob claims match cat-file, modes 0 bad, trees 529 match, S9.json 20 null-needle argv equal in order, 27 s8 pairs == parent-post gaps, 45 s9 pairs == tracked engine, NC.BASE match, all 13 readdir directories fully materialized, no closure file unmaterialized. The only structural additions are `authorizationRule` (text), and `protected: true` on the five engine files; `status` and `authorization` are removed. Core fingerprint of (refs, trees, pairs, files, gated, groups, probes, ids, spec) = f452edbc... for future diffs.

## 3. Round-1 controls not weakened
My round-1 suite adapted to the new <inventory> <authorization> convention (syn-selftest2.cjs, runner copy sha 98b6f358, invented remote only): 42/42 PASS = the 31 original checks (lazy-fetch red control, GIT_NO_LAZY_FETCH guard, post-seal unreadability without the env var and from a git-spawning child, exact materialization, pair mismatch STOP, forged `# pass 1` escaped by Node, planted secret only in raw files, raw hashes == receipt, RAW_DIR_EXISTS) plus 11 new authorization/HELD/budget cases listed above. Run dir syn-1790120766268.

## 4. Item 10 (soak.yml lines)
`.github/workflows/soak.yml` matches the manifest's `**/*soak*` glob, so printing three of its lines is a breach of the letter of the forbidden set. It is not a breach of its purpose: the file is a public tracked workflow, the lines were `uses:`/`node-version` values, no soak data, result or protected byte was read, nothing from it entered the packet beyond the (already true from rebuild.yml) statement that checkout is unpinned, and the author disclosed it unprompted. Severity: low; log it in the ledger as a disclosed grep-scope slip and require future searches to exclude glob-matching names even when public. No remediation of the packet needed.

## Remaining debts for the PM
- D3b: pin AUTHORIZATION.json's sha256 in the workflow and runner; record who authorized. (Small runner/workflow change; would need a round-3 hash refresh.)
- D4: OPEN_PIN_1..4 still open by design; hosted GitHub blob-OID fetch still unmeasured (author Q6).
- Governance, not code: a HELD_INCOMPLETE run proves 19 groups; b-lom stays unproven until Joe rules on soak-stub (author Q1, Q8).

## Read / ran
Read: all 7 packet files (REPORT.md last), runner and workflow in full, SELFTEST.cjs.txt in full; public repo lines via explicit-path git show/grep: shared-preflight.yml and rebuild.yml uses:/node-version lines, engines fields of the three package.json. Ran (C:\Users\joeym\AppData\Local\Temp\earned-h20-review): hash2.ps1, inv2-check.cjs, rerun.ps1 (verify-blobs, verify2, pairs, parent, inv-git, dirs, deps), run-selftest.ps1 (author's suite), syn-selftest2.cjs (mine). Not run: any real group, hosted GitHub, engine modules.
