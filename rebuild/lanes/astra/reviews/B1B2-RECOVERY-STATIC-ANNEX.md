# Independent recovery and tooling static annex

Candidate: `cc6a1315003adc7f3f96d635980cfdf32b5a7a74`; comparison: `100820aa47a4f8729642033499eaec0f0ee282e1` (M). Read-only source review; this reviewer executed no candidate code. Root supplied the execution results below. No protected inputs were read.
Binding references: B13 = `3647e17d:rebuild/lanes/b/BRIEF-B1-GRADING-TIME-WINDOW-v1.3.md`; FG = the same commit's `BRIEF-B1-B2-COMBINED-PREPARATION-v1.0.md`, read with the incorporated v1.2 and PM amendments.

Confirmed actual-consumer failures:
- `rebuild/engine/writers.cjs:2549–2551`: measured clock facts enter `askContext`; unknown target makes `shiftMin` null, so `shiftMin > 0` is false and the fallback says “He already clears his target.” Line 2562 later says comparison unavailable. FG:25–27 and B13:38 forbid the first claim; the later qualification does not erase it.
- `rebuild/engine/today.cjs:265–270`: a configured target plus a current row with invalid hours produces `caution / 0/3 clean`, because recency uses the row's date and the target run becomes zero. `theOneFix` at 302–305 then issues a sleep restriction. The paired reviewer fixture fills expected logging dates and food/weight prerequisites so logging cannot hide this actual decision. B13:32,38 require invalid observation to remain unknown without manufactured debt or positive clearance claims.
- Root reports the direct-helper reviewer annex yielded 18 tests: six passing controls/diagnostic and twelve intended `ERR_ASSERTION` failures, comprising six unknown-target clearance cases and six invalid-current Today cases. Valid-current 0/2/8-hour controls are included. Root also reports the original U+FG 148/148 and B1 27/27 passing; those passes do not discriminate these failures.

Committed construction-fault inventory is exactly 20 rows in `rebuild/engine/test/b1-unknown-recovery.test.cjs:144–307`:
- `U-absence-GREEN`, `U-absence-score`, `U-stale-current`, `U-future-current`, `U-coerce-null`, `U-last5-coerce`.
- `U-warning-suppression`, `U-lift-negative-GREEN`, `U-phase-negative-GREEN`, `U-volume-negative-GREEN`, `U-push-negative-GREEN`, `U-stale-lastnight-copy`.
- `FG-anchor-default`, `FG-lightsOut-default`, `FG-bodyAlarm-fabricated-2330`, `U-zero-is-observation`, `U-last5-backfill`, `FG-unknown-failed-night-counter`, `FG-null-bed-arithmetic`, `U-pick-stale-hour`.
This is 15 U faults and five FG faults. The covered scopes are absence score/band, current-date eligibility, finite-hour acceptance including zero, last-five membership/coercion, independent warning precedence, four negative-GREEN predicates, liftCall current-night prose, pickStructural recency, two duration defaults, target counter/null arithmetic, and bodyAlarm's fabricated 23:30 output.

Required discrimination still missing from that inventory:
- B13:42 explicitly requires a fault that bridges a mean gap. U5 has ordinary gap/stale controls at test lines 63–65, but no construction fault removes the consecutive-window guard. Historical `D8-3` at 351–356 returns early after the last-night threshold, disabling mean detection; it does not reintroduce gap bridging. Required paired bite: three consecutive 6.6-hour nights restrict; a gapped window does not; removing the gap guard must fail the latter assertion while a short final night still restricts independently.
- FG:35 requires targeted removal of each consumer guard. The five FG mutations do not remove the `fiveLevers`, `theOneThing`, `dayProtocol`, or `askContext` qualifications. The actual-consumer FG4 test at `b1b2-sleep-target-cells.cjs:45–49` reaches askContext but its regex does not reject “already clears his target.” Add paired known/unknown target outputs and guard-specific behavioral bites, including the measured-clock branch.
- B13:38,42 require truthful-output qualification faults. `U-stale-lastnight-copy` is the only U9-labelled construction mutation. The existing real `runAdaptive` stand-down and volume-offer assertions at U-test lines 119–124 have no targeted restoration of their old clearance/GREEN claims; the post-refeed output test at 92–95 likewise lacks its own qualification-removal bite. Invalid-current `fiveLevers`/`theOneFix` needs a direct paired output/decision assertion, not only helper-state assertions.
- FG:29,34 requires current three-night debt with absent target. Committed FG3 at target-test lines 32–35 contains only one 2-hour night. Add consecutive three-night debt versus missing/invalid/gapped measurement controls with target known/unknown, preserving the observed warning, prior cost, and truthful recovery/Today/planning output.

Separate PM interpretation: root measured the same yesterday-2h/today-8h state changing from known-target GREEN/80/sleep-cost20 to absent-target WATCH/70/sleep-cost30. The source is `sleep.cjs:243`, which substitutes `Math.min(3, s.sleep.needed) * 10` when the target is absent. FG:27 forbids extra restriction from target absence and FG:29 preserves cost/severity for the same recorded state. This diagnostic is separate from the two established consumer failures; interpretation of that preservation clause remains for PM, without inventing a new score policy.

Complete tooling helper closure for the three named synthetic tooling suites:
- `rebuild/conform/v4/postfix/run.cjs`
- `rebuild/conform/v4/postfix/target.cjs`
- `rebuild/conform/v4/postfix/legacy-gates.cjs`
- `rebuild/conform/v4/postfix/strict-json.cjs`
- `rebuild/m4/spec/native-carriers-errors.cjs`
- `rebuild/m4/spec/load-write-reference.cjs`
- `rebuild/conform/v4/postfix/trace-v2.cjs` (target's sole further import; builtin util only).
- `rebuild/m4/spec/load-write-source.cjs` (reference helper's sole further import; declarations/builtins only at load).
The three tests are `gate-supersession.test.cjs`, `pinned-unchanged-and-ruled-substitutions.test.cjs`, and `parent-pin-shapes-and-spec-successors.test.cjs` under `rebuild/lanes/b/tooling/test/`. Alongside their runner these eight helpers close all source-root reads/imports. Their exact comparison with `f195b7fe69c8ede31c322f5da72481e63b8c3b0b` changes only the pinned test's four-line IDS amendment.
Each suite asserts one main-sequence delimiter and compiles only the prefix; only the two Git fixtures replace CHAIN_REF with their own synthetic branch. Prefix reads are its scratch runner/six copied helper sources for inventories (`b-package.cjs:409–464`); native-carriers-errors additionally reads only source-root run.cjs text. Named calls use scratch files/Git; non-B1B2 checks return before real parent/repaired-carrier loading (runner:1657,1703), and null successor proof returns at 2334.
Actual effects: own os.tmpdir directories, synthetic file writes, checked recursive cleanup; gate/parent fixtures run Git init/config/add/commit/rev-parse/show/ls-files in those repositories. Pinned fixture starts no process. Evidence children are synthetic `ran` records, never spawned. The isolated envelope slice is only runner:3083–3086. No actual H3 artifact, original witness, seed, private/native/frozen input, package main, Reference.create, source baseline/verify, publicReferences, historicalAudit, faultRun, gateRun, worker, or runRaw is reached. This finite fixture closure does not license whole-runner execution.
