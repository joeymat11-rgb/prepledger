# EW2 SPEC narrow re-check, loop round 2
Reviewer: Astra (Codex), commissioned under DECISIONS:412, :569, :602 and :613; narrow re-check, loop round 2; highest effort
Head checked: 1ad61cfe6b2b5f5ad84210b3132213c9147b76c2
VERDICT: BUILD FROM IT WITH NAMED DEBTS

Scope: f6fd29ac36b49977fa94ff6df1263598fd730276..1ad61cfe6b2b5f5ad84210b3132213c9147b76c2, changed EW2-SPEC hunks, changed spike README, all six ew2r7 files, prior BLOCKING B1, single-clause mutants and the stated sealed budget. No general review of unchanged sections.
The author disputes no finding. No UPHELD/WITHDRAWN/STILL DISPUTED measurement pair is therefore owed in this round. This verdict permits a build brief carrying the debts; it does not claim EW2 shipped or waive the named sealed-file authorization.
All Node runs used C:/Users/joeym/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe, with MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York set on separate lines. Cells ran sequentially; their synchronous child helpers were awaited.
Scratch: C:/Users/joeym/AppData/Local/Temp/astra-ew2-l2-1ad61cfe. Relocated module imports only; P5 was run unchanged. Synthetic admitted generations use the author's PE16 boundary, not sealed-port admission. No actual athlete data was used.

## Prior BLOCKING item

B1 - CLOSED for the corrected resolver: my real-host input {exercise_id:"press-old",settings:[{name:"Seat",value:"4"}],cues:"Pause"} saved=true; after press-old->file-press, the old query returned null, while latestNoteOn returned the same Seat=4/Pause, saved_as=press-old, saved_in=document, including after a cold durable-client reopen; the stored op stayed byte-identical.
Card integration of B1 is CLOSED ON PAPER: the full card path is not implemented before the build; hunk E and EW-25 must carry the measured correction into it (D13). Translating the stored identity, rather than only the query, is the right correction.

## New BLOCKING items

None established. The mixed-note refusal disagreement, two-generation read, module-link gap and surviving mutants below are NAMED DEBTS, not an executed reachable saved-note loss in a compliant built card.

## NAMED DEBTS

D1-D10 below are byte-for-byte the ten complete debt lines in R6 and section 14.6 (10/10 comparison true). None was reworded; none was re-investigated. D9's changed evidence labels were independently checked and are correct; it remains carried as the author requests.

D1 - Define immutable per-edit id-space/basis provenance across first admission, native file-id edits, second selection and rollback; prove admission either resolves each saved target to the same lift or refuses before publishing, and make the document-state capture fold handle mixed histories without rewriting operations.
D2 - Implement cold-page recovery without depending on the attempted intent surviving in transient memory; reconcile authenticated stored intents before enabling any fresh review/id, and test a destroyed page/client context plus committed and uncommitted outcomes.
D3 - Name and authorize the authenticated-generation handoff for current/pending views and post-save refresh; one host.read() exposes neither both views nor its generation, and a successful save reply exposes no committed generation; do not satisfy E-R35 by two uncoordinated reads or a second replay implementation.
D4 - Name and authorize the per-adoption Start.plan_basis handoff through the shared gym installation; createGymHost ignores a per-call planBasis option, so the current zero-byte today-bindings contract cannot carry the proposed changing fold label by the stated route.
D5 - Wire and test every required planRoster history consumer with exact lift attribution and counts, including past/current/pending creations, tombstoned creation, removal and same-label re-add; a roster key existing in a fixture is not a history read.
D6 - Specify the admission refusal envelope/reason for occupied file handles consistently with EW-23, and place slugOf/reserved-id minting on a permitted import boundary; E6 forbids the released setup-model import that 13.4's direct provider call assumes.
D7 - Wrap setup-tag projection, validation and detached fold execution at admission as well as the editor; assert the defined default for raw throws and retain diagnostic detail internally, since the host's save-stage catch does not surround admission.
D8 - Complete E-R34's state table for pending-only edits, plan_edit_basis/default failures and failed authentication; show retention/cause claims only when proved, and replace C2's unsupported assertion that a missing correspondence means a different exercise copy.
D9 - Correct evidence/count labels: 11 executable cells include the 3 prototypes; the loader neither prints nor asserts its net count; J1 does not execute patched host/Start or prove three-copy identity; P2 prints a desired union rather than persisting it; four acceptance rows were added, not three.
D10 - Carry the host-day Start/readiness choice into the build/S10 brief and execute a stored Start across midnight; a 60-second interval is not a deadline, 13.7 contains no promised Start readiness check, and EW-19's visibility callbacks cannot prove either claim.
D11 - Resolve machine notes and their admitted correspondence from one authenticated generation; latestNoteOn currently loads a generation and then all() loads another, so bind both to one snapshot or prove and retry a generation mismatch without changing the zero-byte hosts.
D12 - Enforce 14.2's any-untranslated-note refusal before returning a matched record, and assert a generation containing both a resolved native note and an unresolved document note; the prototype currently returns ok=true with untranslated=["press-old"].
D13 - Carry the explicit gym-app hunk E authorization and the new resolver's path, export and browser contract into the build brief, and execute the linked card path; P5 requests a default export absent from the supplied prototype and its parse-only check accepts a missing module or missing method.
D14 - Make EW-25 assert saved_in=document, the literal MACHINE_NOTE_TARGET_UNTRANSLATED code, and the rejection's untranslated keys independently of prototype exports; P4 stays green when each of those clauses is changed separately.

## Journeys and outputs

| Independent input / invariant | Executed output |
|---|---|
| J1: save Seat=4/Pause through the real machine host; synthetic first admission changes press-old to file-press; read with old and new rules | save=true; host.latest(file-press)=null; corrected draft={rows:[{name:Seat,value:4}],cues:Pause}; original op unchanged. |
| J1 cold read: close the host AND durable client, open a new durable client on the same synthetic IndexedDB, boot and open a new host | boot.ready=true; corrected draft still Seat=4/Pause. This is a durable-client reopen, not browser/process kill. |
| J2: save CURRENT file-press Seat=6/Smooth through the reopened real host, same effective date as J1 | save=true; winner=file-press/native, draft Seat=6/Smooth; rows=2; original document op unchanged. |
| J3: remove correspondence from the resolver context, retain the document note | ok=false, record=null, code=MACHINE_NOTE_TARGET_UNTRANSLATED, untranslated=[press-old]. |
| J4: same missing map, with BOTH document and native saved notes | ok=true, record=file-press, code=null, untranslated=[press-old]. Contradicts 14.2's "If any stored note" refusal. Smallest correction: test untranslated before returning record. NAMED DEBT D12. |
| J5: instrument the resolver's lane contract | Calls repository.load then lane.all: two independent reads; real all() itself calls repository.load. No admission race or athlete-visible loss was executed. NAMED DEBT D11. |
| J6: execute existing renderBlock with state=failed and synthetic DOM methods/copy | visible block text="Read failed. Retry."; no empty-state text. This executes the view branch, not a full DOM mount/card handoff. |
| J7: retired old-lateral and new-lateral have the same label; one Pad=3 note belongs to old-lateral | new: ok=true, record=null, untranslated=[]; old: saved_as=old-lateral/native with Pad=3. Synthetic base only; no roster/admission claim. |
| J8: feed both press notes to notesFor for row-old | resolved.length=0. The coach's latestFor also filters identity, explaining the redundant local-filter mutant below. |

Author cells were re-run against this head; support and resolver were exercised through their callers, not treated as standalone tests. All six source SHA256 values exactly match 14.8 and README.

| Cell / spec transcript | Exit and line-by-line comparison |
|---|---|
| ew2r7-b1-note-read / 14.1 | exit 1 as declared; all 10 printed lines match exactly, in order. Actual stdout additionally has one blank and four explanatory lines before the red assertion. |
| ew2r7-p4-note-identity / 14.3 | exit 0; all 19 lines exactly match, including document/native notes, both date permutations, re-add, first-run and named refusal. |
| ew2r7-p5-card-hunk / 14.4 | exit 0; all 8 printed lines exactly match; actual stdout adds ALL ASSERTIONS HELD. |
| ew2r7-d9-labels / 14.7 | exit 0; all 15 printed lines exactly match; actual stdout adds ALL ASSERTIONS HELD. Its loader child exits 0 with zero stdout. |

Thus all 52 quoted transcript lines match byte-for-byte, without whitespace normalization. D9's correction is right: 13 r6 files = 8 witnesses + 3 prototypes + support + loader; 11 executable cells include the prototypes; HUNK_NET_LINES=22 is exported, not printed/asserted; 13.11 has four rows. The unchanged R6 journeys were not re-run wholesale.

## Single-clause mutations

48 individually changed copies, no edited assertions or skipped rows: 25 fail and 23 survive. Each resolver/support mutant ran P4; each card-hunk mutant ran P5. Nonzero rows below failed an assertion or a reached runtime operation, not parsing. Scratch holds every changed copy and stdout/stderr; mutations.json records M01-M40. A surviving mutant does not hold its changed clause; equivalent/redundant guards are not automatically a product defect.

| IDs | Exactly the clause changed | Output |
|---|---|---|
| M01-M04 | Remove absent-generation, absent-localSource, absent-view and ready===true guards separately | All exit 0. P4 does not drive these contextOf branches. |
| M05, M07 | Discard admitted state; discard recorded map | Each exit 1: null result reaches saved_as read. |
| M06, M12 | Accept inherited view-map / map-entry properties separately | Both exit 0. Own-property clauses not held. |
| M08-M09 | Remove invalid/blank-id guard; remove stored-id trim | Both exit 0. Host fixtures supply valid canonical ids. |
| M10-M11 | Lose first-run identity; change first-run space | Both exit 1. |
| M13-M14 | Leave document id untranslated; relabel document provenance native | M13 exit 1; M14 exit 0. The printed document label is not asserted despite 14.3's "yes". D14. |
| M15-M16 | Remove native exercise-array guard; remove null-row guard | Both exit 0. |
| M17-M20 | Native lookup accepts any row; delete native fallback; mislabel native as document; classify unknown as none | All exit 1. |
| M21-M22 | Remove null rows-list fallback; remove absent-machine guard | Both exit 0. |
| M23-M24 | Change untranslated key to wrong; remove requested-id filter | M23 exit 1; M24 exit 0. latestFor still filters the requested identity. |
| M25-M28 | Replace saved_as by mapped id; drop retained row fields; mutate frozen outer row; fail to relabel machine id | All exit 1. M27 throws TypeError on the frozen row. |
| M29-M30 | Select array tail instead of coach date rule; reverse store order | Both exit 1. Date selection and same-day order are held. |
| M31 | Apply unknown-note veto even when a resolved match exists | Exit 0. Mixed resolved/untranslated case is absent. D12. |
| M32-M33 | Remove unresolved refusal; refuse confirmed absence | Both exit 1. |
| M34-M38 | Use first-run context on lane read; discard all rows; return null on success; resolve refusal as null; omit error.code | All exit 1. |
| M39-M40 | Omit error.untranslated; change exported refusal literal to WRONG_CODE | Both exit 0. Separate literal oracle sees WRONG_CODE, expected MACHINE_NOTE_TARGET_UNTRANSLATED. D14. |
| M41-M42 | Card import points at nonexistent module; card calls nonexistent resolver method | Both P5 exit 0, including ALL ASSERTIONS HELD. Parsing/counting cannot prove the link/call. D13. |
| M43 | Mutate inner machine object instead of copying it | Exit 0. Host already returns detached machine copies; this does not prove an immutable-input contract. |
| M44-M45 | Remove era.client; remove scaffold's separately returned client | M44 exit 1 at hostBindings; M45 exit 0. My cold-client journey uses the latter return. |
| M46-M47 | Change rejection message; change success code from null | Both exit 0. Error.code remains held by M38; message/success-code clauses are not. |
| M48 | Exclude retired rows from native lookup | Exit 1. The same-label retired-note control holds that clause. |

## Sealed budget, independently diffed

H2: git diff --no-index --numstat over original/model-prototype copies returns 24 added, 2 removed, net 22, agreeing with the corrected spec. No new H2 behavior was authorized or tested here.
E: my original/card-prototype diff returns 2 added, 1 removed, net 1: one import insertion at :20 and one read-call replacement at :147. These are TWO physical diff hunks grouped under ONE named budget item E. Unicode already present in copied card source was escaped equally on both sides; the same 2/1 count holds.
The combined allowance is H1-H3 plus A-E: EIGHT named core items in THREE sealed files (plan-edit-model.cjs, source-admission.mjs, gym-app.mjs). The separately listed local-source-basis.mjs export and today-entry.mjs wiring make FIVE product files. New lane/resolver/view files and workflow registration are additional allocations, not hidden inside that count.
The correction fits the stated 2/1 E allowance only after gym-app leaves the zero-byte list explicitly as 14.4 proposes; it does not fit the old four-file/zero-gym budget. This review does not supply the PM's pending authorization. D13 carries that exact condition.
Remaining zero-byte names stay: plan-edit-host.mjs, plan-edit-commands.cjs, today-bindings.mjs, workout-host.mjs, host-bindings.mjs, rebuild/coach/**, machine-settings-host.mjs, exercise-catalogue.mjs, setup-model.mjs and setup-host.mjs. The explicit-path f6fd29ac..1ad61cfe diff over these, the five product files and rebuild.yml prints no changes. No tracked file changed during review.
P5's declared interface linked to the supplied resolver exits 1: "does not provide an export named 'default'". The spec's lane-file wording also differs from P5's new machine-note-identity.mjs path. Naming a matching export/import can keep E at 2/1; the new module contract must be explicit. This is a pre-build integration debt, not a claim that the author promised an executed DOM or that an athlete reached this link failure.
The same-generation D11 correction can remain in the new resolver by calling the existing coach machineSettingsIn(generation) and latestFor on that one load; it requires no host or coach edit. D12 is likewise an ordering change in that new resolver. No additional sealed-file hunk is justified by these measurements.

## What I did not verify

No sealed bundle through real admission; second selection/rollback; native-file provenance beyond the first synthetic admission; planRoster consumers; complete EW-21..25; stored Start; full card DOM; browser/phone/process kill; synchronization; full Today step; conformance/private fixtures; protected soak; CI or deployment. A write:false scratch browser-bundle attempt was refused resolving the absolute entry/directory by the environment; no browser-graph result is claimed.
No new hunt through unchanged spec text or re-review of carried debts. No auth file, protected data/source, install, node_modules change, receipt/artifact in the tree, commit, push, checkout, reset, stash, clean or fetch. Only this new review file was written in the worktree. Scratch is retained for reproduction; no deletion was refused.

Final commands and stdout (the report is untracked, so the diff has no output; status also emitted two environment warnings that the global git ignore file was unreadable):
```
git status --porcelain
?? rebuild/lanes/astra/reviews/EW2-SPEC-RECHECK-L2.md
git diff --stat -- rebuild/lanes/astra/reviews/EW2-SPEC-RECHECK-L2.md
```
