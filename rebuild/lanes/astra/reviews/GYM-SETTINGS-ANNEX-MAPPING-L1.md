# GYM-SETTINGS annex assertion mapping L1
Verdict: NOT FULLY SUPERSEDED. Eight behavioral clusters remain OPEN GAP, not proved defects.
Candidate: 04ea69e60dea5875a5a82b562af24b2f2f71c7be; checkout a802905fa5fa80083448dcbaf0024732792c171c.
Independent inventory first, then Claude 50e2886663f605ba64222010276f34ba9c5fcc7d read whole.
D-GSS-ANNEX-SILENT cannot be paid by aggregate counts or the present partial mapping.
Evidence E = %TEMP%/gss-review-l3-a50a5590336944ed99e915494c91e635/; no new runtime.
Inventory: E/annex-mapping-inventory.json, all 14 source blobs checked against candidate.
D = rebuild/lanes/d2/reviews/; F/R2/R3 = memory-lifecycle-{foundation,r2,r3}-annex/.
X = d2-lifecycle-extra; Y = d2-lifecycle-r2-edges; all suffixed .test.mjs.
12 cells: D/LAUNCH-ADOPTION-SETTINGS-PROBES.mjs; F/X-attempt1, F/X-before-observations;
F/X, R2/X, R3/X; R2/Y-attempt1..4; R2/Y, R3/Y. Final X/Y copies are byte-equal.
Both d2-lifecycle-r2-support.mjs copies supply device/page/editor/input/until, not extra assertions.
U = candidate today/test/machine-settings-ui.test.mjs under rebuild/m3/w7-preview/;
U execution witness E/focused-ui.log. L = six named outcomes in E/d2-01.log, directly passed.
S/W = E/section-e-{settings,workout}.mjs plus matching results; P = replacement-editor.mjs/results.
C = E/contract-probes.mjs and contract-extra.mjs plus results. These are the actual executed scripts.

PROVED launch clusters, by name; these direct passes need no invented supersession:
L 'held refusal, cancel/new draft reset, valid save exactly once, success reset': full cluster passed.
U 'S7 - a producer refusal survives a held row-add repaint and a valid retry saves once' covers its refusal/retry subset (EDITOR-LIFETIME/OUTCOME).
L 'old real quota refusal isolated from replacement draft' and 'old real committed success isolated from replacement draft': both real outcomes passed; P adds focus/retry (LATE-COMPLETION).
L 'real saved read: pending visibility differs from readiness; exact record restored without write': exact stored-read/readiness/no-write witness (CACHE-FRESHNESS).
L 'failed read cannot satisfy readiness' and 'known empty can be ready but fails saved-data equality': both passed; U 'D2.2 - a FAILED read says so, never prints the empty state, and seeds no editor' corroborates failure (CACHE-FRESHNESS).

G1 OPEN: X 'D2 acknowledged settings save preserves an actually newer editor answer'.
C/raw proves submitted bytes captured; P types only after replacement, not in the same live editor. Next: hold real Save, edit same input without reopening, settle; assert newer answer/error retained, original payload stored once.
G2 OPEN: X 'D2 independent entry edits do not prevent confirmed settings Save clearing its unchanged editor'; Y 'D2 R2 entry stepper and effort retain their new values when the unchanged settings submission confirms'.
C/independent proves admission only. Next: hold real Save, change blank entry or stepper/effort, settle; verify entry/effort unchanged, unchanged editor closes, exact settings op/outbox +1. Both variants required.
G3 OPEN: X 'D2 real settings transaction failure retains exact editor error and drafts on same-phone remount'; Y 'D2 R2 actual failed settings Save retains its current error beside independent entry edits and on remount'.
S/host-quota proves current-mount refusal/retry, not remount persistence. Next: real quota refusal, same-phone remount with same supplied draft, verify answers/error/entry/effort and old ownership; Cancel leaves ops/outbox unchanged. Include Y's held-save/independent-edit variant.
G4 OPEN: X 'D2 old actual log cannot clear a replacement mount on the same phone'.
P remints only a settings editor; W never replaces a pending-log mount. Next: hold real log acknowledgement, remount same phone/draft, change entry/effort, release; preserve new screen/draft, old ownership false, exact +1 op/outbox.
G5 OPEN: X 'D2 independent editor edits do not hide an acknowledged set log or suppress successful-log clearing'.
U 'D2.3 / S-M6 - and logs with the editor open and UNSAVED text in it' types before submission. Next: hold real Log, edit settings after submission, settle; preserve editor, show Saved/Undo, onChanged once, clear only performed entry/effort, +1 op/outbox.
G6 OPEN: Y 'D2 R2 editor row addition preserves the real submitted set payload and acknowledges it exactly once'.
S/refusal-add-remove-retry is a settings refusal, not a held set. Next: hold actual committed log acknowledgement, add/edit row and repaint, release; exact submitted set payload, preserved new rows, Saved/Undo and one callback/write.
G7 OPEN: Y 'D2 R2 actual pending set outcome reaches the current owning card after an independent editor repaint'.
W/log-quota has no concurrent editor repaint. Next: obtain actual quota result, hold delivery, add row/repaint, release; current error contains result code, entry/effort/rows retained, no onChanged or op/outbox delta.
G8 PARTIAL/OPEN: X 'D2 actual logging stays available while optional settings read is pending and navigation hands off first'.
U 'D2.1 - the card paints and the set LOGS while the settings read is still pending' proves logging; U 'D2.R2 - BACK during a deferred read: the destination screen is never repainted over' proves post-navigation silence.
Next: combine both in one real-host journey; observe ownership inside Back callback, preserve destination after release and exact +1 set/no settings write. Legacy refreshState counters themselves have no GSS replacement API.

Attempt differences preserved, not silently folded into passing final cases:
F/X-attempt1 G4 delays before commit; later X delays acknowledgement after commit. Next G4 proof needs both seams, reporting actual refusal if continuation was retired.
F/X-before-observations differs from final X only by diagnostic files; earlier Cancel lacks settled-repaint wait. Neither supplies missing G1-G5 proof.
R2/Y-attempt1 G6 delays before commit; later versions delay acknowledged success. G6's precommit variant remains separately unproved.
Y-attempt1 G7 ('actual set-write refusal remains visible...') injects quota after repaint; final Y injects quota before repaint and delays delivery. Both outcome placements require proof.
Y-attempt2 G7 ('independent editor repaint must not silently invalidate...') asserts acknowledged false then true in the same run: inconsistent oracle, no valid supersession; PM must disposition it.
Y-attempt3 requires successful precommit continuation despite repaint; Y-attempt4 accepts actual success/refusal and requires current-owner delivery. Neither equals final quota-after-execution seam. Smallest proof: separate precommit no-fault race recording actual envelope, data delta and current UI; do not assume success.
X's 30 paused-write modes and read-pause cell require model lifecycle counters/frozen snapshots/uncertain handles. Not settings-save coverage and NOT superseded by GSS; retain on D2's own API ticket, no restoration authorized here.
Support reach is in E/d2-support-coverage.json; reach before absent APIs is not completion of these assertions. 209 failed cases prove none of the open clusters.

Boundaries: census12 is closed; no thirteenth search, runtime, product edit, commit, push, fetch or ledger write.
S's 64 cases settle Save before editing/retry; W's 10 journeys never edit settings during held Log. Counts and reopen parity therefore cannot close G1-G8.
This checkpoint maps evidence only. Keep D-GSS-ANNEX-SILENT open for the named smallest proofs or explicit PM disposition before package acceptance.
