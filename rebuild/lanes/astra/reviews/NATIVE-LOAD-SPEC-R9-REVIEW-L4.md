# NATIVE-LOAD-SPEC R9.3 Review L4
Reviewer: Astra (Codex), commissioned by the Claude Opus 5.5 PM; paper, engine tier; round 4
VERDICT: ACCEPT WITH NAMED DEBTS

B-R9-6 is closed on paper and at the unchanged reader/capture boundary. No remaining blocking counterexample was found in R9.3's held-lift recovery contract. This accepts the amendment only, not the round-13 build, trial, seal or release.

## Scope and evidence

- Target: uncommitted earned-astra-96/rebuild/coach/NATIVE-LOAD-SPEC.md; SHA256 8a9dea77acdaac02da876e1859bfe9bea19f6c4c7d59f484f0b1acaad1acd4a1, unchanged on recheck. All spec citations below are R9.3.
- Read own L3 and L7 reviews and origin/rebuild/t2-client-core DECISIONS:784,785,793,794 through the explicit path. No other reviewer's report or build report was read. This review makes no project ruling.
- Requested git -C earned-astra-96 diff could not resolve its Git metadata. Read the explicit-path diff from b849508 using this worktree's Git metadata with --work-tree pointing to earned-astra-96. No index/checkout mutation.
- E = rebuild/engine; W = rebuild/m4/workout; L = rebuild/m3/w6/local. Reader/capture bytes came from earned-astra-96; historical compensation/adoption and local-admission excerpts came from explicit paths at 0e4fc74.
- Two successful contained probe runs: unchanged public reader/capture red/green and FC16 controls; then an additional fabricated candidate and six literal recovery traces. A first Node attempt stopped at a module-scope PROFILE redeclaration before the cases; wrapping each module separately fixed the harness. An earlier tool-script construction error launched no Node.
- These are synthetic inputs and paper predicates, not executed FC01/FC03/FC08 recovery, repository, panel or import transitions. A fresh projection from the same invented applied state models cold replay; it is not a durable restart measurement.

## Findings with evidence

### 1. B-R9-6 CLOSED: held native entries no longer obstruct capture

Invariant (:159/:173/:282/:316c): holding Q must preserve its durable history while withholding its numeric advice and structural priority. An unlanded captured descendant must still block Undo without blocking later training.
Input sequence retained from L3: accept Q105 at base100; C2 captures105 but performs100, so Q remains pending; correct C1 evidence; lift becomes BASIS_REPAIR_REQUIRED and C2 bars Undo. The same synthetic day also contains fx-row with its own Q85.
RED, R9.2 projection: null w/wSets but keep Q active. Unchanged E/today.cjs:55-58,98,103 outputs fx-press {w:105,baselineAsk:true,isDebutNow:true}; unchanged W/engine-capture.cjs:64 refuses ENGINE_CAPTURE_BASELINE_UNPROVEN.
GREEN, literal R9.3 :159 projection: null w/wSets and exclude that lift's native entries from the input queue only. The same readers output {w:null,baselineAsk:true,isDebutNow:false}; capture succeeds with three not_prescribed load cells. fx-row wins the structural slot, captures85, and the applied state still contains both queue entries unchanged.
Additional controls: both lifts held still capture all six baseline slots; eight combinations of held debut/unlock, coApproved false/true and reversed queue order preserve the other lift's structural slot. Reconstructing the projection from the untouched synthetic applied state reproduces the identical capture.
This is an input-view correction, not permission to relax ENGINE_CAPTURE_BASELINE_UNPROVEN, delete queue history, mark pending work landed, or bypass COMPENSATION_DESCENDANTS. N27(c) now names the original counterexample and requires actual cold replay.

### 2. Recovery now distinguishes applied authority from the held view

Invariant (:160-163): Undo needs the absence of prohibited descendants and applied prior images; adoption needs a later supported performance and a new explicit yes.
N27(d) paper shape check: disputed adopt-baseline has applied60/prior-null. Applied-base Undo records base60/target-null, hence RESTORE. The old held-view base-null/target-null would be RETIRE and leave60 applied. :160 selects the former explicitly. E/native-load.cjs:330-360 at0e4fc74 supplies the existing descendant, prior-image and tombstone semantics; no new guard exception is specified.
N27(a-c) no longer promise Undo after training. For an eligible later baseline capture, :161 offers only adopt-baseline with base null; its accepted target is the observed full supported vector. Before yes, the hold and null advice remain. After yes, held Q is SUPERSEDED with its spend retained. Undo of that exit restores baseline null, not the previous disputed load.
Six literal paper traces retained the hold without yes; released it only on the later adoption's yes; refused a pre-hold numeric card with PLAN_CHANGED; refused a causally incomparable baseline with the hold code; refused an unequal vector with VECTOR_ADOPTION_UNDEFINED; and kept the hold when a newer holding record was not before the Start.
These traces check the stated contract, not its implementation. At delivery, holding-record ancestry must include the correction/conflict that actually caused the hold; an old accept's sequence alone cannot prove a Start occurred after a later correction. D-R9-EDIT/RECOVERY carries that repeated-hold proof.
D-R9-RECOVERY-SCOPE is now paid as a specification qualification (:163): later training outside :144-145's supported adoption domain may stay held, with the named refusal and no invented scalar. General vector adoption remains a separate debt.

### 3. S1-S8/DERIVABLE answer L7, but are not issuance proof

The twelve L7 variants plus fabricated adoption retain these first-refusal clauses under present or absent revision. This is a paper mapping, not 26 new executed fold cases:

| L7 fabricated input | Required field/clause |
| --- | --- |
| 1 body125; 2 bad programme; 3 bad queue; 5 body125 plus rename | target_load, DERIVABLE: authentic F0 rung105 |
| 4 consumed Start missing from coverage | basis.coverage, S5 |
| 6 empty evidence | evidence, S4 |
| 7 other lift with original consumes | lift_lineage_id, S1 |
| 8 consumed Start absent; 9 Start plus evidence/facts absent | consumes, S3 |
| 10 arbitrary spend | spend_id, S2 |
| 11 forged source | basis.source, S7 |
| 12 absent Start in order | basis.order, S6 |
| Observed105 replaced by adoption110 | target_load, S8 |

Genuine records retain recorded equipment, decoded prior images and historical edit prefixes after rename/window/cache changes (:155-156, DECISIONS:793); no current-input reprice is specified. Window binding is for NEW earns. L3's wrapper/baseline/vector correspondence closure remains applicable; R9.3 did not change it.
Fresh constructed residual: authentic hot opener with terminal at_least3 and one prior sighting, ladder[100,105,110]. Unchanged earnWalk returns only PROPOSED105. Change just candidate.state to DEBUT while retaining newW105, full authentic evidence and its redigested non-reproducible record. S1-S8 do not bind state, and DERIVABLE accepts r1 without proving that DEBUT was issued. It still lacks issuance/eligibility proof even without a larger increment or two-rung target.
Retained residual controls: unchanged nextLoad returns105 for recorded inc5 and125 for forged inc25; below-window reps[10,9,7] still have a derivable rung105; the hot case's fabricated PROPOSED110 is arithmetically derivable but was not issued. No claim that these are valid local-host offers.
All are within :157's named equipment/eligibility/context/issuance residual. N28's local construction is an admitted-generation adversarial fixture, not a demonstrated user ingress. Do not report S1-S8 as complete forgery protection.
Minor retained fixture debt: :314 case7 changes the body lift to fx-row but its generic expected refusal still says fx-press. Pin the affected-lift attribution in the executable row; do not let a malformed record suppress an unrelated lift.

### 4. Threat boundary and external admission

The boundary is fair for this paper: direct encrypted-store tampering is excluded; app production, ordinary edits and crafted external records remain in scope (:18). :149/:169-172 bind local acceptance to the owned issuance and exact guarded generation. :155/:166 cover subsequent edits without rewriting consent.
The residual is unreachable through the specified admitted paths only while :157's gate remains closed. Static L/today-bindings.mjs:644-656 at0e4fc74 refuses a nonempty imported-source collection before the fold; L7's published execution measured that host refusal with zero writes. No fresh deployed-ingress or all-path admission audit was performed here.
Hosted sync is absent according to the commissioned scope and is explicitly refused by the contract when added. FC09/FC10 and positive import rows are future obligations behind this gate, not evidence that imported native records are currently safe.
D-R9-ADMISSION remains essential: cover every real ingress, backups/rollback included where they can carry foreign records. Verify local guarded-host provenance. Lift the gate only after verified issuance or complete historical proof of equipment, eligibility, order and context, plus the separate ruling. A digest, device id or equipment anchor is insufficient.

### 5. FC16 compatibility and legacy refusal

Measured with only :233's specified source_json change made in memory: {value:10,unit:'rep',window_hi:10} passes unchanged capture.read and engine-capture.readLayout. Adding window_hi as an OUTER reps-cell member still refuses WORKOUT_CAPTURE_INVALID. Baseline cells remain not_prescribed; five invalid-hi controls omit the inner key.
Full reconstruction is stricter than format reading: the old producer refuses the new capture with ENGINE_CAPTURE_ORIGINAL_DISAGREEMENT; the amended producer reproduces it. Thus "additive" does not authorize old-producer reconstruction. :392 explicitly retains producer binding, active pre-FC16 resume and pins as D-R9-CAPTURE.
:127 blanket-refuses a NEW earn whose checked completion predates FC16, even when current hi equals the largest target. It cannot offer in the ambiguous raised/lowered/floor cases. Adoption/compensation remain exempt, and accepted historical records retain their original consent. Mixed/missing/invalid fields on the actual host remain delivery tests; do not infer historical hi from target reps.
FC16/FC01/FC03 must ship in the same seal (:233); the older full-capture equality and exact-key checks stay intact.

### 6. Earlier sections and precedence

No remaining contradiction requires another amendment in the reviewed recovery path. :159/:173 explicitly replace quarantining and whole-day refusal; :160 specializes Undo to the applied state without changing :154's descendant bar; :162 limits TARGET_QUEUED to earns for held lifts, allowing the later baseline exit despite :127's general queue refusal.
The retire-only conflict at :165 concerns a never-applied effect whose prior image is unproved. It does not replace :160's RESTORE of a genuinely applied adoption. Correction retains historical consent (:166), while later accepted adoption supersedes its disputed advice (:161).
:391 makes B0/G/H0 historical branch text and records the owner answers. Positive import/vector obligations remain behind their explicit gates. No automatic consent, scalar approximation, pending-target landing or lower capture guard was introduced.

## BLOCKING

- None for this paper amendment. B-R9-6 is closed by the specified projection and its measured reader/capture counterexample.

## NAMED DEBTS

- D-R9-ADMISSION: execute N28 at every real foreign path and prove local provenance; full issuance/historical proof before import or sync opens.
- D-R9-CAPTURE: actual-host FC16 production/reconstruction, active pre-FC16 resume, mixed/invalid/missing window values, successor pins and same-seal proof.
- D-R9-EDIT/RECOVERY: actual N27(a-e), correction/removal, unlanded descendants, all-held/mixed days, applied-base Undo, post-hold ancestry, later adoption, undo of that exit and repeated holds through fold/host/panel/durable restart. Preserve consent, facts, spends and tombstones.
- D-R9-RECOVERY-SCOPE: wording is closed; implement its named unsupported-vector refusals. General vector adoption remains unruled/unbuilt.
- D-R9-DELIVERY: implement R9.3, N25 under R1/R2 with correct lift attribution, N26-N28, durable next card/landing, scoped regressions, browser/phone proof, independent build review, successor seal and exact-head Windows/Linux CI. This paper supplies none of those acceptances.

## Containment and reproducibility limits

Only the inspected public today/progression/earn factories and capture/engine-capture modules executed. The VM exposed no filesystem, process or child capability; require and unexpected engine callbacks refuse. Positive synthetic day/membership/last/meta/target/effort/noise callbacks supplied context; picker, genSession, nextLoad, atTopOfWindow, earnWalk and capture guards were actual source. The JSON-only fixtures used a same-realm JSON clone adapter and JSON.parse for generated duplicate-free cells; no claim about general alias/strict-parser behavior follows.
Source SHA256 prefixes: today685f6e1e907c; progression7031838d37cf; earn4b8838807ee9; capturec20fb021034b; engine-capture309c75d82a03. Only the named FC16 expression was replaced in memory.
No forbidden module/source, private/auth/legacy file, prohibited scratch, generator, receipt, whole suite or protected soak was read or executed. No install, node_modules mutation, tracked edit, ledger/status write, commit, push, fetch or deletion. One supplied node.exe at a time; MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York were set on separate lines before each run. Exactly this new file was written, early and extended; all probes stayed in memory.

## Final scoped commands and outputs

```text
git status --porcelain -- rebuild/coach/NATIVE-LOAD-SPEC.md rebuild/engine/today.cjs rebuild/engine/progression.cjs rebuild/engine/earn.cjs rebuild/m4/workout/capture.cjs rebuild/m4/workout/engine-capture.cjs rebuild/lanes/astra/reviews/NATIVE-LOAD-SPEC-R9-REVIEW-L4.md
warning: unable to access 'C:\Users\joeym/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\joeym/.config/git/ignore': Permission denied
?? rebuild/lanes/astra/reviews/NATIVE-LOAD-SPEC-R9-REVIEW-L4.md
```

```text
git diff --stat -- rebuild/coach/NATIVE-LOAD-SPEC.md rebuild/engine/today.cjs rebuild/engine/progression.cjs rebuild/engine/earn.cjs rebuild/m4/workout/capture.cjs rebuild/m4/workout/engine-capture.cjs rebuild/lanes/astra/reviews/NATIVE-LOAD-SPEC-R9-REVIEW-L4.md
```
The diff command returned no output.
