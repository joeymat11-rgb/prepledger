# S10 integration correctness review, round 5
Reviewer: Astra (Codex), commissioned by the Claude Opus 5.5 PM; blind; package tier; round 5
Head checked: fc6561fd655d6ef29b9bc6676dec200f56d3f43e.
Round-9 delta: 62788b1244510d7ecfa1c6dfc40afcc1f1d86c28..HEAD.
Candidate S9 comparison base: 6dc2596d56e0e8602ce6416f4de996d764992364.
VERDICT: REJECT

B5 remains open: the repaired names refuse, but an allowed extension still
admits previously unreviewed credential containers. D-S10I-14 is implemented
narrowly and correctly. B1-B4 and B6 remain closed within L4's measured limits.
S9 is unsealed; section 2.1 remains STOP. This is no engine or seal acceptance.
Authority: c58b892 brief and its three reviews; chain DECISIONS:627, :780,
:785, :791, :792, :801; :799 supplies the conditional b-lom finalization debt.
Blind source review and initial probes preceded L1-L4. The author report was
read LAST, after the measurements and findings were saved in this file.

## Questions, inputs, invariants and measured outputs
1. Merge fidelity and exactly three clauses: integration inputs are unchanged.
   L2's scoped automatic reconstruction at immutable 92b7487, 9943679 and
   aae3b27 remains applicable: 55/55, 36/36 and 68/68, zero conflicts or
   mismatches. Not reconstructed again in this round. Fresh complete-file
   comparisons prove today.cjs, writers.cjs and engine-capture.cjs each equal
   6dc2596 plus its specified one-line PROPOSED exclusion, at a unique anchor.
   No fourth clause or NATIVE-LOAD delta appears. This is byte evidence only.

2. Runner/standing-step precedent: runner, workflow, registration cells and
   all eight ancestor specs are byte-unchanged from L4. The measured S9
   precedent, IDS/no-register/CHILD_ROOTS registration, paired S10 step name
   and command, and ancestor runner-pin justification therefore still apply.
   No runner predicate or execution root changes in round 9.

3. Supersession reason/current red: the five cells and differential are
   unchanged. They distinguish the ancestors' reconstruction departure from
   S10's two further one-clause module moves. Six fresh contained startups
   each make exactly two permitted reads and stop with actual ENOENT at
   acceptance-s9-ui-pins.json. No reconstruction helper, protected module or
   child process is reached. This is the correct missing-parent STOP, not
   execution of the eventual carrier witnesses or engine differential.

4. Re-pins: S10.json changes ONLY two product posts from L4; both equal disk:
   S10-REGEN.cjs cfead8b045194b45bf2d9bb40b60927b1f5b96b430e0d87bc36b89b4dbb8d0d6;
   S10-REGEN.test.cjs 159bd49fc5b1755f5a1909de0c1b37779ce97f8e82cdc35290c70072e5fb95c0.
   The helper adds admission checks; its cell adds refusal/control rows.
   No prior assertion or test row is removed. Old provider/manifest/carrier
   re-pins are unchanged, so their earlier justifications still apply.

5. Every inventory role: 296/296 role/pre entries freshly reconcile against
   candidate S9's product ownership and runner-derived execution targets.
   Counts: 231 carried, 20 edited, 42 new, two released, one superseded-by-child.
   Zero missing parent product pins; zero released-view child-argv collisions.
   All 296 role/pre values equal L4; only the two posts above change. An
   explicit 296-path Git comparison finds only those two changed files.
   Thus L4's other post checks remain applicable; their content hashes were
   not repeated. Protected engine content was not read. REGEN/cell stay
   new/pre:null; S9.json stays superseded-by-child; released views keep
   parent pre/post:null; parent-released build.mjs/preview.css stay outside.
   These remain candidate roles, not declarations bound to a sealed S9.

6. P-S9-3/CI: actual condition callbacks 32-35 pass 4/4 against rebuild.yml.
   Ten mutated REGEN-step workflows refuse: missing/false/success/nested if,
   duplicate if/owner, wrong path, and three continue-on-error spellings.
   The cell has exactly one direct node --test owner with !cancelled().
   Workflow and fence bytes are unchanged from L4. B6 stays closed as code.
   Existing Today/M4 default conditions imply no hosted execution or pass.

7. Earlier items and STOP boundaries, reconciled:
   B1 CLOSED: fence/residue/dead-key bytes unchanged; L4's 11/12 old versus
   12/12 repaired callback evidence and its scanner limitation still apply.
   B2 CLOSED AS CODE: EPP-R9 and four guarded assertions unchanged; L4's
   synthetic-adapter pass/four deletion reds stand. Real engine proof is owed.
   B3 CLOSED: package/build bytes unchanged; L4's exact 51/29 inventory,
   all 29 omission refusals and three repaired callbacks still apply.
   B4 CLOSED NARROWLY: release-accounting bytes unchanged; prior control,
   seven refusals and re-release control stand. Actual PM grant is owed.
   B5 OPEN: residual below. Old alias/auth-word exclusions are retained.
   B6 CLOSED: exact execution home and condition remeasured in question 6.
   D-REGEN-INPUT/D-S10I-11: committed accepted-parent/release/receipt/note
   controls pass within the 120-row suite; no real write is inferred.
   D-REASON-TEXT remains corrected; D-REGEN-NOTES remains unpaid.
   :785/:791 authorize D-EPP-2 despite the older brief's third-clause STOP.
   No new STOP-field fulfillment, token, release or artifact is invented.

8. Owed/CI-only: packageId, sourceBase, final brief, parent hashes, receipt
   coordinate and token/release claims remain STOPs; 36/36 needles are null.
   Final parent binding, brief proof debts and actual engine/platform routes
   remain below. No rebuild run ID or green hosted conclusion is claimed.

## Round-9 admission and D-S10I-14 measurements
- Committed cell: 120/120 pass, zero skipped. Same callbacks against the
  unmodified 62788b1 helper: 32 pass, 88 fail. No filesystem/Git fixture data
  is real; the helper's ports record invented markers only.
- Removing admission, dot-name, credential-name, key-extension or final-
  extension checks causes respectively 88, 11, 21, 22 and 7 failures.
  Removing the exact-path dot-name exemption causes one failure.
- L4's .netrc, _netrc and .ssh/id_ed25519 each refuse across all nine input
  sources, BY NAME and without a content read. Those exact counterexamples
  are repaired; they are not the reason for this round's REJECT.
- Both exact workflow paths admit and are measured. A third top-level
  workflow and nested .github path refuse before content. The exception is
  exact Set membership, never a directory/prefix match, as :801 requires.
- Synthetic exact-file SCOPE extensions isolate the exemption: known_hosts.json,
  signing.pem.json and plain.txt under a dot-directory still refuse with no
  content read; plain.json admits. Name, key and extension rules still run.
  This altered SCOPE exists only in memory as a probe, not as product code.

## BLOCKING items
- B5 residual, S10-REGEN.cjs:93-112,125-139,186-196,238-239: admission by
  broad root plus allowed suffix does not establish a public file. Ordinary
  credential containers can satisfy every clause. Fresh synthetic inputs
  rebuild/m4/spec/service-account.json and rebuild/m4/spec/kubeconfig.yml
  pass through changed paths, S10.product, parent candidate product,
  candidate execution pins, sealed parent product, artifact path and review
  path: 14/14 reach CONTENT READ and exit 0, instead of refusing before read.
  Sealed execution pins and split sources also admit them (four further
  inputs, metadata only). An id_rsa.backup.json input repeats the same nine
  outcomes; checking only the final-extension stem misses the key name.
  No real auth file was read: every planted payload is an invented marker.
  The counterexample is a credential file admitted through these ports, not
  an assertion that this repository currently contains such a credential.
  Required invariant: every content-read target has positive reviewed-public
  admission; unknown targets refuse before bytes. An extension list cannot
  supply that evidence. Prefer an explicit reviewed file inventory with
  intentional additions; preserve all current refusals and :801's exact
  exemption. Add these cross-source refusal controls red-first and re-pin.
  Do not run repository-wide REGEN on the strength of 120/120 alone.

## NAMED DEBTS
- D-PARENT/LOCK: accepted S9 seal/section 2.1 identities; final input and
  reconciliation tables; eight copy-lock answers, composed-product and
  coordinated-deletion proof, source closure, PM tokens, receipt and needles.
- D-SPLIT: actual-parent D-SPLIT-PARENT, re-cut/independent reconstruction,
  DOM/listener evidence; D3/D5 real eslint-scope/six scope rows, D6/D7/D9's
  21 survivors, and S-R33. Candidate identity cannot pay actual-parent proof.
- D-GSS: merged machine-settings-ui red/green on both systems, physical
  M/R/N accounting and destructure-aware api.lane census. TIMER/LISTEN and
  accepted transient-input/copy limitations retain their existing payers.
- D-EPP/CR: actual R9 green/four-deletion reds, synthetic oracle and D-EPP-3
  gates, separately authorized private gate, capture/configuration with
  PERFORMED_W6_DIR; coach ENGINE_REVISION follows the actual receipt.
- D-ACCEPTANCE: separate Claude Fable final, PM exact-byte read, complete
  Today/measure/bundle/provider/supersession/conformance/package routes and
  exact-head Windows/Linux rebuild run IDs/conclusions. Real runner refusal
  on STOP fields is owed; no TypeError or inferred green substitutes for it.
- D-REGEN-NOTES: final S10.json note still calls D-EPP-4 unpaid and the residue
  row red despite their code fixes. Reconcile at final input binding.
- D-BLOM (:799): when rebinding to sealed S9, drop the b-lom child, retain
  both cell files as carried and carry the named debt/future hosted-packet
  re-point. This does not excuse a red public CI step (:800).

## Method and what I did not verify
Windows Node v24.19.0, commissioned absolute binary only, one process at a
time; MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York set on separate
lines before tests. Positive callbacks/source slices and closed module,
filesystem and child-process ports; no negative-name containment filter.
No protected engine loaded or executed, private/legacy source, full Today
step, package --ci/--full, generator, real REGEN dry run/--write, artifact or
receipt writer, install, dependency change, deploy or import. No commit,
push, checkout, reset, stash, clean or fetch; no tracked file edited.
Scratch retained: %TEMP%/earned-astra-s10-l5-119 (helper-before.cjs, probe.cjs,
package-probe.cjs, identity-probe.cjs). No cleanup attempted. No real auth
file opened. Race resistance/hard-link identity and Linux execution unproved.
Earlier closures are reused only after unchanged-byte checks, not claimed
as new behavioral runs. Current role/pre, changed-pin, condition, startup,
admission and clause-byte measurements are this review's own.
Author report read LAST: 120/120, exact-scope exemption, two pins and role
counts agree. Its finite name matrix does not cover B5's remaining inputs.
Its larger-suite/dry-run claims are not adopted as independent evidence.

Final commands (explicit review/package paths), stdout verified after write:
`git status --porcelain -- rebuild/lanes/astra/reviews/S10-INTEGRATION-REVIEW-L5.md rebuild/lanes/b .github/workflows/rebuild.yml rebuild/lanes/c/ui-port/sealed-inventory-fence.test.mjs rebuild/lanes/c/today-split/writer-fence.test.mjs rebuild/engine/today.cjs rebuild/engine/writers.cjs rebuild/engine/test/proposed-pick.test.cjs rebuild/m4/workout/engine-capture.cjs rebuild/m4/workout/test rebuild/m3/w7-preview/today/test/package.test.cjs rebuild/m3/w7-preview/today/build.mjs rebuild/m3/w7-preview/measure/test/boundary.test.mjs`
```text
?? rebuild/lanes/astra/reviews/S10-INTEGRATION-REVIEW-L5.md
```
`git diff --stat -- <same explicit paths>`
```text
```
