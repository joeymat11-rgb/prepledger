# S3 core R2 — independent review: REJECT

2026-09-13 · ER, Astra MAX. Candidate `0df6ad3f3ec8d69a6e10ba68c279b7b77d061596`; source `946c36059a7ce6b949933da3904db3db8e6b8bd3`. Portable core only, not final S3 or product acceptance.
Bar: PM `ce9779dc765b5520ff68a45e500ef8953e0b7dd5` dispatch, GO230/06631c16, 7502798 disposition, 43006d3/84ca238 exact annex bindings/bootstrap, and the previously read ten immutable construction/provider/local/registry/tool documents. Original `ebc4c4e` and independent rejection `26ba95c` remain preserved. D's R2 report remains UNREAD at this first verdict.
Own fresh `work/pm-caretaker/review-s3-core-r2`, branch `rebuild/astra-review-s3-core-r2`. Three bounded assistants inspected source and authored extra public probes; the primary reviewer inspected and executed every program and owns this verdict. No candidate source changes or push.

## Remaining failures

**R2-1 · P1 · Reached-calendar coverage still misses native timestamp parsing.** `engine-provider.cjs:19–24` wraps the date-helper facade, but real `mergeState` reaches `merge.cjs:515` and directly parses `corr.at`. An invented March7 session corrected at offset-free March8 01:30 chooses the corrected or later plain record under full-year coverage, proving both temporal branches. The identical merge under September-only coverage still returns the later record; a subsequent guard returns safe. The intended context-refusal assertion fails. Cover actual reached native date operations and preserve the existing algorithm, values and sticky refusal; do not normalize input timestamps or invent a new calendar. Original historical-regression refusal now passes, but it does not cover this merge path.
**R2-2 · P1 · A passing selected test can borrow a different test's assertion.** `test/s3/mutations.cjs:10` uses `includes(name)`. In an actual two-test TAP child, `selected boundary` passes while `unrelated selected boundary suffix` fails ERR_ASSERTION; `mutantSummary(...,'selected boundary')` credits the latter. The exact selected-assertion positive passes, and the original TypeError-plus-distinct-unrelated negative now refuses correctly. Bind the selector to the actual intended complete test identity; substring coincidence cannot supply a kill. This does not invalidate the 56 individually audited real kills below.
**R2-3 · P2 · Lexical context both hides an import and rejects valid regex syntax.** `test/s3/run.mjs:51,79–84` treats the property token `of` as a regex-start keyword. Valid `obj.of / import('.\x2fsentinel.mjs') / 2` returns no edges, while the `obj.x` pair correctly refuses the unlisted import. Conversely, valid `if (true) /import(x)/.test('ok')` falsely refuses a computed import; the assignment-regex control passes. All four files pass actual Node syntax checks. Repair the static scanner's expression context. The real unchanged ESM load guard still refuses the same harmless sentinel, so this is a pre-copy closure defect and false refusal, not an observed runtime escape.
**R2-4 · P1 · The new membership check drops a real producer dependency.** `local/source-admission.mjs:151` calls `genSession(state,originalDay)` without sleep; `engine/today.cjs:56` dereferences `slp.last` for an active queued hack debut/unlock. Two newly constructed public fixtures have matching original setup/source and complete real captures made with explicit sleep context. The ordinary fixture qualifies; the pending-debut fixture becomes `LOCAL_SOURCE_WORKOUT_UNRESOLVED` and fails the intended qualification assertion. Source/candidate bytes and checkpoint/generation values remain unchanged. Keep complete original membership proof without losing valid captured workouts to an omitted dependency. Any required original-context authority must be named truthfully; this review does not authorize fabricated sleep or a readiness-policy change.

## What the repair now proves

All four original annexes, with only authorized identity/root bindings and separate top-level argv, pass: admission **8/8**, calendar **4/4**, caught51 **1/1**, harness **9/9**. Together these are 22 boundary checks, combining node:test cases and named harness probes. Their exact original bytes, adapted hashes, occurrence-counted substitutions and reverse equality are recorded. Bootstrap checks are not claimed as loader enforcement.
Caught51 now discriminates: the complete real provider suite is **8/8** original, **7 pass/1 named CAUGHT51 ERR_ASSERTION** under the registered empty-draft substitution, then **8/8** restored. Missing dependency identity is preserved; the former patch60 mask is closed. No setup error earns mutation credit.
Namespace, unexplained-plan, illegal fact-target and whole omitted-lift reproductions now refuse correctly. New tests preserve different captured loads, skips and incomplete sessions. Both CJS and ESM protected children refuse the original harmless unlisted sentinel. Browser schema identity is distinct from its directory.
Fresh real Edge execution compares complete production-provider Node/browser preparation/replay states, bytes, guards, aliases, refusals and actual calculation. Only the documented runtime descriptor differs and is separately asserted. A changed nested output with unchanged counts fails comparison. This closes the original portable-parity evidence gap within the synthetic core scope.

## Independent execution and custody

Windows Node **22.23.2**, npm **10.9.8**, pnpm **9.15.9**, America/New_York. Two fresh dispatcher copies independently install locked root/W6/W5 dependencies; tool-only paths are explicitly licensed. All cwd, TEMP/TMP/cache/store/log/browser-profile paths are owned and contained; Node injection/coverage variables are cleared.
Run A `3220e4da-ec25-4962-b335-653fcdad5cd1`: actual **57/57 core**, **41/41 preserved import**, **56/56 selected assertion mutations** (10 prepare +20 reading +26 core), individual restored controls, and **27 real browser checks** including full parity and owned force-kill/reopen. Run B `633df2c8-ef07-4262-985f-45dc6c39f647`: original admission/calendar/caught51 annexes and additional probes. Original harness annex uses A's actual produced browser evidence.
Mutant child logs are independently parsed by full recorded diagnostic identity, with their own top-level ERR_ASSERTION and baseline/restored accounting. The original 46 cases remain; ten are added. All successful suites have nonzero discovered cells and zero fail/skip/cancel/todo. Extra independent probes total **11 checks: 6 controls pass, 5 intended assertions fail** (calendar2, admission2, harness7); do not count them as another successful suite.
Raw manifest SHA256 `5e5266c253a36304543757a360b74bd0567134dd64da03aae09bf4fe38e6fbee`. All **111** entries independently match exact source Git blobs, candidate Git blobs, materialized disk and both final copies. Source names/order are unchanged; 97 earlier reviewed sources retain bytes, 14 change plus the manifest =15 existing paths. Original11 mutation targets remain, plus3 already-owned harness targets. No extra product/source path is admitted.
Browser: Edge **153.0.4234.32**, product62/test71 build inputs independently bound to source bytes, fresh bundles, real WebCrypto/IndexedDB, owned process exit and same-profile reopen. Evidence remains `PORTABLE ONLY`, with final integration pending. The machine evidence records exact argv, run/log/source hashes, inventories, browser results, original/adapted annex identities and the reviewer's two pre-execution setup corrections.

## Handoff and limits

Executable reviewer artifacts and `S3-CORE-R2-REVIEW-EVIDENCE.json` preserve the independent positives and failures. Raw logs stay in owned scratch. Artifact copies retain the executed bytes; invocation paths and the required scratch materialization are recorded separately. No product fix is included.
No private/seed/history/soak/native/frozen/oracle/port/real-C2/P1/owner-data execution; no protected B capture/Start/resume or C consumer build, whole/full/both-OS CI, receipt, acceptance artifact, integration, deployment or phone proof. No inferred production reference provenance follows from invented fixtures.
D owns corrections within PM's exact custody; PM judges any additional context/path requirement and commissions an exact complete successor. Shared review publication belongs only to the PM's serialized writer. Candidate ancestry stays local/unpushed. Preserve this first verdict before reading and separately reconciling D's report.
