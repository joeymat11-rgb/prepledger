# S9 immutable draft closure staging
Recommendation: YES, a clearly unfinished committed PROPOSED S9 package is valid staging input.
This stages source custody only; it does not assert runner admissibility or package acceptance.
Reviewed public runner/brief at integration80082fa and prospective map/note at92b6f32.
Map SHA256 supplied custody:c2170f4f6788cd1fbc5386cdc814636d5e4343c85efebb466057b6a6ea4b98b0.
Current map contains33 planning groups/83 argv targets; neither is promoted to a final S9 total.

Concrete route
1. Author finishes the off-repo draft of rebuild/lanes/b/tooling/packages/S9.json.
   Use status PROPOSED, which is in the runner's legal vocabulary; literal DRAFT is not.
   Keep brief.acceptedLedgerLine null and unresolved token/needle/pin values honestly unmeasured.
   Put draft limitations in notes and companion prose; do not invent BRIEF-ACCEPTED or citations.
   Children use exact name/argv/needle keys; keep planning metadata outside those closed records.
   Preserve all mapped inherited/substituted groups, grouped targets and the reference reader.
   Python font evidence remains outside this JavaScript child list.
2. Review actual draft argv against current integration bytes, then PM commits that draft on
   rebuild/b-s9-integration. A local immutable commit suffices; no workflow change or CI bypass needed.
   Do not initiate runner/exporter/child execution merely to construct this closure input.
   Publishing that commit can trigger existing CI, which is separate from the read-only staging action.
3. Update only CANDIDATE in the reviewed source checker to that full draft commit SHA.
   Preserve SourceBase0cd07be7cf967dfbfea8c84947ba8477f58cfb5f, five-file policy and algorithm.
   Hash/review that constant-only change and exact spec bytes; no new parser or closure approximation.
4. Use the existing bounded read-only permission for the same five protected sources.
   Checker Stage2 reads sourceBase and children.argv directly; it never invokes runner spec().
   Therefore null needles and unresolved acceptance fields do not block literal-source closure.
   A forbidden path, link/type issue or512-file cap must STOP; do not truncate or substitute a count.
5. Retain exact draft head/spec/checker hashes, roots, closure outcome and limits as draft evidence.
   A successful literal walk is a static lower bound, not execution containment or a runtime trace.
   Changing argv or reachable sources later requires corresponding final-head remeasurement.

Finalization remains separate
Runner spec() requires real child needles and validates claim shapes; the unfinished draft may refuse.
Brief sections3.3/6 require final needles measured on the composed head, actual four token claims,
committed final runner/spec, real CI and proposed()/PENDING export, then final review/full/receipt order.
Only actual acceptance citations justify BRIEF-ACCEPTED; do not change it to make staging checks green.
Existing permission766 is not private census, repository-module execution or exporter permission.
No repeated parent-pin audit, protected source read, checker/runtime execution or repo edit in this review.
Author received the PROPOSED/null-value/exact-child-record guidance directly; no new approval flow added.
