# S9 prospective exporter mechanics recheck
Verdict: ACCEPT corrected mechanics only; execution and final-input containment remain HELD.
Exporter SHA256: 7541ad0eddf124dc61f08ffe2aeae97c583f61d351bf618b40c15b0344deddc4.
Procedure SHA256: 9911a2b2b56dec7f382b3d8f9f4574cc07d2fb1edcfb7636bb7fc6df75f97f06.
Both frozen files read in full; no helper, runner, imported module or test was executed.

Prior corrections resolved
Output parent is a fixed scratch literal, physically resolved and compared before use.
It must be an ordinary directory outside the real checkout; arbitrary output paths are gone.
Only a validated s9-prefixed child is allowed; mkdir is exclusive and physical child type/path rechecked.
Both JSON files use exclusive writes; existing paths refuse without overwrite.
Procedure explicitly retains incomplete output and forbids installation without terminal success,
both exact files, hashes, parsing and independent coordinate/byte comparison.
Post-compile checks compare all product post-images, execution pins and parent artifact/review pins
against exact-head Git and disk; shared pins must agree rather than silently overwrite.
HEAD, the actual chain ref, and clean worktree status are rechecked before scratch writes.
Git replacement objects are disabled both for direct reads and inherited compiled calls, then restored.

Construction and remaining limits
Exact unchanged runner prefix and original module filename/paths still supply spec, parent and proposed.
Unique main marker excludes the campaign; actual proposed supplies the artifact rather than a copy.
PENDING version1/receipt:null and terminal-LF JSON remain compatible with the public envelope reader.
Runner/spec disk and Git identities are frozen; this is a prospective reviewed-input export procedure.
Procedure now explicitly limits direct output claims; imported/subprocess output is not generally suppressed.
Final spec/import/call-effect containment must separately establish all reads, outputs and side effects.
The added pin loop itself reads declared product bytes and needs that same final authority review.
S9.json is absent now; no final closure, source permission, runtime grant or private census is approved.
No protected source was read/hashed, and no author/worktree/ledger file was changed in this review.
PENDING remains unaccepted; final Claude, authorized full proof, CI, integration and seal are separate.
