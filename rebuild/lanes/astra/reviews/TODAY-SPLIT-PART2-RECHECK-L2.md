# TODAY-SPLIT PART 2: narrow re-check, loop round 2
Reviewer: Astra (Codex), commissioned under DECISIONS:412, :569 and :613; narrow re-check, loop round 2; highest effort
Head checked: c38ed5fb7e31e11a6e5169a4afeda2d7bfdc90af
VERDICT: REJECT

Scope: the ten changed files in e08bc11cec423e5d5878110e78d16ccc5f3f8a29..c38ed5fb7e31e11a6e5169a4afeda2d7bfdc90af,
the earlier blocking inputs, independent regeneration, and the new sealed file's banner.
The seven declared STOPs are separate tickets, not findings or work ordered here.

Earlier findings (2b = blind review; 2a = incremental review)

| Finding | Disposition and executed output |
|---|---|
| 2b F1, nested paint | STILL OPEN as D1; UPHELD boundary measurement. Frozen painter reached from a shimmed click records one synthetic food op for cal=2300/pro=150. Direct call after dispatch throws WRITER-OUTSIDE-GESTURE: recordIntake. The new call-site rows do not change that runtime output. |
| 2b F2, initialization order | CLOSED for the original input: model.setFoodDays assigns options.sleep; both real composed PRE and POST pages now return that injected lane. The author's acknowledged second-read deviation is D4. |
| 2b F3, surviving mutations | Eight closures confirmed: M02 gives 402/404 fence; M05/M09/M10/M14/M15/M16/M17 each give 403/404. Own inputs distinguish every mutant from control (table below). STILL OPEN as D3 for M18/M21. |
| 2b F4, stale counts | CLOSED for the two runnable count failures: both pass at both refs. The additional residue correction requires missing eslint-scope; not credited as an executed closure. |
| 2b F5 / 2a F1, lexical anchor | STILL OPEN, B1. Author: added helper makes count 3, refused. Re-check: same input refused; count-preserving variant exits 0 at both refs and changes LOCAL to SEALED. |
| 2b F6 / 2a F4, silenced refusal | STILL OPEN, B2. Author: re-witnessed plain return refused. Re-check: that exact input refused; string-hidden return and false predicate each exit 0 at both refs and remove the refusal. |
| 2b F7, facade capabilities | STILL OPEN as D2. Ungestured facade.foodLane().save({cal:2100,pro:130}) and facade.sleepLane().save({date:"2030-02-03",hours:7},{supersedes:null}) each return ok=true and add one real encrypted synthetic operation. |
| 2b F8, async continuation | STILL OPEN as D1. Await Promise.resolve() in a listener, then recordIntake: WRITER-OUTSIDE-GESTURE: recordIntake, no new save, error text remains empty. |
| 2b F9, inaccurate report | CLOSED. Own initial sleep repaint throw: SYNTHETIC_PAINT_FAILURE, saves=0, busy=true. Two synthetic food dispatches: saves=2, disabled=true. The corrected report describes these outputs; inherited behavior is D2. |
| 2a F2, object-valued refusal | CLOSED. Executing final TA-I067/068/069 with the real plainOrDrop paints exactly: "This night changed while you were editing. Review the saved record before trying again. Nothing was recorded." |
| 2a F3, uninvoked generator | CLOSED as a source omission: row 32 now invokes it. Full generator equivalence remains unverified under D5: row 32 cannot load eslint-scope. No missing row is counted as PASS. |
| 2a F5, row-order dependence | CLOSED. Reverse only files["today-app.cjs"]: cut=0 at s9 and tip; both emitted files equal their respective unreversed outputs byte for byte. |
| 2a F7, uncovered assignments | CLOSED for the changed refusal/write-order clauses in isolation: (uncovered,subStops)=(1,0),(0,1),(1,1) each exits 1 with zero writes; (0,0) writes rows then table. The complete planted-source row is unavailable under D5. |

B1 - BLOCKING: the count still does not identify the binding

Invariant: an accepted cut cannot rewrite a competing binding or strand the original call.
Exact input, separately at each named source ref, with the committed table unchanged:
prepend this helper and add ONE leading space to the original line resolved as TA-I042:

```js
function astraShadow() {
  const sleepNightDate = () => "LOCAL";
    const date = sleepNightDate();
  return date;
}
```

The anchor text remains present twice: in this helper and inside the witnessed sleep writer.
Output at BOTH refs: cut exit=0; input astraShadow()="LOCAL"; emitted helper, called with
facade.sleepNightDate=()=>"SEALED", returns "SEALED". No witness was re-taken.
On the real composed s9 output, api.render("sleep") throws exactly
"sleepNightDate is not defined". The intended original call remains bare after its binding moved.
Control: adding the helper alone refuses "first anchor matches 3 places ... RECORDED 2";
bumping only the table count to 3 still refuses against the outside witness's 2.
Smallest fix: bind TA-I042 to witnessed enclosing context/binding identity, not its ordinal
among identical lines, and keep this count-preserving input as a negative row.
Disposition of the claimed complete closure: UPHELD. The count fix is useful but insufficient.

B2 - BLOCKING: the new control-flow comparison still accepts a hidden early return

Invariant: re-taking a declaration digest cannot authorize a generated row to erase a refusal.
Exact replacement for TA-I018, leaving statementRewrite absent:

```js
    if (!facade.foodLane()) { "//"; return;
```

Run the REAL gen-witness.cjs --declared --regions <scratch-table> --write, then the REAL cut
with --product --only today-app.cjs. At BOTH refs: witness exit=0; cut exit=0.
The changed checker strips line comments BEFORE strings; the apparent comment inside "//"
hides the added semicolon and return from its profile while the emitted JavaScript parses.
Execute the exact emitted guard with foodLane=null, foodLaneFailure="STORE_UNAVAILABLE",
openFoodLane()=>null and synthetic copy constants: control emits exactly
put(map,"stub-note","No store Reason STORE_UNAVAILABLE. Plan not wired"); mutant emits ZERO puts.
The real composed page also loses both the stub-note and food-save elements.
Single-clause companion input: replace the guard predicate with !facade.foodLane() && false,
re-witness, cut: exit=0 at both refs. Same zero-put result; real page instead displays exactly
"Not prescribed. The engine issues no carbohydrate or fat target."
Control: the original plain added return is now refused with return: pre-image 0, replacement 1.
Smallest fix: compare the allowed identifier/call rewrites against parsed structure, retaining
predicate and statement order; add both inputs. Fixing comment stripping alone leaves the
false-predicate input accepted. This asks for instrument validation, not an outcome-type ticket.
Disposition of the claimed complete closure: UPHELD; the original spelling alone is closed.

B3 - BLOCKING: the corrected banner still contains false statements

Input: the committed today-lanes.cjs, parsed with Acorn 8.17.0, and its declared product/substitution rows.

| Banner claim | Exact measured output | Smallest correction through regions.json |
|---|---|---|
| "THE FOUR DECLARATIONS AFTER IT" exhaust the authored declarations | Seven before TA-S01: sleepDraftHeld, willAdopt, ready, gestures, wrapped, wrapFor, gesture. | Say three state declarations plus the four guard declarations/implementation. |
| Every substitution is "a paint-handle rewrite" | W10 changes `{ ...sleepDraft, date }` to `{ ...sleepDraftHeld, date }`; no painter occurs in that row. | Name W10's bound-draft identifier rewrite separately. |
| "this file carries zero string literals of its own" | 127 string Literal nodes; the authored guard alone adds "WRITER-OUTSIDE-GESTURE: " at line 81, outside all moved regions. | Describe injected athlete-facing copy accurately; do not claim zero string literals. |

Confirmed banner quantities: 34 moved regions, 679 moved lines, 41 declared substitutions,
39 applied rows (41 substring occurrences), W2#30/W1#33 already covered; 37 facade getters,
29 hooks, three requires, 22 parameters including 12 copy constants. The guard is present,
checks two hooks synchronously, and allows an admitted asynchronous save to settle later.
The outer facade is frozen; its returned capabilities are not read-only (D2). No STOP is closed.

NAMED DEBTS (each line can be carried verbatim into S10)

- D1 / TODAY-GESTURE-PAINT-ROOTS: exclude paint nested inside a listener from writer admission, and explicitly rule and paint async-continuation refusals; the current tripwire permits the former and silently rejects the latter.
- D2 / TODAY-OUTCOME-TYPE: remove writer-bearing/live mutable facade handles, recover sleepBusy when the initial repaint throws, and independently fence duplicate food saves; these are inherited behavior, not pure-move fixes.
- D3 / S10 writer cells: add repeated same-date readSleepCheckInView forcing forDate before EACH paint (M18), and inspect the typed hours after successful save (M21); both mutations still survive the runnable fence and problem cells.
- D4 / S10 boot contract: preserve one options.sleep acquisition at its original point, or explicitly rule the accepted option shape; a one-shot getter returns its lane after one PRE read but null after two POST reads.
- D5 / S10 instruments: rerun rows 12/13/14/16/32/33 with the real eslint-scope stack, independently exercise generator scope/binding outputs, and retain the uncovered-assignment no-write input; isolated tail checks are not generator-equivalence proof.
- D6 / S10 negative coverage: exercise overlap refusals, including competing replaces nested in a seam; disabling the overlap loop/refusal still survives the runnable instruments cell and the valid-input reconstruction control.
- D7 / S10 evidence: correct the inventory to 182 witnessed move/replace regions plus 20 seams, and validate or label headLines/closeLines as informational; changing either metadata count currently exits 0 with identical product bytes.

Single-clause table

Every mutant starts from original bytes. I001-I146 are individual changes to predicates,
logical/comparison operators, initializers, returns or effects on the changed executable lines;
no test assertion was changed. The exact from/to/line and each failing row are retained in
instrument-mutations.json. "Cell kills" means an ADDITIONAL red beyond the same six missing-scope
rows, not a full-green run. Own profile, boot-input, witness-generation, output and report checks
then measure the survivors. No consequential survivor is called closed because a dependency failed.

| Clause family / exact single change | Executed result |
|---|---|
| I001-I003, cut digest/keyword-set initialization | 3/3 cell kills. |
| I004-I039, control profile/count/diff/authorization, cut:314-344 | 26/36 cell kills; own string/comment/punctuator and emitted-report checks cover additional changed clauses. B2 supplies the surviving accepted attack on the original checker. |
| CF_WORDS: delete each of its 28 recognized words separately | 28/28 killed by independent profile inputs; strings/comments remain excluded in controls. |
| I040-I046, cut witnessed occurrence selection | 6/7 cell kills; valid-ref fallback survivor is recorded, not called a product defect. |
| I047-I049, unapplied-substitution branch/report | 0/3 cell kills; own reconstruction/report comparison kills all three; two otherwise emit different moved bytes. |
| I050-I077, tie order/containment/overlap/witness invocation | 15/28 cell kills; own report comparison kills further diagnostic changes; overlap-loop/refusal survivors remain D6. |
| I078-I081, changed statement-rewrite comparison/report path | 2/4 cell kills; own emitted-report comparison kills the other two. |
| I082-I125, boot classification/traversal/crossing output | 39/44 cell kills; own cases cover calls, new, tagged templates, await, member access, object/array nesting, function-body exclusion and inert declarations. |
| I126-I130, resolve occurrence predicate | 5/5 cell kills. |
| I131-I141, gen-witness digest/count collection/recording | 2/11 cell kills; own full witness generation from tables initially lacking occurrence fields covers recording; redundant refusal-path survivors are recorded. |
| I142-I146, gen-interface stop predicate/exit/JSON write placement | Scope-dependent cell unavailable; own isolated tail kills 5/5, using each stop class alone, both, and neither. |
| All instrument mutants above, including keyword members | 174 unique mutants: 163 distinguished by cell or own checks; 11 survive the exercised inputs. Exact survivor IDs below. |
| Each first.occurrences += 1, independently | 182/182 refused by the named resolver count. |
| Each witnessed occurrence += 1, independently at its OWN restricted ref | 364/364 refused specifically by THE FIRST ANCHOR MATCHES, not by an unrelated source mismatch. |
| Remove each statementRewrite flag, then re-witness | 12/12 refused by control-flow comparison, including GA-R05. |
| Corrupt each changed replacement digest separately | 141/141 refused. |
| Alter one byte in each changed authored head/close element | 31/31 refused by product witness. |
| Corrupt changed product digest; increment headLines; increment closeLines | Digest refused; both count edits accepted with identical bytes (D7). Total table probes: 733, 731 refused, two informational survivors. |
| H1-H4, new boot assignment: omit it; use && null; acquire options.food; assign null | Own callback injection kills all four: null/null/food/null versus the expected sleep lane. |
| M02, decrement 1 -> 0 | Fence 402/404; own keyup then direct call: named refusal -> admitted. |
| M05, fn(ev) -> fn() | Fence 403/404; own Escape keyup: same Event/key="Escape" -> false/null. |
| M09, sleepCorrecting=on -> !on | Fence 403/404; own true/false/true sequence becomes false/true/false. |
| M10, omit pending=null | Fence 403/404; own delayed forDate then forget: pending false -> true, day null in both. |
| M14/M15, each constructor cache guard -> true, separately | Each fence 403/404; three own hook calls: one constructor -> three. |
| M16/M17, each retry assignment omitted, separately | Each fence 403/404; own retry result: published Promise -> null. |
| M18, force true -> false | Fence 404/404, problem 127/131 unchanged; own trace read/paint/read/paint -> read/paint/paint. D3. |
| M21, clearDraft -> undefined | Fence 404/404, problem 127/131 unchanged; own successful {date:"2030-02-03",hours:7.5} save leaves hours="7.5" instead of "". D3. |

Instrument survivor IDs: I034/I039 (false/throw-tail return value), I041 (recorded-ref filter),
I055/I056 (tie ordering), I072/I073/I076 (overlap), I086 (scan early-exit expression),
I136/I138 (redundant gen-witness refusal). These are limits of the exercised inputs, not
additional demonstrated athlete-facing failures or blanket claims of equivalence.

Independent reconstruction and executed bar

My script reads named Git blobs, resolves anchors itself, applies declared substitutions,
assembles declared wrappers, removes/replaces source spans and inserts composition lines.
It imports neither cut.cjs nor resolve.cjs for that reconstruction. Its s9 output equals BOTH
committed files byte for byte; 679 moved + 294 other lines = 973 LF lines in today-lanes.cjs.
Authored arrays are head=42/open=41/close=143; 34 region labels plus 34 separator lines account
for the remainder. No moved byte differs outside a declared substitution.
SHA-256 today-lanes: c9fe11a015d4a3543c42caefb89e1cfa9e7392d6d3b5dd548d07e8791b924d91.
SHA-256 today-app: d1e1f1e7e69a7ed1af6dbbef8f0d1b9c5eef2017deb7e80f9d4df50da3548cad.
At tip the own script equals cut.cjs too; source-line labels and the known released-source
variation differ from the s9-built commit. PART2-CUT-REPORT.json equals the freshly generated
s9 report after removing only its machine-specific root. The new boot crossing is TA-S06@432,
across seam 422; eleven Today statement-rewrite IDs are reported, plus GA-R05 in the other cut.

Native instruments cell: s9=28/34 and tip=28/34. Missing-scope rows at BOTH refs: 12 name census,
13 capture, 14 census without map, 16 product reproduction's capture tail, 32 generator equality,
33 uncovered assignment. The independent callback replay used for instrument mutants first
reproduced exactly these six failures, then ran unchanged assertions serially.
Native writer fence: 404/404. Single Today files: food=56/57, problem=127/131, checkin=28/28.
The five failing build rows hit ASTRA_REVIEW_READ_ONLY: writes inside the worktree are forbidden.
A preload enforced the commissioned write boundary; no row was skipped or weakened.
The first five writer-mutation runs did not reach the CJS loader; those invalid measurements
were discarded and rerun with a loader hook. The counts above are the verified reruns.

All tests used C:\Users\joeym\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe,
with these separate environment lines before execution:
`$env:MEASURED_TEST_NOW = '2026-09-03'`
`$env:TZ = 'America/New_York'`
Node v24.19.0; Node's embedded Acorn 8.17.0/acorn-walk extracted into OS temp; nothing installed.
Scratch scripts, exact mutations and outputs: C:\Users\joeym\AppData\Local\Temp\astra-split-l2-66.
The unchanged cell also creates disposable splitb-* fixtures in OS temp. No cleanup was attempted.

What I did not verify

- No full eslint-scope run, generator binding-equivalence proof, or zero-capture/census claim; six rows are unavailable, not successful.
- No whole Today step, conformance gate, package/seal runner, receipts, CI, browser/phone behavior, real athlete data, auth files, protected paths or protected soak.
- No new hunt outside the changed hunks/prior findings/banner. The seven STOPs remain for their own tickets; no outcome mapper, one-handoff redesign or new guard architecture was built.
- This worktree's DECISIONS.md has 548 lines; :569/:613/:618 are unavailable here. The commissioning message supplies their applicable constraints. No invented local reading is claimed.
- The banner's unenumerated "eleven sentences" is not a finite branch oracle; injected refusal maps and reasons remain dynamic. The 11/2 prose count earns no independent behavioral PASS here.
- No tracked file edit, commit, push, checkout, reset, stash, clean, fetch, install, or deletion. Only this review file is added inside the worktree.

Final workspace evidence (last shell commands and their stdout)

```text
git status --porcelain
?? rebuild/lanes/astra/reviews/TODAY-SPLIT-PART2-RECHECK-L2.md

git diff --stat -- rebuild/lanes/c/TODAY-SPLIT-BUILD-REPORT-2.md rebuild/lanes/c/today-split-spike/cut.cjs rebuild/lanes/c/today-split-spike/gen-interface.cjs rebuild/lanes/c/today-split-spike/gen-witness.cjs rebuild/lanes/c/today-split-spike/regions.json rebuild/lanes/c/today-split-spike/resolve.cjs rebuild/lanes/c/today-split-spike/test/instruments.test.cjs rebuild/lanes/c/today-split/PART2-CUT-REPORT.json rebuild/lanes/c/today-split/writer-fence.test.mjs rebuild/m3/w7-preview/today/today-lanes.cjs rebuild/lanes/astra/reviews/TODAY-SPLIT-PART2-RECHECK-L2.md
```

The diff command produced no stdout. The command invocation also emitted twice on stderr:
`warning: unable to access 'C:\Users\joeym/.config/git/ignore': Permission denied`
