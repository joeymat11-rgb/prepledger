# PM376 — shmbuf 0.1.0 shared-mapping source assessment

**Result: SOURCE-CONTRACT INCOMPATIBILITY. Stop this exact candidate.** Its native return path lets the creating V8 HandleScope end before the returned buffer handle reaches the JavaScript caller. The pinned API contract forbids using that handle outside its scope. This is a source-established contract violation, not an observed crash or a conclusion that every shared-memory approach is impossible.

Authority: PM376 at `d5c13c378b07a9953b00329f2e63aabd18aa2094`; full GO SHA256 `78911723712faea76cb21cfd622feb214e7d5478f62b53ed082e07731ad3c09f`, input manifest SHA256 `333a0382815a85f96a660862b41874553eaddb496295ee6b8e832ce583a0a02e`. Branch `codex/astra-shared-mapping-capability-b` began at `31d5451ad063af76a25ec10fb7d3cdc59bc9dd91` in the named fresh own worktree. The full 2355-path base index was checked before hydrating four permitted public context files.

Input boundary: complete ER368 final report and reconciliation at `2c9c19a1296152fd6ac30a475d9eb69d7726699e` were read; all six ER evidence blobs were hash-checked. ER's current observer remains refuted and both alternatives unresolved. A test-file OS process is not a worker thread. This assessment changed no original child command, outcome, first-failure handling, or fixed private/public diagnostic boundary and ran no child test.

Candidate: only the published 7918-byte shmbuf 0.1.0 tarball, SHA256 `4b0bf434a319cd1de8dfb9289c006922c983d717b33daa13e0beef024b1e41b4`. SHA512 integrity, safe unique regular member names, and all 11 member sizes/hashes (25847 bytes) matched the PM manifest. Four package files were read as text: `src/shmbuf.cpp`, `lib/index.js`, `binding.gyp`, `package.json`; the other seven were preserved and hashed only, including all package tests.

Source keys below are relative to `reviews/shared-mapping-capability/sources/`: S = `shmbuf-0.1.0/`; N = `node-aa4c7758/`; W = `node-addon-api-8.0.0/`. All line numbers refer to the exact copied bytes. `SOURCES.json` records the primary URLs, full-file hashes, read ranges and reasons; `MAPPING.json` identifies each original/copy pair.

**Loader facts.** S/lib/index.js:3–20 catches native-load failure and switches to a process-local Map of newly allocated SharedArrayBuffers. `SHMBUF_NO_WARN` only suppresses the warning. A successful fallback call cannot show cross-process identity. The public export at line32 offers open/close/unlink without a native-mode status signal. The install-script fallback in S/package.json was read, never run. Any later adoption would need to reject fallback before admitting capture; no such integration was built.

**Decisive source facts.** S/src/shmbuf.cpp:47–60 creates an ordinary inner `v8::HandleScope`, builds a SharedArrayBuffer inside it and returns its handle as a `Napi::Value`; line108 returns that helper result from Open. There is no escape, outer-scope handle or persistent JavaScript reference. Returning the value completes before the inner scope's destructor runs, but the caller consumes the returned handle only after that scope has ended.

**Direct API contract.** Pinned official Node v22.23.2 commit `aa4c77582be995286fc6e00aaf530dc7ade102a9`, N/deps/v8/include/v8-local-handle.h:84–103,223–240, makes local-handle validity depend on its creating scope and requires an escaped handle when crossing to an outer scope. Using a handle after its scope is deleted has undefined behavior. The package's return path violates that contract regardless of whether a particular untested run happens to appear successful.

**Conversion and callback trace.** S/src/shmbuf.cpp:58–59 uses the same pointer-conversion shape as Node's N/src/js_native_api_v8.h:303–318; the cast shape itself is not the finding. W/napi-inl.h:593–597 only stores and returns the raw value, without extending its lifetime; lines76–88 and110–118 forward the callback result. Node's N/src/js_native_api_v8.cc:494–515 calls the addon callback first, then consumes its returned value to set the JavaScript return. That occurs after the package's inner HandleScope has ended.

**Backing lifetime is a separate obligation.** N/deps/v8/include/v8-array-buffer.h:458–470,483–501 specifies the array's reference to its backing store and the external store's eventual deleter. This keeps storage while the array is live; it does not root the array itself. S/src/shmbuf.cpp:17–42 stores only raw pointer/size/Windows handle bookkeeping, and retains no independent backing-store owner or JS root. Lines25–34 unmap storage (and close the Windows handle) on deletion. Close/unlink at111–134 erase bookkeeping, with POSIX unlink additionally requested. None rescues the invalid returned handle.

**Narrow inference.** The published native path cannot establish a contract-valid retained parent view, even before proving that operator, unchanged outer node:test runner, and actual test-file OS process attach the same region. Thus this exact candidate is rejected for PM376. No runtime symptom, memory-corruption event, platform success, overall capture feasibility, or independent acceptance is claimed.

**Mapping/name observations only.** S/src/shmbuf.cpp:69–105 accepts a name and bounded numeric size, uses named CreateFileMappingA/MapViewOfFile on Windows, and shm_open plus MAP_SHARED on POSIX; its existing-POSIX branch shows no existing-size check. These are source observations, not separately established OS semantics or an additional platform defect ruling. An operator-generated unique name could be a proposed environment value consumed by the actual helper, preserving the original argv; no concrete environment wiring, uniqueness guarantee or helper attachment has been proved or implemented.

**Stop boundary.** The lifetime defect is decisive, so the four optional OS documents, the three unused Node definition paths and any broader ABI/toolchain investigation were not fetched or read. Existing-region identity/size, exclusive naming, all-writer termination, parent lifetime, setup-failure containment, nonthrowing updates, memory visibility, and prevention of detachable/replaced views remain unassessed. No alternative package was searched and this package was not repaired.

Wrapper scope: selected node-addon-api 8.0.0 is a static lower-bound example for the package's declared ^8.0.0 range, not a resolved installation. Exact version metadata and archive integrity were verified; only package.json/napi.h/napi-inl.h were extracted from that archive. Four directly needed official Node files were fetched at the pinned commit. This report makes no claim about a different installed wrapper or Node version.

Custody: 21 verbatim public source/metadata/authority copies total 579100 bytes. Each copy matched its original size and SHA256. The two tarballs remain only in own ignored scratch; the wrapper's 16 unselected member bodies were not opened. Five authored files (this report, INDEX and three JSON notes) bring this change to 26 paths, wholly within PM376's report/support boundary. No product, lockfile, fixture, golden, CI or acceptance path changed.

Execution boundary: zero package installs, lifecycle scripts, builds, require/import/evaluation, native binary loads or package/product/test executions. No Node runtime was acquired or launched. Byte download/extraction/hash checks, Git custody and report writing used PowerShell/.NET only. INPUT-CUSTODY.json preserves the pre-execution PowerShell parse error, corrected data extraction and later scratch custody-serialization correction; none is a package test result.

Smallest next fact: a separately named candidate whose returned JavaScript handle is valid beyond its creating scope, with directly traceable ownership. That is a new PM commission, not permission to repair or search. The current source finding does not need a runtime experiment to justify stopping, and does not satisfy the later original capture bars or independent executable review. Complete handoff goes to PM; B then returns LOW/paused under320.
