# B1+B2: precise tooling amendment for154 and184

2026-09-13 · Astra B · proposed under PM b77e74ea67ccf66b7d94e0861100fe552dc9682a/DECISIONS214. Public source analysis and isolated synthetic assertion probes only; no mechanism edits, package CLI, private/FULL/seal run or new acceptance. The licensed runtime builder continues independently. PM judgment is required before the changes below.
Implementation parent remains M100820aa47a4f8729642033499eaec0f0ee282e1. Current H3 parent is receipt187/judgment188/integration189:5f0c3781a227e18ffdf8236090fe2499ecd8782b:rebuild/m4/spec/acceptance-h3-clean-init.json SHA256b457b539a384d8c72531b880cd771e996c6b231f034a49272e899c1fba61e61f, independently rehashed, on M's chain and byte-identical at M.172/180/183 are superseded. Later shared coordination must be absorbed before the future gated composition.

##154: source-base Git-blob pins, with explicit seal proof

Ruling154(5) makes an engine package's pins outside rebuild/engine, rebuild/conform and rebuild/m4 Git-blob pins at its sourceBase, with disk re-verification at seal; implementation was explicitly deferred until after H3. Current r10 held()1581-1585 checks every undeclared inherited pin against disk AND HEAD regardless of path; product()1695-1710 also observes carried/pinned-unchanged images from disk.
Executed discriminant: the exact held() declaration from M was evaluated in an isolated VM with only synthetic hash providers and Node assert. For W6 today-bindings, sourceBase matched the expected hash while disk/HEAD changed together: current held refused PARENT-PIN-BROKEN. A core sleep.cjs control also refused, correctly. The probe read no real file/engine/private data and is not a package execution or final test bar.
Proposed fixed path classifier: normalized repository paths beneath engine/, conform/ or m4/ under rebuild remain disk/HEAD pins. Other inherited unchanged pins are sourceBase Git-blob pins. Derive class in reviewed runner code, never from a spec-controlled option or arbitrary missing-field fallback. Existing path/alias/shape validation remains mandatory; case, traversal, prefix and missing-object tricks must not escape it.
Apply the same rule to own unchanged product roles carried and pinned-unchanged, while preserving their distinct attribution, parent pre-image, role/no-change and ancestor checks. Every declared edited/new/superseded-by-child implementation still proves its actual disk/HEAD post-image; sourceBase proof cannot stand in for newly written code. No old artifact, role or pin value is rewritten.
For a Git-blob pin, public construction/CI verifies the exact sourceBase blob/hash and reports that evidence as Git-bound, never disk-observed. Missing/wrong sourceBase bytes refuse even if disk matches. Core pins, current runner/spec fidelity, closed changed-path/child inventories and explicit implementation post-images keep their existing disk/Git checks in every mode.
Proposed seal boundary for PM judgment: every FULL/sealing attempt must additionally verify the unchanged Git-bound pins against both actual disk and HEAD, at entry and again at the terminal evidence decision before artifact/receipt success can be recorded. An authorized136 re-verification retains all sealed-run byte-identity checks. CI's Git-only allowance never overrides them or licenses a FULL/private call.
Wire that phase explicitly through held()/pins(), product() and the main/envelope decision boundary; do not infer sealing from a user-editable spec field or silently equate the first header-only envelope evaluation with completed execution. Existing r10 ran-phasing, same-envelope-after-children and every issuer/receipt/real-chain check remain. No weaker terminal PASS is introduced.
The existing metadata pin shapes stay unchanged: flat SHA or closed pre/post/role. The new package must disclose the complete derived class/sourceBase map and actual verification phase in its profile/report so no inherited outside-root path silently disappears from evidence. New fields, if mechanically necessary, require a separately named schema delta before code.

##184: repair the stale historical workflow assertion

The residual is cell35, the last test at conform/v4/postfix/test/ci-second-gate.test.cjs:29: it demands today's whole rebuild.yml equal the old a777f64318dfb9b4766fa336d623196d07b5fc00 file with only the STEP command substituted. That was true for STEP; successive accepted native/B-NTC/H3 steps and named suites legitimately changed today's file.184/188/189 explicitly carry this pre-existing red to the next tooling round.
Executed discriminant: the exact last callback, isolated with actual public baseline/M workflow blobs, fails ERR_ASSERTION on whole-file equality. The baseline old-command occurrence is exactly1, the current two-OS matrix remains, and current text has no || true. No other callback, engine, protected second gate or CLI was executed; this is an isolated assertion reproduction, not an entire-suite result.
Historical authority is exact STEP receipt71:904d35ddfb81e1a9b4cfc1d6ccbb50b58651149c:rebuild/conform/v4/postfix/acceptance-step-efficacy.json SHA256ff164b8620ee0ab7851e7d9283b32d1b330b261fad1f02178d4266131cfcabb1. Rehashed, ancestor of M and artifact bytes identical at M. That commit's workflow SHA25663e714daa676c46a8dbcc2f43b7a5b189da26ae6e854c83afe9f03ac9fbd04ce exactly equals the baseline with the single declared STEP command replacement. The artifact itself does not carry a workflow pin; do not invent one.
Proposed cell35 repair: bind that historical one-command claim to this exact receipted commit/path/hash and original baseline, retain full-byte equality and one occurrence, and label it historical. Reject missing/wrong commit/blob/hash/receipt ancestry or any unrelated historical workflow alteration; no permissive substring-only replacement. No historical workflow/artifact/receipt or ci-second-gate.cjs runtime changes.
The CURRENT workflow proof belongs explicitly in the already licensed b1b2-registration.test.cjs: actual sole cumulative B1-B2 command at the existing position, both OS jobs, all standing steps, exact current Today inventory plus only named admitted N2, no unconditional-success bypass or hidden/omitted child. This is mandatory alongside the retained historical STEP assertion, not a historical-only substitute for current CI coverage.
Freeze the original synthetic second-gate evidence/refusal/canary tests and their assertions. Keep their real protected CLI/engine witness separate under its existing execution permission. Run only a dependency-proven public cohort before the exact profile grant; disclose any unexecuted protected case explicitly, and retain it in the eventual required gate. Never turn a setup failure or an excluded case into a pass.

## Exact proposed change inventory

Under rebuild/lanes/b/tooling/: b-package.cjs (only the pin-class/phase plumbing above, separately disclosed from already licensed registration/198 adoption); README.md (class, attribution and phase contract); new test/git-blob-pin-classes.test.cjs (public synthetic probes and mutations); existing test/{pinned-unchanged-and-ruled-substitutions.test.cjs,parent-pin-shapes-and-spec-successors.test.cjs,seal-tip-and-byte-identity.test.cjs} (only precise154 class/phase expectations and negative boundaries). Any other failing test's edit requires a named request, not a broad refresh.
Already licensed test/b1b2-registration.test.cjs must add the current-workflow bar described above; packages/B1-B2.json registers every actual helper/test/class/changed pin after authoring. Additional protected test path requested: rebuild/conform/v4/postfix/test/ci-second-gate.test.cjs, only its historical workflow cell35 and explanatory attribution. Its core/conform pin stays a disk pin and is explicitly superseded in the new profile according to its actual inherited role.
No extra runtime, issuer behavior, gate matrix, law/census, reference fixture, old acceptance file, dependency lock, .github file beyond b77's exact successor hunk, helper executable or assertion path is licensed by this proposal. Existing issuer198 exact hunks/tests remain separately identified; interaction with this amendment must be reviewed and newly executed, not credited from old103/103.

## Nonzero behavioral and mutation bar

G1 sourceBase-correct outside pin with different disk/HEAD succeeds only in the Git-bound CI phase; G2 sourceBase-wrong/missing pin refuses even when disk is correct; G3 core engine/conform/m4 changed disk or HEAD refuses in every phase; G4 parent and grandparent flat/spec-shaped pins preserve exact role and pre-image semantics; G5 carried versus pinned-unchanged attribution stays distinct and changed implementation roles still prove actual post-images.
G6 the same outside-pin disk drift refuses at FULL/seal entry and terminal decision; G7 HEAD/disk disagreement refuses there independently; G8 a change after entry but before finalization cannot receive artifact/receipt success; G9 exact136 sealed-run identity and real-chain/head ancestry stay required; G10 traversal/case/prefix/unknown-class metadata cannot bypass classification; G11 wrong current runner/spec/child or unlisted source refuses despite the Git-bound allowance. Every positive must cross the real named reader/decision seam, not only classify a string.
C1 exact receipted historical STEP replacement passes; C2 zero/duplicate old or new command, wrong historical blob/hash or extra unrelated byte refuses; C3 current missing OS/job/standing step or wrong/duplicate cumulative command refuses; C4 omitted/unnamed Today/N2/child or || true refuses; C5 historical authority/ancestry substitution refuses. Historical proof and current proof must be separately attributable in actual command output.
Required mutations: force every path to Git class; accept disk instead of wrong sourceBase; skip final disk verification; skip HEAD equality; bypass current runner/spec identity; weaken historical full-byte equality to substring; remove one OS or one current suite; ignore wrong/duplicate cumulative command. Each must die at its intended assertion/refusal, never syntax, module/setup error, timeout, skip or an unrelated earlier pin. Actual totals and commands are reported only after execution; these labels are planned cases, not invented passing counts.
Return one exact amendment candidate with source/changed-path manifest, sourceBase/role/class map, current parent evidence, real public commands/results and mutation discriminants. Submit it with the combined successor for separate MAX review and PM's exact profile/gate decision. No artifact, receipt, private gate, integration, deployment or phone outcome is implied here.
STATUS · B · TOOLING AMENDMENT PROPOSED ·154 outside-root pin/phase defect and184 stale historical workflow assertion reproduced in isolated public probes. Exact changes and mutation bar named; PM judgment pending. Runtime builder continues; no tooling mechanism or private gate changed.
