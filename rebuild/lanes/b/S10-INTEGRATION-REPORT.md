# S10 integration report, rounds 2 to 10 (builder claude-opus-5-5, 2026-09-23/24)
Worktree %TEMP%\earned-s10int, branch rebuild/b-s10-integration; round 1 pushed by the PM at 7f8b228; round 2 is local commits only, nothing pushed.
Parent: S9 CANDIDATE 6dc2596 (NOT sealed; every S10-WORKING-BRIEF.md c58b892 s.2.1 value is still a STOP). Rulings: DECISIONS:792 at 25c9276.
Local runs: Windows, Node v24.19.0 (CI uses 22), runtime lock, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York, preload guard that throws before any require/import/read of the protected five; guard log empty in every run.

Round 1 (unchanged, 7f8b228): 92b7487 SPLIT b35a48e3 | 9943679 GSS 66d32530 | aae3b27 EPP 0d38b8e + D-EPP-2 5a953d5 (all 0 conflicts) | 5ad69a7/656b793 fence LOOK_EDITS row red (0/1, names the 3 keys) then green 1/1 | 8e0f507 CHILD_SPECS += S10 | 7bb11d7 D-CR-2 re-pins | e57bc80 Today line + E steps | 1cc556c S10.json draft | 7f8b228 report.

## Round 2 commits
| sha | item | content and evidence |
|---|---|---|
| 88cb9ab | 1 red | F6/F7 of pinned-unchanged-and-ruled-substitutions take S10: 17 tests, 15 pass, 2 fail (F6, F7) against the unedited runner |
| d5c4138 | 1 | runner: IDS += S10 (behind S9), NO_REGISTER_IDS += S10, CHILD_ROOTS += rebuild/lanes/c/today-split/ (27); 17/17. Mirrors 085b6bc (H14-H16, F6/F7) and c4224ee (root per CI-homed lane cell, at the END). PUBLIC_TAIL_ROOTS unchanged. Runner 5321181a -> 9fbfdd2d |
| 853223d | 1 | H3, S3-S9 .json tooling.runnerSha256 5321181a -> 9fbfdd2d, one value each, after the last runner hunk (mirrors f925b6f) |
| 26d39a2 | 3 | local-source-profile.cjs:16 SOURCE_PINS today.cjs 685f6e1e -> b4ebee3c (declared edited, pre 2de07820 post 1a93f571) |
| 2d024a2 | 3 | s3-portable-sources.json re-pins local-source-profile.cjs 2de07820 -> 1a93f571 (the only other pin of that file, measured) |
| 9d39f37 | 4 | gss-annex-timing: bare `fake-indexeddb` import -> createRequire from rebuild/m3/w6/package.json (browser-check.mjs pattern); import lines only. RED before: file fails at load, 0/1. GREEN after: 3/3 |
| fdc7bce | 5 red | sealed-inventory-fence rows (32) proposed-pick, (33) engine-capture-proposed, (34) writer fence: `P-S9-3|P-FENCE-1` 6 tests, 3 pass, 3 fail (32-34) |
| f24fec1 | 5 | E steps get `if: ${{ !cancelled() }}`; writer fence gets its CI home (new step, same condition). Rows green: `P-S9-3|P-FENCE-1|D-S9G|D-CONDITION` 11/11 |
| 2262a40 | 1 | standing step flips to `--ci --package S10`, step NAME moves, one comment paragraph (ef21153 / 6dc2596 shape) |
| 2fb6c70 | 2 | six cells s10-supersede-{source-carriers,inherited-carriers,defect-witnesses,writers-differential,second-gate}.test.cjs + s10-engine-files-differential.cjs |
| 5255186 | 1,2,7,8 | S10.json regenerated (sha256 3520c6eb...): 293 paths (232 carried, 19 edited, 2 released, 39 new, 1 superseded-by-child), 36 children, needles null |

## Item 2: S10's own supersession (not S9's reason)
Cells: a774ca3's mechanical mirror, then the reason rewritten: S10 moves today.cjs and writers.cjs, each asserted as the PARENT's Git bytes at S10's sourceBase (tied to the parent post) plus exactly the one :631 clause and nothing else, declared edited; every other engine file at the parent post; files added under rebuild/engine/ (proposed-pick cell, its report) declared new. The differential's moved half follows s3-engine-files-differential.cjs; its needle still states the compared count and "byte-identical ... rebuild/engine" (supersededGates() in b-package.cjs). Static check of the clause rule (no engine loaded): parent + clause == head, both files, true.
Red-first (local, guarded, nothing protected touched): 5/5 cells and the differential fail at load with ENOENT rebuild/m4/spec/acceptance-s9-ui-pins.json, the expected pre-seal red (S9's cells were red the same way before S9.json existed). Their real rows load the engine and run gate programmes: CI-only.
S10.json coverage.superseded: the five parent carriers (nine gates), evidence s10-sup-*, s10-engine-files-differential, a0-journeys, today-17; rulingLineSha256 null. PROPOSED token lines (drafts only; the PM appends after review; `<S10_PACKAGE_ID>` is a STOP):
`- 2026-09-DD · cowork (PM) · GATE-SUPERSESSION <S10_PACKAGE_ID> source-carriers,inherited-carriers,defect-witnesses,writers-differential,second-gate · the token clause for DECISIONS:153, same conditions (i)-(iii), for the child of M2-S9-UI-PINS (grandchild of M2-S8-REAL-SHAPE and the seventh-generation descendant of M2-S3-COMPANION); the child moves rebuild/engine bytes, today.cjs and writers.cjs by one owner-worded clause each (DECISIONS:631 O-1a), both already outside the reconstruction at the parent, and retires the parent's retired gates again under this line and its own evidence - the five carriers cover the nine gates merge-source, migrate-source, writers-source, migrate-differential, witnesses-2, witnesses-5, witnesses-7, writers-differential and second-gate · RULED`
`- 2026-09-DD · cowork (PM) · RELEASE-FROM-SEAL <S10_PACKAGE_ID> rebuild/m3/w7-preview/today/today-app.cjs,rebuild/m3/w7-preview/today/gym-app.mjs · the release grant of the accepted S10 brief (S10-WORKING-BRIEF.md section 4.3) for the child of M2-S9-UI-PINS: exactly these two paths leave the seal, each at its parent pin, and no other path · RULED`

## Item 3: local-source-profile.cjs:16
Where the pin bites: qualify() compares mapping.source_pins to SOURCE_PINS (both from this module), so at runtime nothing reads disk; the disk check is engine-provider.test.cjs:136-154 (S3-PROVIDER-ENGINE-PINS): :143 SOURCE_PINS[f] == sha(disk), :144 manifest == sha(disk), for merge.cjs, today.cjs, engine-runtime.cjs. That cell reads merge.cjs (protected): CI-only.
Red-first by scratch probe of its today.cjs clause only (%TEMP%\opus55-s10build\probe-lsp.cjs, not committed): before 26d39a2 FAIL (pin 685f6e1e, disk b4ebee3c, manifest b4ebee3c); after PASS. Note: production-mapping.cjs:228 embeds Profile.SOURCE_PINS, so the production mapping's source_pins (and its context digest) move with this pin; MAPPING_ID is ENGINE_REVISION-based and unchanged here.

## Item 6: fence residue row (diagnosis; RE-ANCHORED in round 3 at 9642243, see below)
Row `RECORDED RESIDUE gym-app.mjs: released helper parameter mutation still passes` (fence :1442-1444 at 66d32530) plants after anchor `      () => ({ rows: paintedDraft.rows, cues: paintedDraft.cues }), async (outcome) => {`. That line existed at 04ea69e (gym-app.mjs:258; the GSS build had re-anchored the row there from b35a48e3's `recordSettings(map, view, submittedDraft)`). 46af1b8 "Preserve newer gym settings edits across save" (after 04ea69e, ancestor of 79d981a7 and 66d32530) rewrote it to add `revision: settingsDraftRevision`; at 66d32530 the snapshot is `return { rows: paintedDraft.rows, cues: paintedDraft.cues, revision: settingsDraftRevision };` (gym-app.mjs:309). The anchor matches nowhere, planted() asserts the plant landed, so the row fails before releasedRefusals runs: a stale anchor, not a fence verdict. The recorded residue (a mutation of paintedDraft passes the fence) is untested at the integrated bytes.

## Items 7 and 8
7: the seven parent-unpinned files stay role new, pre null (as ruled); none is an S9 execution pin. 8: YES. b-package.cjs proposed() (b-package.cjs:3405 at d5c4138, :3389 at 6dc2596) pins RUNNER, packages/<ID>.json, the brief and every child argv target as executionPins; S10 edits S9.json (runnerSha256), so S9.json is declared superseded-by-child, pre a1f9fa38 (its bytes at 6dc2596, re-measure at the S9 seal), post 5369b99b. It is the only S9 execution pin S10 changes that is not an S9 product pin.

## Round 3 (Fable review l1 ACCEPT WITH NAMED DEBTS, 166f14cd; commits local, not pushed)
| sha | debt | content and evidence |
|---|---|---|
| 9642243 | D-S10I-6 | Residue row re-anchored to `    const paintedDraft = settingsDraft;` (gym-app.mjs:278, unique; the plant mutates paintedDraft before renderEditor at :280 reads it). One hunk, anchor string only, no assertion change; PM ruling: not an S-R30 row, so :626 (4) does not bind it. Red first (guarded, `RECORDED RESIDUE` rows): 10/11 pass, the plant did not land; after: 11/11. Full fence: 404/404 (was 403/404) |
| 469b369 | D-S10I-5 | rebuild/m4/import/test/engine-pins-unprotected.test.cjs: S3-PROVIDER-ENGINE-PINS' two equalities (SOURCE_PINS and the manifest == disk) for today.cjs and engine-runtime.cjs only, reading no protected file; registered by path in the M4 step and the m4-import child. Red first: with local-source-profile.cjs at 26d39a2~1, 0/1 naming today.cjs; head 1/1. The full cell (merge.cjs included) stays CI-only |
| 1786b4b | D-S10I-7 | rebuild/lanes/b/S10-REGEN.cjs: `node rebuild/lanes/b/S10-REGEN.cjs --parent <S9_PARENT_COMMIT> [--receipt-line N] [--write]`, run after merge-forward (never rebase). Re-measures every pre from the sealed artifact's product map, changed S9 execution pins (S9.json superseded-by-child), parent.options[0] sha256/reviewSha256, sourceBase, runnerSha256, D-SPLIT-PARENT; parent-released paths stay undeclared; notes, children and coverage are carried. Dry run by default; --write refuses without the sealed artifact. Dry run at 6dc2596 (candidate mode): 295 paths, 0 entries differ from the committed S10.json; S9.json pre a1f9fa38; D-SPLIT-PARENT 3/3 EQUAL; --write refused, exit 2 |
| 426fbb6 | D-S10I-2, -3 | S10.json (sha256 bf241376...): note [2] now says Markdown under rebuild/engine/ is declared. today-17 keeps its name and a note: it runs 22 files. Needle grammar re-checked: at least 8 chars, no line break, matched at line start (CHILD-NEEDLE-EMPTY, CHILD-NEEDLE-NOT-A-TERMINAL-LINE), so '# pass N', null until observed |

## Round 4 (Astra blind L1 of 1cf99d3: REJECT, B1-B4; commits local, not pushed)
| sha | item | content and evidence (local runs guarded as above) |
|---|---|---|
| (9642243) | B1 | Already paid in round 3. Re-run at 47983ba: full writer fence 404/404, the residue row among them (the plant lands on `    const paintedDraft = settingsDraft;`, gym-app.mjs:278). Astra's in-memory anchor `      }, async (outcome) => {` also lands; both keep the mutation and the guard |
| 12be4ea | B2 = D-EPP-4 | proposed-pick.test.cjs: rows R2-R4 become functions of the engine (bodies and all assertions unchanged); new EPP-R9 runs them on EPP-R7's in-memory half engines and requires RT to fail R2's card check (test:142) and R3's card/record check (test:160), RW R3's untapped check and R4's no-down-pull check, each at that assertion (EPP-D4-SENSITIVITY-REPORT shape). The real cell needs index.cjs: CI-only. Local proof on the twelve-public-factory composition (scratch %TEMP%\opus55-s10build\epp4-harness.cjs, R8 skipped): 0d38b8e cell 7/7, and still 7/7 with R2's or R3's main assertion deleted (the debt); S10 cell 8/8, and each of four deletions turns exactly EPP-R9 red (7/8) |
| 995e684 | B3 | package.test.cjs: TODAY_REQUIRED_INPUTS gains today-readings.cjs, today-lanes.cjs, gym-settings-lane.mjs (H18's per-input refusal now covers 29); H18b pins 51/29; H18c baseline 51; controls unchanged, no row dropped. Measured by the cell's own rule on build.mjs text: 48/26 at 6dc2596, 51/29 at head, diff exactly those three. Source-slice harness (the file's before() builds the bundle: CI-only): RED before (H18b, H18c), GREEN 2/2 after; negative control: edited cells against 6dc2596's build.mjs refuse, naming the three |
| 6f69c9b | B4 | boundary.test.mjs (g): releaseAccounts() accounts for a drifted sealed file only if the youngest declaring spec releases it (post null, pre == the older spec's sealed post, release block present, no child executing it); the view gets no post pin. Row S10 RELEASE-ACCOUNTING: control accepted, seven ungranted variants refused by name. Source-slice harness (page-stack import: CI-only; protected pins counted via S10.json carried, never read): RED before, undeclared [today-app.cjs]; after (with fdee511), undeclared [], today-app released by S10 from efaf6c0d |
| 47983ba | D-REASON-TEXT | s10-engine-files-differential header no longer says the package moved nothing |
| fdee511 | | S10.json (8ed50317...): 295 paths (231 carried, 20 edited, 2 released, 41 new, 1 superseded-by-child); S10-REGEN dry run at 6dc2596: 0 entries differ |
Astra's named debts taken where feasible: D-REASON-TEXT (paid). The destructure-aware api.lane census is NOT feasible here: no parser resolves from this worktree (acorn absent at root, w6 and w5; the same gap as D5). The rest are the STOPs below.

## Round 5 (Fable l2 ACCEPT WITH NAMED DEBTS, f4d20943; small separate commits)
| sha | debt | content and evidence |
|---|---|---|
| f7c2980 | D-S10I-9 | boundary.test.mjs release-accounting row names the RE-RELEASE (S9 released the path, S10 releases it again; accounted from S8's pinned post, the nearest older non-null post; declaring a re-release at all is the runner's one-generation rule) with an explicit control asserting ok, by S10, pre = S8's post. Source-slice harness: row green, undeclared [] unchanged |
| d1e76b1 | D-S10I-10 | S10-REGEN.cjs regenerates the three measured notes (PRODUCT MAP, PARENT-UNPINNED PATHS, EXECUTION PIN SUPERSEDED) from the run's own values; any other carried note still citing 6dc2596 / a1f9fa38 / CANDIDATE is FLAGGED and blocks --write unless --allow-stale-notes. --receipt-line is REQUIRED with --write, checked first (receiptLedgerLine is a parent binding the runner re-reads; optional only in a dry run). Dry run at 6dc2596: notes [1], [5], [6] regenerated, [0] flagged (the PROPOSED DRAFT note, PM re-authors it at finalization); --write without --receipt-line refused; with it, refused for the absent artifact |
| 2e76e71 | | S10.json (56e75b6e...): same 295-path counts; REGEN dry run 0 entries differ |
D-S10I-8, the EPP-R9 guard's scope and what CI owes. SCOPE: EPP-R9 guards exactly the four assertions the sensitivity pass named as the clauses' behavioural guard: R2's card check (test:142 at 0d38b8e), R3's card/record check (:160), R3's untapped-entry check and R4's no-down-pull check. The assertions of R1, R5, R6, R7 and R8 are OUTSIDE it (R1 producer order, R5 tap control, R6 reversed-order control, R7 half-repair control, R8 import road); deleting one of those is not detected by R9. OWED FROM CI on the real cell (index.cjs, both OS, frozen env): the cell green with 9 rows; and, as the red half, the four single deletions (R2 card check, R3 card/record, R3 untapped, R4 no-down-pull) each turning exactly EPP-R9 red. Only the scratch composition has shown that red (7/8 each), never the real engine.

## Round 6 (Astra L2 of 9bd4414: REJECT, B5; commits local)
c675711 red: S10-REGEN.test.cjs compiles the helper with fake node:child_process/node:fs (every git answer and file invented, every read recorded): control + 7 refusals (out-of-scope, conform/private, soak inside a root, traversal, disk symlink, junction above, Git 120000), each BY NAME with no read of it and no product read first; the 5d42dac helper 0/8 ('READ rebuild/secret/other.json before refusing it'). a8eff43 (B5): discovery is `git diff -- <reviewed roots>`; every spec/artifact/pin/declared/changed path is validated before any read (scope, shape, forbidden set, ls-tree `--` mode 100644/100755 at parent and HEAD, no link up the path); blobs read by object id only; protected five never read (object ids and status). D-REGEN-INPUT: --write needs an ACCEPTED parent review; parent-released paths leave the whole declared union (b1339ca rows: old 0/2, new 2/2). cad0b03 adds the test cell to the scope. Cell 10/10. f9477f3: S10.json (79933a86...) re-pins S10-REGEN.cjs fe184527 and declares the cell new: 296 paths (231 carried, 20 edited, 2 released, 42 new, 1 superseded-by-child). Dry run --parent 6dc2596: 35 roots, 614 path/revision pairs validated, 73 changed in scope, 0 entries would change, D-SPLIT-PARENT 3/3 EQUAL, exit 0. Open: the cell has no rebuild.yml step (tooling, not product); say if it should.

## Round 7 (Fable l3 ACCEPT WITH NAMED DEBTS at 6696062; published by the PM at ac79eab)
D-S10I-11: S10-REGEN.test.cjs gains two rows on the same fake ports: --write with no --receipt-line refused by name with ZERO reads; --write refused while a carried note cites the S9 candidate (sealed, ACCEPTED fake parent). Against the 1786b4b helper 0/2 (both attempt to write S10.json); against HEAD's helper the cell is 12/12. D-S10I-12: the cell gets a rebuild.yml step by exact path with `if: ${{ !cancelled() }}` (the P-S9-3 shape S9 gave its lane cells) and row (35) of sealed-inventory-fence.test.mjs reads it: red 6/7 before the step, 12/12 selected rows after. No declared child mirrors it: rebuild/lanes/b/ is not a CHILD_ROOT, and widening CHILD_ROOTS for operator tooling would move the runner and re-pin the eight ancestor packages; the workflow step is its home. S10.json re-pins from the working-tree bytes: S10-REGEN.test.cjs dd665b42, rebuild.yml 45b286b7, sealed-inventory-fence e49c3b0c -> 6abf4d4d. REGEN dry run --parent 6dc2596 on the uncommitted worktree reports those three as disk-vs-HEAD (expected); on a scratch detached worktree with this diff committed (removed after) it reports 0 entries would change, exit 0.
D-S10I-13, DISCLOSURE: every S10-REGEN run before a8eff43 (the 1786b4b and d1e76b1 helpers: my round 3-5 dry runs at 6dc2596, and Fable's l2 dry run) HASHED the bytes of the protected five (seed, migrate, merge, index, oracle-shim under rebuild/engine/) through `git show` and a disk read, because S9.json pins them as carried product. They were never loaded or executed and only their sha256 was printed; no content entered any output. From a8eff43 on the helper reads only their Git object ids and `git status -- <path>`, never their bytes (Fable l3's probe: no read of seed.cjs in any case).
## Round 8 (Astra L3 of 6696062: REJECT, B5 residual and B6; left UNCOMMITTED for the PM)
B6: PAID by round 7 (ac79eab): the rebuild.yml step "B - S10-REGEN's path boundary and write contract, on synthetic ports" (`if: ${{ !cancelled() }}`, `node --test rebuild/lanes/b/S10-REGEN.test.cjs`) and sealed-inventory-fence row (35), which reads that step by exact path.
B5 residual, red first: S10-REGEN.test.cjs gains 19 rows on the fake ports. Astra's 13 ADMIT spellings as changed paths (LEDGER, Ledger, SRC, `ledger.`, `ledger `, SEED.cjs, Merge.cjs, `seed.cjs.`, `index.cjs `, ORACLE~1.CJS, auth.json, .credentials, a.cjs:secret), each refused BY NAME with no read of it and no product read first; then a LEDGER path and an auth.json path through each other input source (S10.product, the parent product map, the parent artifact path). Against the ac79eab helper 12/31: all 19 new rows fail with 'the helper READ <name> before refusing it'. After the fix: 31/31, guard log empty.
Fix, in S10-REGEN.cjs shape(), which every source passes before any Git or disk read. A colon in a segment (ADS), `~digit` (8.3) and a segment ending in a dot or a space are refused outright. FORBIDDEN is tested on the name and on its canonical form (lower case, trailing dots and spaces stripped per segment). Any other spelling of a protected file is refused. Under the allowed roots, a segment named auth.json or .credentials, or matching /auth|credential|token|secret/i, is refused unless it is in AUTH_ALLOWLIST, which is EMPTY: no path that S10.json declares matches, nor any S9 product or execution pin (46 tracked in-scope names match; none is declared). Auth is checked after scope, so the old out-of-scope row still reports scope. The scope is unchanged at 35 roots.
S10.json re-pins from the working tree: S10-REGEN.cjs fe184527 -> ef535849, S10-REGEN.test.cjs dd665b42 -> 19e0c5a3. REGEN dry run --parent 6dc2596 on a scratch detached worktree with this diff committed (removed after): 296 paths, 0 entries would change, exit 0 (note [0] flagged as before). Left open: D-REGEN-NOTES. Note [8] still calls D-EPP-4 unpaid and the residue row red, although both are paid (12be4ea, 9642243). Astra places the reconcile at final input binding.

## Round 9 (Astra L4 of 62788b1: REJECT, B5 residual only; left UNCOMMITTED for the PM; D-S10I-14 ruled by the PM, variant installed)
Residual (L4 B5, S10-REGEN.cjs:69, :83-95, :142-153 at 62788b1): the auth exclusion knew only auth|credential|token|secret (plus auth.json, .credentials), so standard credential/key names under the allowed roots (rebuild/m4/spec/.netrc, _netrc, .ssh/id_ed25519) passed shape() and were read from Git and disk through changed paths, S10.product, the parent candidate product and the sealed parent product (Astra 12/12).
Red first: S10-REGEN.test.cjs gains 86 rows on the same fake ports; the only content anywhere is an invented JSON marker, no real credential or key file exists or was opened. (i) 45 = five names (.netrc, _netrc, .ssh/id_ed25519 under rebuild/m4/spec/; .npmrc and keys/deploy.pem under rebuild/m4/workout/) x nine sources: changed paths, S10.product, parent candidate product, parent candidate execution pins (S9 child argv), sealed parent product, sealed execution pins, artifact path, review path, D-SPLIT-PARENT split sources. (ii) 41 changed names, one per clause instance: 9 dot-names, 12 credential/key basenames (case, .pub, _sk, a directory named id_rsa, the stem known_hosts.json), 13 key containers (all ten extensions, upper case, inner signing.pem.json, directory certs.p12/), 7 off-list extensions (none, .txt .py .sqlite .yaml .png .zip). Each row: no read of the name, exit 2, REGEN-PATH-REFUSED with the name and the reason, and (except split sources, validated after the product reads) no read but the fixed or sealed inputs before the refusal.
RED against the unmodified 62788b1 helper (worktree bytes ef535849, equal to the S10.json pin and to the blob extracted with cmd git show): 117 tests, 31 pass, 86 fail. 76 fail "the helper READ <name> (content read reached) before refusing it": all five names through changed paths, S10.product, parent candidate product, candidate execution pins, sealed product, artifact path and review path (35), and all 41 clause rows. 10 fail "not refused" (exit 0): sealed execution pins and split sources admit the names but never read their bytes (object ids only). Tap %TEMP%\opus55-s10build\run-r9-red.tap, sha256 a7335d926663.
Fix (shape(), after every existing refusal, order kept: plain path, ADS, 8.3, trailing dot/space, canonical forbidden set, protected alias, scope, auth words): positive admission, admission(segs). A path is admitted for a content read only if (a) no segment starts with a dot; (b) no segment, nor a segment without its final extension, is in CREDENTIAL_NAMES (_netrc netrc .netrc credentials known_hosts authorized_keys authorized_keys2; id_rsa id_dsa id_ecdsa id_ed25519 id_ecdsa_sk id_ed25519_sk and each .pub; .git-credentials .gitconfig .htpasswd .pypirc .npmrc .pgpass .dockercfg; lower-cased); (c) no dotted part of any segment is in KEY_EXT (pem key p12 pfx kdbx ppk asc gpg jks keystore); (d) the final extension is in ADMITTED_EXT = .cjs .css .html .json .md .mjs .yml, derived from S10.product (296: .cjs 163 .mjs 113 .json 13 .md 3 .yml 2 .html 2) and S9.product at 6dc2596 and at HEAD (255: .cjs 134 .mjs 105 .json 9 .yml 2 .html 2 .md 2 .css 1); no extension is refused. Reason text "not admitted for a content read: ...". The four-word list stays as a second layer, not the contract.
GREEN (before the ruling): 117/117 (31 prior + 86 new), exit 0, guard log empty; tap run-r9-green.tap sha256 aa373ce32458. Final count after D-S10I-14: 120/120, below.
D-S10I-14, the STOP as measured before the ruling (the builder did NOT widen the rule): the helper's actual shape() (source slice, %TEMP%\opus55-s10r9\eval-shape.cjs, names only) over every name S10-REGEN validates at the real repository with --parent 6dc2596 (S10.product 296, S9.product 255 at the parent and at HEAD, S9 execution pins 86, fixed 4, artifact/review 2, split sources 3, changed in scope 73; union 309): 307 admitted, 2 refused, .github/workflows/rebuild.yml (declared edited) and .github/workflows/shared-preflight.yml (declared carried), both "a dot-name segment (.github)". Whole helper on the fake ports: an exact .github/workflows/rebuild.yml change was refused by name, exit 2, so a real dry run would have refused by name at those two paths (the 62788b1 helper admits 309/309).
D-S10I-14 RULED by the PM (the ledger line is the PM's to record): clause (a) does not apply to a path that is EXACTLY an exact-file SCOPE entry of the helper; (b)-(d) still apply to those paths; no directory or prefix entry gains the exemption; any other dot-name path, including a .github path nested under rebuild/, stays refused. The ready variant is exactly that and is INSTALLED as a byte-exact copy of %TEMP%\opus55-s10r9\varB\rebuild\lanes\b\: S10-REGEN.cjs cfead8b045194b45bf2d9bb40b60927b1f5b96b430e0d87bc36b89b4dbb8d0d6, S10-REGEN.test.cjs 159bd49fc5b1755f5a1909de0c1b37779ce97f8e82cdc35290c70072e5fb95c0 (the pre-ruling worktree bytes 83c45d4f and ffb32aea were never committed). The code change is S10-REGEN.cjs:98-103 plus the comment at :87-92: `const EXACT_REVIEWED = new Set(SCOPE.filter((r) => !r.endsWith('/')));`, `const exact = EXACT_REVIEWED.has(segs.join('/'));` and `if (c.startsWith('.') && !exact) return 'a dot-name segment (' + s + ')';`; (b), (c) and (d) still run for every path. EXACT_REVIEWED holds the six SCOPE entries without a trailing slash, and only .github/workflows/rebuild.yml and .github/workflows/shared-preflight.yml have a dot-name segment, so the exemption changes the result for those two paths and no other (S10-REGEN.cjs, S10-REGEN.test.cjs, S9-UI-PINS-BRIEF.md and rebuild/m1/MOCK.md have none). The match is on the whole path, case-exact, after shape() has already refused empty, '.', '..', backslash, ADS, 8.3 and trailing dot/space spellings, so .GitHub/workflows/rebuild.yml or rebuild/m4/spec/.github/workflows/rebuild.yml is still refused by (a).
Red first for the three new rows (the exact rebuild.yml admitted and measured; rebuild/m4/spec/.github/workflows/rebuild.yml and rebuild/m4/workout/.github/x.json refused by name as dot-names, no read of them): the same test bytes against the pre-ruling helper 83c45d4f, 120 tests, 119 pass, 1 fails, the control row ("REGEN-PATH-REFUSED declared or changed: .github/workflows/rebuild.yml (not admitted for a content read: a dot-name segment (.github))"); the two nested .github rows pass against both helpers (they hold the line against a wider exemption). Tap %TEMP%\opus55-s10r9\fin\run-r9-final-red-83c45d4f.tap sha256 00ef5dbb58b2. GREEN, installed bytes, committed REGEN cell suite: 120/120 (117 + 3), exit 0, guard log empty; tap run-r9-final-green.tap sha256 11d8e31d9000. Name-only admission slice (eval-shape.cjs, same sets, --parent 6dc2596) with the installed helper: S10.product 296/296, S9.product 255/255 at the parent and at HEAD, execution pins 86/86, fixed 4/4, artifact/review 2/2, split sources 3/3, changed in scope 73/73; union 309 admitted, 0 refused (same run with the 83c45d4f helper: 307 admitted, 2 refused, the two .github files).
Re-pins, S10.json only (git grep of both old values over rebuild/lanes, .github, rebuild/m4/import, workout, spec and rebuild/m3/w7-preview: no other pin): S10-REGEN.cjs ef535849dbeeb02408a7ab45f6b8bfbbfc514dee2c529147407da5e557daba07 -> cfead8b045194b45bf2d9bb40b60927b1f5b96b430e0d87bc36b89b4dbb8d0d6; S10-REGEN.test.cjs 19e0c5a3a05afa9560a278224114e059517c335f3b0dda15ebd4c4f8db3c14a1 -> 159bd49fc5b1755f5a1909de0c1b37779ce97f8e82cdc35290c70072e5fb95c0, both re-pinned to the installed bytes (the pre-ruling pins 83c45d4f and ffb32aea are superseded). The S10.json diff against 62788b1 is exactly those two lines (L155, L160; blob read by node, 2024 lines each side). Roles unchanged (new, pre null); 296 paths (231 carried, 20 edited, 42 new, 2 released, 1 superseded-by-child). No REGEN --write or dry run against repository data (the committed cell suite and the name-only slice only); nothing loaded the protected five.
Remaining named debts, unchanged: D-PARENT/LOCK, D-SPLIT, D-GSS, D-EPP/CR, D-ACCEPTANCE, D-REGEN-NOTES. Not claimed (L4 limits): race resistance between validation and read, hard-link identity.

## Round 10 (Astra L5 of fc6561f: REJECT, B5 residual; Fable l4 ACCEPT WITH NAMED DEBTS D-S10I-15, D-S10I-16; left UNCOMMITTED for the PM)
Residual (L5 B5): name rules cannot show a file is public. rebuild/m4/spec/service-account.json, kubeconfig.yml and id_rsa.backup.json (Astra), keys.json, passwords.json, private.json, env.json, apikey.json, wallet.json and keystore/x.json (Fable D-S10I-15) pass every name and extension rule and reached a content read.
Fix, POSITIVE REVIEWED PROVENANCE (S10-REGEN.cjs, B5 L5 comment and code; no name rule added, every name/extension rule kept as defence in depth). validate() gains a last check, after shape() (plain path, ADS, 8.3, trailing dot/space, canonical forbidden set, protected alias, scope, auth words, admission (a)-(d)), the Git regular-file check and the on-disk link check: the path must be an EXACT member (string equality, case-exact, on the name shape() already normalized) of REVIEWED, the union of the inventories the run binds: the four fixed inputs; the exact-file SCOPE entries (:801); the S9 parent artifact and review, bound to the runner's slug s9-ui-pins (rebuild/m4/spec/acceptance-s9-ui-pins.json, review-s9-ui-pins.json; b-package.cjs:2072, S9-UI-PINS-BRIEF.md:669-670), and S10.json must name exactly those two (else REGEN-PATH-REFUSED parent artifact, before any read); the product keys of S10.json; the product keys of the parent's S9.json at P and its execution-pin paths (runner, S9.json, brief, every child argv target, computed once from S9.json at P in both modes). Checked against it: the changed paths the run would declare and read, the sealed artifact's product and executionPins keys, the artifact/review paths and the D-SPLIT-PARENT sources. blobAt() and diskAt() also throw on any non-member (last line).
What the helper reports for a changed path outside the inventory: exit 2, `REGEN-PATH-REFUSED declared or changed: <path> (not admitted for a content read: not an exact member of a reviewed inventory (...); declare it in S10.json first)`, before any read of it. So REGEN no longer auto-declares an undeclared changed path as new; it must be declared in S10.json first (a reviewed change). The exact .github/workflows/rebuild.yml row still admits (an exact SCOPE file). Changed Markdown outside rebuild/engine/ and S10.json itself are never read or declared and stay under the name rules only (control row).
RED, the new test bytes against the unmodified fc6561f helper cfead8b0 (worktree bytes equal to the pin and to the blob extracted with cmd git show): 183 tests, 121 pass, 62 fail. 41 "the helper READ <name> (content read reached)": the ten names through changed paths, the sealed parent product, the artifact path and the review path (40) plus an undeclared ordinary changed name rebuild/m4/workout/b.cjs; 20 "not refused" (sealed execution pins and split sources, exit 0, object ids only); 1 SyntaxError (S10.json naming the declared a.cjs as the artifact: read and parsed). Tap %TEMP%\opus55-s10build\run-r10-red.tap sha256 449b73118c0a.
GREEN, installed bytes: 183/183 (120 prior + 60 provenance rows + 3), exit 0, guard log empty; run-r10-green.tap 7b7060cc0b99. Sensitivity (flat scratch copies, %TEMP%\opus55-s10r10\mut): without the validate() provenance line 122/61 (21 caught only by the blobAt/diskAt last-line guard as an Error, 20 not refused, 20 artifact/review rows refused by the binding instead), fa72a9113b05; without the artifact binding 182/1 (the a.cjs-as-artifact row), 474a49ba0f6b.
Fixture: world() now declares the split source VIEW as an S9 parent product pin that S10 carries (the three real sourceBlobs.s9 are S9/S10 product paths); no assertion removed or loosened, all 120 prior rows still present and green.
LIMIT, measured (scratch probe, not a committed row): a name that S10.json or the parent S9.json ITSELF declares is a member by that declaration. The ten names through S10.product, the parent candidate product and the candidate execution pins: 30/30 read, exit 0 (%TEMP%\opus55-s10r10\limit-probe.out). Provenance cannot refuse a declared name; the review of the declaration is its gate. Not claimed as paid; STOP 12 below.
D-S10I-16 PAID: the test resolves REPO once, from the helper under test (path.resolve(path.dirname(HELPER), '..', '..', '..'), the helper's own expression), header documents it. Red: the fc6561f test at a 3-deep scratch path with S10_REGEN_UNDER_TEST naming a flat copy of the cfead8b0 helper: 120 tests, 1 pass, 119 TypeError (e70abda812fd). Green: the new test at the same path and the same flat helper: 121/62, the same red as in the tree (0d19cdfd4a40); the installed helper as a flat copy 183/183 (7afe34561b10).
Real slice (eval-provenance.cjs: the ACTUAL shape(), REVIEWED construction and artifact binding cut from the installed helper, names only, --parent 6dc2596): S10.product 296/296, S9.product@P 255/255 (and @HEAD 255/255), execution pins 86/86, fixed 4/4, artifact/review 2/2 (binding EQUAL), split sources 3/3, changed and read 66/66, changed never read 7/7 (six Markdown reports under today-split-spike/ and today-split/, plus S10.json); reviewed inventory 303 exact paths; union 309 admitted, 0 refused. No declared path is refused.
Dry run, guarded, on this UNCOMMITTED worktree (--parent 6dc2596): 614 path/revision pairs validated, 303-path inventory, 296 paths, no refusal; exactly 2 entries would change and 2 PROBLEM lines, both the uncommitted S10-REGEN.cjs/test (disk differs from HEAD), exit 1 as expected before commit; D-SPLIT-PARENT 3/3 EQUAL; note [0] flagged as before. Nothing written.
Re-pins, S10.json only (git grep of the two old values over rebuild/lanes, .github, rebuild/m4/import, workout, spec and rebuild/m3/w7-preview: only S10.json L155/L160 and this report's text): S10-REGEN.cjs cfead8b045194b45bf2d9bb40b60927b1f5b96b430e0d87bc36b89b4dbb8d0d6 -> 246e6dcc9a060809b60be401c9f6bfd37d4a7e547551a50af67ac51bc51506ab; S10-REGEN.test.cjs 159bd49fc5b1755f5a1909de0c1b37779ce97f8e82cdc35290c70072e5fb95c0 -> 487552385a7b12ad8670d2da5b7c5845746ffbdf6438849b6cf0ff094b0617c0. S10.json diff against fc6561f: exactly L155 and L160 (2024 lines each side, blob read by node). Roles unchanged.

## STOPs remaining (D-S10I-1, complete list)
1. Every s.2.1 placeholder: S9_PARENT_COMMIT/sourceBase, the S9 spec/receipt/verdict and both-OS CI ids, the S9 artifact and review sha256 and receipt line, S10_PACKAGE_ID, the S10 brief path/sha, THEME, BRIEF-BY-SHA and the two PROPOSED token lines above, all child needles.
2. D-SPLIT-PARENT (3.1): re-measure the three sourceBlobs.s9 at S9_PARENT_COMMIT. They are EQUAL at 6dc2596 only; S10-REGEN.cjs prints the check and refuses --write on a mismatch.
3. Exact-head both-OS rebuild run id and conclusion at the composed S10 candidate (10.2, :627); no green inferred from a lane run.
4. CUI0 acceptance with its independent audit and its pins at the S9 parent, and the section 8 copy lock with its eight answers, source ownership and CI step (12.1, :732 A4). Neither exists.
5. D-GSS-PASSTHROUGH: the destructure-aware census of api.lane at the integrated bytes (round 1 ran a regex census only: 0 product readers, 1 test reader; no parser available locally); D-GSS-TIMER reported, not fixed (6.2).
6. S-R30 re-assertion (4.2): at the integrated bytes no today-app exception row changed and each gym row matches its :628 ruling. Measured so far: the fence blob before S10's own hunks equals 66d32530's (09a6dd18), and S10's two fence hunks are the LOOK_EDITS row and the residue re-anchor, neither an exception-site row. A reviewer's final assertion is still owed.
7. D-EPP-4: paid in bytes (12be4ea); the real cell's green and its four-deletion red are owed from CI (D-S10I-8). D-EPP-3 gates owed: port oracle, sensitivity, private gate, exact-head CI.
8. D3, D5 (the real eslint-scope stack), D6, D7, D9, the S-R33 boot row, the six inherited scope rows, the Track A re-cut and DOM/listener cells, and the M/R/N hunk classification (3.2, D-GSS-LINES): not done.
9. machine-settings-ui three-way merge, red-first on both OS; engine-capture.test and configuration-capture.test need PERFORMED_W6_DIR; the coach ENGINE_REVISION moves only after a receipt.
10. D-S10I-4 (CI); Astra's re-review of B1-B4 at this head; the separate Claude final (D-ACCEPTANCE).
11. ruled by PM (D-S10I-14): exact-file SCOPE exemption. Installed (S10-REGEN.cjs cfead8b0, S10-REGEN.test.cjs 159bd49f): 120/120; real names 309 admitted, 0 refused; the ledger line is the PM's to record.
12. PM ruling owed (B5 L5 LIMIT): the commission asked for every source to refuse the ten names; provenance refuses them through six of the nine (changed, sealed product, sealed execution pins, artifact, review, split), but through S10.product, the parent candidate product and the candidate execution pins the name IS a member of the inventory the commission named (30/30 read). Options: (a) rule that a declaration in the reviewed S10.json or parent S9.json is the provenance (reviewers read the declared names; name rules stay the only other check); (b) bind an external reviewed inventory, e.g. the sha256 of the sorted declared key set pinned in the helper and re-pinned on every intentional addition (moves the helper at the S9 seal); (c) add the name words D-S10I-15 listed, which the commission excluded as the payment. The builder chose none.
CI-only (not run locally): Today step (except gym and the five annexes), measure suites, proposed-pick, engine-provider (incl. S3-PROVIDER-ENGINE-PINS) and the rest of the m4-import step, `b-package --ci --package S10`, the other tooling suites (they spawn the runner), sealed-inventory-fence's real and fixture rows, the s10-sup cells' real rows, the split instruments.

## Round 11 (runbook S10-SEAL-RUNBOOK.md a39a0253 T3a; PM rulings DECISIONS:816; builder claude-opus-5-5, 2026-09-25; left UNCOMMITTED for the PM)
Base: W HEAD f97924a; parent d7f654017962 (sealed S9, receipt :809); CHAIN 94977a9 read for :810-:816 only. Every run went through %TEMP%\pm-run.cjs (shared slots), Node v24.19.0, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York, preload guard (throws before any protected load). Guard refusals in the whole round: 3, all rebuild/engine/migrate.cjs, all from machine-settings-ui S14 (the build row); nothing protected was loaded or read. Scratch and logs: %TEMP%\opus55-s10r11.
PRODUCT BYTES CHANGED (the PM re-runs REGEN --write): rebuild/m3/w7-preview/today/test/problem.test.mjs (declared edited) sha256 5fe767e02937745f22700316f8950af932977d05085ba09fe78fd068f2b1cbd8; rebuild/lanes/c/today-split-spike/cut.cjs (declared new) sha256 10ea939b9b9042a4901ac8d6ac4bd6df43fb012fff2f9bed8f88fc7a82b293bb. Not product: rebuild/lanes/b/tooling/packages/S10.json notes[8] only, sha256 194eaf1e49c97678978c37ddbf461581bb5468f7e8907ea99bfb404cf6033120 (before the REGEN re-pin of the two files above).
PARSER ROUTE (brief 6.1, runbook Q11), measured: acorn 8.17.0 and acorn-walk exist INSIDE node.exe v24.19.0 (internal/deps; the route Astra L3/L4 used, nothing installed); round 11 reads them with --expose-internals, and for child processes copies Node's own embedded sources into scratch (%TEMP%\opus55-s10r11\instr2, acorn sha256 f4ecbb1935ad718f). eslint-scope: ABSENT everywhere searched (W node_modules junction, rebuild/m3/w5 and w6 junctions, %TEMP%\p2-tools, C:\Users\joeym\.cache, AppData\Roaming\npm, Astra's parser scratch, Node internals). No farm folder exists on this PC (/home/claude/farm is a Linux path).
| item | result |
|---|---|
| D-SPLIT-PARENT (3.1) | EQUAL 3/3 at d7f6540: today-app ea98aef6, gym-app 48bf0531, today-model 6a146ff9 = regions.json sourceBlobs.s9 |
| Track A re-cut (6.1) | cut.cjs --product --witness s9 at the parent's three blobs (both historical invocations), exit 0 both; 6/6 outputs EQUAL the Track A base b35a48e; today-app d1e1f1e7, today-lanes f1d95426, today-model fb6802f2, today-readings f7b3ae45 EQUAL HEAD; gym-app and gym-settings-lane differ at HEAD only by Track B. Re-run after the D6/D7 fixes: same 6/6 |
| Independent reconstruction | recon.cjs (imports no instrument; blobs by object id; own anchor/context resolution, witness sha/line checks, substitutions, product and compose assembly): 6/6 EQUAL b35a48e, 4/4 EQUAL HEAD, moved lines 679/41/36 = witness, substitutions 39 of 41 (the two covered rows of L3), 2/2, 5/5; 0 problems |
| DOM/listener cells (D.2/D.2b) | PART2-DOM-LISTENERS at BEFORE = d7f6540: 7/7 DOM EQUAL, 7/7 listener censuses EQUAL (16 kinds), NOT COVERED 0. Ran as a scratch copy whose ONLY change is the Windows ESM path form (3 imports wrapped in pathToFileURL, jsdom by createRequire): the committed harness fails on Windows with ERR_UNSUPPORTED_ESM_URL_SCHEME before measuring (D-S10R11-1). W restored, HEAD-equal checked |
| D3 / M18 (red-first) | row 'S10 D3 / M18' in problem.test.mjs: red only against mutant m18 (readSleepCheckIn(date, false) in readSleepCheckInView), trace read/paint/paint; green at head (read/paint/read/paint) |
| D3 / M21 (red-first) | row 'S10 D3 / M21': red only against mutant m21 (success path without painter.clearDraft()); green at head (hours "" after the save, one op after a second tap) |
| S-R33 boot row (red-first) | row 'S10 S-R33': plain options (counting Proxy, no accessor), model.setFoodDays hands over options.sleep; pins sleep 2, workout/checkin/setup/installation/setupFirst/food 1 each, and api.sleepLane() is the handed-over lane. Red only against mutant sr33 (bootFoodDays re-acquisition removed = L2 F2); green at head |
| D3/S-R33 runs | each mutant: 2/3 with exactly its own row red; head 3/3; today-lanes.cjs restored and HEAD-equal |
| D6 (red-first, instrument fix) | competing replaces nested in ONE seam were never compared (nested rows leave the flat overlap check): TA-I004x on TA-I004's line in seam TA-M03, fully witnessed, HEAD cut exit 0 with a different today-app.cjs. cut.cjs now compares every nested replace pair and refuses by name. Retained: move/move overlap (TA-S01 into TA-S02) refused before and after |
| D7 (red-first, instrument fix) | headLines/closeLines + 1 on each of the 3 product blocks: HEAD cut 6/6 exit 0 with base bytes; fixed cut 6/6 refused by name. D6/D7 harness: HEAD cut 3/10 as required, fixed cut 10/10 |
| D9 (21 survivors) | EQUIVALENT by reading, argued per call site: I042, I052 (calledOn null/undefined: every consumer tests truthiness or === a string), I046 (no non-member callee carries an Identifier object), I088, I089 (canon is only called with null/undefined, arrays or typed nodes), I172 (parse options set no loc/range), I194 (firstDiff consumers test truthiness; report.structure is written only for rewrite rows, measured 25, never null), I224, I229, I235, I236 (unreachable: fileStructure parses the same text with the same options before the loop, so the whole-file candidate always parses), I281 (enclosingHeader: compared with a string, or serialized inside an array), I289, I295, I301 (contextAt: sole caller filters by truthiness). DISTINGUISHED by branch-specific planted inputs (instrument mode, never --product): I047 and I048 (facade[k]() accepted, exit 0), I082 (array hole: TypeError instead of the refusal), I094 (facade.x(0) read as x, accepted), I196 (null vs node: TypeError), I107 (refusal text differs at the first-difference path only). 6/6, d9.cjs |
| Instruments cell | instruments.test.cjs 47/53 with HEAD cut and with the fixed cut (same rows): 5 cite the absent eslint-scope; row 27 compares the codemod output with the COMMITTED gym-app.mjs, which is post-GSS at the integrated bytes, so it is red by construction (D-S10R11-3) |
| machine-settings-ui three-way (Windows) | control 92b7487's own test on its product 53/54; RED merged HEAD test on the pre-merge product (gym-app, gym-settings-lane, machine-settings-view at 92b7487) 52/66, 13 genuine (the 12 GSS rows + S6b); GREEN head 65/66. The 1 failure in all three is S14, guard-tripped (build reads rebuild/engine/migrate.cjs): PM/CI only. Linux arrives only in hosted CI (Q12) |
| M/R/N (3.2, D-GSS-LINES) | 299 physical hunks over the 65 declared non-carried paths, 0 unclassified: Track A (P->b35a48e) M 15, R 127 (today-app M 15 R 109: 645 moved / 338 authored-or-substituted changed lines; today-lanes R 1: 686/299; today-model R 4; today-readings R 1; gym-app R 11; gym-settings-lane R 1); Track B (b35a48e->HEAD) N 60 (gym-app 47, gym-settings-lane 13); rest of S10 (P->HEAD) N 83, R 14 (8 runnerSha re-pins, local-source-profile, 4 s3-portable-sources, configured-history run.cjs). Rows: mrn-rows.json. Counts are at f97924a (before this round's two files) |
| D-GSS-PASSTHROUGH | destructure-aware AST census (acorn; not scope analysis) of 1165 tracked code files under rebuild/ and .github at HEAD: 138 reads of a `lane` key (member 138, computed 0, destructure 0, in 0, reflect 0); passthrough class 2 = gym-app.mjs:607 (the passthrough's own definition, api.lane()) and machine-settings-ui.test.mjs:1253 (unit.lane.api.lane); second pass: 426 reads of a `settings` key, 10 bindings of one, 0 reads of lane through any chain, alias or destructure. Unresolved computed keys 3, none a settings object. Retirement stays a separate ruling |
| D5 and the six inherited scope rows | STOP: they need eslint-scope, which no existing route provides (above). Named debt candidate D-S10R11-2 (D5 = the six rows). Never paid by a substitute |
| D-REGEN-NOTES (notes[8]) | reconciled: the EPP-R9 real cell's GREEN half is hosted run 36158401022 step 23 success on both OS (step conclusion only); the four-deletion RED half stays owed (D-S10I-8); writer fence CI step 25 success on both OS |
| CI-0 36158401022 | measured per step: 13 standing step failure (both OS), as predicted; 16 fence, 23, 24, 25, 26, 33, 34, 35, 37, 38 success on both OS; the rest skipped |
New named items: D-S10R11-1 PART2-DOM-LISTENERS.mjs cannot run on Windows as committed (ESM path form); D-S10R11-2 eslint-scope route absent (D5 + six scope rows); D-S10R11-3 instruments row 27 red at integrated bytes (compares against post-GSS gym-app.mjs); D-S10R11-4 D6/D7/D9 inputs live in scratch harnesses (d67.cjs, d9.cjs), not in instruments.test.cjs: durable home is the PM's call (that cell is never CI).
STOPs still open after round 11: D5 and the six scope rows (D-S10R11-2); every s.2.1 placeholder (T3b/T5/T6); CUI0 acceptance and the copy lock (T3c); S-R30 reviewer re-assertion; the EPP-R9 real red half and D-EPP-3 gates; Linux red/green of machine-settings-ui (hosted); S14 and every guard-tripped row (PM under grant (g)); B12 (declaration provenance ruling); Astra L7 and the Claude final; exact-head both-OS CI.
