# M2-S6-TODAY-CHILD - author report, round 1

Branch `rebuild/b-s6-today-child`. Base `0635f4b6`; the tip moved to `2a269bd8` mid-round (DECISIONS:491-492 only, B-LOM accepted and held for S6) and is MERGED in, not rebased - stop 7. Brief of record measured on the branch: sha256 `8a9a63ee...`, 41900 bytes, the `:489` values exactly. The citation method was validated on S5's own three FIRST - line 460 -> `00f955e7`, 461 -> `2e84f849`, 462 -> `f9de6a7b`, each matching the field `S5.json` was sealed with - and only then used on S6's 488 / 489 / 490.

## 1. The merges and every conflict

`c-s6-small 6fad3472` clean; `b-s6-child-tail d1001da9` clean; `c-p3-import-ui-2 4c3a5474` conflicted in FIVE files, every one among the seven `:484` predicted. All resolved by carrying BOTH sides:

1. `today-entry.mjs` - `setupFirst` and `installation: hosts` are orthogonal options on one `mountToday` call.
2. `today-app.cjs` - the build footer and the Import entry both append to the Today root; the footer's contract is that it is LAST, so the entry goes first and the footer keeps its place.
3. `today/build.mjs` - `commitOf`/`injectCommit` and `assertImportRouteIsolation` are independent exports; the result object and the A1 PASS line carry both field sets.
4. `import/test/page-bundle.test.mjs` - UI-2 does not drift off the old input law, it RULES it false (`:475 (1)`), so its rule supersedes the tip's `assert.throws`; but the inventory is the post-merge tree's, so the m4/import list is the union of TEN and the graph figure is RE-MEASURED as 133 + 1 (F7) + 2 (F8, RV-G4 router) + 4 (the route's own) = 140, not summed from 136 and 138, neither of which had seen the other.
5. `w6/test/local-today-journey.test.mjs` - PAGE_PINS. Both branches pinned today-entry.mjs at their own post, so neither sha is true here; re-measured over the post-merge bytes.

Two more cells were green on each branch alone and false together, corrected the same way: ROUTE_MODULES 16 -> 18 and the P3-B5 delta 16 -> 18 (boot count still 121).

## 2. Declaration: brief versus tree

Every root reconciles EXACTLY (`git ls-tree -r` on the post-merge head, `Object.hasOwn` against all five specs): w6/local 13/1, w6/host 13/5, w6/test 83/1, m4/import 30/0, m4/workout 3/0, lanes/d 8+2+4+1+1+1, w7/import 9/0, measure 15/15, today 57/15. All 89 exist; none is already S5-declared. No count differs from the brief.

**Only 66 of the 89 are declared - stop 1.** The other 23 are unchanged here AND executed by no declared child, and the runner's closed vocabulary has no honest role for that: `new` means a file that did not exist or one this package moves, and `pinned-unchanged` is refused by `PRODUCT-PINNED-UNCHANGED-IS-NOT-EXECUTED-BY-A-DECLARED-CHILD`. Inventing a child to make a pin legal would be gaming the rule.

`packages/S6.json` declares **187** = 114 S5 pins re-asserted + 66 of the 89 + this package's own six cells + `packages/S5.json`. By role: 96 `carried`, 18 `edited` (including `rebuild.yml`), 17 `new`, 54 `pinned-unchanged`, 2 `superseded-by-child` over parent EXECUTION pins - `packages/S5.json` and, measured not assumed, `today/test/copy.test.mjs`, unpinned only in the parent's PRODUCT map while the parent EXECUTES it (`:109` gives that case its own role).

## 3. The flake: cause, reproduction, fix

**Cause, measured rather than described.** `settle` was a FIXED 24-turn macrotask drain with no condition. Instrumented on an idle PC the trial table needs **18 of those 24** turns after the last waist reading (measure screen 10, marker-pick close 8): a margin of SIX TURNS on the machine where it passes. A runner that advances the page less per turn crosses it, the drain returns anyway, and the next line dereferences null or reads `no trial table rendered` - both reported symptoms, from one line.

**Reproduced red on purpose.** Machine starvation does NOT reproduce it - 24 spinners, `UV_THREADPOOL_SIZE=1`, and a 6 ms-per-turn main-loop hog all stayed green at 73-91 s, because slowing the machine slows the drain too and so gives the page MORE wall time. The starvation that matches the mechanism is the budget itself: with the pre-fix `settle(rounds)` cut to 12, cell (a) goes RED with `the markers pick screen is not offered`; at 16 it passes, so the threshold sits between 12 and 16 against a shipped 24.

**Fixed at the cause; the same starvation is green.** The wait is now the page's own condition with a bounded deadline that fails LOUDLY naming the slot, the elapsed ms and the turns. `page()` awaits `api.ready` - today-app.cjs publishes it for exactly this - then the named screen; `go(slot)` waits for its control, clicks, waits for the screen to CHANGE, then for the terminal state the caller names; `typeWaist` waits for the table to repaint AND then to stop repainting; `pickMarkersOnScreen` waits for the pick screen to close; `close()` waits for the page to be still before tearing the lane down. Under the identical starvation the fixed code is GREEN - the knob is inert, because there is no budget left to starve. The red side is executed too: with the deadline cut to 40 ms it fails `MEASURE-SETTLE-DEADLINE: waited 40 ms over 4 macrotask turns for the Today screen to stop changing after mountToday`, instead of a TypeError three frames away.

Two further faces surfaced only once the condition was explicit, and both are fixed: the trial table repaints in STAGES, so "it changed" was true while the last week still read `Not enough data yet`; and tearing the lane down mid-repaint made the next paint call `markers()` on a closed client (`LOCAL_CLIENT_CLOSED`, cell (e)). `trialDevice()` is now per cell - the reason the flake always took (a) and (d) TOGETHER. The file costs 147 s instead of 53 s, three real devices instead of one; that is the honest price. `import/test/support.mjs` was checked as the brief asks: it has no drain at all and awaits real promises, so it does not carry this race.

## 4. Files and hunks

`b-package.cjs` CHILD_ROOTS +9 (the brief's eight plus `m3/w6/test/`, the author's correction - the brief declares three files there and says the W6 whole-tree suite runs them, and rebuild.yml has no such step); PUBLIC_TAIL_ROOTS +2; IDS and NO_REGISTER_IDS gain `S6`, lose `B-LOM`; new guarded `RETIRED_IDS` (stop 6). `pinned-unchanged-and-ruled-substitutions.test.cjs` F6 / F7 (all 17 roots, first eight re-asserted unchanged) / F8 (two added, six withheld named) + the RETIRED_IDS pin. CHILD_SPECS `'S6'` in `boundary:83`, `food:792`, `machine-settings-ui:709`, `setup:2315`; `problem.test.mjs:1583` lifted onto a module-level `CHILD_SPECS` iterated youngest-first with its rule stated - it never gained `'S5'` and stood TWO generations stale, and c-s6-small moves today-entry.mjs, which that cell hard-pins two ways. `measure/test/{support,journey}` the flake fix (S5-sealed: a disclosed sealed move). `rebuild.yml` A0 gains food-live-save and host-seams; four new steps give the Import screen, the m4/import lane, the lane D suites and the two w6 cells a CI home; the standing `--package S5` step is UNTOUCHED. `packages/{H3,S3,S4,S5}.json` runnerSha256 re-pinned (S5's own product pin for the runner deliberately NOT re-targeted - S5 did not produce these bytes); `packages/B-LOM.json` DELETED with its id (`:487` stop 2). New: five `s6-supersede-*.test.cjs` and `s6-engine-files-differential.cjs`, in S5's shape.

## 5. Bar rows executed, and the suites

Green and executed: 13, 14, 15, 18, 20, 21, 24 (stated in advance), 26, 27, 28, 30, 31, plus S5's twelve carried. Row 22 executed in the adjusted form (stop 4). **Row 23 PASSES against the S5 constant**: `MAPPING_ID` and `ENGINE_REVISION` are only CONSTRUCTED, tested against the same live constant, and DISPLAYED; a scan of every tracked `.cjs/.mjs/.js/.json` finds NO comparison of a stored mapping id or engine_revision against a current one, and `local-source-profile.cjs`'s `qualify()` reads neither - so the rotation cannot invalidate an admitted import. Re-run in round 2 against the S6 constant before the coach line moves. Rows 16, 17, 19 are NOT met (stop 1). Row 29's 20 windows-latest runs cannot be run from here; the measured margin, the deterministic red and the green under the same starvation stand in their place.

`--ci --package S6`: **PUBLIC CI EVIDENCE PASS, exit 0**. All 21 children OBSERVED exit 0 at their exact declared needles; PRODUCT IMPLEMENTED (37 post / 0 pre / 96 carried / 54 pinned-unchanged / 0 unlisted drift, covering all 114 parent pins); FIDELITY and AUTHORITY OBSERVED (both S6 lines resolved on the chain branch); LAWS 45/45 executed, ending `AUDIT RED-FIRST FAIL` - the `:485` observation, unchanged by S6 and the baseline for a package with no D-id (bar row 25); 9 gates SUPERSEDED under DECISIONS:490.

Needles, measured on the post-merge head and none copied: today-17 `# pass 681`, measure-hermetic 11, s4-real-day 15, a0-journeys 23, s6-sup-* 4/3/3/3/3, d-plan-edit 68, m4-import 62, m4-import-production 24, d-import-retract 13, d-admission-swap 4, d-replay-measure 9, d-capture-start 14, food-live-save 6, w7-import 35, w6-host-seams 9, w6-local-source 25, and `ENGINE FILES DIFFERENTIAL: 27 tracked ... 18 named and NOT ONE moves, so all 45 tracked rebuild/engine file(s) stand byte-identical to the parent's own post` - bar row 26, 45 tracked, 0 moved.

`--ci --package S5`: RED, **`SEALED-PROFILE-RECOMPUTATION`, exit 1**, reached only after SPEC OBSERVED and PARENT BOUND print - so the re-pin worked and S5 stops exactly where H3, S3 and S4 stop under a child. No refusal token is changed and no coverage is lost. Tip control in a second worktree at the clean tip, because two reds had to be adjudicated rather than assumed: `s3/harness.test.mjs` 3/14 HERE and 3/14 THERE; `p3-replay-all` 26/0 THERE and 25/1 HERE.

## 6. Stops

1. **23 of the 89 cannot be declared** (section 2): build/dev-host glue `w6/local/{build,source-commit}.mjs`, `w6/host/{plan-edit-host,build-host,esbuild-probe}.mjs`, `w6/host/index.html` (HTML - nothing can execute it at all); harnesses reached only by spawn or a constructed path (`m4/import/test/{mutations,reading-replay-faults}.cjs`, the four `test/s3` files and its `.json` fixture, `lanes/d/plan-edit/{model-mutants,host-mutants,astra-rerun}.mjs`, `w6/test/local-source-consumer-browser.mjs`); and those whose only child is red for a cause this package did not make (`m4/import/replay-registry.cjs`, the four `p3-replay-all` cells). PM: widen "executed", add a role, or accept the shorter list.
2. **`d-replay-all` is a BLOCKING regression this merge makes.** 26/26 green on the clean tip, 25/1 here. P3-EN1 finds three writers in the shipped page with no replay family and no declared reason: `body-composition-class.cjs#body-composition-source`, `measure-replay.cjs#body-composition-source`, `sleep-replay.cjs#sleep`. Structural, and exactly what the custody hole hid: UI-2 puts the Import route INTO the shipped page graph, so modules that were route-only when P3-REPLAY-ALL-FAMILIES measured its register are now, by that cell's own definition, writers the page has. Register gains three declared reasons, or the enumeration excludes the lazy Import route - lane D's ruling. This package will not edit another lane's guard to go green.
3. **`m4-import-s3` is red for an environment, not for this branch**: 3/14 here and 3/14 on the clean tip, the same `Actual owned dispatcher scratch required` - the IMPORT_M4_DIR class of `:459` NOTE 3. Not declared a child; named absent in rebuild.yml's own comment.
4. **PUBLIC_TAIL_ROOTS: the brief says three, `:487` says two.** The narrower reading is taken and the disagreement recorded, with a measured fact that cuts the same way - `page-bundle.test.mjs` under the third root carries the literal `ledger/` in its own FORBIDDEN list, so TAIL_DENYLIST's content gate would withhold that child's tail on any failure that prints the list anyway.
5. **UI-2's boundary named-drift set could not have passed on any tree.** It named FOUR files as the sealed drift under today/; S4 pins only two. preview.css and build.mjs are among the seven S4 left unpinned, so they can never appear in a drift-from-S4 set. The cell's own note says why nobody noticed: on the UI-2 branch the assertion above it was red, so execution never reached the list. Corrected TIGHTER, not looser - the sealed set is still exact, and the two unsealed movers are asserted to be unsealed AND declared.
6. **`RETIRED_IDS` is a new runner constant and needs review.** Removing an id and deleting its spec is one act (`:487` stop 2), but TOOLING_FILES is derived from IDS, so the deleted path stops being named there while git still reports it - the runner would refuse `UNLISTED-SOURCE-CHANGE` for a deletion it made necessary. The product map cannot say it: `post: null` reads as "not written yet" and puts the package in PARTIAL. Fixed in the runner, literal, guarded (a retired id must not be runnable and holds no exemption) and pinned by F6.
7. **The tip must be MERGED, never rebased.** A rebase was attempted once when the tip moved: it replayed all 24 commits, linearised the three frozen merges and re-created every c-p3-import-ui-2 conflict, rewriting the frozen branches' own commits. Aborted and merged instead. That is `:467` note 1's mechanical reason, and it holds for every future round of this branch.

## 7. Open items

- **B-LOM is not merged this round**, per the ticket. It is now ACCEPTED and held for S6 (`:492`, `rebuild/d-b-lom` 8e03401d at da7d5b37). Round 2 merges it LAST and re-measures every post after it. For round 2: `:491`'s figures (A1 122 pinned inputs, Import graph 136 -> 137) were computed against a base without UI-2's four route modules, and this round measures that graph at 140 - so round 2 re-measures rather than adds.
- The `:456` trip-wire is NOT armed by this branch: the standing rebuild.yml step is still `--package S5`, so `engine-revision.test.cjs` stays green and C5 is not red yet. That is the PM's flip at the ff-merge, in `:465-467`'s order, and the constant moves ONCE.
- `MEASURE_SETTLE_DEADLINE_MS` and an env-gated `SETTLE-TRACE` line are left in the measure support on purpose: the first is how the red side of the new wait is executed, the second is how the next occurrence is diagnosed instead of guessed at. Both are off by default.
- DASHES, disclosed rather than left to be found. NO product string gains an en or em dash this round; the built-asset guard is untouched. Five lines the author wrote do carry one: the new `rebuild.yml` step NAMES (`A0 —`, `P3 —`, `M4 —`, `D —`, `W6 —`), written that way because all eight step names already in that file use it and a mixed list would be the worse defect. Every other added line a dash scan reports is carried, not authored: the PM's own `:491`/`:492` lines, b-s6-child-tail's merged tail-diagnostic comments, the owner and contract ledger lines quoted byte-exact into `S6.json` (a citation may not be edited), and `refusal-route.test.mjs`'s `/[–—]/`, which must contain them to detect them. If the PM reads the rule as covering CI step names too, it is a five-line change.

# ROUND 2 (DECISIONS:493)

## R1. The merges, and the one conflict

Four merges, none rebased (`:493 (8)`). (1) the tip `7c77d0b6` - DECISIONS:493 only, one line. (2) `origin/rebuild/d-en1-route-graph 196717cf` - CLEAN: two lanes/d files and its review, and `lanes/d/p3-replay-all/*.test.mjs` is **28/28** on this tree. (3) `origin/rebuild/d-b-lom da7d5b37` LAST, as `:492` orders. (4) the tip AGAIN at `2364b98` just before this report, because it moved to `:494` (it merged EN1 itself at `6ba896fc`; this branch already carried `196717cf`, so the only new bytes are the ledger line and one line of lane D's report).

`today-bindings.mjs` and `gym.test.mjs`, the two SEALED moves, auto-merged clean. `source-admission.mjs` and `p3-capture-start/capture-start.test.mjs`, both-sides files, auto-merged clean. **ONE conflict: `import/test/page-bundle.test.mjs`**, resolved by carrying both sides and RE-MEASURING rather than summing, which is what that cell demands of anyone who moves it:

- P3-B2, the Import graph: this branch had **140** on a base that had never seen B-LOM's module; B-LOM had **137** on a base that had never seen UI-2's four. Neither is the post-merge truth. Measured: **141**, and B-LOM's `assert.ok(paths.includes(legacy-order-mapping.cjs))` is carried.
- P3-B5, the delta: **18 -> 19**, and for the first time it is NOT all route. `legacy-order-mapping.cjs` is reached from `today-bindings.mjs`, so the **Today boot graph moves 121 -> 122** and the total 139 -> 140; the ROUTE-ONLY set is unmoved at 18 and is now asserted as such beside the delta, because "what the route costs" is P3-B4's eighteen and not this subtraction against a frozen constant of another commit. Both sides disclosed in the cell and in `S6.json`'s own round 2 note.

`:494`'s lane C carry named four stale cells on the plain tip+UI-2 tree; on THIS tree round 1 had already re-pinned two of them, so the two above are the whole of it and `route.test.mjs` is green untouched. `w7-preview/import` is **35/35**.

## R2. Declaration: 72 of 89, and the 17 with their reason

`packages/S6.json` declares **196** = 114 S5 pins + **72** of the brief's 89 + B-LOM's three new files + this package's own six cells + `packages/S5.json`. By role: 94 `carried`, 20 `edited`, 25 `new`, 55 `pinned-unchanged`, 2 `superseded-by-child`. **23 children**, all OBSERVED exit 0.

Round 1 declared 66 and its SEALED note miscounted them as 67/22 (**MAJOR 2**); the true split was 66/23 and the undercount was the enumeration - `m4/import/test/s3/` holds SIX undeclared paths, not five. Corrected in the artifact, not only here. Round 2 then declares six more, so **72 / 17**:

- **replay-registry.cjs and the four `p3-replay-all` cells** - their only child was red for a cause this package did not make; lane D's EN1 fix landed, the suite is 28/28, and the `d-replay-all` child runs it at `# pass 28` with a rebuild.yml step of its own (**MAJOR/BLOCKING 1**).
- **`w6/host/plan-edit-host.mjs`** (**MAJOR 3**) - round 1 filed it under "glue nothing executes" and the reason was false: `d-plan-edit` reaches it through `new URL('../../../m3/w6/host/plan-edit-host.mjs', import.meta.url)` in `durable-host.test.mjs:37` and `browser-build.test.mjs:13`. Declared `pinned-unchanged`, and `executedClosure` now reads a RELATIVE `new URL(..., import.meta.url)` specifier exactly as it reads a relative import. That closes a false negative and weakens nothing: the specifier must still be a relative literal resolving to a real file, and the pinned-unchanged rule is untouched. Pinned by new **F10** with both sides - red before the widening (measured: 15/1), green after, the CONTROL through a plain import, and the BOUND asserted (a computed specifier is still unseen). That residue is the lane B tooling carry `:493 (3)` names: "executed" is a lower bound, never an upper one.

**THE 17, none of which any declared child executes:** build and dev-host glue - `w6/local/build.mjs`, `w6/local/source-commit.mjs`, `w6/host/build-host.mjs`, `w6/host/esbuild-probe.mjs`, `w6/host/index.html` (HTML: nothing can execute it at all); harnesses reached only by spawn or by a COMPUTED path - `m4/import/test/mutations.cjs`, `m4/import/test/reading-replay-faults.cjs`, the six `m4/import/test/s3` paths (`browser-entry.mjs`, `current-head.cjs`, `harness.test.mjs`, `mutations.cjs`, `run.mjs`, `s3-portable-sources.json`), `lanes/d/plan-edit/{model-mutants.cjs,host-mutants.mjs,astra-rerun.mjs}`, `w6/test/local-source-consumer-browser.mjs`. Accepted as the shorter list by `:493 (5)`.

## R3. B-LOM, declared and both carries discharged

`engine-order.cjs`, `legacy-order-mapping.cjs`, `test/legacy-order-mapping.test.cjs`, `lanes/d/b-lom/legacy-order.test.mjs` are declared; `CHILD_ROOTS` gains a tenth root `rebuild/lanes/d/b-lom/`; the `b-lom` child runs the provider and the route at `# pass 30`, with its own rebuild.yml step. `today-bindings.mjs` and `gym.test.mjs` move to role `edited` over their parent PRODUCT pins - the sealed moves, declared as such.

**The R3 MINOR is now a named cell, LOM-S6**, not a sentence: on the real route the anchor is ALREADY on the facts before `attach()` re-states it (both ids equal to the admission's own digests); `today-bindings.mjs` is the SOLE builder of the mapping (read off the tree, not declared), calls `attach()` in exactly one place, and hands the pinned host the WRAPPED projector; and - said out loud rather than argued away - `attach()` handed a projection no order law ran over DOES stamp an anchor, while a disagreeing one refuses. Red side executed: with the wrapper's `importAnchor` forwarding removed, LOM-S6 goes red with LOM-A/LOM-B (18 -> 13/5); mutant reverted, bytes restored.

**The R4 MINOR is a stated header**: `legacy-order-mapping.cjs` now says that `order_input` is presence-bound like the ten digests, that the undetected case is the narrower "map deleted AND the session-start scrubbed from `order_input`" rather than "all three copies of Q", and why no wrong order can still result.

**Bar row 4 re-run on the merged tree**: LOM-A/LOM-B, both seasons, green in the 30 - the hand table with the governing record's SHAPE compared, not only the printed line. **No-import byte identity re-run**: LOM-E green on this tree (two identical no-import installations byte-equal on the whole engine view; `metadata.localSources` absent, no `legacy_baseline`, no `import_anchor`). The cross-tree half stands on B-LOM R4's own 36-line probe; what this tree adds is that **no engine byte moves** (all 45 byte-identical to the parent) and that the only module B-LOM puts in the boot graph is the provider, which imports nothing.

## R4. Bar row 23, executed as a cell with a red side (MAJOR 4)

`P3-P6` in `m4/import/test/production-admission.test.mjs`. An import is admitted end to end against the PRODUCTION mapping under the standing constant `M2-S5-TODAY-CHILD@0df73b01f3d2d935`, asserted BY NAME. The constant is then substituted in the module cache and `production-mapping.cjs` re-required over it - **no `rebuild/coach` byte moves**, `engine-revision.test.cjs` stays green, and the substituted literal is deliberately one nobody will ship. `MAPPING_ID` is asserted to differ ONLY in its suffix; the two digests a rotation-sensitive read would compare are asserted to DIFFER, so the cell cannot pass vacuously. Then the same store is read back through a registry built AFTER the rotation, by `reopen()` - a real re-qualification, not a cached read: still `ready`, the imported loads and session-log days identical, **the durable record's `engine_digest` untouched**, Today's own consumer still adopting it, `retractImport` still refusing an admitted import by name and `rollback()` still re-qualifying it. The one thing the rotation does move is measured rather than glossed: a fresh qualification names the mapping that qualified IT, so its own `engine_digest` is the rotated one; nothing is committed by the read. **RED SIDE, applied and reverted**: with `qualify()` mutated to compare the row's `engine_revision` to the current constant, P3-P6 is the ONLY cell of that file that goes red (7 pass / 1 fail, `SOURCE_ENGINE_CONTEXT_UNPROVEN`); reverted, `git diff` empty.

## R5. The two MINORs and the NOTE

**MINOR 5, F7's title**: it said "adds exactly eight" over a nine-element `slice(8)`. The TITLE was the false thing and the title is what changed - now "adds exactly ten", with the tenth (`lanes/d/b-lom/`) stated where the ninth's reason stands. **MINOR 6, PUBLIC_TAIL_ROOTS**: two roots, `:493 (6)`. The finding was that `p3-replay-all` bought nothing without a consumer; it is answered by supplying the consumer (the `d-replay-all` child), not by retreating. `lanes/d/b-lom/` is a new CHILD root and is NOT added to the tail list: a root earns a tail by being argued for, never by a child appearing under it. **NOTE 7, the two both-sides auto-merges of round 1, disclosed**: `rebuild/lanes/c/P3-RUNBOOK.md` and `rebuild/m3/w7-preview/import/test/support.mjs` changed on both sides of the UI-2 merge and git merged them without a conflict; both were read after the fact - the runbook is documentation and the support module's two sides touch different exports.

## R6. The post, re-measured after the last merge

Every declared file's post is the post-merge byte, recomputed from disk and checked against its role (`carried` and `pinned-unchanged` must stand at their pre; `edited`, `new` and `superseded-by-child` must reach a post they do not already stand at). `H3/S3/S4/S5.json` `tooling.runnerSha256` re-pinned to `8d9a94c20faa` - one line each, and S5's own PRODUCT pin for the runner deliberately NOT re-targeted. `A1 TODAY BUILD PASS`: **140 pinned inputs**, **boot graph 122 modules**, route-only 18. `A5 PWA BUILD PASS`. Bar rows executed green this round: 4 (B-LOM's), 13, 14, 15, 18, **19** (the lanes/d classes are closed and run), 20, 21, 22, **23**, 24, 26, 27, 28, 30, 31, plus S5's twelve carried. Rows **16 and 17** are met for the 72 and reported short for the 17 named above. Row 29's 20 windows-latest runs still cannot be run from here.

## R7. Verbatim tails

```
B PACKAGE S6 PUBLIC CI EVIDENCE PASS - public evidence only, NOT the package verdict; the 19 original gates, the private oracle and independent exact-artifact acceptance remain separate, and POSTFIX PACKAGE PASS is unavailable on this mode at any time   EXIT=0
B PACKAGE S6 PRODUCT IMPLEMENTED; 47 at the declared post-image / 0 at the pinned pre-image / 94 carried byte-identical from the parent / 55 declared role "pinned-unchanged" - executed by a declared child, produced by nothing / 0 unlisted drift; the inventory covers all 114 parent-pinned product files; 2 declared role "superseded-by-child" over a parent EXECUTION pin
B PACKAGE S6 LAWS 45/45 executed | TOTAL 45 laws - 45 RED-frozen - 39 RED-candidate - 89 GREEN repair controls - 97/104 mutant executions DETECTED - 0 HARNESS_ERROR - AUDIT RED-FIRST FAIL
B PACKAGE S6 COVERAGE 0/19 original gate(s) covered by 0 executed child(ren); 9 SUPERSEDED under DECISIONS:490 (defect-witnesses 1, inherited-carriers 3, second-gate 1, source-carriers 3, writers-differential 1); 10 re-execute under --full
B PACKAGE S6 NO-REGISTER OBLIGATION S6 registers no D-id ... 11 of 11 declared child(ren) executing one of this package's own role:"new" product file(s) ran in this process, exit 0, with their exact declared needle at line start (23 child(ren) declared in total)
B PACKAGE S5 FAIL SEALED-PROFILE-RECOMPUTATION; required evidence missing or failed; local diagnostics withheld   EXIT=1 (after SPEC OBSERVED and PARENT BOUND print)
ENGINE FILES DIFFERENTIAL: 27 tracked rebuild/engine file(s) outside this package's declared product, all byte-identical to the parent; 18 named and NOT ONE moves, so all 45 tracked rebuild/engine file(s) stand byte-identical to the parent's own post;
CHILDREN, all 23 OBSERVED exit 0 at their exact declared needle: today-17 # pass 682, measure-hermetic 11, s4-real-day 15, a0-journeys 23, s6-sup-* 4/3/3/3/3, d-plan-edit 68, m4-import 62, m4-import-production 25, d-import-retract 13, d-admission-swap 4, d-replay-measure 9, d-replay-all 28, b-lom 30, d-capture-start 14, food-live-save 6, w7-import 35, w6-host-seams 9, w6-local-source 25
A1 TODAY BUILD PASS: 3 assets; 140 pinned inputs (15 engine, 12 client); build earned-8993e0805f69; Today boot graph 122 modules, carrying no migrate.cjs, no merge.cjs and none of the m4/import lane
A5 PWA BUILD PASS: 13 files in .tmp\slice-pwa-dist; 11 precached and pinned by sha256   EXIT=0 | rig187 => PASS  EXIT=0
COACH C5 tests 231 / pass 231 / fail 0 (engine-revision.test.cjs green: the standing id is still S5, and no rebuild/coach byte moves this round)
lanes/d/p3-replay-all/*.test.mjs tests 28 / pass 28 / fail 0   |   lanes/b/tooling/test/pinned-unchanged-... tests 16 / pass 16 / fail 0 (F10 red before the widening: 15/1)
b-lom pair tests 30 / pass 30 / fail 0   |   m4/import/test/production-admission.test.mjs tests 8 / pass 8 / fail 0 (mutated qualify(): 7/1, P3-P6 alone)
w7-preview/import tests 35 / pass 35 / fail 0   |   today/test/package + design + child-diagnostic-tail tests 33 / pass 33 / fail 0
```

## R8. Open items for the PM's --full

- The 10 gates that re-execute under `--full`, the private oracle and the exact-artifact acceptance are unchanged by this round; `AUDIT RED-FIRST FAIL` is still the `:485` observation and still the baseline for a package with no D-id.
- `m4-import-s3` remains undeclared: 3 pass / 14 fail here AND on the clean tip, the `IMPORT_M4_DIR` class of `:459` NOTE 3. Named absent in rebuild.yml's own comment.
- `executedClosure` still cannot see a COMPUTED specifier. Lane B tooling ticket after S6, per `:493 (3)`; F10 asserts the bound so the gap cannot quietly close or quietly widen.
- DASHES: exactly THREE added lines in the whole round 2 diff carry one, and none is a product string - the two new rebuild.yml step NAMES (written that way because all twelve step names already in that file use it) and the PM's own `:493` ledger line, which may not be edited. Zero CRLF in every touched file.
- The `:456` trip-wire is still not armed: the standing rebuild.yml step is `--package S5`, so the coach constant moves ONCE, at the PM's ff-merge, in `:465-467`'s order.

# ROUND 3 (the lane D P2-W1 full-scope fix)

- Merged `rebuild/d-p2w1-scope` 67e0130 at `e5e5fba`, no rebase; what it brings over `a32e606e` is exactly two paths, the cell and lane D's 29-line report. The review `f49b370` (VERDICT ACCEPT, no stops) is carried onto this branch at `ef5b8a8`.
- The cell now reads line 4 of the oracle by RULE - same N on both sides of each fraction, frozen = unfrozen by backreference, N >= 7, `scope FULL` captured when FULL prints it - so the PM's `--full` run reads GREEN without the PUBLIC count standing in the pattern. The guard is not weakened: a red gate, a smaller class count and a mismatched frozen/unfrozen count each still fail, and the new sibling cell executes that red side.
- Re-measured in `packages/S6.json`: `rebuild/m3/w6/test/local-source-consumer.test.mjs` post `856b3627665b016e...`, and the `w6-local-source` needle `# pass 25` becomes `# pass 26`.
- Its role is `new`, NOT `edited`: this file is not a parent pin (S5 pins neither w6 local-source cell, which is why both stood `pinned-unchanged`), and the runner refuses `edited` there by name - measured, `UNLISTED-PRODUCT-DRIFT ... is not parent-pinned and is not declared new`. `new` over a REAL pre-image is the same shape round 2's both-sides files carry. Disclosed side effect: the no-register obligation line now reads 12 of 12 children executing one of this package's own role `new` files, up from 11.
- Child tail, `TZ=America/New_York MEASURED_TEST_NOW=2026-09-03`: `# tests 26 # pass 26 # fail 0`.
- `--ci --package S6` prints `PUBLIC CI EVIDENCE PASS`, exit 0, with every ledger line resolved on the chain tip `656473bd`.
- `today-17` FLAKED once mid-round (681 of 682, caught by the runner's own CI-TODAY-CHILD-FLAKE diagnostic tail, whose last 60 lines do not carry the failing name). Re-run on its own argv it is 682/682, and it is OBSERVED exit 0 in the passing `--ci` run. Not this round's doing; the flake itself is not diagnosed here.
- Hygiene: LF only, no en or em dash added, tree clean, no engine byte and no private path touched.

## Artifact proposed

- `rebuild/m4/spec/acceptance-s6-today-child.json` now exists: sha256 `be87e8b870647d542cb817dea51868a20c9ba14ea840e7d97a2d34fda9115bf0`, 80638 bytes, LF only, `JSON.stringify(x, null, 2) + '\n'`, the exact byte shape `acceptance-s5-today-child.json` carries.
- It came from the runner's OWN `proposed(s, bound)` and was never hand written: a scratch script outside the repo compiled `b-package.cjs` up to the `8. main sequence` delimiter exactly as `tooling/test/execution-targets.test.cjs:43` does, then called the runner's own `spec()`, `parent()` and `proposed()` in the main sequence's own order. No product, engine, coach or spec byte moved this round.
- Shape vs S5: the 21 closed `ARTIFACT_KEYS` stand in the same order as S5's; `status` is `BRIEF-ACCEPTED` with its evidence resolved on the chain branch, theme `DECISIONS:488` `a824d88d27cf`, brief acceptance `DECISIONS:489` `6044b121f506`, supersession ruling `DECISIONS:490` `f2e3fe7412f2`; `spec.sha256` and `executionPins["rebuild/lanes/b/tooling/packages/S6.json"]` are both `bad4cdcba68e`, `runner.sha256` `8d9a94c20faa`, parent `S5` `84e3430ddd85`; the same nine byte-identity gates S5 retired stand in `coverage.superseded`, with `covered` 0, `run` 10, 23 children, 196 product files and 62 execution pins.
- Proved to BE `proposed()`, not merely written by it: with a scratch `review-s6-today-child.json` (`PENDING`, deleted again, never committed) the unmodified runner gets past `SEALED-PROFILE-RECOMPUTATION` and prints `B PACKAGE S6 ENVELOPE PENDING artifact=be87e8b870647d54... spec=bad4cdcba68e1d4e... runner=8d9a94c20faa2e85...; independent exact-artifact acceptance required`, then `PUBLIC CI EVIDENCE PASS` exit 0 with all 23 children OBSERVED exit 0.
- With that scratch review deleted, `git status` carries the artifact and this report and nothing else; LF only, no en or em dash added, no private path opened.
- CI-step fix. The PM flipped the standing CI step `--package S5` -> `--package S6` in `.github/workflows/rebuild.yml` AFTER the seal, at `cdff26a1`, and `rebuild.yml` is a declared product file, so the byte-identity step refused `WORKTREE-SOURCE-PIN`. The flip is kept and `packages/S6.json`'s post for that file is re-measured to the disk bytes, `675024cba223` -> `8f1d5ba2b087`, role still `edited`; `cdff26a1` moved exactly one other file, `rebuild/coach/engine-revision.cjs`, which is in NO product map here and is reverted to the S5 literal until the new receipt exists.
- Artifact re-proposed through the runner's own `proposed()` on the re-measured spec `18583855755d`: `rebuild/m4/spec/acceptance-s6-today-child.json` is now sha256 `ad957db3ecb2e0e9fcfde96f80b47a18e551076f46200ce5a798b2917904ca9a`, 80638 bytes, and against a scratch PENDING review (deleted again, never committed) the unmodified runner prints `ENVELOPE PENDING artifact=ad957db3ecb2e0e9...`, past `SEALED-PROFILE-RECOMPUTATION`.
- THE RULE FOR EVERY SUCCESSOR, and S5 already had it right at `657b847`: the standing CI step flips INSIDE the package, before the seal, so the post-image the spec measures is the flipped byte. Flipping it after the seal moves a declared product file out from under its own pin and costs a re-propose.
- STOP, not fixed here: `receipts/S6.json` is removed with `review-s6-today-child.json` and `VERDICT-S6.md` (none of the three is product-declared), but the standing step now reads `--package S6`, so the DECLARED child `m4-import-production` reads that same receipt and is 24/25 against its `# pass 25` needle (`P3-M3 ... ENOENT receipts/S6.json`); `--ci --package S6` therefore ends `FAIL CHILD-REQUIRED-EXIT-ZERO`, and `rebuild/coach/test/engine-revision.test.cjs` is RED on the same file, 1 pass / 2 fail. Both go green when the PM writes the new receipt.
- Round 5, merge: lane D's P3-P6 seal-window fix `40cdff82` came in `--no-ff` at `6533858` and its review `rebuild/lanes/d/P3-P6-SEAL-WINDOW-REVIEW-R1.md` (VERDICT: ACCEPT) landed at `5a05e66`. Bar row 23 no longer carries either revision literal: `production-admission.test.mjs` now computes `standingSeal(REPO)` from the receipt, `packages/rebuild.yml` and the coach constant, so the cell is green in the window state AND in the sealed state the literal could never reach.
- Re-measured: `packages/S6.json` post for `rebuild/m4/import/test/production-admission.test.mjs` moves `766211fc7cc2` -> `142eb51849ba`, role still `new`; nothing else in the product map moved. The declared child needle `m4-import-production` STAYS `# pass 28` because no cell was added or removed: the pair measures tests 28, pass 28, fail 0 under `TZ=America/New_York` and `MEASURED_TEST_NOW=2026-09-03`.
- Re-proposed through the runner's own `proposed()` on spec `2415c473b090`: `rebuild/m4/spec/acceptance-s6-today-child.json` is sha256 `0e52357ed62249d4ee94b473e2dde20220b0a82c3737414bba0603fa76bf040f`, 80625 bytes, amended into the single spec commit `44a46e9`. Against a scratch `review-s6-today-child.json` (`PENDING`, deleted again, never committed) the unmodified runner prints `B PACKAGE S6 ENVELOPE PENDING artifact=0e52357ed62249d4... spec=2415c473b090e808... runner=8d9a94c20faa2e85...`, then `PUBLIC CI EVIDENCE PASS` exit 0 with all 23 declared children OBSERVED exit 0.
- Tails: `rebuild/coach/test/*.test.cjs` is green at tests 234 / pass 234 / fail 0 exit 0, `engine-revision.test.cjs` included, so the coach half of the standing STOP is closed and only the PM's new `receipts/S6.json` is still owed. With the scratch review deleted `git status` is empty; LF only, no en or em dash added, `rebuild/engine/**` untouched, no guard weakened, no npm run, junctions only, and the private census was never opened or named.
