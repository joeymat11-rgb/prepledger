# M2-S6-TODAY-CHILD - author report, round 1

Branch `rebuild/b-s6-today-child`. Base `0635f4b6`; the tip moved to `2a269bd8` mid-round (DECISIONS:491-492 only, B-LOM accepted and held for S6) and is MERGED in, not rebased - stop 7. Brief of record measured on the branch: sha256 `8a9a63ee...`, 41900 bytes, the `:489` values exactly. The citation method was validated on S5's own three FIRST - line 460 -> `00f955e7`, 461 -> `2e84f849`, 462 -> `f9de6a7b`, each matching the field `S5.json` was sealed with - and only then used on S6's 488 / 489 / 490.

## 1. The merges and every conflict

`c-s6-small 6fad3472` clean; `b-s6-child-tail d1001da9` clean; `c-p3-import-ui-2 4c3a5474` conflicted in FIVE files, every one among the seven `:484` predicted. All resolved by carrying BOTH sides:

1. `today-entry.mjs` - `setupFirst` and `installation: hosts` are orthogonal options on one `mountToday` call.
2. `today-app.cjs` - the build footer and the Import entry both append to the Today root; the footer's contract is that it is LAST, so the entry goes first and the footer keeps its place.
3. `today/build.mjs` - `commitOf`/`injectCommit` and `assertImportRouteIsolation` are independent exports; the result object and the A1 PASS line carry both field sets.
4. `import/test/page-bundle.test.mjs` - UI-2 does not drift off the old input law, it RULES it false (`:475 (1)`), so its rule supersedes the tip's `assert.throws`; but the inventory is the post-merge tree's, so the m4/import list is the union of TEN and the graph figure is RE-MEASURED as 133 + 1 (F7) + 2 (F8, RV-G4 router) + 4 (the route's own) = 140, not summed from 136 and 138, neither of which had seen the other.
5. `w6/test/local-today-journey.test.mjs` - PAGE_PINS. Both branches pinned today-entry.mjs at their own post, so neither sha is true here; re-measured over the post-merge bytes.

Two more cells were green on each branch alone and false together, corrected the same way: ROUTE_MODULES 16 -> 18 and the P3-B5 delta 16 -> 18 (boot count still 121).

## 2. Declaration: brief versus tree

Every root reconciles EXACTLY (`git ls-tree -r` on the post-merge head, `Object.hasOwn` against all five specs): w6/local 13/1, w6/host 13/5, w6/test 83/1, m4/import 30/0, m4/workout 3/0, lanes/d 8+2+4+1+1+1, w7/import 9/0, measure 15/15, today 57/15. All 89 exist; none is already S5-declared. No count differs from the brief.

**Only 66 of the 89 are declared - stop 1.** The other 23 are unchanged here AND executed by no declared child, and the runner's closed vocabulary has no honest role for that: `new` means a file that did not exist or one this package moves, and `pinned-unchanged` is refused by `PRODUCT-PINNED-UNCHANGED-IS-NOT-EXECUTED-BY-A-DECLARED-CHILD`. Inventing a child to make a pin legal would be gaming the rule.

`packages/S6.json` declares **187** = 114 S5 pins re-asserted + 66 of the 89 + this package's own six cells + `packages/S5.json`. By role: 96 `carried`, 18 `edited` (including `rebuild.yml`), 17 `new`, 54 `pinned-unchanged`, 2 `superseded-by-child` over parent EXECUTION pins - `packages/S5.json` and, measured not assumed, `today/test/copy.test.mjs`, unpinned only in the parent's PRODUCT map while the parent EXECUTES it (`:109` gives that case its own role).

## 3. The flake: cause, reproduction, fix

**Cause, measured rather than described.** `settle` was a FIXED 24-turn macrotask drain with no condition. Instrumented on an idle PC the trial table needs **18 of those 24** turns after the last waist reading (measure screen 10, marker-pick close 8): a margin of SIX TURNS on the machine where it passes. A runner that advances the page less per turn crosses it, the drain returns anyway, and the next line dereferences null or reads `no trial table rendered` - both reported symptoms, from one line.

**Reproduced red on purpose.** Machine starvation does NOT reproduce it - 24 spinners, `UV_THREADPOOL_SIZE=1`, and a 6 ms-per-turn main-loop hog all stayed green at 73-91 s, because slowing the machine slows the drain too and so gives the page MORE wall time. The starvation that matches the mechanism is the budget itself: with the pre-fix `settle(rounds)` cut to 12, cell (a) goes RED with `the markers pick screen is not offered`; at 16 it passes, so the threshold sits between 12 and 16 against a shipped 24.

**Fixed at the cause; the same starvation is green.** The wait is now the page's own condition with a bounded deadline that fails LOUDLY naming the slot, the elapsed ms and the turns. `page()` awaits `api.ready` - today-app.cjs publishes it for exactly this - then the named screen; `go(slot)` waits for its control, clicks, waits for the screen to CHANGE, then for the terminal state the caller names; `typeWaist` waits for the table to repaint AND then to stop repainting; `pickMarkersOnScreen` waits for the pick screen to close; `close()` waits for the page to be still before tearing the lane down. Under the identical starvation the fixed code is GREEN - the knob is inert, because there is no budget left to starve. The red side is executed too: with the deadline cut to 40 ms it fails `MEASURE-SETTLE-DEADLINE: waited 40 ms over 4 macrotask turns for the Today screen to stop changing after mountToday`, instead of a TypeError three frames away.

Two further faces surfaced only once the condition was explicit, and both are fixed: the trial table repaints in STAGES, so "it changed" was true while the last week still read `Not enough data yet`; and tearing the lane down mid-repaint made the next paint call `markers()` on a closed client (`LOCAL_CLIENT_CLOSED`, cell (e)). `trialDevice()` is now per cell - the reason the flake always took (a) and (d) TOGETHER. The file costs 147 s instead of 53 s, three real devices instead of one; that is the honest price. `import/test/support.mjs` was checked as the brief asks: it has no drain at all and awaits real promises, so it does not carry this race.

## 4. Files and hunks

`b-package.cjs` CHILD_ROOTS +9 (the brief's eight plus `m3/w6/test/`, the author's correction - the brief declares three files there and says the W6 whole-tree suite runs them, and rebuild.yml has no such step); PUBLIC_TAIL_ROOTS +2; IDS and NO_REGISTER_IDS gain `S6`, lose `B-LOM`; new guarded `RETIRED_IDS` (stop 6). `pinned-unchanged-and-ruled-substitutions.test.cjs` F6 / F7 (all 17 roots, first eight re-asserted unchanged) / F8 (two added, six withheld named) + the RETIRED_IDS pin. CHILD_SPECS `'S6'` in `boundary:83`, `food:792`, `machine-settings-ui:709`, `setup:2315`; `problem.test.mjs:1583` lifted onto a module-level `CHILD_SPECS` iterated youngest-first with its rule stated - it never gained `'S5'` and stood TWO generations stale, and c-s6-small moves today-entry.mjs, which that cell hard-pins two ways. `measure/test/{support,journey}` the flake fix (S5-sealed: a disclosed sealed move). `rebuild.yml` A0 gains food-live-save and host-seams; four new steps give the Import screen, the m4/import lane, the lane D suites and the two w6 cells a CI home; the standing `--package S5` step is UNTOUCHED. `packages/{H3,S3,S4,S5}.json` runnerSha256 re-pinned (S5's own product pin for the runner deliberately NOT re-targeted - S5 did not produce these bytes); `packages/B-LOM.json` DELETED with its id (`:487` stop 2). New: five `s6-supersede-*.test.cjs` and `s6-engine-files-differential.cjs`, in S5's shape.

## 5. Bar rows executed, and the suites

Green and executed: 13, 14, 15, 18, 20, 21, 24 (stated in advance), 26, 27, 28, 30, 31, plus S5's twelve carried. Row 22 executed in the adjusted form (stop 4). **Row 23 PASSES against the S5 constant**: `MAPPING_ID` and `ENGINE_REVISION` are only CONSTRUCTED, tested against the same live constant, and DISPLAYED; a scan of every tracked `.cjs/.mjs/.js/.json` finds NO comparison of a stored mapping id or engine_revision against a current one, and `local-source-profile.cjs`'s `qualify()` reads neither - so the rotation cannot invalidate an admitted import. Re-run in round 2 against the S6 constant before the coach line moves. Rows 16, 17, 19 are NOT met (stop 1). Row 29's 20 windows-latest runs cannot be run from here; the measured margin, the deterministic red and the green under the same starvation stand in their place.

`--ci --package S6`: **PUBLIC CI EVIDENCE PASS, exit 0**. All 21 children OBSERVED exit 0 at their exact declared needles; PRODUCT IMPLEMENTED (37 post / 0 pre / 96 carried / 54 pinned-unchanged / 0 unlisted drift, covering all 114 parent pins); FIDELITY and AUTHORITY OBSERVED (both S6 lines resolved on the chain branch); LAWS 45/45 executed, ending `AUDIT RED-FIRST FAIL` - the `:485` observation, unchanged by S6 and the baseline for a package with no D-id (bar row 25); 9 gates SUPERSEDED under DECISIONS:490.

Needles, measured on the post-merge head and none copied: today-17 `# pass 681`, measure-hermetic 11, s4-real-day 15, a0-journeys 23, s6-sup-* 4/3/3/3/3, d-plan-edit 68, m4-import 62, m4-import-production 24, d-import-retract 13, d-admission-swap 4, d-replay-measure 9, d-capture-start 14, food-live-save 6, w7-import 35, w6-host-seams 9, w6-local-source 25, and `ENGINE FILES DIFFERENTIAL: 27 tracked ... 18 named and NOT ONE moves, so all 45 tracked rebuild/engine file(s) stand byte-identical to the parent's own post` - bar row 26, 45 tracked, 0 moved.

`--ci --package S5`: RED, **`SEALED-PROFILE-RECOMPUTATION`, exit 1**, reached only after SPEC OBSERVED and PARENT BOUND print - so the re-pin worked and S5 stops exactly where H3, S3 and S4 stop under a child. No refusal token is changed and no coverage is lost. Tip control in a second worktree at the clean tip, because two reds had to be adjudicated rather than assumed: `s3/harness.test.mjs` 3/14 HERE and 3/14 THERE; `p3-replay-all` 26/0 THERE and 25/1 HERE.

## 6. Stops

1. **23 of the 89 cannot be declared** (section 2): build/dev-host glue `w6/local/{build,source-commit}.mjs`, `w6/host/{plan-edit-host,build-host,esbuild-probe}.mjs`, `w6/host/index.html` (HTML - nothing can execute it at all); harnesses reached only by spawn or a constructed path (`m4/import/test/{mutations,reading-replay-faults}.cjs`, the four `test/s3` files and its `.json` fixture, `lanes/d/plan-edit/{model-mutants,host-mutants,astra-rerun}.mjs`, `w6/test/local-source-consumer-browser.mjs`); and those whose only child is red for a cause this package did not make (`m4/import/replay-registry.cjs`, the four `p3-replay-all` cells). PM: widen "executed", add a role, or accept the shorter list.
2. **`d-replay-all` is a BLOCKING regression this merge makes.** 26/26 green on the clean tip, 25/1 here. P3-EN1 finds three writers in the shipped page with no replay family and no declared reason: `body-composition-class.cjs#body-composition-source`, `measure-replay.cjs#body-composition-source`, `sleep-replay.cjs#sleep`. Structural, and exactly what the custody hole hid: UI-2 puts the Import route INTO the shipped page graph, so modules that were route-only when P3-REPLAY-ALL-FAMILIES measured its register are now, by that cell's own definition, writers the page has. Register gains three declared reasons, or the enumeration excludes the lazy Import route - lane D's ruling. This package will not edit another lane's guard to go green.
3. **`m4-import-s3` is red for an environment, not for this branch**: 3/14 here and 3/14 on the clean tip, the same `Actual owned dispatcher scratch required` - the IMPORT_M4_DIR class of `:459` NOTE 3. Not declared a child; named absent in rebuild.yml's own comment.
4. **PUBLIC_TAIL_ROOTS: the brief says three, `:487` says two.** The narrower reading is taken and the disagreement recorded, with a measured fact that cuts the same way - `page-bundle.test.mjs` under the third root carries the literal `ledger/` in its own FORBIDDEN list, so TAIL_DENYLIST's content gate would withhold that child's tail on any failure that prints the list anyway.
5. **UI-2's boundary named-drift set could not have passed on any tree.** It named FOUR files as the sealed drift under today/; S4 pins only two. preview.css and build.mjs are among the seven S4 left unpinned, so they can never appear in a drift-from-S4 set. The cell's own note says why nobody noticed: on the UI-2 branch the assertion above it was red, so execution never reached the list. Corrected TIGHTER, not looser - the sealed set is still exact, and the two unsealed movers are asserted to be unsealed AND declared.
6. **`RETIRED_IDS` is a new runner constant and needs review.** Removing an id and deleting its spec is one act (`:487` stop 2), but TOOLING_FILES is derived from IDS, so the deleted path stops being named there while git still reports it - the runner would refuse `UNLISTED-SOURCE-CHANGE` for a deletion it made necessary. The product map cannot say it: `post: null` reads as "not written yet" and puts the package in PARTIAL. Fixed in the runner, literal, guarded (a retired id must not be runnable and holds no exemption) and pinned by F6.
7. **The tip must be MERGED, never rebased.** A rebase was attempted once when the tip moved: it replayed all 24 commits, linearised the three frozen merges and re-created every c-p3-import-ui-2 conflict, rewriting the frozen branches' own commits. Aborted and merged instead. That is `:467` note 1's mechanical reason, and it holds for every future round of this branch.

## 7. Open items

- **B-LOM is not merged this round**, per the ticket. It is now ACCEPTED and held for S6 (`:492`, `rebuild/d-b-lom` 8e03401d at da7d5b37). Round 2 merges it LAST and re-measures every post after it. For round 2: `:491`'s figures (A1 122 pinned inputs, Import graph 136 -> 137) were computed against a base without UI-2's four route modules, and this round measures that graph at 140 - so round 2 re-measures rather than adds.
- The `:456` trip-wire is NOT armed by this branch: the standing rebuild.yml step is still `--package S5`, so `engine-revision.test.cjs` stays green and C5 is not red yet. That is the PM's flip at the ff-merge, in `:465-467`'s order, and the constant moves ONCE.
- `MEASURE_SETTLE_DEADLINE_MS` and an env-gated `SETTLE-TRACE` line are left in the measure support on purpose: the first is how the red side of the new wait is executed, the second is how the next occurrence is diagnosed instead of guessed at. Both are off by default.
