# M2-S6-TODAY-CHILD - the reseal that closes the custody hole

**STATUS: READY FOR ACCEPTANCE BY NAME. FINAL LIST WAITS ON: B-LOM (placeholder row only, section 6).** Lane B, ENGINE-TIER PACKAGE PROCESS, child of `M2-S5-TODAY-CHILD` (`rebuild/lanes/b/tooling/b-package.cjs`), size M. Parent artifact `rebuild/m4/spec/acceptance-s5-today-child.json` sha256 `84e3430ddd851965d630cecffe4bf7ead88ec10ed53635c503671fd616c1169e` (receipt `DECISIONS:466`, merge `:467`). Walked on tip `07f1e4ba` (`DECISIONS` 486) plus `origin/rebuild/c-s6-small` `6fad3472`, `origin/rebuild/b-s6-child-tail` `d1001da9` and `origin/rebuild/c-p3-import-ui-2` `4c3a5474`; every count below is measured against all five specs, not carried from a report.

## 1. Why

S6 has two jobs and no behaviour of its own. If a reviewer finds a behaviour change here, that is a finding.

**(1) Reseal `today/**`.** `DECISIONS:455` stands: S4 sealed the declared product under `rebuild/m3/w7-preview/today/**` and S5 re-pinned it, so any byte change there fails `SEALED-PROFILE-RECOMPUTATION` on its own branch. Three accepted branches wait on this package, and a fourth (B-LOM) is folded into it by `:486` (c).

**(2) Close the `DECISIONS:473` custody hole.** The runner recomputes only what a spec DECLARES, so every lane D and lane C module merged since S5 was sealed is invisible to it: `--ci --package S5` printed EXIT 0 at `:473`, `:477`, `:478`, `:481`, `:485` and `:486` not because nothing moved but because nothing that moved was declared. The PM ruled that a hole by design of the runner is not a licence, and ruled further (round 1, stop 7) that **a `lanes/d` test file a declared child EXECUTES is itself declared product** - a child without a declared test is the same hole wearing a different name. `:485` records the hole's first live casualty: FOOD-LIVE-SAVE fixed a defect that refused **every food day and machine note written on the shipped build since 2026-09-15**, and it merged on its own CI because the two files it moves are in no package at all.

## 2. The declaration list, walked

Method the author repeats rather than trusts: `git ls-tree -r` per root on the tip, plus `git diff --name-status` of each accepted branch against its own base; each path tested with `Object.hasOwn(product, path)` against `B-NTC`, `H3`, `S3`, `S4`, `S5`; `git log --diff-filter=A` for the landing date. Roles below are product / test / pinned-unchanged; the spec's own `role` is `new` for a first declaration, `carried` for a re-pin with `pre === post`, `edited` where a byte moves.

**Counts per root (tracked after the three frozen branches land / S5 declares / S6 adds), B-LOM excluded.** `m3/w6/local/` 13 / 1 / **12**. `m3/w6/host/` 13 / 5 / **8**. `m3/w6/test/` 83 / 1 / **3**. `m4/import/` 30 / 0 / **30**. `m4/workout/` (named files only) 3 / 0 / **3**. `lanes/d/plan-edit/` 8 / 0 / **8**. `lanes/d/p3-replay-measure/` 2 / 0 / **2**. `lanes/d/p3-replay-all/` 4 / 0 / **4**. `lanes/d/p3-capture-start/` 1 / 0 / **1**. `lanes/d/import-retract/` 1 / 0 / **1**. `lanes/d/p3-followons/` 1 / 0 / **1**. `m3/w7-preview/import/` 9 / 0 / **9**. `m3/w7-preview/measure/` 15 / 15 / 0. `m3/w7-preview/today/` 57 / 15 / **7**. **That is 89 new declarations, plus the 114 S5 pins re-asserted.** B-LOM adds two or three more and moves two already-declared files; section 6.

**2.1 `m3/w6/local/` - 12, role product, all `new`.** `today-bindings.mjs` is the only one S5 declares (`carried`), and that single declaration is the whole of `:473`'s complaint. Named by `:476`/`:478`: `source-admission.mjs`, `import-bundle.mjs`, `local-client.mjs`, `browser-entry.mjs`. Their eight siblings, undeclared on the same rule: `build.mjs`, `host-bindings.mjs`, `host-browser-entry.mjs`, `local-era.mjs`, `local-keys.mjs`, `source-commit.mjs`, `source-platform.mjs`, `today-browser-entry.mjs`. **`source-admission.mjs` is the most-moved file in this package**: hunks from `:477` (retract), `:478` (Route 1), `:481` (F7), `d-p3-replay-all` (4) and `:486` (F3, the identity answer), and one more from B-LOM. Its post is measured last.

**2.2 `m3/w6/host/` - 8 to add, 5 standing.** S5 already declares `engine-runtime-host.cjs`, `workout-host.mjs`, `test/journey.test.mjs`, `test/engine-equivalence.test.cjs`, `test/local-real-day.test.mjs`. **`engine-runtime-host.cjs` is ALREADY declared** - the S6 intake lists it as a hole and it is not one; it is a `carried` re-pin. Add role product: `plan-edit-host.mjs` (the third `:473` runtime file), `build-host.mjs`, `esbuild-probe.mjs`, `host-entry.mjs`, `index.html`. Add role test: `test/host-seams.test.mjs`, `test/journey-fixture.cjs`, and `test/food-live-save.test.mjs` (new at `5c231e4a`, and its root is already a `CHILD_ROOT`, so it becomes a child rather than a workflow line).

**2.3 `m3/w6/test/` - 3 of 83, and the restraint is deliberate.** LOCAL-CAPTURE-START-RESUME moves `local-source-admission.test.mjs`, `local-source-consumer.test.mjs` and `local-source-consumer-browser.mjs`; S5 declares only `local-today-journey.test.mjs`. S6 takes the three that moved and leaves the other 79. That is the same shape as the `today/` tree and it belongs to the same ticket (2.6).

**2.4 `m4/import/` - 30, none declared.** Role product, 13: `production-mapping.cjs`, `measure-replay.cjs` (`:477`/`:481`), `sleep-replay.cjs`, `body-composition-class.cjs`, `replay-registry.cjs` (P3-REPLAY-ALL-FAMILIES; `replay-registry.cjs` moved again at `:486`), `replay-core.cjs`, `browser-replay.mjs`, `engine-provider.cjs`, `local-source-order.cjs`, `local-source-profile.cjs`, `daily-history.cjs`, `prepare.cjs`, `reading-replay.cjs` (the last three 09-09, pre-dating the chain; see 2.7). Role test, 17 under `test/`: `production-mapping.test.cjs`, `production-admission.test.mjs`, `browser-parity.test.mjs`, `engine-provider.test.cjs`, `local-source-order.test.cjs`, `prepare.test.cjs`, `reading-replay.test.cjs`, harnesses `mutations.cjs` and `reading-replay-faults.cjs`, and the eight-file `test/s3/` harness (`browser-entry.mjs`, `current-head.cjs`, `engine.cjs`, `fixtures.mjs`, `harness.test.mjs`, `mutations.cjs`, `run.mjs`, `s3-portable-sources.json`).

**2.5 The rest.** `m4/workout/` role product, 3: `plan-edit-commands.cjs`, `plan-edit-model.cjs` (`:473`) and **`engine-order.cjs`**, undeclared today and moved by B-LOM in two hunks, so S6 declares it whether or not B-LOM lands. `lanes/d/plan-edit/` 8, role test: `model.test.cjs`, `durable-host.test.mjs`, `browser-build.test.mjs`, `client-p6.test.cjs`, harnesses `model-mutants.cjs`, `host-mutants.mjs`, `astra-rerun.mjs`; plus `f2-tag-adapter.cjs` role **pinned-unchanged** (`:473`: a byte-identical copy of the published F2 adapter, imported by no runtime file, held only by a cell). Role test under the stop-7 ruling: `lanes/d/p3-replay-measure/{measure-family,measure-order}.test.mjs`, `lanes/d/p3-replay-all/{class-membership,sleep-family,writer-enumeration,writer-order}.test.mjs` (`writer-order` moved again at `:486`), `lanes/d/p3-capture-start/capture-start.test.mjs` (new at `ff099d14`), `lanes/d/import-retract/retract.test.mjs`, `lanes/d/p3-followons/admission-swap.test.mjs`.

**`m3/w7-preview/import/` - 9, the Import screen, all `new`.** Role product: `import-screen.mjs`. Role test: `test/{route,refusal-route,page-bundle,live-clock,refusals}.test.mjs`, `test/support.mjs`, `test/edge-seed-entry.mjs`, `test/edge-route.mjs`. `support.mjs` is moved by both `c-p3-import-ui-2` and `:486`; `page-bundle.test.mjs` by `c-p3-import-ui-2` and `d-p3-replay-all`.

**`today/` unpinned, 7:** `build.mjs`, `gym-app.mjs`, `problem-report.cjs` (`c-s6-small`), `preview.css`, `test/copy.test.mjs` (`c-p3-import-ui-2`), and **`food-host.mjs`, `machine-settings-host.mjs`** (FOOD-LIVE-SAVE at `5c231e4a`). `build.mjs` is moved by two branches.

**Two relayed facts corrected by measurement, and the author must not restate them.** (i) `P3-IMPORT-UI-2-REVIEW-R4.md`'s drift paragraph says "Drift 17 files ..., SIX in S5.json" and names `today/test/copy.test.cjs`. The file is `today/test/copy.test.mjs` and `Object.hasOwn` is FALSE for it in all five specs: the UI-2 drift is **21 paths, 15 non-document, FIVE of them S5-declared** (`today-app.cjs`, `today-entry.mjs`, `w6/test/local-today-journey.test.mjs`, `today/test/package.test.cjs`, `measure/test/boundary.test.mjs`). `copy.test.mjs` is a NEW declaration, not a sealed-byte move, and the difference decides whether its red on the branch is expected. (ii) That same paragraph omits `today/build.mjs` and `today/preview.css`, which the diff shows modified; both are undeclared and both are S6's to declare.

**2.6 NOT taken, named so the omission is a decision.** 35 further undeclared files under `m3/w7-preview/today/` (`setup-*`, `sleep-*`, `checkin-*`, `gym-model.mjs`, `design.cjs`, the two shells, `test/catalogue.test.mjs`) and 79 of the 83 under `m3/w6/test/`. `:455`'s phrase "sealed all product files under today/**" is true of the 15 S4 and S5 declare and of no more; `:485` shows what that costs. TODAY-TREE-CUSTODY (`:482`) is the ticket, and this brief asks the PM to **widen it to name `m3/w6/test/` as well**, because the pattern is now measured in two directories, not one.

**2.7 Unsure - the author resolves each before the spec is written.**
1. `w6/host/index.html`, `w6/host/{build-host,esbuild-probe,host-entry}.mjs`, `w6/local/{build,host-browser-entry,today-browser-entry}.mjs`: build and dev-host glue, not code the phone runs. Product, or tooling outside the product map? I read them as product (they decide what the phone gets) but did not measure reachability.
2. `m4/import/{daily-history,prepare,reading-replay}.cjs` and their four test helpers (09-09, before the chain): still live, or M4 legacy nothing imports? If legacy, role pinned-unchanged, not product.
3. `m4/import/test/s3/` (8 files, one a `.json` fixture): declared whole, or only `harness.test.mjs` and the fixture? `:486` records the S3 portable manifest carrying 6 stale pins, which argues for declaring the fixture by name.
4. `w6/local/local-keys.mjs`, `local-era.mjs`: not opened here. Declaring is byte-pinning and carries no value, but the author must confirm no path this spec declares resolves into `ledger/`, `rebuild/conform/private` or `src/history.js` before the spec is written. A pinned path is printed in CI.
5. `lanes/d/plan-edit/astra-rerun.mjs` and `import/test/edge-seed-entry.mjs`: harnesses (test), or one-off scripts that should not be pinned at all. `edge-seed-entry.mjs` is only reachable from `edge-route.mjs`, which no runner executes.
6. RESOLVED at round 1: `lanes/d/**` test files a declared child executes ARE product. The six lane D directories enter the product map on that ruling.

**2.8 One measured hazard, now a required bar row (row 23).** `m4/import/production-mapping.cjs:199` is `const MAPPING_ID = 'earned/import/production-producer-mapping/' + ENGINE_REVISION;`, and `ENGINE_REVISION` is `M2-S5-TODAY-CHILD@0df73b01f3d2d935`. The reseal rotates it. The mapping id is not a comment: it is a field of the mapping the registry qualifies against, and `:480` records the Import screen PRINTING it to the athlete ("Checked against engine revision ..."). **Before the coach constant moves**, the author proves with a cell that an import admitted under `M2-S5-TODAY-CHILD@0df73b01f3d2d935` remains ADMITTED, ADOPTED and RETRACTABLE once the constant becomes the S6 revision. If it does not, **the author STOPS and writes the finding before the constant moves**; a reseal that can invalidate a history Joe has already imported is BLOCKING against the reseal, not against the mapping.

## 3. Children

The ten S5 children are re-pinned by name with needles RECOMPUTED on the post-merge head, never copied (`today-17` was `# pass 666`; `c-p3-import-ui-2` already measures 667/662/5 with the five sealed-byte declaration reds): `today-17`, `measure-hermetic`, `s4-real-day`, `a0-journeys`, five `s5-sup-*`, `engine-files-differential`. The `s5-sup-*` files keep their names: a package does not rename its parent's evidence. S6's own six supersession children are `s6-sup-*`, new files in S5's shape (section 9's closing note).

New children, for suites that today run in NO workflow. Measured: `rebuild.yml` has exactly 12 `run:` steps and `shared-preflight.yml` one, and none names `rebuild/m4/import`, `rebuild/lanes/d/` or `rebuild/m3/w7-preview/import`.

| child | argv | why |
| --- | --- | --- |
| `d-plan-edit` | `lanes/d/plan-edit/{model.test.cjs,durable-host.test.mjs,browser-build.test.mjs,client-p6.test.cjs}` | `:473`'s 68/68 |
| `m4-import` | `m4/import/test/{prepare,reading-replay,engine-provider,local-source-order}.test.cjs`, `browser-parity.test.mjs` | the standing import lane |
| `m4-import-production` | `m4/import/test/{production-mapping.test.cjs,production-admission.test.mjs}` | `:477`, `:478` |
| `m4-import-s3` | `m4/import/test/s3/harness.test.mjs` | check `IMPORT_M4_DIR` first (`:459`) |
| `d-import-retract` | `lanes/d/import-retract/retract.test.mjs` | `:477` |
| `d-admission-swap` | `lanes/d/p3-followons/admission-swap.test.mjs` | `:478` Route 1 |
| `d-replay-measure` | `lanes/d/p3-replay-measure/{measure-family,measure-order}.test.mjs` | `:481` by name |
| `d-replay-all` | `lanes/d/p3-replay-all/{class-membership,sleep-family,writer-enumeration,writer-order}.test.mjs` | P3-REPLAY-ALL-FAMILIES |
| `d-capture-start` | `lanes/d/p3-capture-start/capture-start.test.mjs` | `:486`; carries the P3-CSR5 mirror cell B-LOM repairs |
| `food-live-save` | `m3/w6/host/test/food-live-save.test.mjs` | `:485`; its root is ALREADY a `CHILD_ROOT`, so this needs no widening |
| `w7-import` | `m3/w7-preview/import/test/{route,refusal-route,live-clock,page-bundle,refusals}.test.mjs` | the Import screen; `support.mjs` and `edge-seed-entry.mjs` are helpers, not argv |

`edge-route.mjs` is **not** a CI child and is not enumerated in `rebuild.yml`: it needs `W6_BROWSER_BIN` and a real Edge, which no runner has. It and `edge-seed-entry.mjs` are declared and pinned as product (role test), and the `w7-import` step carries a comment saying so, so that a file with no CI home is a file somebody decided about (`DECISIONS:186 (3)`). The three `m3/w6/test/` files of 2.3 are declared but are NOT made children: they run inside the W6 whole-tree suite, and the author states that in the report rather than inventing an eleventh child for them.

**`CHILD_ROOTS` gains EIGHT directories** - the largest widening the list has had; S5 added one and disclosed it at length. It stands at eight, and every new child above is refused by `CHILD-ARGV-TARGET` (`b-package.cjs:483`) until its root is in it. The eight: `rebuild/m4/import/test/` (which covers `test/s3/` by `startsWith`), `rebuild/lanes/d/plan-edit/`, `rebuild/lanes/d/p3-replay-measure/`, `rebuild/lanes/d/p3-replay-all/`, `rebuild/lanes/d/p3-capture-start/`, `rebuild/lanes/d/import-retract/`, `rebuild/lanes/d/p3-followons/`, `rebuild/m3/w7-preview/import/test/`. S5's `F7` cell pins `CHILD_ROOTS` by `deepEqual`; it takes the new list and its red side is re-run. The list stays fixed in the runner (W7) and unreachable by any spec, so a package still cannot name its own root.

## 4. Guards

**4.1 `CHILD_SPECS` gains `'S6'`.** Four cells carry a named const and take one literal each: `measure/test/boundary.test.mjs:83`, `today/test/food.test.mjs:792`, `today/test/machine-settings-ui.test.mjs:709`, `today/test/setup.test.mjs:2315` - all `['H3', 'S3', 'S4', 'S5']` becoming `['H3', 'S3', 'S4', 'S5', 'S6']`. `boundary.test.mjs` is also moved by `c-p3-import-ui-2`, so it is `edited` twice over and the S6 literal is applied ON TOP of that branch's post, never instead of it. The fifth is NOT the same edit and the intake should not be read as if it were: `today/test/problem.test.mjs:1583` has no `CHILD_SPECS` const at all, but an inline `for (const id of ['S4', 'S3', 'H3'])`, youngest first, and **it never gained `'S5'`**. S5's brief explains why it was left (S5 moved no file it guards) and carried it. Its chain is now two generations stale, and S6 cannot leave it: `c-s6-small` moves `today-entry.mjs`, which the same cell hard-pins through `local-today-journey.test.mjs`. The edit: lift the array to a named `const CHILD_SPECS = ['H3','S3','S4','S5','S6']`, iterate youngest-first as the other four do, keep the red side. Anything less leaves a guard blind to two seals. Also carried: the dead exported `CHILD_SPECS` at `m3/w6/test/local-today-journey.test.mjs:670` (`['H3','S3','S4']`, no importer, `:459` MINOR) is DELETED by `c-s6-small` per `:476`; S6 re-pins the file at its post and states the deletion, it does not re-add it.

**4.2 The runner re-pin, and the dead B-LOM skeleton.** `b-s6-child-tail` moves `b-package.cjs`, so `tooling.runnerSha256` changes and every spec that pins it by sha is re-pinned onto the S6 runner: `H3.json`, `S3.json`, `S4.json` role `edited`; `S5.json` role `superseded-by-child`, the parent EXECUTION pin, exactly as S5 did to `S4.json` (S4-REVIEW-R1 finding 4: a spec on a moved runner stops at `RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER` before any evidence is read). `IDS` (`b-package.cjs:156`) and `NO_REGISTER_IDS` (`:260`) each gain `'S6'`; `S6` is an S- id, so the exemption-shape assertion passes without a PM by-name ruling. `pinned-unchanged-and-ruled-substitutions.test.cjs`: `F6` gains the `S6` literal in both pinned orders, `F7` takes the widened `CHILD_ROOTS`, `F8`/`F9` (the tail arrays and `TAIL_BYTES`) are re-pinned unchanged. **Measured and unresolved: `rebuild/lanes/b/tooling/packages/B-LOM.json` already exists on the tip**, `status: "SKELETON"`, empty `product`, `sourceBase e9c50e10`, and `B-LOM` is already in both `IDS` and `NO_REGISTER_IDS` with no receipt. Under `:486` (a) and (c) B-LOM is not a package of its own, so that skeleton is a runnable id pointing at nothing, and its `runnerSha256` goes stale the moment `b-s6-child-tail` lands. S6 either re-pins it with `H3`/`S3`/`S4` or deletes it and removes the id; it does not leave it. See STOP 1.

**`PUBLIC_TAIL_ROOTS` is a decision, not a re-pin.** `b-s6-child-tail` prints a failing child's last 60 lines only when every argv path lies under one of its four `PUBLIC_TAIL_ROOTS`. None of the eight new child roots is in that list, so every new child above would fail with the tail WITHHELD (path policy) and the diagnostics ticket would not fire on the suites S6 adds. Widening it is a privacy-surface change, argued per root with `TAIL_DENYLIST` in hand, never bundled. Recommend: widen for `rebuild/lanes/d/p3-replay-measure/`, `rebuild/lanes/d/p3-replay-all/` and `rebuild/m3/w7-preview/import/test/` only (synthetic-only, and the first two inside the flake's blast radius), and state the other five as deliberately withheld.

**4.3 The standing step and the trip-wire sequence.** `rebuild.yml:127` `--ci --package S5` becomes `--ci --package S6` **only at the ff-merge of the child**. `DECISIONS:465-467`'s order, exactly: (1) the branch names `--package S6`; `rebuild/coach/test/engine-revision.test.cjs` reads that flag (`standingPackageShortId()`) and looks for `rebuild/lanes/b/tooling/receipts/S6.json`, absent until the seal, so the rebuild job is RED at C5 on both OS while `B PACKAGE S6 PUBLIC CI EVIDENCE PASS` prints on both - **that is the `:456` trip-wire working**, it was the single MAJOR of S5's Fable final, and the author report states it in advance. (2) The PM's authorized `--full` writes `SEALED RUN RECORDED receipts/S6.json`. (3) 2.8's cell is run and passes, or the author STOPS. (4) The coach constant becomes `ENGINE_REVISION = "M2-S6-TODAY-CHILD@<first 16 hex of sha256 over the raw bytes of receipts/S6.json>"` in `rebuild/coach/engine-revision.cjs` (S5 `0df73b01f3d2d935`, S4 `171ebcd4d4b3b2b4`), with the comment block's worked example updated too - the test does not read it, a human does. (5) Second authorized `--full`: `AUTHORIZED STEP BYTE-IDENTITY RE-VERIFY`. (6) CI green on both OS. THEN the fast-forward. Red for steps 1 to 4, green from 5; closed BEFORE the fast-forward, never after. **The constant moves ONCE.** B-LOM rides S6 and mints no revision of its own; see STOP 1. `production-mapping.cjs` imports the same constant, so its suites move with it.

**4.4 The `executionId` tie (`:479` carry, Fable MINOR 3).** `production-mapping.cjs:229` is `id: executionId || (EXECUTION_ID_PREFIX + materialDigest)`. In the `{hash, executionId}` shape a caller may supply an id that does not name the digest the row is about and nothing refuses. S6 ties them: a supplied `executionId` must be the id bound to the REVIEWED digest for that `hash`, and one that disagrees with `EXECUTION_ID_PREFIX + materialDigest` for the same material refuses by a named code. Red side: the same call with a foreign id refuses; `{hash}` alone still binds at `qualify()` time and still derives nothing privately (`:478`).

**4.5 The two P3-IMPORT-UI-2 carries (`P3-IMPORT-UI-2-REVIEW-R4.md` notes 4 and 6).** NOTE 4: `confirm()`'s catch treats a throw AFTER `capability.publish()` as a refusal, so a registry that throws on its second `qualify()` leaves the device ADMITTED (applied, basis present) while the screen paints `LOCAL_IMPORT_RETRACT_REFUSED_ADMITTED` and "could not be taken back". No athlete path reaches it; S6 adds the guard and a cell that drives the contrived registry, because the next paint showing the summary is not the same as the state being right. NOTE 6: the done-callback now arms the adoption chain BEFORE its first Today paint - a reorder on a SEALED file. The S6 reseal must carry it **knowingly**: the author states the reorder in the report, pins `today-app.cjs` at UI-2's post, and the reviewer is told to look at it rather than left to find it in a diff.

**4.6 Carries that are NOT S6's, recorded so they are not silently adopted.** HOST-DAY-AGREEMENT (`:485`, lane C, small): taking the installation's clock takes its DAY too, so a host handed an era standing on another day stamps the era's day silently and cannot read its own write back; it must refuse by name when `era.liveDay()` disagrees, across all four family-(b) hosts. No product caller passes `era` today. **It stays a separate ticket**; S6 declares `food-host.mjs` and `machine-settings-host.mjs` at the `5c231e4a` post and does not change a byte of them. Likewise `:486`'s MINORs (`ORDER_EVIDENCE_REQUIRED` unreachable through the controller, the 6 stale S3 manifest pins, `admit()`'s harness default) are named here and left to their own tickets.

## 5. The flow, in S5's order with S6's names

1. **Brief accepted by name** - a PM line naming `rebuild/lanes/b/S6-TODAY-CHILD-BRIEF.md`.
2. **Three token lines on the tip**, in order: THEME `M2-S6-TODAY-CHILD`; BRIEF-BY-SHA with the sha256 of the accepted bytes and the byte count; GATE-SUPERSESSION in `:462`'s exact shape.
3. **The author cites all three in `packages/S6.json`** (`theme.acceptedLedgerLine`, `brief.acceptedLedgerLine`, `coverage.superseded.rulingLineSha256`), each a sha256 over the EXACT line bytes with no trailing newline, the method validated first against S5's own `:459`/`:460`/`:462` before it is trusted on S6's. A null `rulingLineSha256` is a HARD refusal: `supersessionRuling()` asserts it before coverage is printed, so the run stops at `GATE-SUPERSESSION-RULING-NOT-CITED` exit 1 and the theme and brief obligations are never reached. All three are PRECONDITIONS of the evidence, not obligations counted beside it.
4. **`--ci --package S6` prints `PUBLIC CI EVIDENCE PASS`**, the artifact re-proposed through the runner's own `proposed()` path, never hand-written. Run it ON THE PC at the package's base first and explain any difference from GitHub Actions before the FULL (`:485`'s recorded observation; bar row 25).
5. **Fable final review** over the sealed candidate (`:439`), independent of the r1 reviewer, high effort.
6. **PM `--full` with the private census on the PC.** S5's shape at `:465`: `PRIVATE ORACLE PRESENT` verdict-only, `HISTORICAL TOTAL 45 laws`, `104/104 DETECTED`, `0 HARNESS_ERROR`, LEGACY gates OBSERVED, nine SUPERSEDED, `POSTFIX PACKAGE REVIEW-PENDING: 1 open obligation`, exit 2.
7. **The receipt line** (`POSTFIX-ACCEPTANCE M2-S6-TODAY-CHILD <commit> <artifact path> <sha256> ACCEPTED`) discharges that obligation.
8. **`rebuild/m4/spec/review-s6-today-child.json`**, `{status: "ACCEPTED", receipt: {commit, path, line, lineSha256}}`, citing the receipt line.
9. **MERGE the tip into the reviewed head. NEVER rebase** (`:467` note 1): the receipt names the reviewed commit and a rebase rewrites it, which refuses `REVIEWED-COMMIT-NOT-BEHIND-HEAD`, correctly.
10. **Authorized `--full`**: `AUTHORIZED mode=--full`, `ENVELOPE AUTHORIZED`, `PARENT PINS RE-ASSERTED`, `SEALED RUN RECORDED receipts/S6.json`, `POSTFIX PACKAGE PASS M2-S6-TODAY-CHILD` exit 0; `VERDICT-S6.md` names the receipt sha.
11. **2.8's cell, then the coach constant** (4.3 steps 3 and 4). 12. **Second authorized `--full`: `AUTHORIZED STEP BYTE-IDENTITY RE-VERIFY`** - artifact, runner, spec and every pinned file byte-identical to the sealed run, receipt bytes in Git at every base checked, exit 0. 13. **CI green on both OS.** 14. **Fast-forward.**

## 6. Open inputs, and the merge order

**The merge order is fixed at `:484` and extended at `:486` (c). The S6 integrator lands, in this order and no other:** `c-s6-small` `6fad3472` -> `b-s6-child-tail` `d1001da9` -> `c-p3-import-ui-2` `4c3a5474` -> `d-b-lom` (when accepted). **The post of every declared path is ONE sha, measured after the LAST of these.** Seven files are moved by more than one of them - `source-admission.mjs`, `today/build.mjs`, `import/test/support.mjs`, `import/test/page-bundle.test.mjs`, `today-app.cjs`, `today-entry.mjs`, `w6/test/local-today-journey.test.mjs` - so a post measured before the last merge is a pin that is red the moment it is written. The author declares last and the report states the order it measured at.

| input | head | what it adds to the declaration list | state |
| --- | --- | --- | --- |
| `rebuild/c-s6-small` | `6fad3472` | moves 5 S5-pinned (`today-app.cjs`, `today-entry.mjs`, `today/test/problem.test.mjs`, `w6/test/local-today-journey.test.mjs`, `w6/host/test/local-real-day.test.mjs`); adds 3 unpinned (`today/build.mjs`, `gym-app.mjs`, `problem-report.cjs`) | ACCEPTED r3 (`:476` (2)); frozen |
| `rebuild/b-s6-child-tail` | `d1001da9` | moves 2 S5-pinned (`b-package.cjs`, `pinned-unchanged-and-ruled-substitutions.test.cjs`); adds `tooling/test/child-diagnostic-tail.test.cjs`; MOVES THE RUNNER SHA | ACCEPTED r5 (`:476` (3)); frozen |
| `rebuild/c-p3-import-ui-2` | `4c3a5474` | 21 paths, 15 non-document, **5** S5-declared; new `import/import-screen.mjs`, `import/test/{route,refusal-route}.test.mjs`, `edge-route.mjs`, `edge-seed-entry.mjs`; moves `import/test/{support,page-bundle}`, `today/{build.mjs,preview.css,test/copy.test.mjs}` | Fable final ACCEPT r4; notes 4 and 6 are S6 carries (4.5) |
| P3-REPLAY-ALL-FAMILIES | MERGED `:481` | `m4/import/{sleep-replay,body-composition-class,replay-registry}.cjs`; `lanes/d/p3-replay-all/` 4 cells | closed; on the tip |
| **FOOD-LIVE-SAVE** | MERGED `5c231e4a` (`:485`) | **CLOSED with NO sealed drift.** `today/food-host.mjs` and `machine-settings-host.mjs` are in NO package (S5 declares `today-entry.mjs`, `today-app.cjs`, `gym-host.mjs`, `reading-host.mjs` and the thirteen `today/test` files only), so it merged on its own CI and deployed. S6 declares both, plus the new `m3/w6/host/test/food-live-save.test.mjs` as a child | closed; carries HOST-DAY-AGREEMENT out (4.6) |
| **LOCAL-CAPTURE-START-RESUME** | MERGED `ff099d14` (`:486`) | **CLOSED, all eight product paths undeclared:** `w6/local/source-admission.mjs`, `w6/test/{local-source-admission.test.mjs,local-source-consumer.test.mjs,local-source-consumer-browser.mjs}`, `w7-preview/import/test/support.mjs`, `m4/import/replay-registry.cjs`, `lanes/d/p3-capture-start/capture-start.test.mjs`, `lanes/d/p3-replay-all/writer-order.test.mjs`. Declared per the stop-7 ruling | closed |
| **B-LOM** | `rebuild/b-lom-brief` `13b3713e` (brief only; no product branch yet) | **PLACEHOLDER, the last row.** Expected drift from `rebuild/lanes/b/B-LOM-BRIEF.md`: sealed moves `w6/local/today-bindings.mjs` and `today/test/gym.test.mjs` (both S5-declared, re-pinned at B-LOM's post); NEW `m4/workout/legacy-order-mapping.cjs` + `m4/workout/test/legacy-order-mapping.test.cjs`; `m4/workout/engine-order.cjs` 2 hunks; `w6/local/source-admission.mjs` 1 hunk; POSSIBLY `today/gym-model.mjs` (its STOP 3, the clone-identity question, decides); the P3-CSR5 mirror-cell repair under `lanes/d/p3-capture-start/`. **ZERO `rebuild/engine` bytes** | folded into S6 by `:486` (c); taken LAST |

**FINAL LIST WAITS ON B-LOM only.** Its product branch does not exist yet, so its posts cannot be measured and its row is a placeholder, never a guessed list. `engine-order.cjs` and the two sealed files it moves are already in this brief's list because they must be declared whether or not B-LOM lands; only `legacy-order-mapping.cjs`, its test, and possibly `gym-model.mjs` are contingent. If B-LOM slips, the PM either seals S6 without it and accepts a second reseal for the port, or holds; `:486` (c) chose to hold, so that the port needs one reseal and not two.

## 7. The known CI flake, and how it is to be closed

`:481` records it for the fourth time in the class and the second in these exact cells: on `windows-latest` the today+measure suite goes red about one run in three, always in `rebuild/m3/w7-preview/measure/test/journey.test.mjs`, always in **`P-MEASURE (a) - the real route renders the trial table week by week, against the committed table`** (line 41) and **`P-MEASURE (d) - the export block text equals the rendered table, cell for cell`** (line 66), never in `(e)`. Symptom: an 80 s render, then a null element. `ubuntu` green; the same head passes 666/666 on the PC; a retrigger passes. `:467` note 2 already made this a ticket once and it was answered with diagnostics, not a cause.

**S6 must find the cause and fix it inside the reseal.** Widening a timeout alone is refused as a fix: it turns a race into a slower race and the next occurrence is un-diagnosable again.

The lead, measured here and handed over rather than asserted. `measure/test/support.mjs:157` is `export const settle = async (rounds = 24) => { for (...) await new Promise(r => setTimeout(r, 0)); }` - a FIXED 24-turn macrotask drain with no condition attached. Every mount, every `view.go(slot)` and `pickMarkersOnScreen` ends in it; `go` then does `pick(slot).click()`, and `tableOf` returns `null` when `pick(slot)` misses. So when the real `fake-indexeddb` plus `webcrypto` work behind a route change outlasts 24 turns on a cold or loaded Windows runner, the next line dereferences null or the assert reads `no trial table rendered`. Both reported symptoms fall out of that one line. Second half: `journey.test.mjs:17` memoises ONE `trialDevice()` promise for the file, so `(d)` runs `onMeasure()` over the store `(a)` wrote and branches on `view.pick('measure-marker-pick')`; it inherits whatever `(a)` left half-drained. That is why the two fail together and `(e)` does not.

Required shape of the fix: `settle` becomes a CONDITION with a bounded deadline - poll until the awaited slot exists or the store's pending work drains, and FAIL LOUDLY with the slot name and the elapsed time when the deadline passes; `go()` asserts the target slot exists before it clicks. No cell's subject changes and no assertion is removed. The author reproduces the red on purpose (drop the deadline, or throttle the fake store) before claiming the fix, and records the reproduction in the report. The same pattern is checked in `import/test/support.mjs`, which two branches move: the Import route cells mount the same way and would inherit the same race.

**A second, opposite observation is on the record and is NOT the same thing.** `:485` reports a local `--ci --package S5` at the CLEAN tip on the PC printing `AUDIT RED-FIRST FAIL` / `CHILD-REQUIRED-EXIT-ZERO` while GitHub Actions at that same tip and at `5c231e4a` were green on both OS. That is CI green and the PC red, the mirror image of section 7's flake, and it is unadjudicated. The S6 author runs `--ci` on the PC at the package's own base and **explains the difference before the FULL** (bar row 25). If it is the same `settle` cause reaching a different child, say so and fix it once; if it is not, it is a finding of its own and the FULL waits.

## 8. The bar

S5's twelve rows are carried unchanged and re-asked of S6 (pins byte-identical to the accepted bytes; no product byte of S6's own; no engine byte; five carriers refused ON THIS TREE and executed; the evidence can FAIL; refusals inherited, not caused here; each carrier's evidence its OWN; the suites green; the pins declared; nothing weakened). Added, one per new declaration class plus the real-world rows of `:439`:

| # | The claim | How it is measured |
| --- | --- | --- |
| 13 | Every file the four inputs move is DECLARED at its landed post | the inventory; `git diff` of each declared path against the post-merge head is empty, measured AFTER the last merge of section 6 |
| 14 | The `w6/local` and `w6/host` classes are closed | all 13 and all 13 declared; `engine-runtime-host.cjs` shown ALREADY carried, not newly added |
| 15 | The three moved `w6/test` files are declared and the other 79 are named, not swept | 2.3 and 2.6; the report lists them and cites the TODAY-TREE-CUSTODY ticket |
| 16 | The `m4/import` class is closed | all 30 declared, the three P3-REPLAY-ALL-FAMILIES modules among them, named in the verdict line |
| 17 | The `m4/workout` class is closed | `plan-edit-commands.cjs`, `plan-edit-model.cjs` and `engine-order.cjs` declared; `f2-tag-adapter.cjs` proved imported by no runtime file |
| 18 | The `w7-preview/import` class is closed | all 9 declared; `edge-route.mjs` and `edge-seed-entry.mjs` pinned and stated as having no CI home |
| 19 | The `lanes/d` classes are closed under the stop-7 ruling | every test file each new child executes is declared product; no child executes an undeclared file |
| 20 | Every new child EXECUTES and its count is its own | one child per new root, each needle `# pass N` measured on the post-merge head, not copied |
| 21 | `CHILD_ROOTS` cannot be widened unseen | `F7` amended over the eight added roots, red side re-run |
| 22 | `PUBLIC_TAIL_ROOTS` was decided, not drifted | the three widened roots argued by name; the five withheld stated |
| 23 | **A reseal cannot invalidate an admitted import** | a cell proving an import admitted under `M2-S5-TODAY-CHILD@0df73b01f3d2d935` is still ADMITTED, ADOPTED and RETRACTABLE after the constant moves to the S6 revision; if it is not, the author STOPS and writes the finding BEFORE the constant moves |
| 24 | The trip-wire went red and closed BEFORE the fast-forward, ONCE | the CI run at the `--package S6` commit quoted red at C5, the run at the constant commit quoted green; no B-LOM revision is minted |
| 25 | **`--ci` on the PC agrees with CI, or the difference is explained** | `:485`'s `AUDIT RED-FIRST FAIL` / `CHILD-REQUIRED-EXIT-ZERO` on a clean tip where Actions was green: run at S6's own base, quote both, explain, before the FULL |
| 26 | **No `rebuild/engine` byte moves, with B-LOM inside** | `engine-files-differential`: **45 tracked, 0 moved**, re-run after B-LOM lands; B-LOM's own brief requires the same, and the GATE-SUPERSESSION line's "changes no rebuild/engine byte" clause depends on it |
| 27 | `executionId` cannot name material it is not about | the `:479` cell, red side a foreign id |
| 28 | UI-2's two carries are closed, not inherited | note 4: a cell drives the contrived registry and the guard refuses the post-`publish()` throw; note 6: the done-callback reorder is stated in the report and `today-app.cjs` is pinned at UI-2's post |
| 29 | The flake has a named cause and a red-first fix | section 7: reproduction, then fix, then 20 consecutive green runs of `(a)` and `(d)` on `windows-latest` |
| 30 | Real-world invariants (`:439`) | re-proved on the REAL route: setup-first on a fresh install, the sample label absent on every enrolled frame, the refusal card naming the session in hand, `Build <sha>` in the footer, the Import screen's 15 served labels including the identity question verbatim, and **a food day and a machine note saved and read back after dispose/reopen** (`:485`'s defect, proved closed under the seal) |
| 31 | Stub-free cells (`:439`) | every bar item proven by a cell that mounts the real route over `fake-indexeddb` with real ops and asserts rendered text; no fixture-only pass counted |

## 9. The exact commands the PM runs to accept this brief and mint the token lines

S5's pattern (`:459`, `:460`, `:461`), on the chain branch, in this order. The commands are unchanged from round 1; only the THEME line's branch list has grown. The acceptance-by-name line is appended first; the sha in the second is measured from the bytes that line accepts, so the file must not move between the two.

1. `cd /d <worktree> && git checkout rebuild/t2-client-core && git pull --ff-only`
2. Append the ACCEPTED-BY-NAME line, then the THEME line; commit.
3. Measure the brief on that commit:

```
node -e "const f=require('fs'),c=require('crypto');const b=f.readFileSync('rebuild/lanes/b/S6-TODAY-CHILD-BRIEF.md');console.log(c.createHash('sha256').update(b).digest('hex'),b.length)"
```

4. Append the BRIEF-BY-SHA line with that sha256 and byte count, then the GATE-SUPERSESSION line; commit and push.
5. Hand the author the three line NUMBERS. Each citation is sha256 over the exact line bytes, no trailing newline, and the method is validated against S5's `:459`/`:460`/`:462` values before it is used on S6's:

```
node -e "const f=require('fs'),c=require('crypto');const n=Number(process.argv[1]);const L=f.readFileSync('rebuild/DECISIONS.md','utf8').split('\n')[n-1];console.log(c.createHash('sha256').update(Buffer.from(L,'utf8')).digest('hex'))" <line>
```

The three lines, in S5's exact shapes; the date, the sha256 and `<N>` are placeholders the PM substitutes:

```
- <date> · cowork · THEME M2-S6-TODAY-CHILD - the reseal child that closes the DECISIONS:473 custody hole and re-pins today/** over the accepted bytes of rebuild/c-s6-small, rebuild/b-s6-child-tail, rebuild/c-p3-import-ui-2 and B-LOM (folded in by DECISIONS:486 (c)): it declares as its own product the <N> merged-but-undeclared files under rebuild/m3/w6/local, rebuild/m3/w6/host, rebuild/m3/w6/test, rebuild/m4/import, rebuild/m4/workout, rebuild/lanes/d and rebuild/m3/w7-preview/import, gives their suites a CI home in rebuild.yml as a disclosed hunk (DECISIONS:117 (4)), and has NO product behaviour of its own. Standing ruling DECISIONS:455; custody ruling DECISIONS:473; merge order DECISIONS:484 as extended by :486 (c). Its behaviour/delta contract is rebuild/lanes/b/S6-TODAY-CHILD-BRIEF.md; its parent is M2-S5-TODAY-CHILD, rebuild/m4/spec/acceptance-s5-today-child.json sha256 84e3430ddd851965d630cecffe4bf7ead88ec10ed53635c503671fd616c1169e (receipt DECISIONS:466, merge :467); it changes no rebuild/engine byte, and as a third-generation descendant of M2-S3-COMPANION it retires the same nine byte-identity gates again under its own token line below. This line is the THEME citation the seal runner requires; it authorises no PASS word by itself · ACCEPTED
```

```
- <date> · cowork · BRIEF ACCEPTED BY SHA for M2-S6-TODAY-CHILD: rebuild/lanes/b/S6-TODAY-CHILD-BRIEF.md, sha256 <64-hex, the accepted bytes of this file> (<n> bytes), is the brief of record; this line carries the binding sha256 and is the citation brief.acceptedLedgerLine cites · ACCEPTED
```

```
- <date> · cowork (PM) · GATE-SUPERSESSION M2-S6-TODAY-CHILD source-carriers,inherited-carriers,defect-witnesses,writers-differential,second-gate · the token clause for DECISIONS:153, same conditions (i)-(iii), for the child of M2-S5-TODAY-CHILD (grandchild of M2-S4-REAL-DAY, great-grandchild of M2-S3-COMPANION); the child changes no rebuild/engine byte and retires the parent's retired gates again under this line and its own evidence - the five carriers cover the nine gates merge-source, migrate-source, writers-source, migrate-differential, witnesses-2, witnesses-5, witnesses-7, writers-differential and second-gate · RULED
```

The third line must END in `RULED`, and its token clause must be exactly `GATE-SUPERSESSION M2-S6-TODAY-CHILD source-carriers,inherited-carriers,defect-witnesses,writers-differential,second-gate` - any bracket, quote, backtick or extra word inside that clause leaves it something other than the token and frees nothing. A retirement is never inherited: S6 asks every condition of `DECISIONS:153` again and answers with its own six red-first `s6-sup-*` children in S5's shape, because `constants.cjs`, `writers.cjs`, `merge.cjs` and `today.cjs` stand outside the carriers' reconstruction and no descendant of M2-S3-COMPANION can carry them.

**STOP 1, for the PM before the author starts.** `rebuild/lanes/b/B-LOM-BRIEF.md` still describes B-LOM as an engine-tier PACKAGE with its own `packages/B-LOM.json`, `--ci --package B-LOM`, `receipts/B-LOM.json`, its own GATE-SUPERSESSION token line and its own coach constant `ENGINE_REVISION = M2-B-LOM-LEGACY-ORDER-MAPPING@...`. `:486` (a) rules it is NOT an engine-tier package and (c) folds it into S6. Both cannot hold: if the B-LOM author works from that brief as written, the coach constant moves TWICE and the `:456` trip-wire closes on a revision no standing CI step names. The B-LOM brief's flow section must be withdrawn and its bar rows folded into section 8 before its author starts, and the SKELETON `packages/B-LOM.json` (4.2) must be re-pinned or deleted with its id. S6 cannot resolve this from inside itself.
