# M2-S8-REAL-SHAPE - the reseal that makes the owner's own file the programme

**STATUS: READY FOR ACCEPTANCE BY NAME.** Lane B, ENGINE-TIER PACKAGE PROCESS, child of
`M2-S7-PORT-ADMISSION` (`rebuild/lanes/b/tooling/b-package.cjs`), size S. Parent artifact
`rebuild/m4/spec/acceptance-s7-port-admission.json` sha256
`350f56885c5eb55eecada58cadfb011656ddc58b82fc8fdc514ddc4503af791b` (84128 B; receipt `DECISIONS:518`,
merge `:519`). Walked on `rebuild/d-p3-real-shape` with the chain tip
`origin/rebuild/t2-client-core` `8ebc860c30980dc5793be15202ea1a292e3769ba` MERGED in at `1af78de`,
never rebased. That merge is this package's `sourceBase`. Every sha below is measured from Git and
from disk on that base and this head, not carried from a report.

## 1. Why

S7 sealed on 2026-09-17 and the owner retried (`DECISIONS:520`). He unlocked, answered the identity
question Yes, and was refused again by name: `LOCAL_SOURCE_PROGRAMME_UNRESOLVED` on the split, with
the training-week sentence; the route retracted cleanly and nothing on his installation was written.
The cause, read by the PM in the OLD app's public source, was not one field but a shape: every
fixture the import corpus and the lane cells sealed is shaped like the NEW app's clean-init state,
and the owner's file is the OLD app's migrated state. Its split entries carry a third member `why`;
its exercise ids are short handles (`hack`, `abs`, `hipthrust`) where `setup-model.mjs` mints
`slugOf(name)`; it carries no `athlete_label` at all. S7 was necessary and not sufficient.
The PM ruled OPTION A, THE FILE WINS, with the owner's own words `:507` as the tie-breaker, and
dispatched the lane D spec. The spec of record `rebuild/lanes/d/P3-REAL-SHAPE-SPEC.md` v2 was
accepted by name at `:521`, after a first review that REJECTED v1 on six blocking findings. It
FIRST built the fixture of record - the old app's own seed run through the REAL `port.cjs`,
`prepare.cjs` and `engine/migrate.cjs` - and then measured every field the rule and the adoption
path refuse on it, lift by lift, against a fresh-start phone whose setup names the same lifts by
name. THE MEASUREMENT FOUND FIVE GAPS, NOT THREE, each one a cell on the shipped S7 page with the
real-shape bundle: (1) as-is, `LOCAL_SOURCE_PROGRAMME_UNRESOLVED` on `split` - the owner's screen;
(2) `why` removed, `exercise_id`; (3) ids slugged, `steps` - the old app carries no ladder;
(4) ladder added, `inc` - null on a bodyweight lift; (5) `inc > 0`, the file ADMITS and is NOT
ADOPTED, silently, because `local-source-basis.mjs:54` refuses to adopt an import whose state label
differs from the first-run label and no old-app file has a label at all. Gaps (8)-(11) of the same
table add the companion's three refusals and, worst of all, `ENGINE_CAPTURE_LOAD_UNPROVEN` on the
adopted LOWER day: a successful import would have left the owner's lower-body day with NO CARD.

The build landed in two accepted rounds. P3-REAL-SHAPE (`:522`, Fable final ACCEPT) wrote the rule:
PROVED and nothing else (`P-0` one setup op, `P-IDENT` the owner's answer, `P-A` the week over
periods drawn from `{from, map, why}`, `P-BOUND` over the members the file carries, `P-CAP` the
capture provenance against the document that PRODUCED the capture, and the new `P-LABEL` by which a
file naming a DIFFERENT athlete refuses by name instead of today's silent non-adoption); ADOPTED
wholesale (id, n, mg, day, sets, hi, inc, steps, tags, priority_muscles, the whole split with its
`why`, every extra member), with the same lift decided by NORMALISED NAME in one shared helper,
`rebuild/m4/workout/lift-correspondence.cjs`, read by admission, the capture block and the
companion; a pre-import Earned session whose lift matches exactly one file lift re-keyed onto it
with loads, reps and slot count unchanged; every unmatched document lift appended inactive and
tombstoned, so nothing of his is lost. That round MEASURED PM question 1 as NO and STOPPED on it, as
`:521` required: a v1 capture read under the v2 producer blocks the card, and a v2 capture from
first boot blocks mid-session because the accepted projector admits `earned/captured-lift-layout/v1`
ONLY. P3-LAYOUT-V2 (`:523`, Fable final ACCEPT) closed that: the projector's layout law at
`engine-history.cjs:63` admits a v2 layout BESIDE v1 through `LAYOUT_PROFILES`, each read by its own
adapter with byte-for-byte the same checks; `workout-host.mjs` dispatches each stored capture to a
SIBLING adapter minted from this installation's own identity with only `rule_profile` taken from the
capture; `today-bindings.mjs` moves the page's PRODUCER to `CONFIGURATION_PROFILE` for new captures
while v1 stays readable. The deciding cell now passes on BOTH days.

**Every product file that changes is pinned in `packages/S7.json` or is a first declaration of these
two rounds.** Under `DECISIONS:455` a byte moved on a sealed pin fails `SEALED-PROFILE-RECOMPUTATION`
on its own branch, so this change reaches the tip only inside a lane B reseal child. That is the
whole of S8's reason for existing, and `:523` dispatched this package by name.

**S8 adds no behaviour of its own.** Its product delta is exactly the accepted P3-REAL-SHAPE +
P3-LAYOUT-V2 diff and the tooling this package's own id requires. If a reviewer finds a byte here
that no accepted lane D round and no fact of section 5.1 produced, that is a finding.

What the athlete gets: his own file becomes the programme. Importing proves the file is his by its
SHAPE and by his identity answer, keeps his per-lift sets, rep targets, increments, ladders, tags
and priorities, and then ADOPTS them wholesale - his lift ids, his names, his week. A session he
logged on this phone before importing is re-keyed onto the file's lift by name and rides in as
logged. The next morning BOTH cards open: the upper day on the file's total and handle ids, and the
lower day with the bodyweight raise and the hold lift each prescribing. A file that names a
different athlete is refused by name instead of admitting and doing nothing.

## 2. The declaration list, walked

Method, repeated rather than trusted: for every one of the **206 paths the parent artifact
`acceptance-s7-port-admission.json` pins**, the bytes on disk at this head were hashed and compared
with S7's own recorded `post`. **Twenty-four moved; the other 182 agree byte for byte**, so every
`pre` below is S7's own post, measured and not typed. The walk that found them is
`git diff --name-status 285fe08b30b5417ca9c0232c755d226f0ce62597..HEAD -- rebuild/m3 rebuild/m4 rebuild/lanes/d`
plus the three `rebuild/lanes/b` paths and `.github/workflows/rebuild.yml` outside it. The
first declarations were hashed the same way and carry `pre: null`, because they did not exist at the
base; the two files this change moves that the parent does NOT pin carry their `sourceBase` blob as
`pre`, which is the runner's own rule for role `new` (`pre === null` OR `pre !== post`).

**Counts.** 206 carried forward from S7 + 18 of this package's own = **224 declared paths**:
**24 edited**, **17 new**, **182 carried** (`pre === post`) and **1 superseded-by-child**,
`rebuild/lanes/b/tooling/packages/S7.json`, which is the parent EXECUTION pin this package supersedes
inside its own seal now that `rebuild/lanes/b/tooling/b-package.cjs` moves.

**2.1 The eight product files, role `edited` and role `new`.** These are the two rounds' own files
and the only ones.

| path | role | pre | post |
| --- | --- | --- | --- |
| `rebuild/m3/w6/local/source-admission.mjs` | edited | `d0c30f8c5ad2314b0225c92d0a8f24c8ef64cfe3d4c90527719a1a15759069b1` | `2f477147b79d3fc1b03feae873b25f40359334425051a851570880b7423113a3` |
| `rebuild/m3/w7-preview/import/import-screen.mjs` | edited | `637ef3bbfd9d73e3e0236449ef596f78e76c39517b9aed73fa28bd2ed5920c3d` | `ea4a178821ffad7f8fdcec6744e30f78fc873b9275e5da5645cb645cf730649e` |
| `rebuild/m4/workout/plan-edit-model.cjs` | edited | `ccaa90bb98b134ebda14e6f1a5a3c0a7688b9dfe9c0a8e6836a27e1855d89a5b` | `7cdb4ade63c596cd2a5e6aeec7b97fc9c3f2cfc2cd4386cd116c7d8e70ee4724` |
| `rebuild/m3/w6/host/workout-host.mjs` | edited | `8472097cb015f5dce034379f3f08b70aa516ca6d479d4f4fc8f3f0e3ffe08b69` | `f0f36328595673061132d5c55d76b1a9a93de5fc5fca5171ed4a1647e85ccb9f` |
| `rebuild/m3/w6/local/today-bindings.mjs` | edited | `8694a5d60481aceb739ae30db521df70335d6fbe8b1b2b67c43431ce17033d4c` | `23c1179794b12259c027f9b30780186211e7ac4b6f2770eaca3fe663e59bbf12` |
| `rebuild/m3/w7-preview/today/local-source-basis.mjs` | new | `d98015dba23867142147204a35e98f1df2d1b7093327c783720ab012f2f91411` | `3e04c246e1db083b1db66df284f0910560e4a898cd8eeb038c925ddefa031675` |
| `rebuild/m4/workout/engine-history.cjs` | new | `7ab75dc967e721d798d293f992796d67befd68e258e9e508f7bfd7a5cd24be89` | `2ec6e44b2aae61bb80babc0246bde51528685734f545210ab8d39795633e560f` |
| `rebuild/m4/workout/lift-correspondence.cjs` | new | `null` | `8bec2a64bbc39dc1edd5fb8fd6a06b2a15298e323ac2b17188177319813d2e7f` |

`source-admission.mjs` carries `programme()` per spec 2.1-2.3 with Q2 at `:288-:290`, `P-LABEL`
before the lift loop, the returned basis with the adopted file programme and the tombstoned
unmatched document lifts, the capture block's `documentSets` + `documentProgramme` out-parameters
with the REST-day guard first, the re-key of matched pre-import sessions in the PROJECTED session
(never the layout, which would trip `WORKOUT_CAPTURE_LAYOUT_UNPROVEN`), and the fix round's
`liftAttach` repair of the F4 machine-settings guard at `:485` that review R1 measured BLOCKING.
`plan-edit-model.cjs` carries the companion, binding a row by its own id first and then by unique
name with the first-run branch byte-untouched, and the fix round's `P2_ROW -> []` which review R1
measured BLOCKING: `:173` still compared the day and mg that admission no longer proves, so a
corresponded lift on another day admitted, adopted, and then Edit My Week refused for ever.
`import-screen.mjs` carries the screen's side of the same refusals. `local-source-basis.mjs` carries
the adoption label rule of gap (5). `engine-history.cjs` carries `LAYOUT_PROFILES` - the v2 layout
admitted BESIDE v1, every other check byte-unchanged and no v2 slot's `prescribed_load` read;
`workout-host.mjs` the sibling-adapter dispatch; `today-bindings.mjs` the page's producer move.
`lift-correspondence.cjs` is the whole of the new surface: `normaliseName` (NFKD, letters and digits
only), a correspondence injective on both sides, `idCollisions` refused by name, and `matchByName`.

**2.2 The sixteen sibling test files and ancestor specs the parent already pins, role `edited`.**
The first eleven are the two rounds' own sibling cells and the two S7 lane cells they extend; the
twelfth is the lane B tooling cell of 5.1 (6); the last four are the ancestor specs re-pinned onto
the moved runner, `tooling.runnerSha256` only, one byte range each (5.1 (5)).

| path | pre (S7 post) | post |
| --- | --- | --- |
| `rebuild/m3/w7-preview/import/test/support.mjs` | `34e6a7591a85cf66419ee1a9ea7b2fd25734f13ebe72bc3f775efe8954a9c359` | `adea55fcd2ff7aab1177385b8ac8c43135203d15e977dc88a1cd24c361edd2cb` |
| `rebuild/m3/w7-preview/import/test/refusal-route.test.mjs` | `e0c4bc1a84e37127fd91b103a863675a1aa725ecee82f57cb7e7e54f1f2356a6` | `5edb49e3d853d946ea9695cb9e9a04166f5137368e700572d185607565007579` |
| `rebuild/m3/w7-preview/import/test/page-bundle.test.mjs` | `99762e4c4d122e72bf7de36055039fb188c085530305e4d237ff64f7f16d9b9e` | `9b56ee6e22001296f354c77e0fa5fab6711d4c8f635e72a82e6c2977a13c62b3` |
| `rebuild/lanes/d/plan-edit/model.test.cjs` | `3338be39b58e384d19d4f2501c2fa7809c9233b9a0f15012c056d1dfff8e7338` | `afb88e790020cc1140914c1a16eeda009b38cab7cf6dda9561cc5eaf8ea9a275` |
| `rebuild/lanes/d/p3-port-fix/programme-rule.test.mjs` | `845d28d62ab9565d3dd1f722ff20850bf79d795d1abd8c1f75a7b7b6a4e693a7` | `ae5c3c7853960885df175e46a697e817befa58fc038ab79044de9787c80ced83` |
| `rebuild/lanes/d/p3-port-fix/owner-route.test.mjs` | `f9a1cc02eb159b4a7132fe8cc6248c0bb3178f7f275f05e89103f8c5150e0f1c` | `08f9a5fbcb4d83d70c807422578986b751b74bdce849eb046b84157e641bb383` |
| `rebuild/m3/w7-preview/measure/test/boundary.test.mjs` | `54f5e5b45fceec060ebcc9f19cdd99f43af340dc09ffb7514370a4fdaa8b113e` | `b1f066f901c6833c340ff43bffee3389bcece180544c732ad2f0db9ae5f91c6f` |
| `rebuild/m3/w7-preview/today/test/food.test.mjs` | `e932bfb9b7003319665508dd82754927d4f6e97677803c7caa6b9884eff796d8` | `088530cfccbba5eceb19fbae627b7977794f42bd4849126edede061f74a9c95b` |
| `rebuild/m3/w7-preview/today/test/machine-settings-ui.test.mjs` | `a35ad9dad96988a336fea90fcd8c6b9a56bc9e1c6fc5aec9ca4d938a201f4cb7` | `e532f8d4e5aa5435845908e5f76667b4933f7c7a6ffe1ac39740c14d71910a7f` |
| `rebuild/m3/w7-preview/today/test/problem.test.mjs` | `19f808fc046a1a4df1e6a3b4ee22f3af5a19187181090046642ef481d942b800` | `cb42052b4aa055cc5e9a6449fe78d2fcc1425b1adf8c99916b9104608be56b4e` |
| `rebuild/m3/w7-preview/today/test/setup.test.mjs` | `ec490b43d769f2673f6b521db0e2bf0c264366cc8bb8e153eb8ee7cb64c6776f` | `80e5e2eab1e526f7594fb2c304bdb8cc03ff21588c38a4866f53e2091464d90d` |
| `rebuild/lanes/b/tooling/test/pinned-unchanged-and-ruled-substitutions.test.cjs` | `d17f2e45e086363e065862ace674ecee6c15f839bf1e2d536d806faafa2de4aa` | `24525b8f97e90ef0a4501ef233c253989387281315127ed1742943acee19d3e1` |
| `rebuild/lanes/b/tooling/packages/H3.json` | `f1934b8bcce0364fff42aa18cc90d57b8e20cb2a08e1cc96a720c686b05b70cc` | `137cc5ee36bbdd50ab2a34b4f0c7e407e1d889063e2f176b01e9d31566ba275e` |
| `rebuild/lanes/b/tooling/packages/S3.json` | `0d3bc08699f4bb25983c97f159f172410716764642ec5a521c2c3474ce35137e` | `d354cec7bbe020912ff317477dc66e4ef6e9f94e625e729e2504d6e33143d84d` |
| `rebuild/lanes/b/tooling/packages/S4.json` | `797830e0b20d096a925da5e5e8427dd0b6c5d90bf3c8e56c028bd83645c1d007` | `165bd6b16581be91a711fc63addc6835a2eac3f143e649e61d321c5d6a93bab2` |
| `rebuild/lanes/b/tooling/packages/S5.json` | `2923654580ae6ac28bce7483e9d7a1e9786dc798c93353682c9181e0409061b5` | `60a00077b6e2ea48fc5c94832e97ea759852b9c32b9e4c835765737fef4ff1fc` |

**2.3 The runner, the standing CI step, and the two specs the runner's move re-targets.**

| path | role | pre | post |
| --- | --- | --- | --- |
| `.github/workflows/rebuild.yml` | edited | `afce33cf1925f304cfa115053d4c6613002677ba6e74b5bee1b04cfafc8fe93c` | `8403d10b1a54d721a17e92cc0c2eb7fe119379b65cc4ba9625e6a7196ad9d532` |
| `rebuild/lanes/b/tooling/b-package.cjs` | edited | `0fb0570d812ea1c1a9426281ae39c8101c1ddd0206b11ad5efa7a3778339cf8d` | `e31dd206c0fb0fc0c295df45eae3992d4c59b1a76de8da04a4d0f22948e9335e` |
| `rebuild/lanes/b/tooling/packages/S6.json` | edited | `77f24fd77460a10e7637fb6a181c6ecfc39db0ab55e3d5897b9b9770f9b63107` | `45eea528180d256843a6edd71b5768a9fce0f78b0f0742e7cc70680e8a9e10cd` |
| `rebuild/lanes/b/tooling/packages/S7.json` | superseded-by-child | `e7658e21f1178f8316ae6dadc3f13d1cd62210fd37693086fd7c4fcbe65ba285` | `00d7f1f8a43f3c923f6996766cd547f0d50517d1457e5cad23a4afedd263040d` |

`rebuild.yml` is section 5, rule (a). `S6.json` is `edited` and `S7.json` is `superseded-by-child`
for one reason each and the reason is the runner's own: `S6.json` is a parent PRODUCT pin, which the
runner requires to be `carried` or `edited`, and `S7.json` is the parent's own EXECUTION pin, which
`DECISIONS:109` says a child supersedes inside its own seal and which the runner refuses under any
other role. Both move by exactly one byte range, `tooling.runnerSha256`, and neither spec's own
product pin for the runner is re-targeted - S6 and S7 did not produce these bytes, and
`--ci --package S6` or `S7` on this branch is expected to stop where a parent stops under a child.

**2.4 Fourteen more first declarations, role `new`, all `pre: null`.** Six are the supersession
cells this package owes over its own spec (5.1 (4)); eight are the lane cells the two accepted
rounds wrote, declared under the `DECISIONS:487` stop-7 ruling that a `lanes/d` file a declared
child EXECUTES is itself declared product. `legacy-fixture.cjs` and `real-shape-support.mjs` are
reached by relative require from the six cells, which is what `executedClosure` means by executed.

| path | post | bytes |
| --- | --- | --- |
| `rebuild/m4/workout/test/s8-supersede-source-carriers.test.cjs` | `e9996b0a97980b7bd9221c822e619678b0eb84770c232d28ba38594615f5c8b9` | 13090 |
| `rebuild/m4/workout/test/s8-supersede-inherited-carriers.test.cjs` | `cfd81f4bbc05cd21d586d5a7bbf580bff0cc3887dcc6b683a508f3011d46e2d3` | 9744 |
| `rebuild/m4/workout/test/s8-supersede-defect-witnesses.test.cjs` | `32dc363d0a73fc9fe37c1fa729b57d71f229e769a9605c959844811278f2a42d` | 11216 |
| `rebuild/m4/workout/test/s8-supersede-writers-differential.test.cjs` | `684e6fb6a200e82d5b04a44b635b6781fb550b7d2bec71f7276ae0dc5b1bc68d` | 12180 |
| `rebuild/m4/workout/test/s8-supersede-second-gate.test.cjs` | `06a08a396a6e92ad285d7300bced68fe437117f06eb7f6f1eaca7cd4fc5ffbc3` | 10633 |
| `rebuild/m4/workout/test/s8-engine-files-differential.cjs` | `49ee104c00e722137fc21823b20a3f3f9207edf8e9427fa02630f593bdffbe2f` | 6237 |
| `rebuild/lanes/d/p3-real-shape/legacy-fixture.cjs` | `e94ceba9064be78f556d69029009e7730b99087d5565d1d2906a36f31ab48ff3` | 14599 |
| `rebuild/lanes/d/p3-real-shape/real-shape-support.mjs` | `7c27dad17968b9c1d98f5f6943e4e63549c279b4add4cd7eb0979796387d5bf1` | 17085 |
| `rebuild/lanes/d/p3-real-shape/real-shape-walk.test.mjs` | `ee82c8045c9e453b89e9acfdedb9224fb513505f9e12910389d488d7853013cb` | 16642 |
| `rebuild/lanes/d/p3-real-shape/real-shape-capture.test.mjs` | `ade7312b59412bf2532e565bf8c17de721ff9c49cea9fd8336aa512c54ec47c7` | 12651 |
| `rebuild/lanes/d/p3-real-shape/bar-admit.test.mjs` | `3145ebbcec86822c747a4492e43417e2a9fbc21714c6b04f0018ab6d388c9e82` | 18412 |
| `rebuild/lanes/d/p3-real-shape/bar-keep.test.mjs` | `f0268238eb0116d266f402be08f79f3acbc2249866a120a4b8c4879777a1a8a1` | 20418 |
| `rebuild/lanes/d/p3-real-shape/r1-fixes.test.mjs` | `6ab4bd3598050463f6e929efcb844f2b1ea6016e2b0f641de537ed5dbf993c97` | 14700 |
| `rebuild/lanes/d/p3-real-shape/q1-producer.test.mjs` | `9e5d647e5fc02cdb6b319867f4ecb98984058f0a3b91f9a4717d60cf3ed6c8c4` | 7603 |

**2.5 NOT declared, named so the omission is a decision.** The two P3-LAYOUT-V2 lane cells
`rebuild/lanes/d/p3-layout-v2/layout-v2.test.mjs` and `projector-parity.test.mjs` are NOT declared
product and `rebuild/lanes/d/p3-layout-v2/` is NOT added to `CHILD_ROOTS`. `DECISIONS:523` names
"the lanes/d/p3-real-shape cells" and only those; the `:487` stop-7 rule makes a `lanes/d` file
product only when a declared child EXECUTES it, and no declared child of this package does. They are
given a CI home by exact path in the same `rebuild.yml` step as the real-shape cells
(`DECISIONS:117 (4)`, `:186 (3)`) so they are run on both runners, and they are raised to the PM as
this package's one open question rather than settled by the author. The eight lane D **documents**
this branch adds (`P3-REAL-SHAPE-SPEC.md`, its two spec reviews, the two author reports and the
three build reviews) are Markdown, declared by no package in the chain, and are not declared here.
`rebuild/DECISIONS.md` and `rebuild/slice/pwa/DEPLOYS.md` move with the tip merge and are outside
every package's product map by the same standing practice.

**2.6 Everything else is CARRIED byte-identical.** 182 paths, `pre === post`, re-pinned at S7's own
post. That includes all 45 tracked `rebuild/engine` files, `rebuild/coach/engine-revision.cjs`,
`rebuild/m3/setup/port/**`, `rebuild/m4/import/replay-registry.cjs` and `replay-core.cjs`, and the
whole of `rebuild/m3/w7-preview/today/**` that S4, S5, S6 and S7 sealed apart from the four cells of
2.2. If the runner's recomputation finds a twenty-fifth moved path, that is a finding and it comes
back to the PM; it is not absorbed into this list.

## 3. The bar

**3.1 The two author reports' bar tables, reproduced independently by author and reviewer and
spot-checked by the PM (`DECISIONS:522`, `:523`).** Run with `TZ=America/New_York` and
`MEASURED_TEST_NOW=2026-09-03`. The right-hand column is P3-REAL-SHAPE's at `1f6a1a9`/`6074c4f`;
the left is P3-LAYOUT-V2's at `848512c`, which is this head's product state.

| suite | LAYOUT-V2 pass / fail | REAL-SHAPE pass / fail |
| --- | --- | --- |
| `rebuild/lanes/d/p3-real-shape/*.test.mjs` | 56 / 0 | 56 / 0 |
| `rebuild/lanes/d/p3-layout-v2/*.test.mjs` | 6 / 0 | n/a (red first 1 / 5) |
| `rebuild/lanes/d/p3-port-fix/*.test.mjs` | 35 / 0 | 35 / 0 |
| `rebuild/m3/w7-preview/import/test/*.test.mjs` | 35 / 0 | 35 / 0 |
| `rebuild/m3/w6/test/*.test.*` | 587 / 0 | 587 / 0 |
| `rebuild/lanes/d/plan-edit/*.test.*` | 90 / 0 | 90 / 0 |
| `rebuild/lanes/d/import-retract/*.test.mjs` | 13 / 0 | 13 / 0 |
| `rebuild/m4/import/test/*.test.*` | 90 / 0 | 90 / 0 |
| `rebuild/m3/w7-preview/today/test/*.test.*` | 656 / **5** | 661 / 0 |
| `rebuild/coach/test/*.test.*` | 234 / 0 | 234 / 0 |
| `rebuild/m4/workout/test/*.test.cjs` | 214 / **11** | 214 / **11** |
| **total** | **2016 / 16** | **2015 / 11** |

The ELEVEN in `m4/workout` are pre-existing, unmoved and not this package's: nine fail at module
load on `PERFORMED_W6_DIR` and two are the engine byte pins `H3/SUP-3` and `H3/SUP-5` over
`rebuild/engine/merge.cjs` and `today.cjs`, whose numstat over the whole of both rounds is EMPTY.
**THE FIVE in `today` ARE THIS PACKAGE'S, AND THIS PACKAGE IS WHAT TURNS THEM GREEN.** They are
byte-pin cells, not behaviour: `food.test.mjs:826` N1.18, `machine-settings-ui.test.mjs:742` S10,
`problem.test.mjs:1594` N2-08 and the two re-pin cells at `setup.test.mjs:2348` and `:2390`, all over
`today-bindings.mjs` and `workout-host.mjs`. `setup.test.mjs:2306-2345` spells the licence out: a
pinned file is exempt exactly while it stands at the post `S<n>`'s own spec declares for it, read
from `packages/<SPEC>.json` `product[file].post` youngest-first over `CHILD_SPECS`. The P3-LAYOUT-V2
author refused to mint that licence from the lane and left all five red, which is the correct refusal
and which `DECISIONS:523` recorded. The declaring package is this one: 2.1 declares both files with
their posts and 5.1 (7) puts `'S8'` in `CHILD_SPECS`, and the five go green here and only here.

Red-first is proved and is not a claim. P3-REAL-SHAPE opened 32 red / 13 green against the unchanged
tree at `82faa80`, with the spec's invert/stay count settled at 8 / 7; P3-LAYOUT-V2 opened five red
and one green control at `2d0fe12`. This package's own red-first is the refusal chain of section 5.1,
recorded verbatim with exit codes and log paths in `rebuild/lanes/b/S8-PREP-AUTHOR-REPORT.md`.

**3.2 THE DECIDING CELL, which is the reason this package is worth sealing.** The real-shape bundle
sealed by the REAL `port.cjs`, imported on the SHIPPED page booted by `today-entry.mjs`, on a
fresh-start phone whose setup names the same lifts by name: it ADMITS with no refusal line; the next
morning on the same IndexedDB it ADOPTS; the U-day card is ready with the FILE's total and the file's
handle ids (`hack`, not `hack-squat`) and the file's n / sets / hi / day; the L-day card is ready
with the file's L total, the bodyweight raise and the hold lift each prescribing (D-L2-c, D-RS-h,
D-RS-BAR-b); one pre-import session is re-keyed onto the file's lifts with the document's slot count;
Edit My Week opens. The nothing-lost census is 16 / 16 file lifts and 16 document lifts (12
corresponded, 4 shared by id and name, 1 tombstoned), with reads, logs, events, session days and the
pre-import session all present.

**3.3 The S7 children, re-executed.** All 24 children `packages/S7.json` declares are re-declared by
S8 and must be OBSERVED exit 0 at their needles under `--ci`. Several execute a file this package
MOVES, so their needles are RECOMPUTED on this head rather than copied: `today-17` (five cells and
two product files), `w6-local-source`, `w7-import`, `d-plan-edit`, `d-port-admission`, `m4-import`
and `m4-import-production`. The five `s7-sup-*` children and `engine-files-differential` are
re-pointed at this package's own `s8-*` cells; every needle that moves is stated in the author
report with its old and new value.

**3.4 The package's own child.** `d-real-shape`, argv the six lane cells of 2.4, needle measured on
this head. It is the Y1 own-child obligation (`MIN_OWN_CHILDREN = 1`): at least one declared child
must execute a file this spec declares with role `new`.

**3.5 `--ci --package S8` prints `PUBLIC CI EVIDENCE PASS` at the current tip**, and the artifact is
re-proposed through the runner's own `proposed()` path, never hand-written.

## 4. What does not move

- **No `rebuild/engine` byte.** All 45 tracked files stand at S7's own post; the
  `engine-files-differential` child measures it and the runner recomputes the same comparison itself.
  `engine-history.cjs` is `rebuild/m4/workout/engine-history.cjs`, the projector's layout law under
  m4, and is not an engine-tier file; `DECISIONS:523` required the round to STOP and report if the
  law could not admit both profiles without a byte in `rebuild/engine`, and it did not have to.
- **No coach byte.** `rebuild/coach/engine-revision.cjs` reads
  `ENGINE_REVISION = "M2-S7-PORT-ADMISSION@3fd8d36bd4268f14"` and it stays there for the whole of the
  build, the review and the first authorized `--full`. It moves ONCE, after the sealed run writes
  `rebuild/lanes/b/tooling/receipts/S8.json`, to `M2-S8-REAL-SHAPE@<first 16 hex of the sha256 over
  that receipt's raw bytes>`, by the PM, in the commit after the seal.
- **Nothing the lockdown numstat covers.** `:522` and `:523` both record it EMPTY over
  `rebuild/engine`, `rebuild/coach` and `rebuild/DECISIONS.md`.
- **Zero U+2013 and zero U+2014** on any line either round added or this package writes, measured.
- **The sealed bundle and the six words are unchanged.** `sha256 17216d9c...`, 643364 B, already with
  the owner. The retry after S8 is live needs nothing new from him.

## 5. The two reseal rules of VERDICT-S6.md, applied

**Rule (a) - the standing CI step names the new package INSIDE the package's own post, before
`proposed()`, never at the fast-forward.** `.github/workflows/rebuild.yml` becomes
`node rebuild/lanes/b/tooling/b-package.cjs --ci --package S8` in this branch, the step NAME moves
with it, and the file's post sha in `packages/S8.json` is measured AFTER that edit. `DECISIONS:498`
is why: `rebuild.yml` is a declared product file whose post the sealed run pins, so a step flipped
after the seal refuses `WORKTREE-SOURCE-PIN` on the byte-identity step. The same hunk-set adds the CI
home for 2.4's six real-shape cells and 2.5's two layout-v2 cells. The C5 trip-wire behaves as it did
at S7 and this brief says so in advance: from the moment this branch names `--package S8`,
`rebuild/coach/test/engine-revision.test.cjs` reads that flag and looks for `receipts/S8.json`, which
does not exist until the seal, so the rebuild job is RED at C5 on both OS while
`B PACKAGE S8 PUBLIC CI EVIDENCE PASS` prints on both. That is the `:456` trip-wire working, under
the sealing-window rule below.

**Rule (b) - a cell that recomputes the engine revision from the standing package's receipt reads the
SEALING WINDOW (the parent seal while the standing receipt does not yet exist).** `DECISIONS:499`,
one function `standingSeal(repoRoot)` with `REVISION_RULE` quoted in every refusal, duplicated
verbatim in `rebuild/m4/import/test/production-mapping.test.cjs` (cell P3-M3) and
`rebuild/coach/test/engine-revision.test.cjs`. **Read against S8 that is: standing `S8`,
`receipts/S8.json` ABSENT, so the window branch applies; `packages/S8.json` exists and is
`BRIEF-ACCEPTED`; its `parent.chosen` is `S7`, whose `receipts/S7.json` exists at sha256
`3fd8d36bd4268f145b6feb8052710efca41660a8539b490f212421a9ab4013f1`; so the constant must equal
`M2-S7-PORT-ADMISSION@3fd8d36bd4268f14`, which is exactly where the coach constant stands and where
section 4 keeps it.** `P3-P6-SEAL-WINDOW` in `production-admission.test.mjs` derives its expected
constant through the same rule, so it is green in the window too. All three cells stay GREEN for the
whole S8 window and become trip-wires again the moment `receipts/S8.json` lands, which is the same
sharpness, not less. **None of the three is edited by this package**, and all three are carried
byte-identical under 2.6.

**5.1 What the runner needed before any of this could run, and the round that landed it.** The seven
facts below are S7-TOOLING's seven (`DECISIONS:511`, `:515`) mirrored for this package and nothing
more; the refusal chain is recorded verbatim, with exit codes and log paths, in
`rebuild/lanes/b/S8-PREP-AUTHOR-REPORT.md`. The runner now stands at sha256 `e31dd206...` (259614 B);
it stood at `0fb0570d...` before the round.
(1) `IDS` was `['B-NTC','H3','S3','S4','S5','S6','S7','B1','B2','B4','B3']` and the argv gate at
`b-package.cjs:581` refused any other id outright - `--package S8` printed
`B PACKAGE USAGE REFUSED; exactly: --ci|--full --package B-NTC|H3|S3|S4|S5|S6|S7|B1|B2|B4|B3` and
exited 1 before a byte of any spec was read; `'S8'` now stands directly behind `'S7'`, twelve ids.
(2) `NO_REGISTER_IDS` gains `'S8'`; `S8` is an S- id, so the shape assertion admits it without a PM
by-name ruling, exactly as S3, S4, S5, S6 and S7 were admitted.
(3) `CHILD_ROOTS` gains `rebuild/lanes/d/p3-real-shape/` as its twentieth root; without it the child
of 3.4 is refused `CHILD-ARGV-TARGET` and the Y1 own-child obligation cannot be met at all. The root
is deliberately NOT added to `PUBLIC_TAIL_ROOTS`, for the same reason `p3-port-fix` is not: a child
root says a suite may be EXECUTED, that list says its output may be PRINTED, and this suite runs over
the owner's own import path.
(4) the five `s7-supersede-*.test.cjs` cells each read `packages/S7.json` by path and assert against
S7's own parent and sourceBase, and a retirement is never inherited - only the gate list is - so this
package carries its own `s8-supersede-*` family beside them and `s8-engine-files-differential.cjs`
with it, each copied byte for byte from its s7 sibling and changed only where S7-TOOLING changed the
s6 ones: `S7 -> S8`, the parent `M2-S6-TODAY-CHILD -> M2-S7-PORT-ADMISSION`, the parent's token line
`DECISIONS:490 -> :514`, `packages/S7.json -> S8.json`. No assertion, threshold or red control is
weakened; the diff per file is in the author report.
(5) moving `b-package.cjs` moves `tooling.runnerSha256`, so `packages/H3.json`, `S3.json`, `S4.json`,
`S5.json` and `S6.json` are re-pinned role `edited` and `S7.json` is role `superseded-by-child` over
the parent EXECUTION pin - `S6.json` is in that list because S7 re-pinned all of ITS ancestors the
same way, and leaving it would leave the chain one generation stale.
(6) `F6` and `F7` in `rebuild/lanes/b/tooling/test/pinned-unchanged-and-ruled-substitutions.test.cjs`
take the new lists by literal and by `deepEqual`: `IDS` of twelve, `NO_REGISTER_IDS` of eight,
`CHILD_ROOTS` of twenty.
(7) the five `CHILD_SPECS` cells (`measure/test/boundary.test.mjs`, `today/test/food.test.mjs`,
`today/test/machine-settings-ui.test.mjs`, `today/test/problem.test.mjs`, `today/test/setup.test.mjs`)
gain the `'S8'` literal. This is also the fact that turns the five red byte-pin cells of 3.1 green,
because it is what gives `today-bindings.mjs` and `workout-host.mjs` a declaring package.

**The declaration list after the round.** 224 declared paths: **24 edited**, **17 new**,
**182 carried** and **1 superseded-by-child** (`packages/S7.json`). **The needles were measured, not
carried.** Every one of the 25 children was run the way `children()` runs it, on this head, with
`TZ=America/New_York` and `MEASURED_TEST_NOW=2026-09-03`; the table of old value against measured
value is in the author report, and no needle in `packages/S8.json` was typed from a report.

## 6. The flow, in S7's order with S8's names

1. **Brief accepted by name** - a PM line naming `rebuild/lanes/b/S8-REAL-SHAPE-BRIEF.md`.
2. **Three token lines on the tip**, in order: THEME `M2-S8-REAL-SHAPE`; BRIEF-BY-SHA with the
   sha256 of the accepted bytes and the byte count; GATE-SUPERSESSION in `:514`'s exact shape. The
   runner locates the third by its own sha256 as a UNIQUE line of `rebuild/DECISIONS.md` on
   `refs/remotes/origin/rebuild/t2-client-core`, hashing the line's bytes as the file splits them on
   `/\r?\n/` - the leading `- ` included, the trailing newline excluded - and requires the line to
   end in the terminal word `RULED` and to carry the token
   `GATE-SUPERSESSION <packageId> <carrier>[,<carrier>]` alone in its own `·`-delimited clause.
3. **The author cites all three in `packages/S8.json`** (`authorizations.theme`,
   `brief.acceptedLedgerLine`, `coverage.superseded.rulingLineSha256`), each a sha256 over the exact
   line bytes with no trailing newline. A null `rulingLineSha256` is a HARD refusal:
   `GATE-SUPERSESSION-RULING-NOT-CITED`, exit 1, before the theme and brief obligations are reached.
4. **`--ci --package S8` prints `PUBLIC CI EVIDENCE PASS`**, artifact re-proposed through `proposed()`.
5. **Fable final review** over the sealed candidate (`DECISIONS:439`), independent of the build's own
   reviewers, high effort.
6. **PM `--full` with the private census on the PC**: `PRIVATE ORACLE PRESENT` verdict-only,
   `HISTORICAL TOTAL 45 laws`, `104/104 DETECTED`, `0 HARNESS_ERROR`, LEGACY gates OBSERVED, nine
   SUPERSEDED, `POSTFIX PACKAGE REVIEW-PENDING: 1 open obligation`, exit 2.
7. **The receipt line**
   (`POSTFIX-ACCEPTANCE M2-S8-REAL-SHAPE <commit> rebuild/m4/spec/acceptance-s8-real-shape.json <sha256> ACCEPTED`)
   discharges that obligation.
8. **`rebuild/m4/spec/review-s8-real-shape.json`**, `{status: "ACCEPTED", receipt: {commit, path,
   line, lineSha256}}`, citing the receipt line.
9. **MERGE the tip into the reviewed head. NEVER rebase** (`:467` note 1, `:493` (8)).
10. **Authorized `--full`**: `AUTHORIZED mode=--full`, `SEAL BASE ON THE TIP`, `ENVELOPE AUTHORIZED`,
    `PARENT PINS RE-ASSERTED`, `SEALED RUN RECORDED receipts/S8.json`,
    `POSTFIX PACKAGE PASS M2-S8-REAL-SHAPE` exit 0; `VERDICT-S8.md` names the receipt sha.
11. **The coach constant**, once (section 4). 12. **Second authorized `--full`:
    `AUTHORIZED STEP BYTE-IDENTITY RE-VERIFY`.** 13. **CI green on both OS.** 14. **Fast-forward**,
    with the slice-host deploy trigger `:519` used. 15. **The port retry**, with the same bundle and
    the same six words.

## 7. Carries recorded, not adopted

**The identity trade of `:521`, restated because it must be restated at every round.** After this
change the proof that an imported file is his is the sealed file, the six words and his identity Yes.
The per-lift id equality S7 kept was an accidental content guard no fresh-start owner could satisfy,
and it is gone by ruling, not by accident: a wrong athlete's old-app file on the same week would
admit and adopt on the Yes alone. `P-LABEL` narrows it in one direction only - a file that NAMES a
different athlete is now refused by name where it used to admit and silently not adopt. Kept under
`:472 (a)`, `:521` and `:523`, and told to the owner in plain words in the PM chat.

Also carried and none blocking: `layout.profile` is not tied to `producer.rule_profile` in the law
(unreachable in product, performed slots identical); `m4/spec/configured-history-candidate/construct.cjs:6`
patches the old one-profile string and will throw Unique projector edit (env-gated, in no bar row);
`entry.load` is null for a configuration lift (lane C design; the card opens and prescribes); the
programme digest changed shape and `listImports` on the owner's installation is unverified until the
retry; `TODAY-TREE-CUSTODY` (`:482`, `:501`); the lane B tooling carries of `:501`;
`HOST-DAY-AGREEMENT` (`:485`); `PWA-SPLIT` (`:480`); and `:472` BLOCKER 2 - the real retry is the
measurement of the production producer mapping.
