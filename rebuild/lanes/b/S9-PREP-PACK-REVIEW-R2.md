# S9-PREP-PACK REVIEW R2 - second independent review of the two design-pack pin cells

Ticket S9-PREP-C. Branch `rebuild/b-s9-prep-pack` at `00de876173eeec2e663dc45e9029bba5fa8c4080`,
cut from `rebuild/b-s9-ui-pins` at `da9f8683`. Author report:
`rebuild/lanes/b/S9-PREP-PACK-AUTHOR-REPORT.md`, treated throughout as a hypothesis. First
review: `rebuild/lanes/b/S9-PREP-PACK-REVIEW-R1.md` (REJECT, three BLOCKING, eleven notes).

I synced the branch into the farm and read the whole diff `da9f8683..00de8761` before I read
either the author report or R1. Every number and every refusal string below is one I produced
myself, on the PC (Windows 11, node v24.19.0, `%TEMP%\earned-s9c` at `00de8761`) or in a linux
farm scratch of the PUSHED head (`/home/claude/farm/scratch/wt/s9crevB`, node v22.22.2, 2 CPUs),
or on both. I re-ran the mutation table from my own harness rather than reading the author's.

## VERDICT

**REJECT, and the fix is two fixture rows and not one line of engine.** Both BLOCKING findings
below are guards whose removal turns NOTHING red, which the ticket makes a blocking finding by
rule and which R1 established as this build's precedent. Neither is a behaviour defect: I
measured the shipped engine doing the right thing in both cases. What is missing is the proof,
and the day these two files are declared `role: "new"` the missing proof is what lets a sealed
byte move in silence.

**Everything R1 raised is closed, and I checked it by measurement and not by reading.** All
three BLOCKING findings are FIXED, each red first at the commit the report names, each with a
mutation that now dies on BOTH operating systems. Of the eleven notes, nine are FIXED, one is
DISPUTED and I uphold the dispute, one is a PM ruling nobody can take alone. I re-ran fifteen
of the twenty-nine mutation rows on both operating systems and **every one of them reproduced
the author's count exactly**, which is a real improvement on the first build's table.

**The single most valuable thing in this round is still the real-pack run, and I repeated it
myself rather than carrying R1's.** 904 tracked files, a literal I generated by C.5.1's five
steps, zero refusals, and one flipped byte named by path. Numbers in "what I tried to break".

## THE OWNED-FILES FENCE: CLEAN

`git diff --stat da9f8683 origin/rebuild/b-s9-prep-pack` is five files and 2305 insertions,
zero deletions: `pack-pin.test.mjs` 755+, `approved-pin.test.mjs` 501+, `rebuild.yml` 11+, the
author report 696+, and `S9-PREP-PACK-REVIEW-R1.md` 342+, which R1 committed and which the
second author has not edited. Nothing outside the ticket's owned list is touched.

`rebuild.yml` is ONE hunk, `@@ -295,6 +295,17 @@`, eleven added and zero removed, entirely
inside the region after `:297` and before the comment block at `:298`. Nothing is inserted near
`:232` or near `:306`, so the three lanes' regions merge. Parsed with the repository's own
`yaml` at the pushed head: one job `public-gates`, **28 steps**, matrix
`[ubuntu-latest, windows-latest]`, no job-level shell default, the new step present **exactly
once**, command

```
node --test rebuild/lanes/c/ui-port/pack-pin.test.mjs rebuild/lanes/c/ui-port/approved-pin.test.mjs
```

with no glob and no quoting, so it is shell-neutral on both runners. **Zero U+2013 and zero
U+2014** in the two cells, the author report and the eleven inserted `rebuild.yml` lines
(counted by byte, not by eye). `git status --porcelain` on the PC worktree before I wrote this
file: empty.

## BLOCKING

### B1. PACK-PIN's pack-root `lstat` is a green-while-changed guard with no row: mutated to `stat`, a link standing at the pack root reads GREEN on BOTH operating systems

`packPin` opens with `fs.lstatSync(packRoot)` and the cell's own comment at `:187-:188` states
the reason in terms: "lstat, so a link standing where the pack should be is absent too rather
than quietly walked through". **Nothing in either cell exercises that sentence.** The two
PACK-ROOT-ABSENT rows use a path that does not exist and a path that is a regular FILE, and
`stat` and `lstat` agree on both.

Measured by me, the single named edit
`fs.lstatSync(packRoot)` -> `fs.statSync(packRoot)`, applied to a copy of the cell, run, deleted:

```
linux    mutant: 35 tests, 1 fail  -> 0 non-real rows red
Windows  mutant: 35 tests, 1 fail  -> 0 non-real rows red
```

That is the ticket's rule fired exactly: a guard whose removal turns nothing red. And it is not
only a coverage gap, because I then built the case the guard exists for. A directory junction
standing at the pack root, pointing at a directory whose bytes match the literal line for line:

```
linux    SHIPPED engine  => ["PACK-PIN PACK-ROOT-ABSENT /tmp/s9crevB-hold-ZwllDp/approved-2026-09-18"]
linux    stat MUTANT     => []
Windows  SHIPPED engine  => ["PACK-PIN PACK-ROOT-ABSENT C:/Users/.../s9crevB-hold-vcFli3/approved-2026-09-18"]
Windows  stat MUTANT     => []
```

**The shipped code is right and the mutant is green while the tree has changed.** This is R4
section 2's green-while-changed one level up: not a pinned file replaced by a link to a twin,
but the whole pack root replaced by a link to a twin. It is the same attack the cell closes at
`:411-:460` for files and directories INSIDE the pack, and the root is the one entry the walk
never sees.

**Why it is blocking rather than tidy.** S9 declares this cell `role: "new"`, so from S9 on
`lstatSync` at `:190` is a SEALED byte. Today anyone may change it to `statSync`, push, and
watch 55 rows stay green. After the literal is taken, adding the row costs a reseal child.

**Remedy: ONE fixture row, no engine change.** A directory junction needs no privilege on
either operating system (the cell already relies on that at `:442`, `:601` and `:677`), so the
row builds on both: make a good fixture pack, junction to it from a second mkdtemp folder,
point `packPin` at the junction, and assert
`["PACK-PIN PACK-ROOT-ABSENT " + toPosix(linkPath)]`. I have run exactly that assertion on both
operating systems against the unmodified engine and it passes.

### B2. APPROVED-PIN's `lstat` guard is reported as unclosable on Windows (mutation A4, integrator item 9). It is closable, on both operating systems, by a DANGLING junction, and the stated reason is wrong

Section 6.2 and integrator item 9 say: "A file symlink is the only entry for which `lstat` and
`stat` disagree at a file path, so on Windows there is no row an unprivileged process can
build that kills A4." I reproduced A4 first, so the finding is not a disagreement about the
measurement:

```
linux    A-lstat-to-stat mutant: 20 tests, 1 fail -> 1 non-real row red
Windows  A-lstat-to-stat mutant: 20 tests, 1 fail -> 0 non-real rows red   (A4 confirmed)
```

**But the stated reason is false.** A DANGLING link is a second entry for which the two
disagree: `lstat` succeeds and reports a symlink, `stat` throws `ENOENT`. A directory junction
needs no privilege on Windows, and deleting its target leaves it dangling. Measured by me, a
junction at the path of a named reference, before and after its target is removed:

```
                              SHIPPED (lstat)                      stat MUTANT
linux    LIVE junction     => NOT-A-REGULAR-FILE <ref>             NOT-A-REGULAR-FILE <ref>
linux    DANGLING junction => NOT-A-REGULAR-FILE <ref>             MISSING <ref>
Windows  LIVE junction     => NOT-A-REGULAR-FILE <ref>             NOT-A-REGULAR-FILE <ref>
Windows  DANGLING junction => NOT-A-REGULAR-FILE <ref>             MISSING <ref>
Windows  lstat sees        => symlink=true file=false
Windows  stat sees         => THREW ENOENT
```

The two refusal strings differ, on both operating systems, so **one fixture row kills A4 on
the PC**, and the cell stops carrying a guard that is decoration on the machine of record.
Again: the shipped engine is right in every one of the four cells above. What is missing is the
row, and what is wrong is the sentence in the report and in the integrator list that tells the
next round not to bother looking.

**Remedy: ONE fixture row, no engine change** (and, because the report's sentence will be read
on the day of the re-measure, integrator item 9 has to be corrected with it). The same
construction also strengthens PACK-PIN: a dangling junction inside the pack is named
`PACK-PIN NOT-A-REGULAR-FILE <path>` by the shipped walk on both operating systems, which I
measured, and that is a second entry `stat` cannot see either.

## R1 FINDING BY FINDING: FIXED, STILL OPEN, OR DISPUTE UPHELD

Each verdict below is my own measurement at the fix head, on both operating systems, not a
reading of the author's answer.

| R1 finding | verdict | the measurement that settles it |
|---|---|---|
| **B1** APPROVED-PIN has no ORPHAN refusal | **FIXED** | ORPHAN exists and is load bearing: removing the loop kills **5** rows on linux and **5** on Windows; removing its byte sort kills **1** and **1**. Red first reproduced by me at `baf0e88a` (20 tests, 14 pass, **6 fail**) and green at `3601e011` (20 / 19 / 1) |
| **B2** the ignore list was wider than C.5.1's | **FIXED** | `isIgnored` probes, identical on both OS: `quality/run`=false, `quality/run/x`=true, `quality/__pycache__`=false, `a/__pycache__/b.pyc`=true, `quality/my__pycache__notes.md`=false. Widening the prefix back to the bare `quality/run` kills **2** rows on both; widening the segment rule back to any segment kills **1** on both. Red first at `8195f1df` (35 / 29 / **6**), green at `264f7d00` (35 / 34 / 1) |
| **B3a** `label()`'s repo-relative branch had no row | **FIXED** | mutant kills **1** row on linux and **1** on Windows (was 0 and 0) |
| **B3b** the backslash assert had no row | **FIXED** | mutant kills **1** and **1** (was 0 and 0) |
| **B3c** the literal sortedness clause had no row | **FIXED** | mutant kills **1** and **1**, and the silent re-sort is gone: an unsorted literal now fails hard |
| **B3d** P13's Windows column claimed a kill it did not have | **FIXED, and fixed by a row rather than a correction** | `P-named-once` now kills **2** on linux and **1** on Windows. The new row `R4 N1.2 (c)` puts a directory junction AT a pinned FILE, which needs no privilege on either OS. R1's structural argument was right and the author escaped it |
| **N1** P1 also turns the real row green | **FIXED** | re-measured: P1 kills 3 rows and the real row turns GREEN, on both; section 6.1 now flags it as it flags A2 and A6 |
| **N2** the rest of the table is approximate | **FIXED** | I re-ran 15 of the 29 rows from my own harness on both operating systems (P1, P2, P3, P4, P5, P6, P9, P10, P12, P13, P14, P15, P16, P18, P19, A1, A2, A3, A4, A5, A7, A8, A9). **Every count matched the author's table on both columns.** A rebuilt table that reproduces under an independent harness is the right answer to R1 N2 |
| **N3** an unreadable pinned file THROWS | **DISPUTE UPHELD** | the source fact is unchanged (`walk` calls `readFileSync` with no guard) and the author declines to add a seventh refusal in this round, for the reason R1 itself gave. I agree: it is LOUD RED, which is a different class from a silent green, and the vocabulary is a PM ruling once it is sealed. It is integrator item 11. Neither of us can measure the linux half (the farm scratch runs as uid 0) |
| **N4** the parser was tolerant in two ways | **FIXED** | two spaces now fail hard (`literal line is not ...`), a leading space fails hard, an unsorted literal fails hard. Uppercase hex, a malformed line and a duplicate path still fail hard |
| **N5** two residuals were not carried | **FIXED** | (a) is a row, a header paragraph and integrator item 12(a); I re-measured it (`quality/__pycache__/teeth.py` inside the pack => `[]`). (b) is integrator item 12(b) with R1's conclusion kept: state it, do not widen |
| **N6** the rows pinned a foreign machine's errno | **FIXED** | both rows now print rather than pin. Measured on the PC: `PACK-PIN file-link on win32 is unbuildable unprivileged, code: EPERM` and the same line for APPROVED-PIN. A runner that succeeds takes the full attack branch |
| **N7** `APPROVED-PIN` is a token in two files | **FIXED as far as a report can fix it** | integrator item 14. I confirm the cell reads `design.APPROVED` directly and never calls `readApproved`, so attack-plan D7 does not land |
| **N8** a duplicate name in `design.APPROVED` is green | **FIXED as a stated decision** | re-measured `D4 same file twice => []` on both OS; the header says so and the live case is covered by the row that asserts the real list holds no duplicate |
| **N9** the red-first counts were the Windows ones | **FIXED** | section 3 names its OS and carries R1's linux figures with attribution |
| **N10** the day-of cost against the real pack | **FIXED, and I re-measured it independently** | see below; my figures are close to R1's and I did not carry his |
| **N11** the step makes this branch's CI red | **STILL OPEN, correctly: it is a PM ruling** | the author did not decide it alone and did not touch `rebuild.yml` in the fix round, which is right on both counts |

**Nothing the fix round did broke anything I can find.** The three existing rows whose
expectations changed (`C.5.3`, `the file list is re-read`, `the verdict comes from THIS cell's
literal`) each gained true lines rather than losing an assertion; I checked each against the
engine. The ignore-list fix WIDENS the pin in both places and narrows it nowhere, so F.2
STOP-9(b) does not fire. No law, guard, pin or test was weakened, skipped or deleted.

## NOTES

**N1. Integrator item 3 carries a STALE number, and it is the number the day of the re-measure
turns on.** Item 3 says mutation P10 "turns 16 rows red on Windows". Section 6.1 says 23, and
23 is what I measured (`P-sep-not-slash`: **0** non-real rows red on linux, **23** on Windows).
16 was the FIRST build's figure over 25 rows. The integrator list is the part of this document
that is meant to survive to a day months from now; a stale count in it is worth one edit.

**N2. The row titled "the refusal vocabulary is exactly six, and every one of them is reachable
above" does not assert reachability.** Both cells' last row asserts the length, the uniqueness
and the shape of the strings, and nothing more. The title is a claim the row does not make. One
honest closure: derive the set of refusal verbs from the strings each engine can emit over the
fixture rows and compare it to `REFUSALS`. Cheap, and it makes the export the drift detector
the header says it is. Titles matter here because this row is the one a later round reads when
it wants to add a seventh refusal.

**N3. A literal line naming a path outside the pack is benign, and the cell should say so
rather than leave it to be rediscovered.** The attack plan asked for `..`, a leading slash, a
drive letter and `./` to be refused as literal defects. They are not, and they do not need to
be: the engine never opens a literal path, it only asks a Map built by the walk. Measured, both
operating systems, with each line in correct sorted position:

```
"../../etc/hosts <hex>"    => ["PACK-PIN MISSING ../../etc/hosts"]
"/etc/hosts <hex>"         => ["PACK-PIN MISSING /etc/hosts"]
"C:/Windows/win.ini <hex>" => ["PACK-PIN MISSING C:/Windows/win.ini"]
"./x <hex>"                => ["PACK-PIN MISSING ./x"]
```

No traversal is reachable. But the cell already fails HARD on a backslash in a literal path for
exactly the "defect in my own constant" reason, and `..` is the same kind of defect, so the
asymmetry is worth one sentence either way.

**N4. `Object.hasOwn` in APPROVED-PIN is a real hardening with no row.** Replacing it with
`file in literal` turns nothing red on either OS. It is NOT a green-while-changed: a
`design.APPROVED` naming `constructor` or `toString` still goes red, with `MISSING` instead of
`UNLISTED`. I measured the shipped behaviour and it is the better one:
`["APPROVED-PIN UNLISTED constructor","APPROVED-PIN UNLISTED toString","APPROVED-PIN UNLISTED __proto__"]`
on both operating systems. A note, not a finding, because both branches are red.

**N5. The two cells' fixture prefix collides with the lane's own scratch prefix.** Both cells
`mkdtemp` under `s9c-*` in the system temp folder, which is the prefix this ticket also
mandates for a lane hand's `.cmd`, `.log` and `.done` files on the PC. Nothing broke and the
cells clean up after themselves in a `finally`, but a future hand who deletes `%TEMP%\s9c-*`
while a run is in flight will produce a failure that looks like a defect in the pin. One
character (`s9cpin-`) would remove the trap. I used `s9crevB-` for my own fixtures for this
reason.

**N6. The design lane's own checkout is the only place the ignore list is exercised for real,
and I re-measured it.** The farm's checkout of `rebuild/c-ui-0-gates` at `ecbef86a` carries
**913 files on disk** under the pack root against **904 git-tracked**, so nine untracked gate
leavings, and the engine ignored exactly those nine and named none of them. That confirms R1's
count independently and is integrator item 15.

## WHAT I TRIED TO BREAK AND COULD NOT

**THE BAR, RE-RUN BY ME, BOTH OPERATING SYSTEMS, AT THE PUSHED HEAD `00de8761`.**

| suite | the PC (Windows 11, node v24.19.0) | linux farm scratch (node v22.22.2) |
|---|---|---|
| `rebuild/lanes/c/ui-port/pack-pin.test.mjs` | 35 / 34 / **1** | 35 / 34 / **1** |
| `rebuild/lanes/c/ui-port/approved-pin.test.mjs` | 20 / 19 / **1** | 20 / 19 / **1** |
| the two together, the CI step's exact command | **55 / 53 / 2** | **55 / 53 / 2** |
| `rebuild/m3/w7-preview/today/test/design.test.cjs`, unchanged | **11 / 11 / 0** | not run |

**Zero skipped and zero todo on both**, no `skip`, no `todo` and no platform conditional that
turns a row off. The two failures are the two real rows, with byte-identical text on both
operating systems and not a backslash in any of it:

```
PACK-PIN PACK-ROOT-ABSENT rebuild/m1/approved-2026-09-18
APPROVED-PIN UNLISTED rebuild/m1/approved-2026-09-08/Earned-refinement-A.html
APPROVED-PIN UNLISTED rebuild/m1/approved-2026-09-08/Earned-additions-C-approved.html
```

So the ticket's question is answered and I confirm the author's answer: **the refusal this
branch prints is `PACK-ROOT-ABSENT`, not `LITERAL-EMPTY`**, because the checkout is judged
before the cell. `design.test.cjs` is 11 / 11 / 0 on the PC, and `git status --porcelain` in
`%TEMP%\earned-s9c` was empty before I wrote this file, so `design.cjs` was read and not
disturbed.

**RED FIRST, RE-RUN BY ME AT THE FIX COMMITS** (linux; the report's figures are the PC's and
they agree row for row):

```
8195f1df (f1)  pack-pin 35 / 29 / 6   approved-pin 16 / 15 / 1
264f7d00 (f2)  pack-pin 35 / 34 / 1   approved-pin 16 / 15 / 1
baf0e88a (f3)  pack-pin 35 / 34 / 1   approved-pin 20 / 14 / 6
3601e011 (f4)  pack-pin 35 / 34 / 1   approved-pin 20 / 19 / 1
```

Both fixes are red first against the UNCHANGED engine and green only when the engine moves.

**THE ONE TEST NO FIXTURE CAN GIVE, RUN AGAIN AND NOT CARRIED.** In a farm scratch I generated
a literal for the REAL pack by C.5.1's five steps myself: `git ls-tree -r --name-only HEAD --`
the pack root at `ecbef86a` in the farm copy of `rebuild/c-ui-0-gates`, sha256 over
`git cat-file blob HEAD:<path>` for each, one line `<pack-relative path> <space> <64-hex>`,
sorted by path BYTES. I pointed the cell's own engine, extracted verbatim, at the pack:

```
head=ecbef86a   literal lines: 904
cold: refusals=0   45 ms
warm: refusals=0   35 ms
files on disk under the pack root: 913 ; git-tracked: 904
```

I then copied the pack to a scratch of my own, flipped one bit of one real state record and
re-ran:

```
["PACK-PIN MISMATCH quality/baseline/states/T-39-dawn.json"]      (one refusal, and only that one)
```

That is C.5.1 step 5 executed against the real thing: a git enumeration and a working-tree walk
agree exactly on all 904 files, which is the `.gitattributes` argument taken as a measurement.
The literal is a test value and is committed nowhere. It remains a LINUX measurement; step 5 on
Windows is still the integrator's job and is the step I would least like skipped.

**THE DAY-OF COST, my own figures.** The cells' synthetic 904-file row printed **15 ms** on
linux and **58 ms** (and 72 ms in the combined run on a PC shared with at least six lanes) on
Windows. The real 904-file pack walked in **45 ms cold and 35 ms warm** on linux. The whole
two-cell step runs in well under a second on both machines.

**THE BLIND ATTACK PLAN, RUN AS REAL FIXTURE CASES ON BOTH OPERATING SYSTEMS.** Every line
below was produced twice, once on the PC and once in the farm scratch, and the two outputs are
identical string for string.

```
A4  rename                => MISSING states/STATE-INVENTORY-DRAFT.md + ADDED states/STATE-INVENTORY-FINAL.md
A5  case-only rename      => MISSING README.md + ADDED Readme.md            (ON WINDOWS TOO: nothing case-folds)
A9  zero-byte file        => enumerated and hashed (e3b0c442...), green; then written => MISMATCH quality/EMPTY.txt
A10 path with a space     => green, then MISMATCH "ref/a b.png"
A10 non-ASCII path        => green, then MISMATCH ref/cafe.png (e-acute), identical on both OS, nothing normalizes
A12 CRLF to LF            => MISMATCH quality/crlf.txt      (hashed over raw Buffer bytes)
A12 invalid UTF-8 byte    => MISMATCH quality/raw.bin       (0x80 0xFF is not mangled by a decode)
C1  astral vs U+E000      => ADDED order ... [0x69] [0xEE] [0xF0], byte order, as REAL FILES on both OS
C2  states.md before states/x  => true (0x2E before 0x2F), green
B1  decoy app/quality/run/x.js => pinned, and its move named (the cell's own anchor row)
B2  near miss quality/my__pycache__notes.md => pinned, green
A13 a file added BESIDE the pack root => [] (correctly outside the pin)
D2  design.APPROVED not an array      => LIST-EMPTY (not a silent pass over zero iterations)
D3  ../../../../etc/hosts, C:/Windows/win.ini, constructor, toString, __proto__ => UNLISTED, never found
D4  the same file named twice         => [] (the stated residual, reproduced)
D6  the list shrinks                  => UNLISTED <new> + ORPHAN <dropped>
```

A4 is the row R1's plan asked for hardest: the engine reports BOTH halves of a rename from ONE
run and does not bail on the first refusal. A5 is the one I expected to find a defect in and
did not: on NTFS the walk reads `Readme.md` back and the comparison is case-sensitive, so a
case-only rename is two named lines rather than a silent green.

**THE TWO MUTATIONS THE AUTHOR SAID TO ATTACK FIRST, re-measured on both operating systems.**
A2 (drop `UNLISTED`) kills **5** fixture rows AND turns the real row GREEN, on both. A8 (drop
the `ORPHAN` loop) kills **5** on both. P1 (drop `PACK-ROOT-ABSENT`) kills **3** and turns the
real row GREEN, on both. **P10 is still the best argument in this build for the bar of record
being the PC**: the separator mutation moves **0** rows on linux and **23** of 35 on Windows.

**The engine really is shared by the fixture rows and the real rows**, which I checked by
extracting it verbatim from each cell and running it standalone; there is no second code path,
no cache between calls, and the real rows are unconditional.

## FOR THE S9 INTEGRATOR: WHAT I WOULD ADD TO THE AUTHOR'S FIFTEEN

The list of fifteen is accurate as far as I checked it, it carries R4 N2 and R4 N10 as items 1
and 2, and I would keep all of it with two corrections and one addition.

1. **CORRECT item 3's number.** P10 turns **23** rows red on Windows, not 16. The point it
   makes is stronger than the number it cites.
2. **CORRECT item 9.** APPROVED-PIN's `lstat` clause DOES have a Windows row that kills it: a
   DANGLING directory junction, which needs no privilege. Item 9 as written tells a later round
   there is nothing to look for. See BLOCKING B2 for the measurement.
3. **ADD: the pack root itself is the one entry the walk never sees.** If the day-of run ever
   prints `PACK-PIN PACK-ROOT-ABSENT` for a path that visibly EXISTS on disk, the pack root is
   a link and the refusal is correct. See BLOCKING B1.

## STOP CONDITIONS

**None fired, and I checked STOP-9(b) hardest**, because it stops the round if the pin is
NARROWED. The fix round widens the pin in two places (a regular file at `quality/run`, and a
regular file whose last segment is `__pycache__`, are now inside it) and narrows it nowhere;
`quality/**` and `README.md` are inside. STOP-9(a) does not fire: MISMATCH, MISSING, ADDED and
NOT-A-REGULAR-FILE all name the path and all six of B.8's rows fail by name. STOP-10 and
STOP-11 are day-of conditions and belong to the integrator. **No file outside the ticket's
owned list needed an edit**, so nothing needs routing.

**One thing that is not a STOP but is the PM's and not the author's (R1 N11, unchanged):** the
new `rebuild.yml` step makes this lane branch RED in CI, and the same red arrives on the chain
tip the day it merges. The honest choices are to leave the step red with its comment, to hold
the hunk back until the re-measure, or to merge the pack first. I agree with both previous
hands that an author cannot take this alone.

## WHAT I DID NOT VERIFY

1. **GitHub CI.** No `rebuild-public` run exists for any head of this branch that I have seen.
   My both-OS evidence is this PC and a farm scratch of the pushed head. I did confirm the step
   is shell-neutral, unglobbed and present exactly once, and that the job carries no shell
   default.
2. **The real pack on Windows.** `rebuild/m1/approved-2026-09-18/` is not on this branch and
   not on the PC. My zero-refusal real-pack run is linux only. C.5.1 step 5 on the PC remains
   the integrator's job.
3. **Fourteen of the twenty-nine mutation rows.** I re-ran 15 on both operating systems and all
   15 reproduced; P7, P8, P11, P17, A6 and A10 I read but did not re-run.
4. **The linux half of R1 N3** (an unreadable pinned file). The farm scratch runs as uid 0, so
   it cannot be built there, and I did not repeat R1's DENY-ACE measurement on the PC. The
   absence of a `try`/`catch` around `readFileSync` is a source fact on both.
5. **Line numbers cited inside `b-package.cjs`** (`:1201`, `:1970`, `:1972`) and inside
   `design.test.cjs`. I did read `rebuild/m3/w7-preview/today/design.cjs` and confirm `APPROVED`
   is a frozen two-entry array, that its only `require` of a repository file is
   `./plain-copy.cjs`, and that `readApproved` throws `APPROVED-PIN FAIL:` against
   `entry.sha256` - so the cell reading `design.APPROVED` directly is the right call and
   attack-plan D7 does not land.
6. **`scripts/` and any local runner.** `scripts/` is outside the farm's include list, so I
   could not check whether a local bar globs `rebuild/lanes/c/ui-port/`.
7. **I read nothing forbidden.** No `rebuild/conform/private`, no `src/history.js`, no
   `ledger/`, no `EarnedPort`, no `port-real.log`, no protected soak, no design-lane worktree.
   I ran no `b-package.cjs`, no seal generator, nothing that writes a receipt or an artifact,
   and I created no junction into any tree: every link I made is inside a `mkdtemp` folder I
   made and removed. My farm scratch `/home/claude/farm/scratch/wt/s9crevB`, my Windows scratch
   `%TEMP%\s9c-revB-scr` and my `s9c-revB-*` logs are mine and hold only files I wrote; the
   mutation harness wrote a mutated COPY, ran it and deleted it every time. The PC worktree is
   untouched apart from this file. **No owner measurement is anywhere in this review**: every
   fixture byte is generated, and the one real-pack literal is a test value committed nowhere.

---

Reviewed by the Earned lane hand acting as independent reviewer for S9-PREP-C, second round.
This file is the whole of my change; I touched nothing else in the worktree.
