# VERDICT — M2-B-NTC-NATIVE-TREND-CONTEXT · lane B seal · REVIEW-PENDING

Branch `rebuild/lane-b-ntc`, on top of `2a4c26f`: spec citation commit `bc2233f`, **artifact commit `3ab4f2c5862f0b000b9cf7e34253341de6842625`**; this verdict file is committed on top of that commit and is pinned by nothing. sourceBase `7b1678a4a8449053000d52a0a8e9e070f32a240e`. Runner `rebuild/lanes/b/tooling/b-package.cjs` sha256 `eedabccd5b8145bfb07fe653bfb14f9483af8ca52619e813c70bf699407dd612`; spec `rebuild/lanes/b/tooling/packages/B-NTC.json` sha256 `9d8552d9a20af61f8058a31577c23e7d720532e2824be4e1db3f3226041a1a96`.
Every line below was executed on the owner's Windows PC (node v24.18.0, `TZ=America/New_York`, `MEASURED_TEST_NOW=2026-09-03`). No private value, count, hash or prose appears in this file. No PASS word is claimed for this package.

## Authority
- Brief `rebuild/lanes/b/BRIEF-B-NTC-NATIVE-TREND-CONTEXT.md` sha256 `4aa94069d5a8680181618825f3294334d9768acf960daa58c1e137fc6106572c`, accepted by **`DECISIONS:126`** (lineSha256 `fc4bfef5ff1796ec7deec214c0f2c7550a2d4d6d4b2b8018344b190b5c5bd5e1`); spec `status: BRIEF-ACCEPTED`. `:123` is the stale earlier brief-by-sha line and is not cited.
- Theme **`DECISIONS:122`** (lineSha256 `3ca8bf169fe96f3cd0ec51ea68b1406e76fdaa11d67e38cadb0559319d272384`). Owner `DECISIONS:60`. Contract `DECISIONS:49`, byte-equal to the parent artifact's own contract line. Both of this package's own lines are resolved in Git on `refs/remotes/origin/rebuild/t2-client-core`, owner and contract at the parent receipt base `b045e61`.
- Parent (single, immutable): `rebuild/m4/spec/acceptance-native-carriers.json` sha256 `e940359b684b90e2e92ae325a86c018f91a7aa27bec7c5466165116657c2201a`, review `review-native-carriers.json` `9b0918d6…`, receipt `DECISIONS:104`, reviewed at `b95ccca879e371b5ba225ad12cae612ec89469ba`.

## The closed cumulative profile artifact
`rebuild/m4/spec/acceptance-b-ntc-native-trend-context.json` · sha256 **`1588567a55d1d5a991ef6ea7c26b9f6ecae1594bf0844ac23eb9fed422adeb17`** · 33 864 B · lives in commit `3ab4f2c5862f0b000b9cf7e34253341de6842625`.
Envelope `rebuild/m4/spec/review-b-ntc-native-trend-context.json` = `{version: 1, status: "PENDING", receipt: null}`. No receipt is written and no ACCEPTED envelope is claimed.
Coverage: the parent artifact's own nine `coverage.byChild` gates, byte for byte, each carried by a SUCCESSOR under `MOVES_RULING=DECISIONS:113 B-NTC-INHERITED-1`; `coverage.moves` is `{}`; three enumerated substitutions. The runner recomputed the whole artifact from the spec, its own bytes and the pins on disk, and did not refuse.

## Terminals — executed, exit codes measured
1. `node rebuild/lanes/b/tooling/b-package.cjs --ci --package B-NTC` → `B PACKAGE B-NTC PUBLIC CI EVIDENCE PASS — public evidence only, NOT the package verdict; the 19 original gates, the private oracle and independent exact-artifact acceptance remain separate, and POSTFIX PACKAGE PASS is unavailable on this mode at any time`, **exit 0**. One OPEN line — `B PACKAGE B-NTC OPEN independent exact-artifact acceptance PENDING` — which is not CI-blocking and cannot close before the PM's receipt. Immediately before the seal the same command printed `PUBLIC CI EVIDENCE PASS`, exit 0, with `OPEN closed cumulative profile not sealed` as its only OPEN.
2. `--full --package B-NTC` **without** the private fixture → `B PACKAGE B-NTC BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`, **exit 2**.
3. The lane's own FULL on the owner's PC, private fixture prepared per AGENTS.md item 4 → `B PACKAGE B-NTC POSTFIX M2-B-NTC-NATIVE-TREND-CONTEXT REVIEW-PENDING mode=--full` … `B PACKAGE B-NTC POSTFIX PACKAGE REVIEW-PENDING: 1 open obligation(s); independent exact-artifact acceptance required`, **exit 2**.

## What run 3 observed — verdict words only
`ENVELOPE PENDING artifact=1588567a… spec=9d8552d9… runner=eedabccd…` · `PARENT PINS RE-ASSERTED` (29 parent pins + its 20 product pins + 23 un-superseded grandparent pins, byte-identical on disk and in Git at HEAD; 22 superseded pins preserved in Git at sourceBase) · `PRODUCT IMPLEMENTED` — 33 at the post-image / 0 at the pre-image / 20 carried / 0 unlisted drift, inventory covering all 20 parent-pinned product files, 2 declared `superseded-by-child` · `FIDELITY OBSERVED`, 16 of 18 PIN_PATHS present and byte-identical Git vs disk · `AUTHORITY OBSERVED` · `LAWS 45/45 executed` and `LAWS DECLARED-STATE 45/45 rows agree` (the register BASELINE; this package declares no D-id, so those rows prove nothing about it) · `NO-REGISTER OBLIGATION … 15 of 15` declared children executing this package's own `role:"new"` product ran in-process, exit 0, exact needle at line start, 1 required at the seal · `COVERAGE 9/19` carried by 5 executed successor children · `PRIVATE ORACLE PRESENT; verdict-only reporting` · `HISTORICAL … AUDIT RED-FIRST OBSERVED` · `FULL EVIDENCE: 10 of the 19 original gates re-executed and 9 carried by successor children that executed in this run; second gate included`.
Gate verdicts: witnesses-1 OBSERVED · witnesses-3 OBSERVED · witnesses-4 OBSERVED · witnesses-6 OBSERVED · merge-differential OBSERVED · merge-laws OBSERVED · **migrate-full OBSERVED (the private census; verdict-only)** · conformance OBSERVED · selftest OBSERVED · strict OBSERVED. Carried by successors: migrate-source, merge-source, writers-source, witnesses-2, witnesses-5, migrate-differential, witnesses-7, writers-differential, second-gate.
Private preparation, verdict words only: the private blob MATCH and the private golden MATCH against the committed pins; no declared D-id census moved; no golden was regenerated into the tree — the public goldens and `rebuild/conform/oracle/manifest.json` were restored to their committed bytes, and `git status --porcelain` shows nothing under `rebuild/conform/private` or `rebuild/conform/engines` (both gitignored). **The private fixture is left in place** on this machine for the authorized rerun.

## Reviews of record
- `rebuild/lanes/b/reviews/B-NTC-REVIEW-r3.md` @ `46fef4c` — **ACCEPT WITH CHANGES**, "sealable once the two ledger lines exist"; its three prose changes applied at `3258eb9`. Its §F carries 24 executed bites (22 RED, 2 GREEN controls) and the mutant/fidelity work; cited by path rather than re-run here, as the package's own evidence commands are the ones this run executed.
- `rebuild/lanes/b/reviews/TOOLING-REVIEW-r6.md` @ `7b42b4d` — **ACCEPT WITH CHANGES; SAFE TO SEAL B-NTC under `DECISIONS:113`: YES**; its six changes applied at `1e80e8f` / `2f8678e`.
- `rebuild/lanes/b/reviews/B-NTC-REVIEW-r2.md` @ `10feb1e` — REJECT, superseded by r3.

## The batched `rebuild.yml` re-seal item (`DECISIONS:103 (5)`)
`.github/workflows/rebuild.yml` rides this seal as a `superseded-by-child` parent EXECUTION pin, with **four disclosed hunks**, all in BRIEF §4: (1) the A1/A2/A3 step enumerates seven `today/` files, adding `checkin.test.mjs` and `ntc-h6-delta.test.mjs` (`:109`); (2) the parent gate step is replaced by this child's `b-package.cjs --ci --package B-NTC` step (`:113 (3)`, which says not to restore it); (3) the memory-only w7-preview step is deleted — the `:109` pass-19 retirement `:113 (3)` says to keep deleted; (4) a new C5 step `node --test "rebuild/coach/test/*.test.cjs"` (`:117 (4)`). A4's `rebuild/m3/w7-preview/today/test/setup.test.mjs` is **NOT** enumerated: `:117 (4)` enumerates it only if it exists on the tip at seal time, and it does not exist on `origin/rebuild/t2-client-core` at this head.

## Residuals carried — all named, none blocking this seal
r3 §G 1–6 (FIX-REPORT-B-NTC-r2 §5), each dispositioned "may ride": three of the nineteen retired memory-only cells have no successor; the three substitutions are reviewer-proved, not machine-proved (r2 R12, restated as r6 F3); `rirPlan`'s bind window is covered structurally, not behaviourally; neither tooling suite has a CI home; every qualified answer for both athletes is still all-false, a property of the fixture; `run-current-head.cjs --all` does not run in this worktree. Plus r3's own R7 — `--explain`, agreed at `:113 (7)`, unlanded — and r6 F1–F7. One this sealer adds: the runner at this head (`eedabccd…`) is the **r6-applied** runner, later than the `6875576097…` bytes r6 judged, so r6's own six changes have not themselves been independently re-reviewed. Sibling cost stands (`:113 (6)`): B1/B2/B3/B4/B-LOM re-pin `engine-runtime.cjs` at their own rebase onto the accepted B-NTC head.

## Receipt → authorized rerun
Awaiting the PM's receipt line, in the exact shape `DECISIONS:104` used for the parent:
`- <date> · cowork · POSTFIX-ACCEPTANCE M2-B-NTC-NATIVE-TREND-CONTEXT 3ab4f2c5862f0b000b9cf7e34253341de6842625 rebuild/m4/spec/acceptance-b-ntc-native-trend-context.json 1588567a55d1d5a991ef6ea7c26b9f6ecae1594bf0844ac23eb9fed422adeb17 ACCEPTED`
Then lane B incorporates that docs-only receipt base, sets the envelope to `{status: "ACCEPTED", receipt: {commit, path, line, lineSha256}}` — operational coordinates only; if the artifact bytes change the receipt is void and step 3 starts again — and the **AUTHORIZED FULL rerun** on this branch prints `POSTFIX PACKAGE PASS M2-B-NTC-NATIVE-TREND-CONTEXT`, **exit 0**.

## r7 delta review of the runner bytes this seal ran on (added by the lane lead, 2026-09-11 23:05 ET; artifact bytes unchanged)
`rebuild/lanes/b/reviews/TOOLING-REVIEW-r7.md` @ `daa87c8` (rebuild/lane-b-tooling) — **ACCEPT WITH CHANGES; "the B-NTC seal at 7d4aafa stands on these bytes: YES"**. The reviewer reproduced the sealer's `--ci` run on the sealed bytes line for line. One disclosure the PM should read with the PRODUCT line above: r7 F1 — of the "33 at the declared post-image", **7 are role-`new` files whose pre-image equals their post-image and which already exist byte-identical at sourceBase 7b1678a** (`w7-preview/today/test/{adapter,checkin,design,package,view}`, `w6/host/test/engine-equivalence`, `w6/test/local-today-journey`): this package pins and executes them but never wrote a byte of them, so the honest reading is 26 written + 7 pinned-unchanged. Bounded: none of the 15 children executes any of the 7; Y1 / NO-REGISTER 15 of 15 untouched; no refusal weakened. The role vocabulary fix (a "pinned-unchanged" role) is queued as tooling change r7-1 for the next tooling pass; it would change the spec and therefore the artifact bytes, so it is NOT applied to this seal. r7 F2–F6 are tightenings/notes, none affecting this artifact.

## Authorized rerun — executed, PASS
Receipt **`DECISIONS:130`** at commit **`ca50ffa80bfe08518bfb7a649a0ce8113ee7a767`** (lineSha256 `aad291ce61d79bfa233587b18e97cae327635cfcc61a9cd40e8838816c1cb5ab`), naming reviewed commit `3ab4f2c5862f0b000b9cf7e34253341de6842625` and artifact sha256 `1588567a55d1d5a991ef6ea7c26b9f6ecae1594bf0844ac23eb9fed422adeb17` — unchanged; PM judgment `DECISIONS:131`. Envelope `review-b-ntc-native-trend-context.json` set to `{version:1, status:"ACCEPTED", receipt:{commit, path, line, lineSha256}}`; artifact bytes untouched, owner `:60` / contract `:49` / theme `:122` / brief `:126` each re-verified as exact bytes at the receipt base.
`--full --package B-NTC`, private fixture in place → `B PACKAGE B-NTC POSTFIX M2-B-NTC-NATIVE-TREND-CONTEXT AUTHORIZED mode=--full` · `ENVELOPE AUTHORIZED artifact=1588567a… reviewed at 3ab4f2c…; receipt base ca50ffa…` · all 19 gates PASS (10 re-executed incl. **migrate-full PASS**, the private census, verdict-only; 9 carried by successors; second gate included) · `HISTORICAL … AUDIT RED-FIRST OBSERVED` → **`B PACKAGE B-NTC POSTFIX PACKAGE PASS M2-B-NTC-NATIVE-TREND-CONTEXT`, exit 0**, zero OPEN lines.
`--ci --package B-NTC` → `ENVELOPE AUTHORIZED …` → `B PACKAGE B-NTC PUBLIC CI EVIDENCE PASS …`, **exit 0**, zero OPEN lines.
**The receipt base was NOT merged into this branch, and that is deliberate.** `git merge-tree HEAD ca50ffa` (and `HEAD d9fee35`) merges cleanly but moves the bytes of five files this artifact PINS — `rebuild/m3/w6/local/today-bindings.mjs`, `w6/host/workout-host.mjs`, `w6/test/local-today-journey.test.mjs`, `w7-preview/today/test/checkin.test.mjs`, `w7-preview/today/test/view.test.mjs` — which would void the receipt (`UNLISTED-PRODUCT-DRIFT`, and `checkSources` at `3ab4f2c`). `rebuild/engine`, `rebuild/conform`, the artifact and the spec are byte-unchanged by that merge; the five `rebuild/m3` pins are not. The runner does not require the receipt base to be an ancestor of HEAD — only that it is an ancestor of `refs/remotes/origin/rebuild/t2-client-core`, which `ca50ffa` is — so the rerun passed without it. Reconciling those five files with the chain is the PM's merge, after this package lands.
Head after this commit: see `git log -1` on `rebuild/lane-b-ntc`; the branch is not pushed. The private fixture remains in place and gitignored.

## Re-seal on the tip (DECISIONS:132) — supersedes everything above, REVIEW-PENDING again
Merge commit **`ae01216ea8631575269af0430599f5f630ca0fb5`** = `git merge --no-ff` of `origin/rebuild/t2-client-core` at tip **`50fa37a534ce8b2406a88d194b4ee00e3394ede8`** (line 133 docs, on top of `d7d8138` = line 132 and `fc357b9`), merge base `5c6766e`. No conflict; B-NTC's own today-bindings hunk is intact (the one replaced line is the export list, re-emitted with the tip's `createSetupHost` added). `git diff b8d5cc7 HEAD` touches **no** byte of `rebuild/engine`, `rebuild/conform`, `rebuild/m4`, `.github` or `rebuild/lanes/b/tooling` — only `rebuild/m3`, and of those only five files this artifact pins.
Five pins re-taken (roles unchanged, all `new`; `pre` = bytes at the new sourceBase, `post` = merged bytes): `w6/local/today-bindings.mjs` `b243257a…`→**`95315f7a0e63cd49648a8c6c031d38a7bbce3f8e8f64e2fdfaf713b3ee174ce8`** · `w6/host/workout-host.mjs` `262d7d5f…`→**`8472097cb015f5dce034379f3f08b70aa516ca6d479d4f4fc8f3f0e3ffe08b69`** · `w6/test/local-today-journey.test.mjs` `7e5ccb4c…`→**`4572517ce67b99ae93fc369821a387029bcbc2907ff9463ed33c6d123f4d62a7`** · `w7-preview/today/test/checkin.test.mjs` `bdf209db…`→**`3e7be3363105b3fcc481cc1389168c48a92a7086b8ec1e7d86671acb63f6e044`** · `w7-preview/today/test/view.test.mjs` `c9cc240f…`→**`f0d4e66aba82b7a51dd0765e74def27e77481fe263bc7a68a16cbe5356793af2`**. sourceBase `7b1678a` → `50fa37a`; all parent and grandparent pins were checked to still stand there (0 would break). Spec `9d8552d9…` → `05a5aa1f…`.
Suites on the merged tree, counts only: the ten durable journey suites (the seven enumerated `today/` files + A0 journey + engine-equivalence + local-today-journey) **238 tests / 238 pass / 0 fail**, exit 0 — the exact count `b-ntc-journeys.cjs` asserts, so that file does not move; provider cells `native-trend-context.test.cjs` **39/39**, exit 0; children `b-ntc-journeys.cjs` (`B-NTC DURABLE JOURNEYS: 238/238 PASS;`) and `b-ntc-focused.cjs` (`B-NTC FOCUSED: 15/15 PASS;`) both exit 0 with their exact declared verdicts. Nothing red.
**New artifact** `rebuild/m4/spec/acceptance-b-ntc-native-trend-context.json` sha256 **`87f4848c8c20a9a187f440abbfef94244786a6ab6fcf859e64295a29f5b49428`** (34 361 B), in commit **`9ad2ecab141753e9e8e74d3a105bd168d5e72286`**; it supersedes `1588567a55d1d5a991ef6ea7c26b9f6ecae1594bf0844ac23eb9fed422adeb17`, so **`DECISIONS:130` is void by construction** and the envelope is back to `{version:1, status:"PENDING", receipt:null}`.
Terminals, executed in this order: `--ci` with the artifact absent → `PUBLIC CI EVIDENCE PASS`, **exit 0**, only `OPEN closed cumulative profile not sealed`; `--ci` with the re-authored artifact → `ENVELOPE PENDING artifact=87f4848c… spec=05a5aa1f… runner=eedabccd…` → `PUBLIC CI EVIDENCE PASS`, **exit 0**, only `OPEN independent exact-artifact acceptance PENDING`; `--full` with the private fixture → `POSTFIX M2-B-NTC-NATIVE-TREND-CONTEXT REVIEW-PENDING mode=--full` … `POSTFIX PACKAGE REVIEW-PENDING: 1 open obligation(s); independent exact-artifact acceptance required`, **exit 2**, `PRIVATE ORACLE PRESENT; verdict-only`, all 19 gates OBSERVED (10 re-executed incl. **migrate-full OBSERVED**, the private census, verdict word only; 9 carried by successors; second gate included), `HISTORICAL … AUDIT RED-FIRST OBSERVED`. `--full` **without** the private fixture is not re-run: that terminal is a property of the runner and the machine, not of these bytes, and it stands executed and quoted above.
**OPEN for the PM — `DECISIONS:117 (4)` has flipped and this re-seal does not act on it.** `:117 (4)` enumerates `rebuild/m3/w7-preview/today/test/setup.test.mjs` in `rebuild.yml`'s today step **only if it exists on the tip at seal time**. At the first seal the tip was `2d457fd` and it did not exist (the spec said so, truthfully). It exists on `50fa37a` (added to the chain at `fc46ab6`), so the condition is now **met** and the enumeration is **owed**. It is not applied here because `:132` scopes this pass to the five moved pins and the step is a `rebuild.yml` change only the PM may order; the two spec sentences that asserted the condition was unmet now state what is true and that the item is open. The brief is frozen by sha256 `4aa94069…` under `DECISIONS:126` and is **not** edited, so it still carries the first-seal sentence. If the PM wants the step, say so and it is one more disclosed hunk — and one more re-seal, because it moves `rebuild.yml`'s post-image and therefore the artifact.
**The new receipt line for the PM, in the `DECISIONS:104` shape (date is the PM's, as in `:130`):**
`- <date> · cowork · POSTFIX-ACCEPTANCE M2-B-NTC-NATIVE-TREND-CONTEXT 9ad2ecab141753e9e8e74d3a105bd168d5e72286 rebuild/m4/spec/acceptance-b-ntc-native-trend-context.json 87f4848c8c20a9a187f440abbfef94244786a6ab6fcf859e64295a29f5b49428 ACCEPTED`
Then the envelope goes to `{status:"ACCEPTED", receipt:{commit, path, line, lineSha256}}` and the authorized `--full` rerun prints `POSTFIX PACKAGE PASS M2-B-NTC-NATIVE-TREND-CONTEXT`, exit 0. The private fixture stays in place, gitignored. Branch not pushed.

## Authorized rerun on DECISIONS:141 — executed, PASS
Receipt **`DECISIONS:141`** at commit **`32408277be136ff5c41f7585c5f073e96e6c1252`** (lineSha256 `089a2920ae80d85791916ce2ff92263cf0d332db6eed45c143ae75380f3b7e50`), naming reviewed commit `9ad2ecab141753e9e8e74d3a105bd168d5e72286` and artifact `87f4848c8c20a9a187f440abbfef94244786a6ab6fcf859e64295a29f5b49428` — both MATCH on disk and in Git; PM judgment `:142`. Envelope set to `{version:1, status:"ACCEPTED", receipt:{commit, path, line, lineSha256}}`; owner `:60` / contract `:49` / theme `:122` / brief `:126` re-verified as exact bytes at that base. The tip (`beedbf1`) is **not** merged, for the reason recorded in the re-seal section: a merge moves files this artifact pins and would void the receipt; the runner requires the receipt base to be an ancestor of the chain branch, not of HEAD, and `3240827` is.
**`:142` names the `:136 (3)` byte-identity re-verify as the authorized step. That tooling is not available to the sealed runner** — it lives on `rebuild/lane-b-tooling` @ `b46b5fd` (runner `769fd09e…`) and this artifact pins runner `eedabccd…`, which a re-seal would be needed to change. The **FULL rerun with the sealed runner** was executed instead, exactly as at `:130` — a superset: it re-reads every pinned byte from Git at the reviewed commit through `checkSources`, then runs all 19 gates and the private oracle.
`--full --package B-NTC`, private fixture in place → `POSTFIX M2-B-NTC-NATIVE-TREND-CONTEXT AUTHORIZED mode=--full` · `ENVELOPE AUTHORIZED artifact=87f4848c… reviewed at 9ad2eca…; receipt base 3240827…` · 19/19 gates PASS (10 re-executed incl. **migrate-full PASS**, the private census, verdict word only; 9 carried by successors; second gate included) · `HISTORICAL … AUDIT RED-FIRST OBSERVED` → **`POSTFIX PACKAGE PASS M2-B-NTC-NATIVE-TREND-CONTEXT`, exit 0**, zero OPEN lines. `--ci --package B-NTC` → `ENVELOPE AUTHORIZED …` → **`PUBLIC CI EVIDENCE PASS`, exit 0**, zero OPEN lines.
Byte identity checked by hand, the check `:136 (3)` would make, counts only: artifact `87f4848c` MATCH · spec `05a5aa1f` MATCH · runner `eedabccd` MATCH · product pins **53/53 MATCH, 0 MISMATCH** on disk and **53/53** in Git at HEAD · executionPins **18/18 MATCH, 0 MISMATCH** on disk and **18/18** in Git at HEAD · every pinned byte at the reviewed commit `9ad2eca` **56/56 MATCH, 0 MISMATCH**.
Still OPEN and unchanged by this step: the `DECISIONS:117 (4)` enumeration of `today/test/setup.test.mjs`, owed since the tip carries that file and not taken inside a pass that was scoped elsewhere. The private fixture stays in place, gitignored. Branch not pushed.
