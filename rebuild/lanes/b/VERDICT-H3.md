# VERDICT — M2-H3-CLEAN-INIT · lane B seal · **PASSED** on the `DECISIONS:183` receipt

Branch `rebuild/lane-b-h3-r3`, on top of `29a01dbe880e3a3440604599bfbbb1ded18c3450`: **artifact commit `5a5a965e1b38f21ddfb687a070004e27aef865eb`**; this verdict file is committed on top of that and is pinned by nothing. After the r3b confirmation, `rebuild/lane-b-h3` was fast-forwarded to the same head (`6005efe`, 19:5x ET), so the two branch names now point at one history; the FREEZE fallback was withdrawn at `DECISIONS:178`. sourceBase `ce38aa3bd174c94526e01fb5056df850ad322b80`; the chain tip `8ec60ae3343302ff47b2a8748d1f5c54af063b09` (merged at `e07a5c4`; on the guarded paths the whole delta from `42c133a` is two `M` lines, lane C's `:185` hygiene edit to `copy.test.mjs` and `food.test.mjs` — neither H3-pinned, no file added or removed under `today/test`) is an ancestor of HEAD (`SEAL_TIP_RULE = ancestor`, `DECISIONS:145`).
Runner `rebuild/lanes/b/tooling/b-package.cjs` sha256 **`4482bb8aa344b4aa090a01cbcf2164f9834320d71ce24a24bcc0154b0c037db3`** (`rebuild/lane-b-tooling` @ `17341d5`, blind-reviewed r10b plus the `:153 (ii)` evidence slot); spec `rebuild/lanes/b/tooling/packages/H3.json` sha256 **`0253fed8bb68d82eca3e2bdeb32d766fc8576a77f7f4acf5b0c7eec7eca4659f`**.
Everything below was executed on the owner's Windows PC (node v24.18.0, `TZ=America/New_York`, `MEASURED_TEST_NOW=2026-09-03`). No private value, count, hash or prose appears in this file. The PASS word below is the runner's, earned on the authorized FULL rerun against `DECISIONS:183`; it is not lane B's own grant.

## Authority
- Brief `rebuild/lanes/b/BRIEF-H3-CLEAN-INIT.md` v1.9, sha256 `67e95134b2c48f0c175f3481575854e03e685f6ce50fbf97efe041a459f5b301` (34 208 B), accepted by **`DECISIONS:169`** (lineSha256 `59e7bab1affa9a722f20e163c34e6095e4e43777815a5da5510ad9b02ab3da25`); spec `status: BRIEF-ACCEPTED`. `:169` supersedes the withdrawn `:166`, whose role clause the runner cannot read, and re-pins the same brief bytes.
- Theme **`DECISIONS:168`** (lineSha256 `2834242756266eb30cdbfd61d5b7b6146e3389cc5424aa0322a58fff96ac7251`), superseding the withdrawn `:165`. Owner `DECISIONS:60`; contract `DECISIONS:49`, byte-equal to the parent artifact's own. Both of this package's own lines resolve in Git on `refs/remotes/origin/rebuild/t2-client-core`; owner and contract at the parent receipt base `3240827`.
- Gate supersession: the **standing role at `DECISIONS:153`** (lineSha256 `b6f84af6a014cc3a751ff3f077ed03e149047cdcc0f448328038a3e0f0af47e1`, whose own token stands inside a prose clause and therefore frees nothing) granted to this package by the **token clause at `DECISIONS:160`** (lineSha256 `3746ff573af4522ff8199f21d210d5915954bb7190e6914ca5e7a7e5853184bb`, the spec's `coverage.superseded.rulingLineSha256`, located on the chain branch by its own bytes).
- Parent (single, immutable): `rebuild/m4/spec/acceptance-b-ntc-native-trend-context.json` sha256 `87f4848c8c20a9a187f440abbfef94244786a6ab6fcf859e64295a29f5b49428`, review `review-b-ntc-native-trend-context.json` `e0b86ad5…`, receipt `DECISIONS:141`, reviewed at `9ad2ecab141753e9e8e74d3a105bd168d5e72286`, integrated at `ce38aa3` (ledger line 144).

## The closed cumulative profile artifact
`rebuild/m4/spec/acceptance-h3-clean-init.json` · sha256 **`d3c760c3862262beef5d10f6004a5a96e25f1219b23c8da98a653289c0a50cce`** · 36 069 B · lives in commit `5a5a965e1b38f21ddfb687a070004e27aef865eb`. It supersedes `2b6e579e…` in three hashes only — the spec twice and the `rebuild.yml` post — after the r3c step retirement.
Envelope `rebuild/m4/spec/review-h3-clean-init.json` = `{version: 1, status: "PENDING", receipt: null}`. No receipt is written and no ACCEPTED envelope is claimed. `coverage.inherited` and `coverage.moves` are both `{}`; `coverage.successors` is `null`. The runner recomputed the whole artifact from the spec, its own bytes and the pins on disk, and did not refuse.

## Gate supersession — five carriers, nine of the nineteen gates
| carrier of the parent | gates it retires | evidence, all executed as declared children in the same run |
|---|---|---|
| `source-carriers` | `migrate-source` `merge-source` `writers-source` | red-first `h3-sup-source-carriers` · census: the runner's own live-triggered line, which says `none` · legacy `a0-journeys` · writers `today-suites` · engine files `engine-files-differential` |
| `inherited-carriers` | `witnesses-2` `witnesses-5` `migrate-differential` | red-first `h3-sup-inherited-carriers` · same four |
| `defect-witnesses` | `witnesses-7` | red-first `h3-sup-defect-witnesses` · same four |
| `writers-differential` | `writers-differential` | red-first `h3-sup-writers-differential` · same four |
| `second-gate` | `second-gate` | red-first `h3-sup-second-gate` · same four |
`laws` is `null` for all five: the 45-law register is unmoved and this package registers no D-id. The gates are reported **SUPERSEDED**, never OBSERVED and never carried; `--full` still re-executes the other ten. The five carriers are fixed in the runner (W7) and the admitted gate set is derived from the parent artifact's own `coverage.byChild`, so no name in this table was typed into a spec.

## Terminals — executed, exit codes measured
1. `node rebuild/lanes/b/tooling/b-package.cjs --ci --package H3` → `B PACKAGE H3 ENVELOPE PENDING artifact=d3c760c3… spec=0253fed8… runner=4482bb8a…` → `B PACKAGE H3 PUBLIC CI EVIDENCE PASS — public evidence only, NOT the package verdict; …`, **exit 0**. One OPEN line — `B PACKAGE H3 OPEN independent exact-artifact acceptance PENDING` — which is not CI-blocking and cannot close before the PM's receipt. All ten declared children OBSERVED, exit 0, exact needle at line start.
2. `--full --package H3` with the private fixture in place → `B PACKAGE H3 POSTFIX M2-H3-CLEAN-INIT REVIEW-PENDING mode=--full` … `B PACKAGE H3 POSTFIX PACKAGE REVIEW-PENDING: 1 open obligation(s); independent exact-artifact acceptance required`, **exit 2**.
3. `--full` **without** the private fixture was not run here: `H3-REVIEW-r2` executed exactly that terminal on this tree (`B PACKAGE H3 BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`, exit 2) and it is a property of the runner and the machine, not of these bytes.

## What run 2 observed — verdict words only
`PARENT PINS RE-ASSERTED` (1 parent pin + its 53 product pins + 29 un-superseded grandparent pins) · `PRODUCT IMPLEMENTED` — 15 at the post-image / 0 at the pre-image / 49 carried / 0 `pinned-unchanged` / 0 unlisted drift · `FIDELITY OBSERVED`, 22 engine/conform/m4-spec/lane-b-tooling files changed since sourceBase, all in the fixed inventory · `AUTHORITY OBSERVED` · `LAWS 45/45 executed`, `LAWS DECLARED-STATE 45/45 rows agree` (register BASELINE; no D-id) · `NO-REGISTER OBLIGATION … 8 of 8` own children ran, 1 required · `SUPERSESSIONS 5 byte-identity carrier(s) … over 9 gate(s) under DECISIONS:160` · `PRIVATE ORACLE PRESENT; verdict-only reporting` · `HISTORICAL … AUDIT RED-FIRST OBSERVED` · `FULL EVIDENCE: 10 of the 19 original gates re-executed, 0 carried by successor children and 9 SUPERSEDED`.
Gate verdicts: witnesses-1 OBSERVED · witnesses-3 OBSERVED · witnesses-4 OBSERVED · witnesses-6 OBSERVED · merge-differential OBSERVED · merge-laws OBSERVED · **migrate-full OBSERVED (the private census; verdict word only)** · conformance OBSERVED · selftest OBSERVED · strict OBSERVED. The nine above are SUPERSEDED, not re-executed.
Private preparation (copied from the B-NTC worktree, AGENTS.md item 4): fixture and golden **present**; private blob **MATCH** and private golden **MATCH** against the committed pins; the golden's stamp **MATCH** against the manifest's engine/clock/zone/census; `oracle/manifest.json` and both public goldens **byte-identical** to the committed bytes; `git status --porcelain` shows nothing under `rebuild/conform/private` or `rebuild/conform/engines` (both gitignored). The fixture is **left in place** for the authorized rerun.

## Reviews of record
- `rebuild/lanes/b/reviews/H3-REVIEW-r1.md` @ `89dde7b` — **ACCEPT WITH CHANGES**; its changes applied (F-B's first-read trend seed re-authored, the stale cites, the vacuous assertion and the back-heads comment).
- `rebuild/lanes/b/reviews/H3-REVIEW-r2.md`, committed at `905fd08` — **ACCEPT WITH CHANGES**; its one blocker was mechanical (two null citation fields plus a chain merge) and is discharged at `4b4b5c7`, where `--ci` is `PUBLIC CI EVIDENCE PASS` exit 0.
- `rebuild/lanes/b/reviews/H3-REVIEW-r3.md` @ `3bb7612` — **ACCEPT WITH CHANGES**, blind delta review of `e994c10..fffc668`; all four required changes applied: F1 re-sealed on a tip carrying `machine-settings-ui.test.mjs` and naming it, F2 answered by `DECISIONS:178` (variant (i) confirmed, the hunk is lane B's, FREEZE withdrawn), F3 and F5 corrected in `BUILD-REPORT-H3.md` §r3 and in `H3.json`'s `notes`, F4 this file.
- **r3b, this round** — tip `c5552f5` merged at `0633099`, ms-ui named (12 named, `food.test.mjs` the one hold-out), re-pinned and re-sealed at `521a0049`; `--ci` `PUBLIC CI EVIDENCE PASS` exit 0 and `--full` with the private fixture `POSTFIX PACKAGE REVIEW-PENDING: 1 open obligation(s)` exit 2. `BUILD-REPORT-H3.md` §r3b carries it. Not a self-acceptance: this file is lane B's report, and the receipt is the PM's.
- **r3c, this round** — the integrator held the merge on `--ci --package B-NTC`; the cause was that H3 re-pins `packages/B-NTC.json` onto the r10 runner, so B-NTC's own accepted artifact no longer recomputes from the spec on the tree. Fixed as `ce38aa3` fixed it for B-NTC: the parent's standing CI step is RETIRED and H3's takes its position. `BUILD-REPORT-H3.md` §r3c carries both refusals verbatim, the grep custody check and the measurements.

## The `rebuild.yml` hunk
`.github/workflows/rebuild.yml` is declared `edited` (`839a79ab…` → `19e861df…`): the A1/A2/A3 step becomes A1/A2/A3/A4, **names `rebuild/m3/w7-preview/today/test/setup.test.mjs`** (`DECISIONS:142 (2)` option (b) — the `:117 (4)` enumeration rides H3's seal, **closing the item left open in `VERDICT-B-NTC.md`**), and — since r3c — is also the position where H3's own cumulative gate stands: `- name: Cumulative H3 clean-init, native-carrier and legacy-census evidence` / `run: … --ci --package H3` REPLACES B-NTC's step, exactly as `ce38aa3` replaced the accepted originals' step with B-NTC's, because a cumulative child's gate IS the standing step. Under `DECISIONS:177 (A)`/`:178` the today step now names **every file present under that directory but one**: `catalogue`, `copy`, `problem` (r3) and `machine-settings-ui` (r3b). Every file named, never globbed. `food.test.mjs` is the single hold-out, named in the open with its reason in the step comment: its `N1.11`/`D2.1` execute the pre-H3 engine as their expectation and are RED by design here, and its build cells race `view.test.mjs`'s `R3`. Measured at this head: `setup.test.mjs` 157/157; the twelve named files together **490/490, exit 0**, three runs. The step needs jsdom (six of the twelve import it), installed by the pinned W6 browser-build step; no lockfile of its own.

## Residuals carried — all named, none blocking this seal
**F-A** unchanged: `energy.cjs:117 proteinTarget` has no gate of its own; the view layer's `Number.isFinite` keeps the figure off screen. **F-G** `sleep.cleanH`: two engine-stated defaults (`|| 7.5` at `sleep.cjs:1071`, `|| 8` at `sleep.cjs:925`), so a clean-night run stays 0 however well he sleeps — held open by an assertion in cell H3/S1, not on Today's projection, **needs a PM ruling**. **F-H is CLOSED** by `DECISIONS:177 (A)`/`:178`: `catalogue`, `copy`, `problem` and `machine-settings-ui` are now named, and cell H3/13 asserts the directory equals the named set plus the one hold-out, so a new unnamed file is red. What remains open behind it is not lane B's: `food.test.mjs` runs nowhere in CI until lane C updates its two pre-H3 cells and unshares its build cells. `r2`'s own PM item stands: `defect-witnesses` and `second-gate` are **partly** covered — the parent's ten witnesses and its independent second route are inherited by pin, not re-executed; that is the `:153` trade, and it should be accepted knowingly. The four `MG_LABEL` back heads are inert (no producer sets them), said honestly in brief and code. `athlete-state.cjs` gains a `require` into `rebuild/engine/constants.cjs`: the factory is pure and clock-free (measured), but m4 now depends on the engine at load time.
**Brief errata** — `BUILD-REPORT-H3.md` §r2-2, five documentation-only corrections that change no measurement, pin, cell or verdict, standing as errata because the brief's bytes are pinned by the acceptance line: (1) four stale self-version strings — read every self-reference as **v1.9**, which the header does say; (2) §9's runner shas name earlier runners — the runner of record is `4482bb8a…`; (3) the `h3-supersede-inherited-carriers` header promise reads as two differentials, and the file's three cells are the unchanged-prior-module half, the **constants** differential and the no-producer guard, the writers differential living in its own carrier; (4) §7's red-first count is **2 pass / 12 fail** on this head, not 1/13; (5) B1's pre-image for `rebuild/m4/workout/athlete-state.cjs` is **`d0e26f7401f6d04f44aef808eaca3c05e658f9c1ebd25a45ad9d2b3a3e69601c`**. One correction to the errata itself: §r2-2 speaks of `:166`, and the line actually cited is **`:169`**, which superseded it and re-pins the same bytes.

## Sealed-run receipt (`DECISIONS:136 (3)`) — REWRITTEN by the `:183` authorized rerun
`rebuild/lanes/b/tooling/receipts/H3.json` sha256 **`29c29e1b615a69ffaa3a32c56e419882177a9cd2695cce9f2847c4d688d97f08`**, 8 084 B, written by the runner itself on the terminal branch of the FULL run that earned `POSTFIX PACKAGE PASS` on artifact `d3c760c3…`, and committed unmodified. It replaced `40cfd7ff…`, the receipt of the superseded `2b6e579e…` run, deterministically and in place. Its `sealedRun` block carries artifact `d3c760c3…`, spec `0253fed8…`, runner `4482bb8a…`, `envelopeKey ACCEPTED:d3c760c3…:5a5a965e…:55c08e79…`, this verdict file by name and the 64 pinned product shas. With these bytes in Git and this sha256 named here, the `:136 (3)` byte-identity step is AVAILABLE — measured below. Its `sealedRun` block carries artifact `2b6e579e…`, spec `1a57a770…`, runner `4482bb8a…`, `envelopeKey ACCEPTED:2b6e579e…:521a0049…:b1747ec8…`, this verdict file by name, and the 64 pinned product shas. With these bytes in Git and named here, the `:136 (3)` byte-identity step is now AVAILABLE for any further rerun.

### Why it did not exist before this run
`rebuild/lanes/b/tooling/receipts/H3.json` **does not exist**, so there is nothing to commit and no receipt sha to name. The runner writes it in exactly one place: the terminal branch of a `--full` run that has earned `POSTFIX PACKAGE PASS`, never on `--ci` and never on a REVIEW-PENDING (`b-package.cjs`, `writeSealedRunReceipt`). Writing one by hand is the forgery r8 change 1 closed, so none was written. The consequence is stated rather than worked around: **the `:136 (3)` byte-identity step is UNAVAILABLE for this package and the authorized rerun after the PM's receipt will be a FULL run** — the safe default. That run writes the receipt, and its sha256 is then committed and named here. The three evidence hashes the receipt will stand on are already above in full: artifact `d3c760c3…`, spec `0253fed8…`, runner `4482bb8a…`.

## Receipt → authorized rerun — DONE
`DECISIONS:183`, added by chain commit `55c08e790cb33d2dbee01e4f1dc5fd84ab892c74`, line 183 of `rebuild/DECISIONS.md`, lineSha256 `5fee363639e691d414afb8e992f0f4f679976df0045af84ab4ea7df9e5a585a4`, is the receipt of record, and `rebuild/m4/spec/review-h3-clean-init.json` carries it as `{status:"ACCEPTED", receipt:{commit, path, line, lineSha256}}`. It reads:
`- 2026-09-12 · cowork · POSTFIX-ACCEPTANCE M2-H3-CLEAN-INIT 5a5a965e1b38f21ddfb687a070004e27aef865eb rebuild/m4/spec/acceptance-h3-clean-init.json d3c760c3862262beef5d10f6004a5a96e25f1219b23c8da98a653289c0a50cce ACCEPTED`

**`DECISIONS:172` names `61bd6d3f…` and `:180` names `2b6e579e…`; `:184` declares both void for merging. `:183` is the only line a merge may rely on.**

The **AUTHORIZED FULL rerun** was executed on this branch with the private fixture visible and terminated `B PACKAGE H3 POSTFIX PACKAGE PASS M2-H3-CLEAN-INIT`, **exit 0**, after `ENVELOPE AUTHORIZED artifact=d3c760c3… reviewed at 5a5a965e…; receipt base 55c08e79…`, `PRIVATE ORACLE PRESENT; verdict-only reporting` and `FULL EVIDENCE: 10 of the 19 original gates re-executed, 0 carried by successor children … and 9 SUPERSEDED under DECISIONS:160; second gate included`. `--ci` on the same bytes is `PUBLIC CI EVIDENCE PASS`, **exit 0**, ten of ten children `OBSERVED; exit 0` — and since r3c that command IS the standing CI step. A second `--full`, with the receipt in Git and named here, takes the `:136 (3)` byte-identity path. `rebuild/lane-b-h3` and `rebuild/lane-b-h3-r3` are pushed at the same head.
