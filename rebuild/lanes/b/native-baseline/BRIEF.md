# Native baseline candidate behavior/delta brief

Base 71fb2f1; codex/astra-native-baseline. Candidate authoring only, September 11.

Selected sources: new rebuild/m4/workout/native-baseline.cjs projectNativeBaseline; source-projection.cjs createNullSelectionRegistrar.register; rebuild/m3/w6/host/workout-host.mjs workoutProducer (passes the explicit host day and retains structured baseline refusal details). New focused tests under workout/test. No engine bodies, runtime exposure, today-bindings, accepted laws, package files or initial setup writes.

Reuse engine/performed.cjs performedEntry, performedHistoryRows and performedLine, and engine/plan.cjs forksOf/sameEra and progression.cjs loadRungs directly. The registrar reconstructs an owned current state from the supplied immutable initial state plus its separately registered current factual projection. It records source Start, Close, original-slot Set and amendment identities in a separate native_baseline derivation; it writes no legacy entries, feed receipts, stamps or caches. Existing workout_baseline retains its imported-baseline meaning. workoutProducer retains exact structured mapping requirements in lastProducerRefusal.baseline_requirement; UI rendering of this actionable data remains a separate screens join.

Deliberate delta: for an initially null working load, the causally ordered closed entries in the current technique era with a nonempty original performed prefix and one uniform positive numeric load across all performed original positions supply ex.w: the first establishes it and later actual uniform loads carry it forward. A later changed scalar merges an observed off-ladder rung using existing loadRungs and clears topAt/topRun, matching the legacy effect without replay/stamps. A closed partial/early session may supply that factual load; missing/skipped/removed positions do not supply values and existing performedLine/targets rules alone decide the rep line. Added slots never establish the original lift baseline. An open session cannot consume adoption. Removing or amending a contributor reconstructs the result from current facts and unchanged setup. Initial nonnull scalar/vector/configuration values remain unchanged.

Do not choose a scalar for unequal original loads or configuration keys, or replace an existing wSets shape. Preserve those facts and refuse by a named NATIVE_BASELINE_MAPPING_REQUIRED with exact source IDs, typed load vector and required confirmation: a qualified accepted scalar-plus-vector working-load mapping, or configuration-domain prescription mapping. A current-era entry with no original prefix leaves the debut ask and an explicit no-prefix decision in projection provenance; later eligible entries can supply the first usable baseline. Performed entries with unresolved positions require factual resolution, not a guessed baseline. Use causal order supplied by the authenticated projector; missing/unresolved order propagates the existing refusal. Future-dated matching evidence is excluded from baseline derivation. The registrar retains the resulting past-baseline evidence in NATIVE_BASELINE_HISTORY_TIME_REQUIRED because existing downstream readers still consume future history; this is the explicit B2/D7 as-of dependency, not a claim that past work cannot establish a baseline. Foreign lineage and different technique-era rows do not establish this lift.

No programme choice, load increase or scientific applicability is introduced. The unchanged genSession/progression rules operate on the measured starting load. Off-ladder first loads remain exact; later changed-load observed rungs follow the existing source rule; no unobserved rung or completeSession replay occurs.

Focused evidence: start w:null through actual C4 local installation + default B-NTC + gym model; enter 40x10 exact2, close/reopen day+3, record next result and read a third same-group prescription. Initial setup unchanged; synthetic derived cache absent gives same answer. Unit adversaries cover partial/early/open, original holes/added/removed/edits, unequal/configured/per-slot settings, lineage/era, order and future dates. Host join removal must restore the debut failure. Legacy/nonfresh whole-output controls remain unchanged. Dependency paths use existing locked installs only.

Independent executing review and PM brief acceptance are outstanding. This changes source-to-prescription semantics and requires proper engine-tier child qualification, source carrier/fidelity accounting, affected closed package/public gates, protected integrator census and any required rulings before acceptance. No broad campaign, private input, installation, push, merge, deploy or self-acceptance is authorized here.
