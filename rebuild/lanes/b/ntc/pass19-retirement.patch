B-NTC — DECISIONS:109's THIRD BUNDLED ITEM, WRITTEN OUT AND *NOT* APPLIED.
===========================================================================

WHAT :109 ASKS FOR, VERBATIM
~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  "Because B-NTC re-pins rebuild.yml and the wrapper anyway, the remaining
   tooling items ride in THIS seal: enumerate
   rebuild/m3/w7-preview/today/test/checkin.test.mjs (and any today/gym test
   files not yet listed) in rebuild.yml, and retire the old memory-only
   w7-preview child (`# pass 19`) from the wrapper together with its CI step,
   with the successor evidence named."

The FIRST half — the rebuild.yml enumeration — IS APPLIED on this branch
(.github/workflows/rebuild.yml, the A1/A2/A3 step: checkin.test.mjs and
ntc-h6-delta.test.mjs added; 158 tests / 158 pass / 0 fail measured locally).

The SECOND half — retiring the `# pass 19` child — is written here and NOT
applied. Three reasons, stated so the PM can overrule any of them in one line:

  1. SCOPE. The builder pass that produced this branch was dispatched to
     discharge :109's obligation (ii) — the EXPOSED re-pin, the option removal,
     the 28-night proof, the rebuild.yml enumeration, the Y1 children and the
     documents. The `# pass 19` retirement was not in that dispatch, and this
     lane's standing habit for an un-dispatched hunk is a written patch file
     (rebuild/lanes/b/ntc/gym-host.wiring.patch,
     rebuild/lanes/b/ntc/gym-model.previousLine.patch), not a silent
     application.

  2. IT DELETES EVIDENCE, and nothing this package owns covers the deletion.
     Removing the child removes three real executed suites from the wrapper's
     own accounting. Writing a cell that asserts their absence is not lane B's
     to do: native-carriers-package.cjs is the PM's file and the parent profile
     pins its bytes.

  3. "SUCCESSOR EVIDENCE NAMED" IS A JUDGMENT, AND THE WRAPPER IS WHERE IT IS
     RECORDED. The retiring child runs
     rebuild/m3/w7-preview/test/{model,view,package}.test.cjs — the MEMORY-ONLY
     preview page. The candidate successor is the REBOUND today page's suite
     (rebuild/m3/w7-preview/today/test/*), which is a different page over a real
     durable store, not a like-for-like replacement of those cells. Lane B names
     the candidate; it does not record that judgment on the PM's behalf.

MEASURED CONTEXT (this branch, this tree)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  * the child, as it stands:
      rebuild/m4/spec/native-carriers-package.cjs:65
      child('browser-package',['--test','--test-reporter=tap',
        ...['model','view','package'].map(n=>'rebuild/m3/w7-preview/test/'+n+'.test.cjs')],
        env,'# pass 19');
  * its CI step, as it stands:
      .github/workflows/rebuild.yml — "Synthetic Today projection, UI and
      preview package"
  * the candidate successor, GREEN on this branch:
      node --test <the seven today/ files rebuild.yml now enumerates>
      -> tests 158 · pass 158 · fail 0
  * the wrapper ALREADY refuses on this tree, for the re-pin's own reason, so
    this hunk cannot be shown green here. The quoted refusal is in
    rebuild/lanes/b/BUILD-REPORT-B-NTC.md, post-:109 section.

WHAT ELSE MOVES IF IT IS APPLIED
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  * native-carriers-package.cjs is in the parent's executionPins
    (native-carriers-profile.cjs:35, HELPERS `package`), so applying this hunk
    adds one more row to the parent refusal the child already supersedes — it
    does not create a new class of problem.
  * the wrapper's own "13/13 children OBSERVED" line becomes 12/12.

HOW TO APPLY
~~~~~~~~~~~~
  git apply rebuild/lanes/b/ntc/pass19-retirement.patch

The diff below is a real `git diff`, taken with the rebuild.yml enumeration
already in place — i.e. it applies cleanly ON TOP of this branch's committed
tree. Verified with `git apply --check`: exit 0 (see the BUILD-REPORT).

diff --git a/.github/workflows/rebuild.yml b/.github/workflows/rebuild.yml
index 1c2956d..170d6ec 100644
--- a/.github/workflows/rebuild.yml
+++ b/.github/workflows/rebuild.yml
@@ -76,8 +76,11 @@ jobs:
         run: node --test rebuild/m3/w0/test/public-conformance.test.cjs rebuild/m3/w0/test/scope-package.test.mjs
       - name: Accepted synchronous client restart witness
         run: node rebuild/t2/rig187.cjs
-      - name: Synthetic Today projection, UI and preview package
-        run: node --test rebuild/m3/w7-preview/test/model.test.cjs rebuild/m3/w7-preview/test/view.test.cjs rebuild/m3/w7-preview/test/package.test.cjs
+      # RETIRED BY M2-B-NTC, DECISIONS:109. This step ran the MEMORY-ONLY
+      # w7-preview page (rebuild/m3/w7-preview/test/{model,view,package}.test.cjs).
+      # Its successor evidence is the A1/A2/A3 step below, which runs the
+      # REBOUND today page over the real durable store: seven named files,
+      # 158 tests, 158 pass, 0 fail as measured on the sealing branch.
       - name: Cumulative extracted-engine native-carrier and legacy-census evidence
         run: node rebuild/m4/spec/native-carriers-package.cjs --ci
       # The slice suites get their CI home here (DECISIONS 99/101/102 deferred them to
diff --git a/rebuild/m4/spec/native-carriers-package.cjs b/rebuild/m4/spec/native-carriers-package.cjs
index b26f6fb..45d88d9 100644
--- a/rebuild/m4/spec/native-carriers-package.cjs
+++ b/rebuild/m4/spec/native-carriers-package.cjs
@@ -62,7 +62,11 @@ try{
   'rebuild/m4/workout/test/native-next-targets.test.cjs',
   'rebuild/m4/workout/test/native-next-targets-assembly.test.cjs',
   'rebuild/m4/workout/test/native-next-targets-correction.test.cjs'],env,'# pass 15');
- child('browser-package',['--test','--test-reporter=tap',...['model','view','package'].map(n=>'rebuild/m3/w7-preview/test/'+n+'.test.cjs')],env,'# pass 19');
+ // RETIRED BY M2-B-NTC, DECISIONS:109: the memory-only w7-preview child.
+ // Successor evidence, named as the ruling requires: the rebound today page's
+ // own seven suites, enumerated in .github/workflows/rebuild.yml's A1/A2/A3
+ // step and measured at 158/158 on the sealing branch. This wrapper keeps the
+ // `focused` and `profile-refusals` children and all ten carrier children.
  child('profile-refusals',['--test','--test-reporter=tap','rebuild/m4/spec/native-carriers-profile.test.cjs'],env,'# pass 13');
  for(const name of names)child(name,['rebuild/m4/spec/native-carriers-'+name+'.cjs'],env,verdicts[name]);
  if(ci){console.log('NATIVE CARRIERS PUBLIC CI EVIDENCE PASS; the inherited full gate matrix, the private oracle, all FULL gates and independent acceptance remain separate');process.exitCode=0;}
