# PM352 first independent review — REJECT R1; R2 repair substantiated

ER / Astra / 2026-09-13. Candidate `be0aeeb6a8e046f55730edc824ca3dddbb181a09`; tested `df8fa14fa08d28cde6bf53a710df0bb089c4720e`; preimage `f9e45f922bcb489d8ef4a4eded83b968356f16e4`; A `3bfed63febef002b8540d6ff3c56e19d08368711`; authority `ecca0d8d7ef33c1e6088995ceae88c87aa001656`.

**R1 / P1 — enabling the diagnostic changes the actual child outcome.** In `b1b2-evidence.cjs:491–504`, the new async hook is enabled during real exit. Pinned Node's `internal/async_hooks:324` then writes an async-id symbol onto a pending Promise created before hook activation. If that Promise was frozen, sealed or made nonextensible, resolving it throws a builtin TypeError before the Boolean observer callback. The runtime exits 1 and suppresses the previously attached reaction; disabled capture exits 0 and executes the reaction. Retaining the pending marker does not satisfy the required original-outcome parity.

This is confirmed with **four real node:test tests**, owned Node `--test --test-reporter=tap`, the unchanged actual helper, actual filesystem and natural real exit. No hook callback throws by construction; no hook/Reflect/Module seam or fabricated `_exiting` flag produces the failure. The invented Promise is ordinary user-owned JavaScript state, not an original frozen/native input.

The complete licensed preimage helper runs the identical frozen-Promise case with exit 0 and the reaction reached. After restoring the exact candidate, a fresh child exits 1 before the reaction again. This establishes a repair regression, not a setup failure or a preexisting assertion failure. R1 remains open under PM330/344/348/350; do not adopt a fatal hook side effect as honest diagnostic refusal.

**Independent execution:** exact committed public synthetic entry **61/61**, exit 0, twelve meaningful guard reversals killed and all six public source identities restored. Separately: 40 invented child launches, eight real-runner Promise applicability launches, two preimage/restoration launches, and four one-byte TAP controls. Forty of those fifty independent children use the real node:test runner; ten direct-mode discovery cases print their own TAP. All outcomes, including failures, are retained in the annex.

Ordinary four-test completion passes capture, including a preexisting own emitter descriptor. Reader/writer failures retain exit 1, the same 3/4 census and first-failure writer reach. Actual later observations, microtasks/nested promises, manual/reentrant exit, lost emitter ownership, cleanup-scheduled work, late IO and thrown exit refuse capture in the reviewed cases. A discarded nextTick callback did not execute and was accepted here, as PM350 permits. These results do not cure the Promise side effect above.

**R2:** actual census rejects mixed-case root SKIP/TODO, tab-separated directives and indented mixed-case bailout under one-byte feeds, while the valid four-test census succeeds. The committed cohort exercises its source reversals and actual transport cases. Child exit remains distinct from an unavailable census; no category-count diagnostic admits an exact native delta.

**Scope:** twelve inputs and actual owned Node independently pinned; only five candidate paths changed, final commit report-only. Removing the whole additive observer and its single call exactly reproduces A's helper. Profile differs only in helper.post; all 112 product entries, 26 children and 24 D ids otherwise match. Whole engine/m3 trees, source map, static original child, fieldDiff, exact field-delta assertion and whole-output assertion are unchanged.

**Builtin closure:** eleven finite public Node JS modules independently extracted and their relevant routes read before hook enable; four actual function pins match. Callback bodies ignore every argument and only set one Boolean. That callback purity does not make Node's Promise bookkeeping noninterfering. Native/V8/bootstrap, unused lazy/error routes and final filesystem/path-validation boundaries are named as opaque in `b1b2-native-capture-repair-annex/evidence/builtin-closure.json`.

**Custody:** fresh real own nested `work/pm-caretaker/native-capture-review/work/pm-caretaker/b1b2-native-capture`, full 1610-path candidate index, pinned owned Node22.23.2 win-x64 / 86997320 bytes / SHA256 `0d0f5e39f9f3d9587bc19f73eab3c2c9c4903fd02d6dbf9c853dd81b3d95fad4`. All raw public TAP/stderr, input/function/module hashes, control source, exact results, restoration proof and replay instructions are in `b1b2-native-capture-repair-annex/` with a complete SHA256 mapping.

Independent capture transport redirects only an asserted native launch to an invented child; it does not prove production commission/ACL admission. Those guards are separately exercised by the committed public cohort. No original native child, engine/history/seed/private/frozen/H3/FULL/CI or external service was executed. No candidate fix, push, integration, import or deployment.

New author completion report/replay/phase outcomes remain unread at this first verdict; only the two separately PM-verified input-only builtin manifests were read early. Full author reconciliation follows only after this verdict is committed and sent. PM judges next repair authority; original native invocation remains held.
