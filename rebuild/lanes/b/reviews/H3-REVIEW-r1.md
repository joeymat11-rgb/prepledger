# H3 — REVIEW r1 — ACCEPT WITH CHANGES — engineering SEALABLE once the successor grant and brief acceptance exist: YES

head `53450f6` (`rebuild/lane-b-h3`) · base `ce38aa3` · reviewer: independent, blind, wrote none of it · node v24.18.0 · `MEASURED_TEST_NOW=2026-09-03` `TZ=America/New_York` · all counts below EXECUTED in this worktree or in a scratch tree built from `git archive ce38aa3`.

## FINDINGS (ranked)

1. **F-B seeds the trend on a FIRST read that the same row calls set aside.** `writers.cjs:470` `if (first) s.trend = w;` runs before the sealed/off-window test (disclosed, brief §4), but the row and feed it writes say the opposite. MEASURED, clean-init athlete, `hour: 23`: `reads[0].offWindow=true`, `note="late read — set aside"`, feed carries `LATE READ — SET ASIDE`, **and `trend=186.4`**. Sealed first read: `sealed=true`, `note="sealed — excluded from trend"`, **`trend=186.4`**. For the legacy athlete the same two hours leave `trend` at `187.2` (differential below) — so a fresh athlete's late first weigh-in is treated one way and told another. Reachable on 09-13 if the first weigh-in is after the window. Not a wrong figure (it is his own number); it is a copy/semantics contradiction that needs the PM's word. Brief §4's measured list (`reads[0].note = ""`) holds only for the in-window case.
2. **`today-app.cjs:209` is the wrong citation for the load-bearing gate.** Brief §3 table, `athlete-state.cjs:176`, and `h3-clean-init.test.cjs` H3/5 all cite `today-app.cjs:209 (Number.isFinite(g))`. Line 209 is `function arrows(root)`. The real slot gate is **`today-app.cjs:259/:260`** (and `:430` for the dash row). The MECHANISM is correct — I reproduced both halves (below) — only the pointer is wrong, and it is the pointer the whole F-A argument rests on.
3. **`MG_LABEL`'s four new heads are unreachable on the tip, and the in-tree comment overstates why.** `constants.cjs:342` says they carry "the label strings lane C's catalogue uses". Grep over `rebuild/m3/w7-preview/today/*`, `rebuild/lanes/c/dad-first-run/*` and `rebuild/engine/*`: `back_lats|back_upper|back_traps|back_lower` occur ONLY at `constants.cjs:350`; `"upper back"` and `"lower back"` occur nowhere else in the tree; no producer sets `e.head` to any of them (`seed.cjs:16/:18` and `migrate.cjs:669/:670` set only `delts_side`/`delts_rear`). Brief §5 states this honestly; the code comment does not. The entries are inert and forward-looking — which is exactly why the census does not move.
4. **A vacuous assertion in H3/8.** `h3-clean-init.test.cjs:428` `assert.equal(two.trend, 185.95 === expected ? expected : two.trend)`. `expected` is `186` (`(185.95).toFixed(1) === "186.0"`), so the ternary yields `two.trend` and the line degenerates to `assert.equal(x, x)`. It asserts nothing; the line above it already does the work.
5. **Stale line citations inside the package's own commit.** `athlete-state.cjs:135` cites `writers.cjs:427/:917/:1694` and brief §2 repeats it; at HEAD the three sites are **`427/938/1715`** (`:917/:1694` are the PRE-H3 numbers, shifted by this package's own hunk). `athlete-state.cjs:181` and H3/1 cite `writers.cjs:424` for the JSON round trip — it is `:425` (`:424` is the signature). Brief §3/`athlete-state.cjs:161` cite `energy.cjs:116` for `proteinTarget` — it is **`:117`**.
6. **The report's counts do not all reproduce.** `BUILD-REPORT-H3.md` says "Brief v1.2, **125 lines**" — the file on disk is **135 lines / 18,992 B** (sha256 `1e7ba422…`, which DOES match the READ-LIST and `H3.json`). Its `--ci` transcript quotes spec `d3fd0e3c…` and `HEAD f7e0007`; at HEAD the run prints **`1c82289ea3c9…`** and **`HEAD 53450f6`** (the transcript was taken one commit early). Its claim "the single refusal is the unnamed `Required child source-carriers`" is NOT what `--ci` prints: line 14 is `CARRIERS NONE DECLARED; 0 witness flip(s) declared` and the stop is `ENVELOPE ABSENT` + `AUDIT RED-FIRST FAIL`.
7. **M5's second named killer does not kill.** `MUTANTS.M5.killedBy` names "H3/1 … and H3/2"; executed, M5 fails **H3/1 only** (H3/2 passes under M5). M6's only killer is H3/7 itself. Both mutants are still killed; the attribution is loose.
8. **§9's "(and `rebuild/engine/constants.cjs`)" is surplus.** `rebuild/engine/constants.cjs` appears as a literal in **no** file under `rebuild/m4/spec` (0 hits); the refusal actually raised by all five inherited carriers is `Unlisted parent pin drift rebuild/engine/writers.cjs` (executed). `b-ntc-journeys.cjs` refuses on `Pinned executed journey rebuild/m3/w6/host/test/journey.test.mjs`. So the grant needs `writers.cjs` (five gates) and, only if `durable-journeys` is ever a gate, `journey.test.mjs`; `constants.cjs` admits nothing.

## A–G (one line each, all EXECUTED)

* **A — SCOPE PASS.** `git diff --name-status ce38aa3 53450f6` = 33 paths; minus the tooling merge (18 under `rebuild/lanes/b/tooling`, `rebuild/lanes/tooling`, `DECISIONS.md`, `STATUS*.md`) the product diff is exactly `rebuild.yml` (one step + its notes) · `engine/constants.cjs` · `engine/writers.cjs` · `m4/workout/athlete-state.cjs` · `m4/workout/test/h3-clean-init.test.cjs` (new) · `m3/w6/host/test/journey.test.mjs` · `m3/w7-preview/today/test/setup.test.mjs` + brief/report/spec. `rebuild/conform` untouched (0 paths). No other file under `rebuild/engine`.
* **B — CLEAN-INIT PASS.** 32 reader sites traced; the two throws are `energy.cjs:370`/`energy.cjs:84` (exact). All 7 Today readers + `blackoutOn`/`dayWeather`/`weekWeather`/`observedTDEE` return over the constructed state; check-in (`checkin-model.mjs createCheckInModel/sleepNightFor`) reads it with 0 NaN; gym card opens+closes day+0 and day+3 (H3/6 green). **Blackout:** `until=dayBefore(from)` → `dayWeather(from).flags=[] noisy=false`; `until=from` → `flags=["sealwater"] noisy=true` — the builder's derivation reproduced exactly on `sleep.cjs:1879`. **No invented number:** `model.lean` absent → `proteinTarget.g/bf/ffmKg` non-finite → slot `"Not available yet"`, screen digits `["2","7"]`, 0 NaN; `model.lean: null` → slot prints **`"0"` / `"g protein"`**, `g=0 ffmKg=0`, and with a trend `bf=100`. **H1 rule:** `139.7`, `2026-07-21`, `coach's eye`, `DEXA`, `SEAL_UNTIL` all absent from the produced state (false×5) while `seed.cjs` really carries `lean: 139.7`. Constructor is clock-free (identical under `TZ=Pacific/Kiritimati`); its `dayBefore` agrees 7/7 with the tree's existing `checkin-model.mjs:79 dayBefore`.
* **C — F-B PASS (with finding 1).** One reading: `trend === w` verbatim (186.4 and 212.7 both), `reads[0].pt === null`, survives `JSON.parse(JSON.stringify)`, every printed field finite or an honest gate, 0 NaN. `Number.isFinite` is the right predicate: `trend: 0` is NOT first (`trend=0.4 pt=0`), `trend: null` is. **Legacy identity MEASURED, not argued:** 10 `applyRead` calls (5 days × hours 8 and 23) on a trend-carrying athlete produce a **byte-identical** transcript and final-state sha under HEAD's and the parent's `writers.cjs` (`77932fe0…` / `4cc10076…` both trees). My view: the rule needs **no owner ruling** — the only number written is the one he typed — but the branch's POSITION relative to the sealed/off-window test is a user-facing choice and does need one (finding 1).
* **D — F2 LABEL PARTIAL.** `volume.cjs:74 e.head || e.mg` and `:32 MG_LABEL[k] || k` exact; all four heads bucket separately and print their labels, `back` does not appear as a bucket, a head-less lift buckets on `mg` and `mgLabel('quads')==='quads'`, unknown keys fall through. `INDIRECT` byte-identical; laws line `45 RED-frozen · 39 RED-candidate` identical, exit 1. **Census byte-identical, independently proven:** same directory, `node rebuild/conform/run.cjs` with and without H3's `writers.cjs`+`constants.cjs`+`athlete-state.cjs` → **same sha256 `6ad86fb9474775126bfba534a050500856e774bc521f224bdae9a6bac28b8d81`** (HEAD tree is deterministic: `9d54e5fd…` twice). I could not reproduce the report's `401706…`/21,338 B — my log carries absolute paths and one `ENGINE_MAIN absent` line — so I proved the IDENTITY, not that sha. **Catalogue match FAILS: none of the four head strings exists in lane C's catalogue (finding 3).**
* **E — CELLS PASS.** `h3-clean-init.test.cjs` **11/11 exit 0**. RED-first reproduced exactly: parent `athlete-state.cjs` → **6/7** RED (only H3/6 green); parent `writers.cjs`+`constants.cjs` → **4/4** of H3/8–11 RED. Six mutants applied independently by me to a HEAD tree: M1→H3/1,2,3,4,5; M2→H3/2,4; M3→H3/2; M4→H3/1,2,3,4,5; M5→H3/1; M6→H3/7 — **all six killed** (finding 7 on attribution). Lane C's cell inverted correctly: **104/104 exit 0** at HEAD, and RED against the parent's constructor (`not ok 59 - H3 - the accepted engine CAN now paint Today`). Eight today files **268/268 exit 0** · A0 **23/23 exit 0** · provider **39/39 exit 0** · local journeys **68/68 exit 0**.
* **F — OWN BITES 11, all restored.** B1 `until=split.from` · B2 `lean:null` / `lean:NaN` (→`null` through the round trip) · B3 seed leakage · B4 `2026-02-30`/`2026-13-01`/`2026-00-10` REFUSED `CLEAN_INIT_SPLIT_REQUIRED · from must be a real calendar day`, `2026-1-01` REFUSED on shape, boundaries `2026-01-01→2025-12-31`, `2024-03-01→2024-02-29` · B5 seed-with-a-constant + `trend:0`/`trend:null` · B6 sealed/off-window first read (**finding 1**) · B7 full projection + DOM after a weigh-in · B8 `mgLabel` · B9 `dayBefore` cross-check · B10 check-in · B11 TZ. Every mutation was made in `…\rv-h3-scratch\{parent,redA,redB,mut}`; **`rv-h3` itself was never edited** — `git status --porcelain` clean apart from this review file, HEAD still `53450f63fce1bbc2fedb51904ac17336a01135b3`.
* **G — SPEC/BRIEF PARTIAL.** All 13 hunk shas in brief §6 verified against disk and against `git archive ce38aa3`: `athlete-state` `dccc5fb5…→cdf51db8…`, `writers` `00291236…→b57b8f8e…`, `constants` `954e4f4b…→e387579f…`, `rebuild.yml` `839a79ab…→cf4b83c1…`, `journey.test` `228c076d…→aecb8fe4…`, `setup.test` `264358a0…→842e2f49…`, `h3-clean-init.test` absent→`62f6651f…` — **7/7 pairs exact**, and `H3.json` pins the same posts. Brief sha256 `1e7ba422…` matches `H3.json.brief.sha256`. Report counts: findings 5/6. §9's closure claim verified by execution (finding 8). `coverage.successors` is `null` as stated.

## EXACT CHANGES (numbered; all one-liners)

1. `rebuild/engine/writers.cjs:470` — PM's word first, then either (a) keep the seed and make the copy honest (a first read must not carry `"late read — set aside"` / `"sealed — excluded from trend"` while it IS the trend), or (b) gate it `if (first && !sealed && !offW)`. Add a cell for BOTH hours. Proof: `hour:23` clean-init → `offWindow=true note="late read — set aside" trend=186.4`, while the same call on a trend-carrying athlete leaves `trend=187.2`.
2. `rebuild/lanes/b/BRIEF-H3-CLEAN-INIT.md` §3 table, `rebuild/m4/workout/athlete-state.cjs:176`, `rebuild/m4/workout/test/h3-clean-init.test.cjs` H3/5 comment — replace `today-app.cjs:209` with `today-app.cjs:259/:260`. Proof: `:209` is `function arrows(root)`; `:259/:260` are `Number.isFinite(view.proteinTarget.g) ? … : null` / `… ? "g protein" : ""`.
3. `rebuild/m4/workout/athlete-state.cjs:135` and brief §2 — `writers.cjs:427/:917/:1694` → `:427/:938/:1715`; `athlete-state.cjs:181` + H3/1 `writers.cjs:424` → `:425`; `athlete-state.cjs:161` + brief §3 `energy.cjs:116` → `:117`.
4. `rebuild/engine/constants.cjs:342` — drop "carrying the label strings lane C's catalogue uses"; say instead what brief §5 says (the catalogue does not yet name these heads; the entries are inert until it does). Proof: 0 occurrences of the four keys or of `"upper back"`/`"lower back"` outside `constants.cjs:350`.
5. `rebuild/m4/workout/test/h3-clean-init.test.cjs:428` — delete the vacuous `assert.equal(two.trend, 185.95 === expected ? expected : two.trend)`.
6. `rebuild/lanes/b/BUILD-REPORT-H3.md` — "125 lines" → **135**; re-take the `--ci` transcript at `53450f6` (spec `1c82289e…`, `HEAD 53450f6`, line 14 `CARRIERS NONE DECLARED`); drop "the single refusal is … `Required child source-carriers`" from the `--ci` paragraph or move it under `--full`.
7. `MUTANTS.M5.killedBy` — drop "and H3/2" (executed: M5 fails H3/1 only).
8. Brief §9 / report OPEN — drop "(and `rebuild/engine/constants.cjs`)"; name `rebuild/engine/writers.cjs`, and `journey.test.mjs` only if `durable-journeys` is to be a gate.

## PM ITEMS

* **The one behaviour question:** finding 1 / change 1 — should a fresh athlete's FIRST weigh-in seed the trend when it is off-window or sealed? Lane B's answer (yes, disclosed in brief §4) is defensible; the copy that goes with it is not.
* **F2 scope:** the four back heads are inert until lane C's catalogue names them (brief §5 is the honest half; the code comment is not). Confirm the bundle meant new head keys, not the existing `mg` labels.
* **F-C grant:** extend to `rebuild/engine/writers.cjs` (the measured refusal on all five inherited carriers). `constants.cjs` is not in any carrier closure.
* **`rebuild.yml`:** the `setup.test.mjs` enumeration is asserted by **no test in the tree** — `conform/v4/postfix/test/ci-second-gate.test.cjs:29` (the only workflow cell) is stale-RED at HEAD **and at `ce38aa3`** (pre-existing, not enumerated in CI, NOT an H3 regression). The only thing guarding it is `H3.json`'s `rebuild.yml` post pin `cf4b83c1…`.
* Seal-tip (`:135 (4)`) and the two ledger lines are outside this review; I did not re-measure them.

## RESIDUAL RISKS

* The check-in COMMAND path (`checkin-commands.cjs`) is not driven over a clean-init state by any cell; I drove only `createCheckInModel`/`sleepNightFor` (clean, 0 NaN).
* `model.drip: null` reaches `energy.cjs:286/:302` and `sleep.cjs:674` as bare `+ null` (→ 0) rather than through `dripOf`; unreachable with no snapshots today, silent if that changes.
* `0099-01-01` is accepted as a `split.from` (`until=0098-12-31`); harmless, but the calendar guard is a round-trip, not a range.
* `athlete-state.cjs` now carries a second `dayBefore` beside `checkin-model.mjs:79`; they agree 7/7 today, nothing keeps them agreeing.
* H3/7's `dies()` predicates witness that each mutation took effect; they do not run the named killing cell. I ran it separately (section E) — that evidence lives in this review, not in the suite.

## PROVENANCE · BOUNDARIES

Executed on the owner's Windows PC in `…\work\lane-b\rv-h3` (detached `53450f6`, deps junctioned) and in `…\work\lane-b\rv-h3-scratch\{parent,redA,redB,mut}`, each built by `git archive ce38aa3 | tar -x` plus named file swaps. **Nothing in `rv-h3` was modified except this file; no commit, no push, no branch, no worktree; no other `work\lane-b\*` directory touched.** Never opened, hashed or quoted: `rebuild/conform/private/live.json`, the private `live.main` golden (absent here by design — no fixture was sought), `rebuild/conform/goldens`. `--full` was not run. The seal layer (`:135 (4)`, `:136 (3)`, the ledger lines, `brief.acceptedLedgerLine`) is outside this review by instruction; the `--ci` lines above are reported as MEASURED, not as a seal verdict.
