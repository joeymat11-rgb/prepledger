# H3 — REVIEW r2 — ACCEPT WITH CHANGES — SEALABLE now: NO

head `2935179` (`rebuild/lane-b-h3`) · declared `sourceBase` `ce38aa3` · merged chain tip `ec80cbe` · live chain tip `3bb522b` · runner on disk `4482bb8a…` (= `H3.json` pin) · reviewer independent/blind · node v24.18.0 · `MEASURED_TEST_NOW=2026-09-03` `TZ=America/New_York`. Every number below EXECUTED in `rv-h3b` or in a git worktree at `2935179` under `rv-h3b-scratch`. **The engineering is sound and r1 finding 1 is closed; what blocks the seal is mechanical and is in `H3.json`, not in the code.**

## THE BLOCKER, AND IT IS NOT WHAT THE HAND-OFF EXPECTED

**`--ci --package H3` is `exit 2`, not `PUBLIC CI EVIDENCE PASS` exit 0.** 41 lines, all ten declared children green, then: `OPEN theme ledger line accepting this brief is null (THEME-AUTHORIZATION-UNAVAILABLE…)` · `OPEN brief rebuild/lanes/b/BRIEF-H3-CLEAN-INIT.md not accepted by a PM ledger line` · `OPEN closed cumulative profile not sealed` · `CI REVIEW-PENDING: 2 open obligation(s); public evidence only; no PASS is claimed`. **Cause, measured:** `H3.json` still carries `authorizations.theme: null` and `brief.acceptedLedgerLine: null`, and this head's `rebuild/DECISIONS.md` is **161 lines** while `:165`/`:166` stand at lines 165–166 of the **166-line** file on `origin/rebuild/t2-client-core`. The brief's own §9 says `exit 2 … 2 open obligation(s) — theme and brief, and nothing else`, so the brief is honest and the hand-off's expectation is the thing that is wrong. Both lines exist and are well-formed: `:165` THEME, 690 B, terminal `ACCEPTED`, sha256 **`48496d9fe6907f845933af72151dc15e09f17e10702081dc96470c03fcea5dd3`**; `:166` BRIEF-BY-SHA, 843 B, terminal `ACCEPTED`, sha256 **`4816ca9b3ed613f4130802fb11613a1d92cee144fd648d8f3ba4fd261cf60a21`**, citing `67e95134…` and `(34208 bytes)` — both of which match the brief on disk exactly. Fill those two fields (and merge the chain so the worktree carries the lines), re-run, and this verdict flips.

## FINDINGS (ranked)

1. **The blocker above.** Two null fields in `H3.json` + a chain merge. No code change.
2. **The live chain tip is NOT an ancestor of HEAD.** `git merge-base --is-ancestor 3bb522b1 2935179` → exit 1 (`ec80cbe` → exit 0). `SEAL_TIP_RULE` is `'ancestor'`, so a seal taken now stands on a tip two lane-C/lane-D commits stale — and those are the commits that carry `:165`/`:166`. Merge before sealing; do not seal on `ec80cbe`.
3. **Brief v1.9 still speaks as v1.6/v1.7/v1.8 in four places.** §6 header "**v1.8** re-measures every row"; §9 "**v1.7** §9 measured the real one"; §9 "this revision (**v1.7**) supersedes the accepted v1.3 bytes, so the BRIEF-BY-SHA line **must cite v1.8**" (it cites v1.9, `67e95134…`, correctly); §9 "The v1.6 pin … was STALE". A v1.9 document that calls itself v1.7 and instructs a citation of v1.8 is self-contradictory on the one page the `:166` obligation is discharged from.
4. **Two runner shas in the brief are stale against the pin and the disk.** §9 names `59ecc7f9…` (F-D/F-E, at `rebuild/lane-b-tooling` `978d821`) and `c8dfdd49…` (the supersession role, at `0663df1`). The runner on disk and in `tooling.runnerSha256` is **`4482bb8a…`**; `--ci` line 1 prints it. Neither named sha exists on this head.
5. **`h3-supersede-inherited-carriers.test.cjs`'s header over-promises.** It states its replacement includes "the accepted writers factory and H3's … composed on the SAME engine table and must agree on every state the frozen record can contain". Its three cells do not do that; that differential lives in `h3-supersede-writers-differential.test.cjs`. Reading the file alone, the claim is unbacked.
6. **Two internal number errors.** §7 says "RED at the parent: **1 pass / 13**"; measured in a git worktree with the parent's `athlete-state.cjs` + `writers.cjs` + `constants.cjs` it is **2 pass / 12 fail** (14 cells). And §6 gives `athlete-state.cjs` post `d0e26f74…` (correct — disk agrees) while the sibling note gives B1's pre-image for the same file as `e1e05a63…`.
7. **A weakened supersession cell is refused only as a byte pin, not as weakness.** Bite 7c (replace `assert.equal(CARRIERS.length, 4…)` with `assert(CARRIERS.length >= 1…)`) → `--ci` `FAIL UNLISTED-PRODUCT-DRIFT` exit 1. The runner cannot tell a weaker cell from a stronger one; it sees only that a declared product byte moved, so a lane that re-pins a weakened cell passes. That is precisely the judgement `:153` asks the reviewer for, and it is worth one sentence in §9 rather than leaving the runner to look like the guard.

## ESTABLISHED (1)–(7), all EXECUTED

* **(1) SCOPE PASS.** `git diff --name-status` vs the declared `sourceBase` `ce38aa3` **and** vs the live tip `3bb522b1` gives, under `rebuild/engine`/`rebuild/conform`/`rebuild/m4/workout`, the identical set: `M engine/constants.cjs`, `M engine/writers.cjs`, `M m4/workout/athlete-state.cjs`, `A` the 7 new `m4/workout/test` files. `rebuild/conform` untouched (0 paths). `rebuild.yml` unchanged since r1 (`cf4b83c1…`). All 13 §6 hunk shas verified on disk: `d0e26f74 · 0522797d · 106113ba · cf4b83c1 · 7b78319e · e4b797eb · fe247d2d · dc92e918 · 69c8d05f · e41ec9f2 · fec3f052 · d186664a · d0f59c48` — **13/13 exact**.
* **(2) OPTION B PASS, DRIVEN TO THE SCREEN.** `if (!sealed && !offW) s.trend = first ? w : +(s.trend + 0.3*dCl).toFixed(1);` — one window test, both branches. In-window: `trend=186.4`, `pt=null`, `note=""`, slot `"Not available yet"`, 0 NaN. **Late (hour 23): `trend` member ABSENT** (`Object.hasOwn`→false), `offWindow=true`, `note="late read — set aside"`, slot `"Not available yet"`, 0 NaN, `latestRead=186.4` (the reading is kept, not discarded). **Sealed: `trend` ABSENT**, `note="sealed — excluded from trend"`, same F-A surface. A set-aside row followed by an in-window one seeds `186.9` with both rows kept. **r1 finding 1 is closed: copy and arithmetic now agree.** Legacy differential: 10 `applyRead` calls (5 days × hours 8 and 23) on a trend-carrying athlete give a **byte-identical** transcript under H3's and the parent's `writers.cjs` (`9d46bc5f10af` both trees). Census: same directory, with and without H3's two engine files → **same sha `cc4d89716fe8`, 20,394 B** (UTF-16 capture; the identity is the claim, not the byte count).
* **(3) `sleep.needed` PASS.** Value is `require('../../engine/constants.cjs')().SLEEP_ANCHOR_MIN_N` = **3**, not typed; `Reflect.ownKeys(state.sleep)` = `["nights","needed"]` = `SLEEP_MEMBERS`; `closed()` refuses an added member and a renamed one (`STATE_SLEEP_MEMBER_SET / nights,needed`, both measured); clock-free (identical under a foreign `TZ`). Four readers driven: `sleepInfo` now `{"run":0,"atTarget":false,"clean":true,"need":3}` (was `need` ABSENT and dropped by `JSON.stringify`), `fiveLevers().sleep.detail` `"0/3 clean"` (was `"0/undefined clean"`), `atSleepTarget` `{run:0,at:false}`, `recoveryIndex` no longer prints `"0 of undefined clean nights"` or `"NaN more nights"` — **0 `undefined`/`NaN` anywhere**. **F-G stands as disclosed:** three clean 8.3 h nights still give `run:0, atTarget:false`, because `sleep.cleanH` is deliberately absent.
* **(4) SUPERSESSION — five carriers weighed, one line each.** All five suites go RED on the parent's engine bytes in a **git** worktree (my first attempt in a `git archive` tree was invalid — three of them call `git show`, so their RED there was a missing-repo artefact, not evidence): **3/4 · 3/3 · 2/3 · 2/3 · 1/3 cells RED, 11 of 16, every suite exit 1**; green 16/16 at HEAD.
  * `source-carriers` — **COVERED.** Same mechanic rebased: a literal 4-row carrier list applied FORWARD to the `sourceBase` blobs and INVERSE to disk, both byte-exact to the declared pre/post, every site unique, the `rebuild/engine` `.cjs` inventory closed, exactly the two declared files differing. Caveat: the parent's own `BASE` + 48-carrier reconstruction is inherited by pin, not re-executed.
  * `inherited-carriers` — **COVERED** (for its cells): every prior module but the two declared is byte-identical to `sourceBase` *and* to the parent artifact's product pin; both `constants.cjs` privately compiled and the exported key set diffed to **exactly one** moved key (`MG_LABEL`, four added BACK heads, no existing gloss touched); the four heads proved inert tree-wide. See finding 5 on its header.
  * `defect-witnesses` — **PARTLY.** The carrier's shape (complete comparison, both sides produced and read out in full) is reproduced faithfully for **3** witnesses, including `:124`'s refusal against the parent's own constructor compiled from its blob. The carrier's **10/10** comparisons for the parent's ten defects are not re-run and remain inherited by pin.
  * `writers-differential` — **COVERED.** The same three Date/trap modes, both factories on the same engine table, **15/15** `deepEqual` on every trend-carrying state, difference only where the trend is absent, H3's output identical frozen/live/trapped, and the `first` binding pinned to one declaration + four consumers. Battery is hand-written (7 rows) and confined to `applyRead`; the rest of the file is carried by the byte cells above.
  * `second-gate` — **PARTLY.** "The public record does not move" is reproduced in-process: 15 readers over the frozen seed record compared as one string, plus `applyRead` itself, byte-identical. The gate's **independent second route** through the whole engine is not re-run; the census line carries that. Its closing `assert(migrate.includes('trend'))` is near-vacuous.
  * **Engine-files differential — REPRODUCED.** Child exit 0: `27 tracked rebuild/engine file(s) outside this package's declared product, all byte-identical to the parent; 2 named files move and each stands at its declared post-image`. Independently from Git: `git ls-tree -r rebuild/engine` at `2935179` = **45** files, `H3.json` declares **18** under that root, 45−18 = **27**; `git diff --name-only` vs both `50fa37a` and `ce38aa3` under `rebuild/engine` = exactly `constants.cjs`, `writers.cjs`.
* **(5) RUNNER.** `--ci` exit **2** (see the blocker), with `COVERAGE 0/19 … 9 SUPERSEDED`, `SUPERSESSIONS 5 byte-identity carrier(s) … over 9 gate(s) under DECISIONS:160 … BY ITS OWN SHA256 3746ff573af4`, five `SUPERSEDED` + five `SUPERSEDED EVIDENCE` lines, `NO-REGISTER OBLIGATION … 8 of 8 declared child(ren)`. `:160` line sha recomputed from the chain bytes = `3746ff57…` (matches `coverage.superseded.rulingLineSha256`); `:153` = `b6f84af6…` (matches). `--full` exit **2**, terminal `B PACKAGE H3 BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING` — quoted as measured.
* **(6) BRIEF v1.9 vs v1.3 — PASS on scope, FAIL on self-description.** v1.3 at `9a01d6b` = `60567b60…`, 144 lines, 22,468 B (matches `:166`'s prefix); v1.9 on disk = `67e95134…`, 174 lines, **34,208 B** (matches `:166` exactly). Diff is **58 insertions / 28 deletions**, and every hunk maps to one of the six amendments `:166` lists — option B (§4, the held A/B question replaced by the ruled branch), the r1 corrections, the bounded label half, `sleep.needed` (§3a) + F-G, the `:153`/`:160` supersession section with one red-first cell per carrier, the `:153 (ii)` differential — plus three consequential re-measurements it also names (hunk shas on the merged tip, the 321/321 today count, the census re-capture) and two new OPEN items, **F-H** and A4b's amended re-pin cell, which `:166` does **not** list. I judge those two in scope as consequences of the tip merge, but they are additions to a by-name-accepted brief and the PM should see them named. Findings 3, 4, 6 and 7 are the failures. **F-H verified:** `rebuild.yml` contains neither "catalogue" nor "problem"; 11 test files sit under `today/test`, 8 are enumerated.
* **(7) BITES, 8, all restored.** Seed with a **constant** (`first ? 170.0`) → killed by 5 suites / 11 cells (H3/8, H3/9, H3/12, SUP-8/9/11/12/15/16, SUP-1/2). A **copied seed value** (`anchorISO:'2026-07-21', drip:0`) → killed by H3/2 and H3/7 (mutant M3). A **weakened supersession cell** → `FAIL UNLISTED-PRODUCT-DRIFT` exit 1 (finding 7). A **moved engine file outside the brief** (`rebuild/engine/test/writers-reference.cjs` + one comment) → the child refuses `engine file outside the brief moved: … (disk 734beebee5ce, parent 42fc5d84a561)` exit 1, and `--ci` refuses **`PIN-PATHS-GIT-DISK-DISAGREE`** exit 1 — **not** `SUPERSESSION-ENGINE-FILE-OUTSIDE-THE-BRIEF-MOVED`; that token is real (`b-package.cjs:2228`) but is shadowed by the earlier fidelity guard for an *uncommitted* move, and I made no commit, so the token itself is unobserved. Plus: `sleep` member added / renamed (both refused), `sleep.needed` typed as `3` (accepted at run time — caught only by mutant M7's source check), a set-aside-then-in-window sequence, and a foreign-`TZ` re-run.

* **COUNTS.** `h3-clean-init.test.cjs` **14/14 exit 0** (2 pass / 12 fail with the parent's three files) · the five `h3-supersede-*` **16/16 exit 0**, all five RED at the parent (11/16 cells) · `h3-engine-files-differential.cjs` **exit 0, 27 + 2** · eight enumerated today files **321/321 exit 0** (`setup.test.mjs` **157/157**) · A0 **23/23** · provider `native-trend-context` **39/39** · `local-host-journey` + `local-today-journey` **68/68** · laws `45 RED-frozen · 39 RED-candidate` · `--ci` exit 2 · `--full` exit 2 BLOCKED.

## EXACT CHANGES

1. `packages/H3.json` — fill `authorizations.theme` with `:165` (its bytes + `lineSha256` `48496d9f…`) and `brief.acceptedLedgerLine` with `:166` (`4816ca9b…`), then merge the chain so `rebuild/DECISIONS.md` carries lines 162–166, and re-run `--ci`. These two fields are the only reason the run says `2 open obligation(s)`.
2. Merge `origin/rebuild/t2-client-core` `3bb522b1` before any seal — it is not an ancestor of `2935179` (finding 2).
3. `BRIEF-H3-CLEAN-INIT.md` — replace the four v1.6/v1.7/v1.8 self-references with v1.9 and delete "must cite v1.8"; `:166` already cites v1.9 by sha (finding 3).
4. `BRIEF-H3-CLEAN-INIT.md` §9 — replace `59ecc7f9…`/`978d821` and `c8dfdd49…`/`0663df1` with the pinned runner `4482bb8a…` at `17341d5` (finding 4).
5. `h3-supersede-inherited-carriers.test.cjs` header — drop the writers-factory sentence or point it at `h3-supersede-writers-differential.test.cjs` (finding 5).
6. `BRIEF-H3-CLEAN-INIT.md` §7 — "1 pass / 13" → **2 pass / 12**; sibling note `e1e05a63…` → `d0e26f74…` (finding 6).
7. `BRIEF-H3-CLEAN-INIT.md` §9 — say that the runner refuses a changed supersession cell as a product-pin drift and cannot judge its strength, so the reviewer's per-carrier weighing (above) is the guard `:153` asks for (finding 7). Optional, same pass: in `h3-supersede-second-gate.test.cjs` replace `assert(migrate.includes('trend'))` with something that can fail.

## PM ITEMS · RESIDUAL RISKS · PROVENANCE

`defect-witnesses` and `second-gate` are **partly** covered — the parent's ten witnesses and its independent second route are inherited by pin, not re-executed; that is the `:153` trade the PM is being asked to accept, and I think it is acceptable given the byte cells and the 27-file differential, but it should be accepted knowingly. **F-G (`sleep.cleanH`) is a live defect on the tree** — three clean nights count as a run of 0 — disclosed and asserted, needing a ruling on `|| 7.5` vs `|| 8`. **F-H** leaves 2 of 11 today test files with no CI home. The four `MG_LABEL` back heads remain inert (no producer sets them) — now said honestly in both brief and code. `athlete-state.cjs` gains a `require` into `rebuild/engine/constants.cjs`; the factory is pure and the module stays clock-free (measured), but m4 now depends on the engine at load time. `SUPERSESSION-ENGINE-FILE-OUTSIDE-THE-BRIEF-MOVED` is unobserved (no commit made). Executed in `…\work\lane-b\rv-h3b` (detached `2935179`, junctioned) and in `…\work\lane-b\rv-h3b-scratch\{p0,p1,red,redw}`; `redw` is a throwaway `git worktree` at `2935179` used for every mutation and left `git status --porcelain` **clean** after each bite. **Nothing in `rv-h3b` was modified except this file; no commit, no push, no rebase; `rv-h3` untouched at `53450f6`.** Never opened, hashed or quoted: `rebuild/conform/private/live.json`, the private `live.main` golden (absent by design — `--full` reaches exactly that BLOCKED line), `rebuild/conform/goldens`.
