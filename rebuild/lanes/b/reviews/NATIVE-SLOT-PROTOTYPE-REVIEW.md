# PM402 — independent standalone native-slot review

**No blocking finding for the exact PM384/386 Windows standalone capability.** The independent first verdict is preserved after complete author reconciliation. PM is the sole acceptance judge; original R1 capture rejection remains unchanged.

Candidate: `2592f091f9cd2956f5f6c442d36c8763e247cff6`; base: `126fac49474ab2ba8e3dccf7ceb0707ddf65d113`. Authority PM402 at `16a628ed5bfbaa600f3240b80471280c11ae9d3a`, GO SHA256 `66e90b06b147ab963e479bfcb32f432c7b8b0995f9e50f96d49f546b9bce8647`, review input manifest `56423671e38fadda78358b2f1d7bb02549e8e593cff7b4aa93161b1d4145411f`.

ER used fresh `work/pm-caretaker/native-slot-prototype-review-er`, branch `codex/astra-native-slot-prototype-review-er`, Astra/MAX only. The complete 2,373-entry index preceded licensed hydration. All five unchanged source files and directly needed pinned Node/Windows contracts were inspected before execution. No other actor's workspace was operated.

Independent first assessment: `dd4ae18da24a614d468ced172cdf5cc89766d402`, committed and sent to PM before new author outcomes were opened. [FIRST-ASSESSMENT.md](native-slot-prototype-review/FIRST-ASSESSMENT.md) SHA256 `4e986798767e4a79933b7ea4fdc127380939121c93ca4794d11280627af17eed` remains unchanged, with its original evidence.

The source implements a tagged opaque external with a separate native root, explicit close and environment cleanup/finalization, exclusive Windows mapping ownership, fixed identity/header checks and aligned interlocked irreversible progress. It does not return a dead V8 handle or supply a fallback/local shared buffer. The loader and declared writer graph have the limitations below.

The reviewer acquired each of the four exact pinned portable tool artifacts once, verified sizes/hashes and safe unique extraction, then built the unchanged candidate and its version2/size128 fixtures once. The compiler version check and all three builds exited 0; raw arguments, environments, source/tool/library/binary identities and streams are retained.

Independent delivered run: **34/34, exit 0**, zero skipped/cancelled/todo, 5,509.4647 ms; 33 actual outer runners plus 33 separate test-file processes. Independent controls: **4/4, exit 0**, 267.2041 ms, comprising three behavioral controls and one evidence census; five additional child processes. Thus 71 invented child processes were observed independently, excluding top harness/build/compiler processes.

Two-record state4, partial states0–3 and sticky refusal5 were exercised. Late extra progress survived the closed-descriptor diagnostic failure; a complete local-buffer file left the parent's state0. Parent/child mapping lifetime, GC roots, repeatable close, non-owner/forged/closed handles, missing/mismatched setup, stale identity and concurrent writers were checked. The bounded controls are evidence, not exhaustive failure/scheduling proof.

Both cohorts retain the intentional writer exit7 → outer exit1 and raw **2 tests / 1 pass / 1 file failure**. State4 did not erase that failure. Neither the first failure nor its actual census was relabeled as a normal child pass.

Author reconciliation covers full handoff `9cc78e7701957569fd603b9013d2ac454dacb2dd`: five authored records (report, INDEX, INPUT-CUSTODY, SOURCES and MAPPING) plus every mapped public copy. All 249 mapped copies / 5,490,494 bytes match; with five authored records, 254 copies / 5,593,988 bytes are retained. All 114 JSON files were parsed and all 20,830 extraction-file identities compared. Full Git inventory confirms 259 additions, 2,368→2,627 paths, with all five source inputs unchanged.

The author's **34/34, exit0** and 33 nested graphs reconcile with complete raw streams, traces, source/configuration identities and first failures. Its 66 child PIDs are distinct; 32 graphs are 1/1 exit0 and the one nonzero graph remains failed. Nineteen separate owner-state records agree; the remaining state/lifetime claims rely on the pinned parent assertions and corresponding passing top-level tests, not invented extra state files.

Author and independent native binary hashes differ despite the same source/tool pins and normalized compiler arguments. Behavior was independently reproduced; bit-reproducibility and the cause of differing bytes were not established. The author used `downloads/node.exe` and the reviewer its byte-identical `runtime/node.exe` copy. Author binaries were not opened or executed.

Preserved limits: logical64-byte view/header validation, not exact section-size interrogation; declared generated writers, not arbitrary descendants; lexical loader containment, not symlink/loader-hook authentication; trusted compiler/runtime/Windows behavior beyond inspected contracts; no universal OS/VM fault or hostile-native-writer proof. Original noninterference/argv parity, actual helper writer closure, private two-record reduction, integration and other platforms remain unproved.

Historical setup truncation/corrections, original404, PM-reported provider/model events and retained-only-final unused import-library output remain attributed and visible. The first independent archive correction and final offline-checker location correction are preserved. No candidate, compiler or test input was patched, and no unchanged passing runtime run was repeated.

[RECONCILIATION.md](native-slot-prototype-review/RECONCILIATION.md) supplies the claim-by-claim comparison; [RECONCILIATION-CHECKS.json](native-slot-prototype-review/RECONCILIATION-CHECKS.json), [MAPPING-FIRST.json](native-slot-prototype-review/MAPPING-FIRST.json), [MAPPING-AUTHOR.json](native-slot-prototype-review/MAPPING-AUTHOR.json) and [FINAL-CUSTODY.json](native-slot-prototype-review/FINAL-CUSTODY.json) supply complete identities. Verbatim raw files have scoped `-text` attributes and staged Git-blob hash verification. No binary/archive/SDK/cache tree is committed.

No original helper/operator, engine/app/private/history/golden/native gate, H3/FULL/CI, integration or deployment ran. No spending, account/reset, model switch or new writer occurred. Complete branch/head/report/hash handoff goes to PM immediately; ER then returns LOW/paused320.
