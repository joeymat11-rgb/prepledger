# Independent tooling correction review — Astra final

2026-09-11. VERDICT: ACCEPT for the bounded tooling correction.
Reviewer did not build the tooling or B-NTC. PM retains acceptance authority.

## Exact source and scope

- Candidate: c7b7133f3611f0bff98f35c5626d14571b38fbb7.
- Base: 7748880701ef62246c8362c43f760ee10f86c2cc.
- Prior independent review: b148622da0937fc13529e9aa3b0fb80c86fe4028,
  rebuild/lanes/b/reviews/TOOLING-REVIEW-ASTRA-r5.md.
- Runner SHA256: e08580e7bb6b044d578f44f6bb685ff850f606d62e8043be6dbd2004dd39512c.
- Separate branch codex/astra-tooling-review-final, work/astra-tooling-review-final.
  Candidate tracked files remained byte-identical throughout review.
- Governing assignments read from work/astra-pm-resume/rebuild/lanes:
  ASTRA-RESUME.md, ASTRA-TOOLING-FIX.md, ASTRA-CHECKPOINT.md; AGENTS/LANES read.
- Reviewed nine-file delta: runner, six mechanical runner pins, focused regression,
  author report. No product, frozen law/oracle, workflow or lockfile delta.

## Executed evidence

Existing Node v24.19.0 on Windows. Final independent probe: 20 controls PASS, exit 0.
The unchanged runner is loaded before its main-campaign delimiter; source functions
execute directly, actual Node children run, and the exact existing Y1 seal block is
extracted without constructing an accepted envelope. Real B-NTC spec and genuine
NATIVE-CARRIERS parent bind. The controls establish:

1. Bare first-pass/second-throw shape now refuses CHILD-ARGV-BARE-SCRIPT-ARGUMENTS
   at target validation, ownChildren, children and the seal block. proposed() also
   refuses the unused target, preventing a divergent execution-pin list.
2. Direct own success executes and passes the own-child seal block; absent execution
   refuses OWN-CHILD-DID-NOT-EXECUTE. The shipped no-own declaration refuses
   SEALED-WITHOUT-EXECUTING-ITS-OWN-PRODUCT. Direct throwing own code refuses
   Required child, with its actual exception observed in the child log.
3. True --test with explicit TAP reporter executes both good files, records both
   targets and pins both bytes. The second file's unique output is observed.
   True --test with a throwing second file refuses Required child and logs that
   exception. Execution mode, not a positional-file filter, determines targets.
4. All five actual inherited child names, preserving the real nine-gate map but
   preceded by a passing bare script, refuse before coverage can count originals.
   A separate public component control directly executes its pinned file and covers
   its one inherited entry. No original gate was replaced or blanket campaign run.
5. Actual parent plan.cjs carried/edited modes pass product(); role:new refuses
   PARENT-PRODUCT-PIN-NOT-DECLARED-CARRIED-OR-EDITED, and wrong preimage refuses.
   Actual parent rebuild.yml superseded-by-child with correct preimage passes;
   role:new and wrong execution preimage both refuse.
6. Exact root list is the prior five plus only rebuild/m3/w6/host/test/ and
   rebuild/m3/w7-preview/today/test/. Actual synthetic Node tests execute from both.
   Real journey.test.mjs and gym.test.mjs paths validate. Existing adjacent host
   and Today sources and the tooling runner refuse as child targets. Traversal,
   absolute Unix/Windows, backslash, invalid extension/argument and unsupported
   flag controls refuse. MOVES_RULING is still null.

Also executed: committed execution-targets.test.cjs 9 passed / 0 failed / 0 skipped;
runner --check exit 0; six runner SHA pins match; git diff --check exit 0;
git diff --quiet c7b7133 before report creation exit 0; clean working tree then.
The committed regression additionally exercises genuinely new product admission,
execution-pin carried/edited refusal, duplicate reporter and no-target controls.

One initial reviewer control requested a TAP needle without specifying the TAP
reporter; Node emitted its default reporter and the runner correctly refused
CHILD-NEEDLE-NOT-A-TERMINAL-LINE. The harness was corrected to request TAP explicitly;
no candidate change was made. Final 20-control result above is after that correction.

## Retained evidence and limits

r4 authority/receipt ancestry, exact-artifact envelope, X1/X2 and frozen-gate evidence,
and r5 Y2 single-parent evidence are retained as attributed prior review, not new
full reruns. No findings remain from r5's three requested corrections in this scope.

Local reproduction: existing executable
C:/Users/joeym/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe
runs .tmp/astra-review-final/probe.cjs; final log .tmp/astra-review-final/probe.log.
These ignored review aids use five newly generated public fixtures, removed only
after checking their exact worktree paths. No existing data was deleted. The single
inherited positive uses an in-memory component bound, not a fake artifact or receipt.

This is not a package PASS or permission to seal B-NTC. Exact successor mappings,
assertion-preserving execution evidence and the PM ledger authority require separate
review before nonempty coverage.moves can be admitted. Full/private readiness and
acceptance-envelope obligations remain. No private input, install, broad campaign,
push, merge, deployment, protected-soak action or fabricated receipt occurred.
