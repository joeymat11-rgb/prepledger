# TOOLING — REVIEW r10 (9089f48→bd30ed9) — REJECT — the gate-supersession role is SAFE to seal H3 under a PM line of the proposed shape: NO
Blind, MAX, executed at **bd30ed9** in a fresh worktree `rv-tool10` (runner `bd76b8ef49b1…`, 213 247 B, 2 672 lines, re-pinned in all seven specs, every one `coverage.superseded: null`), node v24.18, `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`. Scope is tooling only (`git diff --stat`: 13 files, all under `rebuild/lanes/b/tooling/`). Harnesses compile the REAL runner with `CHAIN_REF` re-pointed and assert exactly one line differs.
## F1 BLOCKING — the "PM's own bytes" gate is a keyword scan, and a line already on the chain passes it
`supersessionRuling()` (b-package.cjs:887-906) asks three things of the located line: `line.includes('M2-'+ID) || line.includes(s.packageId)`, `SUPERSESSION_GRANT.test(line)`, `BYTE_IDENTITY_CARRIERS.some(c => line.includes(c))`. All three are substring/keyword tests over prose. **Measured, by executing `supersessionRuling()` against the real chain's `rebuild/DECISIONS.md` at `f72eacfb` with `--package B-NTC`: `DECISIONS:112` is ADMITTED** as a gate-supersession ruling (3 822 B; its "grant" is the incidental clause *"…exactly as NATIVE-CARRIERS' inherited-carriers superseded LOAD-WRITES"*). That line rules `MOVES_RULING B-NTC-INHERITED-1`, not any supersession. And it is not inert: `acceptance-native-carriers.json`'s own `coverage.byChild` names `inherited-carriers` for `witnesses-2 witnesses-5 migrate-differential`, so `-CARRIER-IS-NOT-A-PARENT-CARRIER` does **not** stop it either — M2-B-NTC could retire three of the nineteen today, citing a line the PM wrote about something else. Scan of all 152 chain lines: 1 already passes all three content tests. A line that *refuses* the role ("M2-H3-CLEAN-INIT may NOT declare source-carriers SUPERSEDED; option (b) is REFUSED") also passes all three (measured). **The admission must be positive and structured, not a keyword scan.**
## F2 BLOCKING — the ruling need name ONE carrier; the spec may then take all five, and nine gates
`supersededGates()` never asks the ruling about the carrier in hand; only `some(c => line.includes(c))` at ruling level. Measured: a fixture PM line reading *"M2-H3-CLEAN-INIT may declare second-gate SUPERSEDED and nothing else"* ADMITS all five carriers → **9 gates** (`PROBE 4h`). Also worth the PM's attention: the request asks for "five gates"; the runner correctly expands the five carriers to the **nine** original gates they cover (COVERAGE prints `9 SUPERSEDED`, `run=10`), i.e. the ruling retires 9/19, not 5/19.
## F3 BLOCKING — the role can never actually seal: the seal assert is asked BEFORE `coverage()` admits anything
`SUPERSEDED_RESOLVED` is written once, by `coverage()` (b-package.cjs:2129), at main-sequence **line 2604**. `envelope()` is first called at **line 2584** and, on an `ACCEPTED` review, unconditionally runs `assert(SUPERSEDED_RESOLVED.size, 'GATE-SUPERSESSION-NOT-ADMITTED-AT-SEAL')` (:2482-2485) — with the map still empty. Measured end-to-end on a sealed+`ACCEPTED` fixture artifact (`same(m, proposed())`, `sealOnTheTip` and the moves assert all passed): `PROBE 8b-first-envelope-before-coverage => GATE-SUPERSESSION-NOT-ADMITTED-AT-SEAL`; the identical call with the map populated gets past it (`=> NO-REGISTER-PACKAGE-SEALED-WITHOUT-EXECUTING-ITS-OWN-PRODUCT`, the next assert). Since `POSTFIX PACKAGE PASS` requires `last.authorized`, i.e. an ACCEPTED review, **no package that declares a supersession can ever reach a PASS.** Fail-safe in direction, fatal to the purpose. Neither the suite (which never calls `envelope`) nor the builder's probe (which stopped at a child) could see it. Fix: guard the assert on `ran` (`if (ran && s.coverage.superseded != null)`), exactly as the Y1 execution half already is at :2500.
## F4 — "re-taken at the seal" is a cache hit; the chain is read once per process
`supersessionRuling()` short-circuits on `SUPERSESSION_KEY === sup.rulingLineSha256` (:891), so the seal call re-validates the **cached string**, never re-reading `rebuild/DECISIONS.md`. Measured: first read ADMITTED → ruling line deleted from the chain and the commit amended → `CHAIN NOW CONTAINS THE RULING LINE: false` → second call still **ADMITTED** (`PROBE cache-B2`). A fresh process does refuse (`PROBE cache-A => …NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH`, and bite 11 end-to-end). So "a seal cannot stand on a withdrawn ruling" is true across runs and false within one — which is the only window W5's own re-evaluation exists to close. Same pattern in the pre-existing `RULING_TEXT` cache (:804); fix both by keying the cache on the resolved chain tip.
## F5 — the "own evidence" is one child named three times, and the two run-phase refusals are dead code
No rule requires the evidence to differ per carrier, to be distinct children, or to bear on the gate. Measured ADMITTED: one declared green child in all three child slots (`PROBE 5i`, `executed=["h3-cells","h3-cells","h3-cells"]`, 9 gates) and a child executing a file with no relation to any carrier (`PROBE 5j`). In the real H3 probe all five carriers carry the **identical** evidence triple. Separately: `children()` sets `ok: true` unconditionally and throws `Required child <name>` on exit≠0 (:1790), so `GATE-SUPERSESSION-EVIDENCE-CHILD-NOT-GREEN` and `-NOT-EXECUTED` are **unreachable in any real run** — the executed-ness claim holds (children() enforces it earlier and harder), but the role's most likely real failure prints a bare `FAIL` with no code, since `Required child` is not in `FAIL_CODES`. Measured in the control run and in the builder's own probe.
## F6 — misattribution of the ruling, in the code, the FAIL messages and the artifact prose
`DECISIONS:147`'s `(b)` is *"(b) split is refused as the plan of record"* / *"CONTINGENCY: … lane B ships (b) H3-core at once"* — the H3-CORE split. It says nothing about supersession. The role is lane B's **REQUESTS 2026-09-12 08:40 (b)**, still unruled. ~30 sites say `DECISIONS:147 (b)`, including user-visible asserts (`'DECISIONS:147 (b) needs a PM line…'`, `'DECISIONS:147 (b) frees exactly …'`) and the README/REPORT. Re-label to "REQUESTS 08:40 (b), pending a PM line"; the terminal's own `DECISIONS:<n>` (from `SUPERSESSION_AT`) is correct and should stay.
## F7 — disclosed cost, confirmed: `proposed()` gains two `coverage` keys, so no existing sealed artifact recomputes
`coverage.superseded` + `coverage.supersessions` are now always emitted (:2260-2262); `acceptance-b-ntc-native-trend-context.json` on `origin/rebuild/lane-b-h3` carries `coverage` keys `covered run moves successors byChild`, so `same(m, proposed(s,bound))` cannot hold and `SEALED-PROFILE-RECOMPUTATION` is the refusal. The report's OPEN 4 says this; it means B-NTC needs a re-seal (hence a re-acceptance) before H3 seals under this runner.
## B — admission surface, measured (own fixture over the real runner; codes are the runner's own)
| # | control | code / result |
|---|---|---|
| (1) | a sixth gate, five spellings (`cases traces witnesses conformance migrate-full`) | `GATE-SUPERSESSION-CARRIER-IS-NOT-A-BYTE-IDENTITY-GATE` ×5 |
| (2) | one of the five, absent from the parent's `byChild` | `GATE-SUPERSESSION-CARRIER-IS-NOT-A-PARENT-CARRIER` |
| (3) | carrier claimed by a successor **and** superseded | `GATE-SUPERSESSION-CARRIER-IS-ALSO-CLAIMED-BY-A-SUCCESSOR` |
| (4) | `null` · bad shape · no such line · names nobody · no grant word · no carrier · other package | `…RULING-NOT-CITED` · `…SHA256-SHAPE` · `…NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH` · `…DOES-NOT-NAME-THIS-PACKAGE` · `…DOES-NOT-GRANT-A-SUPERSESSION` · `…DOES-NOT-NAME-A-BYTE-IDENTITY-CARRIER` · `…DOES-NOT-NAME-THIS-PACKAGE` — **but F1/F2** |
| (5) | undeclared child · declared-not-run · red · laws w/o D-id · laws w/o accepted brief · self-printed census string · runner census line while live-triggered · empty `redFirst` | `…EVIDENCE-CHILD-NOT-DECLARED` · `…NOT-EXECUTED` · `…NOT-GREEN` · `…LAWS-MOVED-OUTSIDE-THE-REGISTERED-INVENTORY` · `…LAWS-MOVED-WITHOUT-AN-ACCEPTED-BRIEF` · `…CHILD-NOT-DECLARED` · `…CENSUS-LINE-IS-NOT-CLEAN` · `…RED-FIRST-UNDECLARED` — **but F5** |
| (6) | bound: `inherited` keeps a superseded gate · drops a non-superseded one · adds an extra · empty `gates` · non-object block | `…GATE-IS-ALSO-INHERITED` · `INHERITED-COVERAGE-IS-NOT-THE-PARENT-COVERED-SET` · same · `…GATES-UNDECLARED` · `…BLOCK-UNDECLARED`. Cannot exceed or fall short: `deepEqual(inherited, byChild∖superseded)` pins it, so `COVERED-SET-BOUND` is now arithmetically redundant (belt-and-braces, not a defect). Mixed case measured: `covered=6 superseded=3` |
| (7) | `--full` sweep with 3 gates superseded (`R.gateRun` recorded) | `re-executed=10 / 19`, `overlap=0`, skipped exactly `merge-source migrate-source writers-source`; with no supersession `re-executed=19`. Set still closed against `GATE_IDS` |
| (8) | artifact · ruling edited after the seal | `superseded=9 run=10`, disjoint, ruling sha + all five evidence names recorded ✔ · **re-take does not refuse within the run (F4)**, and the sealed case is unreachable (F3) |
## C — the honesty question, gate by gate (what was proved for NATIVE-CARRIERS vs what the named evidence proves)
| carrier (gates) | what the byte-identity gate proved | does the named evidence cover it? |
|---|---|---|
| `source-carriers` (migrate/merge/writers-source) | the 48 literal carriers of `native-carriers-changes.json` (pinned by `CHANGES_SHA`) reconstruct the accepted engine sources byte-for-byte from the frozen `BASE` — i.e. the shipped bytes ARE the accepted delta | **NO.** A child that touches `writers.cjs` necessarily breaks it, and cells/census/differentials prove the child agrees with *itself*, never that its bytes are the accepted delta over BASE. Honest replacement = the child's **enumerated, sha-anchored accepted-brief hunks** over the parent's blob + the legacy differential. The runner names neither |
| `inherited-carriers` (witnesses-2/-5, migrate-differential) | the LOAD-WRITES carriers still reconstruct, and the migrate differential against that reconstruction is identical | **PARTIAL.** The legacy differential is a genuine like-for-like, but it runs against a file in the child's own tree, not the parent's accepted engine read from Git at the parent acceptance commit |
| `defect-witnesses` (witnesses-7) | ten complete witness comparisons against the reconstruction | **MOSTLY.** The 45-law register row is a real independent substitute — but `laws: null` is a *declaration*; nothing binds it to the `LAWS 45/45 … AUDIT RED-FIRST` line the same run prints |
| `writers-differential` | writers output identical across the 3 Date/trap modes against the reconstruction | **MOSTLY** — the closest substitute in the set; same baseline weakness as above |
| `second-gate` | an independent second implementation gate over the reconstruction | **NO ANALOGUE.** Nothing in the named evidence is a second independent implementation. This is the largest unpriced loss |
**So the ruling text should be strengthened**, and in these five words: (i) name **every** carrier it frees **and the nine gate ids**; (ii) require, per carrier, the child's **enumerated sha-anchored brief hunks** against the parent's blob at the parent acceptance commit; (iii) require the legacy/writers differentials to load the **parent's accepted engine out of Git**, not a tree file; (iv) require **distinct** evidence children per carrier (at least one unique to each) and forbid one child filling three slots; (v) say out loud that `second-gate` has no substitute, or refuse that one carrier. Add to the runner: per-carrier ruling-name matching, and a positive ruling shape (e.g. `GATE-SUPERSESSION <packageId> <carrier>…`) instead of a keyword scan.
## D — regression, terminals, bites
**Suites 94/94, 0 fail** (measured: eight tooling suites **85** + shared `rebuild/lanes/tooling/test/preflight.test.cjs` **9**; `gate-supersession` 10/10). ≥6 r9b controls re-run as named cases, all pass: paste-the-loaded-body (`r9 F1`, `Z2 …COPIES…`, `:147 the copy test moved with the floor`), review custody (`r9 F3 … under the reviews directory`), the four spec-phase successor refusals (`r9 F5`), seal-tip ancestry (`:135 (4)` ×4 + `r8 F1 SEAL_TIP_RULE`), byte-identity receipt in Git (`:136 (3)` ×2), the load floor (`:147`). `--ci --package`, **exit 2, `CI REVIEW-PENDING`, no PASS word**: B1 **6** · B2 **6** · B3 **10** · B4 **10** · B-LOM **12** · B-NTC **5** · H3 **12** — every count equal to r9b's/r7's. `--full --package B1` → exit **2**, `BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`.
**Bites (12, scratch clone `fx-h3`, each restored):** ruling sha `null` → `GATE-SUPERSESSION-RULING-NOT-CITED` · `f×64` → `…NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH` · sixth carrier → `…CARRIER-IS-NOT-A-BYTE-IDENTITY-GATE` · undeclared evidence child → `…EVIDENCE-CHILD-NOT-DECLARED` · superseded+inherited → `…GATE-IS-ALSO-INHERITED` · extra block key → `…BLOCK-KEYS-NOT-CLOSED` · short `why` → `…WHY-MISSING` · `laws:["D7"]` → `…LAWS-MOVED-OUTSIDE-THE-REGISTERED-INVENTORY` · census not a child → `…EVIDENCE-CHILD-NOT-DECLARED` · runner byte edited → `RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER` · **PM line withdrawn on the chain → `GATE-SUPERSESSION-RULING-LINE-SHA256-NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH`** · empty `gates` → `…GATES-UNDECLARED`. All exit 1. Nine uncommitted spec edits additionally refused `SPEC-BYTES-NOT-THE-REVIEWED-SPEC-IN-GIT` before anything else — a control in its own right. Restoration after every bite: `H3.json e95021d2f2e2`, `b-package.cjs bd76b8ef49b1`, chain `ce7dcc9cf3d6`, `git status` empty.
## The H3 probe — it goes FURTHER than the build report says, and the walls are real
`origin/rebuild/lane-b-h3 @ 902fd88` cloned `--shared` to `…/rv-tool10-scratch/fx-h3`; one plumbing commit on the **real** chain tip `f72eacfb` appends a probe ruling line (`DECISIONS:152`, sha `2568f34b1a5d`) and `refs/remotes/origin/rebuild/t2-client-core` points at it; r10 runner + suites overlaid, all seven specs re-pinned, H3's two `superseded-by-child` tooling posts re-stated, `H3.json` marks the five carriers SUPERSEDED, empties `coverage.inherited` and drops the five carrier children. Junctions supply `node_modules` (root, `m3/w6`, `m3/w5`) and the two engines. No worktree touched.
**Every line after `LAWS`:** `LAWS 45/45 executed | TOTAL 45 laws · 45 RED-frozen · 39 RED-candidate · 89 GREEN repair controls · 97/104 mutant executions DETECTED · 0 HARNESS_ERROR · AUDIT RED-FIRST FAIL` → `LAWS DECLARED-STATE 45/45 … register BASELINE` → `CARRIERS NONE DECLARED; 0 witness flip(s)` → `CHILD h3-cells OBSERVED; exit 0, 3140 bytes` → `CHILD a0-journeys OBSERVED; exit 0, 5239 bytes` → `CHILD today-suites OBSERVED; exit 0, 59640 bytes` → `CHILD ntc-provider-cells OBSERVED; exit 0, 8591 bytes` → `COVERAGE 0/19 … 9 SUPERSEDED under DECISIONS:152 … 10 re-execute under --full` → `SUPERSESSIONS 5 byte-identity carrier(s) of B-NTC SUPERSEDED over 9 gate(s) under DECISIONS:152, located on refs/remotes/origin/rebuild/t2-client-core BY ITS OWN SHA256 2568f34b1a5d…` → five `SUPERSEDED <carrier> <- <gates>` + five `SUPERSEDED EVIDENCE …; 3 named child(ren) executed green in this run` → `NO-REGISTER OBLIGATION … 2 of 2 declared child(ren)` → `OPEN theme …` → `OPEN brief …` → `OPEN closed cumulative profile not sealed` → `CI REVIEW-PENDING: 2 open obligation(s)`, **exit 2**. So: `fake-indexeddb` is **not** a blocker in this worktree — `h3-cells` and `a0-journeys` both exit 0 — and `coverage()`, `SUPERSESSIONS` and the evidence lines ARE reached. H3 goes from **12** open obligations to **2** (theme + brief acceptance, both PM-side): that is exactly what (b) buys, and the build report understates how far the role already works.
**The walls, verified independently of the briefs.** Control = same tree, same r10 runner, five carriers NOT superseded → stops immediately after `CARRIERS NONE DECLARED` with a bare `FAIL` (exit 1). Running each carrier directly: all five refuse `AssertionError: Unlisted parent pin drift rebuild/engine/writers.cjs` — `b-ntc-successors.cjs:142` (the `else` branch), because H3 changes `writers.cjs`, a path B-NTC pins and did not declare `superseded-by-child` (`SUPERSEDED` there is only `engine-runtime.cjs` + `rebuild.yml`, :40). Read on the parent blob at `9ad2ecab`: `:141` holds each declared child supersession at `pin.post`, `:145` holds **every** path `packages/B-NTC.json` declares at that spec's own post (`CHILD_SPEC`, :37). So the briefs' generalisation is **correct as measured** — no child of M2-B-NTC that changes any file the parent spec declares can carry the five — with one citation correction: for H3 the refusing line is **:142**, not :141/:145.
## PM items
1. **Do not write a line of the proposed shape yet.** F1 and F2 mean it would grant more than it says; the runner must match the ruling per carrier and stop scanning prose for keywords. 2. Fix F3 before any seal — the role is currently inert *and* unusable. 3. Decide §C: the ruling should name all five carriers **and the nine gate ids**, and require hunk-level + parent-blob-anchored evidence with distinct children per carrier; `second-gate` needs either a substitute or an explicit write-off. 4. Note that (b) retires **9 of 19** gates, not 5. 5. B-NTC needs a re-seal (F7) before H3 seals. 6. Re-label `DECISIONS:147 (b)` (F6).
**Provenance / boundaries.** Read-only outside this one file; no commit, no push, no worktree but `rv-tool10` touched. `rv-tool10-scratch` holds `probe.cjs`/`run-b.cjs`/`b1.cjs`/`b2.cjs`/`b3.cjs`/`b4.cjs`/`b5.cjs` (fixtures over the real runner, one line re-pointed), `setup-h3.cjs`/`control-h3.cjs`/`bites.cjs`/`bites2.cjs`, `fx-h3` (scratch clone, reset to `4d646fd`, clean) and every captured output. No private fixture sought; the private blob was never opened. `--full` reached only `BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`, so the 19-gate matrix was measured by a recorded `R.gateRun`, not by running it.
