# TOOLING — REVIEW r10b (bd30ed9→04bae27) — ACCEPT — the gate-supersession role is SAFE to seal H3 under a GATE-SUPERSESSION token line: YES
Blind delta, MAX, executed at **04bae27** in a fresh worktree `rv-tool10b` (runner `637d4d0f1c75…`, 222 516 B, 2 794 lines, re-pinned in all seven specs, every one still `coverage.superseded: null`), node v24.18, `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`. Diff is tooling + my r10 file only. Every r10 finding was re-attacked with my own r10 harness, re-pointed at this runner; the fixture asserts exactly one line (`CHAIN_REF`) differs from the shipped bytes.
## F1 CLOSED — the grant is a token, and the r10 exploit is dead
`supersessionRuling()` (b-package.cjs:915-950) now requires a `RULED` ledger line carrying `GATE-SUPERSESSION <packageId> <carrier>[,<carrier>…]` and reads no prose. Re-run of my r10 controls: **the real `DECISIONS:112`** (verbatim off `f72eacfb`) → `GATE-SUPERSESSION-RULING-DOES-NOT-CARRY-THE-GRANT-TOKEN`; **the line that refuses the role** → same; a prose line saying "may declare source-carriers SUPERSEDED and NOT-INHERITABLE" → same; token on a `PROPOSED` line → `…RULING-IS-NOT-A-RULED-LINE`; token naming another package → `…DOES-NOT-NAME-THIS-PACKAGE`; token naming `cases` → `…NAMES-A-CARRIER-THAT-IS-NOT-A-BYTE-IDENTITY-GATE`; `null` → `…RULING-NOT-CITED`; unknown sha → `…NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH`. Scan of all 152 lines of the real chain: **0** carry the token, so the r10 live path (B-NTC retiring three gates on `:112`) is gone, not narrowed.
## F2 CLOSED — per carrier, and the count is now reported per carrier
`supersededGates()` asks `ruling.granted.has(carrier)` (:2189). Measured: the one-carrier token with a five-carrier spec → `GATE-SUPERSESSION-CARRIER-IS-NOT-IN-THE-RULING`; the same token with a one-carrier spec → ADMITTED, **1 gate** (`second-gate`). `supersededByCarrier()` reports `defect-witnesses 1 · inherited-carriers 3 · second-gate 1 · source-carriers 3 · writers-differential 1` = **9 of 19**, in the coverage line and in the artifact.
## F3 CLOSED, and on stronger evidence than the build report claims
The report's own proof slices the ACCEPTED block into a `new Function` and drives it three ways; that re-implements the context rather than running it. I drove the **real `envelope()`** on a sealed + `ACCEPTED` fixture artifact (`same(m, proposed())`, `sealOnTheTip`, the moves assert all passed): (a) header-only first call with the map empty → **passes the supersession block** and dies later at `THEME-AUTHORIZATION-UNAVAILABLE` — the r10 blocker is gone; (b) end-of-run call with `ran` and an empty map → `GATE-SUPERSESSION-NOT-ADMITTED-AT-SEAL` (correct); (c) end-of-run with the map `coverage()` produced → passes to `THEME`. Main sequence confirmed: `const first = envelope(s, bound);` / `const last = envelope(s, bound, ran);`, no third call site. **Stated residual, not an open finding:** no end-to-end `POSTFIX PACKAGE PASS` is constructible on this machine (private fixture + a real ACCEPTED receipt), which is r9b's standing R4 and not new to this role. The builder's deviation is honest and its conclusion holds.
## F4 CLOSED — no cache
`SUPERSESSION_TEXT`/`SUPERSESSION_KEY` are gone; the chain file is re-read per call. Measured in one process: first read ADMITTED at `DECISIONS:1` → the line deleted and the commit amended (`CHAIN STILL CONTAINS THE TOKEN LINE: false`) → the re-take refuses `…NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH`. r10's cache hit no longer reproduces.
## F5 CLOSED — per-carrier, distinct, bearing evidence, and `CHILD-REQUIRED-EXIT-ZERO` is reachable
Measured: one child in three slots → `…DIFFERENTIALS-ARE-THE-SAME-CHILD`; a differential also in `redFirst` → `…SLOTS-SHARE-A-CHILD`; **r10's exact admission** (one triple shared by all five carriers) → `…EVIDENCE-IS-NOT-THIS-CARRIER-OWN`; a child running a file outside `s.product` → `…DOES-NOT-EXECUTE-THIS-PACKAGE-PRODUCT`; red child → `…EVIDENCE-CHILD-NOT-GREEN`; declared-not-run → `…-NOT-EXECUTED`. And `children()` was driven with a real child that exits 3: `CHILD-REQUIRED-EXIT-ZERO`, now in `FAIL_CODES` — so the role's most likely real failure names itself instead of printing a bare `FAIL`. All nine new codes are in the vocabulary.
## F6 / F7 CLOSED
`DECISIONS:147 (b)` sites: runner **0**, README **0**, TOOLING-REPORT **0**, r10 report **0**; the two in `test/gate-supersession.test.cjs` are the F6 assertion's own text. `REQUESTS 08:40 (b)`: 14 in the runner. F7 measured on the **real merged B-NTC artifact** off `origin/rebuild/lane-b-h3` (`coverage` keys `covered run moves successors byChild`, no `superseded`): read as a parent it admits 9 superseded gates with a supersession and 9 covered without one — unchanged either way. Only B-NTC's own re-seal recomputes, exactly as stated.
## N1 RESIDUAL (recorded, latent, not blocking) — the token is still a substring, and negation is still unread
A `RULED` line that **quotes** the token while refusing it grants the supersession. Measured, with the runner's own regex: `…REFUSED: GATE-SUPERSESSION M2-H3-CLEAN-INIT source-carriers is NOT granted · RULED` → grant `[M2-H3-CLEAN-INIT, source-carriers]`; `…lane B asked for GATE-SUPERSESSION M2-H3-CLEAN-INIT source-carriers; the PM declines · RULED` → the same. This is far narrower than r10's F1 — it now takes a line that literally spells the token, and no chain line does — but it is the same class. Cheap structural fix, and it costs the PM nothing: require the token to **begin** its own `·`-delimited clause. Measured against that rule: the two refusals above both stop, `· GATE-SUPERSESSION M2-… · RULED` passes, and `(GATE-SUPERSESSION …)` already refuses today (the `(?:^|[\s·])` prefix). Second, smaller half of the same note: that prefix means a token wrapped in `(`, `**`, `"` or `` ` `` frees nothing, so **README §r10 should tell the PM to write the clause bare** — a line the PM believes grants the role can silently grant nothing.
## N2 — one number in the r10 build report is wrong
`TOOLING-FIX-r10-REPORT.md:48` says the H3 probe ends `CI REVIEW-PENDING: 3 open obligation(s) (theme, brief, unsealed profile)`. Measured twice: the terminal prints **2**. Three `OPEN` lines print, but the unsealed-profile line is raised by `note(…, false)` and is deliberately **not** CI-blocking, and the terminal counts `blocking.length`. Correct the number; the three `OPEN` lines are right.
## B — the H3 probe, reproduced in two passes
`origin/rebuild/lane-b-h3 @ 902fd88` cloned `--shared` to `…/rv-tool10b-scratch/fx-h3`; one plumbing commit on the real chain tip `f72eacfb` appends my own token line (`DECISIONS:152`, sha `301a13047fa8`, bare clause) and `refs/remotes/origin/rebuild/t2-client-core` points at it; r10b runner + suites overlaid, all seven specs re-pinned, H3's two `superseded-by-child` tooling posts re-stated, `coverage.inherited` emptied and the five carrier children dropped. **Pass 1** (H3's single evidence triple, as the lane ships it) → `FAIL GATE-SUPERSESSION-EVIDENCE-IS-NOT-THIS-CARRIER-OWN`, exit 1 — the build report's own finding, reproduced independently. **Pass 2** (one red-first child per carrier — `h3-cells`, `h3-cells-b/c/d`, `ntc-provider-cells`; `a0-journeys` legacy, `today-suites` writers) → all **7** children exit 0, `COVERAGE 0/19 … 9 SUPERSEDED under DECISIONS:152 … 10 re-execute under --full`, the `SUPERSESSIONS` line naming the token's sha, five `SUPERSEDED`/`SUPERSEDED EVIDENCE` pairs each with its **own** red-first cell, and `CI REVIEW-PENDING: 2 open obligation(s)` (theme, brief), exit **2**. H3 goes from **12** open obligations to **2**, both PM-side. `fake-indexeddb` is not a blocker with the root + `m3/w5` + `m3/w6` junctions.
## C — what the PM's line should say about `second-gate` (≤5 lines)
1. `second-gate` is the only one of the five with **no analogue** in the evidence set: it re-derives the nineteen gates' verdicts from a *second, independent* reconstruction, while H3's cells, census and both differentials all run against H3's single engine.
2. Omitting it from the token does not help: its carrier child then has to run, and it refuses `Unlisted parent pin drift rebuild/engine/writers.cjs` (r10, measured), so H3 could not seal at all.
3. So the line must include it **and price it in the same RULED sentence**, e.g. `… GATE-SUPERSESSION M2-H3-CLEAN-INIT …,second-gate — second-gate is superseded with NO independent second derivation in its place; the nineteenth gate is not re-derived for this package …`.
4. It should also carry the debt forward by name: B-NTC's `second-gate` stands as the last independent derivation, and the next package that re-anchors the carrier list restores it.
5. If the PM will not write that sentence, refuse `second-gate` in the token and rule the successor route for that one carrier instead — which H3-CORE measured as refused a layer deeper, so it needs a ruling of its own.
## D — regression, terminals, bites
**Suites 95/95, 0 fail** (nine suites; `gate-supersession` is now 11 cases and carries named r10 F1–F7 regressions). `--ci --package`, **exit 2, `CI REVIEW-PENDING`, no PASS word**: B1 **6** · B2 **6** · B3 **10** · B4 **10** · B-LOM **12** · B-NTC **5** · H3 **12** — every count equal to r10's, r9b's and r7's, so the fix moved no obligation. `--full --package B1` → exit **2**, `BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`.
**Bites (9, on the pass-2 `fx-h3`, each restored):** token line withdrawn from the chain → `…RULING-LINE-SHA256-NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH` · the token clause rewritten as prose → same (the sha moves) · spec takes a carrier the token omits → `…CARRIER-IS-NOT-IN-THE-RULING` · five carriers share one cell → `…EVIDENCE-IS-NOT-THIS-CARRIER-OWN` · differentials the same child → `…DIFFERENTIALS-ARE-THE-SAME-CHILD` · sixth carrier → `…CARRIER-IS-NOT-A-BYTE-IDENTITY-GATE` · superseded gate kept in `inherited` → `…GATE-IS-ALSO-INHERITED` · ruling sha `null` → `…RULING-NOT-CITED` · runner byte edited → `RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER`. All exit 1; control and FINAL both `CI REVIEW-PENDING: 2`, exit 2. Restoration after every bite: `H3.json f853ff1aa7f8`, `b-package.cjs 637d4d0f1c75`, chain `1c3f373533bd`, HEAD `2238c8c1a0f7`, `git status` empty.
## PM items
1. Write the line with the token in its **own bare `·` clause** (N1), and price `second-gate` in it (§C). 2. Amend REQUESTS 08:40 (b) to carry the token shape, as the lane asks. 3. H3 must author **one red-first cell per superseded carrier** before (b) buys a seal — pass 1 is the proof. 4. B-NTC needs a re-seal before H3 seals under this runner (F7's other half). 5. §C's stronger asks (hunk-level, parent-blob-anchored differentials) stay a further pass if ruled; nothing in r10b weakens them. 6. Fix the `3 open` number (N2).
**Provenance / boundaries.** Read-only outside this one file; no commit, no push; only `rv-tool10b` (this file) and `rv-tool10b-scratch` written, and `rv-tool10` left as it stood. `rv-tool10b-scratch` holds `probe.cjs`/`run-b.cjs`/`c1.cjs`/`c2.cjs` (fixtures over the real runner, one line re-pointed), `setup-h3b.cjs`, `bites.cjs`, `fx-h3` (scratch clone at the pass-2 commit, clean) and every captured output. No private fixture sought; the private blob was never opened. The 19-gate `--full` sweep was measured in r10 by a recorded `R.gateRun` and is unchanged here (`SUPERSEDED_RESOLVED` skip + the `GATE_IDS` closure assert are byte-identical to bd30ed9).
