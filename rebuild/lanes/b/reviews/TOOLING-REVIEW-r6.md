# TOOLING — REVIEW r6 — ACCEPT WITH CHANGES — SAFE TO SEAL B-NTC under DECISIONS:113: YES
head `8d3d362` (`rebuild/lane-b-tooling`) · base `165b0fb` · chain `refs/remotes/origin/rebuild/t2-client-core` = `84996a5f` · worktree `work/lane-b/rv-tool6` (read-only except this file) · scratch `rv-tool6-scratch` (throwaway `git clone --shared` fixture + one probe worktree; fixture ends at `8d3d362`, `git status --porcelain` 0 lines).
Reviewer: I wrote none of this; builder claims were treated as hypotheses. Every number below is from a command I ran on this machine on 2026-09-11 with `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`. No private data was sought, opened, hashed or quoted. Runner measured at the head: **114 817 B, 1431 lines, sha256 `6875576097c366921083b511b162873c2282421d793e62631b69864700fd2941`** — equal on disk and in Git at HEAD, and equal to `tooling.runnerSha256` in all six specs here and in B-NTC's own spec at `3258eb9`.
Scope A: `git diff --name-status 165b0fb 8d3d362` = 19 paths, all under `rebuild/lanes/b/tooling/**`. `git grep -e 614717800602 -e B-NTC-SUCCESSORS -- rebuild/lanes/b/tooling` exits **0**, not 1: the two needles survive only inside the two report files that quote the command (`TOOLING-FIX-r5-REPORT.md:48`, `TOOLING-REPORT.md:1836`). No digest, policy file or void theme is load-bearing anywhere; `astra` survives as prose and as one `TOOLING_FILES` entry.
## 0. Findings, ranked
F1 · MED · **Z10 NOT DONE.** `TOOLING-REPORT.md:1809-1814` publishes the runner as 1401 lines / 112 245 B / `448a3352…` and says the six specs carry `448a3352…`; the head is 1431 / 114 817 / `6875576097…`. `README.md:130-131` and `:307-309` still state that the **brief acceptance** and the **theme** are found "in Git at the parent's receipt base" — the one sentence commit `8d3d362` falsified. The self-clearing commit updated neither file.
F2 · MED · **`:113 (b)`'s "private module that never enters `require.cache`" has no assertion in the runner** (`require.cache` does not occur in `b-package.cjs`). It holds *in fact* for B-NTC — `git show 3258eb9:rebuild/m4/spec/b-ntc-successors.cjs` compiles through `new Module()` + `_compile` with an injected resolver — but the tooling does not test it, so a later successor could `require()` its original and the runner would not say so.
F3 · MED · **`:113 (c)`'s "pin re-target ONLY" is not enforced; only "enumerated verbatim" is.** `b-package.cjs:1065-1073` requires each `from` to stand exactly once in a parent-pinned original and `:1116` deep-equals the successor's own table to the spec's list — nothing asks whether the change *is* a re-target of a `superseded-by-child` path. B-NTC declares 3 substitutions at `3258eb9`, of which only #1 is a sha re-target; #2 rewrites a `deepEqual` expectation and #3 re-points a mutant-detector path. `:113 (c)` names #2 and #3 by description, so this is settled for B-NTC — but a fourth, unratified substitution would be admitted by the runner and caught only by a human reading the spec.
F4 · LOW-MED · **The product reorder makes `pre === post` a fully-satisfied product claim for roles the word does not fit.** My controls r6-P1/r6-P2 show an `edited` parent-PRODUCT pin and a `superseded-by-child` parent EXECUTION pin, each declared `pre === post` and untouched on disk, now reporting `PRODUCT IMPLEMENTED`. No spec on this branch or on `rebuild/lane-b-ntc` does it (all 20 `pre === post` files here are role `carried`, counted separately), so nothing is mis-reported today.
F5 · LOW-MED · **Z6 stops at this file's own vocabulary.** `FAIL_CODES` = 282 names, all derived from `b-package.cjs`; the refusals thrown by the required originals are not in it, so the highest-value forgery refusals print no code: `RECEIPT-EXACT-LINE-MISSING`, `RECEIPT-CONTENT`, `JSON-NONCANONICAL-BYTES`, `WORKTREE-SOURCE-PIN` are each `false` in `FAIL_CODES` (measured). My forged-theme bite printed `B PACKAGE B1 FAIL;` with no code.
F6 · LOW · **The cited ledger COORDINATE is never verified.** `claim()` (`b-package.cjs:441-446`) requires only that `ledgerLine` be a positive integer; my probe-chain control cited `998`/`999` and the runner printed "theme DECISIONS:998 … found in Git on refs/remotes/origin/rebuild/t2-client-core". The line BYTES are verified; the number beside them is the spec's word.
F7 · LOW · **The successor path is inert on this branch.** All six specs here declare `coverage.successors: null`; B-NTC's 9 inherited gates are carried by 5 parent-pinned executables. Every successor proof below is fixture-based or derivation-based, never a production run.
## 1. The self-clearing commit `8d3d362`, judged first
**(a) product() reorder — CORRECT, no refusal weakened. ACCEPT.** The post-image is asked before the pre-image at `b-package.cjs:826-829`; the two images can differ only where `pre === post`, and the `carried` branch (`:826`), the drift branch (`:829`), the parent-pin equalities (`:798-812`) and the inventory-completeness assert (`:834`) are untouched. Lane B's suite: `node --test rebuild/lanes/b/tooling/test/product-phase-and-ledger.test.cjs` → **tests 7 · pass 7 · fail 0 · exit 0**. My own controls (a scratch copy of that suite + 4 cases): **tests 11 · pass 11 · fail 0** — r6-P3 re-fires all four parent-map refusals (`UNLISTED-PRODUCT-DRIFT pre-image is not the parent pin`, `PARENT-PRODUCT-PIN-NOT-DECLARED-CARRIED-OR-EDITED`, `PARENT-EXECUTION-PIN-NOT-DECLARED-SUPERSEDED`, `… is pinned by the parent and is not in this product inventory`), r6-P4 shows both orders agree whenever `pre !== post`. What it clears is real and bounded: B-NTC's own spec at `3258eb9` has **7 of its 33 non-carried files with `pre === post`** (`w7-preview/today/test/{adapter,checkin,design,package,view}`, `w6/host/test/engine-equivalence`, `w6/test/local-today-journey`); the other 26 must still each reach a distinct post before `IMPLEMENTED`. Residual = F4.
**(b) ledger anchor — CORRECT, N4 intact. ACCEPT.** Where each of the four is resolved now: **owner** `b-package.cjs:912` and **contract** `:913` at `bound.receiptBase` (the parent's receipt commit, set at `:681`), with `:914` re-asserting `contract.lineSha256` byte-equal to the parent artifact's; **theme** `:915` and **brief acceptance** `:916` at `own = CHAIN_REF` (`:911`; the constant is `:104`). N4 holds, proved both ways in the throwaway clone: a theme line I committed into the fixture's OWN `rebuild/DECISIONS.md` at HEAD and cited exactly → **exit 1, refused** (it is not on the chain ref); the same two lines committed on a **probe chain** (chain head + one commit touching only `rebuild/DECISIONS.md`, with `refs/remotes/origin/rebuild/t2-client-core` pointed at it) → found: `AUTHORITY OBSERVED … theme DECISIONS:998 found in Git on refs/remotes/origin/rebuild/t2-client-core`, both obligations gone, OPEN **6 → 4**, exit 2. `envelope()` re-resolves **all four** at the package's own receipt base (`:1313-1316`) and asserts that base is an ancestor of `CHAIN_REF` (`:1328`), so the seal is not weakened and this path is not stronger than the seal. **No path found where the seal passes with the line absent from the real chain**, given the local object store and `refs/remotes/*` are trusted (boundary, below). The asymmetry runs the safe way: a line at the chain TIP but not at the package's own receipt base clears `--ci` and refuses the seal.
## 2. Z1–Z11 (my proofs, not the fixer's)
Z1 **DONE** — `SUCCESSOR_RULING='DECISIONS:113'`, `SUCCESSOR_PACKAGES={B-NTC}`, `MOVES_RULING=null` (`:113-152`). Bites: moves with no id / with `DECISIONS:999` / with the real `DECISIONS:113` on `conformance` (a gate whose carrier does not read `engine-runtime.cjs`) all → `COVERAGE-MOVES-REFUSED-WITHOUT-A-PM-RULING`, exit 1; `SUCCESSOR-RULING-NOT-CITED` and `SUCCESSOR-PACKAGE-NOT-RULED` both fire; a tenth gate → `INHERITED-COVERAGE-IS-NOT-THE-PARENT-COVERED-SET`, exit 1. Derivation run on the REAL parent bytes (`acceptance-native-carriers.json`, sha `e940359b…` = the artifact `:113` names and the one B-NTC's spec chooses): parent `byChild` 9 gates → `successorGates()` admits exactly those 9 over 5 carriers, each reaching `engine-runtime.cjs`; nine names are typed nowhere.
Z2 **DONE (with F2/F3)** — sha vs `executionPins` AND vs `git show b95ccca:<path>`, copy-instead-of-load, dropped/extra/undeclared substitution, a replace site outside the table: all refuse. Lane B's suite 8/8; my 5 added controls all RED (a substitution over a non-parent-pinned file; an extra table entry; successor = a parent-pinned file; successor not this package's own `new` product; a closure that leaves the package) → **tests 13 · pass 13 · fail 0**.
Z3 **DONE** — the needle is the wrapper's own `verdicts` entry in full; a prefix and `0/6` both → `SUCCESSOR-EXECUTED-VERDICT`; an edited wrapper → `SUCCESSOR-WRAPPER-BYTES`.
Z4 **DONE** — unissued authority is OPEN, not FAIL: `--ci --package B-NTC` exit **2** with `OPEN theme …` and `OPEN brief … not accepted`; a wrong or forged authority is exit **1**.
Z5 **DONE** — no `.sourceCommit` read exists in the runner (its two occurrences are comment prose); `SUCCESSOR_PARENT_COMMIT` is a runner constant asserted to be an ancestor of `CHAIN_REF` and of HEAD before any blob is read.
Z6 **DONE, with F5** — `B PACKAGE <ID> FAIL <CODE>` observed on stderr for 11 of my 13 bites; no child stdout ever reached stderr (checked on the forged-child bite); the private path printed exactly `B PACKAGE <ID> BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING` and nothing else, for B1 and for B-NTC.
Z7 **DONE** — an uncommitted `notes` append to `packages/B1.json` now refuses `SPEC-BYTES-NOT-THE-REVIEWED-SPEC-IN-GIT`, exit 1 (r5's E10 ran clean; it no longer does).
Z8 **DONE** — `execution-targets.test.cjs` **9/9, exit 0**; the case that read `packages/B-NTC.json` is gone (the only remaining mention, `:100`, is a comment), so the suite is branch-independent by construction and gives 9/9 against B-NTC's spec as well.
Z9 **DONE** — `successor-moves.test.cjs` **8/8, exit 0**; it builds its own Git repository under `os.tmpdir()`, re-points exactly two runner constants (asserted to be the only two differing lines) and deletes its tree.
Z10 **NOT DONE** — F1.
Z11 **DONE** — every UNDECIDED run says `OPEN single-parent scan (TOOLING-REVIEW-r4 Y2) did NOT run…`; observed on all six packages. All six parents are undecided here, so both halves of Y2 are unexercised on this branch — said out loud, not hidden.
## 3. Bites — 13 in a throwaway `git clone --shared` fixture: 12 RED, 1 positive control, all restored
RED (exit 1 each): moves-no-ruling `COVERAGE-MOVES-REFUSED-WITHOUT-A-PM-RULING` · moves + `DECISIONS:999` same code · moves + real `DECISIONS:113` on the wrong gate same code · uncommitted spec edit `SPEC-BYTES-NOT-THE-REVIEWED-SPEC-IN-GIT` · runner bytes edited `RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER` · `--eval=` in a child argv `CHILD-ARGV-INLINE-CODE` · successor block on B1 `SUCCESSOR-PACKAGE-NOT-RULED` · successor block not citing the id `SUCCESSOR-RULING-NOT-CITED` · forged theme committed on the local branch `FAIL` (no code, F5) · chain ref moved so the receipt base is no longer an ancestor `FAIL` (no code; the `git merge-base --is-ancestor` refusal, X2) · forged child printing the needle + 300 B `INHERITED-COVERAGE-CHILD-IS-NOT-A-PARENT-PINNED-EXECUTABLE` · a tenth inherited gate `INHERITED-COVERAGE-IS-NOT-THE-PARENT-COVERED-SET`. GREEN by design: the probe-chain positive (§1b).
Restoration: after the last bite the fixture is `HEAD 8d3d362`, `git status --porcelain` **0** lines, runner `6875576097C3`, `B1.json 8C0DFAC58740`, `B-NTC.json 13C57731C057`, `DECISIONS.md 141F270C7215` — each equal to `rv-tool6`'s. `rv-tool6` itself: `git status --porcelain` lists only this review file.
## 4. Terminals on this tree (exit codes measured, never inferred)
`--ci --package B1` → `CI REVIEW-PENDING: 6 open obligation(s); public evidence only; no PASS is claimed`, exit **2** · `B2` 6 open, exit **2** · `B3` 10 open, exit **2** · `B4` 10 open, exit **2** · `B-LOM` 12 open, exit **2** · `B-NTC` 5 open, exit **2** (`PRODUCT NOT-IMPLEMENTED; 0 post / 5 pre / 20 carried / 0 drift`, `COVERAGE 9/19`, `NO-REGISTER … 0 of 0`, `LAWS 45/45`, `16 of 18 PIN_PATHS`).
`--full --package B1` → stderr `B PACKAGE B1 BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`, exit **2**; `--full --package B-NTC` → the same line, exit **2**. No private fixture exists in this tree and none was sought.
Usage: missing `--package`, lowercase `b1`, `--ci --full` together, and unknown id `B5` → `B PACKAGE USAGE REFUSED; exactly: --ci|--full --package B-NTC|B-LOM|B1|B2|B3|B4`, exit **1** in all four.
## 5. Changes required (none of them blocks the seal; 1 and 2 should land with it)
1. `TOOLING-REPORT.md:1809-1814` — re-measure at the head (1431 lines / 114 817 B / `6875576097…`; specs `B-NTC.json` 24 020 B, `B-LOM.json` 13 757, `B1.json` 25 107, `B2.json` 26 013, `B3.json` 21 006, `B4.json` 20 473) and correct `:1836`, which claims a `git grep` exits 1 where it exits 0. Proof: the table's sha equals `(Get-FileHash rebuild/lanes/b/tooling/b-package.cjs).Hash`.
2. `README.md:130-131` and `:307-309` — say that **theme and brief acceptance resolve on `CHAIN_REF`** while owner and contract stay at the parent's receipt base, and that all four are re-resolved at the package's own receipt base at the seal. Proof: the two paragraphs match `b-package.cjs:911-916` and `:1313-1328`.
3. `b-package.cjs` `proveSuccessor()` after `:1097` — assert `:113 (b)`'s cache clause: for every file in the successor's closure, `!requiresOriginal(file, original)`, under a name such as `SUCCESSOR-REQUIRES-THE-ORIGINAL-INSTEAD-OF-COMPILING-IT`. Proof: a successor rewritten to `require('./native-carriers-<child>.cjs')` refuses with that code.
4. `b-package.cjs:580-590` — require each declared substitution to be a re-target of a path or pin this spec declares `superseded-by-child` (`from`/`to` differing only in such a path or in its `pre`/`post` sha), or else to be named in the ruling text the spec cites. Proof: a fourth substitution over an unrelated assertion refuses. Until then F3 stands and the package review is the only check.
5. `b-package.cjs:826-829` — forbid `pre === post` for roles `edited` and `superseded-by-child` (`assert(pin.pre !== pin.post || pin.role === 'carried' || !fs.existsSync(rel(file)))`), or give the "declared but unchanged and not parent-pinned" case its own role so `at.post` counts only what this package produced. Proof: my r6-P1/r6-P2 controls flip from `IMPLEMENTED` to a refusal.
6. `b-package.cjs:1421-1428` — admit the originals' own closed codes (`RECEIPT-*`, `JSON-*`, `WORKTREE-SOURCE-PIN`) into `FAIL_CODES` from their modules, the way `BLOCKED` is already imported, so a forged ledger citation prints a code. Proof: the forged-theme bite prints `FAIL RECEIPT-EXACT-LINE-MISSING`.
## 6. PM
Nothing here needs a ruling. `DECISIONS:113` settles r5's PM items 1–4: (1) `coverage.inherited`, not `coverage.moves`, with `moves` staying `{}`; (2) the lane's own carriers are the successors; (3) r2 item 7 / the terminal vocabulary is answered by `:113 (7)` and delivered as Z6; (4) Astra's `sourceCommit` went with the reverts. One reading is worth recording because the runner takes it: `:113 (1) (e)` ("refuses `coverage.inherited` in any package that does not cite it") is implemented as refusing only the SUCCESSOR-carried case — B1..B4 each declare 9 inherited gates, cite nothing and run — which is what the same ruling's "for M2-B-NTC only … X1 is not widened for any other package" requires.
Provenance: `8d3d362` = two reverts + Z1–Z11 in lane B's own name + the self-clearing commit. I reproduced the scope, the three suites (7/7, 8/8, 9/9), two suites of my own (11/11, 13/13), 13 bites and 12 terminal runs. Astra's reports and lane B's `TOOLING-FIX-r5-REPORT.md` §4b were read as hypotheses; both of §4b's claims were re-derived from the source and from execution rather than accepted.
Boundaries: `rv-tool6` was never written except this file; all mutation happened in `rv-tool6-scratch` (a `--shared` clone plus one probe worktree), and `work/lane-b/{ntc,tooling,rv-ntc3,rv-*}` were never opened. No private data was sought, read, hashed or quoted. One trust boundary worth naming once: `CHAIN_REF` is a LOCAL remote-tracking ref, so every chain claim — X2's, and now the theme/brief anchor — is only as good as the local object store and `refs/remotes/origin/*`; a hand that can rewrite that ref can forge both, exactly as it could before this delta.
