# TOOLING — REVIEW r7 (delta 7b42b4d→2f8678e) — ACCEPT WITH CHANGES — the B-NTC seal at 7d4aafa stands on these bytes: YES
head `2f8678e` (`rebuild/lane-b-tooling`) · base `7b42b4d` · chain `refs/remotes/origin/rebuild/t2-client-core` = `4ee62da2` · worktree `work/lane-b/rv-tool7` (read-only except this file) · scratch `rv-tool7-scratch` (two throwaway `git clone --shared` fixtures, `fx` on `rebuild/lane-b-ntc`, `fx2` on this branch; both restored, below).
Reviewer: I wrote none of this; lane-b-fixer6's `TOOLING-FIX-r5-REPORT.md` §"r6 applied" was read as six hypotheses and every one was re-executed here on 2026-09-12 with `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`, node v24.18.0. No private data was sought, opened, hashed or quoted; no private fixture exists in either tree.
Scope A: `git diff --name-status 7b42b4d 2f8678e` = **14 paths, all under `rebuild/lanes/b/tooling/**`** (13 M, 1 A = `packages/H3.json`). Runner at the head **123 956 B, 1540 newlines, sha256 `eedabccd5b8145bfb07fe653bfb14f9483af8ca52619e813c70bf699407dd612`**, equal on disk and in Git, and equal to `tooling.runnerSha256` in **all seven** specs — and byte-equal to `git show 3ab4f2c:rebuild/lanes/b/tooling/b-package.cjs`, i.e. the seal ran on exactly these bytes.
## 0. Findings, ranked
F1 · MED · **Change 5 stops one role short of r6's own proof line, and the sealed spec is the case it misses.** The landed assert (`b-package.cjs:910-912`) exempts role `new` outright; r6 change 5's proof line exempted only `carried` and a file absent from disk. The spec the seal ran (`9d8552d9…`) declares roles carried 20 / new 31 / superseded-by-child 2, and **7 of the 31 `new` files carry `pre === post`** — `w7-preview/today/test/{adapter,checkin,design,package,view}`, `w6/host/test/engine-equivalence`, `w6/test/local-today-journey`. I read each out of Git at the spec's own `sourceBase 7b1678a`: **all seven are PRESENT there and their bytes hash to exactly the declared pre = post**. So 7 of the "33 at the declared post-image" are files M2-B-NTC did not write a byte of. Executable: mutant B (a 32nd `new` file re-declared `pre === post === disk`) ran to `PUBLIC CI EVIDENCE PASS`, **exit 0**, printing the identical `PRODUCT IMPLEMENTED; 33 at the declared post-image / 0 at the pinned pre-image`. Bounded, and I checked both bounds: the phase word survives on the other 26 (`at.pre` is 0 either way), and **none of the 15 declared children executes any of the 7**, so `ownChildren`/Y1 and `NO-REGISTER … 15 of 15` are untouched. This is r6's F4 unclosed for one role, not a delta regression — the post-first order landed at `8d3d362`, which r6 judged and accepted.
F2 · MED · **Change 4's branch (B) is much wider than "the substitutions the ruling names".** Gate 1 is `ruling.toLowerCase().includes(<last hyphen segment of the module basename>)`: measured against the real `DECISIONS:113` line on the chain branch, that admits **8 of the 17 `native-carriers-*.cjs` parent originals** (cases, defect-witnesses, inherited-carriers, package, profile, second-gate, source-carriers, witnesses), one non-re-target each via `ruledOriginals`. Gate 2 (`echoed`) is close to free — for B-NTC's own `cases` substitution it matched `native target rebuild`, words that stand in almost any m4/spec quotation. F3 is therefore narrowed from "any number over any module" to "one each over 8 of 17", not to "the two the ruling describes". Strictly a tightening; it cannot admit anything `7b42b4d` refused.
F3 · LOW-MED · **Change 6 does not reach the runner's own unnamed asserts.** `FAIL_CODES` goes 101 → **399** names (96 admitted from the six fixed modules, `RECEIPT-EXACT-LINE-MISSING`/`RECEIPT-CONTENT`/`JSON-NONCANONICAL-BYTES`/`WORKTREE-SOURCE-PIN` all present) and the forged-ledger refusal now prints its code. But `failCode()` is presentational only (`:1536`, exit codes identical in both catch branches), and two high-value refusals I fired still print a bare `FAIL`: the X2 chain-ancestry refusal (chain ref moved to the root commit → exit 1, no code) and the theme-line SHAPE assert at `:683` (`'Theme line binds this package id'` → exit 1, no code).
F4 · LOW-MED · **`rulingText()` is a new hard runtime coupling to a ledger line number.** Every successor run now reads line 113 of `rebuild/DECISIONS.md` on `CHAIN_REF` and requires it to contain `B-NTC-INHERITED-1`. Measured RED when my fixture's chain ref resolved to a different tip: `FAIL SUCCESSOR-RULING-COORDINATE-IS-NOT-THE-RULING`, exit 1. Correct and welcome (it answers r6 F6 for this one coordinate) — but a PM renumber of `DECISIONS.md` now breaks every B-NTC run until the runner is re-reviewed, and the same treatment was not given to the four `claim()` coordinates F6 was actually about (`:470` still requires only a positive integer).
F5 · LOW · **The H3 skeleton contains a pin that change 5 will refuse.** `packages/H3.json`: `status SKELETON`, `dIds []`, `laws 0`, `children []`, `successors null`, one product file role `edited` with pre and post both `null` — i.e. `pre === post` on a role change 5 forbids. It refuses earlier today (`REGISTER-D-ID-INVENTORY-EMPTY-AND-NOT-EXEMPT`), so the second refusal is latent, but H3's author will hit it the moment a D-id is filled in.
F6 · LOW · **`IDS` order does not match the order `:124` rules.** The array is `B-NTC, B-LOM, H3, B1, B2, B3, B4`; `:124` says "ORDER B-NTC → H3 → B1 → B2 → B4 → B3". Cosmetic — `IDS` is used for membership and for the usage string, never for sequencing — but the comment at `:100-102` claims the array encodes the ruled order and it does not.
## 1. The six changes — DONE / NOT / WEAKENED, each with my own executed proof
| # | r6 asked | verdict | my proof | code | exit |
|---|---|---|---|---|---|
| 1 | re-measure `TOOLING-REPORT.md:1809-14`, correct `:1836` | **DONE, true at the head** | `:1812` = 1540 / 123 956 / `eedabccd…` and `:1818-1822` spec sizes — all seven equal my own `Get-FileHash`/byte counts (B-NTC 24 020, B-LOM 13 757, H3 11 706, B1 25 107, B2 26 013, B3 21 006, B4 20 473); README/test rows equal too; `:1843/:1847` now says the `git grep` exits **0**, 2 matches | — | — |
| 2 | `README.md` ledger paragraphs match the code | **DONE** | `README:132-149` and `:310-333` say theme + brief acceptance resolve on `CHAIN_REF`, owner + contract at the parent receipt base, all four re-resolved at the package's own receipt base; matches `b-package.cjs:976-977` (`ledger`), `:979-1015` (`authority`) and the `envelope()` re-resolution | — | — |
| 3 | refuse a successor that `require`s its original | **DONE** | fx @ `bc2233f`: `b-ntc-traces.cjs` prefixed with `require('./native-carriers-traces.cjs')`, its product post re-pinned, committed | `SUCCESSOR-REQUIRES-THE-ORIGINAL-INSTEAD-OF-COMPILING-IT` | **1** |
| 4 | refuse a 4th substitution that is neither a re-target nor named in the ruling | **DONE, with F2** | fx @ `3ab4f2c`: a 4th over `native-carriers-errors.cjs`; and a 2nd non-re-target over the ruled `witnesses` original | `SUCCESSOR-SUBSTITUTION-NOT-A-RE-TARGET-AND-NOT-RULED` (both) | **1** |
| 5 | refuse `pre === post` for `edited` / `superseded-by-child` | **DONE for those two roles; WEAKENED vs r6's proof line — F1** | fx @ `bc2233f`: `.github/workflows/rebuild.yml` post := pre (the superseded pin no substitution quotes, so change 4 cannot mask it) | `PRODUCT-CHANGE-ROLE-DECLARES-NO-CHANGE` | **1** |
| 6 | a forged ledger citation prints a code | **DONE, with F3** | fx @ `bc2233f`: `authorizations.theme.line` replaced by a well-shaped line standing on no branch, `lineSha256` recomputed | `RECEIPT-EXACT-LINE-MISSING` | **1** |
**Could any of the six ADMIT what `7b42b4d` refused?** No, and I proved it two ways. Structurally: the runner diff removes exactly one assertion — the `dIds` conjunction at old `:470` — and re-states both halves (`:516-524`), the empty-inventory half now carrying its own name; every other change is an added `assert` (3, 4, 5) or the `FAIL_CODES` set, which `:1536` uses only to choose a printed word. By control: the unmutated B-NTC spec runs **identically before and after** — fx @ `bc2233f` control → `PRODUCT IMPLEMENTED; 33 / 0 / 20`, `PUBLIC CI EVIDENCE PASS`, **exit 0**, reproducing the sealer's run 1 line for line; and change 4's positive control (a 4th substitution that IS a genuine re-target of the declared `engine-runtime` superseded pin) was **admitted** by change 4 and stopped 28 lines later by the older `successorProof`, so the new gate does not over-refuse. Change 5's control on the real spec passes because **B-NTC's two `superseded-by-child` pins genuinely differ** — `engine-runtime.cjs` `9be21897…`→`c03732e8…`, `rebuild.yml` `f57ced40…`→`839a79ab…`.
## 2. The H3 skeleton
`--ci --package H3` → `B PACKAGE H3 FAIL REGISTER-D-ID-INVENTORY-EMPTY-AND-NOT-EXEMPT`, **exit 1**, a named refusal and not a crash; `--full --package H3` gives the same code and exit. `IDS` = `B-NTC B-LOM H3 B1 B2 B3 B4` (7), and the usage line prints exactly those seven; `--ci --package h3` is refused case-exactly, exit 1. No other behaviour change: every other package's terminal and open count is unchanged from r6 (§3), and `NO_REGISTER_IDS` still = `{B-NTC, B-LOM}` — H3 is not in it, which is what makes its refusal the right one. See F5 and F6.
## 3. Regression on this tree (exit codes measured, never inferred)
Suites: `product-phase-and-ledger` **7/7**, `execution-targets` **9/9**, `successor-moves` **9/9** — **25 cases, 0 fail, exit 0** each (r6 measured 7/9/8; the 9th successor case is change 3's own control, and it restores the module and re-admits it). The three new fixture copies of the original modules are real `readFileSync` copies of this tree's bytes, not re-typed constants.
Terminals, `--ci --package`: B-NTC `5 open` · B-LOM `12` · B1 `6` · B2 `6` · B3 `10` · B4 `10` — `CI REVIEW-PENDING … no PASS is claimed`, **exit 2** each; H3 **exit 1** (above). Every count equals r6 §4, so no change in this pass moved an obligation. `--full --package B1` → `BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`, **exit 2**. Usage: missing `--package`, `b1`, `--ci --full` together, `B5`, `h3` → `B PACKAGE USAGE REFUSED; exactly: --ci|--full --package B-NTC|B-LOM|H3|B1|B2|B3|B4`, **exit 1** in all five.
## 4. Bites — 9 RED, 2 GREEN controls, all restored
RED, exit 1 each (fx2 @ `2f8678e`, package B1 unless noted): moves non-empty with no ruling `COVERAGE-MOVES-REFUSED-WITHOUT-A-PM-RULING` · uncommitted spec edit `SPEC-BYTES-NOT-THE-REVIEWED-SPEC-IN-GIT` · runner bytes edited `RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER` · `--eval=` in a child argv `CHILD-ARGV-INLINE-CODE` · a successor block on B1 `SUCCESSOR-PACKAGE-NOT-RULED` · the same not citing the id, same code · a tenth inherited gate `FAIL` (no code, F3) · an inherited pair re-keyed to an unlisted gate `FAIL` (no code, F3) · X2, chain ref moved to the root commit so the receipt base is no longer an ancestor, `FAIL` (no code, F3). GREEN by design: the fx @ `bc2233f` control (exit 0) and change 4's re-target control (§1).
Restoration: fx2 `HEAD 2f8678e`, `git status --porcelain` **0** lines, chain ref back at `4ee62da2`, runner `EEDABCCD…`, `B1.json 32657F41B51B6D25CFA9436EA846DCBAECC826E47ECB821BAE2B9D306744342C` — equal to `rv-tool7`'s. fx `HEAD bc2233f`, porcelain **0**, `B-NTC.json 9D8552D9…` and runner `EEDABCCD…` — equal to the sealed bytes. `rv-tool7` itself: `git status --porcelain` lists only this review file.
## 5. The seal at `7d4aafa` — could the six changes have let anything false through?
**No.** Changes 3, 4 and 5 are added assertions; change 6 changes only a printed word; changes 1 and 2 are prose. Nothing was removed or relaxed, and I reproduced the sealer's own run 1 on the sealed spec and runner: `PUBLIC CI EVIDENCE PASS`, **exit 0**, with `PRODUCT IMPLEMENTED; 33 / 0 / 20`, `AUTHORITY OBSERVED` (theme `DECISIONS:122`, brief `DECISIONS:126`, both on the chain branch), `LAWS 45/45`, `16 of 18 PIN_PATHS`, and the three enumerated substitutions each cleared — #1 by change 4's re-target branch, #2 and #3 by the ruling's own bytes. The sealed artifact `1588567a…` and spec `9d8552d9…` I re-hashed out of Git at `3ab4f2c` both equal the verdict's figures.
**One verdict line needs a correction, and it is not the delta's doing.** In `## What run 3 observed`, "`PRODUCT IMPLEMENTED` — 33 at the post-image / 0 at the pre-image / 20 carried / 0 unlisted drift" is true as measured but over-reads as production: by F1, **26** of the 33 are files this package moved, and 7 stand at `sourceBase` bytes the spec itself declares as both pre and post. The phase word `IMPLEMENTED`, the `COVERAGE 9/19`, the `NO-REGISTER … 15 of 15` and every gate verdict are unaffected. The defect is inherited from `8d3d362` (r6 §1a, accepted) and change 5 narrowed it rather than closing it; no PASS or OBSERVED in the artifact or the verdict rests on the seven.
## 6. Changes required (none blocks the seal; 1 should land with the verdict)
1. `rebuild/lanes/b/VERDICT-B-NTC.md`, the `PRODUCT IMPLEMENTED` clause — say **26 produced + 7 declared `new` and unchanged since `sourceBase`**, or re-role those seven. Proof: `git cat-file blob 7b1678a:<file>` hashes to the declared pre = post for each of the seven.
2. `b-package.cjs:910-912` — drop the `|| pin.role === 'new'` exemption in favour of r6's own line, `|| !fs.existsSync(rel(file))`, so a `new` file that exists and declares no change refuses instead of counting. Proof: my mutant B flips from `PUBLIC CI EVIDENCE PASS` exit 0 to `PRODUCT-CHANGE-ROLE-DECLARES-NO-CHANGE` exit 1, and B-NTC's seven must then be re-declared before the next seal.
3. `b-package.cjs:670-675` (change 4 branch B) — replace the basename-token test with the substituted module's **full basename** and require the ruling to quote something from `sub.from`/`sub.to` that is not a word of any repository path. Proof: the 8-of-17 admission count in F2 falls to the 2 the ruling describes.
4. `b-package.cjs:273-287` and the catch at `:1530-1539 — give the X2 ancestry refusal, the `:683` theme-shape assert and the two inherited-coverage refusals code names, so the three bites in §4 that print a bare `FAIL` print one. Proof: those three bites print a code.
5. `b-package.cjs:100-102` and `IDS` — either reorder to `B-NTC, H3, B1, B2, B4, B3, B-LOM` or delete the claim that the array carries `:124`'s order. Proof: the comment and the array agree.
6. `packages/H3.json` — set the product post, or drop the role to `new`, before a D-id is added. Proof: `--ci --package H3` after the inventory is filled refuses at `REGISTER-…` only, never at `PRODUCT-CHANGE-ROLE-DECLARES-NO-CHANGE`.
## 7. PM
Two items. (a) **Is H3 a `NO_REGISTER_IDS` package?** `:124` scopes it to `rebuild/m4/workout/athlete-state.cjs` and calls it small; today the runner refuses it for registering no D-id, which is the safe default but leaves the id unrunnable until someone decides. The fixer explicitly left this to you. (b) **`:113 (c)`'s two ratified substitutions are described, not named.** F2 shows why that matters mechanically: the runner can only match the ruling's prose by token, and the prose happens to contain the distinguishing token of 8 of the 17 parent originals. A future ruling that lists substitutions by `original` + a quoted fragment would let change 4 be exact.
Provenance: `2f8678e` = `1e80e8f` (the six changes + `DECISIONS:124`'s H3 id and skeleton) + `2f8678e` (change 3's suite control + the r6-applied record). I reproduced the scope, the runner and all seven spec hashes, three suites (7/7, 9/9, 9/9), 6 mutants and 9 bites in two throwaway clones, 13 terminal runs and both documentation changes. Lane B's §"r6 applied" table was re-derived line by line rather than accepted; all six of its claimed codes reproduced, and its claim that "every count equals r6 §4" is true.
Boundaries: `rv-tool7` was never written except this file; all mutation happened in `rv-tool7-scratch` (`fx`, a `--shared` clone on `rebuild/lane-b-ntc`, and `fx2`, one on this branch), and `work/lane-b/{ntc,tooling,docs,rv-*}` were never opened or written — the two junctioned `node_modules` and the two copied engine files were read only. No private data was sought, read, hashed or quoted; `--full` was run only where it refuses for want of the private oracle. The trust boundary r6 named is unchanged and is now wider by one: `CHAIN_REF` is a LOCAL remote-tracking ref, and `rulingText()` adds the **text** of `DECISIONS:113` on that ref to what a hand rewriting it could forge.

