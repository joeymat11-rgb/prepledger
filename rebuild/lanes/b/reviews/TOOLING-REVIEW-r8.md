# TOOLING — REVIEW r8 (daa87c8→cbcdf29) — ACCEPT WITH CHANGES — SAFE TO SEAL H3 and the B1+B2 bundle under DECISIONS:136(3): YES

head cbcdf29 · base daa87c8 · 11 commits · runner 56aba344…, 2114 lines, 169 960 B · reviewer: independent, blind, wrote none of it · node v24.18.0, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York.
YES is conditional on change 1: the FULL seal path is sound and unchanged, so H3 and B1+B2 may seal now; change 1 is required before any run prints POSTFIX PACKAGE PASS on the byte-identity step instead of a FULL run.

## A. Scope, executed
Diff touches only `rebuild/lanes/b/tooling/**` (16 paths) and `rebuild/lanes/tooling/{preflight.cjs,test/preflight.test.cjs}` — 18 files, +2157/-147. Runner disk sha256 = `56aba344b4…09d3f` and all seven specs (B1 B2 B3 B4 B-LOM B-NTC H3) pin exactly that. `git log daa87c8..cbcdf29 -- rebuild/m4/spec/acceptance-b-ntc-native-trend-context.json` is empty; the merged artifact is `87f4848c8c…49428` on origin/rebuild/t2-client-core and untouched here. The runner's only write sites are `.tmp/b-package/<ID>` and `receipts/<ID>.json` (b-package.cjs:1435,1502,1905-1906,2003,2024) — no artifact is ever rewritten.

## B. Item table (my own executed controls, not the lane's suite)
| item | verdict | control |
|---|---|---|
| r7 F1 pinned-unchanged / F2 ruled substitutions | DONE | real DECISIONS:113 yields exactly 2 descriptions; B-NTC's own two match one each; 3 fabricated thirds match none |
| r7 F3 named refusals / F4 ruling by sha / F5 F6 | DONE | 10/10 bites print a code, not a bare FAIL; fabricated ruling id → SUCCESSOR-RULING-NOT-CITED |
| H3 in NO_REGISTER_IDS under a written rule | DONE | b-package.cjs:202-210; H3 --ci is 12 open exit 2, no REGISTER-D-ID refusal |
| :135(4) seal on the tip, both directions | DONE | on-tip PASS; tip moves → SEAL-BASE-IS-NOT-THE-CHAIN-TIP; `merge --no-ff` still refuses though `merge-base --is-ancestor` succeeds; rebase restores; freeze by sha on CHAIN_REF frees, wrong package/base refuse |
| :136(3) identical → PASS, product byte / runner byte / absent / mis-shaped | DONE | ok=true; VOID; VOID; ABSENT (old behaviour); SHAPE for extra top-level key, extra sealedRun key, version 2, wrong lanePackage, product-as-array; duplicate JSON key → UNREADABLE |
| :136(3) a forged receipt | **NOT DONE** | see C / finding 1 — a hand-written receipt no seal step ever produced returns ok=true |
| preflight, each check fires | DONE | CI-UNVERIFIED · DIFF-OUTSIDE-CUSTODY (untracked stray) · REPORT-TOO-LONG (61) · REPORT-COUNTS-MISSING · STATUS-LINE-TOO-LONG (409) · UI-CUSTODY-EN-OR-EM-DASH (em and en) · CI-RUN-ID-SHAPE · USAGE (repeat) ; PASS line exact `PREFLIGHT PASS <40-hex head>`, one stdout line, exit 0 |
| F-E parent-pin normaliser | DONE (one WEAKENED edge) | flat sha, {pre,post,role}, post-image of an edited pin, post:null→pre all correct; 9 garbage shapes → PARENT-PIN-SHAPE; see finding 3 |
| F-C spec-driven successors | DONE | wrong sha → …SHA256-NOT-A-UNIQUE-LINE; foreign id → …DOES-NOT-NAME-THIS-PACKAGE; support not in the line → …DOES-NOT-NAME-THE-SUPPORT-FILE; support not a declared change → SUCCESSOR-SUPPORT-IS-NOT-A-DECLARED-CHANGE; unpinned/non-reaching carrier → gate absent |
| B-NTC's merged artifact readable as a parent | DONE | object-form product normalises; H3's spec binds it at 87f4848c… |

## C. Admission surface — could anything PASS that r6/r7 refused?
Nine new admission points. Seven are gated by a PM ruling line or by byte identity: sealOnTheTip only *narrows* (its escape is a FREEZE line matched by its own sha on CHAIN_REF); parentPin's value is the sealed parent's own hash-verified byte; successorRuling/successorGates replace `SUCCESSOR_PACKAGES=['B-NTC']` but require a PM line found by sha on CHAIN_REF naming `M2-<ID>` (from argv, never the spec), the word SUCCESSOR, the base conditions and the support file, and the gate set is the parent's byChild ∩ declared carriers ∩ parent executionPins ∩ closure-reaches-support; the FAIL_CODES widening changes only what a refusal prints. **One is not gated**: the :136(3) step. `main` skips `privateOracle()`, `historical()` and `gates()` whenever `reverify.ok` (b-package.cjs:2059-2074), and the only thing standing behind that is `receipts/<ID>.json`, which is (a) plain disk-read, never required in Git, (b) exempt from `fidelity()`'s UNLISTED-SOURCE-CHANGE via TOOLING_FILES:246, (c) not pinned by the sealed artifact, and (d) computable entirely from public data. Executed: `--full --package B1` here is `BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING` exit 2 — with an ACCEPTED envelope and a hand-forged receipt that branch is never reached, so the owner's "the first FULL run with the private census is unchanged" is not enforced by anything. Residual second surface: a change outside `s.product` ∪ PIN_PATHS ∪ the four trees `fidelity()` diffs (e.g. `rebuild/m3/**`, `rebuild/coach/**`) leaves the receipt matching, so a gate regression there is caught by r7's rerun and not by r8's. Two rulings widened beyond their literal words and should be confirmed, not assumed: branch (A) now re-targets over EVERY `edited` pin of the package, not only `superseded-by-child` (:113(c)'s words), and :136(3)'s anchor "the sealed run the verdict file reports" became a runner-written JSON with the verdict file demoted to a three-hash `includes()` test.

## D. Regression
Suites **66/66, 0 fail** (7 files, 6.62 s), exit 0. `--ci --package`, all exit **2**: B1 6 open · B2 6 · B3 10 · B4 10 · B-LOM 12 · B-NTC 5 · H3 12 — every count equal to the lane's claim. `--full --package B1` → `B PACKAGE B1 BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`, exit 2.
Ten standing bites, all exit 1, each by its own name: runner edited → RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER · spec re-pins a foreign runner → same · forged needle child → CHILD-NEEDLE-NOT-A-TERMINAL-LINE · `--eval` as argv and `--eval=` prefix → CHILD-ARGV-INLINE-CODE · moves non-empty → COVERAGE-MOVES-REFUSED-WITHOUT-A-PM-RULING · inherited pair for an unlisted gate → COVERAGE-GATE-IS-NOT-AN-ORIGINAL-GATE · sourceBase non-ancestor (X2) → SOURCEBASE-NOT-BEHIND-HEAD · fabricated ruling id → SUCCESSOR-RULING-NOT-CITED · parent-pinned product dropped → UNLISTED-PRODUCT-DRIFT · B-NTC with zero children (Y1) → COVERAGE-CHILD-NOT-DECLARED. An uncommitted spec edit refuses earlier at SPEC-BYTES-NOT-THE-REVIEWED-SPEC-IN-GIT or JSON-NONCANONICAL-BYTES, so every bite above was committed in a throwaway scratch worktree. Restoration: HEAD `cbcdf29b210a250324c11412f607bffb16244194`, `git status --porcelain` 0 lines, runner `56aba344b40d6acc3e28f271502c6df709fa130f51b62f72ecf28e9364009d3f`, B1.json `1ec3cc3db7e20fdcd20e25bf48a9c3e812732a9f79d299a15c92303f67c5735a`, B-NTC.json `537119fdf801631507590c620713dbe3c4b9b91c0e82dca630b3e4acc27c6fa4`.

## E. Changes (numbered, ranked)
1. **b-package.cjs:1862-1896 — the sealed-run receipt carries no authenticity, only consistency.** Executed: a receipt written by hand, by a process that never ran a gate, returns `ok:true`; the verdict cross-check passes with the three hashes pasted into arbitrary prose. Bind it to evidence the lane cannot write: require the receipt's own sha256 to be NAMED in `VERDICT_FILE`, AND require `gitSha(<a commit that is an ancestor of HEAD>, RECEIPT_DIR+'/'+ID+'.json')` to equal the disk bytes, so an untracked or freshly-written receipt refuses. Until then the byte-identity step must not print POSTFIX PACKAGE PASS in place of a FULL run.
2. **TOOLING_FILES:246 — `receipts/*.json` is exempt from UNLISTED-SOURCE-CHANGE.** That exemption is what makes 1 invisible to `fidelity()`. Keep the exemption only for the packages' own test files; receipts should be listed, not exempt.
3. **TOOLING-REPORT.md:1922 and :1932 — head figures are one revision stale.** The table still says `b-package.cjs | 1970 | 158 238 | 769fd09e…` and "the seven package specs carry 769fd09e…"; the shipped runner is 2114 lines, 169 960 B, `56aba344…`. Neither TOOLING-REPORT.md nor README.md contains the string "r7b" or "56aba344"; r7b is documented only in TOOLING-FIX-r7-REPORT.md. Restate both figures (:119(6) counts).
4. **b-package.cjs:1183 — `entry.post || entry.pre` treats a falsy non-null `post` as absent.** Measured: `{pre:<sha>, post:0}`, `post:""` and `post:false` all return `pre` instead of PARENT-PIN-SHAPE. Unreachable through a runner-sealed parent, but the normaliser is meant to be total: test `post === null` explicitly.
5. **preflight.cjs:133 — the UI dash check reads `git ls-files` only.** Measured: an em dash in an UNTRACKED file under the declared UI globs → `PREFLIGHT PASS`, while check (1) does count untracked files. Scan the same set both checks use.
6. **Factual correction to the brief and the lane's note: H3 is not yet the first consumer.** `origin/rebuild/lane-b-h3:…/packages/H3.json` still pins runner `769fd09e…` and declares `coverage.successors: null`; so do all seven specs on this tip. The F-C successor path has no shipped consumer anywhere and is exercised only by the suite and by my controls — its first real exercise will be H3's re-pinned spec, and that spec should be re-reviewed when it lands.

## F. For the PM
1. **:135(4) first-parent vs ancestry — recommend RELAXING to ancestry of the CURRENT tip.** Measured: a lane that runs `git merge --no-ff <tip>` gets `SEAL-BASE-IS-NOT-THE-CHAIN-TIP` exit 1, even though `git merge-base --is-ancestor <current tip> HEAD` succeeds; only a rebase or a FREEZE line clears it, and B-NTC's own head (:144, merge ce38aa3) is exactly such a merge while :137(1) makes `--no-ff` merging the house move. The failure :135 diagnoses is "a seal on a stale base", and `ancestor(tip, HEAD)` already excludes every stale base — the *current* tip being an ancestor means no chain commit is missing. First-parent additionally forbids the merge workflow, which buys nothing against that failure. One line of `sealOnTheTip()` (b-package.cjs:1819-1824) changes and the suite's rebase case becomes the merge case; keep the FREEZE escape unchanged.
2. Confirm or narrow the two widenings in C: branch (A) over every `edited` pin (:113(c) says `superseded-by-child`), and :136(3)'s anchor moving from the verdict file to a runner-written receipt.

## G. Provenance and boundaries
Worktree `…/work/lane-b/rv-tool8` @ cbcdf29 (detached), junctions for the three node_modules, engines copied from `…/lane-b/ntc/rebuild/conform/engines/`; scratch `…/work/lane-b/rv-tool8-scratch` holds a second throwaway worktree (`…/rv-tool8-scratch/mut`) where every mutant was committed and then `git reset --hard cbcdf29` + `git clean -fd`, and three reviewer-written controls (`rv8-control.cjs`, `rv8-control2.cjs`, `rv8-control3.cjs`) that compile the REAL runner with only `CHAIN_REF` re-pointed at a fixture branch. This file is the only byte I wrote inside rv-tool8; nothing was committed or pushed on rebuild/lane-b-tooling; `…/work/lane-b/{ntc,tooling,h3,docs,integrate,rv-*}` were never opened for writing; no private fixture exists in any tree I touched and none was requested.
