# TOOLING — REVIEW r9 (978d821→ea6956d) — REJECT — SAFE TO SEAL H3 (and B1+B2, B4+B3, F1) under DECISIONS:147: NO

Blind, MAX, executed at **ea6956d** in `rv-tool9` (detached; read-only but for this file), node v24.18, `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`. **A.** Scope is `rebuild/lanes/b/tooling/**` only — 13 files, +702/−21; runner `44649169a6e6…` re-pinned in all seven specs (verified per file). I wrote none of this; every claim below was re-measured.

## F1 — BLOCKING. The floor and the copy test are NOT measured on the body the wrapper loads
`:147` fixes the floor "against the body of the **ORIGINAL the wrapper loads**". `proveSuccessor()` (`b-package.cjs:1777-1786`) instead walks `parentClosure(PARENT_COMMIT,[original])` and takes **the fattest body anywhere in that closure**. Measured against the real parent `9ad2ecab…` for **all five** B-NTC gate carriers (`b-ntc-{source,inherited}-carriers`, `-defect-witnesses`, `-writers-differential`, `-second-gate`): the file chosen is `rebuild/conform/v4/postfix/acceptance-step-efficacy.json` — **7 791** qualifying lines — and **never** `b-ntc-successors.cjs` (174), the module those wrappers actually load. So, on the real H3 case:
* the floor is satisfied by a JSON data fixture and proves nothing about a load; and
* `SUCCESSOR-COPIES-THE-ORIGINAL-INSTEAD-OF-LOADING-IT` is inert. My own fixture (`h10-floor.cjs`: 9-line wrapper → 5-line loaded body → fatter unloaded sibling in closure) pastes **the entire loaded body verbatim** into the successor and the runner returns **ADMITTED**. Before `:147` the same shape refused `SUCCESSOR-ORIGINAL-TOO-SHORT-TO-PROVE-A-LOAD`.

The copy test is the only mechanical proof that `:113 (b)`'s body is *loaded* rather than *pasted*; `:147` moved it, it did not license switching it off. **Fix (small, one expression):** pick `floorFile` only among files reachable from `original` by **relative require/import** edges (never path literals, never `.json`), and name it in both refusals. Then re-state README §r9 bullet 4 and TOOLING-REPORT r9 item 3, which both assert the module is what gets measured.
## F2 — the report's closure figure does not reproduce
TOOLING-REPORT.md:2121-2123 / README.md:972: "the closure of B-NTC's **six** source carriers is **171 files**". Measured: 171 is the closure of **one** carrier (`b-ntc-source-carriers.cjs`). The five gate carriers a child must declare give **175**; ten spec carriers 180; all fifteen children 219; the 18 `executionPins` 222. Correct the sentence (the mechanism is unaffected).
## F3 — the review clause is additive but self-satisfiable
`reviewFile` is read off the **worktree** (`rel()`), unpinned, with no custody or authorship test. Executed: a review the package itself writes in the same commit → ADMITTED; a review that only mentions both strings in passing prose ("we discussed …") → ADMITTED; a path outside `rebuild/lanes/b/**` is caught by `spec()` (`SUCCESSOR-REVIEW-FILE-SHAPE`) but **not** re-checked in `successorProof()`. Not a bypass of anything else, but it is not evidence a reviewer saw the substitution. Pin it (sha256 beside the path) before it is relied on.
## F4 — two refusals still fire BARE (pre-existing; untouched by this delta)
`sourceBase` naming a commit that does not exist → `B PACKAGE B1 FAIL;` with no code; parent artifact `sha256` tampered by one nibble → same. Z6 requires every refusal to carry a name.
## F5 — the new `spec()` assertions have no suite
No suite calls `spec()`; `SUCCESSOR-SUBSTITUTION-TARGET-SHAPE`, `-IS-A-PROTECTED-SURFACE` (spec half), `SUCCESSOR-REVIEW-FILE-SHAPE` and `SUCCESSOR-BLOCK-KEYS-NOT-CLOSED` are measured only by this review (harness 4, a scratch clone). Add them.

## B — the closure, reproduced
`parentClosure()` reads **Git blobs only**: a file present at HEAD and absent at `9ad2ecab…` yields an empty closure from itself; a file written after the commit is invisible. Bounded at 512 (limit not reached). It carries all five `BRIEF-H3 v1.6 §9` names — `native-carriers-source.cjs`, `-reference.cjs`, `-changes.json`, `native-next-target-candidate/source-delta.cjs`, `b-ntc-successors.cjs`. Of 175 files only **36** pass the `rebuild/m4/spec/**` root, and 4 protected files (`rebuild/conform/oracle/**`) are in it. **Builder OPEN 1 answered by execution:** a target loaded through a computed path is **refused** `…-TARGET-NOT-IN-THE-PARENT-GATE-CLOSURE`, never silently admitted — narrower than `:147`, acceptable.

## C — admission surface (every control executed; harness 2 = own fixture, harness 4 = scratch clone of the tip)
| # | control | `spec()` | `successorProof()` |
|---|---|---|---|
| (i) | target committed but reached by nobody | — | `SUCCESSOR-SUBSTITUTION-TARGET-NOT-IN-THE-PARENT-GATE-CLOSURE` |
| (i-b) | target reached only by a computed path | — | same code — refuses |
| (ii) | bytes moved since the reviewed commit | — | `SUCCESSOR-ORIGINAL-NOT-THE-PARENT-ACCEPTANCE-BLOB` |
| (iii) | successor replaces a pair the spec omits | — | `SUCCESSOR-SUBSTITUTION-TABLE-DISAGREES-WITH-THE-SPEC` |
| (iv) | review quotes 1 of 2 / review absent | — | `…-NOT-ENUMERATED-IN-THE-REVIEW` / `SUCCESSOR-REVIEW-FILE-ABSENT` |
| (v) | `conform/oracle/**`, `conform/private/**` | `SUCCESSOR-SUBSTITUTION-TARGET-SHAPE` | `…-TARGET-IS-A-PROTECTED-SURFACE` |
| (vi) | **whole-file replacement** (`from` = the file) | `SUCCESSOR-SUBSTITUTION-NOT-A-RE-TARGET-AND-NOT-RULED` | **ADMITTED** — the run-phase gate does not ask |
| (vii) | another gate's original | — | `…-TARGET-NOT-IN-THE-PARENT-GATE-CLOSURE` |

(vi) is covered only because `spec()` always precedes `successorProof()`; say so, or re-assert it. Note also that the closure is the **union over all declared carriers**, so declaring a second carrier widens the target set for every substitution.

## D — floor / copy, measured
Wrapper 6 qualifying lines + fat loaded module → ADMITTED. Wrapper whose loaded module is 2 lines → `SUCCESSOR-ORIGINAL-TOO-SHORT-TO-PROVE-A-LOAD`. Successor that `require()`s the original → `SUCCESSOR-REQUIRES-THE-ORIGINAL-INSTEAD-OF-COMPILING-IT`. Successor that pastes the loaded module → `SUCCESSOR-COPIES-THE-ORIGINAL-INSTEAD-OF-LOADING-IT` **in the builder's shape only** (see F1). `require.cache` carried **0** entries for any fixture original or successor.

## F — regression
Suites **79/79, 0 fail**: seven lane-B suites 70 (`product-phase-and-ledger` 7, `execution-targets` 9, `successor-moves` 9, `pinned-unchanged-and-ruled-substitutions` 12, `seal-tip-and-byte-identity` 16, `parent-pin-shapes-and-spec-successors` 8, `parent-gate-closure-and-load-floor` 9) + shared `rebuild/lanes/tooling/test/preflight.test.cjs` 9. `--ci --package`: **exit 2, `CI REVIEW-PENDING`, no PASS word** for B1 **6** · B2 **6** · B3 **10** · B4 **10** · B-LOM **12** · B-NTC **5** · H3 **12** — equal to the claim. `--full --package B1` → exit 2, `BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`.
## Bites (scratch clone of the tip; every file restored, `git status` empty)
forged needle child → `CHILD-DID-NOT-REALLY-EXECUTE` · `--eval` in child argv → `CHILD-ARGV-INLINE-CODE` · runner edited → `RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER` · spec edited after its hash → `SPEC-BYTES-NOT-THE-REVIEWED-SPEC-IN-GIT` · zero own children (Y1) → `COVERAGE-CHILD-NOT-DECLARED` · `moves` non-empty without an id → `COVERAGE-MOVES-REFUSED-WITHOUT-A-PM-RULING` · fabricated ruling id/sha → `SUCCESSOR-RULING-LINE-SHA256-NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH` · successor extra unratified substitution → `SUCCESSOR-SUBSTITUTION-NOT-A-RE-TARGET-AND-NOT-RULED` · X2 `parentAcceptanceCommit` off the chain → `SUCCESSOR-PARENT-ACCEPTANCE-COMMIT-IS-NOT-THE-PARENT-REVIEWED-COMMIT` · `reviewFile` key removed → `SUCCESSOR-BLOCK-KEYS-NOT-CLOSED` · non-existent `sourceBase` → **bare FAIL** (F4) · parent artifact sha tampered → **bare FAIL** (F4). **Seal off the tip is not reachable on a REVIEW-PENDING package** (the run ends `CI REVIEW-PENDING`); it stands only on `test/seal-tip-and-byte-identity.test.cjs` 16/16. Restoration: `B1.json 633c4833…`, `B-NTC.json f85454b8…`, `b-package.cjs 44649169…` all byte-identical after every bite.
## Exact changes required before H3 seals
1. `b-package.cjs:1777-1786` — restrict the floor/copy fallback to bodies reached from `original` by relative require/import edges, exclude `.json`, and refuse when none qualifies (proof: F1's paste control must return `SUCCESSOR-COPIES-THE-ORIGINAL-INSTEAD-OF-LOADING-IT`, and the five real carriers must name `b-ntc-successors.cjs`). 2. `README.md:980-984` + `TOOLING-REPORT.md:2131-2139` — the "measured against the module" sentences. 3. `TOOLING-REPORT.md:2121-2123`, `README.md:972` — 171 → 175 (five carriers), and drop "six". 4. Add `reviewFile`'s sha256 to the closed block (F3). 5. A suite over the four `spec()`-level successor refusals (F5). **PM items:** (a) `:147`'s "private fixture" is named nowhere in the runner — `rebuild/conform/private/` does not exist in the tree, so only `golden/` and `oracle/` are live roots; PM should name the fixture path or record that the shape root covers it. (b) The shipped root `rebuild/m4/spec/**` is narrower than `:147`'s words — correctly declared by the builder; PM to confirm the narrowing is intended.

**Provenance / boundaries.** Read-only outside this file; no commit, no push; nothing under `…/work/lane-b/{ntc,tooling,h3,docs,integrate,rv-*}` other than `rv-tool9` (this file) and `rv-tool9-scratch` was touched. No private fixture sought, no `--full` private path run beyond its own refusal. Harnesses 1-11 and all captured output are in `…/work/lane-b/rv-tool9-scratch/`; each compiles the **real** runner with at most `CHAIN_REF` re-pointed (asserted: exactly one line differs).
