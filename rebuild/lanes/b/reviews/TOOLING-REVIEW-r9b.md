# TOOLING — REVIEW r9b (ea6956d→e4c4e8a) — ACCEPT — SAFE TO SEAL H3 (and B1+B2, B4+B3, F1) under DECISIONS:147: YES
Blind, MAX, executed at **e4c4e8a** in a fresh worktree `rv-tool9b` (runner `c609cf71…`, re-pinned in all seven specs), node v24.18, `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`. Every r9 control was re-run unchanged in shape, plus the new ones. My r9 file stands in Git at `e4c4e8a` byte-for-byte (`9a65fa03…`, 9 890 B, 48 lines) — unaltered.
## F1 CLOSED — the floor and the copy test now follow the compile edge
`parentClosure(…, 'require')` walks relative `require`/`import` only: no path literals, no `.json`. Measured on the real parent `9ad2ecab…`, each of B-NTC's five gate carriers now compiles a **7-file** set — `b-ntc-<gate>-carriers.cjs`, **`b-ntc-successors.cjs`**, `native-carriers-reference.cjs`, `native-next-target-candidate/source-delta.cjs` and three `conform/v4/postfix` modules — and `acceptance-step-efficacy.json` (7 791 lines) is **gone** from it. The copy test runs over **every** one of those bodies (`b-ntc-successors.cjs` included) and is asked **before** the floor. My r9 defeat control, re-run verbatim (9-line wrapper → 5-line loaded body → fatter *unloaded* sibling):
* pasting the whole loaded body: r9 **ADMITTED** → r9b **`SUCCESSOR-COPIES-THE-ORIGINAL-INSTEAD-OF-LOADING-IT`**;
* no paste, thin loaded body: r9 ADMITTED → r9b **`SUCCESSOR-ORIGINAL-TOO-SHORT-TO-PROVE-A-LOAD`** (the fat sibling no longer counts);
* pasting the wrapper's own lines now also refuses `…COPIES…` (r9 reached a different code).
**One doc correction (non-blocking, N1):** `README.md:695-696` still says the wrapper "is measured against that module's 174 qualifying lines". Measured: the **floor** file for all five carriers is `rebuild/m4/spec/native-next-target-candidate/source-delta.cjs` (**256**), since the floor is the largest compiled body — as `README.md:998-999` correctly says. `b-ntc-successors.cjs` (174) is covered by the **copy** test, which is the point. Fix the one sentence at :696.
## F2–F5 CLOSED
**F2** `171` (one source carrier) / `175` (five gate carriers) reproduce exactly and both now stand in `README.md:975` and `TOOLING-REPORT.md:2124`, with the "six carriers" attribution withdrawn. **F3** review custody is real: path root `rebuild/lanes/b/reviews/`, bytes pinned by `reviewFileSha256`, bytes re-read from Git at HEAD — refusals `…NOT-IN-THE-REVIEWS-DIRECTORY`, `…BYTES-NOT-THE-PINNED-REVIEW`, `…NOT-IN-GIT-AT-HEAD` each fired on my own controls (outside dir / edited after the pin / written-never-committed / committed-then-edited). The authorship limit is stated out loud in the runner and the README, which is the honest place for it. **F4** both bare refusals are named: `SPEC-SOURCE-BASE-NOT-A-COMMIT` and `PARENT-ARTIFACT-BYTES`. **F5** the block is lifted into `successorSpecShape()` and its refusals are reachable; I measured all four end to end in a scratch clone (below), and the protected-surface refusal now precedes the root shape, so a golden target refuses for being a golden.
## C — admission surface, re-measured (own fixture, `successorProof()`)
| control | code |
|---|---|
| (i) target reached by nobody · (i-b) reached only by a **computed path** · (vii) another gate's body | `SUCCESSOR-SUBSTITUTION-TARGET-NOT-IN-THE-PARENT-GATE-CLOSURE` |
| (ii) bytes moved since the reviewed commit | `SUCCESSOR-ORIGINAL-NOT-THE-PARENT-ACCEPTANCE-BLOB` |
| (iii) successor replaces a pair the spec omits | `SUCCESSOR-SUBSTITUTION-TABLE-DISAGREES-WITH-THE-SPEC` |
| (iv) review quotes 1 of 2 · review absent | `…NOT-ENUMERATED-IN-THE-REVIEW` · `SUCCESSOR-REVIEW-FILE-ABSENT` |
| (v) `conform/oracle/**` | `SUCCESSOR-SUBSTITUTION-TARGET-IS-A-PROTECTED-SURFACE` (both phases) |
| **(vi) whole-file replacement, RUN phase** | **`SUCCESSOR-SUBSTITUTION-IS-A-WHOLE-FILE-REPLACEMENT`** (r9: ADMITTED) |
Baseline (two re-targets, review committed and pinned) ADMITTED. `require.cache` carried **0** entries for any fixture original or successor. `SUCCESSOR-REQUIRES-THE-ORIGINAL-INSTEAD-OF-COMPILING-IT` still fires.
## Stated residuals (recorded, not blocking)
**R1** the run-phase whole-file gate is a 40-byte remainder threshold: a `from` that leaves **50 bytes** of the parent body standing is ADMITTED there (measured). `spec()` still refuses it `SUCCESSOR-SUBSTITUTION-NOT-A-RE-TARGET-AND-NOT-RULED`, which is the real gate; the run-phase check is a backstop and should be described as one. **R2** a parent body reached only by a **path literal** — which is how `b-ntc-successors.cjs` names the originals it compiles privately — is outside the copy test by design (measured: pasting it does not trip `…COPIES…`). It is bound instead by the per-substitution sha anchors and the table deep-equal. Both are narrowings in the safe direction for the shipped shape, but H3's review should re-check R2 against H3's own successor. **R3** the closure remains the union over all declared carriers. **R4** "seal off the tip" is still unreachable on a REVIEW-PENDING package; it stands on `test/seal-tip-and-byte-identity.test.cjs` 16/16.
## F — regression
**84/84, 0 fail**: `product-phase-and-ledger` 7 · `execution-targets` 9 · `successor-moves` 9 · `pinned-unchanged-and-ruled-substitutions` 12 · `seal-tip-and-byte-identity` 16 · `parent-pin-shapes-and-spec-successors` 8 · `parent-gate-closure-and-load-floor` **14** (was 9) · shared `rebuild/lanes/tooling/test/preflight.test.cjs` 9. `--ci --package`: **exit 2, `CI REVIEW-PENDING`, no PASS word** — B1 **6** · B2 **6** · B3 **10** · B4 **10** · B-LOM **12** · B-NTC **5** · H3 **12**, every count equal to r9's and r7's, so the fix moved no obligation. `--full --package B1` → exit 2, `BLOCKED REQUIRED-PRIVATE-PREPARATION-MISSING`.
## Bites (14, scratch clone of `e4c4e8a`; each restored)
runner edited → `RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER` · spec edited after its hash → `SPEC-BYTES-NOT-THE-REVIEWED-SPEC-IN-GIT` · `--eval` in a child argv → `CHILD-ARGV-INLINE-CODE` · `moves` without an id → `COVERAGE-MOVES-REFUSED-WITHOUT-A-PM-RULING` · non-existent `sourceBase` → `SPEC-SOURCE-BASE-NOT-A-COMMIT` · parent artifact sha tampered → `PARENT-ARTIFACT-BYTES` · forged needle child → `CHILD-DID-NOT-REALLY-EXECUTE` · fabricated ruling sha → `SUCCESSOR-RULING-LINE-SHA256-NOT-A-UNIQUE-LINE-ON-THE-CHAIN-BRANCH` · `reviewFileSha256` removed → `SUCCESSOR-BLOCK-KEYS-NOT-CLOSED` · review outside the reviews dir → `SUCCESSOR-REVIEW-FILE-SHAPE` · bad sha shape → `SUCCESSOR-REVIEW-FILE-SHA256-SHAPE` · oracle target → `…TARGET-IS-A-PROTECTED-SURFACE` · target outside `rebuild/m4/spec` → `SUCCESSOR-SUBSTITUTION-TARGET-SHAPE` · whole-file at spec phase → `…NOT-A-RE-TARGET-AND-NOT-RULED`. Restoration proved: `B1.json bc499f4f…`, `B-NTC.json 9d9e325e…`, `b-package.cjs c609cf71…`, `git status` empty.
**Provenance / boundaries.** Read-only outside this one file; no commit, no push. Only `…/work/lane-b/rv-tool9b` (this file) and `…/work/lane-b/rv-tool9b-scratch` were written; `rv-tool9` stays at `ea6956d`. No private fixture sought. Harnesses `a-api`, `b-realfloor`, `c-fixture`, `d-f1`, `e-bites`, `f-bites2` and their output are in `rv-tool9b-scratch`; each compiles the **real** runner with at most `CHAIN_REF` re-pointed (asserted: exactly one line differs).
