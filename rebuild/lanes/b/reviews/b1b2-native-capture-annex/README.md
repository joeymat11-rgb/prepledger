# Independent native-capture review evidence

PM336; exact candidate `f9e45f922bcb489d8ef4a4eded83b968356f16e4`. All execution used invented public values, owned pinned Node 22.23.2, and the actual licensed helper/operator. No original native child was executed. Author reports and outcomes remained unread during this first verdict.

## Results and limits

- Exact committed cohort: **26/28, exit 1**. Positive identity and positive main fixtures assumed a physical `b1b2-native-capture` leaf. The mandated ER worktree instead ends in `er-b1b2-native-capture`; both refused at the production root guard. The direct raw-sentinel-main test passed by that early refusal, so it did not prove child reach.
- Approved exact-source nested replay: **2/3, exit 1**. Identity and raw-sentinel main reached their intended checks. Positive main reached its invented child, then unchanged validation refused because Git's relative engine pathspec was interpreted from the nested directory; it returned an empty source delta. This is preserved as a fixture failure, not a behavior kill.
- Rooted synthetic metadata transport: **3/3, exit 0**. Only those three affected identity/main tests ran. `GIT_DIR` explicitly identified the ER worktree metadata and `GIT_WORK_TREE` the invented copy, restoring root-relative pathspec meaning without Git writes. The exact six public input copies and Node stayed identical. Canonical containment, no links, metadata source, empty Git prefix, and candidate HEAD were verified. Production root/argv/identity checks and author sources were unchanged.
- Independent cases: **5/9, exit 1**. Four failures prove two candidate mechanisms; all expected positive/refusal controls pass. Raw failures are retained, not relabeled as a passing suite.
- Four independent guard reversals each lost the named `ERR_ASSERTION` and passed again against fresh original source: observation hook, completion refusal, output whitelist, first failure. No loader/setup failure counts as a kill. These use private module source strings; the original disk files remain exact.
- Real hardlink control verified the helper retains the pending marker after a two-link diagnostic file; it is not an additional production operator run. Actual Windows protected owner/SYSTEM ACL application/readback was exercised by the unmodified operator on invented directories.

## Finding reproduction

`controls.test.cjs` uses the actual operator's directory/ACL/capture functions. Its only process transport checks the exact production executable/argv/cwd/stdio before substituting an invented child. The child uses real `node:test`, the actual helper, and public map/Git metadata validation. `fieldDiff`, original assertions, returned values and comparison validation are not substituted.

`late-exit` performs two valid observations in test four, then registers another actual process exit listener. The helper's previously registered exit listener removes the pending marker first. The later listener calls the helper a third time, which sets its failure flag after completion is already recorded. Evidence proves the listener ran with the marker absent both before and after. The child still reports real 4/4 and exit 0; capture incorrectly returns PASS and writes complete:true. `normal` passes and the same extra observation before exit correctly returns capture FAIL.

The three `manual-*` negative controls call both real observations and then emit invented TAP with `# SKIP`, `# TODO`, or `Bail out!`, followed by a contradictory all-pass trailer. Each is accepted as gate/capture PASS. The canonical manual-positive control passes. The parser stores only `ok`/`not ok` and ignores bailout before its trailer, so terminal counters alone can erase the contradictory evidence. These are fabricated-stream parser controls, not claims that the original immutable native child emitted them.

## Replay sequence

Use a new owned `work/pm-caretaker/er-b1b2-native-capture` worktree of the exact candidate with its full 1610-path index, only NEXT/AGENTS and the six licensed public inputs hydrated, and the pinned official Node copied to `.tmp/runtime/node.exe`. Copy this annex's reviewer sources into the same relative directory. The runtime SHA is in `evidence/source-audit.json`.

1. `source-audit.cjs` verifies the pinned authority/proposal/input blobs and index and hydrates only the six licensed inputs; its initial source-audit runtime path records the reviewer's prior **owned** verified Node used for setup.
2. Run `run-exact.cjs` with owned Node. It captures the unchanged committed test, retains the actual nonzero result and verifies restored inputs.
3. Run `run-independent.cjs`. Exit 1 and the four named failure assertions are the current candidate result. Its generated child and all data are invented.
4. Run `identity-replay.cjs prepare`; inspect the complete copied test and computed closure. Run `identity-replay.cjs run closure-inspected`, then `identity-replay.cjs run closure-inspected metadata-root`. Both outcomes are retained; the latter uses the explicitly disclosed synthetic metadata transport.
5. `package-evidence.cjs` preserves exact raw outputs/evidence plus metadata before staging any review artifacts. It refuses an altered full index or already existing archive destination files. The large invented cap streams remain in owned scratch; their exact lengths, hashes, manifests and outcomes are archived instead of duplicating hundreds of MiB.

The only reachable helper validation uses the named public map and Git commit/ancestry/name-status metadata. The original child is static context only. Protected engine/seed/history/frozen/private bodies, FULL/CI/artifact/receipt/phone/import/deployment paths remain unexecuted. Fixed category counts do not settle exact native deltas, which remain UNKNOWN.

`EVIDENCE-MAPPING.json` maps every archived output back to its original owned path with exact bytes/SHA256. `source-audit.json` records the initial sparse hydration correction: NEXT/AGENTS were explicitly materialized after the no-checkout sparse flags did not create them; the full index stayed exact. No test-source repair or product change was made during review.

The raw TAP failure diagnostics contain twelve whitespace-only lines flagged by the ordinary whitespace check. They remain byte-exact evidence; authored files pass the check when those raw TAP files are excluded.
