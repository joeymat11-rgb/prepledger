# PM352 independent capture-repair evidence

Candidate `be0aeeb6a8e046f55730edc824ca3dddbb181a09`. First verdict: R1 remains open because enabled capture changes an otherwise successful real Node child's result. R2's reviewed repair is substantiated.

`evidence/assessment.json` states the independent observations and limits. `MAPPING.json` binds every exact public copy to its original owned scratch path, bytes and SHA256. All failures remain, including direct-mode discovery, real `node:test` confirmation and the whole-preimage/restored-candidate control. No original native input or child was run.

The first three nonextensible-promise failures used direct children with a manually printed four-test TAP census; they are discovery controls, not node:test results. `promise-followup/` confirms the same mechanism with four real tests. `promise-causality/` proves the same child succeeds under the licensed preimage and fails again after exact candidate restoration. Neither modifies async hook callbacks or fabricates termination state.

Replay only in a separately owned fresh real worktree based on the candidate, retaining the actual `/work/pm-caretaker/native-capture-review/work/pm-caretaker/b1b2-native-capture` suffix. Start with a full candidate index and sparse public hydration as described in the review commission. Copy this annex's source files into that tree without staging them first. Independently own the exact pinned Node22.23.2 win-x64 binary at `.tmp/runtime/node.exe`; do not borrow another actor's scratch. No package install is needed.

Run the drivers below with that executable and the actual worktree root as working directory. Do not run the static original child. This sequence intentionally refuses to overwrite a prior result directory; create a new owned review tree for replay.

1. `source-audit.cjs` verifies twelve immutable inputs, full index, licensed Git metadata and runtime; hydrates only six candidate public files, NEXT and AGENTS.
2. `inspect-builtins.cjs` with the eleven exact module names in `builtin-custody.cjs` extracts public builtin JavaScript text without evaluating or enabling it. Read the recorded relevant routes before the next step; full source text is not archived.
3. `builtin-custody.cjs` verifies independently inspected builtin hashes and four actual function pins. It creates an empty hook but never enables it. The two author manifests are the only PM-permitted early input-only author reads.
4. `run-entry.cjs` runs the exact committed public synthetic entry once. Wait for completion and source restoration before other execution. It saves raw TAP, stderr, the complete public evidence and result identities.
5. `run-independent.cjs` runs 40 invented children and four one-byte TAP controls. Only the operator's child launch is redirected; the requested original argv is asserted and never executed. This seam does not claim commission or ACL admission coverage.
6. `promise-followup.cjs` runs eight real node:test children to resolve applicability of the direct-mode finding.
7. `promise-causality.cjs` temporarily uses the exact whole licensed preimage helper for one invented child, restores the candidate in finally, then repeats that one case in a fresh child. Assertions expect the observed regression; this is not a product fix.
8. `assess-and-pack.cjs` asserts observed results/restored identities and copies only the named public review output families into the annex. Remove the copied archived `evidence/` and `MAPPING.json` from the new replay annex before this step; never delete source evidence from the original review tree.

All source and outcome paths resolve inside the reviewer's own worktree. Builtin native/V8/bootstrap/path-validation/filesystem boundaries remain explicitly opaque. `node:test` moves child stderr into root TAP diagnostics, so preserve both streams. The public evidence uses invented values and only the already licensed public map and Git metadata.
