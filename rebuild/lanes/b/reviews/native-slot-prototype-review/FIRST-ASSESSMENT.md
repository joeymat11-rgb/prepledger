# PM402 independent first assessment — author outcomes unread

Conclusion: no blocking finding for the exact Windows standalone capability under PM384/386. Independent source review, one unchanged three-binary build, one delivered test run and three bounded extra controls succeeded. This is a standalone review conclusion for PM's judgment, not original-capture acceptance, integration or release authority.

Candidate 2592f091f9cd2956f5f6c442d36c8763e247cff6; base126fac49474ab2ba8e3dccf7ceb0707ddf65d113; branch codex/astra-native-slot-prototype-review-er; own fresh work/pm-caretaker/native-slot-prototype-review-er. Full2373-entry index/tree541ff76585e1700dacb58b6d677def1c41ba9df0 preceded licensed hydration. All five candidate files were completely read and byte-verified before runtime acquisition/build, and remained unchanged after execution. New B outcomes at9cc78e7701957569fd603b9013d2ac454dacb2dd have not been opened. This note and its complete own evidence will be committed and sent before reconciliation.

## Independent source findings

The opaque external returns through Node-API's default native-call handle scope; no ended inner V8 scope is returned. A separate reference of count1 retains it until explicit close or environment cleanup. Type tag, cookie, process and environment checks precede native data use. The Node22.23.2 [default-scope contract](https://github.com/nodejs/node/blob/aa4c77582be995286fc6e00aaf530dc7ade102a9/doc/api/n-api.md#L1523), [reference implementation](https://github.com/nodejs/node/blob/aa4c77582be995286fc6e00aaf530dc7ade102a9/src/js_native_api_v8.cc#L2745) and [external/tag implementation](https://github.com/nodejs/node/blob/aa4c77582be995286fc6e00aaf530dc7ade102a9/src/js_native_api_v8.cc#L2608) support that ownership. Externals are accepted by this API8 reference path; IsExternal precedes IsObject in napi_typeof because External implies Object internally. No repeat of the rejected shmbuf experiment occurred.

Close marks the handle closed before releasing its view/OS handle and root; native bookkeeping remains until finalization. Environment cleanup marks its hook inactive, releases resources/root, then the finalizer can free bookkeeping without removing an already-run hook. Normal GC finalization removes a still-registered hook first. [Node's cleanup-before-finalizer contract](https://github.com/nodejs/node/blob/aa4c77582be995286fc6e00aaf530dc7ade102a9/doc/api/n-api.md#L2036) supports the ordering. OS cleanup failure is reported on explicit close and may retain resources for a safe later retry; arbitrary OS/VM failures are not proved nonthrowing. The uninspected runtime implementation behind the public cleanup API remains a trusted boundary.

The Windows creator refuses ERROR_ALREADY_EXISTS. Attach opens the named region and checks ready/magic/version/declared view length/owner PID/nonce. Page-file initialization, view retention and coherence follow the inspected [CreateFileMappingA contract](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-createfilemappinga). A64-byte view/header is checked, not an independently queried exact section allocation. The incompatible size128 fixture verifies a header mismatch; it does not prove rejection of every maliciously forged larger section. Default creator ACL and unpredictable invented names do not constitute authenticated arbitrary-writer confinement.

The aligned32-bit progress word uses [compare/exchange](https://learn.microsoft.com/en-us/windows/win32/api/winnt/nf-winnt-interlockedcompareexchange) and [exchange](https://learn.microsoft.com/en-us/windows/win32/api/winnt/nf-winnt-interlockedexchange) with their documented atomicity and ordering. Only0→1→2→3→4 is valid; any invalid or failed step writes5, and no valid step can leave5. Fixed ASCII/hex identities and integral bounded owner/version/size values are validated without JavaScript coercion; native advance returns the existing handle. Header corruption, a closed/foreign handle, pending exceptions or a closing VM are refusal/error boundaries, not an unconditional successful atomic-update guarantee.

The delivered synthetic graph is an owner process launching node --test and its distinct test-file OS process. The owner waits for the outer process's synchronous completion, checks error/signal/status and records trace/census before reading state. It proves the declared generated writer set, not arbitrary detached descendants or the original helper's writer set. The driver's state4 condition also requires exit0; independent raw-census verification below checks the actual generated outcomes. A general adversarial TAP validator is not supplied by this prototype.

The loader refuses platform/missing/failed binary setup without a fallback, but its directory test is lexical, not symlink-resistant provenance or protection against arbitrary module-loader hooks. This reproduction supplied hash-verified regular binaries in its own scratch. Future original integration must establish its own loading and writer/capability custody.

## Independent execution

Tools were acquired once from the four exact primary URLs and matched all published pins before extraction/use: Zig0.15.2 zip92614574/SHA2563a0ed1e8799a2f8ce2a6e6290a9ff22e6906f8227865911fb7ddedc3cc14cb0c; Node22.23.2 exe86997320/0d0f5e39f9f3d9587bc19f73eab3c2c9c4903fd02d6dbf9c853dd81b3d95fad4; headers9932325/daaf13ec5d45a38bbcfcff06d0723f72a6813b89ab27bb5af2ac8e1f6259dde0; node.lib2790574/ac15f1e9d7c8279353723a77f6319967f1a41c06026521094a8234c2e6fbe052. Safe unique extraction verified19312 Zig members/18105 files/348607584 bytes and3325 header members/2725 files/58889427 bytes. Compiler executable SHA256d408dd38eed3e5204af841bcebf70502a4dbbb8399a3a3262be55059370bc018. All tool/cache/temp paths stayed under own scratch; no package/system install or alternate toolchain.

Unchanged build.cjs er-independent-one at candidate head: compiler version and three builds each exit0 with no compiler stderr. Candidate173568/SHA25666bee62804fa30196a384fd202d373d83511dbd4fb2e120d6a9a3f39b8dee170; version2 fixture173568/4196dc1c2aebeb1e3233b9c2f57f407beb6e2e4ec16c226c10d3bebefe0e21c7; size128 fixture173568/103f5459c633b6fea818acfa83ccf70605752f177c8c5457d382c2b31e0f5259. These are owned runtime binaries retained only in ignored scratch, not committed artifacts. Exact compiler argv, environment, source/header/library pins and raw stdout/stderr/exit are preserved.

Delivered standalone driver:34/34, exit0, no skips/cancellations/todos,5509.4647ms;33 actual nested graphs =33 outer runners plus33 distinct test-file processes, ownerPID55476, top invocationPID54288. Independent extraction of every raw TAP/census agrees with recorded identity/outcome.32 graphs are1/1 exit0. The intentional nonzero graph retains writer exit7, outer exit1 and actual2 tests/1 pass/1 file failure while shared state4 remains insufficient. That child failure is not relabeled PASS by the successful containing control.

The delivered cases observe0/1/2/3 partial states,5 for invalid/extra/out-of-order/duplicate progression,4 for valid two-record progress, and5 after a late third progress call despite EBADF on the later closed-descriptor diagnostic write. The earlier complete file remains. Local-buffer/complete-file leaves authoritative state0; stale nonce, wrong descriptor/header, missing region/native binary, duplicate ownership, forged/non-owner/closed handles and unclosed-child teardown are checked. GC retains an open rooted external; explicit repeatable close and region replacement behave as required.

Bounded independent controls:3 behavioral controls plus1 evidence-census subtest,4/4 exit0,267.2041ms. Two concurrently launched nested writers leave5 after both actual1/1 exit0 outcomes; this observed run is not an exhaustive concurrency proof. A direct IPC child retains its attached view after owner close: recreation refuses until child exit, owner reads refuse, the closed external is collected under two requested GC cycles, child then closes/exits0 and recreation starts at0. Six invalid boxed/NUL/nonintegral/oversized/symbol setup calls refuse with zero user coercions and parent state0. Extra graph =2 outer runners+2 test-file processes+1 direct writer;71 invented child processes total across both independent runs, separately from top harness/build/compiler processes.

## Custody and limits

MAPPING-FIRST.json identifies all266 exact own raw copies totaling5587513 bytes; raw/** has scoped -text attributes and staged Git-blob hash verification. Sources/authorities, download and full extraction inventories, bootstrap/build/driver/control argv and results, generated child sources, raw streams, PID traces and state observations are retained. No setup, compiler or unexpected test failure occurred; the deliberate nonzero child remains intact. No unchanged passing run was repeated. A custody-copy guard stopped at an import-library side output; two previously copied PDBs were returned to ignored own scratch before staging. The exception, exact moves and a corrected null byte-total aggregation are recorded in raw/setup/archive-first-failure.json; no runtime outcome changed.

Trusted boundaries remain the exact compiler/runtime/header/library and Windows implementation beyond the inspected contracts. None of these executions ran the original helper/operator, engine, app, private/history/golden/native gate, H3/FULL/CI or deployment. Full original noninterference, record reduction/privacy, complete original writer closure, other platforms and universal resource-failure behavior remain separate obligations. R1's previous rejection and earlier feasibility/source findings are unchanged. Next action after sending this immutable first assessment is the already-authorized complete author reconciliation, with no additional runtime run implied.
