# PM402 author reconciliation

The independent first verdict is unchanged: no blocking finding for the bounded PM384/386 Windows standalone capability. That assessment and its original evidence remain at `dd4ae18da24a614d468ced172cdf5cc89766d402`, committed and sent to the sole PM before reading the new author outcomes. This reconciliation does not grant original-capture acceptance, integration or release authority. The previous R1 rejection remains unchanged.

## Complete author material and identity

The final author handoff is `9cc78e7701957569fd603b9013d2ac454dacb2dd`. The full report, INDEX, INPUT-CUSTODY, SOURCES and MAPPING were read. These are the five authored report/custody records in the handoff: the report is outside the support directory, and the other four are inside it. Every one of the 249 mapped public copies was retrieved from that exact Git tree, checked against its recorded length/SHA256, decoded as text and copied verbatim under `raw/author/`. All 114 JSON files among those copies and authored records were parsed completely. Original `.tmp` paths remain attributed metadata; the review did not operate or read another actor's workspace.

`MAPPING-AUTHOR.json` accounts for all 254 copies, 5,593,988 bytes. The original 249-copy subtotal is exactly 5,490,494 bytes. The original author MAPPING and all five authored identities remain unmodified in that archive. Its 253 support paths are exactly 249 mapped copies plus four authored support records. Full Git inventories independently confirm base 2,368, source checkpoint 2,373 and author final 2,627 paths; all 259 changes are additions in the five source, report and support locations. No hidden product change, binary or archive is present in that diff. All five source copies match candidate `2592f091f9cd2956f5f6c442d36c8763e247cff6`, and the original source paths are unchanged through the final author commit and in the reviewer's working tree.

## Claims reconciled against independent findings

| Author claim | Independent conclusion and practical limit |
|---|---|
| An owned C Node-API external avoids the old V8 scope defect. | Agrees. The complete five-file source review and directly needed Node definitions established the callback scope, external/type tag and separate reference. Additional pinned Node implementation reads were made independently before the author report. The rejected shmbuf path was not repeated. |
| Root, explicit close, environment cleanup and finalization maintain safe ownership. | Agrees within the inspected contract and observed runs. Closed handles refuse, owner and attached rights differ, the open root survives requested GC, and the independent retained-child-view control verified lifetime after owner close. Arbitrary OS release failure and closing-VM/pending-exception behavior are not universally proved nonthrowing. |
| A named mapping communicates irreversible progress across actual processes. | Agrees. Aligned interlocked operations implement 0→1→2→3→4 and sticky 5, with no successful transition out of 5. The independent concurrent-writer control also observed refusal. This is neither exhaustive scheduling proof nor authenticated confinement of arbitrary native writers. |
| Identity and size checks reject incompatible setup. | Agrees for the pinned trusted-creator protocol. Nonce, owner, version, declared length, missing region, duplicate ownership and incompatible fixtures are covered. The 64-byte view/header is logical validation, not a separate exact Windows section-size query; the author explicitly states that limit. |
| Toolchain and input identities are pinned. | All four artifact size/hash pins agree with independently acquired own bytes. All 18,105 compiler-file and 2,725 header-file metadata entries match the independent extraction inventories by exact member, length and hash. The twelve shared Node/Windows definition bodies and three authority records match independent input copies. This comparison does not prove past author tool execution or audit compiler/SDK internals. |
| Build-01 has one version check and three successful builds. | All four complete stdout/stderr/result records agree, exit 0, without errors/signals. Exact ordered compiler arguments and environments agree after explicit own-root/build-phase normalization. Source, header and import-library identities agree. Candidate and incompatible fixtures remain separately identified. |
| The standalone executable cohort demonstrates the capability. | The author's 34/34 result and 33 nested graphs reconcile with raw streams, generated source, configurations, traces and outcomes. The earlier independent unchanged build/run passed 34/34 and the additional review controls passed 4/4. These are separate executions; hashes alone were not treated as reproduction. |
| Complete state cannot excuse a failed child. | Agrees. Author outer PID 39100 contains test-file PID 26120, which sets exit 7. Raw TAP retains the first file failure and 2 tests / 1 pass / 1 fail; outer status is 1. The pinned parent test asserts state 4 and rejects completion. Its containing control's success does not rename that failed child as a pass. The independent cohort preserved the corresponding actual failure separately. |
| A late extra write remains detectable despite unavailable later diagnostic IO. | Agrees. The pinned writer advances again from an exit-listener microtask, then asserts EBADF when writing the closed descriptor. The original `complete\n` file survives; the parent test asserts native state 5. The local-buffer control leaves state 0 despite its complete file. Both top-level tests pass independently and in the author archive. |
| No original integration or all-platform result is claimed. | Agrees. The driver closes the declared invented writer graph, not arbitrary descendants or the real helper's complete writer set. Original command/noninterference, private record reduction, platform coverage and acceptance remain outstanding under separate PM scope. |

## Actual run and build differences

The author's top invocation has PID 51560, owner/test driver PID 17076, duration 5,971.2452 ms and stdout SHA256 `5992dd37b3955ed85fca8446f1b91098bb2b073d806b7e73b7dfe268e5b0cf9c`. Its raw top census has 34 tests / 34 pass / 0 fail / 0 skipped / 0 cancelled / 0 todo, exit 0. The 33 case records contain 66 distinct child PIDs with each recorded test-file parent equal to its outer runner. The other 32 graphs each have 1 test / 1 pass, exit 0. Every raw census field was required exactly once and compared with the case and aggregate records; each generated writer source and raw stream hash was independently verified. All 33 generated writer sources and configurations agree with the independent cohort.

Nineteen cases have separate owner-observation JSON records; all agree with the independent observations and expected state/completion. Late IO, local buffer, nonzero child and the remaining setup/lifetime cases carry parent assertions and top-level TAP results, without separate owner-observation files. The reconciliation does not invent additional state logs. The full source, diagnostic files and actual process outcomes support those qualified claims. `RECONCILIATION-CHECKS.json` retains every case identity and census.

The independent run has a distinct owner PID 55476 and 66 nested child processes. Its additional three behavioral controls plus one evidence-census subtest add two outer runners, two test-file processes and one direct IPC child: 71 invented child processes in the independent evidence, excluding top invocation/build/compiler processes. The immutable first assessment retains the full execution details.

Both builds used the same four pinned tool artifacts. The author ran `downloads/node.exe`; the reviewer ran its verified identical `runtime/node.exe` copy. The independent top-level test used the equivalent relative source path from its own root; the author's launcher used the absolute path. These location/launcher differences are explicit and do not establish original-helper argv parity.

The three author and independent `.node` files are each 173,568 bytes, but their SHA256 values differ. `RECONCILIATION-CHECKS.json` records both identities. The independent candidate is `66bee62804fa30196a384fd202d373d83511dbd4fb2e120d6a9a3f39b8dee170`; the author candidate is `6bb68a2d28021703d776ee2afbbf9bee318354b5d3f4f6481e5294e8407822d0`. This demonstrates independently reproduced behavior from identical source and pinned tools, not bit-reproducible builds. The cause of differing binary bytes was not investigated or asserted. No author binary was opened or executed. PDB identities are metadata; the author retains only the last unused shared `mapping.lib` side output. That loss of earlier unused side-output bytes is not hidden or substituted for a tested binary.

## Historical and custody limits

The complete preserved SETUP/INPUT-CUSTODY records explicitly report the early truncated Git text-pipeline diagnostic, wrong gate-window lookup and generated PowerShell helper error, then their corrections. The final full Git tree/index counts can be checked; the missing original setup output cannot be reconstructed into a complete log. The provider-capacity interruption and withdrawn Sol proposal are PM-reported events in the author custody record, not independent observations of runtime execution. Author claims about no additional historical attempts remain attributed to those records, not proved by the absence of other files.

The original OpenFileMappingA 404 error text, its original manifest entry, the corrected official Winbase page and supplement are all retained. Its 404 status has not been relabeled as a successful download. The author's Zig helper validates all members before extraction; its header helper validates members as streamed before creation/use. The reviewer independently checked both complete archives before extraction. The matching complete file inventories establish the compared bytes, not a broader audit or a universal safe-extractor claim.

The independent first archive correction remains in its immutable evidence. During this reconciliation an offline checker initially required identical relative runtime locations and stopped; its original source, exception and corrected comparison are retained in `raw/reconciliation/`. The correction compares each graph against its own pinned runtime and verifies identical executable bytes. An optional state-file read, truncated combined display and Git's initial sparse-path staging refusal are also recorded honestly. Only the licensed review paths were then added with explicit sparse-path support; index contents and sparse settings were preserved. These are review-tool/custody corrections; no candidate source, compiler output or test result was changed, and no runtime run was repeated.

`FINAL-CUSTODY.json` inventories the final review artifacts apart from its own self-referential hash, supplied separately in the PM handoff. Raw evidence remains under scoped `-text` attributes with exact staged Git-blob byte/hash checks. The initial 270 review paths are unchanged. All tools, caches and binaries remain in ignored own scratch.

No material author claim remains unreconciled within the bounded standalone bar. No repair is requested of B on this record. The PM remains the sole acceptance judge; original capture/noninterference, full real writer closure, private reduction, integration and other-platform work remain separate. ER returns LOW/paused under PM320 after the complete handoff.
