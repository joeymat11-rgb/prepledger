# Standalone native-slot prototype, before capture integration

PM384, 2026-09-13, under193/306/320/330/344/348/350/364/368/374/376/382. B at MAX builds one minimal public prototype to test the missing parent-retained atomic-state capability. This is an explicit narrow build/runtime grant for invented data only; no original native gate, product acceptance or app integration follows.

1. Use fresh own work/pm-caretaker/native-slot-prototype-b, branch codex/astra-native-slot-prototype-b, from126fac49474ab2ba8e3dccf7ceb0707ddf65d113. Full index before licensed public hydration; every command names this root. Read NEXT/current required context, GATE-WINDOW, this entire bar, NATIVE-SLOT-PROTOTYPE-INPUTS.json (SHA256 50a38c17b56af818b7173ac6f4f46e8e1478863b92632aad5dee0bb15645ddad), full ER368 report and full B376 source rejection at2043fb96df5bc91a5c2ec6abf952b9b5bdc3687e. Preserve rejected source and all historical outcomes; do not execute the known-invalid upstream addon to manufacture a red test.
2. The separately named candidate is an owned, test-only C Node-API fixed-state mapping binding. Return an opaque, correctly rooted native handle and keep state transitions/reads in native code. Use named Windows mapping plus aligned interlocked operations; no V8 C++ local-handle cast, node-addon-api wrapper, SharedArrayBuffer fallback, resource/Promise hooks, scheduler replacement or raw memory exposure. This choice avoids the identified lifetime defect and unverified C++ ABI/toolchain dependency; it does not pre-award correctness. No personal data may enter the mapping.
3. Write only tooling/native-slot-prototype/ under rebuild/lanes/b with mapping.c, loader.cjs, build.cjs, PROTOCOL.md and test/prototype.test.cjs; plus rebuild/lanes/b/BUILD-NATIVE-SLOT-PROTOTYPE.md and raw public evidence/source/custody notes under b/reviews/native-slot-prototype/. Necessary generated fixture scripts belong in own ignored scratch. No existing helper/operator, engine/m3/m4/conformance/profile/map, package/lockfile, workflow, golden, fixture or acceptance changes. No .node binary, compiler/runtime archive, system SDK or generated worktree is committed.
4. Build locally on Windows x64 with exactly the manifest's portable Zig0.15.2 zip (92614574 bytes, SHA2563a0ed1e8799a2f8ce2a6e6290a9ff22e6906f8227865911fb7ddedc3cc14cb0c), official Node22.23.2 exe, header archive and win-x64/node.lib. Download only exact primary URLs, verify hashes before extraction/use, reject unsafe member paths, and keep every artifact/cache/temp/output under the new own root. Set compiler caches and child temporary directories explicitly there. No npm/prebuild/node-gyp/lifecycle execution, system installation, administrator action, global PATH/config change, WSL/remote/CI run or other toolchain search. If this pinned C build is incompatible, preserve the precise failure and return the smallest missing prerequisite; do not switch toolchains silently.
5. Standard compiler/header execution is authorized only for this small C binding and standalone driver. Inspect/pin the enumerated direct Node-API definitions and official Windows mapping/interlocked contracts actually needed; treat OS/runtime/compiler behavior beyond that as an explicit trusted-toolchain boundary. Do not turn this into another general source audit. Record exact compiler/runtime/build argv/environment overrides/source/library/binary hashes, without exposing unrelated environment or credentials.
6. The fixed state must distinguish none, first pending/completed, second pending/completed and a sticky refusal for extra/out-of-order/invalid progress, without wrapping back to valid. A parent keeps an authoritative view until every relevant writer has terminated; only then may it inspect completion and clean up. Validate mapping identity/version/size/ownership and reject missing, mismatched or process-local substitutes. A closed/forged handle, duplicate cleanup or failed attach must not become use-after-free, a process crash or false completion. Setup failures are contained and reported; no normal-path fallback hides them.
7. Execute meaningful standalone invented-data tests: a real parent -> node --test -> test-file OS-process graph attaches the same mapping; exact two-record progress is visible after child exit; missing/partial/extra/out-of-order progress refuses; late third progress is visible despite unavailable later diagnostic IO; parent lifetime survives child teardown and proper cleanup is repeatable; missing native binary/region, mismatch and invalid/closed-handle cases fail closed safely. At least one negative control must distinguish actual cross-process memory from a local buffer or stale record. A synthetic test path is not the original production child argv and must not be reported as its proof.
8. Test only the compact owned prototype. Retain every setup/compiler/test failure, raw stdout/stderr/exit/census and exact source for each phase; do not normalize or overwrite failed results. Use deliberate negative controls that exercise the claimed contract, not repeated positive runs or implementation-mirroring assertion counts. This cannot prove full original noninterference, private two-record capture, all-platform behavior, native field deltas or any existing engine PASS. Those remain later exact integration/review obligations.
9. Deliver the complete <=60-line report, exact candidate/tested heads, source and toolchain inventory, real results and residual limits immediately. ER/MAX will independently inspect the exact source before receiving author outcomes, then execute the specifically licensed standalone checks under this same preregistered bar; do not self-accept. Once this finite handoff is complete, B returns LOW/paused320. E XHIGH380 continues the separate science correction contract. Existing Codex usage only; no paid API, purchases, account/reset/import/phone/deploy action.
