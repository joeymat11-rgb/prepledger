# TOOLING FIX r7 — six review changes, two PM rulings, the shared preflight

Branch `rebuild/lane-b-tooling`, base `daa87c8`, last code commit `5119fd4` (this docs commit stands on top of it), author `lane-b-tooling7`, not pushed. Runner `769fd09e2baa8b5ea4a628d20b781a321bd2d13d4601ae3b17ecf04520962186`, 1970 lines, 158 238 B, pinned by all seven specs. Suites **58/58, 0 fail** (7 + 9 + 9 + 12 + 13 + 8). Everything below executed with `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`, node v24.18.0. Full detail in `README.md` and `TOOLING-REPORT.md` §r7.

## The six r7 changes — all DONE

* **F1** a fifth product role, `pinned-unchanged`: `pre === post` (both real), NOT a parent pin, and a declared child's argv names the file or reaches it through a relative require (`executedClosure`, read never executed, bounded at 512 and the bound reported). It is counted in its own bucket, never among "at the declared post-image". Role `new` now means `pre === null` or `pre !== post`. 5 cases.
* **F2** branch B matches the ruling's own enumerated DESCRIPTIONS: `ruledDescriptions()` parses `(two at <commit>: … and …)`; `describes()` needs the module's token IN that description plus one other described word standing in the substitution's text and in NO repository path. Descriptions are consumed, so the ruling's count is the ceiling. r6's 8-of-17 predicate is re-stated in the suite and MEASURED to admit a third substitution that r7 refuses; B-NTC's own two are measured admitted.
* **F3** one `ancestor(commit, of, code)` helper carries every `merge-base --is-ancestor`, so X2's chain-ancestry refusal names itself; plus named theme-shape, both coverage-map shapes and the successor-block key shape. Wider than asked: the FAIL_CODES harvest now admits a name followed by a COLON, recovering six more (`SEALED-PROFILE-RECOMPUTATION`, `SINGLE-PARENT-CHAIN`, `SINGLE-PARENT-CHAIN-SEALED`, `BRIEF-ACCEPTED-WITHOUT-A-CITED-LEDGER-LINE`, `THEME-AUTHORIZATION-UNVERIFIABLE`, `BRIEF-ACCEPTANCE-UNVERIFIABLE`) that printed nothing for no reason but punctuation.
* **F4** the ruling is located by `coverage.successors.rulingLineSha256`: the runner searches `DECISIONS.md` on `CHAIN_REF` for the ONE line hashing to it and requires `B-NTC-INHERITED-1` on it. A PM renumber moves nothing.
* **F5** the H3 skeleton's shape is valid — `pre` is a real sha and `post` is null, so `pre !== post` and F1 cannot fire; its note now says that instead of describing the old refusal.
* **F6** `IDS` = `B-NTC, H3, B1, B2, B4, B3, B-LOM`: `DECISIONS:124`'s order exactly, with B-LOM after it because no ruling puts it inside. Comment and array agree.

**How F1 handles the sealed case, as asked.** Role `new` with `pre === post` refuses only while the artifact is UNSEALED. A spec whose SEALED artifact carries that same file, role and two shas is GRANDFATHERED, named on every run (`PRODUCT DECLARED-UNCHANGED-UNDER-ROLE-NEW …`) with a non-blocking OPEN obligation to re-declare at the next seal. B-NTC's sealed artifact semantics are unchanged; a fresh spec cannot make the same declaration.

## The two rulings and the preflight

* **`:135 (4)` SEAL ON THE TIP.** The ACCEPTED branch of `envelope()` refuses unless the chain tip stands in HEAD's **first-parent** chain; the escape is a PM FREEZE line cited in the one optional `authorizations.freeze` key and matched by the line's own sha256 on the chain branch, naming this package and a base in this first-parent chain. 5 cases both ways. **`git merge --no-ff <tip>` from the lane does NOT satisfy it** — the tip lands on the second parent. Rebase, or freeze.
* **`:136 (3)` BYTE-IDENTITY RE-VERIFY.** The seal step — the FULL run reaching `POSTFIX PACKAGE PASS` — writes `receipts/<ID>.json`: artifact, spec, runner, envelope key, verdict coordinate and every pinned product byte. A later `--full` on an ACCEPTED envelope re-takes all of it. Identical → `--ci` + pin verification + receipt check + `POSTFIX PACKAGE PASS`, with no private oracle, no historical audit and no 19 gates. Any byte change → `SEALED-RUN-RECEIPT-VOID` naming what moved, and the FULL run exactly as today; a missing receipt the same, which is why the FIRST full run with the private census is unchanged. `:136 (3)`'s "the verdict file names the sealed run's evidence hashes" is checked literally against `rebuild/lanes/b/VERDICT-<ID>.md`. 8 cases.
* **`:135 (3)` BUILDER PREFLIGHT** at `rebuild/lanes/tooling/preflight.cjs`, 160 lines, exactly one stdout line. Diff inside custody (committed, uncommitted and untracked, `--untracked-files=all` so a stray file is named not collapsed), report ≤ 60 lines, counts present, longest STATUS line ≤ 400 code points, no U+2013/U+2014 in declared UI custody. CI at the exact head sha is offline-unreachable and is NOT faked: the exact `gh run list … --commit <head>` and the commit URL are printed, `--ci-run <id>` is REQUIRED, else `PREFLIGHT FAIL CI-UNVERIFIED`. 8 cases.

## Terminals, executed

`--ci --package` → `CI REVIEW-PENDING`, public evidence only, exit **2** for all seven: B1 **6 open**, B2 **6**, B3 **10**, B4 **10**, B-LOM **12** — every count equal to r7 §3, so nothing in this pass moved an obligation. B-NTC on this branch **5 open**, exit 2, also unchanged. H3 **12 open**, exit 2, where before this pass it was `FAIL REGISTER-D-ID-INVENTORY-EMPTY-AND-NOT-EXEMPT`, exit 1.

**H3 on its own branch** (scratch clone of `origin/rebuild/lane-b-h3`; that worktree never opened or written): the register refusal is gone. It then refuses `PRODUCT-CHANGE-ROLE-DECLARES-NO-CHANGE` for `rebuild/m4/workout/athlete-state.cjs`, declared role `new` with `pre === post`; re-rolled `pinned-unchanged` it reads `PRODUCT IMPLEMENTED; 2 at the declared post-image / 0 at the pinned pre-image / 52 carried / 1 pinned-unchanged` and `LAWS 45/45`, then stops at H3's OWN known blocker F-C (`b-ntc-successors.cjs` pins `journey.test.mjs` at `228c076d…`, H3 declares `aecb8fe4…`). **One line for its builder: re-declare that pin `pinned-unchanged`.**

**B-NTC: these changes must WAIT until after its merge.** In a throwaway clone of `origin/rebuild/lane-b-ntc`: the r7 runner alone → `FAIL RUNNER-BYTES-NOT-THE-REVIEWED-RUNNER`; spec re-pinned → `FAIL SUCCESSOR-BLOCK-KEYS-NOT-CLOSED` (F4's new key); that key added → `FAIL SEALED-PROFILE-RECOMPUTATION`, exit 1. The last is structural and not a defect: the artifact `87f4848c…` pins spec `05a5aa1f…`, and re-pinning the runner moves the spec's bytes — ANY runner change does this to ANY sealed artifact. B-NTC keeps `eedabccd…`, the runner it sealed with, and this branch is taken up at its next re-seal. The sealed bytes with their own runner reproduce `ENVELOPE PENDING artifact=87f4848c… spec=05a5aa1f… runner=eedabccd…`, `PRODUCT IMPLEMENTED; 33/0/20` and `LAWS 45/45` in the clone, then stop in the clone's own `node_modules` (`@noble/hashes/sha2.js`) — a fixture gap, so `PUBLIC CI EVIDENCE PASS` was not re-reached there and is not claimed.

## OPEN

1. **For the PM.** `:135 (4)` as built refuses a lane that MERGED the tip rather than rebasing onto it — B-NTC's own head is such a merge. Confirm rebase-or-freeze, or say ancestry is enough and the first-parent rule should be relaxed.
2. Items 3 and 4 are ONE commit, not two: their code meets at one call site in the main sequence and their suite is one file. Every other item is its own commit.
3. `receipts/` is empty here — nothing on this branch has an ACCEPTED envelope, so the seal step's write is exercised only by the suite and never yet by a real seal.
4. This branch's head is not on the chain tip; a seal from it would refuse at `SEAL-BASE-IS-NOT-THE-CHAIN-TIP` until it rebases. Nothing here seals, so nothing refuses.

## r7b — the two defects the H3 builder found (runner `56aba344b40d6acc3e28f271502c6df709fa130f51b62f72ecf28e9364009d3f`, suites 66/66)

* **F-E, and it blocked EVERY child of B-NTC.** The chain carries two artifact shapes: the accepted originals write `product` as a flat file → sha256 map, this runner writes file → `{pre, post, role}`. `held()` and product()'s pre-image check knew only the flat one, so a child bound to `acceptance-b-ntc-native-trend-context.json` refused `PARENT-PIN-BROKEN-AT-SOURCEBASE` on `rebuild/engine/plan.cjs`, whose bytes are identical everywhere. **The READERS are fixed, never the sealed artifact** — it is merged, receipted at `DECISIONS:141` and named by a verdict file; one normaliser (`parentPin`) takes either shape and gives the parent's pinned byte (`post || pre`), and anything else is `PARENT-PIN-SHAPE`, never a skipped entry. The grandparent is read through it too.
* **F-C, `DECISIONS:142`'s grant, made declarable.** `SUCCESSOR_PACKAGES`, `SUCCESSOR_PARENT_COMMIT`, `SUCCESSOR_WRAPPER` and `SUCCESSOR_SUPPORT` are gone. What admits a package now is `successorRuling()`: the ruling LINE, located on the chain branch by the sha256 the spec records (r7 F4), must name `M2-<ID>` (from the command line, never the spec), use the word SUCCESSOR, stand on `DECISIONS:113`'s conditions (a)–(e), and NAME the support file — which must be a path this spec declares it changes. The acceptance commit is the parent receipt's own `reviewedCommit`; the accepted schedule is the parent artifact's own `children` needles, falling back to a spec-named wrapper that must be a parent execution pin; the gate set is the parent's `byChild` ∩ the carriers this spec declares ∩ the carriers whose closure reaches the support file. Conditions (a)–(e) are enforced exactly as before. Branch (A) of change 4 widens from `superseded-by-child` to any declared change (`edited` too), which is what `:142`'s "in the :113 shape" means over a parent PRODUCT pin; every image it may carry is still one product() re-verified against the parent artifact and the disk.
* **H3 scratch terminal** (clone of `origin/rebuild/lane-b-h3` @ `1cc8148`, worktree never opened; the r7b runner dropped in, `runnerSha256` and H3's two `superseded-by-child` posts re-stated as its builder would): `PARENT PINS RE-ASSERTED … 53 product pins` · `PRODUCT IMPLEMENTED; 9 at the declared post-image / 0 at the pinned pre-image / 49 carried byte-identical` · `FIDELITY OBSERVED` · `AUTHORITY OBSERVED` · `LAWS 45/45 executed` — the H3 builder's own rehearsal figures, reproduced — then the first REAL line: child `source-carriers` refusing `Unlisted parent pin drift rebuild/engine/writers.cjs` inside `b-ntc-successors.cjs`'s own `preflight()`. That is F-C, H3's package work, and it is now declarable.
* **For the PM, no code:** `:135 (4)` "on the tip" is implemented as the chain tip standing in HEAD's **first-parent** chain, so `git merge --no-ff <tip>` from a lane does not satisfy it (B-NTC's own head is such a merge) and the answer is a rebase or a FREEZE line. Lane B will ask whether plain ancestry suffices; if it does, one line of `sealOnTheTip()` changes and the suite's rebase case becomes the merge case.
* **OPEN:** B-NTC's sealed spec/artifact are unchanged and stay readable AS A PARENT — that is what F-E fixes — but B-NTC itself still keeps the runner it sealed with (`eedabccd…`); `SUCCESSOR-BLOCK-KEYS-NOT-CLOSED` then `SEALED-PROFILE-RECOMPUTATION` if it is re-pinned, as r7 measured. Its next re-seal takes this runner up.

## r8-fix — TOOLING-REVIEW-r8's five changes (runner `1a3d395d80fcdc9c1ccc3a85f118a8feaa2a314d1553d3420dc864d67e66cd00`, suites 70/70)

* **Change 1, the load-bearing one.** The `:136 (3)` receipt was consistent, not authentic — r8 fired a hand-written one and the private oracle, the historical audit and the 19 gates were skipped on a plain disk read. `receipts/<ID>.json` must now be COMMITTED (its bytes at `HEAD`, and at the package's receipt base if it already stands there) and its OWN sha256 must be NAMED in the verdict file — the one string that exists only after a seal step has written one. Either missing → `SEALED-RUN-RECEIPT-NOT-IN-GIT` / `SEALED-RUN-VERDICT-DOES-NOT-NAME-THE-RECEIPT`, the step is unavailable and the FULL run stands. The authenticity test runs BEFORE the content tests, so an edited receipt reads NOT-IN-GIT and not VOID. The seal step prints `SEALED RUN NEXT STEP …` telling the sealer to commit it and name it. 4 cases both ways.
* **Change 2.** The `receipts/*.json` exemption from `UNLISTED-SOURCE-CHANGE` narrows to THIS package's own receipt, decided in `fidelity()` where the id is known; it cannot be removed outright because change 1 requires the receipt to be committable.
* **Change 3.** `TOOLING-REPORT.md` and `README.md` carry the head's own figures, the string `r7b`, and the runner sha — both were one revision stale and neither documented r7b.
* **Change 4.** `parentPin` treats only the literal `null` as "no post-image": `0`, `""`, `false` and `NaN` are `PARENT-PIN-SHAPE`, not a silent fallback to the pre-image.
* **Change 5.** The preflight's UI dash check scans untracked files too (`git ls-files --others --exclude-standard`) — the same set check (1) already counted, so an em dash in an uncommitted screen template no longer reads `PREFLIGHT PASS`.
* **`:135 (4)` stays first-parent**, as the PM asked. It is now ONE constant, `SEAL_TIP_RULE`, and the suite compiles the runner a second time with that one word changed — asserted to be the only line that differs — and measures both settings on one repository, including the case that divides them (`git merge --no-ff <tip>`: an ancestor of HEAD, not in its first-parent chain) and the stale base both still refuse.
* **OPEN.** r8 §C's two widenings are still for the PM to confirm or narrow: branch (A) re-targets over every `edited` pin (`:113 (c)` says `superseded-by-child`), and `:136 (3)`'s anchor is a runner-written receipt with the verdict naming it. r8 §C's residual second surface stands as written: a change outside `s.product` ∪ `PIN_PATHS` ∪ the four trees `fidelity()` diffs leaves the receipt matching. r8 §E6 is right that H3 is not yet a consumer — every shipped spec still declares `coverage.successors: null`, so the F-C path is exercised only by the suite and by r8's own controls.

## DECISIONS:145 — seal-on-the-tip is ANCESTRY (runner `59ecc7f91f5f8f6d40c5c0f61267c6f71ca7edd7987b8022da39c5a04a1cce0d`, suites 70/70)

The PM ruled r8 F1's recommendation: the CURRENT `origin/rebuild/t2-client-core` must be an ancestor of the branch head; a merge and a rebase both count; a stale base does not; the FREEZE escape is kept. `SEAL_TIP_RULE` flips to `'ancestor'` with `:145` cited beside it — one word, as r8-fix built it — and the refusal sentence now names the rule it applied. `'first-parent'` stays implemented, and the suite still compiles the runner a second time with that one word changed (asserted to be the only differing line) and measures both settings on one repository: `git merge --no-ff <tip>` is a SEAL under the shipped rule and a refusal under the other, a rebase satisfies both, and a stale base is refused by both. Terminals unchanged; all seven specs re-pinned; README and TOOLING-REPORT say ancestry in one sentence each, with the r7 first-parent paragraph kept beside it for the record of what changed.
