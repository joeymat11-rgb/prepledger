EARNED — SUCCESSOR POLICY BYPASS CORRECTION
2026-09-11. Author candidate; independent re-review still required.
Branch codex/astra-tooling-policy-fix; base 7cd7a5b.
Assignment ASTRA-TOOLING-POLICY-FIX.md; independent finding 9c95dee P1.

(1) FIX
B-NTC policy selection now follows the fixed CLI package identity unconditionally.
Candidate argv, an unchanged helper, missing parent or parent pins cannot disable
it. Exact policy/source/authority checks remain before the campaign and at
coverage/proposed-artifact/seal. B-NTC inherited coverage has no original fallback.
The ANY-parent-pin early return is removed.

Other packages retain genuine original inheritance only through an exact accepted
child schedule: either embedded in the accepted artifact, or the five literal
inherited names/argv/verdicts from the legacy NATIVE-CARRIERS schedule. That known
route binds parent artifact e940359b... and native-carriers-package.cjs SHA
0fe94beee6a6e57315eee8535690d378cf42171e4c64bcf6172284fa47703655,
verified in Git at the parent's actual reviewed commit and on disk. Every inherited
route checks the accepted gate/name mapping, exact argv/needle, actual run targets
and verdict, plus unchanged executable bytes in HEAD and on disk. Unknown parent
formats refuse. No old parent re-seal is needed for the known legacy schedule.

Policy614717800602ce09f792b77a2ef04f191a9d156573b572b772aa1854afae17ee
and all 82 source pins are byte-unchanged. moves={} / MOVES_RULING=null remain.
All authority, receipt/remote-chain and own-child assertions are retained.
Six specs change ONLY runnerSha256, mechanically to
ca0419e61beac8ffe9c89971e11fd1f3cfb0913af1d2d5d05ddbdf12c057f484.

(2) EXECUTED DELTA
Existing Node24.19.0, Windows; no install. Focused suites run once after correction:
node --test --test-reporter=tap rebuild/lanes/b/tooling/test/execution-targets.test.cjs
  => 9 pass / 0 fail / 0 skipped. Trailing argv, real multiple-test execution,
  own-child checks, product roles/preimages, paths and target pins remain covered.
  The former arbitrary-parent-pin positive now expects refusal. The target-pin
  component uses B1 so it does not bypass B-NTC's now-unconditional policy.
node --test --test-reporter=tap rebuild/lanes/b/tooling/test/successor-authority.test.cjs
  => 10 pass / 0 fail / 0 skipped. Existing eight authority/source/actual-successor
  groups retained; two added regression groups discriminate:
  - Keep all 15 real candidate declarations, exact product/map and theme=null;
    retarget only the five inherited children to unchanged parent-pinned
    native-carriers-source.cjs via Node --test, needle # pass 1. All five actual
    processes exit 0. Actual source7cd7a5b returns null support and falsely counts
    all 9 inherited gates from their run results. Corrected source refuses
    SUCCESSOR-CHILD-DECLARATIONS before campaign and before coverage counting.
    Other-package legacy-route checks refuse the same helper executions.
  - Exact known legacy schedule verifies; real native-carriers-source-carriers.cjs
    executes and its actual result satisfies the three inherited source-gate
    checks. Changed needle and schedule-source drift refuse. The separate real
    B-NTC source-carriers successor positive remains green.
node --check runner; git diff --check => exit 0.

These are component tests on real runner functions and real Node children in
isolated public fixtures. The baseline false coverage is not package acceptance.
No function body, stdout, artifact receipt or PM authorization was fabricated.
The original-route test restores only its isolated fixture runtime/workflow to
exact accepted-parent bytes, then restores candidate bytes and rechecks the full
policy closure. Builder sources and the real chain refs remain untouched.

(3) REMAINING
Same independent reviewer must recheck this exact delta. No PM policy authority
is issued by this candidate. Genuine exact theme/brief citations on the fixed
accepted chain and candidate ancestry, final artifact receipt, CI/FULL and private
custody remain separate requirements. Prior review's unaffected authority/ancestry
and original13/child4 evidence is retained; no engine/full campaign repeated.
No private read/materialization, dependency install, lockfile change, shared
ledger edit, push, integration merge, deployment or self-acceptance.
