# Fresh weight feedback and useful nutrition: implementation sequence

Planning only, September 11. Source: owner-entry d05876fbc01765f7958f6e5a1a58e06e9a53f263. Implements PM ASTRA-FRESH-NUTRITION-PLAN.md. No product changes or new physiology approved here.

Read the external owner-approved PRODUCT-GOAL.md v3 and current decisions P1/P4/P5 at `C:/Users/joeym/Documents/Codex/2026-09-06/earned-process-proportionality-audit/`, existing ROADMAP/DECISIONS, owner-entry evidence, clean-init, and DAILY-PLAN-CONTRACT-v1. Preserve one coordinated plan, all four nutrition fields, individual goals and learning from real response. The unavailable UI is a temporary dependency, not the endpoint.

## 1. First delivery: useful scale feedback, independent of lean mass

Publish a bounded native reading contract, then implement a source-owned pure projection. Keep `athlete-state.cjs` and immutable initialSetup unchanged. The source supplies the initialization and recurrence; Today supplies neither a fake trend nor a synthetic model/blackout date.

Proposed interface: `projectScaleFeedback({basis, readingFacts, dayContext, asOf})` returns frozen `{profile, sourceRevision, observationRefs, latestObservation, baseline, smoothedWeight, scaleRate, requirements}`. Baseline is either unavailable or `{value, unit:'lb', sourceOpId, effectiveDate}`. It is the first eligible real reading, not a previous observation. First-row prior value is absent; one row cannot establish a measured rate. Keep body-fat/fat-loss rate unavailable independently of scale feedback. With no eligible rows, retain all observed weights and state `WEIGHT_ELIGIBLE_READING_REQUIRED` with the actual reason.

The trusted host must supply same-generation authenticated/current factual records, including original op ID, resolved corrections/reclassifications, tombstones/conflicts, effective instant/date/offset, source disposition and relevant dated context. `today-bindings.mjs:269` currently exposes only date/lb/op_id; that presentation list cannot prove eligibility. Reuse the `client/index.cjs:130` readingProjector boundary and its `earned/reading-projection/v1` shape where applicable; a qualified implementation is not present at this candidate. Bind the real interpreter through the existing stage/host composition, not an arbitrary page-created DTO.

Define these native mappings before retaining calculation code:

- Classify each observation at its actual effective context. `readWindow` currently uses evaluation-time today/hour and legacy sessionLog; replaying old rows as today's fresh observations changes meaning. Preserve the established before-noon/pre-training rule and intake behavior. Bind native session chronology to the training predicate without manufacturing sessionLog; publish the exact event that makes the day trained and refuse ambiguous order. Missing time/context must be a named mapping requirement, not morning-by-default.
- Preserve source-qualified sealed/blackout exclusions without inventing a personal blackout. Late/otherwise excluded first readings remain visible and do not seed eligible trend; an explicit accepted reclassification may change that. Future observations cannot affect an earlier as-of view. Specify same-day duplicate selection and correction ordering from accepted reading semantics; do not select by array insertion order.
- On every reopen/cache loss, fold current resolved facts afresh: corrected first reading changes the baseline; removed first reading promotes the next eligible real contributor; removing all restores no baseline. Corrections to later rows recompute downstream values. Preserve originals and receipts; generate no durable legacy read receipts during projection.

Reuse the existing eligible-update arithmetic and scale-regression/uncertainty behavior, rather than creating another smoothing coefficient or sample threshold. When its parent is stable, factor the relevant arithmetic into a shared engine helper with legacy parity checks. `applyRead` assumes trend/blackout; do not make it callable by injecting made-up state. `currentRate` also adds `model.drip` even when returning scale regression: separate its scale result from the body-composition-dependent fat result. Suppress its unmeasured numeric prior as a measured result; do not invent weekly snapshots to reach a gate.

Acceptance: empty; first eligible; first late then eligible; prior training with proven/ambiguous native chronology; same-day repeat; correction/reclassification/tombstone of first and later rows; all removed; conflicts/rejected records; future/as-of; UTC+13/DST/reopen; cache missing/stale. Show one real baseline without rate, then qualified scale rate at the existing evidence threshold without any model. Demonstrate exact legacy configured-state parity and that a missing model cannot block scale-only feedback.

## 2. Durable personal inputs, alongside that work

Use a separate versioned nutrition-input/goal record and effective complete plan groups. Do not add fields to the closed original setup, reseed the installation or copy an athlete's ledger. Proposed command boundary: `prepareNutritionInputs({expectedRevision, proposal})` validates a closed typed payload; `commitNutritionInputs({preparedId})` uses the existing durable/authority/currentness path. Exact leased operation encoding must close DAILY-PLAN U1/U2 before enabling writes; generic event/plan class names alone are not a contract. Retain original source, effective date, supersession/correction, uncertainty and explicit unknown/cleared meanings.

| Input | Required when / actual consumer |
|---|---|
| Goal and agreed phase, effective date, priorities and acceptable tradeoffs | Required for a nutrition prescription. `apModeOf` otherwise silently selects recomp; cutRateBand substitutes165lb. Separate agreed fat-loss/maintenance/gain/recovery from inferred regime. Unsupported phase mapping returns `NUTRITION_PHASE_MAPPING_REQUIRED`, never recomp. |
| Actual bodyweight reference | Required for body-mass/rate-based routes; use stage1 observation references, not manually copied history. No eligible reference means a specific request for a reading. |
| Governing policy ID/version and applicability | Required for advice. Bind the actual public or individually agreed owner policy through its existing qualification path; users should not have to choose technical IDs. A personal coefficient is not a public default. |
| Body-composition source, date, original measurement/units, method and confidence | Conditional on the selected lean-mass/energy-floor route. `bfEst`, `proteinTarget`, `calorieFloor`, partition/energy-density paths consume model evidence. An unknown answer remains unknown; never relabel an estimate DEXA or assume lean drift. |
| Exercise/activity and applicable constraints | Collect/reuse only the fields required by the chosen policy, with source/date. `calorieFloor` currently falls back to four sessions/week when exercise-energy evidence is unavailable: this is not Dad's programme or measured expenditure. Actual programme frequency alone is not measured exercise energy. |
| Existing agreed calories/protein/carbohydrate/fat, if any | Optional input route. Preserve source, units, target/minimum/range/not-prescribed and agreement; recorded agreement alone does not qualify advice. Never turn a larger agreed protein target into the policy minimum. |
| Actual intake totals/completeness, estimates and activity records | Required later to learn personal maintenance; missing intake is not zero. Intake estimates/completeness need explicit representation. Age/height/sex or other descriptors are required only if the selected qualified route actually consumes them. Preferences are optional until they constrain that route. |

Minimum UI: reuse current weight entry and saved programme. One short “Your nutrition” form asks the person's goal/phase and whether an existing plan should be recorded; show conditional measurement/constraint questions and “I don't know.” Reuse existing answers visibly. Present a review of actual values/meaning and one clear confirmation; validation retains input and committed success alone advances. No invented starter plan or Dad six-screen implementation.

## 3. Connect a qualified daily plan, then learn from response

Close the existing DAILY-PLAN-CONTRACT U1–U5 in this feature; do not invent a second macro framework. `readDailyPlan()` should return its four typed fields and agreed/proposed/constraint distinctions from one trusted generation. Each unavailable field names its missing input or policy dependency; valid governing fields are not blanket-hidden.

Reuse explicitly applicable ratified `earned.protein.public v1`/`earned.floor.public v1` branches and individually bound owner policy where qualified. Their distinction is already intentional: no universal coefficient decision is needed. A public body-mass branch may avoid requiring lean mass for protein if its actual eligibility is met. If no existing branch covers a first-use case, commission only that bounded applicability/formula review. Existing `proteinTargetForRegime` is not a bodyweight-only escape: it calls proteinTarget first.

Calories: assess the existing labeled mass-based provisional route and its upstream cut-band, body-composition floor and energy-density dependencies together. Its “no body-fat number” comment does not remove those calls. Reuse it only when its governing inputs/applicability are established. Otherwise explicitly choose and qualify a first-use prior/floor route; do not silently remove the floor, fabricate lean mass or turn an estimate into measured maintenance. Missing body composition must not force indefinite abstention where a qualified alternative can be implemented.

Carbohydrate/fat: no numeric producer is present in this Today path. Require their actual agreed meaning and, if generated, a scoped producer policy: quantity definition, rounding and energy-coherence semantics. Neither a guessed ratio nor a generic4/4/9 validation is authorized by the read DTO. Explicit not-prescribed is legitimate only when it is the agreement, not a placeholder hiding an unfinished producer.

Follow immediately with durable truthful intake totals and the existing observed-maintenance path. `observedTDEE` uses measured rate plus intake evidence and matched endpoints, with stated sparse-data fallback; preserve those evidence labels and applicable source qualification. Keep estimates separate from governing targets. Re-evaluation produces an explained proposal/hold with response criteria, then updates the real complete plan group through the agreed consent path. Goal/phase changes are explicit; scale plateau or inherited free/costing/accretionBound labels do not prove physiology.

Acceptance: missing goal/policy/source gives a field-specific action; all arithmetic finite; no silent recomp/165lb/FFM/four-session assumptions; explicit source confidence and phase boundaries; owner/public distinction; exact four-field meanings; atomic coupled updates/conflict fallback; correction/removal/cache loss; same-generation currentness; provisional-to-observed transition with genuine qualifying intake/weight evidence; changed estimate does not rewrite an agreement; one proposed change/hold produces coherent Today/full-plan output and durable follow-through. Compare matched synthetic cases to the existing qualified behavior, without claiming better outcomes from more tests.

## 4. Ownership and scheduling

- Independently author proposed contracts, pure factual projection and tests under `rebuild/m4/nutrition/` and its spec, plus the input form against that published interface. Root assigns one owner per file. No qualification receipts or moving B-NTC pins are edited during this preparation.
- At integration, one assigned builder owns the actual reading interpreter/stage wiring, `m3/w6/local/{local-client,host-bindings,today-bindings}.mjs`; another nonoverlapping assignment owns `m3/w7-preview/today/{today-model,today-app,today-entry}.mjs/cjs` as applicable and the new form. Retain original initialSetup and calendar contracts.
- Engine work is a separately scoped delta in `engine/{writers,energy}.cjs` plus any shared scale helper/runtime export, only under its explicit brief. Use the PM-named immutable accepted integration parent containing setup/entry and the applicable B-NTC/B1/B2 source semantics; independently verify ancestry and affected APIs. Do not repeatedly seal successive moving parents or impose unrelated package completion as an automatic dependency.
- Deliver stage1 scale feedback and input capture while resolving only the concrete policy/wire gaps above. Bind the daily-plan/learning sequence to the same feature brief, with named next builder and acceptance cases. Final engine/source qualification and affected CI occur once on the stable candidate; independent review remains required.

Evidence: source inspection only. Root's listed failure probes are reused as reported findings, not re-executed here. No literature campaign, product edits, private reads, installation, tests/full gates, receipt claims, push or deployment. One planning document committed.
