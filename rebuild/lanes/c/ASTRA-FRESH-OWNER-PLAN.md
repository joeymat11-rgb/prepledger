# Fresh owner daily use — executed findings and next build brief

September 11, 2026. Preparation only; no product changes or acceptance claim.
Base: 7b1678a4a8449053000d52a0a8e9e070f32a240e. Branch: codex/astra-fresh-owner-plan.
Read NEXT.md, ROADMAP.md, lanes/LANES.md, DECISIONS:100,109–111, T2 report template, approved Refinement A/Additions C handoffs and pending lane-c/dad first-run brief. Public source and synthetic inputs only.

## Findings

1. **Setup has no authoritative fresh-enrollment carrier.**
   `rebuild/m3/w6/local/local-client.mjs:232–238` creates empty client collections and puts cleanInit ONLY in derived.value. Boot deliberately rejects an absent/malformed cache (`:306–318`). The Today wrapper enrolls with optional cleanInit (`local/today-bindings.mjs:176–200`) but exposes no setup read to the model. `today/today-entry.mjs:157–177` supplies neither cleanInit nor a durable basis: default boot uses SYNTHETIC_DAY and createTodayModel's fixture. `today/today-model.cjs:105–139` reconstructs from an injected basis or createBasisState, then replays weigh-ins. Persistence of the cache's bytes is not authority or a reconstruction path.

   Synthetic execution: enroll a unique one-lift clean-init state through the actual local era; record and close a set. Searching collection VALUES for its synthetic athlete label finds only derived. Separately remove derived from a synthetic sealed generation using repository.commit, close, and reopen: ready=true, derived=null, derivedStale=true, DERIVED_SIDECAR_MALFORMED; ops unchanged; setup label absent from all remaining collections. This is deliberate synthetic cache loss, not deletion of real data. Import custody is an authoritative original for an explicit imported bundle (`local/import-bundle.mjs:380–418,506–520`); fresh enrollment does not create one. T2's consented planTransactions are another existing authority mechanism, but enrollment creates none and an initial setup must not be mislabeled as a consented transaction (`client/index.cjs:71,280–285`).

2. **The default page is still a fixed-date demo.**
   Entry passes SYNTHETIC_DAY to installation/readings; model closes over that day; no midnight/pageshow/visibility refresh exists. `local/today-bindings.mjs:164–168` uses 13:00Z, -05:00 and monotonic 0 for day-bound hosts. Its default liveClockOver also freezes that time; wallClock computes a real local date but freezes the offset at creation. Passing today's string alone therefore fixes the displayed date while leaving fabricated operation times/offsets. Readings execute through the installation's client; gym/check-in get independent day-bound stages. C4 correctly preserves host-day alignment, explicit-provider precedence, named clock mismatch and recorded later-day adoption; replacing those controls with one ambient Date call would regress C4.

3. **First performed load saves; the next baseline does not adopt it.**
   Executed the actual local installation + createGymModel path, no collaborator stubs. Synthetic state: one U exercise, one original slot, hi10, inc5, actual rungs20–50, every history empty, w:null; Fri Sep11 and Mon Sep14 are U. Day1 read: READY, “Find a working load × Record the reps performed”, load/reps null. Start succeeds. Typed40 lb ×10 with explicitly chosen exact2 effort saves and reads back; Finish succeeds. Close installation and reopen Sep14 over the same IndexedDB/key custody and same initial setup: READY again with the debut wording and null performed fields; lastProjection.accepted_state.exercises[0].w remains null.

   Results are identical on integrated 7b1678a and pending B-NTC source0876043. The latter was imported read-only from astra-ntc-join-review HEAD3501d112; executed git diff0876043..3501d112 over engine/, m3/w6/local/, m3/w7-preview/today/ and m4/workout/ is empty. The probe writes only synthetic in-memory IndexedDB. This is not proof of a browser gesture, real kill, or progression. It disproves the pending Dad brief §5 assertion that merely exposing the debut path completes the first-load journey.

   Exact seam: source-projection.cjs createNullSelectionRegistrar clones the supplied initial state while attaching workout facts separately; workout-host.mjs:160–161 registers it without adopting an exercise baseline. today.cjs:80 tests e.w==null before numeric targets. Legacy writers.cjs:335–340 adopts the first numeric completed load, but native gym Finish writes a close operation; it does not call completeSession or replay its mutations. Do not call that legacy writer over the native log to make this work.

   Search of integrated lanes/, slice/ and m4/spec/ finds B-LOM recorded as a separate legacy/native order-mapping seam (STATUS:40,44), with a tooling skeleton, not a fresh baseline implementation brief. Existing LOAD-AND-SLOT-EXTENSION / PERFORMED-ENGINE-v1 own native load semantics and must be reused. No existing fresh baseline implementation assignment was found in that searched scope.

4. **Approved aesthetic and workout entry can be reused; Dad's six-screen flow is pending.**
   approved-2026-09-08/{REFINEMENT-A-HANDOFF,ADDITIONS-C-APPROVED-HANDOFF}.md authorize paper/ink/green, Instrument type, plan-first Today, editable performed fields and the optional recovery check-in. They explicitly exclude example numbers from production defaults. lane-c/dad/rebuild/lanes/c/dad-first-run/BRIEF.md says design/mock only, owner approval by viewing; it also leaves Q1–Q3 unresolved. Its “Dad is a beginner” premise is the brief's premise, not a new verified personal fact here.

   Reuse a short editable setup summary and the approved input/action treatment. Separate explicitly supplied personal/equipment facts from initial plan choices and identify the latter's source and acceptance. Name, chosen days, available equipment and optional priorities are distinct from exercises/sets/reps prescribed by a programme. Do not silently pick5 lb, make Dad invent sets/reps, or turn “I don't know” into an unusable coaching product. The constructor currently supports U/L only, demands exercises/sets/hi/inc/steps, rejects exercise.setup and starts every w:null (`m4/workout/athlete-state.cjs:59–113,130–173`). That is the current interface, not authorization to reduce the eventual product to a manual upper/lower planner. Nutrition targets have no setup members; check-in facts currently do not write sleep.nights. Preserve qualified unavailable/no-reading states (DECISIONS:110), never transplant demo targets or imply saved check-in answers changed the plan.

## Next immediately buildable slice: authoritative initial setup, no prescription decisions

PM assigns lane C a separate branch from the integrated B-NTC/C4 join. This plumbing slice does not require approving Dad's mock and must not wait for an onboarding design decision.

Exact ownership: `rebuild/m3/w6/local/local-client.mjs`, new `local/initial-setup.mjs`, `local/today-bindings.mjs`, new `rebuild/m3/w6/test/local-initial-setup.test.mjs`, affected local enrollment tests and one builder report. No engine, law, fixture, client-core or product UI edits. Coordinate today-bindings with the B-NTC owner before dispatch; retain its binding unchanged.

Required behavior:
- Add a versioned, immutable initial-setup record as a separate authoritative collection, e.g. collections.initialSetup.initial. Store explicit setup document, athlete identity, local creation stamp and constructor/profile identity; preserve the document's exact values. Construct/validate through existing createCleanInitState. Commit setup with first enrollment in the same encrypted generation, publish success only after commit. Do not store only a derived engine state, or name this record an accepted plan transaction.
- Expose validated setup and reconstructed initial engine basis on reopen. Derived can cache the reconstructed result but must be disposable. Missing/malformed/foreign setup on an existing generation returns a named setup-required/integrity result and never infers one from a fixture or stale cache. Existing no-setup preview enrollment stays explicitly demo/legacy-unconfigured; never re-enroll or overwrite it to enter owner mode. Preserve existing no-setup callers until the owner entry is wired.
- Add a clear setup-specific enrollment API; do not reinterpret arbitrary old enroll(cleanInit) payloads as authoritative. Refuse a second initial setup and retain existing records. Preserve this collection through all normal staged writes and generation recovery copying; give export/restore consumers its profile and preservation obligation. Hosted sync/port activation remain their own joins, not inferred from local durability.
- Proof: explicit setup roundtrip after release/reopen; derive-cache absence/malformed/basis-stale with same reconstructed basis; weigh-in + set + check-in preserve authority; failed initialize gives no setup success; concurrent enrollment/second submit cannot replace original; wrong athlete/profile fails by name; no fixture data; partial key/store/marker loss remains restore-required. Exercise restore/copy serialization with synthetic setup, not private import. One independent executing reviewer plus affected green CI; no full engine gate claim for this plumbing-only slice.

## Dependent slices, already concrete enough to dispatch after ownership assignment

A. **Owner calendar/entry join** — PM screens + lane C coordinated custody: today/{today-entry.mjs,today-model.cjs,gym-host.mjs,reading-host.mjs,checkin-host.mjs}, local/today-bindings.mjs, new local/calendar.mjs, affected Today/local tests. Default owner entry reads authoritative setup; missing setup routes to setup-required, never fixture. Explicit demo entry has a separate database/namespace and visible demo identity. One sampled real instant yields local date/time/offset and engine/client adapters; permission time stays actual now, effective exercise day remains explicit for prior-session close. Explicit test clock/day semantics remain supported and named; no silent second-provider override. Refresh on reopen/foreground/local-day boundary and before mutations, release stale handles, preserve half-entered drafts and original Start identity. Never silently retarget an old set/check-in to the new day: existing unfinished-session flow resolves it. Test23:59→00:01, reopen next day, DST offset change and UTC+13, interleaved reading/gym/check-in stamps, explicit clocks, stale handles and prior-day close. Operation local date, local time and offset must agree with the sampled instant (or deliberately selected historic effective date) while the lease checks real permission time. Use authoritative setup for all engine reads; remove default fixture-only recovery/nutrition claims.

B. **Native debut baseline adoption delta** — lane B owns the semantic rule/package. Primary candidate files: engine/{performed.cjs,today.cjs,progression.cjs}, m4/workout/{engine-runtime.cjs,engine-capture.cjs}, w6/host/engine-runtime-host.cjs only as exposure/composition requires; exact changed subset and seal determined in its brief. Reuse native performed original-slot/close/order/identity semantics and the existing load package. A source-owned working-load projection must carry the qualifying original operation identities and rebuild from facts, without changing immutable setup or replaying completeSession. Prove first completed uniform-load native lift leaves debut, subsequent targets use that recorded baseline, caches lost/reopen are identical, repeated use truly progresses, and amendment/undo/early close/skipped/added slot/unequal performed loads have explicit qualified outcomes. Obtain the existing accepted rule for selecting a working baseline when loads vary; if absent, that is a narrow product-rule decision, not permission to choose a mean or last set. Keep B-LOM separately scoped to legacy/native order. Existing gym editable fields already meet the input seam; add a real first-use browser journey rather than another preloaded-w fixture.

C. **Onboarding choice still needing product evidence** — select an existing qualified, source-attributed starter-plan supplier that can propose exercises/sets/reps after a few preferences, or identify the exact programme the owner approves for an initial offer. The inspected artifacts do not establish one. This cannot be solved by pretending every prescription is an athlete fact or making Dad design the whole programme. Prepare the summary/validation/draft reducer and authoritative-save adapter now; keep plan offers visibly proposed and require the applicable acceptance path. Coordinate pending Dad design revision and exercise.setup contract addition with its owner; do not call the six screens approved.

## Execution limits

Used existing Node/dependencies; no install, network, private reads, purchases, product changes, pushes, merges or deploys. Synthetic probes retained ignored under .tmp/{fresh-probe,cache-probe}.mjs. Current conformance gates not run: this report changes no executable product or laws. No acceptance, clinical qualification, complete restore drill, real-phone or S2 readiness verdict follows.
