# Owner entry/calendar candidate — builder report

2026-09-11. FILE authority: ASTRA-OWNER-ENTRY-NEXT.md in astra-pm-resume.
Branch codex/astra-owner-entry; base 9e779f682593c4b9a15850d384799b1e0c528ed2.
Candidate development only. No independent acceptance, integration, package closure, deployment or phone-readiness claim.

Changed product
- local/calendar.mjs: actual device date, ISO instant, local offset/hour and monotonic clock. Serializes operations with one sampled context; stale-day writes refuse LOCAL_CALENDAR_DAY_CHANGED. Explicit historical continuation permits only close; permission checks still use actual time.
- local/today-bindings.mjs: opts into that calendar without overriding an explicit provider. Reading/check-in saves and gym public-client preparation/mutation use the calendar. A reading cannot name a different date from its handle. Existing explicit day/fixed-clock callers keep their prior behavior. B-NTC provider/resolver/day-facts binding is preserved; only its engine clock argument gains actual mode.
- today/gym-host.mjs: forwards calendar/enroll to the same installation.
- today/today-entry.mjs: default boot opens actual owner storage with enroll:false and reads initialSetup().basisState. Missing setup is visibly setup-required, with no model/inputs/targets; damaged storage remains restore-required. Explicit ?mode=demo uses earned-today-explicit-demo / earned-today/demo. Injected day/model/basis/hosts retain explicit test composition. Concurrent page boots serialize. Foreground/15-second day checks and before-write guards prevent stale recording. Live editors retain DOM and entry-owned drafts; explicit Return to Today opens the new day, retained editors remain accessible, and an earlier Start is closed through the existing unfinished-workout path. Reading changes refresh the gym composition from current facts. Page teardown removes listeners/timer and releases the shared installation.
- today/today-model.cjs: owner basis comes only from stored authority; reading facts come from the client's projection. Owner weight trend/nutrition are explicitly unavailable for initial setup, with no fabricated baseline, score or targets. A future split cannot expose the engine fallback week. Explicit test/demo projections remain on their prior engine path.
- today/today-app.cjs and index.shell.html: actual athlete/date or explicit fictional-demo label; compact unavailable nutrition state using existing layout; mount cleanup and successful-weight refresh hook. Approved styles, fonts and templates were not changed.

Executed evidence (Windows; Node v24.19.0; existing dependencies; all data synthetic)
1. node --test --test-reporter=tap over local-owner-entry, local-initial-setup, local-today-journey and Today view/adapter/gym/checkin/design/ntc-h6-delta: 241 tests, 241 pass, 0 fail. Log .tmp/final-affected.tap. C4's two changed entry/wrapper source pins were updated after rereading the one-store composition; its behavior controls remain intact.
2. Added future-split control after that combined run; final local-owner-entry run: 15 tests, 15 pass. Includes default boot with no day/basis/calendar injection, authoritative label, fresh versus damaged/no-setup, actual three-path stamps, UTC+13, same-day DST offset change, 23:59 to 00:01, stale reading/set/check-in refusal, original Start identity and historical close, next-day reopen, demo isolation, concurrent boot, check-in/weight/workout draft retention. Historical close after actual lease expiry refuses state20 and leaves one Start/no close on disk; read-only preparation is allowed.
3. node rebuild/m3/w7-preview/today/build.mjs: A1 TODAY BUILD PASS; 3 assets, 93 inputs (13 engine, 12 client), 68 approved bound classes, 2 pinned fonts, no literal template figure or asset network reference.
4. W7_BROWSER_BIN pointing to existing Chrome; node rebuild/m3/w6/test/local-owner-browser.mjs: OWNER ENTRY BROWSER PASS. Real headless Chromium, fresh context, 390x844, America/New_York. Unchanged built app default boot shows setup-required; ignored probe bundle enrolls explicit synthetic setup through public API; genuine UI saves weight170.5, set40x10 and recovery Moderate. Reload retains all four operations, original active workout and recorded check-in. Demo roundtrip leaves owner weight intact. No page errors. Primary fits inside the approved phone viewport. Screenshot .tmp/owner-browser/owner-reopened.png inspected; no fixture nutrition values. Browser script writes only ignored local probe assets and serves only those assets on loopback.
5. git diff --check passed. No local-client, engine, client core, law, fixture, baseline projection or tooling source changed. Existing dependency junctions only; no install, private read/import/export, push, merge or deploy. Failed interim checks were real: dependency resolution, expected changed page pins, inaccurate browser selectors and a concurrent-boot clock race; the race was fixed by serialized boot and covered by test.

Explicit limits and next source-policy work
- Clean-init intentionally provides no energy/trend baseline. Applying the legacy Today readers directly was executed and threw on missing blackout.until (energy.cjs:370); applyRead requires blackout.until at writers.cjs:427, subtracts s.trend at431 and updates it at452. Empty blackout alone cannot supply the absent trend and would permit NaN. This branch does not catch arbitrary reader exceptions and call them insufficient observations; it explicitly declares the fresh-state projection unsupported. Stored weights stay intact and visible. Follow-up needs an approved first-reading/trend initialization contract, qualified read-window treatment, and the required nutrition inputs; adaptive weight/nutrition remains part of the product goal.
- No Dad six-screen implementation or starter-programme invention. Setup entry decisions remain pending. The separately accepted native baseline candidate is not in this base; join it under its own accepted scope.
- Unsaved drafts are transient within the live page, retained across navigation/day transition; they are not promised to survive reload or process termination. Saved facts do survive the browser reload exercised here.
- Independent review and affected Linux/Windows CI have not been run by this builder; only local Windows execution above. Full conformance/private-golden gates, physical iPhone save/restart/next-day checks and hosted readiness remain unexecuted. The browser probe is not a phone or crash-kill proof.
