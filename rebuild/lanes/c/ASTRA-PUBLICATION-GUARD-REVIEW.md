# Independent publication-guard review — ACCEPT

September 11, 2026. Source 9ee440c83fc579bdbdbaff09feefac4f0a924e9b; compared with accepted base 7b1678a4a8449053000d52a0a8e9e070f32a240e. Independent reviewer, isolated codex/astra-publication-guard-review. Assignment ASTRA-PUBLICATION-GUARD-REVIEW.md at PM f996aed; original brief and candidate report read. This verdict accepts the narrow local workflow change, with no required correction.

Only deploy.yml's preview condition and adjacent overview comment change, accompanied by the builder's report. Independently reconstructed the complete expected old-to-new workflow text and asserted byte equality with the candidate. The entire test/production region is byte-identical. All other preview properties and top-level configuration compare equal after parsing. The complete slice-host.yml is byte-identical (SHA256 42f43c3b40a31f929a57de197da3c4888bdf9105f7163ac58bbe88e67ec681fb). No trigger, permission, secret reference, publication step, production condition or test dependency changed. The separate slice workflow retains its existing exact branch/path triggers; the new comment does not promise that every rebuild branch receives a slice deployment.

Executed a new independent ignored .tmp/independent-publication-guard.cjs using retained Node24 and the existing yaml dependency. Both complete deploy workflows parse with zero errors/warnings. The probe evaluates the actual parsed expressions in their inspected restricted subset, with case-insensitive string operands and the implicit successful dependency requirement. This is a local evaluator, not actionlint, the GitHub expression implementation or a remote Actions run. No installed actionlint was found. Reviewed GitHub's official semantics: string comparisons and startsWith are case-insensitive, an if without a status function retains the default success requirement, and failed/skipped needs prevent dependent jobs. Sources: https://docs.github.com/en/actions/reference/workflows-and-actions/expressions and https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax (accessed September 11, 2026).

All 36 independently specified scenarios passed, checking both preview and production eligibility in each. Twenty-four cover push and workflow_dispatch after successful test for main, MAIN, rebuild/t2-client-core, codex/astra-b1, ReBuIlD/topic, CODEX/ASTRA-review, feature/new-ui, codex/general, rebuild, rebuildish/topic, codex/astra and codex/astral-topic (all refs/heads/). Main retains production only; both excluded prefixes refuse both publication jobs, including case variants; ordinary branches and prefix near-misses retain preview only. Three PR controls (actual refs/pull/71/merge plus main/ordinary refs) refuse both jobs. Nine controls cross main, ordinary feature and rebuild with failed, skipped or cancelled test: both jobs remain ineligible. These scenarios assume an otherwise triggered workflow with ordinary changed paths; the inherited ledger-only push exclusion remains unchanged.

Concrete before/after counterexample executed from the actual old/new expressions: github.ref=refs/heads/rebuild/t2-client-core, github.event_name=push, test=success previously yields preview=true and now yields preview=false. The corresponding successful ordinary-feature control remains true. The guard removes only the two requested prefix classes from the original preview eligibility; it neither suppresses the test job nor excludes all codex branches.

Candidate diff check passes. No source fix, dependency installation, engine/conformance campaign, private data read, remote publication, PR, push, merge, deployment or authority mutation was performed. Builder's 144 cells remain its local evidence; this independent 36-scenario check does not relabel either set as GitHub execution. Remote workflow outcomes, hosted application acceptance and final package qualification are outside this verdict.
