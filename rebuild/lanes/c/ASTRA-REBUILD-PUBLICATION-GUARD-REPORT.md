# Frozen preview publication guard — local candidate

September 11, 2026. Branch codex/astra-rebuild-publication-guard; exact base 7b1678a4a8449053000d52a0a8e9e070f32a240e. FILE ASTRA-REBUILD-PUBLICATION-GUARD.md at PM 0414201. Read NEXT/ROADMAP, the entire deploy.yml and the dedicated slice-host trigger. Candidate requires independent review; no publication or deployment was performed.

Changed only deploy.yml's preview job condition and its directly misleading overview comment. The condition preserves the original non-main/non-pull-request restrictions and additionally excludes refs/heads/rebuild/ and refs/heads/codex/astra- using startsWith. The comment points rebuilt-app hosting to slice-host.yml. All preview steps, site/token lookup, test dependency, production behavior and workflow triggers remain unchanged.

Executed ignored local probe .tmp/publication-guard.cjs using existing Node24.19.0 and existing yaml package from astra-owner-browser-fix/node_modules. Both original and corrected complete workflows parse with zero YAML errors/warnings. The probe parses the actual job expressions using their bounded grammar (github fields, quoted equality/inequality, conjunction, negation and startsWith), with Actions-style case-insensitive string comparison, and evaluates an independently specified expected matrix. This is local expression/source validation, not an Actions runner or GitHub CI result; no installed actionlint was available.

144 cells pass: 16 branch names × push/pull_request/workflow_dispatch × successful/failed/cancelled test dependency. Twelve successful push/dispatch cases change from original preview eligibility to refusal: rebuild/t2-client-core, rebuild/authority/docs, codex/astra-b1, codex/astra-authority and mixed-case versions of both prefixes. Main push/dispatch still reaches only production after test succeeds. PR merge refs never reach either deployment; failed/cancelled test dependency prevents both. The original test remains unconditional for triggered runs, with the existing ledger-only push exclusion untouched. Cases assume an ordinary nonignored changed path; no workflow-trigger expansion is claimed.

Ordinary feat/ui and codex/general keep preview eligibility, as do prefix near-misses rebuild, rebuildish/topic, codex/astra, codex/astral-topic, feat/rebuild/topic, codex/astraX and main-next. Manual dispatch uses the same branch condition; this does not assert that GitHub exposes a dispatch button for a workflow absent from its default branch.

Byte comparisons against exact base pass for the complete test/production job region and all dedicated slice-host.yml bytes. Parsed top-level configuration and every preview property other than if also compare equal. Dedicated slice-host SHA256: 42f43c3b40a31f929a57de197da3c4888bdf9105f7163ac58bbe88e67ec681fb. git diff --check passes. No other workflow, frozen source/script/data, dependency, secret, remote setting, source-policy artifact or unrelated file changed. No install, broad engine test, push, remote PR, merge, Netlify draft or deploy. One report accompanies the one workflow edit.
