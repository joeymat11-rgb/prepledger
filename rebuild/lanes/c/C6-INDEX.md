# C6 - VOICE ONBOARDING (index)

The brief of record is `rebuild/coach/BRIEF-C6-VOICE-ONBOARDING.md` (coach custody, not this folder).
Authority `DECISIONS:89`, `:133 (3)`, `:134`, `:135`, `:138`, `:139`. Split per `:139`: **Part A** text
rehearsal in the C5 prototype, buildable now, 1 day, lane C, no model and no network; **Part B** the relay,
lane D (Astra); **Part C** the phone voice session, brief and bar only, build after A4b under
one-Today-build-at-a-time. Section 4's token hand-off contract is a PROPOSAL: neither lane builds against it
until lane D answers by REQUESTS. Part A's claim and its acceptance: a scripted transcript and the
equivalent taps produce the SAME setup op bytes, on a real local-era world, through the same validated
`setup-commands.mjs` the screens use.
