# GYM-SETTINGS-WRITER-SEAL specification
Author: Astra (Codex), a named specification assignment under DECISIONS:412, :569 point 3 and :613; high effort; PAPER ONLY, no product or test byte changed
READ source baseline / prior measured head: e08bc11cec423e5d5878110e78d16ccc5f3f8a29; final paper folds the complete local REVIEW-R2 file and PM DECISIONS:628 into the accepted specification.

Status: FINAL PAPER after the PM's rulings of DECISIONS:628; the build has not started.

## Invariant and scope
The released settings view supplies raw answers and an opaque editor token to a sealed writer. Drawing cannot mutate cached reads or acquire the settings host, except the exactly pinned public first.settings.lane passthrough. A pending same-writer operation cannot be entered again. Existing payloads, notes and sentences remain unchanged; stale/repeated submissions are silent, and late completion cannot replace a newer editor.

## Evidence convention
READ means inspected source at the measured head unless another ref is named. MEASURED (author, prior round) means executed in the original specification assignment; MEASURED (reviewer) attributes a reproduced or new measurement to GYM-SETTINGS-WRITER-SEAL-SPEC-REVIEW-R1.md unless R2 is named. Unqualified existing MEASURED claims retain their author/prior-round scope. READ reviewer section references name that review. MEASURED (Round 2) claims are prior-author evidence, reproduced by REVIEW-R2 with retained probes named below; no new runtime probe is claimed in this final paper pass. Proposed requirements and row names are prescriptions, not passing results. Numbers in section labels, finding IDs, paths and source citations are READ identifiers; proposed counts are READ targets, never passing evidence. Protected data and real athlete inputs are excluded.

## A. Current inputs, outputs and authority
Path abbreviations: T = rebuild/m3/w7-preview/today; W = rebuild/m3/w6; F = rebuild/lanes/c/today-split/writer-fence.test.mjs; Split = rebuild/lanes/c/TODAY-SPLIT-SPEC.md; S8 = rebuild/m4/spec/acceptance-s8-real-shape.json; Producer = rebuild/coach/machine-settings-commands.cjs. All line references below are READ physical lines unless marked MEASURED.
Authority READ: origin/rebuild/t2-client-core DECISIONS:550, :562, :574, :584 and :610 were read separately. The existing remote-tracking ref was used, without fetching. Start from origin/rebuild/c-after-the-cut-tickets:rebuild/lanes/c/AFTER-THE-CUT-TICKETS.md, T1 and its paired contradictions. This ticket follows the pure cut and precedes S10; it is not another pure-cut claim (:574).

| READ path | Input -> decision -> output |
|---|---|
| T/gym-app.mjs:232-250 | Open seeds draftFrom(latest); Save calls released recordSettings(map, view, paintedDraft), then puts its promise in hooks.saving. |
| T/gym-app.mjs:257-288 | owns/draft identity/lift checks; machineFromDraft; empty refusal; acceptable; disable Save; facade.lane().save(machine); enable; failure text or clear matching draft; drop/read that lift. These decide WHETHER and WHAT is stored. |
| T/machine-settings-view.mjs:39-57 | machineFromDraft preserves row order, string-coerces and trims answers, removes wholly blank rows, omits empty cues; acceptable calls the producer's machineOf on a JSON copy. It does not pass machineOf's return value to save. |
| T/machine-settings-host.mjs:72-103 | Producer supplied to hostBindings; save sends client.execute('workout', {action: ACTION, input: {machine}}); catches exceptions into ok:false; ok derives from acknowledged. |
| W/public-client.mjs:597-600; W/bridge.mjs:8-49,:61-63 | Queued execution, cloned input, load/stage, successful-result/batch checks, repository.commit; acknowledgement is released after commit. |
| W/repository.mjs:300-307,:239-259 | Clone generation, seal, publish, revision/token and synchronous validation, put previous/active (and optional adoption), then transaction completion. A trace, not an executed durable transaction here. |

READ read path: host.latest (T/machine-settings-host.mjs:85-89) loads the generation, uses machineSettingsIn/latestFor; Producer:133-151 copies each machine but freezes only the wrapper. Lane:51-64 stores {state:'known', latest} or {state:'failed', latest:null}; entryFor at :93 returns that very object. Card:217-241 obtains host readiness and entry/latest, renders it, and seeds an editable draft. View:28-33 copies pairs into draft rows; :142-161 mutates those rows/cues on input, add and remove.
READ every exposed settings-data/capability holding site: lane:34 owns injected host; :46 cache; :47 in-flight read set; :55-63 read promise; :77 opening promise resolves to host; :88 lane thunk gives actual host; :89-91 pending/ready/reading promises; :93 live entry; :94 scalar state. hooks.open (:97) also returns the host-resolving opening promise; dropRead (:99) lets drawing evict cache; saving (:100) lets it replace the pending slot. Card:223-225 holds entry/latest, :233/:239 draft aliases, :139 WeakMap messages, :240 passes mutable draft to renderEditor. first.settings (:540-550) exposes pending, ready, lane, read, stateFor, owns, in that order. Freezing wrappers at lane:86-101 does not detach their results.
READ counterexample: a later formatter changes entry.latest.machine.settings[0].value; the next editor seeds that changed value and Save accepts it. No claim that today's renderer performs this mutation. A replacement Save node can bypass the released disabled property during pending work; the current lane has no pending admission test.

| READ gym action and caller | Durable put reachable? | Ticket disposition |
|---|---|---|
| recordSettings, card:249/:276 | Yes, host/public-client/bridge/repository path above. | Owned here. |
| logSet, card:386-401; model:502-516 | Yes, prepareWorkoutContinuation -> executeResumedWorkout (:510) -> W/public-client.mjs:387 -> bridge. | Sealed dispatch/pending owned here; model admission unchanged. |
| finish, card:413-420,:439,:467-470; model:538-546 | Yes, continuation -> executeResumedWorkout(close) -> same tail. | Owned here. |
| undo, card:473-480; model:526-533 | Yes, prepareWorkoutEdit -> commitWorkoutEdit -> W/public-client.mjs:365 -> same tail; append removal, no deletion. | Owned here. |
| forget, card:469; model:536 | No: function body only assigns saved = null. It is transient advance, not durable. | Retain named serialization subject; do not count as a durable writer. |
| start, card:516-520; model:472-493 | Yes, startPreparedWorkout -> W/public-client.mjs:458 -> same tail, from paint. | GYM-START-IN-PAINT; preserve existing call and exception here. |
| closeUnfinished, model:557-575 | Yes, prior-day host continuation -> executeResumedWorkout(close, early); finally closes that host. | Recovery/Today handoff ticket; not a gym-card control here. |

READ Split:687-695 names submitWeighIn, recordIntake, recordSettings, logSet, finish, forget, undo. This is not seven gym writers. The first two belong to TODAY-MODEL-HANDOFF and TODAY-OUTCOME-TYPE respectively; recordSleep already has its separate fence and is not substituted into that list. READ today-lanes.cjs:63-75 at e08bc11c uses a synchronous ambient gesture counter, which ends before awaited continuations; MEASURED (reviewer R2, section 6) it moves unchanged to :70-82 at c38ed5fb. Pending serialization and gesture provenance are separate invariants.

MEASURED synthetic probes at this head: entryFor returned the same unfrozen object; changing its state and nested value changed subsequent facade reads, with zero save calls. The exact forget body cleared synthetic saved state and returned undefined; calling the actual createGymModel(...).forget() with a trap client made zero client calls. This establishes no put on forget itself, not that its caller's later paint can never start a workout. READ card:469 -> paint:517 retains that separate Start exception.
MEASURED the shipped today-lanes guard fragment: a repaint nested in a shimmed click executed a synthetic writer once; an awaited continuation was refused. This was a direct guard probe, not a DOM or durable-write run. MEASURED helper probes: blank -> null/refused; half-pair and trimmed duplicate name -> refused; cues-only, ordered nonblank rows and case-distinct names -> accepted. No real athlete input or store was used.

## B. Design contract (UNMEASURED until built and driven)
Use the existing gym-settings-lane as the sealed controller. No new validator, producer, store, clock or persist path. All new state, dispatch, lifecycle, read-detachment and result code below is NEW HAND-WRITTEN WRITER LOGIC (N). Exact helper relocation alone is PURE MOVE (M). Named import/export/call and copy-mapper transformations are DECLARED STATEMENT REWRITES (R). A rewritten writer body is N even when its old expression is retained. These classifications apply to every implementation hunk; a builder must list any unclassified hunk for PM review.

### Entry points and custody
The following signatures are normative design, not a report of existing exports. Internal commands are not returned from the factory. DOM is admitted only to infrastructure binders, never to a command or its result. Editor tokens are frozen empty objects compared by identity against sealed records; callers cannot construct authority by guessing a number or copying fields.

| Sealed entry point | Exact input/output and purpose | Class |
|---|---|---|
| createGymSettingsLane(doc, phone, model, settings, painter) | Returns Object.freeze({facade, hooks, api}); all tables frozen. painter remains Object.freeze({repaint: () => paint()}). phone is used only to bind/verify controls. | N; call-site R |
| hooks.readView() | Promise<view \| null>. Calls existing model.read, retains its private context, returns a detached deep-frozen view. Newest requested read wins; late reads/left mount return null. No new Start call. | N |
| hooks.settingsEditOpened(), hooks.settingsEditClosed(editorToken) | Open returns {editorToken, latest} \| null only for the sealed current active lift with known cache and available host; replaces the editor and returns detached latest. Close returns void and invalidates only the matching identity; stale Cancel cannot close its replacement. | N |
| hooks.leave() | void. Synchronously retires mount, editor and control bindings before navigation; pending operations still settle. | N |
| hooks.bindSettingsSave(editorToken, readRaw, onOutcome) | () => void disposer. Selects the unique intended Save control inside phone after show has installed the root; readRaw(): {rows:Array<{name:string,value:string}>, cues:string}. Sealed listener captures raw answers and editorToken and calls the private command. This is the normal UI shape, not a new runtime type rejection; preserve E's supplemental coercion inputs. No DOM/event is returned to that command. | N |
| hooks.bindGymAction(action, readRaw, onOutcome) | () => void disposer. action is 'logSet'/'finish'/'forget'/'undo'; sealed context must allow it. logSet readRaw(): {load:string,reps:string,effort:object\|null}; others readRaw(): undefined. IDs come from held model view; select inside phone after show, never from a drawing-supplied root/context. | N |
| hooks.open(), hooks.startRead(liftId) | null or Promise<boolean>; Promise<void> respectively. Preserve optional nonblocking opening/reading; never resolve to a host. Remove public dropRead and saving. | N |
| hooks.listen(element, type, listener), hooks.unlisten(element, type, listener), hooks.paint(draw) | Built under G-R3/G-R5: paint returns the synchronous draw result; listener methods return void. Matching wrappers for ordinary card listeners use the refusal-depth scope below, never write permission. Preserve event arguments/removal identity. | R calls, N infrastructure |
| facade.available(), facade.entryFor(liftId), facade.stateFor(liftId), facade.hasRead(liftId), facade.settingsBusy() | boolean; detached frozen entry or null; existing scalar state; boolean; boolean. No host, mutable cache, operation result or host-resolving promise. | N |
| api.pending(), api.ready(), api.lane(), api.read(), api.stateFor(liftId) | Last admitted settings-operation promise or null; sanitized opening promise or null; actual host or null; read promise or null; scalar state. Only api.lane is writer-capable. | N, public mapping R |

READ interface accounting: the table has 10 groups, grouping editor open/close and refusal infrastructure; this is not a claim of 10 callable functions. The cut removes present, cardToken, permit and facade.effortChoices. hooks.paint is only the synchronous adapter required to scope the review's paint rule; it creates no write authority.

Binder onOutcome signatures are (outcome: SettingsOutcome) => void | Promise<void> and (outcome: GymOutcome) => void | Promise<void>; a returned promise is awaited for cleanup only and its resolved value never becomes a public result. A settings binder registers against the existing editor token; it cannot mint one.

Internal exact command signatures: recordSettings(rawAnswers, editorToken): Promise<SettingsOutcome>; logSet(rawAnswers, context), finish(context), undo(context), forget(context): Promise<GymOutcome>. Only the sealed binders call them. Context is a private captured model view, never a token or input returned by drawing. No permit, cardToken or present API exists; editorToken is the only identity token. The synchronous pending reservation is the single-use command admission, with replay also checked by event identity.
READ source cost: card:511's model.read becomes hooks.readView with null handling. Renderers carry no new context token. Keep model.effortChoices at card:353 unchanged: MEASURED (reviewer, section 3.1) repeated calls return distinct arrays and distinct choice objects. Pin the remaining model sites: constructor handoff, effortChoices and existing Start (:517). No claim that the model is inaccessible while these remain released.

### Editor and pending state transitions
- Mint only when settingsEditOpened admits the sealed current active lift and known cache. Seed the view's mutable draft with existing draftFrom on the returned latest. No caller-supplied machine, exercise_id, admission boolean or normalized draft enters recordSettings.
- Save accepts only this mount's current editorToken, its captured lift, live Save binding. The ordered admission sequence below owns capture and reservation; recheck ownership immediately before host.save. Apply the moved machineFromDraft(raw, heldLift) then acceptable(machine) in the original order. Do not replace machine with acceptable's canonicalization result, sort rows, parse values or add keys.
- Invalidate on matching Cancel, replacement Open, leave, a completed readView showing another lift or nonactive phase, and matching successful Save. A same-lift repaint, row add/remove, refused save or read refresh retains the editor token and its existing error. readView has its own monotonically ordered request ownership; a late read cannot switch the active lift back.
- Save completion always accounts for its original captured lift. On success evict/re-read that lift even if its editor was replaced or left; re-read may update private cache but may paint only a live mount. Only an exactly matching editor may be cleared or receive a refusal. On matching success, capture its current binding, check ownership, retire the editor and deliver saved synchronously without an intervening await; the released mapper compares its held token before clearing. Before any other released onOutcome, recheck ownership; never reuse a captured DOM map to set a later error. Retain onOutcome for the latest binding of the matching editor so a row-add repaint receives its existing refusal.
- Separate settingsPending from workout pending. Preserve optional settings independence: a slow settings read/save does not block Log Set. The owned subjects have named flags recordSettingsBusy/logSetBusy/finishBusy/forgetBusy/undoBusy, reserved by the sequence below and cleared in finally. Additionally retain the old shared workout exclusion for logSet/finish/forget/undo; do not accidentally allow finish while logSet is pending. Settings is outside that shared exclusion.
- Hold recordSettingsBusy through host result and required cache refresh. api.pending returns the last admitted operation, published by the sequence below; an ignored repeat cannot replace it with an already-settled promise. A failure releases the slot and permits retry. The saved result must not be downgraded to a failed write when a subsequent read fails.
- forget takes the shared workout slot through its resulting paint settlement (onOutcome may return that Promise), so repeated advance cannot slip through while its screen is replacing. Release at that paint settlement, without extending the slot across an optional settings read or an extra scheduled turn. Its model call is still just saved = null. Record its result as transient; no durable count is added. Always release flags if reading, writing or painting rejects; notification failure cannot retry a completed command.

### Gesture admission: RULED depth-zero plus static zero-dispatch
N: binders select inside their own phone, after show installs the root. The fixed mapping is settings-save; log; primary on complete -> finish; primary on saved -> forget when the held view.next exists, otherwise finish; undo on saved -> undo. Require unique matching control and sealed phase agreement. Bindings are keyed by control; rebinding the same action revokes/removes its previous wrapper. Capture sealed model context when binding; invalidate mismatching action bindings when a current read changes phase/start/slot/lift, and recheck that context at dispatch. Check isConnected and phone.contains(control) at dispatch. A detached or replaced root confers no authority. leave revokes all bindings; a disposer revokes only its exact binding, never a replacement. A child-icon click is valid through the intended currentTarget.
N: use a private refusal-depth scope, not the shipped ambient permission counter. Every sealed listener wrapper, synchronous raw/outcome callback and wrapped synchronous drawing return runs at nonzero depth; a writer wrapper admits only if its entry depth was zero. Finally restores depth when that synchronous call returns or throws; never hold depth while awaiting a promise. No depth value, event, command or permission crosses into drawing. Paint wrapping is refusal-only infrastructure, not a writer entry point; its concrete adapter is specified below.
N ordered admission sequence (the only ordering definition; step labels are READ specification order):
1. Check entry depth is zero, live mount/binding, action/held context and editor identity where applicable, connected containment, enabled control, event.type === 'click', event.currentTarget === bound control, and event absent from the private consumed-event WeakSet. Missing settings availability is also silent. Any failure is ignored with the matching reason and no released outcome callback.
2. Check the settings slot or shared workout exclusion. If busy, return ignored/in-flight without replacing api.pending. Otherwise mark the event consumed, reserve the relevant flags, and publish the admitted settings promise before calling any released code.
3. Under refusal depth, invoke readRaw once and copy its raw values into a fresh plain structure; do not trim, validate, coerce or change row order here. Recheck editor/binding/context ownership after that callback. Use existing helper coercion at the private command, not a new boundary type-check.
4. Invoke the private command using that reservation (no second self-busy check). Settings uses machineFromDraft then acceptable, unchanged. Model arguments use the sealed captured context. All asynchronous write/read work stays inside the admitted operation.
5. On settlement check current ownership, deliver the detached outcome through a synchronous refusal-depth callback, and await its returned promise only for cleanup. Preserve rejection. Release flags in finally at their specified settlement boundary; never retry a completed command because drawing rejected.
N paint adapter: hooks.paint(draw) executes a synchronous drawing call under private refusal depth and returns its value; it grants no admission and holds no slot. R wraps only paint's synchronous drawing returns, READ at e08bc11c: :513 refusalScreen(view, view), :514-515 stub(...), :518 refusalScreen(view, started), :528 renderSaved(view), :531 renderComplete(view), :532 renderActive(view). Each becomes return hooks.paint(() => ORIGINAL_CALL), preserving the complete original call and branch. Keep Object.freeze({repaint: () => paint()}) exactly; do not restructure paint or wrap its awaits/recursive return.
READ paint spans at e08bc11c: :507-511 before/through model.read's await; :512-517 resumes after that read and reaches model.start's await; :518-520 resumes after model.start. The middle span contains an await and cannot be one synchronous draw closure; full segment scoping would split control flow, so G-R5 defers its pricing to GYM-START-IN-PAINT's spec, which restructures paint anyway. The READ six-call-site design gives up runtime refusal of a bare click between branch tests or after the second await: only the named static plants close those positions.
MEASURED (Round 2; reproduced by reviewer R2, gss-r2-depth.mjs): an ordinary scoped listener starts paint and its resumed segment clicks the intended control; unwrapped resumed scope admits 1, full segment scoping admits 0, final depth is 0. MEASURED (reviewer R2, same retained probe, renderSites): six-return wrapping refuses the render-helper plant (render=0) but admits the bare positions (resumed=1, afterStart=1). These are synthetic scope outputs, not DOM/host/write proofs and not evidence that full segment scoping is being built.
MEASURED (reviewer, section 4.3): nested paint and an ordinary sealed listener clicking Save produce zero writes; intended Save and its child-icon click each produce a write; replay of that event and retained disposed wrapper are refused. A timer outside all seal code clicking the enabled live Save IS admitted at runtime. Close that deliberate dispatch spelling statically: GSS-GESTURE-CONTROL also asserts zero .click( and dispatchEvent spellings in released gym/Today sources, with separate inserted-line RED plants. This is a tripwire, not an arbitrary-JavaScript security claim; delayed released callbacks have no private command to call.
MEASURED (Round 2; reproduced by reviewer R2, sections 3/11, at e08bc11c and c38ed5fb; reproduced in this pass at e08bc11c by retained dispatch-scan.mjs in H's OS-temp folder) of the same T paths in D: each has 0 matches for /\.click\s*\(/ and 0 dispatchEvent spellings. The probe reads only the explicitly named public source blobs; no whole-tree search was run.
MEASURED (reviewer, section 4.2): jsdom click/dispatch produce isTrusted false; property redefinition throws; subclass and Proxy attempts still deliver false; Enter/Space synthesize zero clicks. The browser driver does not reach gym and reports NOT RUN with a successful exit if its browser variable is unset. RULED G-R3: keep jsdom positives; no event.isTrusted admission, trust override or native-browser gate in this ticket. Browser/assistive activation remains unmeasured; H names the runtime residue.
READ affected existing drives: UI test:96 and :985; gym.test:499,:505,:506. MEASURED (reviewer, section 4.1): the lane-D2 annex has 13 cells and 2 support modules (15 files) driving settings-save via p.click and settings.pending, with no CI home; LAUNCH-ADOPTION-PROBES.mjs also drives settings controls outside the bar. Preserve those drive semantics; D orders the post-build annex run and report. This paper claims neither a native gym gate nor executed annex evidence.
READ limitation: released markup and raw callbacks still supply the athlete's answers. Runtime checks do not prove arbitrary hostile markup/callbacks truthful. Exact action/holder pins, counterexamples and independent review remain required. View row-edit listeners remain ordinary and unchanged; the static dispatch prohibition applies there too.

### Detached data, freshness and typed paint
N: clone host latest on cache ingress with structuredClone, recursively freeze every plain object/array, then clone-and-deep-freeze on each entryFor/open-editor return. Do not freeze a host-owned object in place. The entry wrapper, machine, settings array and each pair are all detached; unknown and failed still differ from known-null. Keep private read deduplication. Late answers update only their lift's entry; suppress repaint after leave. Public read/ready promises must resolve to inert values, not the cache or host. Raw drafts remain mutable, independently owned by drawing.
READ freshness cost: lane:53 already skips a cached/in-flight lift. Keep that policy and the post-save invalidation; no new network poll or mandatory reread on Open. A frozen snapshot stays at its read version until the view asks again; new entryFor gets the latest cached version. Cloning costs memory/work proportional to returned data, unbenchmarked here. No time-based freshness guarantee is introduced.

| SettingsOutcome (deep frozen) | View behavior, preserving READ card:260-288 and :71-81 |
|---|---|
| {kind:'ignored', reason:'stale'\|'gesture'\|'unavailable'\|'in-flight'} | No text, clear or paint; pending operation stays authoritative. |
| {kind:'invalid', reason:'empty'\|'shape', editorToken} | SETTINGS_NOTHING or SETTINGS_REFUSED through plainOrDrop; retain typed draft and error across repaint. |
| {kind:'not-saved', editorToken} | SETTINGS_NOT_SAVED, without appending host code/copy; retain answers. Real host already catches throws at host:98-100. |
| {kind:'saved', editorToken, liftId} | Clear only matching editor; no new success sentence; refreshed block comes from host.latest, not submitted data. |

N: outcomes carry no promise, node, host or arbitrary message composer. The sealed api.pending() holds the admitted operation promise; it is a separate object from the resolved outcome and never accepts a promise supplied by drawing. Tokens preserve identity when freezing results (do not clone the token). For stale completions return ignored for UI while still refreshing committed data. GymOutcome is {kind:'ignored',reason:'stale'|'gesture'|'in-flight'} or {kind:'gym-result',action,result}; result is the unchanged detached model response, with undefined represented only for transient forget. Retain existing refusalText/refusalScreen and success callback ordering (card:393-400,:417-419,:478-480); no new gym sentence. Unexpected programmer/injected-host rejection stays a rejection with finally cleanup, not invented 'saved' or an unproven no-write claim.
R: settings error-text mapping stays in gym-app; no SETTINGS_* copy injection into the lane. Pending presentation disables each newly painted Save using sealed busy data (facade.settingsBusy(): boolean). After settlement, repaint/enable only the current matching editor. A refusal repaint must not clear the error; the view retains errors keyed by editorToken instead of mutable draft identity.
N: api is a capability holder separate from facade. Exactly map first.settings's pending/ready/lane/read/stateFor to api and keep owns in its existing position, tied to both released show ownership and hooks.leave. Preserve key order, frozen property descriptors, host identity from lane(), and the awaitable first-paint promise. G-R4 rules that the opening promise resolves to boolean availability; this is an explicit API-value change, not a pure move. No drawing/admission site may use api, first.settings or its aliases.

### Helper custody: RULED relocation, changed-then-pinned
M: relocate the exact machineFromDraft and acceptable bodies from view:39-57, including their adjacent comments, into gym-settings-lane. R: import producer machineOf in the lane; keep SETTINGS_MAX/MAX_ROWS in the view; re-export/re-import helpers there so named/default external exports remain equal. N: use those helpers only inside sealed recordSettings. Keep renderBlock/renderEditor/draftFrom unchanged.
RULED G-R1: DECISIONS:628 amends Split:1335's pinned-unchanged disposition to changed-by-this-ticket-then-pinned; S10 pins the resulting view bytes. GSS-CUSTODY is unconditional and independently extracts the MEASURED 23-line helper/comment span to prove byte identity. This authorization is separate from :584's pure-cut substitution families.
The pinned-view import alternative was considered and refused because it retains writer helpers in a look-editable module and saves no fence work.

## C. File-by-file custody, hunks and accounting
READ custody below is S8 key membership, not a hash revalidation or S10 acceptance. 'Born' means the split introduced the path; it is sealed-to-be, not proof that S10 has landed. The builder owns only the product/test paths below under DECISIONS:628; this paper changes none of them. No seal manifest, decision/status ledger, historical split generator/witness or receipt is edited by this ticket. S10 integration must explicitly carry the changed product and execution pins.

| File | READ custody/evidence | Proposed hunks and classification |
|---|---|---|
| T/gym-app.mjs | S8 product :696; intended released by Split B.9/S10. | :117-139 constructor/editor state; :108 leave; :171 show; :217 readiness; :232-288 Open/Cancel/Save/result mapping; :353 effort choices unchanged; :386-480 action bindings; :511 read context; :540-550 API. N ownership/typed outcomes/raw callbacks; R exact callback retargeting/copy mapping and ordinary listener calls to hooks.listen/unlisten. READ at e08bc11c, six DECLARED STATEMENT REWRITES (R) add hooks.paint(() => ORIGINAL_CALL): PAINT-BLOCKED :513 refusalScreen; PAINT-FINISHED :514-515 stub; PAINT-START-REFUSED :518 refusalScreen; PAINT-SAVED :528 renderSaved; PAINT-COMPLETE :531 renderComplete; PAINT-ACTIVE :532 renderActive. Delete released writer body, preserve behavior. No paint control-flow restructure; :517 Start call/branch policy and :520 recursive return unchanged. |
| T/gym-settings-lane.mjs | Born by split; F:178 at e08bc11c sealed-to-be; no S8 key. | :1-30 truthful banner replacing obsolete pure-move/token-future claims; :32 constructor; :34-48 private state; :51-79 detached/sanitized reads; :82-102 new interfaces; helpers, command, tokens, control dispatch and flags. M helpers only; R imports; N all lifecycle/writer additions and updated custody banner. |
| T/machine-settings-view.mjs | Free in S8 product/execution key sets (:200,:1600); READ G-R1 amends Split:1335 to changed-by-this-ticket-then-pinned. | :19-21 producer destructure; :36-58 move helpers; :165 preserve default surface via imported names. M exact helper span; R producer/import/export wiring and truthful comments. S10 pins resulting bytes; independent GSS-CUSTODY is required. |
| T/test/machine-settings-ui.test.mjs | S8 product :756 AND executionPins :1611. | :81-120 retain jsdom control harness and its existing dispatch semantics; :390-413 producer-owner assertions move to lane; :455-581 parity/late editor; :599-630 independent logging; :765-784 exact import boundary; :901 editor seed. Extend with D/E rows. N test logic, R owner/path assertion changes; preserve every original behavior case. |
| T/test/gym.test.mjs | S8 product :751 AND executionPins :1610. | Rendering journey at :472, existing log/undo/finish cases :153,:205,:226; retain the .click drives at :499/:505/:506 and add controlled pending/dispatch/forget fixtures; no replacement fake DOM or new browser gate. N test logic. Model-only rows stay. |
| F | Free in S8; split-born test cell, not a sealed S8 product/execution pin. | D's exact row replacements plus table/holder/import updates; N assertions/mutants, R changed source anchors. Retain lexer and all unrelated controls. Do not redesign its scanner in this ticket. |

READ untouched dependencies: T/machine-settings-host.mjs is S8 product :706; T/gym-model.mjs is absent from S8 keys and S10 pinned-unchanged by Split:2047; producer/bridge/public-client/repository are unchanged. T/build.mjs is S8 product :686; no new module input is needed because the lane/view/producer already ship. REQUIRED_INPUTS membership is not a license to edit build bytes; if a build assertion disagrees, report the measured named exception for PM integration.
MEASURED scratch-only partial diff under C:/Users/joeym/AppData/Local/Temp/astra-gym-settings-spec-e08bc11c: exact helper/comment span is 23 LF physical lines; view skeleton is +3/-24, lane skeleton +26/-0. Removing released recordSettings and its adjacent explanatory comment/blank span is 37 lines, card skeleton +0/-37. The skeleton numbers are skeleton-dependent author measurements; MEASURED (reviewer, section 2) independently reproduces only the helper span and card-removal span. These are partial diff results, not full implementation estimates: the card removal alone is deliberately nonfunctional. No token/binder/result implementation was authored or line-priced. Do not repeat T1's old estimated full line count as a measurement.
READ R3 binding method (Split:300-345; review-R3:108,:167,:309,:378,:414): exact moves need independent source hashes and unambiguous anchors; new logic cannot be proved by regenerating its own expected text. D-GSS-LINES: authored lines are unpriced by either side; the builder reports PURE MOVE (M), DECLARED STATEMENT REWRITE (R) and NEW WRITER LOGIC (N) by physical hunk, old/new path, source anchor and actual line counts, and runs the focused dynamic rows. Independent Claude review attacks every N hunk; PM reads every authored line. No new statement rewrite claims :584's old W-family preapproval.

## D. Rows, red first (all proposed/unexecuted here)
READ PM order, DECISIONS:628: the build starts from the split's ACCEPTED head, not before. Its first act is to re-measure every anchor there and report any disagreement with this paper before writing a byte. This baseline paper does not nominate a moving split tip as accepted.
READ PM order D-GSS-D2-ANNEX: after the build, run and report the lane D2 settings-save annex under rebuild/lanes/d2/reviews (MEASURED reviewer: 15 files, 13 cells plus 2 support modules), exercising the support modules through their cells. They have no CI home; a silent stop proves nothing. Report each cell's completed assertions/result and support coverage, not exit code alone, using synthetic inputs and the same serial Node/environment discipline as the focused commands below.
Each named row below has a positive control and a concrete defect plant. Implement plants in disposable scratch copies, not the tree, and assert each plant actually changes the named bytes. Run the existing focused cells serially using the commands below, not the whole Today step. Compare real operation/outbox bytes after synthetic-store reopen where 'operation' is asserted; a save-call spy alone proves no durability. Keep separate fast command/binder probes for admission failures. A row passing only with its own rewritten expectation is not evidence.
READ G-R2 row classification: FOUR DURABLE PLUS ONE TRANSIENT = recordSettings, logSet, finish, undo plus transient forget; this ticket owns their pending/dispatch bundle. Start remains the separate paint exception. No word leaves the PUT list.

| Row name | Invariant/input/output | Planted defect that must make it red |
|---|---|---|
| GSS-RAW-PARITY (includes GSS-RAW-SNAPSHOT) | E's valid/invalid inputs use the old helper/producer path, IDs from sealed context. Mutate source rows/cues after dispatch with host deferred: operation still uses captured answers. | Trim/drop/reorder in drawing, substitute drawing exercise_id, bypass acceptable, or retain rows by reference across await; plant separately. |
| GSS-TOKEN-IDENTITY | Forged, copied, other-mount and old editor tokens never call host; exact live editorToken succeeds. | Compare public lift/sequence instead of identity; omit identity check. |
| GSS-EDITOR-LIFETIME | Open/replace/Cancel/leave/lift change/nonactive phase invalidate; same-lift row-add repaint and refusal retain token/error. | Skip each invalidator separately or invalidate every paint. |
| GSS-LATE-COMPLETION | Deferred old success/refusal cannot clear, error, focus or navigate replacement/left editor; success refreshes original lift. | Remove post-await ownership check or refresh current rather than captured lift. |
| GSS-CACHE-DETACHED | Mutate returned wrapper/array/pair/cues and original host object; new read/seed unchanged; each returned data level frozen/detached. | Return live entry, shallow-freeze only, or freeze original host object. |
| GSS-CACHE-FRESHNESS | Reading/failed/known-null distinct; latest changes only after refresh; other lift unaffected; stale readView cannot reactivate old context. | Treat failure as absence, omit save eviction or let earlier read win. |
| GSS-NO-HOST-LEAK (includes GSS-API-PARITY) | Inspect facade, hooks and resolutions: no host except exactly pinned api.lane identity; first stays awaitable, settings keys/order/descriptors unchanged, pending tracks admitted op, opening promise resolves to boolean availability. | Return host from open/ready or entry.host, add facade.lane, rename/reorder public keys, lose pending liveness; plant separately. |
| GSS-PENDING-SETTINGS | Deferred host then enabled replacement Save: repeat makes no extra operation; original pending retained; refusal/throw releases flags and retry succeeds. | Release before await/refresh, DOM-disabled only, or overwrite pending on ignore. |
| GSS-PENDING-GYM | Parameterize logSet/finish/undo/forget; same/cross-action repeats quiet while pending; success/refusal/throw releases flags. Forget releases at paint settlement, with unrelated settings read still unresolved. | Lose shared workout exclusion, clear early, omit finally, or extend forget through optional settings read. |
| GSS-INDEPENDENT-LANES | Settings read/save deferred; Log Set records unchanged set without synthesized settings operation. | Share exclusions or await settings before render. |
| GSS-FORGET-NO-PUT | Actual model clears saved state; no client execute/prepare/repository put or op/outbox delta; bound advance paints once. | Add execute/commit call or count transient result as durable. |
| GSS-GESTURE-CONTROL | RULED depth-zero: jsdom Save/child-icon positives; other control, disabled, detached and replay negatives. Static half asserts zero .click( and dispatchEvent spellings in T/gym-app.mjs, T/machine-settings-view.mjs, T/gym-settings-lane.mjs, T/today-app.cjs and T/today-lanes.cjs. Demonstrate timer click outside depth is runtime-admitted but its source plant is RED. | Drop currentTarget/enabled/type/containment/event-consumption checks; separate timer .click and dispatchEvent plants. GSS-STATIC-AFTER-READ: insert one intended-control .click() immediately after await hooks.readView() (READ original card:511 at e08bc11c), before the ownership/phase tests. GSS-STATIC-AFTER-START: insert one intended-control .click() immediately after await model.start() (READ card:517 at e08bc11c), before :518's branch. Each static plant must be independently RED at that exact resumed position; computed dispatch is outside this spelling proof. |
| GSS-GESTURE-NESTED (negative half of former GSS-GESTURE-ASYNC) | A render-helper plant under hooks.paint, ordinary sealed listener, readRaw and onOutcome cannot submit/replay; delayed released callback has no command; valid Save proceeds once. While paint's read is pending, the athlete's intended tap remains admissible because depth has been released. | Expose command; omit refusal depth around the render-helper plant or other synchronous callbacks; copy ambient positive-permission counter. GSS-DEPTH-HELD-AWAIT: hold refusal depth across a deferred paint read, then tap the live enabled control; the athlete's own tap is refused and the positive must turn RED (MEASURED reviewer R2, gss-r2-depth.mjs: refused:depth, 0 writes). |
| GSS-BINDING-DISPOSE | Repaint/leave removes exact wrapper; retained wrapper, detached node, superseded action/context and duplicate bind cannot dispatch. Old disposer cannot remove replacement. | Remove callback rather than wrapper, omit revocation, or bind using drawing context/root. |
| GSS-OUTCOME-PARITY (positive half of former GSS-GESTURE-ASYNC) | E's admitted awaited refusals/rejections and exact text/placement/focus; ignored paints nothing; saved with failed refresh stays saved/unread. | Recheck ambient counter after await, default unknown kind to success, append host code, clear on refusal, downgrade saved on refresh failure or paint finish refusal in place. |
| GSS-START-EXCEPTION | Existing ready-phase paint calls model.start and no owned command; no new Start tap/text. | Guard/remove existing call or add settings save to paint. |
| GSS-CUSTODY (UNCONDITIONAL, G-R1) | Independent extraction proves the MEASURED 23-line helper/comment span byte-identical; view re-exports preserve every named/default export and producer behavior; S10 pins the changed bytes. | Change trim/cap/coercion/order/gate while labeling move M, or lose a named/default export. |

READ row accounting: 17 unconditional groups, including GSS-CUSTODY. RAW-SNAPSHOT is grouped with RAW-PARITY; the review's ASYNC and API merges preserve both halves. Grouping is not permission to omit any assertion or RED plant. MEASURED (reviewer R2, sections 3/8, retained table census): baseline had 20 groups and Round 2 had 17 with custody conditional; G-R1 makes custody unconditional without deleting a group.
READ command paths: focused existing cells below; prescriptions only, not runs in this paper. PowerShell, from repo root, each command completes before the next starts:
```powershell
$env:MEASURED_TEST_NOW='2026-09-03'
$env:TZ='America/New_York'
& 'C:\Users\joeym\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --test rebuild/m3/w7-preview/today/test/gym.test.mjs
& 'C:\Users\joeym\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --test rebuild/m3/w7-preview/today/test/machine-settings-ui.test.mjs
& 'C:\Users\joeym\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --test rebuild/lanes/c/today-split/writer-fence.test.mjs
```

### Existing writer-fence rows to replace, never delete
READ F line numbers below are at e08bc11c unless another head is named. MEASURED (reviewer R2, section 6): at c38ed5fb the five gym product/test paths remain byte-untouched, but F is already shared, +216/-1 (1908 -> 2123 lines); earlier empty-intersection evidence is superseded. F:1751-1785 at e08bc11c is :1755-1789 at c38ed5fb; F:1817-1907 at e08bc11c is :1821-1911 there; other cited anchors remain unmoved. Re-measure at the accepted head as ordered above. Preserve each old counterexample with its new expected result/reason; retarget missing anchors and prove plants land, never skip. The scanner remains S-R26's tripwire, not writer-correctness certification.
READ count law, F:1724 at c38ed5fb (reviewer R2, section 6): a pinned count is an EQUALITY, not a ceiling; every changed count, including a drop, is reported beside its cause. Apply to every changed pin: GYM_DECLARED_SITES/seam keys (READ 6 -> 1, only model.start); LISTENERS_OUTSIDE_SHIM[gym-app.mjs] (READ 19 -> 0, binders plus ordinary hooks.listen/unlisten); frozen wrappers (READ 3 -> exact outer/facade/hooks/api shape, 4 interface objects, with detached-data freezes separately measured); static lane imports (READ 0 -> 1 producer edge); released lane-use windows (READ 3 -> 1 api.lane passthrough, other api/model acquisition windows explicitly pinned). Report old/new counts and causes for any additional holder/use-window multiplicity changed by the declared rewrites. Unchanged dynamic host edge and one-entry painter remain exact equalities; no blanket count ceiling or automatic snapshot update.
READ laws sharing F: Today's appended law requires a guarded writer directly inside a hooks.listen callback; the gym card's law is sealed binders plus refusal depth. Today E.6 remains Today-only; do not apply its ambient counter to gym or relabel forget durable.

| READ existing row/table at e08bc11c | Replacement and reason |
|---|---|
| :19 header; :192-210 GYM_DECLARED_SEAMS; :789-801 exact sites | Replace released raw-writer allowance with only exact model.start acquisition/call; move owned action calls into seal. Retarget the repeated-write plant from facade.lane().save to api.lane().save outside its pinned public mapping; keep the READ old six-site evidence in explanation; forget is transient, not PUT. Add each binder's exact action/selector acquisition pin and fail a second binding/raw model alias. |
| :552-585 RELEASED_FILES gym-app entry (array continues); :1110 preconditions; lane-acquisition mutants :1388-1400,:1504,:1568,:1590-1606 | Remove drawing/admission lane windows; allow api.lane only inside exact first.settings mapping. Pin api holder construction/destructure/passthrough windows and reject aliases, Reflect, computed access, await-ready host and additional acquisition. Constructor settings handoff gains phone. Existing spelling mutants remain red by acquisition. Do not silently reuse facade-only lane detection for api. |
| :882-889 frozen-wrapper count; :897 unfrozen mutant | Replace incidental call-count assertion with exact interface keys, frozen tables and dynamic deep-data proof GSS-CACHE-DETACHED. The positive shape must add api: Object.freeze({ alongside facade/hooks and the frozen outer return; an unfrozen table remains red; extra Object.freeze calls needed for detached data are legal. |
| :891-895 one-entry painter | Retain one-entry frozen repaint handle exactly. No new owns callback; sealed hooks.leave controls lifetime. Positive shape unchanged. |
| :958-989 sealed import edge rows | Replace :968's no-static-import assertion with an exact allowlist containing only '../../../coach/machine-settings-commands.cjs', the producer static import for moved helpers; keep the dynamic allowlist exactly ['./machine-settings-host.mjs']; extra drawing import remains red. Retain the import assertion and readings module's independent zero-import rows. |
| :1404-1422 S-R29 recorded laxity | Update :1409 to createGymSettingsLane(doc, phone, model, settings, painter), with a contained synthetic phone, model read fixture, {latest} settings and {repaint} painter; replace with desired detached/frozen contract and shallow/live-return mutants; old live-cache result must now fail. Never delete the former witness. |
| :1444-1475 recorded residue | Model computed/alias/Reflect plants become red at pinned model acquisition/use; cache-mutation plant remains possibly scanner-green but must be dynamically harmless. Replace deleted recordSettings anchor with actual raw-answer callback; mutable draft editing remains legal raw input, while post-capture mutation cannot alter save. Keep every shape and explain why its authority changed. |
| :1751-1785 listener exemption | MEASURED (reviewer R2, section 7): baseline 19 sites, including 5 writer-control sites, also holds at c38ed5fb. RULED hooks.listen/unlisten route ordinary card listeners and sealed binders own writer controls; LISTENERS_OUTSIDE_SHIM[gym-app.mjs] equals 0. Preserve Today shim rows; add gym bindings and a direct listener plant. View row-edit listeners remain unchanged without command authority. |
| :1817-1907 E.6 counter tests | Retain existing Today-only assertions with explicitly limited labels; append control-specific gym rows. Do not wrap gym commands in the tested ambient counter or report an aggregate 'guarded writers' denominator including forget. Counter mutants prove Today behavior only. |
| :995-1021 paint exception; :1669 copy-free seal; :773 PUT/STORE/ADOPT lists | Retain Start exception/counterexample and Today copy rows. Remove no word-list entry merely to make forget disappear; correct its semantic classification in new rows. Extend zero-copy literals to newly authored lane code without widening phrases. |

N test-row implementation is new handwritten verification logic; R updates to anchors/import ownership must be declared separately. No lexical implementation changes are preauthorized: if the existing scanner cannot pin the new api/model holder using its current facilities, specify the failing acquisition plant and obtain a narrow PM ruling, keeping other implementation work reviewable.

## E. Parity cells: identical answers, operations and painted text
READ baseline expressions are card:267-288, view:39-57, Producer:49-84 and card constants:71-81. MEASURED helper probes cover the rows marked 'probe'; all old/new durable and DOM comparisons below are UNMEASURED requirements. For each case run old and new against separate synthetic stores with identical starting generation, athlete/device/lease, sequence, keys, injected clock and deterministic operation-identity inputs. Compare the complete serialized operation(s) and outbox entry after reopening, including IDs, dates, times, causal parents and payload key/row order. Compare rendered text after plainOrDrop and editor contents/visibility. Compare operation plaintext, not randomized encrypted-store ciphertext. No dropping fields to manufacture parity. Compare disabled/hidden state, focus target and tap count as well as text; a whole-screen refusal is not equivalent to an in-place sentence.

| Synthetic input/sequence | Required old/new operation and text result |
|---|---|
| Blank draft, whitespace-only rows/cues (probe) | No operation. 'Add a setting or a cue before saving. Nothing was recorded.' Draft remains. |
| Name without value; value without name; trimmed repeated name (probe) | No operation. 'Each setting needs a short name and a short value, and each name only once. Nothing was recorded.' |
| Non-string synthetic draft rows (supplemental helper/boundary parity) | Preserve String(row.name \|\| '') and equivalent value/cues coercion. MEASURED (Round 2; reproduced by reviewer R2, retained gss-r2-coerce.mjs), exact view:39-49 helper with exerciseId='synthetic-lift', value/cues empty: names 0, false, null -> null machine; name 7 -> {exercise_id:'synthetic-lift',settings:[{name:'7',value:''}]}, READ view:54-57 and Producer:49-84: existing acceptable refuses that half-pair. Do not type-reject raw answers before the helper. The review's blanket numeric-to-blank wording does not describe this truthy-number input. |
| Numeric pair {name:7,value:5}, empty cues, exerciseId='synthetic-lift' | MEASURED (reviewer R2, section 4, retained gss-r2-coerce.mjs): machine={exercise_id:'synthetic-lift',settings:[{name:'7',value:'5'}]}, acceptable=true. ACCEPTED AND STORED today as strings '7'/'5' by the READ existing save path; the probe measures helper/acceptance, not a durable put. Required old/new operation parity preserves this write; typed rejection at readRaw would lose it. |
| Case-distinct 'Seat'/'seat' (probe) | Same accepted operation; both answers retained in original order. No new success sentence. |
| ' B '/' four ', wholly blank row, 'A'/'5', whitespace cues (probe) | Same machine with ordered B/four then A/5; blank row removed; cues omitted; exercise_id from editor's lift. Stored 'four' is a word, not a number. |
| Cues-only ' Remember ' (probe); valid rows with cues | Same cues 'Remember'; no settings member for cues-only; same member insertion order and saved notes. |
| READ caps Producer:33-36: settings 12, pair text 40, cue text 400, exercise ID 80 | At cap accepted where otherwise valid; just over cap refused with SETTINGS_REFUSED and no operation. Empty/overlong exercise identity cannot be minted through a valid view; direct helper equivalence covers its existing refusal. |
| Raw strings containing newline, apostrophe, Unicode supplied as escapes; duplicate after trim; non-BMP length boundary | Same UTF-16 length checks, exact stored strings and existing plainOrDrop rendering; no new normalization/case fold/unit. |
| Replace prior record by removing a row or cues; latest for another lift exists | Same complete replacement machine for this lift; no deletion operation; other lift unchanged; next block reads latest stored record. |
| Host returns null/ok:false, closed host, actual transactional storage fault | No accepted operation/outbox change on the fixture's proven failure; 'These settings could not be recorded on this device, and no part of them was recorded.' Retain answers and permit retry. Do not infer no-write from a random thrown promise. |
| Injected host.save rejects before writing | Controlled synthetic host makes no operation, then rejects. No refusal text, no clearing or success paint; api.pending rejects with that error and flags/disabled state release in finally. A rejected host after a write does not prove no-write; retain the rejection without inventing such a claim. |
| Save acknowledged, subsequent latest fails | Same committed operation; editor closes only if matching; read block uses 'Settings could not be read.' plus existing SETTINGS_UNREAD_ACTION, never 'No settings saved yet.' or SETTINGS_NOT_SAVED. |
| No lane; read pending; read failed; known-null | Same hidden block or existing reading/unread/empty text. Only known data offers an editor; optional read never delays Log Set. |
| Existing draft, refusal, add/remove row, retry | Exact refusal persists through repaint; corrected answers store same operation on retry. |
| Cancel before Save; old event after replacement/leave/lift change | No new operation and no new text. Intentional stale-event tightening, not a new athlete-visible refusal. |
| Pending Save, repeat gesture, repaint then repeat | New side admits only original operation; old side may expose duplicate opportunity. This is an explicit concurrency correction, not a byte-parity claim for two accepted old writes. Same original-operation bytes and normal-screen text required. |
| Deferred old success/refusal, then new editor/leave | Original accepted operation still equal; no clearing/error/focus takeover of replacement. Required safety correction where old visibility differs. |
| Blank performed load/reps, or missing effort | READ model:38-39,:503-507: ENTER_PERFORMED = 'Enter the weight and reps you actually completed.'; CHOOSE_EFFORT = 'Choose clean reps left, or Unsure.' No operation; exact plainOrDrop(refusalText(result), 'gym-error') in #gym-error, no screen replacement. |
| Undo refused | READ card:478: put(map, 'rest-note', refusalText(result)) replaces NO_REST_PRESCRIBED in place; no new operation on the controlled refusal, no screen/focus replacement. |
| Finish refused | READ card:417 -> refusalScreen:501 -> stub:198 -> show:171-176: replace whole screen and focus new h1 (root fallback). Keep exact existing copy and focus move; never map to in-place error. |
| logSet raw load/reps/effort (including blank and Unsure), finish, undo, forget | Same model request semantics, operation/outbox bytes and refusalText/refusalScreen output on admitted gesture; forget has no operation. Keep original load/reps trim at sealed input boundary, model Number conversion and reserve cloning; no new effort default. |

READ copy constants remain defined in gym-app and existing design copy-source inventory is unchanged. R moves exact mapper expressions only; N does not introduce a sentence. Invalid/stale/in-flight input does not borrow another editor's previous success. A direct helper probe on non-UI types is supplemental; UI parity domain is raw strings and existing effort objects, not arbitrary proxies/functions/getters submitted as 'answers'.

## F. What the owner would notice
Normally nothing changes. Your machine settings, notes, buttons and refusal sentences stay the same. Tapping Save again while it is still saving does not create another saved note. If you close an editor or open a different one, an older save finishing later does not take over the new editor. Logging a set still works while settings are being read or saved. This ticket does not add a workout Start tap or change your workout rules.
READ basis for this promise: existing strings/card flow at T/gym-app.mjs:59-81,:232-288,:386-480; existing optional-lane controls in UI test:599-630,:995-1069. The new concurrency and stale-event cases are safeguards, not permission for copy/layout redesign. Retain native button activation wiring; this paper claims no browser/assistive measurement. RULED G-R3 keeps existing node DOM drives and adds no native-browser gate.

## G. PM rulings: RULED at DECISIONS:628
READ authority: refs/remotes/origin/rebuild/t2-client-core:rebuild/DECISIONS.md, physical line :628, read through a single-line filter in this pass. The review loop is over. These decisions close the former branches; no owner question or dependent design choice remains open. This paper grants no build acceptance or merge authority.

| Ruling | Decided design and evidence |
|---|---|
| G-R1 (former G1): RULED - helper relocation | PURE MOVE of machineFromDraft/acceptable into the sealed lane; MEASURED helper/comment span 23 lines, independently extracted under unconditional GSS-CUSTODY. Re-export in the view preserves every named/default export. Split:1335 is AMENDED to changed-by-this-ticket-then-pinned; S10 pins resulting bytes. B/C/D contain only this design. |
| G-R2 (former G2): RULED - pending and dispatch ownership | YES: recordSettings, logSet, finish, undo and transient forget belong to this ticket's bundle, with model admission unchanged. New fence rows say FOUR DURABLE PLUS ONE TRANSIENT; no word leaves PUT. MEASURED author and reviewer: actual forget makes zero client calls. |
| G-R3 (former G3): RULED - depth-zero and static dispatch | NO event.isTrusted and NO native-browser gate here. Build hooks.listen/unlisten and synchronous refusal depth; READ target LISTENERS_OUTSIDE_SHIM[gym-app.mjs]=0. Retain jsdom positives and static zero-dispatch proof. D-GSS-TIMER in H names the admitted outside-scope click and computed-spelling residue. |
| G-R4 (former G4): RULED - boolean opening and pinned passthrough | YES: opening promise resolves to boolean availability; api.lane remains the one pinned host passthrough with public keys/order/descriptors preserved. MEASURED reviewer R1 spelling census finds zero settings.lane/settings.ready readers; this pass did not rerun that census. D-GSS-PASSTHROUGH in H reserves retirement to S10 after a destructuring-aware census. |
| G-R5 (R2-N1): RULED - six call-site paint shape | READ directly at e08bc11c: synchronous drawing returns :513, :514-515, :518, :528, :531, :532. Wrap those calls with hooks.paint; each is a named DECLARED STATEMENT REWRITE in C. No paint control-flow restructure. D statically plants at both bare post-await positions; B states the runtime limit. Full segment scoping is re-priced in GYM-START-IN-PAINT's spec. |

## H. Residues, limits and completion evidence
D-GSS-TIMER (READ PM ruling; MEASURED reviewer admission probes): a programmatic click from outside every sealed scope at the live intended control is admitted at runtime and caught only by the static row; a computed spelling is caught by neither. If a real-browser gate ever reaches the gym card, trust may be added as browser-only hardening, never as a node row's positive. This ticket adds no such gate or trust rule.
D-GSS-PASSTHROUGH: MEASURED reviewer R1 spelling census finds zero readers of settings.lane/settings.ready, without a destructuring-aware proof. Whether api.lane retires is a question for S10's brief after a census that also sees destructuring, not for this ticket; preserve its exactly pinned public mapping here.
D-GSS-LINES and D-GSS-D2-ANNEX remain explicit builder obligations in C/D: authored lines are unpriced by either side, and the annex has no CI home. Neither paper acceptance nor a silent cell exit proves the build.
UNMEASURED: no candidate implementation of editor tokens, controller, depth-zero binders or typed outcomes; no old/new operation comparison, durable put/reopen, browser/native/phone/assistive-tech drive, performance measurement, full Today run, conformance gate, build/design gate, S8/S9/S10 package or receipt. Proposed RED plants were not run. No claim that the existing app presently corrupts settings. No deployed/service state inspected. The guarantee for arbitrary hostile released code is not proved by these tripwires.
READ-only limits: traced the settings and gym action tails, not all injected producer/stage/adoption internals. gym-model.rebase (:160-175) can acquire a host through injected hostForDay; holdForAdoption (:140) changes only flags. These are lifecycle seams, not controls assigned here; host opening/lease metadata effects were not measured. Model.read preparation and host reopening were inspected only to cited action boundaries, not re-proved globally pure. Snapshot freshness is the existing per-mount cache policy, not cross-device freshness. S8 key membership was read; pin hashes and descendant package custody were not revalidated.
MEASURED prior-author execution scope: sequential synthetic Node probes only, using C:/Users/joeym/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe; MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York set on separate lines before each process. No real installation, private fixture, athlete record or auth input. Existing guard fragment and exact forget body were lifted from source, and a separate actual-model forget call confirmed no client entry.
MEASURED prior-author scratch retained: C:/Users/joeym/AppData/Local/Temp/astra-gym-settings-spec-e08bc11c/{probe.mjs,forget.mjs,view.before,view.after,lane.before,lane.after,card.before,card.after}. These are probes/partial diffs, not proposed product artifacts. Retention is the prior report's claim, not a fresh filesystem check here.
READ retained reviewer evidence (REVIEW-R2 sections 3/11): gss-r2-coerce.mjs covers exact helper coercion including the accepted numeric pair; gss-r2-depth.mjs covers full-segment versus six-return scoping, render-helper/bare-position plants and depth held across await. The reviewer retains them in the farm scratch worktree of e08bc11c used in round 1 beside gss-probe.mjs, gss-forget.mjs, gss-trust.mjs, gss-trust2.mjs and gss-admission.mjs. Static dispatch counts are recorded in REVIEW-R2 from explicit-path grep at both heads. This pass does not claim to have re-executed the reviewer's probes.
MEASURED final-pass static probe retained at C:/Users/joeym/AppData/Local/Temp/astra-gss-final-paper-2864ca58488b4bf7b828b2c07a217ea5/dispatch-scan.mjs: each named source had zero click/dispatchEvent spellings at e08bc11c. Run with the exact Node path and separate environment assignments above, one process. The same folder retains dispatch-scan.ps1, whose execution was blocked by PowerShell policy; no measurement relies on it. No scratch was written in the worktree.
READ acceptance boundary, DECISIONS:628: Claude builds from the split's accepted head; Astra reviews that build blind at highest effort; PM4's Fable final reads every hunk. This final paper adds no review-loop round or native-browser gate. The historical cut's accepted byte identity does not prove the new writer decisions. Only this specification is edited; no product/test byte or decision/status file is changed, and no commit, push, checkout, reset, stash, clean, fetch, package install, receipt or tree artifact is authorized here.
READ final-pass scope: full REVIEW-R2 and the single PM decision line read; paint's returns and refusalScreen anchor directly read at e08bc11c. Initial NEXT/ROADMAP combined output was truncated; the current ROADMAP preamble/Now was subsequently read separately, and no claim relies on truncated history. File hygiene, table counts and final scoped status/stat are checked at completion; their console output is not copied into a self-changing transcript.

## Round 3: the PM's rulings and the reviewer's round 2 notes
G-R1: LANDED in B/C/D/G; unconditional pure-move custody, view re-exports and amended changed-then-pinned disposition.
G-R2: LANDED in A/B/D/G; pending/dispatch bundle owns FOUR DURABLE PLUS ONE TRANSIENT and preserves PUT words.
G-R3: LANDED in B/C/D/G/H; depth-zero, hooks.listen/unlisten, zero direct card listeners and D-GSS-TIMER.
G-R4: LANDED in B/D/G/H; boolean opening promise, pinned api.lane and D-GSS-PASSTHROUGH.
G-R5: LANDED in B/C/D/G; named six return rewrites, exact post-await static plants and deferred full segment scoping.
R2-N1: LANDED in B/C/D under G-R5; no control-flow restructure, every paint rewrite classed, runtime gap explicit.
R2-N2: LANDED in B/E; resumed model.start segment begins :518 and refusalScreen is :501.
R2-N3: LANDED in A/C/D; baseline clauses, both drifted fence anchors, equality-and-cause count reporting and distinct Today/gym laws.
R2-N4: LANDED in E; numeric pair accepted/stored as strings, with helper measurement separated from required durable parity.
R2-N5: LANDED in D GSS-GESTURE-NESTED; held-depth-across-await RED plant must detect refusal of the athlete's own tap.
R2-N6: LANDED in B/E/G/H; retained probe names and evidence scopes; G-R3 closes the trust alternative.
