# P3-IMPORT-UI-2 REVIEW R2 (independent, Opus high)

VERDICT: ACCEPT, with one MAJOR only the PM can close.

78eb15f on rebuild/c-p3-import-ui-2, detached in %TEMP%\earned-iu2-rv (clean,
three junctions). Re-fetched before reporting: origin/rebuild/d-p3-followons is
26ab3ab3, UNMOVED, no rebase needed. (The tip has moved: origin/rebuild/
t2-client-core is e4c1bd6b, not the dispatch's ee5fc3d6.) All of it I executed on
my own cells and my own synthetic bundles sealed by port.cjs from the public
journey fixture; no private fixture, ledger or real out-folder was touched.

MY OWN GRAPH WALK (%TEMP%\iu2-rv\graph.mjs), not the author's assertion: I built
A1, read esbuild's metafile and walked it from today-entry.mjs refusing the one
dynamic edge. 121 boot modules, carrying NONE of migrate.cjs, merge.cjs or
rebuild/m4/import/*. The route-only set is exactly the 15 modules the report
names, production-mapping.cjs among them and no TEST-ONLY registry anywhere.
THREE MUTANTS OF MY OWN, each refused by name AND by the module that reached it:
a boot module requiring replay-core.cjs; a SECOND importer of the route; a boot
module importing merge.cjs by statement.

MY OWN CELLS on the real route in jsdom over fake-indexeddb, on the phone's ONE
store (%TEMP%\iu2-rv\rv-route.test.mjs, 7/7):
- RV-1: the Today link admits end to end, and revision/ops/outbox/imports/basis
  are byte-identical through pick, file, Unlock and the identity question. Unlock
  is write-free, custody() is null while the question stands, the painted
  question is source-admission.mjs:96 byte for byte and the controller's review
  asks the same one. No trap fired; no op minted by the admission.
- RV-2: a reopen settles the Today link to "History imported" (finding 4).
- RV-3/RV-4: a LABEL-ONLY stranger (same programme, other athlete_label) is
  ADMITTED by the machinery, executed both ways, so the author's central claim
  holds: the identity question is the only identity guard, and No writes nothing.
  Wrong words and one flipped byte both paint exactly "BUNDLE_AUTH_FAILED That
  passphrase or file did not unlock. Check the six words and the file.", write
  nothing, keep the draft.
- RV-5/RV-7: a programme-mismatched file paints LOCAL_SOURCE_PROGRAMME_UNRESOLVED
  once and only once, and Back on the review is a cancel; both retract, list it,
  leave no basis, application, op or outbox entry, and leave Today's model read
  and the gym card read the strings they were before.
- RV-6: the route builds Production.createProductionProducerRegistry({hash}) and
  names no second registry; it REFUSES SOURCE_ENGINE_CONTEXT_UNPROVEN on a
  material it was not presented; the review paints the production
  ENGINE_REVISION; the admitted record carries no TEST-ONLY name.

MY OWN REAL-EDGE RUN: P3 EDGE-ROUTE PASS, real msedge.exe, real clock,
America/New_York, 15 labels. It clicks the Today link (44 px checked), types into
a box the SERVED stylesheet computes at 16px, and reads
M2-S5-TODAY-CHILD@0df73b01f3d2d935 off the review. Every runbook label matches
what I saw. DRIFT, checked by me: 17 files, 6 declared in packages/S5.json, no
engine byte, no src/, no w6 product byte, no CR byte; exactly one added line
carries U+2013/U+2014, the dash detector's own regex. The amended tests state the
new rule where the old one stood, with no assertion deleted without replacement.

## Findings
1. MAJOR (PM only) - DECISIONS:480 RULING 1 IS HALF-OVERRIDDEN AND NOT CITED.
   After round 1 the PM ruled that "the Import entry stays on the Measure
   screen's No baseline yet line AND AT SETUP'S END (:470)", and that round 2
   "must drive the REAL one-store configuration through the tapped link and go
   green on the family branch". Round 2 keeps the Measure entry and pins it
   (P3-X10), which is right, and DELETES the setup-end entry the same ruling
   preserved, arguing against :470 without ever citing :480. The deletion is
   defensible on its own evidence (P3-U5: on setup's last screen the first run is
   unsaved, so the walk can only refuse LOCAL_SOURCE_PROGRAMME_UNRESOLVED), and
   restoring it drifts setup-app.mjs, which this dispatch's sealed-drift budget
   forbids; the author chose the dispatch over :480. That is a PM call: amend
   :480 RULING 1, or take the setup-app.mjs drift and pin the refusal the way
   Measure's is pinned. The asymmetry, one ruled-for link kept and pinned and the
   other deleted, is the finding.
2. MAJOR (landing dependency, not a defect here) - :470's primary entry still
   cannot admit on a phone: the first Measure render writes two
   earned/measure-trial-start/v1 ops and the pick a third, all class
   body-composition-source, and source-admission.mjs:148 answers
   LOCAL_SOURCE_CONTEXT_UNRESOLVED for each. The fix is lane D's
   P3-REPLAY-MEASURE-FAMILY, dispatched at :480 and not in this base.
3. MINOR - report section 3 says the chunk question needs a "PM word". It has one
   (:480 RULING 2, ACCEPTED INLINE, PWA-SPLIT named as the revisit).
4. MINOR - a stale first frame on Today, measured. renderMeasure awaits the
   adoption chain before painting its link; renderToday does not, so on a reopen
   of a device that already admitted, the first painted Today link reads "Import
   my history" and the workout cascade then repaints it "History imported" with
   no explicit render (I checked both). A tap inside that window opens the pick
   step, where the machinery answers the rebase code verbatim.
5. MINOR - bar (e)'s "second import attempt" is proved by calling
   browser-entry.importBundle directly (P3-U3), because the route is read-only
   after admission by design. That is the better answer, but the bar's
   route-level claim is unproved; the report should say which.
6. MINOR - today-app.cjs moves by more than the dispatch asked (one screen case,
   one lazy loader, two entry links): 123 added lines over about seven hunks,
   importDeps, importAdmitted, the installation option and two accessors among
   them, so the S6 rebase onto c-s6-small is harder than planned.
7. MINOR - sealed drift is six S5-declared files, not three. PRODUCT drift is the
   budgeted two (today-app.cjs, today-entry.mjs); the other four are forced
   re-pins of those two, each stating its rule. today-bindings.mjs is untouched.
8. NOTE - the one path retract cannot undo is unexercised: if reconcile() throws
   after publish() succeeded, retract is refused for an admitted import and the
   screen prints the RETRACT code with the real cause demoted to detail.
   readLists() turns a failed listImports into an empty list, so a failed read
   paints no summary rather than saying so. "Nothing on this phone was changed"
   after a retract is true of his history, not of the disk. And before a markers
   pick exists the Measure link sits at the END of that screen, not on a "No
   baseline yet" line (my Edge run read "Strength markers" there).

## Bundle figures and tails, mine (TZ=America/New_York)
app.js 1,961,495 B on disk; per-input accounting 1,953,002 B; the 15 route-only
modules are 287,887 B of it, 14.7%. index.html 30,994 B, styles.css 178,107 B.
Today boot graph 121 modules. NO second output file: app.js itself carries
"Choose the earned-port file" and has exactly one init_import_screen() call site,
the dynamic import. A5: 13 files, 11 precached, app.e37f0cb4e4852e60.js carries
the route. Tails: import/test 34/34/0 | my cells 7/7/0 | today-17 by name
(MEASURED_TEST_NOW=2026-09-03) 667/662/5, the five being P-MEASURE (g), N1.18,
S10 and two setup.test re-pins, each a sealed-byte declaration red with no
behaviour behind it | W6 586/586/0 | w6/host 42/42/0 | m4/import 86/86/0 |
production-* 24/24/0 | retract 13/13/0 | local-source-consumer 6/6/0 | port
65/65/0 | coach 231/231/0 | client 18/18/0 | A5 suites 56/56/0 | rig187 PASS |
A1 "A1 TODAY BUILD PASS: 3 assets; 136 pinned inputs (15 engine, 12 client);
build earned-669539fe19ae; Today boot graph 121 modules, carrying no migrate.cjs,
no merge.cjs and none of the m4/import lane" | A5 "A5 PWA BUILD PASS: 13 files;
11 precached and pinned by sha256" | b-package --ci --package S5 exit 1, "B
PACKAGE S5 FAIL SEALED-PROFILE-RECOMPUTATION", expected, lands in S6 | Edge "P3
EDGE-ROUTE PASS - the shipped A1 dist, real msedge.exe, real clock,
America/New_York, 15 labels recorded".
