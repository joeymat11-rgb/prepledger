# P3-IMPORT-UI-2 REVIEW R4 (Fable final, round 4 re-check; DECISIONS:431 point 17, :439)

VERDICT: ACCEPT - both R3 MAJORs are fixed and executed on the athlete's own
path, the three MINORs and the carried R2 items are closed, the Measure
collision pair is honestly re-reasoned into one positive one-store cell, and
nothing was weakened. Six NOTEs below, none a bar.

Subject: a342653 on rebuild/c-p3-import-ui-2 (author round 4), rebased onto
origin/rebuild/d-p3-replay-measure 47a223d (the F7 family), reviewed on the
detached worktree %TEMP%\earned-iu2-rv at a342653, three junctions, tree clean
before and after. Bundles SYNTHETIC (port.cjs over the accepted clean-init
constructor); no private fixture, ledger or real folder opened. TZ=New_York.

## What I executed myself (%TEMP%\iu2-rv\rv4-route.test.mjs, 11/11)
- RV3-1 summer / RV3-2 winter, ONE store (gym-host openTodayHosts): the whole
  Today-link walk admits; the identity question typed by me from
  source-admission.mjs, verbatim before importBundle; custody null and the
  production registry NOT built while it stands; Yes builds it once ({hash},
  no digest); no op minted, no outbox change, no TEST-ONLY string; Today, gym
  card and baseline adopt; trial day one is the enrol day; ops -04:00/-05:00.
  NEW: holdingBytes() false at done, no "Working." there; on a dispose/reopen
  the FIRST Today frame carries no link (null, never the offer), ready then
  paints "History imported"; the second import THROUGH THE ROUTE: the reopened
  link opens the read-only summary, no chooser, no Unlock, and reopen() on an
  admitted route still paints no chooser. A set saved after survives reopen.
- RV3-3: wrong words then a flipped byte, BUNDLE_AUTH_FAILED once each, nothing
  written. NEW (R3 MAJOR 2): Back without custody returns to Today; the link
  reopens on the CHOOSER, step "pick", refusal null, holdingBytes() false, the
  damaged file's name gone; the GOOD file then admits in the same page
  session, imports 1, retractions none, no op minted.
- RV3-4 label-only stranger: No writes nothing; Yes admitted to review (the
  question is the only guard); Back on the review retracts to pre-stage for
  every consumer, revision +2. RV3-5 programme stranger: refused AT CONFIRM,
  LOCAL_SOURCE_PROGRAMME_UNRESOLVED once, NO "Working." beside it (R3 MINOR 3
  on the confirm path), bytes dropped, retracted clean. RV3-6: a registry
  refusing SOURCE_ENGINE_CONTEXT_UNPROVEN prints it once, retracts; MAPPING_ID
  not TEST-ONLY.
- RV3-7, the unenrolled installation, now a bar (R3 MAJOR 1): Today paints NO
  import-entry, neither phrase anywhere, no control on the page names the
  import, none on setup 1-6, and the unenrolled MEASURE screen (rendered to its
  markers pick) paints no entry either; imports and retractions empty. The walk
  forced open by API still takes custody and refuses; no painted control does.
- RV4-8 summer AND winter (the :480 primary, positive): first run saved,
  Measure rendered to its markers pick (3 ops on the store, 2 measure-class),
  the "Import my history" link on Measure tapped, pick/words/Unlock/question
  verbatim/Yes/confirm: ADMITS; applied+basis true, boot flag false, no op
  minted, no retraction, the measure-class ops byte-identical before and after,
  Today and the gym card on the imported loads, baseline column non-empty, the
  Measure link then "History imported" beside the hidden No baseline yet note,
  trial day one unmoved, ops all on the day's offset.
- RV4-6b probe (recorded, NOTE 4). rv3-graph.mjs on the new head: 137 inputs,
  boot 121, route-only EXACTLY 16 (R3's 15 plus measure-replay.cjs 4,029 B);
  app.js 1,967,853 B, route-only 293,357 B; one init_import_screen() call
  site; four metafile mutants and the ONE real mutant (today-entry.mjs imports
  merge.cjs, restored) refused by name; tree clean after. Edge: the author's
  edge-route.mjs run by me TWICE, once with every suite running (the R3 race)
  and once idle: PASS both, 16 labels, "Today entry link, settled height: 44
  px"; my probe reads no link on the first Today frame, 44 px on every later.

## Dispositions of R3 and the carried R2 items
MAJOR 1 FIXED (today-app.cjs 634, 946: !firstRun() on both entries; P3-U5 red
side on the unenrolled Today frame; my RV3-7). MAJOR 2 FIXED (resetDraft() on
Back-without-custody and reopen(); P3-X10; my RV3-3). MINOR 3 FIXED (fail()
clears the note; P3-X11 and my RV3-5 both refuse at confirm). MINOR 4 FIXED
(settled rect; passed under load). MINOR 5 FIXED (Measure pre-check withdrawn;
the Sleep caution verified by me: sleep-commands.cjs OP_CLASS "sleep" falls to
source-admission.mjs:167's catch-all; step 4 pick-again; step 6 paints what
the screen paints). R2 MINOR 4 FIXED (settleAdoption/paintTodayEntry; my
RV3-1). R2 MINOR 5 ANSWERED at the route; the deviation (closed door, not a
painted code) is stated in P3-U3 and the report and is the honest fact. R3
NOTE FIXED (bytes dropped on admission and on every refusal I measured). STEP
4: P3-X9 is the positive one-store cell with the old assertion named where it
stood; P3-X10 keeps its number with the new defect; nothing deleted unreplaced
(refusal-route -14/+42 assert lines; page-bundle's delta-13 re-reasoned to 1).

## Findings (none blocking)
1. NOTE - P3-U5's `[data-go="import"]` assertion is vacuous: importLink wires
   its click by addEventListener and carries no data-go; the slot check works.
2. NOTE - edge-route.mjs's settled-rect loop resolves only on `now > 0`; a node
   detached by a repaint would spin on rAF for ever and the run would hang, not
   fail. Unreachable here (the link is painted after the chain settles; my
   probe agrees): bound the loop.
3. NOTE - runbook step 2 says the Measure link sits "beside the No baseline
   yet line"; before the markers pick is answered it sits at the end of the
   markers screen (Edge label "the line it sits on: Strength markers"). The
   Sleep paragraph lists `steps` as having a family; steps is
   LOCAL_SOURCE_DAILY_UNRESOLVED (nothing on the page writes it, no hole).
4. NOTE - confirm()'s catch treats a throw AFTER capability.publish() as a
   refusal: my contrived registry (throws on its second qualify) left the
   device ADMITTED (applied, basis present) while the screen painted
   LOCAL_IMPORT_RETRACT_REFUSED_ADMITTED and "could not be taken back"; the next
   paint shows the summary. No athlete path reaches it; a guard for S6.
5. NOTE - carried: a second entry staged through the MACHINERY leaves the boot
   flag IMPORT_REBASE_REQUIRED while Today still adopts the admitted basis
   (RV3-9); unreachable through the route. Two trial-start ops on Measure's
   first render (author's open item 4) are lane D's.
6. NOTE - the done-callback now arms the adoption chain BEFORE its first Today
   paint, a reorder on a sealed file (journey 51/51, today-17 unchanged); the
   S6 re-seal of today-app.cjs must carry it knowingly.

## Drift, law, tails
Drift 17 files (git diff --name-only 47a223d a342653), SIX in S5.json:
today-app.cjs, today-entry.mjs, local-today-journey.test.mjs,
today/test/package.test.cjs, measure/test/boundary.test.mjs,
today/test/copy.test.cjs; no rebuild/engine, src/, ledger, w6 product or
m4/import product byte; no CR; one added dash line (the detector's regex).
Tails: my cells 11/11 | import/test 35/35/0 | retract 13/13/0 |
p3-replay-measure 9/9/0 | production-* 24/24/0 | local-source-consumer 6/6/0 |
client 18/18/0 | m4/import 86/86/0 | port 65/65/0 | coach 231/231/0 | w6/host
47/47/0 | local-today-journey 51/51/0 | W6 586/586/0 | today-17
(MEASURED_TEST_NOW=2026-09-03) 667/662/5: P-MEASURE (g), N1.18, S10, two
setup.test re-pins, the five sealed-byte declaration reds and nothing else |
A5 suites 56/56/0 | rig187 PASS | A1 "A1 TODAY BUILD PASS: 3 assets; 137
pinned inputs (15 engine, 12 client); build earned-1b924c85bd83; Today boot
graph 121 modules, carrying no migrate.cjs, no merge.cjs and none of the
m4/import lane" | A5 "A5 PWA BUILD PASS: 13 files; 11 precached and pinned by
sha256" | b-package --ci --package S5 exit 1 "B PACKAGE S5 FAIL
SEALED-PROFILE-RECOMPUTATION" (expected, S6) | Edge x2 "P3 EDGE-ROUTE PASS -
the shipped A1 dist, real msedge.exe, real clock, America/New_York, 16 labels
recorded".
