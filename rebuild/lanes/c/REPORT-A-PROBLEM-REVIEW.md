# REPORT A PROBLEM - INDEPENDENT REVIEW, ROUND 1
Independent (author != reviewer), effort HIGH, told to disagree; every claim re-executed, not read. Candidate `rebuild/lane-c-report` @ **41fdda5** (code **489c8c7**), base **964f183** (the A4b merge, `:149`). Bar: `REPORT-A-PROBLEM-BRIEF.md` R1-R12 + mutants E1-E9, under `DECISIONS:140 (3)`, `:135 (1)`. Executed evidence, the leak probe, the build-id recompute and the mutant table: **`REPORT-A-PROBLEM-REVIEW-ANNEX.md`**.

## VERDICT: ACCEPT WITH CONDITIONS
Nothing I executed found a leak, a durable write, a fabricated id or a dash. The design is the right one: `buildProblemReport` is handed an eight-member `state` and can print nothing else, so the privacy property is structural rather than promised. All five conditions are NON-BLOCKING; the first is the one that matters.

- **C1 NON-BLOCKING - pin the block's SHAPE and VALUE SETS in the node suite.** Two of my mutants SURVIVED `problem.test.mjs`: **E2**, which puts `JSON.stringify(model.read())` into the `user agent` field and really does print page internals, and **Z4**, which adds a NINTH field. `R2` and my own probe both miss them because both search for SPECIFIC planted values rather than checking what a field may contain. `browser-check.mjs` catches Z4 (it deep-equals the field names) but not E2, and it is not in CI. Red-first test: assert the block is exactly `FIELDS.length` lines, every key in `FIELDS`, and every value in its declared set - `enrolment` in `ENROLMENT`, `offline-ready` in `OFFLINE`, `build` matching `/^earned-([0-9a-f]{12}|notinjected)$/`, `device` matching `/^(device-[0-9a-f]{8}|none)$/`, `lane open` a subset of `LANES` or `none`, `at` the stamp shape, and `user agent` byte-equal to `navigator.userAgent`. RED under both E2 and Z4 today.
- **C2 NON-BLOCKING - the copy deviates from the brief and the REQUESTS line is not on the tip.** Shipped: "Copied. Send it to Joe." / "Select all and copy, then send it to Joe."; the brief says "Paste it to Joe." The change is disclosed in `today-app.cjs` and better reasoned (on Dad's phone he is not pasting into a PM chat, and the page cannot know whose phone it is before the first run has named him), and the brief's own open question 2 asks the PM for the wording - but `rebuild/lanes/REQUESTS.md` on this tip carries no such line. Post it before the owner look.
- **C3 NON-BLOCKING - an undisclosed rider.** `rebuild/coach/BRIEF-COACH-WAVE1-TEXT.md` (178 lines, docs-only) rides this branch. It is WITHIN lane C's licence (`LANES.md:10` gives lane C `rebuild/coach/*`) but outside the custody list for this build; name it in the report or land it separately. `C6-INDEX.md`, also named in the dispatch, is not in the diff at all.
- **C4 NON-BLOCKING - one bar item is unmeasured by the shipped check.** `browser-check.mjs` does not assert Today's primary action is still in view with the box open. I measured it in msedge and it passes (390px: top 452 bottom 511 of 842; 320px: 444/503; no sideways scroll; the box sits 199px below the primary). Add the assertion.
- **C5 NON-BLOCKING - the 3-row hand test is owed.** A human run on the owner's iPhone, and its row 3 is the only thing that can answer whether the iOS clipboard copies at all. Not performed by this reviewer.

## THE PIN-AVOIDANCE JUDGMENT: both readings are sound
**The device id off the authority lease is the SAME value**, executed: `handle.lease.device_id === era.deviceId === handle.deviceId`, and `devicePrefix` gives the same eight hex from either. It refuses rather than invents - `null`, `""`, `"device-"`, `"device-xyz"`, a seven-hex id, `42` and `{}` all give `none` - and the lease is the right source for the stated reason: it exists from the first launch, before any operation does.
**Restore-required off the page's own status line IS bound to `rebuild/client/copy.cjs`**, executed: `gym-host.RESTORE_REQUIRED` is byte-equal to the client's `RESTORE_REQUIRED` (`"Restore required — sign in"`), the declared `RESTORE_MARK` is its first two words, and P1's boundary rewrites the dash AFTER the mark, so the painted line `"Restore required: sign in (KEY_MISSING)"` still answers `restore-required`. Boot's other status sentence answers `no-store`, not a false positive. Subtest 7 pins the binding by requiring `copy.cjs` at test time, and my mutant **Z1** turns it RED. Two narrow fragilities are residuals, not defects: the binding is a PREFIX, and a dash `plainCopy` could not rewrite would drop the line and the answer would fall back to `no-store`.

## Counts, executed by this reviewer at 41fdda5
problem **23** (bar floor 16) / `rebuild.yml`'s today step as written **164** / setup **157** / catalogue **43** / copy **36** / W6 **552** / journey **51** (PAGE_PINS unmoved and byte-equal to disk) / host **32** / w7 **19** / `b-package.cjs --ci --package B-NTC` run ALONE **exit 0**, worktree clean before AND after / `build.mjs` **PASS, 104 pinned inputs, build earned-a66db853886a**. The six files pinned on disk are byte-identical to the tip by sha256.
**The build id, recomputed independently**: I re-hashed all 104 inventory files with node's own crypto and rebuilt the digest - `earned-a66db853886a`, equal to build.mjs's full digest; the shipped `app.js` carries it exactly once and the placeholder does not survive; one changed byte moves it to `earned-5386dd23a589` and restoring moves it back.
**The privacy bar, my own probe**: planted `Dadworth`, `Zorbaflex press` and a `181.4` weigh-in in a REAL store, booted the REAL page over it and tapped the control. Zero leaks of any planted value, no `op-device-` id, no profile string, no 32-hex run, the device field eight hex, the eight fields in `FIELDS` order, no dashes, the clipboard handed the same string as the box, and ops **2 -> 2**: the control writes nothing.
**The clipboard**: success says "Copied. Send it to Joe."; absent and throwing both say "Select all and copy, then send it to Joe."; the box is open and pre-selected whole (0..257) in ALL THREE.

## Mutants: 9 killed / 2 survived
KILLED: E1 (label into a printed field) 22/1 by `R2`; E3 (full 32-hex id) 21/2 by `R4` and `R2`; E5 (threw but says Copied) 22/1 by `R7`; E6 (box dropped when the API is absent) 22/1 by `R7`; E7 (hard-coded build id) 22/1 by `R3`; E8 (em dash in a confirmation) 17/6 AND the build exits 1; plus three of my own - **Z1** the restore mark stops matching the client's sentence 21/2, **Z2** enrolment always answers `enrolled` 20/3, **Z3** the box dropped when the clipboard RESOLVES 22/1. SURVIVED: **E2** and **Z4**, which is condition C1. Not run by me: E4 (no clean anchor; `R6` plus my own 2 -> 2 op count cover it) and E9. Every file restored byte-identical, `git status --short` empty after each.

## Residuals
1. **iOS clipboard unverified, by design.** No iPhone has run this build (`A5-REPORT.md:524-528`); the unconditional box is what makes that acceptable, and hand-test row 3 is the only thing that can answer it.
2. **The gym card has no copy of this control.** The brief scopes it to Today; putting it on the gym card mid-set (hand-test row 2 reaches it only by navigating back) is a SECOND brief, not a fix here.
3. **CI**: `problem.test.mjs` is not in `rebuild.yml`'s enumerated today step and `.github` is editable only inside a re-pinning engine package (`:112`); it rides the next re-seal with the setup, catalogue and copy suites.
4. The restore-required binding is a PREFIX of the client's sentence, and a dash `plainCopy` could not rewrite would drop the status line; both would degrade to `no-store`. Guarded today by subtest 7 and by P1's own suite.
5. `build: earned-notinjected` is what an UNBUILT module prints (a node test, or the page loaded off disk). That is the documented truth, not a fault - but anything pasted from a non-built context will say so, and the PM should read it as "not the shipped page".

OWNER LOOK: pending (PM).
