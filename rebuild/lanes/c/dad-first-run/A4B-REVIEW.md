# A4B - INDEPENDENT REVIEW, ROUND 1
Independent (author != reviewer), effort HIGH, told to disagree; every claim re-executed, not read. Candidate `rebuild/lane-c-a4b` @ **e67fde6**, base **4ee62da** (`origin/rebuild/t2-client-core`, still the tip at fetch). Bar: `A4B-BRIEF.md` S26-S44 + mutants M21-M32, under `DECISIONS:125`, `:127`, `:129`, `:119`. Executed evidence, the day-count oracle, the band re-derivation, the catalogue sample and the mutant table: **`A4B-REVIEW-ANNEX.md`** (this file is the `:119 (6)` form).

## VERDICT: ACCEPT WITH CONDITIONS
No defect was found in anything executed. The three rulings `DECISIONS:129` made are matched by the build: the licence (1) is the custody as measured, the payload (2) is exactly `{profile, setup, tags}` closed, and the day-kind rule (3) is marked invented ON SCREEN with its intent carried in words and its citation in `split-kinds.mjs`'s header. One condition blocks the OWNER LOOK only; the rest are corrections to the brief, which the build was right to contradict.

- **C1 BLOCKING (the owner look, not the code).** The page served at `http://127.0.0.1:4178/` (pid 60960) is a STALE bundle: its `app.js` does not contain `COPY.screen2Why`, the `:129 (3)` intent line, though the source, the fresh dist and the suite all carry it (served 1429004 bytes, no match; fresh dist 1429563 bytes, match). The owner would judge screen 2 without the sentence the ruling requires. Fix: `node build.mjs` then restart `serve.mjs` before the look. Red-first: a probe that fetches the SERVED `/app.js` and asserts `COPY.screen2Why` is in it - RED against pid 60960 today, GREEN after the rebuild.
- **C2 NON-BLOCKING.** Brief 4.2's "always 8 lifts x 3 sets = 24 sets at every day count" is arithmetically false; amend the brief, not the code (judgment below). Red-first: a subtest asserting lower session size is 5 lifts / 15 sets at D_L >= 3 and 8 / 24 below it.
- **C3 NON-BLOCKING.** S31 and `:127 (3)`'s "stopping at the group is complete" cannot hold for `arms` and `legs`; the build is right to refuse. Because it departs from an OWNER ruling, it needs a REQUESTS line to the PM and a line in the owner-look note, not only a report paragraph. Red-first: the existing `S31 ... refuses to guess a bucket it cannot name`, which M30 turns RED.
- **C4 NON-BLOCKING.** Brief section 1 says "`today-bindings.mjs` is NOT touched"; it is (+11/-2), necessarily, because `save()` carries the payload. Within licence, but the contradiction is undisclosed: name it in the report. Red-first: none needed, a disclosure line.
- **C5 NON-BLOCKING.** The F1 sentence renders `Earned's` with a straight apostrophe while every other string on the same screen uses U+2019 (`Earned's suggestion`, `Earned's own rule`). It is verbatim from `:125 (2)`, so the PM or the owner decides; flag it at the look.
- **C6 NON-BLOCKING.** S44, the hand test, is a human run through both doors. Not performed by this reviewer and not substitutable.

## Counts, executed by this reviewer at e67fde6
setup **145** (was 104; +41, the bar asked for 40) / catalogue **43** (bar: 34) / today **64** / copy **36** / gym **64** / checkin **28** / W6 **552** / journey **51** / A0 host **31** / w7-preview **19** / `native-carriers-package.cjs --ci` **PASS** with the worktree clean before AND after / `build.mjs` **PASS** at **102** pinned inputs, "no em/en dash in any text the athlete can see".
Zero regressions: every S43 base row is unchanged. Four msedge checks **PASS** with verified real `taskkill /F /T`: `setup-check.mjs` (**6** kills, one MID-FLOW leaving zero operations and no partial athlete, screens 2 and 3 in three states each, 320px, and A4's C1 landing sentence still shown), `browser-check.mjs`, `gym-check.mjs`, `checkin-check.mjs`.

## Mutants: 16 killed / 0 survived
M21-M32 all killed, plus four of this reviewer's own: **R11** the starter week under-fills a major (S32, 9 red), **R12** a catalogue `lend` of 1.5 (S29), **R13** tags need not match the document's ids (S35), **R14** the starter week invents a load (S33). Every file restored byte-identical; `git status --short` empty after each and at the end. M31 turns the suite AND the build red (`AI_DASH_IN_BUILD`), M27's `head` on a document exercise takes 52 subtests down through `closed()`.

## Custody
CLEAN. 18 paths: the four `dad-first-run` docs, the three new modules, `test/catalogue.test.mjs`, `test/setup.test.mjs`, the four A4 `setup-*` modules, `build.mjs`, `design.cjs`, `today-entry.mjs`, `today-bindings.mjs`, `local-today-journey.test.mjs`. Nothing under `rebuild/engine`, `rebuild/client`, `rebuild/conform`, `rebuild/m4/**`, `rebuild/m3/w6/host`, `.github`, `src` or `ledger`; `screens.template.html` and `today-app.cjs` are licensed and untouched.
The three files outside the brief's own lists are all WITHIN LICENCE: `today-bindings.mjs` and `today-entry.mjs` under `:106 (b)` / `:111` (lane C exclusive and the one-store wrapper), and the journey re-pin under `:106` with the pin's own failure message instructing it - **the other three PAGE_PINS are byte-identical** to A4's, which is the check that the wrapper files were not disturbed. See C4 for the brief sentence this contradicts.

## Judgment 1: the builder's disagreement with brief 4.2 - THE BUILDER IS RIGHT
Brief 4.2's normative parts are its two lists and `lifts(major, D) = 2 if D <= 2 else 1`. Its closing clause does not follow from them: read off the catalogue by this reviewer, four of the five minors are UPPER (`delts_front` U4/L0, `delts_rear` U3/L0, `biceps` U6/L0, `triceps` U5/L0) and exactly one is LOWER (`abs` U0/L9). So at D >= 3 an upper session is 8 lifts and a lower session is 4 majors + `abs` = **5 lifts / 15 sets**, which my own re-derivation prints at D=6 and D=7. The alternative - a second lift on a lower major at D_L >= 3 - would put those buckets at 18 sets, **HIGH**, outside the band section 4 exists to hit. The build implemented the rule and pinned it; **the brief's sentence is wrong and 4.3, which is per bucket, is unaffected** (every cell reproduced from `volume.cjs:74-83` and `constants.cjs:327`, the two honest LOW rows included, no major ever UNDER).

## Judgment 2: S31 and `CUSTOM_REGION_REQUIRED` - THE BUILDER IS RIGHT, THE BAR'S SENTENCE FAILS
Executed: `chest -> mg=chest`, `back -> mg=back`, `shoulders -> mg=delts`, `core -> mg=abs`, all with `head: null`; `arms` and `legs` refuse `CUSTOM_REGION_REQUIRED`. They have no single engine label - the eleven split arms into biceps/triceps/forearms and legs into quads/hams/glutes/calves. Honouring the sentence literally would mean storing a coarse `"arms"`/`"legs"` (exactly the seven-group invention `DECISIONS:115` struck out, and a bucket `volume.cjs:74` knows nothing about) or picking a member for him. Asking for the part is the only honest option and costs one tap on two of six groups. **S31 and `:127 (3)` asserted a property their own six-group list cannot satisfy.** Because it is an owner ruling, C3 routes it rather than leaving it as a report reading.

## Residuals
1. CI: `test/setup.test.mjs`, `test/catalogue.test.mjs`, `setup-check.mjs` and P1's copy tests are not in `rebuild.yml`'s enumerated today step; they ride the B-NTC seal (`:112`, `:117 (4)`).
2. F1 (engine, lane B): a two-day athlete's majors sit at 6 a week, `VOL_BANDS.floor`, not in band, and the screen says so. No twelve-lift workaround was attempted, as `:129 (3)` requires.
3. F2 (engine, lane B): `tags` is stored and nothing reads it; `INDIRECT` is still keyed to the owner's four ids, so a fresh athlete earns none of it.
4. H3 and H2 carried from A4, untouched; A4's S19 sentence still on Today and still shown after this build.
5. Screen 3 after "Build my week for me" is ~8300px in an 842px viewport (~10 screens of scroll) at 390px. S41 permits it and the report names it; the owner should judge the length in the pane.
6. Brief 3.1's "two primary actions and nothing else to read" is looser in practice: the standard-start block and the empty day lists sit beneath the doors. S34 passes; the shape is the owner's to judge.
7. The branch was cut from the tip and the tip had not moved at fetch; the integrator re-pins and re-runs.

OWNER LOOK: pending (PM).
