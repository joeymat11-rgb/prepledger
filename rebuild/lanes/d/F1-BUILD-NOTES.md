# F1 common machinery candidate notes

Lane D author notes; not a review, acceptance, receipt or seal.
Authority: PM DECISIONS:148; base 964f183 (A4b integrated, H3/B packages pending).
BRIEF-F1-FULL-BODY v0.1 remains frozen as the submitted proposal; these notes record its implementation.
The PM authorized common machinery and section 5 custody; the starter set count remains provisional.
No v1.0 by-name acceptance is claimed before the owner's starter answer.

## Implemented scope

- `plan.cjs`: F is a session kind; exercises stay U/L. Stable family orders interleave U then L; no new identity, persisted F order or schema.
- `today.cjs`: active U/L pools compose once, configured set slots remain, missing active family refuses by name, one structural main/rider budget remains.
- `athlete-state.cjs`: F covers both families; malformed spellings, F exercise identity, duplicate ids and one-family F setups refuse before construction.
- `volume.cjs`: query each Monday-to-Sunday date; F gives each family one exposure. No-F branch retains the accepted fixed-week behavior.
- `writers.cjs`: F frequency prices proposal consequences, aggregate direct bucket sets obey the existing cap of 8, and session bookkeeping/advice labels recognize F.
- `energy.cjs` and `sleep.cjs`: F counts once as scheduled training and receives the existing body-alarm advice; thresholds and historical refeed paths are unchanged.
- F proposals preserve the chosen per-session delta when filing their consequences. The pre-existing no-F sweep delta discrepancy remains outside this package.

## Clarifications to the proposed brief

F1-12 lives in `fiveLevers`. Its numerator is the rolling seven logged dates, so the F denominator classifies those same seven dates, including effective split changes. A Monday calendar denominator would mismatch the numerator. The no-F output remains unchanged.

The aggregate cap limits adaptive additions only. An athlete's supplied workload is neither truncated nor silently reduced. The proposed 32/24-set starter is not installed by this engine candidate; Lane C owns the setup companion and the owner has not chosen its starter standard.

F1-02b intentionally requires B2/D9's latest-effective split selection. Current base still picks the last inserted effective entry. Its RED result must remain visible until the accepted predecessor supplies the fix; no F1 exception or skipped test is permitted.

The initial prototype's three parent witnesses now compile only allowlisted source blobs at frozen 964f183. It remains historical arithmetic/rejection evidence, not product proof. Actual product proofs live in `f1/product-laws.test.cjs` and the public native journey.

## Handoff and remaining gates

Lane B owns F1 runner admission, the eventual predecessor pins, inherited-gate successors, public CI registration and the private FULL verdict. Lane D reads no private paths and supplies no census claim from synthetic examples.

The documented `rebuild/lanes/tooling/preflight.cjs` is absent at base 964f183 and in the lane tooling search. Request its executable before PR-READY. The PM supplies the independent Claude MAX reviewer; Lane D's builders and proof authors are not independent review.

Rebase onto H3, B1+B2 and B4+B3 in the ruled order before the final seal. Re-run the unchanged failing date-selection cell and native continuity after that rebase. Formal brief v1.0, F1-14/F1-15 setup proof, F2 tag projection, full census, review, receipt and authorized rerun remain open.
