# S3 production replay provider: exact source dependency note

2026-09-13 · Lane D · PM's finite follow-up to e7e452e; documentation/static public source only, <=60lines. Reference M=100820aa47a4f8729642033499eaec0f0ee282e1; fetched current=1343aaa43ba95fa2f62953400d1df421ed11a001. The named import/engine/Today-entry+build/workout-host areas have no M->current diff. No candidate, engine, test, browser or oracle was executed; no installed/private/retained/soak data was read.
Read the specifically named public roots and their already licensed public dependencies. Stop edges remain seed.cjs, source history/ledger, installed/oracle-shim runtime and other checkouts; none was opened. D610f6dc reproduction/order contract, e7e452e harness map and E ff17d333 registry remain unchanged. This note names one production provider seam, not a new family/precedence rule or implementation grant.
Prefixes: I=rebuild/m4/import/; Egn=rebuild/engine/; W=rebuild/m3/w6/; Tdy=rebuild/m3/w7-preview/today/; M4=rebuild/m4/workout/; H=rebuild/m4/import/test/s3/. Line references below are actual M source lines, not truncated-output positions. Proposed names and error codes are prospective.
## What the real callers do

I/prepare.cjs14-65 requires SCHEMA_V, migrate and dataLossGuard; mergeState is required only with localBytes. It snapshots/parses BOTH original byte inputs first; source and optional local must each have integer v in3..SCHEMA_V. Less than3/future refuse before an engine call. No caller supplies a fresh seed.
The exact sequence at prepare44-54 is migrate(clone(source)); optionally migrate(mergeState(migratedSource,clone(local))); strict JSON round trip/current-schema check; then dataLossGuard against EACH untouched preimage. Preserve that argument order. merge.cjs856-860 names its arguments (local,remote) and spreads remote then local, so the first, migrated-source argument supplies the scalar v=60. The optional older local input is NOT independently patched; do not change that behavior under a provider fix.
Egn/constants.cjs9 has SCHEMA_V=60. migrate.cjs2125-2172 heals existing containers, settles v60 in place, and for3<=v<60 clones then applies PATCHES with n>v before the same settle. The null/v1/v2 full-SEED branch is outside prepare's admitted domain; the raw engine's future-schema identity return does not override prepare's future refusal.
I/reading-replay.cjs59-62 repeats that preparation and requires EXACT candidate_json byte reproduction. Its139/151 calls applyRead(state,date,quantity,{hour}), writeDaily(state,date,values) and currentRate(state);267 reproduces ancestor/local merge again. Source preparation uses the bound context.clock day/noon; factual replay uses the original effective hour/date; final calculation uses asOf/noon. Stored candidate, saved coverage and prior derived cache are comparison targets, never replacement inputs.
Thus the minimal engine facade has exactly seven members: SCHEMA_V plus migrate, mergeState, dataLossGuard, applyRead, writeDaily, currentRate. It does not expose completeSession, runAdaptive, applyProposal, arbitrary engine methods or isPristineSeed. Native workout reading/capture remains the already named M4 projector/provider seam, not a historical command replay.
## Factory loading versus actual data reads

index.cjs requires seed.cjs when its factory array loads; that alone makes importing index unsuitable. Requiring public migrate/merge does NOT import seed: both export factories, and migrate captures E.SEED at construction. sleep captures E.HISTORY; energy captures E.ROLLUPS; writers captures E.SEED. Capturing an opaque dependency reference is different from dereferencing its data.
The current-schema settle closure (migrate2082-2122), mergeState and dataLossGuard use actual supplied state and the public normalizers/reconcilers. The six exposed method bodies and their inspected public call paths do not read HISTORY/ROLLUPS or call isPristineSeed/runAdaptive. applyRead424 uses readWindow/date/state; writeDaily2822 uses proteinTarget/proteinHit and state; currentRate225 uses state reads/weekly/model and dates. No recorded dataset is needed merely to load these factories.
Actual SEED readers: migrate553/566/576/583/607 (patches5/6/7/8/10); patchV60 at1250; excluded fresh/v1/v2 migrate branch; isPristineSeed2176; writers/runAdaptive1987. isPristineSeed compares SEED's reads/sleep/dailyLogs/sessionLog projection, but dataLossGuard2263 does NOT call it. Do not import that private projection to implement the guard.
Actual HISTORY readers are sleep/labAnalytics414 and labAnalytics2:1144; ROLLUPS is spread by energy/stepEfficacy1175, also reachable through stepPush/lab analysis. They are not required by the replay facade. Existing Today projection calls nowModel/statusFace/currentRate/calorieTarget/proteinTarget/marchingOrder/readRecency plus genSession, and the inspected call paths do not enter those dataset readers; keep runtime reach controls because a future call expansion could change this.
Critical catch behavior: patchV60 wraps its corrections in try/catch and sets s.v=60 afterward (1299-1301); patchV51 catches its draft scan (1700-1706). A thrown missing provider can therefore yield an apparently migrated partial state. An empty SEED, empty historical arrays, undefined fallback or throw-only stub is insufficient: missing-dependency use must be remembered outside the engine and checked after its return.
## Exact data-dependent schema reach at M

| Actual migrate input | Public algorithm/data requirements beyond the source itself | Honest result with today's bare build/day context |
| --- | --- | --- |
| v60 | Container healing and _settleExit only; no SEED projection. | Candidate for reproduction through the real algorithms, subject to complete clock/build/context and ordinary preparation guards; not a runtime PASS. |
| v10..59 | Every chain includes patchV60, whose normal path reads SEED.insertions.fly and .hipthrust, including known absence; earlier input versions add the clock/draft requirements below. | Missing verified seed insertion provider => dependency-required, original retained/pending. Source insertions are what this patch CORRECTS, so they cannot supply the reference. |
| v8..9 | Above, plus patchV10's ordered SEED.exercises id/mg entries. | Require the exact producer's verified projection; matching current source labels/IDs is insufficient evidence of those reference values. |
| v7 | Above insertion requirements, plus patchV8's id/lastMeta fallback and patchV10's id/mg. | lastMeta from actual source history is used by the unchanged algorithm when available; any reached seed fallback requires verified reference evidence. |
| v5..6 | Above, plus patches6/7's id/n/setup entries as applicable; patch8 and10 follow. | Exact ordered reference projection required for reached patches, preserving field absence and values. |
| v3..4 | Above, plus patchV5 can copy FULL missing exercise objects in seed order and use SEED.exOrder when source exOrder is falsy. | A subset of guessed labels/weights is not enough. Full required reference objects/order are presently missing. |
| malformed / v<3 / v>60 | Existing strict parser and preparation schema rules. | Existing refusal, unchanged; never route these to a synthetic/fresh seed. |
This matrix applies to actual migrate arguments in the exact prepare sequence, not an invented extra migration of localBytes. In particular source60/local59 still reaches a v60 second migrate because the source argument wins v; retain that control and actual merge/data-loss behavior. Do not quietly turn a provider into a migration-order repair.
Additional reached inputs: source<=50 can execute patchV51's ordered drafts.length/key(i) scan; index29 has an explicit empty-drafts default, but the selected producer must be proved to have used it before reproducing it. Only key names/order are consulted, not draft bodies. Conditional corrections in patches44/52/53 call _stampCorr and need the exact clock.nowISO() result (1084/1598/1533/2493), including repeated-call behavior; a day label alone does not establish that value.
No identity mint is needed by the inspected replay-method paths: anchorDexa/_freshId and completeSession/proposal writers are not this facade. Keep IDs as an explicitly unavailable capability with an outer failure witness; never install a random or synthetic fallback. Any future reached capability is a named unmet dependency, not an invitation to guess a value.
## What source/context supplies and what it does not

Controller-verified source_json/local_json supply the exact athlete state, programme, readings, sleep, daily/session stores, existing fields/absence and original bytes. The checkpoint/current operations supply actual native facts and captured effective tuples. A source-owned exById(s,id)=s.exercises.find(e=>e.id===id) reads those supplied exercises without any seed lookup. Missing state facts retain the actual engine's guarded/refusal behavior; no synthetic basis is introduced.
engine_context_json currently proves only the checked build/day fields to reading-replay, within the separately authenticated material. Exact public algorithm/build correspondence must be established by the reviewed producer/profile mapping, not by comparing an arbitrary build label or equating the new provider hash with the old whole-engine tree hash. Q's existing material/engine/interpretation commitments must bind that mapping and every actual dependency input; no new Q field or fabricated receipt is needed.
Clock is a real portability dependency: Egn/dates8/11/23 uses host-local Date constructors/getters and elapsed milliseconds, so DST/realm timezone can affect the same dated source. Bind the preparation clock, original replay tuple and declared calendar zone to the actual producer context. A verified deterministic producer clock mapping can derive exact methods; otherwise a verified transcript/context is needed. Never infer nowISO from candidate corr.at or use the current phone clock to reproduce old preparation.
The existing C2 oracle metadata can identify its named clock/zone, but an oracle setting alone must not be relabeled as proof of a different preparation provider's clock. For this minimal provider, require a verified matching JS calendar realm; absent/incompatible zone is an explicit context refusal. Do not silently substitute UTC, a fixed offset, a synthetic8am/noon timestamp or a newly written calendar algorithm. Cross-zone support would need a separately named algorithm/context change.
Missing reference data remains missing: source.exercises is not SEED.exercises, source.insertions is not SEED.insertions, source.dailyLogs is not HISTORY, and source.weekly is not ROLLUPS. The last two are unnecessary for this facade. For old schemas, a future producer-bound reference projection/explicit default or absence witness could supply the exact required inputs; there is no such general projection in the presently checked build/day fields. This note does not authorize extracting it or altering the C2 file.
The advertised preparation domain3..60 is not changed here. The provider must expose a dependency-required result for a concrete unreproducible input, with the schema/reason visible in the construction/report; it must not announce all legacy imports supported. If PM elects a narrower initial delivery, that is an explicit scope ruling, not a silent v60 filter. Existing source/candidate/checkpoint and all native records stay retained until faithful reproduction is possible.
## Smallest proposed production binding and exact file delta

New I/engine-provider.cjs, sole export createSourceReplayEngine({engineContext,clock,dependencies,nativeTrendContext}), returning the frozen seven-member facade above. Instantiate a private engine table with exact public factories; forward calls unchanged, preserving input mutation/return semantics required by prepare. Dependencies are controller-owned, immutable, provenance-bound capabilities, not untrusted payload-supplied functions or a test engine.
Static factory order: Egn/{dates,constants,performed,plan,progression,sleep,energy,policy,today,volume,migrate,earn,merge,writers}.cjs. Egn/entered-load.cjs is the existing predicate required by performed, not a factory supplying engine exports; this clarifies the15-module shorthand in e7e452e. No dynamic require/glob/compiler. Reuse every algorithm byte; index/seed remain excluded. These15 public M modules total1063843bytes; lexical 'path space SHA256 LF' inventory digest is65be42aa6737ddb396c0229fb0c17bff158e2d573f8887ada0f6621b94c8b626 (metadata only).
Install opaque unavailable SEED/HISTORY/ROLLUPS capabilities before factory capture, not empty datasets; data access records a sticky, instance-owned missing-path flag and throws. Do the same for absent clock/draft/ID capabilities. Every facade call checks that flag after success OR caught engine failure, rejects any partially produced result and leaves the poisoned instance unusable. A fresh verified context needs a new instance/Q; an engine catch cannot erase the evidence.
Before known old-schema execution, identify required seed/draft capabilities; allow only exact producer-bound projections with the required property-presence/order semantics. For conditional reads, the sticky witness closes caught fallbacks. Return fixed proposed SOURCE_ENGINE_DEPENDENCY_REQUIRED or SOURCE_ENGINE_CONTEXT_UNPROVEN at the S3 admission boundary, with no source values/error prose. Existing Node prepare/replay fixed-error mappings remain unchanged; their consumers must not promote a generic migration failure to completion.
I/browser-replay.mjs imports this production provider and the already proposed W/local/source-platform.mjs; I/replay-core.cjs still performs the actual preparation/replay and candidate comparison. C's named import-host/Today-entry/today-model/today-bindings bind only the resulting verified source view to both consumers. No H/engine.cjs/fixture import, planted cache or client-provided Q can reach the production entry.
Tdy/build.mjs needs an explicit successor exception for only I/engine-provider.cjs and the previously named four pure I modules, plus Egn/migrate.cjs and Egn/merge.cjs only via this provider. Check the actual browser importers and exact source pins, not only an output substring. Keep index/seed/history/private/engine-tests/H paths and Node facades forbidden. W/build-browser.mjs and its two-importer Node-crypto rule need no change.
Existing M4/engine-runtime.cjs and W/host/engine-runtime-host.cjs stay the separate narrow gym reader compositions. W/host/workout-host.mjs65/163-168 remains the protected host/source-registrar seam; its new local-profile binding is already in B's inventory. Rebind real engineState and native-trend-context to the exact reproduced source/current native facts for Start, previous performance, continuation and resume. Do not export the migration table into the gym or make unavailable context look clean.
Exact incremental files beyond e7e452e: new production I/engine-provider.cjs; new I/test/engine-provider.test.cjs; proposed edits to the already named I/browser-replay.mjs, I/replay-core.cjs, W/local/source-admission.mjs and Tdy/build.mjs; add Tdy/test/package.test.cjs for the specific build-boundary successor controls. C/B must inspect that test's current assertions before the licensed edit; preserve them and add exact allowed-provider/forbidden-neighbor cases. Runtime inventory would be30,11new; no public engine factory edit is proposed.
B reconciles those deltas into the already named rebuild/m4/spec/{s3-portable-harness.cjs,s3-portable-sources.json}, source/helper/test pins and current command inventory; ancestor/shared-edit/R1 pins stay intact as in e7e452e. Add literal rebuild/m4/import/test/engine-provider.test.cjs to its Node-suite argv; package.test.cjs already has a named Today CI home. No new workflow, package, lock, C2 script or broad source grant follows.
## Required future parity and refusal controls

Run both original Node preparation/replay and the actual production provider/core over identical unmodified source/local/context bytes, not only H's synthetic facade. Compare complete candidate JSON bytes, guard results, source preservation, mutation/alias behavior and each read/daily replay step/calculation; verify source60/local59 order and repeated merge. Run existing hosted/v1/v2 tests unchanged. Synthetic schema fixtures exercise every matrix row and every conditional provider use; missing inputs are explicit refuses, never skipped positives.
Include a patch60 witness where the inner catch still returns v60 after a missing SEED read; the outer provider must refuse. Mutants removing the sticky check, supplying empty seed/history/drafts, copying candidate insertion/correction outputs into dependencies, skipping source reproduction, changing migration argument order or accepting an unverified build/zone must die by named assertions. Controls must distinguish real algorithm failure from missing-provider failure without exposing values.
Real-source parity, only through the separately authorized actor, must compare the gate-produced C2 preparation with this exact provider at the selected engine/context; cover current-schema settle, older-schema paths with genuinely supplied reference evidence and explicit refused paths where absent, both with/without local bytes. Check matched timezone/DST boundaries and wrong-zone refusal. Corpus equivalence or a synthetic15-module hash alone is not real-file reproduction.
Then retain P1's actual file/controller review+factual prefix answer, completed workout BEFORE import, imported-history-dependent measured Today and actual gym/previous performance, later native descendant, force-kill/reopen and byte preservation exactly once. Producer-bound context changes invalidate Q and guidance through the existing contract. Real-C2 actor/output exception remains the previously disclosed point-of-use prerequisite; no new authority to run it is requested here.
STATUS · D · S3 PROVIDER NOTE READY · Static M/current trace names engine-provider.cjs, seven-member facade, actual seed/schema/clock dependencies, caught-failure guard and exact build/test delta. No runtime or data execution; faithful real-source parity and PM/B custody remain required.
