# S3 provider context map: authenticated fields and reviewed derivations

2026-09-13 · Lane D · DECISIONS220 finite source closure. M=100820aa47a4f8729642033499eaec0f0ee282e1; fetched current=3dd77e0f573db8afb1eb36cd8e5eee6797ae0183. The eight producer/decoder/replay roots compared have no M->current diff. Static public code only; no candidate/module/CLI/test/oracle execution or seed/history/private/fixture/installed/retained data read.
Companion to [production-provider note](S3-PRODUCTION-PROVIDER-NOTE.md) at ba8c648 and [portable harness](S3-PORTABLE-HARNESS-BRIEF.md) at e7e452e; those files and D610f6dc remain unchanged. P=rebuild/m3/setup/port/; E=rebuild/engine/; I=rebuild/m4/import/; W=rebuild/m3/w6/; T=rebuild/m3/w7-preview/today/. All source line numbers below are at M.
Checker names below are proposed construction details within already named files: A=W/local/source-admission.mjs internal verifyProducerContext; L=I/local-source-profile.cjs exact context/interpretation encoding; Ptf=W/local/source-platform.mjs calendar/raw-byte helpers; Prov=I/engine-provider.cjs createSourceReplayEngine; Core=I/replay-core.cjs. None exists as a completed implementation or current acceptance claim.

| Input / actual reach | Authenticated producer field or reviewed derivation | Exact verifier / honest unavailable outcome |
| --- | --- | --- |
| Original inputs and comparison target | P/port493-506 seals source.sha256+base64 bytes, optional local equivalent, migrated.sha256+state, engine and oracle inside AES-GCM plaintext; AAD is only [bundle profile,source SHA] (194-204). All plaintext fields are tag-authenticated. Envelope sealedAt is outside AAD/plaintext and is not an input witness. | Existing W/local/import-bundle.unsealBundle257-290 checks GCM, raw source/local hashes, JSON.stringify(migrated.state) hash and qualifyBundle; reference P/unseal54-89 checks GCM/profile/AAD agreement only. A must reload authenticated custody and match exact material/identity; Core repeats preparation and compares complete candidate bytes. Existing authentication/payload/qualification refusals remain; tag validity authenticates sealed content, not which executable actually ran. |
| Durable context versus today's build/day check | W/local/import-bundle.custodyMaterial386-392 retains profile,bundleProfile,createdAt,engine,sourceSha256,migratedSha256,oracle,dataLoss,census,local SHA in engineContextJson. It does NOT manufacture reading-replay's top-level build/clock. I/reading-replay39-45 checks only its separate context.build/importBuild and context.clock. | A parses that retained context and original bytes; L derives an immutable checked replay-context object from the rows below while retaining the original context string exactly. Core consumes only this controller result. Never rewrite custody to pretend C2 emitted build/clock, trust a caller's normalized object, or accept current importIdentityOf's smaller tuple as all provenance. Missing/mismatched mapping => SOURCE_ENGINE_CONTEXT_UNPROVEN. |
| Algorithm/build correspondence | Authenticated engine.{sha256,treeSha256,schemaV,path}. P/engineDigest221-225 hashes selected entry bytes; treeSha256 is SHA256 of sorted sibling *.cjs lines 'basename space rawSHA', joined by LF with NO trailing LF. It is not recursive/transitive, and does not bind port.cjs, prepare.cjs or the strict parser. P464 measures after preparation/gating. No producer-script digest or runtime version is sealed. | A matches exact entry+whole-tree+schema+path to a B-reviewed producer mapping and exact public factory closure; L binds that mapping's complete content/source pins, not a free build label. Prov/build pins must match the mapped algorithms. The ba8c648 15-public-module digest is a DIFFERENT inventory, never equal-by-assumption to treeSha256. Actual accepted whole-tree mapping and corresponding production-run/output evidence remain unmet here; no read of private modules to reconstruct them. Unknown/arbitrary --engine, missing mapping or source-at-run correspondence => context-unproven. |
| Default producer construction | P366-375 checks conflicting process MEASURED_TEST_NOW/TZ, then sets both to GATE.clock='2026-09-03'/tz='America/New_York' BEFORE requiring the selected engine. Default path is E/oracle-shim.cjs. Shim3-25 invokes index.createEngine({clock,ids}) with no drafts. P421-425 prepares through that actual table; the gate receives the same selected engine path. | A's reviewed mapping must identify this construction, using authenticated engine entry/tree/path plus the exact authorized producer/output correspondence; matching a path or PASS string alone cannot establish it. P's exported seal() can seal other payloads. No port/build identity attestation is added by AES-GCM. Existing real-C2 evidence channel must supply the missing execution correspondence at point of use; absent => context-unproven, not another owner question or new output grant now. |
| Preparation day | Authenticated oracle.gate.clock and .tz are emitted from the SAME GATE constants used in the parent process (P478-486), so under the reviewed default construction they describe preparation as well as its gate children. | A validates exact profile/mapping and real calendar day, requires mapped GATE constants; Ptf constructs the checked day. It must not treat gate-child labels as the clock of an arbitrary --engine. Changed/missing day, unproved producer or ignored environment => context-unproven. Default day derivation is now named, not intrinsically missing. |
| nowISO, nowMs, hour, dow and repeated calls | Shim9-18 constructs ONE noon=new Date(year,month-1,day,12,0,0,0); today returns anchor, hour/dow return that noon's local getters, nowISO returns its ISO string and nowMs its epoch. Repeated calls reuse the same noon, with no advancing counter. Conditional migrate patches44/52/53 call _stampCorr2493. | Ptf derives these exact methods only after A qualifies day/construction/calendar realm; Prov installs this controller-owned frozen clock for preparation. No separately recorded timestamp/transcript is necessary for this verified default derivation. createdAt (real wall time), envelope sealedAt and candidate corr.at cannot supply nowISO. An alternate engine's missing clock/call semantics => context-unproven; a reached unavailable clock poisons Prov and yields dependency-required. |
| Gate preload versus preparation clock | P/runGateMode sends --import tools/_fixed-now.mjs only to its frozen child; unfrozen child omits it. _fixed-now54-78 replaces zero-argument Date/Date.now while explicit Date arguments retain native behavior. Parent run() does not itself install that preload. Shim's explicit noon remains explicit. | A retains both gate modes as evidence of the actual run but never fabricates a frozen parent Date. The reviewed producer mapping/evidence must cover actual parent invocation/ambient preloads if relevant; these are not separate sealed fields. Ptf follows shim's injected clock and native explicit-date semantics. An unreviewed ambient override or arbitrary engine using an unbound wall clock => context-unproven. |
| Calendar zone and runtime behavior | Authenticated oracle.gate.tz='America/New_York' plus reviewed parent setting establishes intended default zone. E/dates8/11/23 uses local constructors/getters and elapsed milliseconds; DST behavior belongs to native Date. C2 does not seal Node/ICU/tzdata versions or an offset table. | Ptf verifies the actual replay realm's resolved zone and mapped native-Date behavior; A requires B/P1's reviewed calendar compatibility evidence for the reached dated calculations, beyond a matching label or one noon probe. Missing/incompatible zone/runtime evidence => context-unproven. No UTC/fixed-offset substitution or new calendar engine. Ptf cannot set a browser's system zone; cross-zone execution stays explicitly unavailable under this proposal. |
| Factual replay clock / final calculation | I/reading-replay139 uses each authenticated original effective date/hour for applyRead and date/values for writeDaily;151 calculates currentRate at independently bound asOf/noon. Those tuples come from original native facts/interpretation, not the C2 preparation day. | A validates original family payloads/effects and binds asOf/currentness under D610f6dc/E registry; Core keeps preparation, each fact and final calculation clocks distinct, Ptf uses the qualified calendar semantics. Missing effective context retains the existing family/context refusal. No fixed8am, preparation-day overwrite or correction timestamp inferred from candidate. |
| Draft default and ordered scan, only if reached | Shim25 omits drafts; index29 therefore supplies Object.freeze({length:0,key:()=>null}). This is a VERIFIED CODE DEFAULT for the mapped default construction, not an empty substitute. migrate1700-1706 reads only length/key(i) in order, catches errors; no draft bodies. Arbitrary --engine may inject a different storage-like object; C2 has no draft/transcript field. | A qualifies default construction; Prov supplies that exact zero-length/key-null default only then. For a genuinely different producer, require its reviewed deterministic derivation or producer-bound length and complete ordered key-call results, preserving absence/repeated-call behavior; unavailable reached scan => SOURCE_ENGINE_DEPENDENCY_REQUIRED. Do not read phone drafts, guess an empty list or demand older-schema draft inputs for v60. Sticky outer witness defeats the caught scan. |
| SEED.insertions.fly/.hipthrust, patch60 in source v3..59 chains | E/migrate1250 reads the reference (including actual known absence) after some mutations; source insertions are correction targets, not reference evidence. Default index6 loads seed privately; engine.treeSha256 commits sibling bytes but neither reveals the projection nor proves an extracted value. There is NO reference projection in sealed C2 metadata. | A/Prov require producer-bound exact reached values/property absence and correspondence to the reviewed engine reference. Evidence currently unavailable: dependency-required for that concrete reached path. Never candidate-derived dates or an empty SEED. Catch1299 then v60 return1301 must still poison/refuse outside the engine. v60 settle does not read this projection. |
| SEED.exercises id/mg order, patch10 for source<=9 | migrate609 scans the reference in order and conditionally assigns mg. C2 seals source.exercises, not these reference entries. No field/derivation supplies the reference data in this read scope. | A/Prov need exact producer-bound ordered IDs and mg presence/values, including duplicate/lookup behavior; unavailable reached projection => dependency-required. No inference from source labels or current candidate. Applies only when this patch actually runs. |
| SEED.exercises id/lastMeta fallback, patch8 for source<=7 | migrate584-592 first uses existing lastMeta or latest actual session record, otherwise finds matching reference id and optional lastMeta. No corresponding C2 reference field exists. | A/Prov require exact producer-bound lookup/absence and fallback objects only where reached; use the unchanged source-history branch where it suffices. Missing reference on a fallback => dependency-required, never copied candidate lastMeta. Other reached later patches keep their own requirements. |
| SEED.exercises id/n/setup, patches6/7 for source<=6 | migrate567-577 walks reference exercises and overwrites matched source n/setup. No C2 reference field; source n/setup are inputs to be corrected, not proof of the frozen values. | A/Prov require producer-bound ordered reference IDs plus exact n/setup and absence semantics for applicable patches; unavailable => dependency-required. No guessed exercise catalogue. |
| Full missing SEED exercise objects / exOrder, patch5 for source3..4 | migrate554-557 can splice full missing objects at reference order positions; falsy source.exOrder uses the reference exOrder. Neither required reference objects nor order is carried as a C2 dependency projection. | A/Prov need producer-bound full copied objects, exact enumeration/order and exOrder when reached; a fields-only approximation is insufficient. Missing => dependency-required. No private extraction is sought by this table. |
| Non-reached datasets/capabilities and schema60 | Inspected seven-member facade's current-schema settle/merge/read/daily/rate closure requires no SEED/HISTORY/ROLLUPS data or ID mint. Capturing opaque references at factory construction is not reading them. | Prov keeps unneeded capabilities unavailable with sticky outer detection. A must not demand older-schema reference/draft evidence just to evaluate current60. Any unexpected later dereference is an explicit dependency-required result, not silently granted reach. No all-legacy support claim. |

Concrete current60 construction is therefore explainable: authenticated original material + reviewed exact default producer/algorithm mapping + qualified matching calendar realm => derive the parent day/noon clock and any default drafts from source, compose the real public factories, and reproduce/guard/compare full candidate bytes. Older seed values are not prerequisites. This is an input contract, not proof that a particular real bundle or browser has yet met those conditions.
Preserve prepare's actual sequence: migrate(clone(source)); if local exists, migrate(mergeState(migratedSource,clone(local))); strict round trip and guards against BOTH originals. source60/local59 still gets scalar v60 from the first merge argument; no independent local59 migration is inserted. This is a core order control; C2 CLI's separate local-lineage check may refuse that pair before preparation.
Q.material_digest continues binding exact original source/candidate/local/context bytes and Q.checkpoint_digest binds the checkpoint; L binds the full verified construction/dependency/calendar mapping and any authorized evidence identity into existing Q.engine_digest/interpretation_digest with explicit canonical/domain encoding, and Core uses only the A-owned result. No new Q field, hosted receipt, caller-authored qualification or candidate-supplied provider. Changed material/mapping/context invalidates Q. An attested digest alone is not usable missing reference data.
SOURCE_ENGINE_CONTEXT_UNPROVEN and SOURCE_ENGINE_DEPENDENCY_REQUIRED remain fixed prospective admission codes; expose schema/reason enum and pending status, never source values. Prov checks sticky faults after success OR thrown/caught engine failure and keeps the instance poisoned. Original bytes/native facts/candidate comparison target remain retained; no completion or guidance is published from partial reproduction. Existing Node facade error mappings remain unchanged.
Incremental runtime path count stays30/11new: A and L implement the context verifier/encoding, Ptf implements verified clock/calendar helpers, Prov enforces dependencies, Core/browser-replay accept only checked inputs; all are already proposed. No additional runtime path, C2 producer/decoder/custody edit, external data file, package, lock or workflow change is necessary to express the available/default versus explicitly unmet inputs. B folds exact mapping/source pins into its existing s3-portable-harness.cjs/s3-portable-sources.json successor and the named T/build boundary.
New cases go in already proposed I/test/engine-provider.test.cjs, W/test/local-source-admission.test.mjs, I/test/browser-parity.test.mjs and harness mutants: genuine default-clock/draft positives; repeated nowISO equality; altered day/zone/path/tree/mapping; unbound arbitrary engine; swapped clock domains; erased context; output-derived reference; schema60 without seed; source60/local59; missing older projection; caught patch60/draft fault. Keep alias/order/full-candidate parity, real-source P1, named assertion-killed mutants and existing suites. No execution now.
Exact M public source SHA256: P/port.cjs=08f877c73baaea6d7ab9737df303a8bd6e83c17697780130eea64e6fc615b070; P/unseal.cjs=515822a128031f03dde27d96c161d5528dd444673e00fa2e5c9a1d40719abf02; E/oracle-shim.cjs=dd653bc170d3c5ae3b8cfa5c2ca8166b1de385a0903e056eab7ea062c125052d; E/index.cjs=40ccc489a44dfdb4581e157a06f8bcf70fe77e25f33051ffca90b5910cd6b893; tools/_fixed-now.mjs=ab939a356467d095f357d796f87a6567b9bcb9e0171b17fe4fb0f447916cf5a7. Public text hashes only; no full engine-tree recomputation.
STATUS · D · CONTEXT MAP READY · Default C2 parent clock/draft derivations and sealed/custody fields mapped to exact proposed checkers; producer/run/calendar correspondence and reached older references remain explicit unmet inputs. PM/B reconciliation next; no runtime, private/oracle/output grant or owner question.
