# S3 R3 — finite engine-context capability proposal

2026-09-13 · D → sole PM. Proposal only, requested after final ER fe2c79ef R2-1/R2-4. Preserve R2 candidate0df6ad3/source946c360; no protected source edit or new execution is authorized by this note.
B companion custody proposed: exactly rebuild/engine/merge.cjs, rebuild/engine/today.cjs and rebuild/m4/workout/engine-runtime.cjs, plus B-named companion tests/registration/pins on a PM-named successor. B1+B2 repair/review priority remains; D does not author or substitute these files.
Merge seam: change the existing createMerge(E,{clock}) dependency signature to createMerge(E,{clock,nativeDate=Date}); retain its CommonJS factory export and returned engine members. The default is the native constructor, so existing callers retain native behavior.
All five executable parse sites, at946c360: merge.cjs76 _corrOf(at);161 _fileCorr(at);177 _fileCorr(latest9);515 _mergeSession(c.at);565 _adjInstant(x.at). Route each unchanged argument through nativeDate.parse; the Date mention at165 is commentary, not a sixth call.
The sole constructor operation at177 becomes new nativeDate(nativeDate.parse(latest9)+1).toISOString(); preserve the live-only branch, +1 millisecond, raw stamps, native coercion/NaN/invalid-Date behavior and existing error catches. _sessionAtMs471 remains numeric coercion; no new date interpretation there.
D consumes the dependency only in I/engine-provider.cjs (I=rebuild/m4/import): supply an instance-local native constructor/parse adapter capturing the real native implementation; no global Date replacement, source-text rewrite/evaluation, copied merge algorithm or extra production engine member.
Context binding: obtain the adapter from the same private sourceEngineContext handle, immutable producer/execution/material digest, exact engine closure and native-calendar zone/range already bound to Q. An arbitrary clock label, caller-supplied Date class or {verified:true} cannot supply it.
Extend I/local-source-profile.cjs with an explicit native-Date capability under that immutable execution calendar: reviewed parse vectors bind original raw input to exact epoch/NaN outcome; constructor vectors bind epoch to native ISO/invalid outcome. Retain raw inputs and numeric results; never normalize/rewrite recorded timestamps.
At each actually reached operation, use the native implementation, validate its operation kind and covered native civil date/zone against that capability before returning to the engine, and validate constructor results including the +1 boundary. Malformed inputs retain proved native failure/NaN behavior; absent or uncovered compatibility yields SOURCE_ENGINE_CONTEXT_UNPROVEN, never guessed parsing.
Missing native-Date evidence poisons that provider instance outside merge's internal catches, including a later dataLossGuard call. Failures expose only fixed codes. Existing seven-member source-replay facade and unavailable-reference poisoning stay unchanged; test vectors remain explicitly synthetic, not producer/C2 provenance.
Membership seam: B adds sessionMembership(s,iso) to createToday's returned readers. Extract only genSession's existing ord/filter/exActive/sort pool into one private _sessionPool(s,dt), reused by genSession and sessionMembership; genSession retains its original day guard and pickStructural/active/pool call order.
sessionMembership returns null for the existing non-U/L day predicate, otherwise a fresh immutable {day:dt,exercise_ids:[...]} containing the actual complete ordered pool IDs. It reads no sleep, structural activation, target/load calculation or historical-readiness substitute and returns no exercise-object aliases.
B adds only sessionMembership:(s,iso)=>E.sessionMembership(s,iso) to createEngineRuntime's frozen reader facade and updates EXPOSED/COMPOSITION from four to five names. Its module exports stay createEngineRuntime/COMPOSITION/absentProvider; no writer, seed, migration or merge becomes reachable through that runtime.
D changes W/local/source-admission.mjs (W=rebuild/m3/w6) to call that reader under the existing authenticated original Start local date and opaque engineContextAt clock, using the same held/reproduced programme basis and Q digests; compare its ordered IDs with the complete stored capture layout.
Keep the existing independent set-count validation and all original capture loads/reps/effort/skip/incomplete facts. Membership proves only pool/order; it must not invent sleep to reproduce structural targets or claim full historical prescription equality. Any further original-context need returns to PM by exact capability name.
B tests: exercise all five parse sites and the constructor through native-vs-injected equivalence controls, preserving existing temporal branch winners, malformed/native fallback behavior, live +1 stamping, untouched input bytes and read-only semantics. Include the review's offset-free March DST pair and explicit-offset controls.
D tests: full-year context permits those exact native results; September-only context withholds the same reached March parse and remains poisoned afterward. Add reached-constructor range-crossing and malformed-input controls under the explicitly evidenced native behavior; no reference data or new calendar algorithm.
B membership tests compare reader IDs with real genSession(...,recordedFixtureSleep).ex IDs for ordinary and pending-hack-debut public fixtures, ordering/retirement/rest-day controls, while preserving the original genSession result byte-for-byte. No-debut and debut captures use their actual explicit fixture sleep only at the existing producer.
D admission tests retain the matched full-capture positives, omitted-whole-lift refusal, differing historical loads and skip/incomplete preservation; replace only the sleep-dependent membership call. R2-2/R2-3 harness corrections remain a separately released existing-path task, not part of this protected edit proposal.
Pin effects: B supplies exact reviewed companion source/candidate heads, generated-engine correspondence and cumulative profile/CI/public-closure deltas; parent evidence remains historical. D then updates the provisional111-source hashes, actual public-factory/context mapping identity and the byte-bound engine-runtime loop pin from that named companion, never guessed successor hashes.
No new production dependency/file is proposed: the existing12 runtime factory edges and14 source-provider factories remain; reader count changes only in B's runtime composition. B names any affected census/registration/test paths before its edit. D-owned provider/profile/admission tests, browser parity and manifest must prove the exact joined source.
Validation after precise release: preserve original57+41/56mutant/27browser/22annex evidence at946c360, then run the corrected named extra witnesses and healthy pairs, affected source/guard mutations, complete portable real-provider Node/browser proof and restoration checks on a fresh owned copy of the named successor.
Sequence: PM names B companion custody/base → B authors/tests and separate reviewer inspects exact companion → PM releases exact B/D join → D wires guarded capability/membership and reruns its full portable bar → separate MAX reviews the complete successor; PM alone judges. No source or merge acceptance is inferred from this proposal.
Unchanged holds: no protected edits by D, historical sleep fabrication, source-field normalization, broad factory rewrite, memory runtime, private/native/final/real-C2/P1/CI retry, receipt, merge or deployment. No source branch is advanced until PM's named successor record and exact scope.
