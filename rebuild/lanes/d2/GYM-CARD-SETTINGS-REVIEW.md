# Gym-card settings independent review, D2, 2026-09-12

FINAL VERDICT: REJECT. Findings 1-3 are BLOCKING. This is a lane review, not PM acceptance or merge authority.

Reviewed Claude candidate rebuild/lane-c-settings @ fffc983c64a2a15e1583c2cb7944d33faddb280b against rebuild/lanes/c/GYM-CARD-SETTINGS-DISPLAY-BRIEF.md and :159. Own sparse worktree work/lane-d2/review-settings; no private data. Brief/bar first, independent execution next, builder report last. Effort MAX. N1 changes are absent from this head, as disclosed.

## Findings
1. BLOCKING, P1, S8: optional settings I/O is now a prerequisite for painting the gym card. gym-app.mjs:444 awaits settingsLane.latest before renderActive. With a pending read, mountGym never finishes and there is no log control; releasing that read makes it appear. Independent probe uses a real started workout and a deferred settings reader. Render and operate the workout independently of the optional read, and apply late results only to the still-current exercise/mount. Test slow/pending reads and a failed read while logging through the actual control.
2. BLOCKING, P2, S1/S2: failed read is falsely rendered as absence. gym-app.mjs:445 catches any read error and assigns null; settingsPaint still passes that null to renderBlock, which prints No settings saved yet. Independent probe first durably stores a setting, then injects a read failure and observes the empty claim. Preserve the distinction between unknown/unavailable and confirmed empty, without hiding or blocking the workout. A read failure must not seed a replacement editor as though no prior settings existed.
3. BLOCKING acceptance gap, S8/S-M6: a mutation adding `|| !settingsLatest` to the log-button guard survives all 43 tests, although it makes saved settings a prerequisite for logging. The S8 test at machine-settings-ui.test.mjs:541 calls kit.model.logSet directly; the handler can be broken while the test remains green. Exercise the actual log button with no saved settings and with unsaved editor text, and kill this variant. The required browser run passed, but its log-after-capture journey does not cover the no-saved-settings variant.

## Independent evidence
- machine-settings-ui.test.mjs: 43/43, including same-producer payload and coach recall assertions. Ten mutation probes: 9 killed, S-M6 survived; original tracked bytes restored and 43/43 rerun. Counts/repro in GYM-CARD-SETTINGS-REVIEW-ANNEX.md.
- Combined today, coach, W6 and host suites with --test-concurrency=1: 1,253/1,253. Initial concurrent run: 1,252/1,253, build-ID assertion R3 failed; isolated problem.test.mjs 25/25 and serial full rerun 1,253/1,253. No product defect is inferred from that concurrent-run failure.
- Build PASS at restored head: 3 assets, 107 pinned inputs. Five boundary files (bindings plus four PAGE_PINS files) byte-identical to integration 573581e; no candidate engine/client/m4 changes. The CSS addition supplies the tested narrow layout; it is reported in the candidate diff, not a hidden pin change.
- machine-settings-check.mjs PASS on installed Edge: 3 actual taskkill process kills, reload/correction/cancel/log-with-editor, 390x844 and 320px geometry, 16px inputs, 44px targets, one primary action and no off-origin requests. Only this candidate's required new browser check was independently driven, not all five checks claimed by its builder.
- GitHub API verified exact-head CI 34705578831: Windows and Ubuntu success. Deploy/preview CI 34705578835 also success at the same full head; production skipped. Neither workflow runs the new settings suite; the brief's disclosed CI residual remains for the named handoff.
- Local B-NTC --ci is custody-blocked: its traces child tries to open excluded src/history.js. D2 did not restore or read it. Remote exact-head gate is green. This is not classified as a candidate defect; an authorized owner can complete local gate verification.
- The builder's never-blocks statement is too broad for the awaited-read mechanism. Its claimed ten killed mutants do not establish the handler-level S-M6 variant above. No private material, raw logs, deployment or authority acceptance is included in this review.

Return an exact new head with read-state separation, nonblocking workout rendering and a handler-level S8 regression. Rebase/integration with N1 still requires the relevant final-head checks and the existing PM process.
