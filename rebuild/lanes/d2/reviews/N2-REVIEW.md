# N2 SLEEP ENTRY independent review, D2 round 2, 2026-09-12

FINAL VERDICT: REJECT. Six blocking findings below. This is a lane review, not PM acceptance or merge authority.

Exact Claude head rebuild/lane-c-n2 @ 7d2fdab6fd0a41ead7c9edc4534d002ca2905504, base bcdea5d (settings integrated). Contract: BRIEF-N2-SLEEP-ENTRY.md v1.0 b0969edb, :167 narrowings, :173 no invented defaults, N2-SOURCE-ERRATUM.md, and :178 routing. Own sparse work/lane-d2/review-n2 on rebuild/lane-d2-review-n2-r2; brief/bar first, independent execution before C's report, effort MAX. Synthetic inputs only.

## Blocking findings
1. P1, stale correction is checked outside the transaction (N2-01/05). sleep-host.mjs:133-149 reads `supersedes` before sleepClient.execute, then drops it from the command. Two concurrent save calls on the REAL host, both superseding the same op, both acknowledge and append; the second overwrites a night its editor never saw. Probe R2-1: initial 7 h, concurrent 8 h and 5 h, three ops, winner 5 h. Enforce the expected night revision in the generation actually committed, including a CAS retry; a pre-read is insufficient. The new sequential stale test passes but misses this race.
2. P1, real gym consumers still retain old sleep (N2-09/10). today-app.cjs:423-434 attaches replay after boot without rebinding the gym; :1424 discards all completion work when a save's mount token is stale. R2-7 executes actual boot with a stored 3 h night: Today reads 3, the real gym accepted_state has no night. R2-6 saves 1 h, navigates to Today before completion, then refreshes the gym: Today reads 1, gym still has no night. Keep navigation ownership while updating shared consumer state; cover boot, relaunch and late save, not only an awaited save on Sleep. Preserve injected host identity and active Start/set bytes as well as the draft.
3. P2, the second check-in visit loses the current draft (N2-07). today-app.cjs:1469-1500 creates `fresh` but never retains it as the active check-in; every later rebind copies the original entry's draft. R2-3 proves the first carry works, edits that replacement's actual soreness input, leaves and reopens: the original text returns and the new text disappears. Retain and rebind the current draft/model, invalidating only sleep confirmation when its referenced night changes. Test repeated visits and a second sleep correction.
4. P2, real A3 duration reuse is unreachable (N2-08). today-app.cjs:1078 requires `row.answers.sleep_hours` to be a number, but a real createCheckInEntry saves/reads `{value:7,unit:'h'}`. R2-2 saves entered hours plus Good quality through A3: quality displays, `Use these hours` stays hidden. Read the actual unit/value and entered-source shape, restrict the offer to the following-day check-in, and prove the real control appends the authenticated source reference. The current numeric mock hides this mismatch; historical date/quality and rollover journeys remain unproved.
5. P1, save outcomes still misstate the record (N2-05/14/15). today-app.cjs:1115 uses `logged || ack`, so a committed 8-to-5 h correction with failed read displays 8 h even though sleepAck and the durable op both say 5 (R2-4). At :1404-1421, a failed reconciliation read is treated like proof of no write: R2-5 commits through the real host, loses the acknowledgment, then fails the read; the UI says `Nothing was recorded` and enables Save. Prioritize the acknowledged correction over stale cache; retain UNKNOWN when reconciliation itself fails and fence resubmission. Match the intended operation during reconciliation rather than assuming any new last op is this save.
6. P1, actual coach read-side disagrees (N2-12). local-world.mjs:224-247 constructs Today/check-in without sleep replay; tools.cjs:605 reports the captured night as a successful fact. Independent execution saves a 1 h N2 op into the REAL coach world's own authenticated generation, then calls actual `today_checkin`: it returns ok:true and 8 h both after save and after reopening, while the stored op remains 1 h. This is not an unavailable consumer. :167 makes the companion optional only when unnecessary; provide the read-side wiring or an honest qualified refusal and actual consumer tests. Calling the pure projector twice does not discharge this row.

## Fixes that hold
- Round-1 findings 4 and 7 close on this head: unordered devices yield no projected winner, and same-date overlay preserves unrelated fields while retiring obsolete clock fields. Independent mutants that remove either behavior are killed.
- Source authentication now refuses forged/mismatched references and existing-record confirmations as origins. Sequential stale refusal works; only its atomicity remains open in finding 1.
- The normal awaited save rebuilds the real gym host and carries its draft; the first check-in draft carry works. Dated controls, quality display, correction/cancel, corrected stamp and no guessed source date are present. A first acknowledged night survives read failure, and late saves no longer navigate. These narrower successes do not close the journeys above.

## Independent execution and evidence limits
- Whole sleep suite 43/43; serial today/coach/W6/host 1374/1374, zero failed/skipped. Each N2-01 through N2-18 name-pattern command executed; selected pass counts 4,3,3,3,5,5,2,2,2,1,1,1,2,5,1,4,1,1. Labels overlap and are not additive; a source scan is not an executed user journey.
- Nine defect observations: seven actual host/factory/DOM/boot probes plus two actual coach calls. An independently injected IDB abort after the active-generation put refuses and leaves both ops and outbox unchanged. Executable witnesses and commands are in N2-REVIEW-ANNEX.md.
- Fourteen independent mutations killed, all assertion failures, no parse errors. M01-M14 failed-test counts 1,2,1,1,9,6,3,3,1,1,1,3,1,1; restored all original bytes, final sleep 43/43 and tracked diff empty. The M07 runner initially refused a non-unique anchor before editing; narrowed the anchor and resumed.
- Build PASS: 113 inputs, earned-d409fd478846. sleep-check.mjs PASS: three verified Edge process kills, both modes/correction, real gym return, same-page check-in and 390/320px app-frame checks. Independent 375px normal and doubled-font checks reach inputs/Save by Tab with zero app-frame overflow. Outer preview document is 398px wide at 375px, so this is not a whole-page no-overflow claim; full-bleed remains P5.
- Process kills before commit and after commit/before acknowledgment, clock rollover, real iPhone keyboard/zoom and active Start/set-byte preservation across the rebind are NOT PROVED. The IDB abort and injected lost-ack probes above are distinct from process-kill evidence. N2-11 still asserts a pre-H3 model.read throw; integrated H3 Today/gym/recovery proof is not present on this candidate and no defaults were added.
- GitHub API verified exact head: 34727122011 Windows/Ubuntu success (22 passed steps each); 34727122066 suite/preview success (8/7 passed steps), production skipped. sleep.test.mjs is not enumerated. Per :178/:184 its naming belongs to B1+B2; this is not a renewed pin/CI hold or a D2 workflow review.
- Diff from bcdea5d changes no today-entry, W6 bindings, engine, client, m4 or workflow bytes. Local B-NTC was not run because it requires excluded src/history.js; no custody bypass. Exact-head CI is inherited evidence, not execution of the new sleep suite there.

Builder report read last. Its first-path fixes and basic browser claims reproduce. Its claim that all seven findings are answered overstates the tested paths, and its unchanged claim that the coach companion is unnecessary is contradicted by the actual tool result. Return a fixed exact head with red-to-green consumer, repeated-draft and commit-boundary evidence; preserve :178 custody and routing.
