import fs from 'node:fs';import assert from 'node:assert/strict';
const file='.tmp/d2-module-link-finalize.mjs';let text=fs.readFileSync(file,'utf8');
const edits=[
 [",suffix=raw.subarray(-2).toString();assert.equal(suffix,'\\\\n');",';'],
 ["JSON.parse(raw.subarray(0,raw.length-2).toString())","JSON.parse(raw.toString())"],
 ["Committed observation writer appends literal backslash-n (5c6e), not a JSON whitespace newline. Raw bytes retained; only these two bytes were omitted from this parsed inspection. No test rerun, source repair, or assertion credit arises from this metadata formatting issue.","Raw observation is valid JSON with a terminal newline (last two bytes 7d0a). First reviewer post-run analysis mistakenly expected literal backslash-n and stopped at that assertion before copy/derived output. Exact failed analyzer retained separately; corrected analysis parses the full raw bytes. No tests rerun and no candidate change."]
];for(const [from,to] of edits){assert.equal(text.split(from).length,2);text=text.replace(from,to);}fs.writeFileSync(file,text);
fs.writeFileSync('.tmp/d2-module-link-finalize-setup-error.json',JSON.stringify({stage:'D2 post-run metadata inspection; no candidate execution',command:{executable:'.tmp/tools/node.exe',args:['.tmp/d2-module-link-finalize.mjs']},source:'.tmp/d2-module-link-finalize-setup-error.mjs',exit:1,error:{name:'AssertionError',code:'ERR_ASSERTION',actualTailHex:'7d0a',expectedTailHex:'5c6e',location:'d2-module-link-finalize.mjs:4:237'},effect:'Stopped before raw observation copy or final evidence write. Public12/12 and selected F2/F3 2/2 results unchanged. Fixed only own analyzer and reran read-only packaging; not a candidate defect or regression kill.',sourceOfFailureRecord:'Observed exec tool result chunk b9e798'},null,2)+'\n',{flag:'wx'});
