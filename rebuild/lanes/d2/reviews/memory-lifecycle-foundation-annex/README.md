# D2 memory lifecycle foundation replay
Candidate1316639e71cc8b23b24570bb6aa0e7508da90032, sourceaec118c4c23eb55e32e29ffdf8be60cfb30f28d0, baseline0c744cbd2d53c3be738320f726004a4e1871cce5. The adjacent review is REJECT, not a product patch.
Create a fresh owned public-only checkout at the candidate and install its root/W6/W5 locks with scripts disabled. Use Node22.23.2, America/New_York, owned TEMP/TMP, no NODE_OPTIONS/V8 coverage. The public closure and metadata inventory are in the evidence JSON; do not widen into protected or provider work.
Copy d2-lifecycle-extra.test.mjs into that checkout's ignored .tmp/. Its relative imports reach the actual production handlers. Run `node --test .tmp/d2-lifecycle-extra.test.mjs`. The exact current result is37 tests,35 pass,2 intended failures. The two synthetic observation files are written in .tmp immediately before those assertions. This script neither mutates product source nor installs substitutes for the model/UI handlers.
The original licensed command is `node --test rebuild/m3/w7-preview/today/test/gym.test.mjs rebuild/m3/w7-preview/today/test/machine-settings-ui.test.mjs`; its137 tests include the unchanged embedded S14 build. Do not replace it with a separate build/browser/full/CI invocation.
Earlier witness versions are retained for failure attribution only. The initial variant assumed a pre-commit paused resume remained admissible after a newer preparation and closed the DOM too early; the final variant pauses acknowledgement after genuine commit and awaits Cancel repaint. No such harness failure receives product or mutant credit.
The static closure/identity/evidence scripts and inventories are byte-exact execution records. Evidence generation also requires the named raw logs in D2's owned review .tmp; do not borrow another lane's dependencies/logs or treat an absent artifact as a pass. Build metadata is read from the original S14 output, never regenerated by the evidence script.
