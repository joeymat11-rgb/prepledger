# D2 lifecycle R2 replay
Exact candidate18ff0dcf015193a9b76b83fbbc80fbe888f15d24, source/tests dc768c967a230e61ec84f96a06b1034ca54f0470, base1316639e71cc8b23b24570bb6aa0e7508da90032. The adjacent independent review is REJECT for one visible-error failure; the PM judges.
Use a fresh owned public checkout at the candidate, fresh root/W6/W5 locked dependencies with scripts disabled, Node22.23.2, America/New_York, contained TEMP/TMP and no NODE_OPTIONS/coverage injection. The evidence inventories define the licensed source, metadata and dependency closure. Do not read the author report before an independent verdict.
Copy d2-lifecycle-extra.test.mjs into the checkout's ignored .tmp/. It is the exact original37-case replay, with original production imports and no adaptation. Run `node --test .tmp/d2-lifecycle-extra.test.mjs`; observed37/37 PASS.
Copy d2-lifecycle-r2-support.mjs and d2-lifecycle-r2-edges.test.mjs into the same .tmp/. Support is the helper prefix of the original37 plus exports; it does not run those37 tests again. Run `node --test .tmp/d2-lifecycle-r2-edges.test.mjs`; observed3 PASS/1 intended visible-refusal failure of4, no cancel/skip/todo or late activity.
The failing case uses the actual Log handler, gym model, client and IndexedDB quota refusal. It holds delivery only after the real command returns, lets independent editor Add row repaint the same owning card, then delivers that result. The intended assertion requires the actual refusal on the current card; both drafts and old operations/outbox are retained. No fabricated acknowledgement, replacement model or source mutation.
The literal licensed original command is `node --test rebuild/m3/w7-preview/today/test/gym.test.mjs rebuild/m3/w7-preview/today/test/machine-settings-ui.test.mjs`; observed141/141, including the unchanged S14 embedded build and S10 metadata exception. No separate build/browser/full/CI command is granted here.
Attempt1–4 sources and observations are diagnostic only. Their before-enqueue delay lets a later continuation preparation retire the pending handle; that is not evidence that normal client serialization cancels a submitted set. Attempt2 also contains contradictory outcome assertions, and attempt3 uses an overbroad commit oracle. Final evidence uses a real already-completed quota refusal and only the scoped visible-error requirement.
Identity/closure/evidence scripts are exact execution records, with references to D2's own prior identity inventories and raw logs. They require those named inputs and are not drop-in product checks. Do not borrow another lane's tree/dependencies or rerun a build to manufacture missing evidence. The s14-app.js.meta.json file is the actual embedded-build metadata.
Raw TAP/install logs remain in D2's owned ignored .tmp; every hash and final/initial test outcome is recorded in the adjacent evidence JSON. The scoped annex attributes preserve exact bytes; do not normalize diagnostic files and call them the same execution source.
