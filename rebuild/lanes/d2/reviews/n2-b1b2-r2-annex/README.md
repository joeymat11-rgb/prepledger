# D2 N2 affected R2 executable annex
Exact candidate c4716edad91453e74ba17f70ab076781ff5367de; original verdict and evidence JSON are adjacent.
These files are byte-exact reviewer sources. Copy the witnesses/support to `.tmp/` of a fresh owned public-only checkout at the candidate before using their relative imports. Root/W6/W5 dependencies must be installed from the candidate locks in that own checkout. Use Node22.23.2, America/New_York, owned TEMP/TMP and no injected NODE_OPTIONS/coverage.
Run the evidence JSON's literal retained and availability argument arrays with Node. The first retains actual77 sleep + original21 controls + adapted7 consumers; the second has16 new named tests. The `-original` consumer source is historical and is not the R2 oracle; the evidence JSON contains its only two expectation-site changes.
`d2-combined-closure.mjs` audits the public graph before execution. Its deny list must remain. `d2-combined-build.mjs` calls the actual fresh build; then the candidate's sleep-check.mjs is run using a fresh profile and an existing Edge executable.
Identity/evidence generators also name prior D2-owned files and local raw logs for exact history comparisons. Those paths identify their actual execution context; do not substitute foreign worktrees, dependencies or protected files to make them run elsewhere. The initial evidence generator is preserved solely to identify its LF/CRLF manifest failure, not as an additional test or intended pass.
No source mutation or full/native/private/package/CI execution is licensed by this annex. Synthetic host facts and intentionally pre-existing basis rows are labeled in the tests.
