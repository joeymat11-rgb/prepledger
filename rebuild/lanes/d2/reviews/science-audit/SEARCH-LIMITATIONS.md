# Scope, search and verification limits

Independent assessment under PM356, before reading E's findings. This is a finite audit of the 12 dispatched questions, not an exhaustive systematic review. The original 72-row inventory was verified without alteration. Supplemental browser-engine and workout-engine-runtime sources were read only after direct PM input-only authorization and pinned separately.

Search families covered: maintenance energy density and compensation; assigned/achieved loss rates; male EA consensus and short-term studies; protein dose in trained energy restriction; surplus trials; fractional sets/SDES and deficit volume; RIR accuracy, repeat reliability, current ACSM guidance; sleep restriction, deloads; strength versus body composition; ICECAP, refeed reanalysis/response, diet-break correction and recent reverse-diet evidence. Primary publication dates were distinguished from online/issue dates where verifiable. Relevant correction searches and access levels are in SOURCES.

The existing corpus and later corrections were read before current claims were classified. No old paper or local document was treated as permission to execute its scripts, reopen retired interventions, change an accepted policy or assume a current athlete profile. The original runtime sheet and ingestion protocol were identity-checked but not fully read. Missing reach and applicability remain explicit.

No application modules, tests, fixtures, historical inputs or source/private state were evaluated. Own metadata-only Node scripts read UTF-8 source, check hashes, print selected line ranges and compare byte spans. Git metadata/static blobs and ordinary web access were used. Even source files containing require/new Function/test calls were read as text only. No software dependencies were installed, and no repo tests or scientific datasets were run.

One own line-reader request exceeded starter-week's file length (requested through 165; file has 162 lines); it failed before product execution. The next read covered the complete file. Some large tool responses were truncated; follow-up bounded reads recovered important claimed passages. Coverage records use a conservative manually confirmed range inventory, not every range requested by a tool. Search hits and clipped long inline test controls never count as full-file reads. Identity verification is distinct from semantic review.

Only own branch documentation paths named in the dispatch are written. GATE-WINDOW at dispatch 45eb03a02e0dc268178f87927a8bb56fe0e8730c was fully read and said ENDED. No shared push, deployment, product/policy/acceptance edit, medical clearance or whole-app validation is authorized or claimed. This first-stage report still requires reconciliation with E's complete immutable package.
