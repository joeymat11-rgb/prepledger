# G2: proposed memory consent and programme custody after C/B tracing

PM204 design follow-up. C note a3a10a3072950de209d9843e41181529e00cda90, lanes/c/MEMORY-CONSENT-ADMISSION-NOTE.md (39 lines), and B map73eee8f6a70a379ac33796d73d6efbcd6f737ea2 are source evidence only. Neither proves an admitted positive path. No runtime custody is released; PM assigns final authors/file subset. Paths below are under rebuild/.

## Chosen positive witness to design against

Use the existing engine-issued inferred machine-weight ladder for one active exercise. C traced progression.proposeLadder's existing qualification, writers.sweepLadders' real proposal with apply:{kind:"ladder",exId,rungs}, and writers.applyProposal's existing steps/stepsAt/working-weight/wAt consequence. Select a public eligible fixture whose actual next prescribed load changes under those rules; do not invent a new rule or alter current qualification to force an effect.
The coach currently wraps bare proposeLadder output, which does not supply the reason that its l.why/l.t lookup expects. C must use the actual sweep-issued immutable identity/body/reason and exact revision, not synthesize a reason. The source test cited by C is a candidate fixture, not an executed memory witness.
Required joined proof: real eligible issuance -> displayed exact change and reason -> explicit bound yes -> coordinated durable admitted/application outcome -> close/reopen actual installation/hosts -> same accepted status/reason in legacy and new views, changed exercise ladder/working weight and applicable nextLoad/gym prescription. Also execute no-offer, decline, stale/forged/malformed input, failed commit/read, retry and unchanged/refusal cases. All-refusal fails M10/J6.

## Why the initial producer-only proposal is not presently admitted

C traces schema2 producer.prepare/validate as synchronous op preparation; the stage then commits ops/outbox/sequence. It does not supply a second arbitrary issuance/application transaction. Local host admission also rejects inbound authority; a stage allowInbound flag is not a bypass.
Client proposal-response uses proposal_id/answer, while authority validation requires issuance_id/chosen_outcome_id/consent_digest with APPLY|KEEP|NO. Numeric engine snapshot leaves are not automatically a valid typed payload. Authority issuance requires trusted evidence/digests/conflict domains/apply_members, and its apply step is distinct from response admission. Zero apply_members cannot establish programme change.
Client model.issuances comes from a separate collection, answers reads the old response shape, accepted/live plan and onFold consume different records. Workout source projection after reopen also needs a valid registered basis. A memory-only response/history module cannot repair these joins.
Therefore retain producer injection for confirmed memory facts, but withdraw any assumption that the decision producer can complete G2 without protected/shared client/authority/stage work. Do not implement a second plan applier in E's modules. PM must select an exact coordinated transaction/admission/restoration contract using the existing trusted boundaries before releasing decision code.

## Proposed custody by exact candidate paths

| Proposed owner | Paths and purpose |
| --- | --- |
| E after release | coach/{memory-commands.cjs,memory-model.cjs,memory-host.mjs,memory-tools.cjs,decision-commands.cjs,decision-history.cjs}; coach/test/{memory.test.cjs,decision-memory.test.cjs,memory-journey.test.cjs}. Memory producer/read models; decision encoding/history only after PM contract. No authority grants, direct plan mutation or manufactured issuance status |
| C after release | coach/{tools.cjs,local-world.mjs,wave1-tools.cjs,TOOL-CONTRACT.md,model-adapter.md}; coach/test/{tiers.test.cjs,wave1-demo.test.cjs,no-dashes.test.cjs}. Actual sweep-issued proposal, awaited consent, real installation and awaited historical reason with {why,source}; retain all tiers and current explanation behavior |
| C conditional review surface | coach/memory-view.mjs plus m3/w7-preview/today/{today-app.cjs,build.mjs,screens.template.html}. Name actual caller/interaction before release; same single Today owner |
| PM assigns shared-boundary implementer | client/{index.cjs,face.cjs,ops.cjs,plan.cjs}; authority/{validate.cjs,issue.cjs,admit.cjs,plan.cjs,reduce.cjs}; m3/w6/{t2-stage.cjs,public-client.mjs,local/local-client.mjs,local/host-bindings.mjs}. This is the bounded fallback inventory from C/B, not permission to edit every file. PM selects the minimal contract-driven subset and names one author; E's current custody does not expand |
| B routes only if necessary | m3/w6/{local/today-bindings.mjs,host/workout-host.mjs}; m4/workout/{source-projection.cjs,authority-profile.cjs}. Existing pinned admission/projection boundaries must stay intact; any required edit follows the applicable reviewed successor/W6 pin route before code |

No new path beyond B's38-path map is proposed. Exact tests for changed shared boundaries remain to be named by their owner; old archived identities and an uninvoked W6 package script do not satisfy them. No engine source edit is proposed for the ladder rule. If contract tracing requires one, stop that scope and route a B engine package.

## Contract closure required for final implementation release

1. Name who authors trusted issuance and maps the real engine proposal to exact apply_members without fabricating quantities or permissions; validate a lossless typed/canonical body/reason representation and consent binding against actual authority contracts.
2. Name the actual schema2 async durable entry and coherent commit/recovery mechanism. Show where response, application and legacy issuance status derive from the same authoritative outcome; no success before durable completion, second uncoordinated write, synthetic identity or synchronous bypass.
3. Name programme/source restoration through the actual reopened Today/workout host, including old-shape replay. Historic reason omissions remain unknown; current recalculation cannot become remembered evidence. Existing client face/answers/issuedInstance and new history reader must agree on status without rewriting old records.
4. PM selects the exact file subset/author, B confirms that subset's pin and CI route, then PM releases bounded implementation under202. D2 independently reviews later; a third actor integrates. Positive controls/mutants and both-OS exact-head/full applicable gate evidence remain required.

Disposition: C's G2 source trace and B's G3 inventory are now received and reconciled. They expose a missing shared consent/application contract rather than clearing the original isolated-producer design for code. G1 prerequisites, the four precise closure items above and final custody remain open. No repeat owner/design approval is needed for204's corrected acceptance bar. S3 existing-history delivery remains a separate explicit priority and has no dependency on finishing coaching memory.
