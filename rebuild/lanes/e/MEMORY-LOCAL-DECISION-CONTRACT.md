# Memory: one local decision, one ordered programme restoration

PM238/fdf0987 selects this route; PM240/6248def selects its direction with this finite grammar/state/retry amendment. E is provisional shared-contract author, not runtime author yet. Static public reads remain C bfc2935, D ebc4c4e (code0d50571), accepted230/context226/provider220 and MEMORY-G2-CUSTODY-PROPOSAL239ca308. No tests/provider/source execution, private data, accepted artifact edits or S3 changes. Paths below are relative to rebuild/; W=m3/w6, I=m4/import, T=m3/w7-preview/today. Proposed interfaces below DO NOT exist yet.
## Closed record and complete effect
Use one schema2 `kind:fact,class:event,payload:{profile:"earned/local-coaching-decision/v1",decision_json:<canonical lossless JSON string>}` through the existing `execute("workout",{action:"confirm-local-decision",input:{handle_id,confirmed:true}})` route. No generic proposal-response, plan-mutation, authority fields or arbitrary patch endpoint. T2 supplies op_id/device_seq/predecessor/lease/effective/HMAC; caller cannot override any envelope field, clock, source, effect or identity.
The closed decision has EXACT keys `{version:1,decision_id,installation_id,era_id,athlete_id,device_id,session_epoch,offer_id,source,frontier,source_revision,programme_digest,engine_context_digest,execution_json,offer_json,offer_digest,consent:{answer:"YES",offer_digest},effect}`. Every new object below is closed; unions use their literal tag or primitive type and forbid all other keys. IDs are nonempty Unicode-scalar strings <=256 UTF8 bytes; session_epoch retains the host's exact ID string OR nonnegative safe integer, with no coercion; new digest fields are64 lowercase hex characters. Numeric counts/indices/sequences/revisions are nonnegative safe integers, device sequences start at1. Commitments and existing Q/M fields preserve their original accepted grammar/bytes, not a guessed hash format. No keys, signatures or encryption secrets enter this payload.
Canonical grammar J: null/booleans/Unicode-scalar strings/finite binary64 numbers except negative zero, dense arrays in recorded order, and plain own-data-property objects only. Reject undefined, holes, accessors, symbols, cycles, duplicate parsed keys, invalid surrogates, NaN/Infinity and lossy round trips. Sort object keys by UTF16 code-unit order (not locale); emit JSON string/number spellings using ECMAScript JSON.stringify, no whitespace/BOM or Unicode normalization. Strings preserve every character. For each *_json string require J(strictParse(string))===string. `H(tag,x)=lowerhex(SHA256(UTF8(tag+"\n"+J(x))))`; tags below are literal, versioned domain separators. J changes object-key serialization only, not recorded array order/values; raw source bytes remain separately retained. decision_json=J(decision), <=65536 UTF8 bytes; oversize refuses without truncation.
`source` is EXACTLY `{kind:"null",W:0,setup:{op_id,commitment},basis_digest}` OR `{kind:"local-source",W:0,Q,M}`. Null basis_digest=H("earned/local-decision-null-basis/v1", actual source-controller clean-init state from that authenticated setup, before native replay). Selected Q/M must be owned/restored by the source controller; M may be null only under its existing empty-side rule. Q's exact keys are profile/installation_id/era_id/athlete_id/device_id/source_digest/material_digest/checkpoint_digest/local_selection_id/operation_digest/interpretation_digest/programme_digest/order_map_digest/engine_digest/replay_profile/as_of, with literal profiles earned/local-source-basis/v1 and earned/local-source-replay/v1. Use accepted Q digest algorithms; do not rehash them with H or invent a null selection. Future decision coverage is specified below, not present in current Q.
M's exact keys are profile/installation_id/era_id/athlete_id/source_digest/legacy_members_digest/native_root_id/native_members_digest/checkpoint_digest/assertion/root_interpretation_digest; profile is earned/local-source-order-map/v1, assertion is `{kind:"athlete-confirmed-legacy-prefix",answer:true,prompt_version:"earned/legacy-prefix-prompt/v1",review_digest}`. Validate it through the controller's existing map grammar/digests, not by accepting these strings. `frontier={through,causal_tips,originals_digest,interpretation_digest}`: through is null for empty prefix or `{op_id,commitment,device_seq}`; causal_tips is the exact prefix DAG-tip list `{op_id,commitment}` sorted by op_id code-unit order. originals_digest=H("earned/local-decision-originals/v1", the complete prefix's original envelopes in validated device_seq order). interpretation_digest=H("earned/local-decision-interpretation/v1", {source,originals_digest,collections,interpreter:{sources,as_of}}). Here collections is the EXACT authenticated prefix collections object excluding only derived; it includes original ops/outbox/dispositions/receipts/rejected/plan/capture controls with original field absence. sources is the reviewed interpreter/validator source manifest as exact {path,sha256} rows sorted by path code-unit order, as_of is the issue clock local_date. Unknown collections/decoder reach refuse; no field filtering beyond derived. This binds the full interpretation inputs and their decoder, not only op IDs/counts. This is a compact commitment to reconstructable originals, never a list of future ops or wall-time order.
`source_revision={revision,token_digest}`; token_digest=H("earned/local-decision-revision-token/v1", exact loaded repository token). programme_digest=H("earned/local-decision-programme/v1", `{source,frontier,state_digest:H("earned/local-decision-state/v1",S0)}`), so no consumed programme field is omitted. `offer_json=J({id,rid,d,title,why,apply:{kind:"ladder",exId,rungs},resolved:false})`, exactly the seven real sweep fields; rungs are the real finite positive lb array, not chosen/rounded by this codec. offer_id is that id, d a real YYYY-MM-DD, title/why exact strings; missing historic why is never populated. offer_digest=H("earned/local-coaching-offer/v1",strictParse(offer_json)); consent.offer_digest must equal it.
`execution_json=J({profile:"earned/local-ladder-execution/v1",producer:{engine_build_digest,mapping_digest,id_provider_digest},issue:{clock,ids},confirmation:{clock,ids}})`. Each clock is EXACT `{now_iso,now_ms,zone,local_date,local_time,utc_offset,hour,dow}`: now_iso is canonical UTC YYYY-MM-DDTHH:mm:ss.sssZ; now_ms its exact safe-integer epoch milliseconds; zone the controller-verified IANA calendar realm; local_date YYYY-MM-DD, local_time HH:mm, utc_offset +/-HH:mm, hour0..23 and dow0..6 all derived from that SAME instant/realm. Reject impossible dates/offsets or mismatched fields. Sample issue and confirmation separately from the trusted host clock; bind engine today/hour/dow/nowISO/nowMs and T2 effective `{local_date,local_time,utc_offset}` to the SAME confirmation sample. Permission/session/CAS freshness still checks live context; never resample the operation's effective timestamp independently or accept caller effective/clock values.
Each ids transcript is an ordered array of EXACT `{prefix,id}` for actual E._freshId calls/returned values. Issue allows only prefix ladder_ (all actual sweep calls, preserving order); confirmation allows exactly one adj_ result for this successful ladder branch. IDs are unique against originals/each other; provider verifies its actual underlying ID interface before release. Capture real returned IDs, do not invent them from the model or regenerate historical calls. Revalidation uses the recorded issue clock/call sequence in the owned provider, rejecting missing/extra/different calls; confirmation records its own actual calls. engine_context_digest=H("earned/local-decision-engine-context/v1",strictParse(execution_json)); producer digests bind actual reviewed build/mapping/ID provider content. No arbitrary renderer transcript qualifies.
Let I be EXACT `{installation_id,era_id,athlete_id,device_id,session_epoch,source,frontier,source_revision,programme_digest,offer_digest,issue_context_digest}`, with issue_context_digest=H("earned/local-decision-issue-context/v1", `{producer,issue}` from execution). `decision_id=handle_id="local-decision:"+H("earned/local-coaching-decision-id/v1",I)`; offer_id is already bound by offer_digest. Confirmation time/effect are intentionally excluded so the handle exists before yes. The immutable authenticated operation binds the COMPLETE final body; two retained ops with equal decision_id but unequal J(decision), or any identity recomputation mismatch, refuse. A caller cannot supply a replacement body. Retry never builds a new confirmation transcript for an already committed ID.
Three states: S0 is the original authenticated programme at frontier, BEFORE issuing anything; sweep a separate clone, then Soffer=clone(S0) plus EXACTLY the selected newly produced real proposal, discarding every other newly swept offer. Require no existing matching selected id/rid; preserve all original unrelated proposals. If proposals was absent, Soffer creates only its one-element array. Sresult is the actual returned applyProposal(Soffer,offer_id,0,"cal") under the confirmation clock/IDs. Effect and state digests ALWAYS compare S0→Sresult, never Soffer→Sresult. Reject superseded/no-effect and any unintended change; no historical replay calls this writer again.
`effect={profile:"earned/local-ladder-effect/v1",unit:"lb",exercise_id,before_state_digest,after_state_digest,changes}`; before_state_digest=H("earned/local-decision-state/v1",S0), after_state_digest=H(the SAME tag,Sresult). Each change is EXACT `{target,before,after}`, value grammar `{present:false}` or `{present:true,json:J(actual value)}`. Targets are ONLY `{kind:"exercise-field",exercise_id,field}` with field steps|stepsAt|w|wAt; `{kind:"proposal-insert",proposal_id,index,container_present}`; `{kind:"adjustment-append",adjustment_id,index}`; `{kind:"feed-prepend",index:0,prior_length,prior_digest}`. container_present is a boolean describing S0.proposals absence/array; proposal index is its original length or0, adjustment index is S0.adjustments.length, prior_length is S0.feed.length, prior_digest=H("earned/local-decision-feed-prefix/v1",S0.feed). Require unique exercise/id matches and real arrays for adjustments/feed; preserve absent versus null exactly.
Order changes by target kind as listed, exercise fields steps/stepsAt/w/wAt; no duplicates, equal-value field changes or other targets. Include each changed exercise leaf, exactly one absent→present resolved selected proposal, exactly one absent→present actual adjustment `{rid,id,d,at,title,nudge:0}` and one absent→present feed `{d,t,how}` from the writer. Proposal after has exactly the offer fields plus nudge:0/at, with resolved:true. Array tails/prefixes and duplicate feed multiplicity remain untouched; no value-based dedupe. Validate complete structural S0→Sresult diff equals these changes; restoring them at S0 must reproduce Sresult's digest. No history snapshot, generic pointer, deletion, discarded extra effect or quantity coercion. The ladder branch stamps stepsAt and numeric wAt, resolves/stamps proposal, appends adjustment BEFORE branching and prepends feed; w may stay equal because sweep includes current w.
## Trusted preparation, staged commit and retry
Proposed `createLocalDecisionCoordinator` in coach/decision-commands.cjs is constructed by the local host, with the actual canonical source controller and its owned engine capability. Public methods are `offer({exercise_id})` and `confirm({handle_id,confirmed:true})`; renderer/model receives only frozen display data and an opaque handle ID. Set handle_id to the domain-qualified decision_id (SHA256 identity, not a secret): an uncommitted handle requires its private pending map, while a committed retry can find the exact original after restart without that map. No configurable renderer producer, input state, ready flag or callbacks. Ordinary decline/no-offer writes nothing; optional declined-reason memory needs its separate yes.
`offer` runs in the durable client's serialized queue: load/authenticate current generation using stageVerified's verifiedHistory/lease path, obtain controller.prepareDecisionBasis at that SAME revision/token/frontier, and run actual sweepLadders on a clone. Select only its eligible ladder offer; retain its exact ID/body/reason, not other newly swept offers. The provider owns the real clock/ID dependency and records its identity/call outputs for validation; no model-generated id or seed-backed engine. Keep handle in a private map, bound to session/observation lifetime, day/source/engine/interpretation/programme and prefix; displaying an offer commits nothing.
`confirm` FIRST reloads in that queue, authenticates the CURRENT installation/session/observation and original operations, then looks up handle_id/decision_id in that authenticated log. If found, validate its full closed body/identity and uniqueness and return its CURRENT canonical restored status (including retained/unresolved); do not require the old private handle, historical session_epoch, original prefix still being current or a new write allowance. Preserve original timestamps/epoch/confirmation transcript, and never regenerate/apply an effect to answer this branch. Same ID with unequal retained bodies refuses. If the current authenticated read/restore is unavailable, report that state; do not enter the uncommitted branch speculatively.
ONLY when authenticated lookup proves no committed decision: require confirmed:true, the live owned private handle, matching historical-session-to-current-session/observation/source/day/engine/prefix/programme/revision/token and current valid write lease. Reproduce the selected issue with its original issue clock/ID transcript, comparing exact bytes and S0. Derive Soffer; take ONE trusted confirmation sample, use it for engine application and T2 effective fields, and record actual application ID calls to obtain Sresult. Any changed offer/basis retires the handle and requires a new display/yes. Prepare the closed operation/effect synchronously; its new sequence/op cannot enter its own frontier/digests. No Promise producer or async Store.transaction callback.
Actual call chain to extend: localHostBindings(client,{workoutCommands}) -> createT2Stage(localEraConfig,{allowInbound:true,workoutCommands}) -> Client.commitBatch's schema2 producer.prepare/validate -> Ops.build -> onPreparedBatch -> Store.transaction(ops,outbox,device sequence) -> public-client stageVerified -> existing bridge.execute/repository sealed-generation CAS. Keep synchronous isolated-memory stage distinct from asynchronous durable completion. No local-client command whitelist change or authority edit is needed for this command route.
Add an internally configured localDecision capability to public-client and the t2-stage handoff; it may run only with an active private coordinator attempt, not from generic execute args. Before stage, authenticate/resolve asynchronously; stage takes a private frozen prepared action and validates exact final envelope plus lookup-bound originals. Force stageVerified's authenticateLocalHistory on decision-enabled preparation/confirmation/reopen; give T2 compatibility views only the internally verified decision set, never trust raw profile-shaped ops on direct Client.boot. The host's existing sidecar/commitFailure validation remains; public-client.validateCommit additionally matches candidate batch count1, exact op/effect/sequence/frontier, snapshot revision/token and live session/context, and rechecks AFTER the downstream validator. No mutable generation or authority grant leaves the capability; generic forged workout/profile calls refuse.
Bridge completion is the only success signal. Abort leaves prior generation/ops/outbox intact. A completed commit followed by unavailable read reports committed/verification-unavailable and retains the decision; never call applyProposal again to resolve it. Retry always follows the authenticated committed-first branch above, even after reopening/new session/newer workouts/source switch; equal retry appends nothing and may report retained/unresolved rather than applied-current. A race with no identical committed decision refuses stale. Reopen derives status from authenticated ops, not an in-memory accepted map, pending flag or cached success.
## One restoration seam, currently missing
Exact gap at D ebc4c4e: source-admission.createLocalSourceController exports reviewSource/prepareSource/reopen/rollback/assertCurrent/view (plus invalidate/close); private replay(held) requires an import, folds readings then food then workout facts, and rejects nonempty issuance/plan-history stores and unknown plan/effect families. It exposes NO historical-prefix programme image or decision insertion. replay-core.createReadingReplay.projectLineage is hosted accepted-source replay, not local decision order; createSourceReplayEngine exports six methods plus SCHEMA_V and neither sweepLadders nor applyProposal. Its IDs capability is deliberately unavailable. None may be assumed to provide this memory contract.
Required B/D-owned extension of THAT controller: `prepareDecisionBasis({through,expectedSource}) -> opaque basis`, `restoreCurrentProgramme() -> opaque current view`, and an internal synchronous `validateDecisionCandidate(basis,operation,generation)` used by the stage validator. Controller owns repository reads/authentication; through names an actual prefix of retained originals, never a caller-supplied generation/state. It returns state/engine only to the trusted coordinator, and a read-only canonical view to C. Both null-selection (actual clean-init setup plus local facts) and selected import (existing reproduce/Q/M) use this ONE provider pipeline. These are prospective exports/callers requiring PM/B scope, not a second projector or S3 acceptance claim.
Insert a decision barrier INSIDE source-admission's canonical replay, after all inputs in its recorded frontier and before later native facts. Authenticate/order the entire log first, reconstruct that exact prefix without later corrections leaking backward, verify source/engine/programme and effect before-digest, restore the recorded complete effect once, verify after-digest, then continue through existing family/workout projectors. Record applied decision IDs only as rebuildable coverage. Do not overlay historical w at the end, call completeSession again, re-run today's sweep for old why, sort by wall time, or feed all-current workout facts into a historical prefix.
Subsequent corrections/source switches/rollback require the controller's original lineage/interpretation rules: retain the decision and all later originals; if its source frontier cannot be reproduced, expose retained/unresolved and withhold affected guidance. Do not silently rebase consent or rewrite Q/M/captures. REQUIRE the FUTURE provider extension to bind decisions/effects and complete interpretation in its Q/currentness commitments; current D Q does not implement this. Changed state invalidates eligible idle preparations. Restoring a null-source decision after a later import needs explicit controller-proved correspondence between its old prefix and selected source, not conversion of null into Q by ID resemblance.
Provider extension in I/engine-provider.cjs/browser-replay.mjs: add a PRIVATE bounded ladder-issuance/application capability from the same real factory table, with sticky missing-dependency checks and controller-owned clock/ID transcript. Preserve the public seven-member S3 facade. Its actual E._freshId dependency/call contract must be exposed/verified by its owner; permitted reads show writers forwarding to E._freshId but not a usable exported decision capability. No synthetic IDs/defaults in production, dynamic loader, arbitrary engine method or engine algorithm edit. Historical restoration consumes stored complete effect, never invokes that writer again.
## Legacy views and actual host lifecycle
Client/index derives local decision answers/history/instances from authenticated schema2 originals on boot/stage, with a separate domain-qualified local issuance map; no recordIssuance/respond second write and no populated independent issuances collection. Client/face filters hosted unissued offers ONLY through existing hosted records; a local matching bare ID cannot unlock one. Local answer/instance adapters expose saved-local/effect-restored or retained-unresolved, never hosted ACCEPTED. Preserve original legacy records; client/plan's existing hosted deriveInstance/project stays unchanged. C and decision-history read the same decoded decision and current restoration result.
C today-model.stateFromOps/foodProjectionOf consumes the current canonical programme view, with complete covered-operation identity so existing sleep/read/food replay does not apply covered facts twice. today-entry.createWorkoutEntry currently captures model.stateFromOps at openGym160/hostForDay164; refresh merely gym.reads the OLD host. Replace that join through the existing actual hosts.createGymHost with the SAME newly restored state/basis used by Today, including other-day recovery. local-world's gymOver has the same captured-state problem and must use the same lifecycle.
On confirmed durable decision, C refreshes canonical view first. Rebuild only an idle host whose current preparation can be retired; preserve the entry's gymDraft object, user-entered values and existing active/unfinished workout/capture/continuation policy. Do not call retireWorkoutPreparations indiscriminately: current implementation also retires resumptions/edits and fences uncertain starts. Recheck phase/currentness after awaits; an in-flight/uncertain Start or active session keeps its host/basis and drafts, marks next idle preparation for refresh, and never gets a replacement plan mid-set. Unavailable refresh shows saved-but-guidance-pending. Use actual closed/read/refusal outcomes; no lazy fresh host to evade source pins.
## Exact proposed custody and executable bar
E shared runtime subset after release: coach/{decision-commands.cjs,decision-history.cjs}; client/{index.cjs,face.cjs}; W/{t2-stage.cjs,public-client.mjs,local/host-bindings.mjs}. Keep client/plan.cjs, client/ops.cjs, local/local-client.mjs and authority/* unchanged; any newly demonstrated need returns an exact request. G1's existing memory modules remain separately scoped. No E source provider writes.
PM240 reserves D as the ONE future provider author AFTER its S3 R1-R10 repair and independent acceptance: W/local/source-admission.mjs; I/{engine-provider.cjs,browser-replay.mjs,local-source-profile.cjs}. Final base is that accepted repaired successor, not ebc4c4e. I/replay-core.cjs stays conditional on demonstrated shared-fold need and a later exact grant. B maps incremental protected pins/CI in parallel while its current review candidate stays frozen; B owns protected workout/source/capture consumers, W/local/today-bindings.mjs and final pins. Any missing actual host lifecycle export must be named before C code. All runtime remains held.
C sole consumer subset: coach/{tools.cjs,local-world.mjs,wave1-tools.cjs,TOOL-CONTRACT.md,model-adapter.md}; T/{today-model.cjs,today-entry.mjs}. Existing memory-view/today-app/build/template paths only for previously named memory surface, plus measured browser/provider closure under B's incremental route. Entry is B-NTC pinned; no pin refresh or host replacement exemption. D's local-source-browser driver remains D-owned; memory adds its own journey coverage, never edits frozen S3 evidence.
New tests: coach/test/{decision-memory.test.cjs,memory-journey.test.cjs}, using actual enrolled host/stage/provider; source owner extends I/test/engine-provider.test.cjs and W/test/local-source-admission.test.mjs for ordered decision barriers and both source families. E extends W/test/{public-client.test.mjs,local-host-journey.test.mjs}; preserve local-client.test.mjs. C extends coach/test/{tiers.test.cjs,wave1-demo.test.cjs,no-dashes.test.cjs} and T/test/{adapter.test.mjs,gym.test.mjs,view.test.mjs}; no weakening old controls. All are proposed custody, not current execution permission.
Literal focused argv: `node --test rebuild/coach/test/decision-memory.test.cjs rebuild/coach/test/memory-journey.test.cjs rebuild/m3/w6/test/public-client.test.mjs rebuild/m3/w6/test/local-host-journey.test.mjs rebuild/m3/w6/test/local-client.test.mjs`; provider argv retains its owned current-head preload with the two named tests. Preserve `node rebuild/t2/rig187.cjs`, the current coach suite and all14 current Today files, A0 and local journey in B's exact final CI. No old suite count becomes a floor by inference. Inspect complete authorized public test closure before any future invocation; no installed/private seed fallback.
New real browser program, C-owned after release: T/test/memory-check.mjs, invoked `node rebuild/m3/w7-preview/today/test/memory-check.mjs`; build actual candidate, drive real Today/coach/host/IndexedDB, fresh contained profile, actual force-kill/reopen. B names exact CI registration/current-source pin for this added program, not an assumed existing glob. Required mutation mode `node rebuild/coach/test/decision-memory.test.cjs --mutations` runs explicitly named isolated source mutations, assertion failures and restored positive controls; B maps that mode separately from node:test discovery.
Positive witness: use the public writers-differential ladder-sweep case (70,82,95,100; inc5) through real sweepLadders, not its separate hand-authored apply-proposal fixture. A w82 control gives nextLoad87 before/95 after by the actual progression reader; this alone is insufficient: select actual current session conditions whose genSession/gym prescription changes, show exact real reason/rungs, yes, one durable decision/effect, legacy/new agreement, close/reopen, later saved workout and reopen retaining its newer load. Exercise actual null source AND S3 selected source with prefix evidence; full memory remains pending if either consumer lacks the real provider join.
Fault cases/mutants: remove yes/body/source/frontier/programme/engine binding; accept spoofed caller effective/IDs or Promise action; remove final CAS/context recheck; acknowledge before commit; apply retry twice or after unknown commit; omit adjustment/feed/proposal timestamp effect; reset absent/null or units; let local ID unlock hosted offer; restore after later workout or leak future correction into prefix; duplicate read/food/workout replay; rebuild active/uncertain host or discard gymDraft; retain stale idle host. Each must fail a named assertion with nonzero matched controls, never count setup/syntax/skip as a kill. Wrong session/lease, failed load/commit/read, races, source rollback/recovery and ordinary decline preserve originals and expose truthful states.
PM240 regression additions in the same named tests/mutation mode: key-order/array-order and absent/null/negative-zero/invalid-surrogate codec controls; wrong domain or identity preimage; altered transcript call/order; engine/T2 confirmation-sample disagreement; Soffer wrongly used as before digest or missing proposal creation; retry after new session/newer workout/rollback without pending map; different retained bodies under one ID; postcommit read-unavailable never reapplies. These supplement, not replace, the existing positives/failures.
Disposition: PM240 finite grammar/S0-Soffer-Sresult/committed-first retry corrections supplied; exact missing controller prefix/current-view/validation exports and owned writer capability identified above for PM/B adjudication. G1, current N2 priority, separate MAX protected review/D2 consumer review, B incremental pins/current bothOS/applicable gate, third integration and real-C2/P1 controls remain. S3 remains independent; this contract adds no S3 prerequisite, runtime grant, owner question or calendar claim.
