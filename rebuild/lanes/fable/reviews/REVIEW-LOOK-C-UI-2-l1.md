# REVIEW-LOOK-C-UI-2-l1 (Fable, independent reviewer, 2026-09-25)

Object: the uncommitted diff in C:\Users\joeym\AppData\Local\Temp\earned-look-cui2 (branch rebuild/c-look-cui2, HEAD df91ad1) against df91ad1. Static review plus one re-run of the look unit tests through pm-run.cjs shared. Nothing committed by me; the only file I wrote is this one.

## VERDICT: ACCEPT WITH NOTES for the PARTIAL delivery as reported. 1 MAJOR (a layering defect the builder did not report), 2 MINOR, 3 NOTES for the PM. The two STOPs (#greeting, #status-line) are correct and grounded.

## What I checked, with the read path
- `git status --porcelain`: exactly 4 modified paths, no untracked file, all under rebuild/m3/w7-preview/today (preview.css, scene.mjs, screens.template.html, today-app.cjs). No pack byte, no engine/conform/m4/spec/tooling/workflow byte, no sealed file (checkin-app.mjs, today-entry.mjs, gym-model.mjs, today-model.cjs, the test cells) moved. design.cjs, build.mjs, browser-check.mjs untouched.
- sha256 re-measured (Get-FileHash, LF, CR=0 in all four): preview.css ebc88c9c..., scene.mjs 6526490e..., screens.template.html 3bf844a3..., today-app.cjs d5c01525... All four match the builder's report. Added diff lines carrying U+2013/U+2014: 0.
- Identities against gate.py :83-93 and JS_FONTMAP :216-228: `.wordmark` is `header.header > div.brand > span.wordmark` with the text "Earned"; app.css:190 gives it var(--serif), no preview rule touches it. `#start span:not(.arrow)` is `button#start.primary > span[data-slot=primary-label]` (bound text) beside `span.arrow` holding the pack's svg; app.css:244 gives .primary var(--serif) and the arrow span is excluded by the selector. Both are real bound elements of the composed screen; no alias, no hidden node, no stand-in. `#greeting` and `#status-line` are absent, as reported (STOP). No gate threshold, baseline or check moved (no quality/ byte).
- The STOP is grounded: DECISIONS:534(c) says move.title and marchingOrder.why "must NOT" be carried into greeting or status-line and the inventory needs a greeting row first; the inventory (states/STATE-INVENTORY-DRAFT.md) has no greeting or status-line copy row; brief s2 lists P1 FIRST. The pack prototype itself draws the engine headline into #greeting (states-today.js T-06 :65, T-11 :83, T-39 :135), which is exactly the tension Q1(a) puts to Joe.
- Copy: every static string in the new t-today is already in design.cjs APPROVED_COPY (:116-118): "Earned", "Your plan for today", "Eat about", "Your full nutrition plan", "How are you feeling today?", "Ask your coach", "Your plan, progress and the reasons behind it.". No new visible string. The board's own labels ("Weigh in before breakfast.", "Recovery check in", "Talk through today's plan") are NOT used; they are Q2. No dash, readiness word or vendor name added.
- Owner rules: Start sits in the fixed .stack (always in the first viewport); embers remain the only animation (no transition/animation in the CSS block; scene.mjs hunks only attach fades); reduced motion untouched; Log is the workout ticket's.
- Pack class vocabulary: every class token in the new t-today occurs in the four pinned stylesheets except `page` (needed by the sealed view.test.mjs:556). .day-foot, .on-scene, .note-block, .marker(.on) are states.css:14-15, :28-29, :32-33, :82-83. T-25's marker placement matches states-today.js:17-19 (a span.marker inside the card's .sub).
- Chassis: the frame is `scene-frame screen screen-today is-active` (scene.mjs:351) and the host is its `.ui`, so app.css:852-868 (.screen .ui > .body / > .stack, fades, safe area) apply; the C-UI-2 override `.scene-frame > .view.ui:has(> .stack)` (0,4,0) restores the pinned absolute flex column over CUI1's D2 rule (0,3,0) only where a .stack child exists, so no other screen moves. scrollFades toggles can-scroll, at-end and at-start (scene.mjs:48-50), the same three the pack's app.js:138 toggles.
- Unit tests re-run (pm-run shared, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York, log %TEMP%\fable-cui2-l1-tests.log): 37 tests, 21 pass, 16 fail; design.test 15/15, scene.test 6/7; the 16 failures are all "Cannot find package" (esbuild 15, jsdom 1: this worktree has no node_modules junction). Identical to the builder's counts.

## Findings
- MAJOR M1 (unreported): the live weigh-in sheet now paints UNDER the fixed stack. today-app.cjs:851 appends `.sheet-panel` to the host; preview.css:33-43 gives it `position: absolute; bottom: 0` and no z-index; app.css:859 gives `.screen .ui > .stack` `position: relative; z-index: 2`; the host is a stacking context (preview.css `.view { z-index: 4 }`). Inside that context the stack (z 2) paints above the sheet (z auto), so Start, Recovery and Talk draw over the sheet's field and controls at the bottom of the screen. They are inert but visible. T-42 to T-52 are LIVE now and the sheet is the product's only weigh-in path until C-UI-3 lands the inline card (ruling 5). Fix is one additive line inside the C-UI-2 block, on a preview-owned class (design.cjs PREVIEW_CLASSES): `.scene-frame > .view.ui:has(> .stack) > .sheet-panel { z-index: 3; }`. Not caught by jsdom cells; the browser check or the phone would show it.
- MINOR N1: `.scene-frame > .view.ui > .stack > .primary { border: 0 }` (0,5,0) also beats the pack's `.primary:disabled { border: 1px solid var(--line-soft) }` (app.css:835, 0,2,0), so the disabled Start (T-06 blocked, T-12 no session, T-03 fallback) loses its pinned edge. Scope the override with `:not(:disabled)` or restate the disabled border in the block.
- MINOR N2: T-39 (headline shrink) is now dead code: fitHeadline (today-app.cjs:337) looks for `[data-slot="primary"]` inside `root`, which is the .body, and Start is in the stack, so it returns null on every call and the MutationObserver at :830 re-runs a no-op. The behaviour is legitimately obsolete under ruling 4 (the stack never moves, so no title can push Start below the fold), but the inventory row T-39 says LIVE now and browser-check's headline sweep still expects the floor. Either strip the fitter and its observer in this ticket (released file, bindings only) or list it: PROPOSED PACK EDIT P6 candidate, inventory T-39 marked superseded by ruling 4. The builder reported the browser-check effect but not the inventory row.
- NOTE N3 (for the PM): the h1[data-slot=instruction] and its section carry no pack class; with `.intro` gone the legacy Additions C `h1` rule (47px, 'Instrument Serif') styles it and `.intro .label` no longer colours "Your plan for today". The gate's notSans sweep (G:225-227) will name it. This is the same red the builder expects for the missing #greeting and resolves with Q1; no action now, but Joe should know the headline he sees until then is in neither pinned face.
- NOTE N4 (for the PM): problem.test.mjs R10 (:309-322) and view.test.mjs:556 conflict on `.page` (R10 allows only pack classes plus PREVIEW_CLASSES; :556 queries `.page`). Both cells are sealed; I confirmed by reading that `page` is the ONLY non-pack token left in t-today, so R10 was red at the head (mast, intro, food, food-grid, small, coach-entry, recovery-link, trend, bottom) and is now red on one token. The PM rules which sealed cell moves red-first in S12.
- NOTE N5: legacy leaks the builder did not name and that are harmless: Additions C `.primary { gap: 12px }` (space-between makes it moot), `.brand { font-size: 27px }` on the div (no text node of its own), Refinement A `button:active:not(:disabled) { transform: translateY(1px) }` (pre-existing on every screen; a transform, not a transition, so JS_ANIM does not see it).
- Additive and delimited: preview.css is one BEGIN/END block; screens.template.html wraps the whole t-today in BEGIN/END (the ticket's MAY CHANGE names t-today, so a rewrite is in scope); scene.mjs hunks carry "C-UI-2:" comments; today-app.cjs hunks carry "C-UI-2:" comments, two of them modify existing lines (show()'s signature, the weigh-sheet inert lines) rather than add, both backward compatible. C-UI-3 will have to edit inside the t-today block (the proposal card goes into .timeline); the PM should sequence that, as the ticket already says.

## Expected gate rows (agree with the builder, with one change)
- Green: primary in first viewport, bottom safe area, wordmark serif, Start serif, tertiary links no underline, card inner edge for the four named cards, icon columns for #recovery and #talk-today.
- Red from this ticket's scope: KNOWN_FACE today (#greeting/#status-line null); "serif for names and numbers, sans for the rest" (#greeting missing, the h1 in 'Instrument Serif'); right glyph column (#card-train .chev, #plans-changed); fits without scrolling; visual regression and every T statesheet row until Q1/Q2; type scale WARN 47px.
- M1 is not a gate row (the gate never opens the sheet) but it is a phone-sheet defect (brief s7 (3)).

## Proposed pack edits (all as text for the PM's list, none applied)
- P1 (builder's): Today greeting row and status-line row in states/STATE-INVENTORY-DRAFT.md, text per Q1.
- P6 candidate (mine, N2): inventory T-39 row marked superseded by ruling 4 once the fitter is removed.

## Questions for Joe: the builder's Q1 to Q4 stand as written; I add none. Q1 is the one that unblocks both STOPs.
