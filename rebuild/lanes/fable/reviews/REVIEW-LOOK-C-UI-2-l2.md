# REVIEW-LOOK-C-UI-2-l2 (Fable, independent reviewer, 2026-09-25)

Object: C-UI-2 fix round 2 in C:\Users\joeym\AppData\Local\Temp\earned-look-cui2 (branch rebuild/c-look-cui2, HEAD df91ad1), uncommitted, checked against REVIEW-LOOK-C-UI-2-l1.md (M1, N1, N2) and against df91ad1. Static read of every hunk plus one re-run of the unit suites through pm-run.cjs shared. I wrote neither round. Nothing committed; the only file I wrote in the worktree is this one.

## VERDICT: READY TO PUSH. M1 and N1 are fixed exactly as l1 asked (N1 with one reasoned narrowing), N2 is left as instructed, nothing regressed. One MAJOR item for the PM at composition (M2, not a builder defect of this round): the new test file joins CUI1's scene.test.mjs in the H3/13 red.

## What I checked, with the read path
- `git status --porcelain --untracked-files=all`: 4 M (preview.css, scene.mjs, screens.template.html, today-app.cjs, all under rebuild/m3/w7-preview/today) + 2 untracked (rebuild/lanes/fable/reviews/REVIEW-LOOK-C-UI-2-l1.md, rebuild/m3/w7-preview/today/test/look-cui2.test.mjs). No pack byte, no rebuild/engine, conform, m4/spec, tooling or workflow byte, no sealed file edited. `git diff --stat df91ad1 -- <the four paths>`: 112+ 41-.
- sha256 re-measured (Get-FileHash, LF, CR=0, added U+2013/U+2014 = 0): preview.css 6b84a944... (10795 B, its 2 dashes at :90-91 are pre-existing, count unchanged), look-cui2.test.mjs 7197d784... (3593 B, 0 dashes), scene.mjs 6526490e..., screens.template.html 3bf844a3..., today-app.cjs d5c01525... The last three are byte-identical to l1's measured hashes, so the l1 read of them stands; only preview.css and the new cell moved this round. Rules file 51706c33..., brief rev2 952f6e12... as commissioned.
- preview.css delta vs l1 = exactly the three single-occurrence replacements of %TEMP%\cui2-opus55\r2-fix.cjs (two comment additions, the rule split, the z-index line), matching the full `git diff df91ad1 -- preview.css` I read. Line refs check: N1 comment :164-165, M1 comment :168-170, rules :173 (font-weight: inherit), :174 (:not(:disabled) border: 0), :175 (sheet-panel z-index: 3).
- M1 fixed: the sheet is `form.sheet-panel` (screens.template.html:98, template("t-weigh") at today-app.cjs:841), appended to `phone` (today-app.cjs:851), and `phone` is the same #phone element scene.mjs:326 makes the `.view.ui` host inside `.scene-frame`; the stack is a direct child of the host (show() replaceChildren(root, stack)). So `.scene-frame > .view.ui:has(> .stack) > .sheet-panel { z-index: 3 }` (0,5,0) matches exactly while Today is mounted, beats preview.css:33 `.sheet-panel` (0,1,0, position absolute, no z-index), and 3 > the pack's `.screen .ui > .stack { position: relative; z-index: 2 }` (app.css:859) inside the host's own stacking context (`.view { z-index: 4 }`, now position absolute under the C-UI-2 chassis rule, still a context). `bottom: 0` keeps resolving against the host. No other screen has a .stack child, so nothing else moves.
- N1 fixed: the legacy 2026-09-08 rule is `.primary{...border:1px solid var(--ink)...font-weight:600}` at (0,1,0) (Earned-additions-C-approved.html:5 inline style). Disabled Start now: border from the pack's `.primary:disabled { border: 1px solid var(--line-soft) }` (app.css:835, (0,2,0)) beats the legacy border; the C-UI-2 `:not(:disabled)` rule (0,6,0) no longer applies. Enabled Start: border 0 as before. Departure accepted: `font-weight: inherit` stays unscoped because the pack sets no weight on `.primary:disabled` and the legacy 600 would otherwise return on the disabled state; that is the pack's own weight (button font: inherit), not a new value.
- N2: fitHeadline (today-app.cjs:335-337) and its observer (:828-830) untouched, P6 listed as text. As instructed.
- New cells (look-cui2.test.mjs): bytes only, reads preview.css and the pinned app.css, imports node:assert/fs/path/test only; no DOM, no engine, no protected file. Red-first evidence preserved: %TEMP%\cui2-opus55\r2-red.tap has both cells `not ok` on the pre-fix bytes (M1 "0 !== 1" at :47, N1 "scoped away from the disabled state: .scene-frame > .view.ui > .stack > .primary" at :62); r2-green.tap 2/2 after. The cells are not vacuous: M1 requires the pack to give the stack exactly one z-index and the lift to exceed it; N1 walks every C-UI-2 rule on `.primary` that touches a border property.
- Identities (template unchanged since l1, re-read :21-96): `.wordmark` = span.wordmark "Earned" inside header.header > div.brand; `#start span:not(.arrow)` = the bound span[data-slot=primary-label] beside span.arrow. #greeting and #status-line absent (STOP stands). #card-train carries no chevron (R4), no "Plans changed?" (R5), the Train card has no tap action and the weigh-in path is the sheet (R6). No new visible string in the diff (CSS declarations and comments only this round).
- Unit suites re-run (pm-run shared, job fable-cui2-l2, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York, log %TEMP%\fable-cui2-l2\l2-suites.tap): design.test.cjs + scene.test.mjs + package.test.cjs + look-cui2.test.mjs = 38 tests, 23 pass, 15 fail; all 15 are "Cannot find package 'esbuild' imported from rebuild/m3/w6/build-browser.mjs" (14 package.test hookFailed, 1 scene.test #38), the same set as the builder's r2-suites.tap. look-cui2 ok 16-17, design 15/15, scene 6/7. l1's 37/21/16 counted copy.test.mjs's file-level jsdom failure; the builder correctly dropped copy.test.mjs this round (it loads rebuild/engine/index.cjs; listed for the PM, as is view.test.mjs). I did not run copy.test.mjs or view.test.mjs.

## Findings
- MAJOR M2 (for the PM at composition, not a fix for this round): rebuild/m4/workout/test/h3-clean-init.test.cjs H3/13 (:720-746, DECISIONS:186) reads the rebuild.yml today step and asserts `readdirSync(rebuild/m3/w7-preview/today/test)` filtered to *.test.(mjs|cjs) deepEquals the files that step names, both directions. Measured statically at this head (%TEMP%\fable-cui2-l2\h313.cjs, a replica of the cell's own reads, not the cell): named 18, on disk 20; on disk but not named = look-cui2.test.mjs AND scene.test.mjs (CUI1's, at 3d01e5a). So H3/13 is already red at the look head from CUI1, and this round adds a second name to the same red. The brief forbids any look hunk in rebuild.yml beyond the standing step (s6, s13) and look-gates.yml does not satisfy H3/13 (it reads rebuild.yml). The PM rules ONE of: (a) declare H3/13 a sealed cell that moves red-first for both files under STOP-S12-NEEDLES / STOP-S12-CI; (b) relocate both look cells out of today/test/ to a CHILD_ROOTS dir with no closed enumeration (rebuild/lanes/c/ui-port/ beside approved-pin/pack-pin, or rebuild/m3/w7-preview/test/; the only readdirSync over a test dir in the child roots is H3/13's), adjusting the relative paths in look-cui2.test.mjs (:9-11) and scene.test.mjs; (c) a rebuild.yml today-step name for both, which the brief currently STOPs. Neither builder nor l1 named this; the builder's "needs a b-package child and a look-gates.yml step" is necessary but not sufficient.
- NOTE N6: look-cui2.test.mjs's `rules()` parser ignores @media nesting (app.css has nested blocks); it is exact enough for the two selectors it filters (`.screen .ui > .stack` has one z-index rule at app.css:859, `.primary:disabled` one border rule at :835) and both cells assert that count, so a second pinned rule would turn them red rather than silently pass. Fine as a static pin.
- NOTE N7: `:has()` now carries the sheet lift as well as the chassis rule (round 1). Same browser floor as before (Chrome 105+, Safari 15.4+); the phone sheet check (exclusive slot, not yet done, brief s7 (3)) is where M1 and N1 get their visual proof. No change asked.

## Unchanged from l1 (still true): N3 (the h1 in 'Instrument Serif' until Q1), N4 (.page vs problem.test R10, ruled R1: keep .page), N5 (harmless legacy leaks), expected gate rows, P1 and P6 as text, Q1-Q4 for Joe.

## Disclosures
- One `git grep` of mine (design.cjs sheet list) had its `--` path filter broken by PowerShell's handling of an embedded double quote and searched the whole checkout; its output went to a file that I only filtered by line for the design.cjs / today-app.cjs / scene.mjs lines named above. No other line of it was read or reproduced. No soak, conform/private, ledger, src/ or protected-five content is in this review.
- Scratch: %TEMP%\fable-cui2-l2\ (s1-s11.ps1, l2-suites.cmd, l2-suites.tap, h313.cjs). Nothing under rebuild/ was written except this file.
