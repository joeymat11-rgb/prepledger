# REVIEW-LOOK-C-UI-3-l1 (Fable, independent reviewer, 2026-09-25)

Worktree %TEMP%\earned-look-cui3, branch rebuild/c-look-cui3 at 5296f20, uncommitted C-UI-3 build (5 M + 1 A, all under rebuild/m3/w7-preview/today/; sha256 of every file re-measured and equal to the builder's six). Read: opus55-RULES.txt 51706c33, S12-LOOK-BRIEF-DRAFT.md rev2 952f6e12, DECISIONS :817/:820/:821 (origin/rebuild/t2-client-core after fetch, 826 lines), pack README C-UI-3 :482-494, states/TICKET-proposal-response.md, app/states-today.js T-40..T-52, app/app.html #weigh-form and #card-proposal, app/app.css and states.css proposal rules, quality/gate.py :89-93 and :292-293, engine/today.cjs :568-581 and engine/writers.cjs (propose :1645-1672, applyProposal :2120-2268, dismissProposal :2382-2384, applied :1630) by explicit-path git grep and byte-exact git show; S11's today-entry.mjs at origin/rebuild/e-native-load-build 189d076 :158-179 and :268-292. No protected five loaded, no src/, no conform/private, no ledger.

## VERDICT: NOT READY (one builder fix round F1; two PM rulings R1, R2; the rest is accept-quality with the named debts below)

## Findings

F1 FIX (today-app.cjs, MAY CHANGE): plainOrDrop drops the pack's own sentence with the engine's reason. proposalFace composes `why + " " + "Nothing changes until you say yes."` into ONE string and bindProposal passes it whole to plainOrDrop. Every reason the engine files for the kinds this card draws carries U+2014: writers.cjs:1900 (volpush sets, "... nothing looking wrong - lifts not falling ..."), :1915 (volroll sets), :143 (ladder, "... does not divide - so the engine ..."). So on every real engine card the open state shows NO reason and NO "Nothing changes until you say yes."; the test's SETS.why is dash-free, so cell 22 never sees it. Same for the lift fallback: `lift = card.lift || card.title` and every engine title carries U+2014 (writers.cjs:1899 "CHEST - EARNED VOLUME: ...", :142 "X - N REAL RUNGS", :1518/:1528/:1544 agent titles), so a card with no exId (kind "cal": bandtop/redline :1791/:1793, steppush :1779) draws #proposal-lift EMPTY under the eyebrow. Fix: drop the why alone (plainOrDrop(why) then the pack sentence, which is dash-free and always shown), and when the lift is empty after the drop do not draw the card (or PM rules what the serif slot shows). Red-first: a cell with a dashed why and a dashed title.

R1 RULING (STOP-CUI3-GATE-LIFT, confirmed): gate.py :220-224 requires every SERIF_SELECTORS entry, '#proposal-lift' included (:90), on `.screen.is-active`, rendered, at the default URL `?theme=..&screen=today&chrome=..&date=board` (:293). fixtures.cjs:32 gives the sample athlete `proposals: [], agentProposals: []`, today-model read() projects null, bindProposal removes the card, so the gate FAILs "#proposal-lift is not on the page" on every run of this build. The identity is real and bound (cell 21 verified: one #proposal-lift, class lift, data-slot, inside #card-proposal after #card-train, pack app.css:230 `.proposal .lift { font-family: var(--serif) }`), but unreachable by the gate. Options for the PM, none taken here: (a) the synthetic athlete carries ONE open proposal in propose()'s exact shape (writers.cjs:1672: {rid, id: rid_date, d, title, why, apply:{kind:"sets", exId, delta:1, mg, owner:true}, resolved:false, pg}) targeting a fixture exercise, in fixtures.cjs (moves every sealed cell over the sample view); (b) the board-date replay runs the engine's own owner's-call producer (writers.cjs:1937-1956, exId ham/press/rearDelt) so the card is engine-issued; (c) the scene `state` review hook (scene.mjs:20, :340 root.dataset.reviewState, read by nothing today) selects a fixture with a proposal for review runs only. (c) is closest to a prototype substitution and needs the :732 test.

R2 RULING (STOP-CUI3-RECORD, confirmed): the yes/no answer lives in mountToday's proposalAnswers Map only; nothing writes it (TICKET-proposal-response.md :7-9: `respond` is outside the staged command set). This IS the design of record: README C-UI-3 LOCKED "until then the card stops at recorded" and ACCEPTANCE "a tap on a decision records and Change my answer restores". But T-40b's "It applies when your plan is next built." is not true on this build, and under :820 ("an element with no action is never drawn tappable") the two decisions enact nothing durable. PM confirms the README reading (debt D-CUI3-RECORD: page-held, lost on reload, sentence made true only by lane B) or pulls the decisions until lane B (then no T-40b/T-40d on the app, a recorded departure).

F2 OK, traced: "Applied" only from stored evidence. today-model storedProposalAnswers marks "applied" only for a RESOLVED proposal with an adjustments row of its rid that is neither undone nor dismissed, the same predicate as writers.cjs:1630 `applied`; applyProposal pushes that row at :2153 (cal) and :2177 (before every other kind branch: sets :2183, ladder :2203, refeed, exit, phase, break, phasePlan) so no applied kind is missed; dismissProposal :2384 pushes {dismissed:true} and sets p.dismissed (declined); undoLast :2418-2419 reopens the proposal and marks the row undone; superseded/stoodDown/resolvedHow are every withdrawal path (:1669, :1696, :1710, :1721, :2059, :2081, :2133, :2138, :2172). proposalFace draws a stored state only for the id this page showed and only when no card is open; a tap sets "recorded"/"declined" only (cell 23). The rebuild has no local command that applies, so "Applied" can arise only from admitted history. Honest.

F3 OK: openProposalCard is exactly the engine's own pick (today.cjs:577 first9 = first unresolved proposals entry else agentProposals[0]) and decisionsN (:568) counts the same two stores, so the status line and the card agree. Lift = exercises[].n by apply.exId, a lookup only; no number is derived or shown; "Add one set this week" binds only to kind "sets" && delta === 1 (writers :1901/:1916/:1956 file sets with delta from the engine), break/ladder words only to those kinds; every other kind draws no decision.

F4 OK: words. All 23 new strings are in RUNTIME_COPY under the C-UI-3 comment and each is in states-today.js (cell 28); the three template strings ("Your weight in pounds", "Your weight", "lb") are the board's app.html:43-45 and the existing t-weigh sheet's (:138/:141); design.test 5-7 green. No fake, alias or hidden identity; #weight does not collide with the sheet's #morning-weight. Status lines T-40/T-40b/T-40d are states-today.js:11/:142/:148 verbatim, drawn only for one open proposal on an unstarted session over his own figures (cell 24). Gold edge: app.css:824 `.proposal { border-color: var(--line-gold) }`, :831 lowers it for .is-recorded/.is-applied/.is-declined/.is-superseded; preview.css adds no .proposal rule (cell 25). states.css (eyebrow :89, is-invalid :41) is in the shipped bundle (design.cjs:44-50).

F5 OK with a note: earned-weight card CSS. `[data-native-load="offer"]` matches NOTHING on this branch (today-entry.mjs here has no native-load; it arrives at the S11 merge-forward). Shape verified against S11 189d076 today-entry.mjs:280-289 (h3, ul > li > span[set-load], p[data-native-load=reason], button.btn[data-native-load=yes|decline]); the rule set uses only pack tokens and pack type sizes, sans only, no serif identity (cell 27). column-gap 9px and margin-top 10px are not traced to a pack rule (spacing WARN possible). Cannot be seen on the phone sheet until S11 lands: debt, composition item.

F6 DEPARTURE RECORD CORRECTION: "T-40h not drawn: the engine issues no weight-change proposal" is inaccurate. writers.cjs:1544 files agentProposals kind "reset" with newW beside ex.w (and applyAgentProposal :2352 applies it): a bound "Use <newW> / Keep <ex.w>" exists in principle. The prototype itself marks T-40h DORMANT (states-today.js:159), so not drawing it is within the design of record; record it as "dormant in the prototype; the reset producer's reachability on the rebuild replay is unmeasured", not as an engine absence.

F7 DEBT: the eyebrow "One call needs you" is drawn over kinds with no decisions (cal, sets with delta != 1, volume/reset agent cards) while the status line says "One change to review."; nothing is tappable (:820 kept) but the card invites a call it cannot take. PM: draw nothing for undrawable kinds, or accept as debt until the prototype has words for them.

F8 DEBT (builder-named, confirmed): two weigh-in surfaces on Today while the weight is owed: the inline form and the primary button still opening the t-weigh sheet (today-app.cjs:1025 openWeighIn). Retiring the sheet moves the sealed cells the builder lists; composition item. The inline path is the sheet's own (model.weighIn(raw === "" ? raw : Number(raw)), the same call as :1142; OUT_OF_RANGE is exported at today-model.cjs:525; refusals are the model's sentences; the field is flagged only for empty/NaN/out-of-range; T-48 refused on submit, not disabled up front, a recorded departure).

F9 ENV: scene cell 35 red on esbuild (no node_modules junction in this worktree), identical at the builder's base run; not a C-UI-3 red. The builder's disclosed junction slip (earned-adm -> prepledger-dev) is noted; not reproduced here.

## Runs (pm-run shared, job fable-cui3-l1-run1, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York, cui3-guard.cjs + cui3-guard-esm.mjs preloaded)
- Guard self-test: CJS require of engine/index.cjs refused; ESM import of engine/seed.cjs refused; engine modules loaded 0 in every process.
- RED at 5296f20 (%TEMP%\cui3-red, today-app.cjs blob d4b16659 = git 5296f20:today-app.cjs): look-cui3 0 of 8 pass (all 8 not ok).
- GREEN on the build: look-cui3 8/8, look-cui2 5/5, design 15/15, scene 6 of 7; 34 of 35, only "C-UI-1 build reaches the scene through the actual offline preview bundle" red (esbuild).
- NOT RUN (load the protected five via today-engine.cjs or directly; listed for the PM): today/test/copy.test.mjs, view.test.mjs, adapter.test.mjs, setup.test.mjs, package.test.cjs; w7-preview/test/model.test.cjs, package.test.cjs; import/test/page-bundle.test.mjs. Sealed today-model.cjs read() gains `proposal` and `proposalStored`; any deepEqual over a read() view moves (declared).

## Bytes
- Changed: today-app.cjs 0f697a00.., today-model.cjs 8ea21684.., screens.template.html 63002dda.., preview.css f794cb10.., design.cjs 74d157b1.., test/look-cui3.test.mjs 569f53d3.. (LF; 316 added lines, 0 with U+2013/U+2014; test file dash-free).
- No pack, engine, conform, m4/spec, tooling, workflow byte (git status: only the six above). Pack edits P2-P5c PROPOSED only in %TEMP%\cui3-pack-proposals.txt 10483526.. (re-hashed); P2's words checked against S11 NATIVE_LOAD_COPY 189d076 :158-168 and :174 (undoHeading) and today-entry.mjs:281 ": next weight"; NL_REASON is the open STOP-S12-COPY.
- This review: rebuild/lanes/fable/reviews/REVIEW-LOOK-C-UI-3-l1.md (new, uncommitted). Nothing committed or pushed.

## AAR
asked: independent l1 review of C-UI-3 against the brief, the pack and :817/:820, suites re-run under the guard.
happened: every hunk read, engine paths traced by explicit-path grep, red 0/8 and green 34/35 reproduced, gate reachability checked against gate.py and fixtures.cjs.
rework: F1 (dash drop kills the pack sentence and the lift fallback on every real card) plus the departure record F6; rulings R1/R2 are the PM's.
escaped: the builder's own cells use dash-free fixtures, so F1 never went red; the T-40h reason was asserted, not measured.
lesson: a copy-drop rule must be tested with the engine's actual strings (they all carry U+2014); a gate identity needs a reachable state before it is called built.
cost: about 55 minutes, 14 PC calls, one shared slot for about 25 s.
