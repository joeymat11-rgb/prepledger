# REVIEW-LOOK-C-UI-3-l2 (Fable, independent reviewer l2, 2026-09-25)

Worktree %TEMP%\earned-look-cui3, branch rebuild/c-look-cui3 at 5296f20, round 2 uncommitted (git status: 5 M under rebuild/m3/w7-preview/today/, 2 untracked: the l1 review and test/look-cui3.test.mjs). Bytes re-hashed and equal to the builder's table: today-app.cjs 659bbf4f, today-model.cjs 72b5b357, test/look-cui3.test.mjs 6800f29a; screens.template.html 63002dda, preview.css f794cb10, design.cjs 74d157b1 unchanged. Read whole: opus55-RULES.txt, S12-LOOK-BRIEF-DRAFT.md rev2, REVIEW-LOOK-C-UI-3-l1.md, the two diffs (311 added lines), the test file, today-model.cjs :40-135 and :300-500, today-app.cjs :330-410 and :914-1030, today-engine.cjs; by explicit path only: writers.cjs :1538-1546, :1612-1622, :1644-1673, :1934-1957; today.cjs :378-392, :486-531; states-today.js :44-70; screens.template.html :71-92; fixtures.cjs :4-38 (grep). No protected five loaded or shown, no src/, no conform/private, no ledger, no prepledger-dev path, no junction created (none present in the worktree).

## VERDICT: NOT READY (one small builder fix, L2-1, red-first; one PM item, L2-2; everything else is accept-quality and the four builder STOPs stand as listed)

## Findings

L2-1 FIX (today-model.cjs read(), pendingAdoption gate; MAY CHANGE): the pending-adoption Today loses its status line. Round 2 puts the R1 proposal into the basis, so the projection's nowModel.decisionsN is 1 for the sample athlete on every read. The pendingAdoption branch (today-model.cjs :463-474) sets view.proposal = null ("the fixture's proposals are not his either") but keeps nowModel, so decisionsN stays 1; bindProposal then draws no card, statusLine takes its open-proposal branch with card undefined and returns "". Measured with the status-line block evaluated alone (%TEMP%\fable-l2\probe.log): round-1 bytes on the pending-adoption view shape (exerciseCount null, decisionsN 0) say "Nothing scheduled today, so there is nothing to start."; round-2 bytes on the same view with decisionsN 1 say ""; with decisionsN 0 they say the round-1 line again. That line is the recorded departure F4 (today-app.cjs :357-361: the pending-adoption Today says T-12's words instead of T-03's), and round 2 breaks it without naming it. No cell covers it: cell 24 has no pending-adoption row. Fix: the same gate that hides the fixture's proposal should not let its count stand (nowModel.decisionsN 0 beside view.proposal null, under the existing "not his" comment), or statusLine falls through to the phase branches when the view carries no proposal; red-first with a pending-adoption row in cell 24 (view exerciseCount null, decisionsN as the model hands it, card undefined, want the F4 line). Not a fabrication either way: the count being removed is the fixture's own hidden card.

L2-2 PM ITEM (not a block; two rulings meet): the live open card ends its reason with "Nothing changes until you say yes." (proposalFace, words present for the R1 sets card) while R2 removed Yes and No from the live face. The sentence is true (nothing can change) and F1 said keep it, but it invites the call the card cannot take, which is exactly why F7 dropped the eyebrow and why the builder dropped it on every live card. PM: keep it live (F1 as ruled) or drop it on the live face only until lane B stores an answer (one line in liveProposalFace, registry faces untouched). Recorded either way.

F1 OK, verified: proposalFace cleans each field with the page's plainOrDrop handed in (bind :850 passes plainOrDrop); the reason is dropped alone (clean(open.why) || ""), the pack sentence is appended after it for decision kinds and never dropped; the lift (card.lift || card.title) is cleaned alone and an empty result returns null, so no card. Cell 29 is red-first (red run: not ok 9 on round-1 bytes) and uses engine-shaped strings: EM in the volpush title and reason shapes (writers.cjs :1899-1900), EN in the owner's-call range (:1939), plus an unspaced shape plainCopy cannot rewrite. The builder's correction of the l1 premise is right: the probe log shows plainCopy rewrites spaced dashes to ": " and ranges to " to "; only an unrewritable shape drops.

R1 OK, verified as option (a), nothing faked: (b) is unreachable by fixture data. Explicit-path grep of writers.cjs, today.cjs, sleep.cjs, progression.cjs, today-model.cjs, today-engine.cjs, food-model.cjs, sleep-model.cjs, today-readings.cjs, fixtures.cjs, today-entry.mjs, gym-model.mjs: runAdaptive appears only at its definition (writers.cjs:1615), in two comments and in the export list; no call site; the owner's-call producer (:1944-1957) runs inside it. The fixture proposal (today-model.cjs withBoardProposal :196-209) is byte-shaped like propose() :1672 ({rid, id rid_day, d, title, why, apply, resolved:false, pg: planGen || 0}, key order asserted in cell 31), with the owner's-call apply of :1956 ({kind:"sets", exId, delta:1, mg, owner:true}), the rid family of :1954 ("volpush_chest_" + Monday) and the producer's own seen guard (:1948-1950, proposals and adjustments). The target is the fixture's own exercise "demo-press" / "Chest press" (fixtures.cjs:11), so #proposal-lift binds an existing name; title and reason are states-today.js:46 SET words verbatim (asserted against PROTO in cell 31), and the title is never shown because the lift wins. No gate, scene or review-hook substitution: gate.py untouched, scene.mjs untouched, the card is bound on the default route. fixtures.cjs is byte-identical (STOP 1: carried in today-model.cjs createBasisState :93 because fixtures.cjs is pinned at m4/spec/acceptance-load-writes.json:97; PM approves or not). Side effects measured: the sample athlete's headline is protected (planMove :126-131 strips proposals before the second nowModel); today.cjs reads proposals only at lastUndoable :382 (adjustments, none), coachOwed :432 (gate === "coach", not this card), statusTarget :522 (exported, not called by today-model) and nowModel :568-581 (decisionsN 1, move replaced). The S1 comment's "returned by IDENTITY" no longer holds for the sample athlete (a second projection now runs on every read); harmless, note it. The T-02 line change (STOP 2) is grounded: states-today.js :49-51 T-02 calls no noProposal, so the board's sample face draws the card under "Upper body today. Sample data."; the app now does the same (probe: "Upper body today. Sample data."). Cell 31 red-first (red: not ok 11).

R2 OK, verified: liveProposalFace (:512-517) takes no answer, nulls yes, no and kind on the open face and returns null for recorded, declined and applied; bindProposal (:850-876) holds no answers, always removes proposal-decisions and proposal-undo, binds no Yes/No/"Change my answer", and the string "It applies when your plan is next built." lives only in proposalFace's recorded face, which the live route cannot reach. Withdrawn (T-40e) stays live and comes from storedProposalAnswers (superseded / stoodDown / resolvedHow) only. Debt D-CUI3-RECORD named at the rule and at the bind. STOP 3 (no eyebrow on any live card) is consistent with F7. Cell 30 red-first (red: not ok 10).

F6 OK: the record now says T-40h is DORMANT in the prototype (states-today.js:159, status: 'DORMANT') and names writers.cjs:1544 (sweepStalls files agentProposals kind "reset" with newW beside ex.w, applied at :2352). sweepStalls is called only from sweepLab :1566; nothing on the preview calls sweepLab (explicit-path grep of writers.cjs, today.cjs, today-model.cjs, today-engine.cjs, food-model.cjs, sleep-model.cjs). Cell 32 red-first (red: not ok 12).

F7 OK: no eyebrow for a kind with no decisions (proposalFace kind ""), and no eyebrow at all on the live face; the empty head is removed at bind ((kind.parentNode || kind).remove()) and that parent is template :78 <div class="head"> holding only the kind, so #proposal-lift (:79, a sibling) survives and the gate identity is untouched. The prototype has no open card without Yes and No (T-40, T-40f, T-40g, T-40h), so there was no wording to borrow; recorded as a departure in the block comment. Cell 22 red-first (red: not ok 2).

Bytes and boundaries: 0 CR in the three changed files, LF last byte; 311 added lines (222 app, 89 model) with 0 U+2013/U+2014; test file dash-free. git status shows only the five M and two untracked: no pack, engine, conform, m4/spec, tooling or workflow byte. Test reads writers.cjs as text and requires only plain-copy.cjs and fixtures.cjs (fixtures.cjs has no require); the guard reported 0 engine modules in every process. No junction in the worktree (Get-ChildItem ReparsePoint sweep, depth 4: none); neither my cmd files nor the builder's name prepledger-dev.

Sealed cells: the builder's consumer list (every reader of createBasisState or the default createTodayModel; m3/w6 host and journey tests; w7-preview import and measure tests; lanes/c p3/s9; browser-check and the T-02 statesheet) is declared, not run here either; the moves are unmeasured until S12 composition, and machine-settings-ui.test.mjs moving is the C-UI-5 STOP (builder STOP 4). L2-1 adds no file to that list.

## Runs (pm-run shared, jobs fable-cui3-l2-green and fable-cui3-l2-red, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York, cui3-guard.cjs + cui3-guard-esm.mjs preloaded; slots held 0 s wait each)
- GREEN on the build (%TEMP%\fable-l2\green.log): look-cui3 12/12, look-cui2 5/5, design 15/15, scene 6 of 7: 38 of 39; the one red is scene 39 "C-UI-1 build reaches the scene through the actual offline preview bundle" (esbuild, no node_modules junction here), the same env red as l1 and the builder, named not fixed. Guard: engine modules loaded (0).
- RED (%TEMP%\fable-l2\red.log), my own scratch tree %TEMP%\fable-l2\red from cui3-red.tar with round-1 bytes today-app.cjs 0f697a00 and today-model.cjs 8ea21684 (= l1's round-1 hashes) and the final test 6800f29a: look-cui3 6 of 12; not ok 2 (F7), 4 (T-02 line), 9 (F1), 10 (R2), 11 (R1), 12 (F6). Real red for every round-2 cell. Guard: engine modules loaded (0).
- Probe (%TEMP%\fable-l2\probe.log, pure vm evaluation of the status-line block): the L2-1 measurement above.
- NOT RUN: anything loading the engine or the protected five (copy, view, adapter, setup, package and the consumer list).

## Bytes
- This review: rebuild/lanes/fable/reviews/REVIEW-LOOK-C-UI-3-l2.md (new, uncommitted, LF, dash-free). Nothing else written in the worktree. Scratch: %TEMP%\fable-l2\ (scripts, logs, red tree). Nothing committed or pushed.

## AAR
asked: independent l2 review of C-UI-3 round 2 against the PM's rulings F1, R1, R2, F6, F7; suites re-run under the guard through pm-run shared.
happened: every hunk read; R1's (b) unreachability and (a) shape traced by explicit-path grep and byte reads of writers.cjs; R2 traced through the live face and the bind; red 6/12 on round-1 bytes and green 38/39 reproduced in my own scratch; a pure probe of the status-line block found the pending-adoption line lost.
rework: L2-1 (the hidden fixture proposal still counts in decisionsN under the adoption gate, so the F4 line goes empty), one small fix plus one cell row; L2-2 is a PM word.
escaped: the builder's status-line cell has no pending-adoption row, so a registered face changed without a red; l1 could not see it because round 1 had no basis proposal.
lesson: when a fixture gains state, walk every view gate that hides that state and check the counts derived from it are hidden too; a departure record (F4) is a test row waiting to be written.
cost: about 50 minutes, 22 PC calls, two shared slots for about 20 s each.
