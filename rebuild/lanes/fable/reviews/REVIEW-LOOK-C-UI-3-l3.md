# REVIEW-LOOK-C-UI-3-l3 (Fable, independent reviewer l3, 2026-09-25)

Worktree %TEMP%\earned-look-cui3, branch rebuild/c-look-cui3 at 5296f20, round 3 uncommitted. git status: the same 5 M under rebuild/m3/w7-preview/today/ as round 2 plus 3 untracked (the l1 and l2 reviews, test/look-cui3.test.mjs); nothing under pack, engine, conform, m4/spec, tooling or workflows; no reparse point in the worktree (depth 3 sweep); no path into prepledger-dev in any cmd file of mine or the builder's. Rules file opus55-RULES.txt read whole (sha 51706c33). Read: REVIEW-LOOK-C-UI-3-l2.md whole, today-model.cjs :455-485 and :507-530, today-app.cjs :330-545, look-cui3.test.mjs :1-70 and :160-205 plus the whole round-2 to round-3 diff of the test, cui3-guard.cjs, pm-run.cjs, the builder's red/green/mut cmd files. writers.cjs only counted as text by node (lines containing "until you say yes": 0). No protected five loaded or shown, no src/, no conform/private, no ledger, no soak path.

## VERDICT: ACCEPT (L2-1 and L2-2 fixed as ruled, the STOP1 adoptBasis cell is in and bites, nothing else changed versus round 2)

## Bytes: identity versus round 2 proved by reconstruction
Round-2 bytes of today-app.cjs and today-model.cjs no longer exist anywhere on the PC (only the round-2 test, 6800f29a, at %TEMP%\cui3r2-red). So I reversed the builder's described edits on the round-3 bytes with a node script (%TEMP%\cui3-l3\recon.cjs) and hashed the result:
- today-model.cjs: put back the round-1 one-line "not his" comment in place of the round-3 three-line one and delete the one added line `if (view.nowModel) view.nowModel = { ...view.nowModel, decisionsN: 0 };` -> sha256 72b5b357 = the round-2 hash recorded by l2. So round 3 changed exactly those 3 lines (+3 lines, 566 -> 569).
- today-app.cjs: put back the round-2 F1 comment wrap ("is always kept; when the bound lift name") and the one-line `if (face.state === "open") { face.yes = null; face.no = null; face.kind = ""; }` in liveProposalFace -> sha256 659bbf4f = the round-2 hash. So round 3 changed exactly the F1 comment (+1 line) and liveProposalFace (+6 lines: the 3-line L2-2 comment, the strip line, the reflowed braces); 2280 -> 2287 lines.
- Every added byte in both files is therefore the ASCII text of my reconstruction script: 0 U+2013, 0 U+2014, 0 CR, last byte LF (measured on the round-3 files: CR=0 in all three changed files; the U+2014 counts in the whole files, 25 app and 6 model, are pre-round bytes already accounted for by l1 and l2).
- test/look-cui3.test.mjs b2a1c21c versus round-2 6800f29a: 60 insertions, 2 deletions, dash-free (EM and EN are built with String.fromCharCode). The deletions are the two assertions L2-2 reverses (cell 10 and cell 11 expected the sentence on the live reason).
- Round-3 hashes re-measured and equal to the builder's table: today-app.cjs e5a8f2e6..., today-model.cjs 4db1be46..., test b2a1c21c...; design.cjs 74d157b1, preview.css f794cb10, screens.template.html 63002dda, fixtures.cjs 42d16ee3 unchanged.
- No new visible word: the round-3 delta adds no string constant and no template byte; cell 8 (every C-UI-3 word declared in design.cjs) stays green with design.cjs unchanged.

## Findings
L2-1 OK (today-model.cjs :470-475): inside the pending-adoption gate, beside `view.proposal = null`, the count is zeroed (`decisionsN: 0`) under the extended "not his" comment. Consequence traced in today-app.cjs statusLine (:382-383): decisionsN 0 skips the open-proposal branch, exerciseCount is null from the same gate, so the line is STATUS_NOTHING_SCHEDULED, the recorded F4 departure. Nothing else reads decisionsN (grep of both files: app :382-383, model :112 comment, :472 comment, :475). Cell 4's new row evaluates the gate block alone in a vm over a view with decisionsN 1 and the fixture card, then hands the gated view and liveProposalFace's state to statusLine. Red on round-2 bytes: not ok 4, expected the F4 line, got "" (my own red tree, below). The block extraction is safe: `if (pendingAdoption) {` occurs once in the model (block() asserts uniqueness).

L2-2 OK (today-app.cjs :514-525): liveProposalFace, open face only, strips the trailing PROPOSAL_UNTIL_YES and trims; proposalFace is byte-identical to round 2, so the review faces (recorded, declined, registry) keep the sentence, and cell 10 asserts both sides (live reason = engine reason alone; face() reason still ends with the sentence; a dropped engine reason leaves the live card no reason at all). Cell 11 (R1) now wants the fixture's own why with no sentence. Red on round-2 bytes: not ok 10 and not ok 11, both with the sentence appended in the actual. Note, not a finding: the strip is by suffix on the composed reason, so an engine reason that itself ended with the exact pack sentence would lose those words; writers.cjs has no line containing "until you say yes", so no engine reason can today.

STOP1 cell OK (cell 13): static, withBoardProposal( occurs exactly twice (definition and the one call inside createBasisState) and createBasisState( exactly twice (definition and the basisState-absent default at :272); then adoptBasis evaluated alone (block :516-523, unique) over a basis that carries the board card: the athlete's own state comes back byte-equal, basis is his, pendingAdoption false, openProposalCard null, no volpush_chest_ rid, and his own one proposal is kept exactly; today-lanes.cjs hands adoptBasis setup's or the admitted source's state and neither today-lanes.cjs nor local-source-basis.mjs names createBasisState or withBoardProposal. adoptBasis itself is untouched by C-UI-3 (not in the diff). It is a confirming cell (green on round 2), so its red is the mutation: `basis = withBoardProposal(clone(state), day)` in adoptBasis -> not ok 13 (withBoardProposal calls expected 2, actual 3), the other 12 green. Reproduced in my own tree.

Sealed cells: unchanged from l2 (the consumer list is declared, not run; C-UI-5 STOP for machine-settings-ui.test.mjs stands). L2-1 and L2-2 add no file to that list.

## Runs (pm-run shared, jobs fable-cui3-l3-green, -red, -mut; MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York; cui3-guard.cjs + cui3-guard-esm.mjs preloaded; each slot held 0 s wait; logs in %TEMP%\cui3-l3\)
- GREEN on the worktree (green.log): look-cui3 13/13, look-cui2 5/5, design 15/15, scene 6/7: 39 of 40. The one red is scene 40 "C-UI-1 build reaches the scene through the actual offline preview bundle", ERR_MODULE_NOT_FOUND esbuild (no node_modules junction here): the same env red as l1, l2 and the builder, named not fixed. Guard: engine modules loaded (0) in every process.
- RED (red.log): my own scratch tree %TEMP%\cui3-l3\red from cui3-red.tar with the reconstructed round-2 bytes (hashes printed in-run: today-app.cjs 659bbf4f, today-model.cjs 72b5b357) and the round-3 test b2a1c21c: 10 of 13, not ok 4 (L2-1), 10 and 11 (L2-2). Real red for every changed cell, matching the builder's red.log.
- MUTATION (mut.log): round-3 bytes with adoptBasis mutated as above: 12 of 13, not ok 13 only. Second mutation of my own (mut2.log): the L2-2 strip disabled (`if (false && ...)`) on round-3 bytes: 11 of 13, not ok 10 and 11 only. So each round-3 cell bites on exactly its own code.
- NOT RUN: copy, view, adapter, setup, package, any engine-index test, anything loading the engine or the protected five.

## Bytes written
- This review: rebuild/lanes/fable/reviews/REVIEW-LOOK-C-UI-3-l3.md (new, uncommitted, LF, ASCII). Nothing else written in the worktree. Scratch: %TEMP%\cui3-l3\ (scripts, logs, the red tree, the two reconstructed round-2 files). Nothing committed or pushed.

## AAR
asked: independent l3 review of C-UI-3 round 3 (L2-1, L2-2, the STOP1 adoptBasis cell, byte identity versus round 2, red-first, no forbidden byte), suites re-run under the guard through pm-run shared.
happened: round-2 bytes were gone from the PC, so identity was proved by reversing the described edits and matching the round-2 hashes exactly; every hunk read; red 10/13 on those bytes, green 39/40 and the adoptBasis mutation reproduced in my own tree, plus a second mutation of the L2-2 strip.
rework: none.
escaped: nothing found; one non-finding noted (suffix strip versus an engine reason ending in the pack sentence, impossible on today's writers.cjs).
lesson: when a round overwrites the previous round's uncommitted bytes, a hash-matched reconstruction from the described edits is a full identity proof; a builder should still leave the prior round's bytes in scratch so a reviewer need not reconstruct.
cost: about 35 minutes, 24 PC calls, three shared slots for about 5 s each.
