# REVIEW-NATIVE-LOAD-BUILD-R19-l1

Reviewer: Claude Fable 5.1 (claude-fable-5-1), independent of the builder (Claude Opus 5.5) and of the PM. 2026-09-24.
Subject: NATIVE-LOAD build round 19, UNCOMMITTED in worktree earned-nlr (branch rebuild/e-native-load-red, HEAD dfc4445 = origin rebuild/e-native-load-build round 18).
Judged against: spec R9.10, rebuild/coach/NATIVE-LOAD-SPEC.md at d3a3ffa (extracted by cmd /c git show, sha256 6bd14f813aec52e5aa90517502a5557df614b2e196469aacc48a6857b1920605, matches), section L; grants DECISIONS:803 (e) and :804 (f) with :804's payments D-R18-SETS-RESTORE and D-R9.9-LATER-HOLD-UNDO; the PM rulings at DECISIONS:807 (read from origin/rebuild/t2-client-core, 807 lines). Every ruling below was tested against the spec and the code, not taken as given.
Rules: %TEMP%\opus55-RULES.txt read whole. Every run took %TEMP%\earned-runtime.lock exclusively ('fable r19 review'), removed only that lock, preloaded the builder's guard.cjs; every log cited prints "GUARD protected-in-cache: none; refused: none". No protected-five file was opened or loaded; no src/ or private path read. TZ America/New_York, MEASURED_TEST_NOW 2026-09-03. Scratch: %TEMP%\fable-r19 (out\ holds every log cited; hashes are sha256 prefixes of the log files, computed with node).

VERDICT: ACCEPT WITH NAMED DEBTS (D-R19L1-1..D-R19L1-4)

No blocking item. Every engine byte is inside section L and the grants; red-first is genuine and reproduced by me on the dfc4445 product compiled in memory; the seven PM rulings at :807 hold against the spec and the code (I disagree with none; two of them correct spec sentences that are wrong as measured, see F5 and F6).

## 1. Hashes and hygiene (recomputed with node, byte-exact)
- rebuild/m4/workout/engine-capture.cjs fa68a748680b5b646c52c769f9a2ff2d30b0846417887a9e0a5374e55f43aee4 (11278 bytes; base 94685ca4... 10797)
- rebuild/engine/progression.cjs 84ca3e53f1c7a1023d0807f7bc3d6941a4ec0466a9acc27c7582e391f0498dbc (53673; base 7031838d... 53582)
- rebuild/engine/native-load.cjs (FC01) d76657291f42c820345066dfddb6a5f568948bb736328d4825cf66c869fbf017 (51039; base e61ab73f... 49029)
- rebuild/m4/workout/native-load-effects.cjs (FC03) eb3a761749c50039c4434f6dc17d67a8b37355dbff88014e537085ea6752324d (85756; base 75494ab5... 82968)
- rebuild/m4/spec/native-load-options.test.cjs (FC12) 2ff69b3c379ae9a1fb63fa2f00521f9eb5a83438ba15cd5c5f11f894312efdc7 (398231; base 4036d18a... 349351)
- rebuild/m3/w7-preview/today/test/native-load-panel.test.mjs (FA03) be1b0b46fb584bdba60806471ce3bf581a0496d7ca5f8076b572dfbebf563fd2 (88931; base cf47ea1a...)
- rebuild/m4/workout/test/engine-capture.test.cjs df145a20a1f3d108f2ffa0cf055fb49344d67330bcc30e8134336ba2371ce974 (21033; base 6d19eebb...)
- rebuild/m4/workout/test/configuration-capture.test.cjs a50db77216165eeea239b9ede996039e8f5024405e4c4654b3c642cbf04a0dc5 (11128; base 839076da...)
- rebuild/lanes/e/NATIVE-LOAD-BUILD-REPORT.md 4c6081269185c3ac1b54b09fc42ca1d8168f84eb369923e6c36e3c34dee8551d (150167; not in the builder's index, which says "this report")
All eight builder shas match the REVIEW INDEX. All nine files: CRLF 0, U+2013/U+2014 0 added (progression.cjs carries 80 pre-existing dashes and 101 non-ASCII bytes at dfc4445 and the same 80/101 now; configuration-capture.test.cjs 5 non-ASCII at both). The base blobs I extracted hash exactly as the builder's nlr19\base copies (94685ca4, 7031838d, e61ab73f, 75494ab5, 4036d18a, cf47ea1a, 6d19eebb, 839076da). git status lists exactly the eight files plus the untracked Astra review (not read). PRODUCER_REVISION recomputed by nlr-build\revision.cjs over the fourteen engine files on disk: earned/native-load/v1+sha256:b9763f168d002f57aae7119663058e63f1546ed486f154985f2c91fbbb9ee19e, equal to FC03:1068; FC12 R2-REVISION green on the head bytes.

## 2. Every engine byte against section L (git diff dfc4445 -- explicit paths; diff-product.txt in scratch)
- W/engine-capture.cjs (+3 lines at :20-22, :82-83 changed; 7 lines in the stat): fit(v,n) beside number, validates the WHOLE stored v through Array.from (a sparse slot fails) with the :82 predicate, fails ENGINE_CAPTURE_LOAD_MAPPING_REQUIRED, returns v.slice(0,n) or [...v, ...Array(n-v.length).fill(last)], a fresh array; :82 keeps q.newW!==card.w, :83 keeps card.w!==original.w; :84 scalar and :85 final guard unchanged; :64-77, :86-130 unchanged. Exactly L FIT and CLAUSES THAT CHANGE. D-EPP-2 :69-70 and FC16 :91 untouched.
- E/progression.cjs: exactly one line, :81 (_prescribedLoads body), k = min(i, len-1) when wSets is a non-empty array, else -1; wSets[k] when non-null else ex.w. Exactly L READER. Grant (f). No other E/ byte.
- FC01: planVector :110 (READER mirror); entryTarget :176-177 fit(newWSets,count); private fit :183-186 (null on malformed); step 2 split :244-247 into (a) count or fork -> PLAN_CHANGED planRefs(), (b) Array.isArray(ex.wSets) && ex.wSets.length !== originals.length -> SET_COUNT_BASIS_UNPROVEN [closeRef] field null, (c) typed comparison -> PLAN_CHANGED planRefs(); planRefs() reproduces :233's refs exactly (forkRefs only when forkLater, which (a) already consumed, so (c) never adds them: identical to dfc4445's single condition); compensation applied branch :401-402 n0 = originalSlots(rows[lastConsumed].entry).length (lastConsumed is guaranteed = rows.length-1 by :385, so rows[lastConsumed] exists), target projected over sets n0; landing exact :629 gains (!Array.isArray(q.newWSets) || originals.length === q.newWSets.length). TARGET_QUEUED :212-217 still precedes the guard; compensation still dispatched at :210 before step 2. Header comment line 2 re-cites R9.10 (comment only; every prior round did the same; not a semantic byte).
- FC03: selectedEntry :134-137, :136 same(cap, fitVector(newWSets, cap.length)) (fitVector null -> same(cap,null) false, so a malformed vector is never the card, as today); entryTargetOf :139 (null only on malformed; its two callers are guarded: the anchor pick :464 requires selectedEntry first, the host comparison :1025 feeds same(), no throw); fitVector :143-146; projectBase :151-156 READER; laterHold :737-742 and refusedAccept :747 (HELD_BACK.has || laterHold); host-slot FIT GUARD :1017 first, same code, refs [{op_id, commitment}] as the PLAN_CHANGED at :1041, field null; planNow :1021 READER; PRODUCER_REVISION :1068 re-bound. HELD_BACK :20, BLOCKING :16, HOLD_CODES :237, AFTER_STEP_2 :224 and exit (b) :947-978 unchanged. Header comment line 2 re-cites R9.10.
- No FC08, FA02, FC16, D-EPP-2 or package byte (diff --stat over the nine paths: 654 insertions, 57 deletions, no other file in git status).
Reading note on laterHold: L says "the pending native entry of the lift that causes it belongs to an earn whose spend is in this fold's repair map"; the build requires EVERY pending native entry of the lift to be such an earn (pending.every). Stricter than the sentence; equivalent over reachable inputs (a second pending earn entry needs a later consumed Close, which breaks the claim's latest-Close check at :460 before refusedAccept). Named D-R19L1-3.

## 3. Red-first, reproduced by me (dfc4445 product compiled in memory with nlr-build\r18\swap.cjs over the head test bytes; nlr19\swap-base.json)
- FC12 head: 192 tests, 192 pass (out\fc12-head.txt ccbeab14). FC12 over dfc4445: 192 tests, 22 fail = R2-REVISION plus exactly the 21 named reds: N29-LATER-HOLD R9.10, FIT-ORDINARY-UP, FIT-ORDINARY-DOWN, FIT-DEBUT-UP/DOWN, FIT-REPLAY/WINDOW, FIT-STORED, FIT-PARITY, FIT-GUARD, FIT-UNDO, FIT-DEVICE, N29-VECTOR-LAYOUT-R9.10, READER-UP, READER-IDENTITY, READER-HISTORY, READER-PARITY, READER-UNDO-TEXT, RESTORE-COUNT, RESTORE-COUNT-BASELINE, N29-LATER-HOLD-UNDO, N29-LATER-HOLD-EXIT-A, N29-LATER-HOLD-C3 (out\fc12-base.txt 3e50c0d9). Every green-kept row (FIT-IDENTITY, FIT-MALFORMED, FIT-HELD, READER-DOWN, READER-NULL-LAST, RESTORE-FORGED, RESTORE-NO-EDIT, N29-LATER-HOLD-REVERT, LATER-HOLD-SCOPE) is green on both products, as marked.
- FA03 head 44/44 (out\fa03-head.txt 81dc0fba); FIT-PANEL R9.10 [Y] red over the dfc4445 product (out\fa03-base.txt 7c38e1a6).
- configuration-capture.test.cjs (PERFORMED_W6_DIR = this worktree; its imports load none of the protected five, guard line clean): head 10/11 with only the pre-existing 'v1 stays numeric-only' red (out\cct-head.txt 5d750908); over dfc4445 the carried row '... R9.10' red in addition (out\cct-base.txt 63086a6a).
- engine-capture.test.cjs cannot load locally before or after: "Retained source unchanged rebuild/engine/constants.cjs" from its Source.baseline() pin (out\ect-real-head.txt c1ceda07); CI only, as the builder says. The scratch harness nlr19\ect-scratch.test.cjs (row bodies verbatim, same adapter): head 4/5 with only the ORIGINAL dfc4445 [40] assertion red, "Missing expected exception" (out\ect-head.txt 0242a1fe); over dfc4445 4/5 with only the '... R9.10' row red, ENGINE_CAPTURE_LOAD_MAPPING_REQUIRED (out\ect-base.txt 8e806c05). The original red is kept, as :804 requires.
- READER rows against the fit-only intermediate (head product with the dfc4445 progression.cjs compiled in memory): READER-UP, -IDENTITY, -HISTORY, -PARITY red, 3 pass (out\readerup-fitonly.txt f3e5ddc5): the READER hunk is load-bearing, not decorative.
- The dfc4445 FC12 over the R9.10 product: 164 tests, 2 fail, exactly N29-LATER-HOLD and N29-VECTOR-LAYOUT (out\oldfc12-on-head.txt fce08304): no other existing row moves; 164 - 1 + 29 new = 192, so exactly one test body (N29-VECTOR-LAYOUT) left the file, replaced by N29-VECTOR-LAYOUT-R9.10, which carries every retained sub-assertion (TARGET_QUEUED pending, the Undo offered, 2 -> 3 -> 2 gives the [105,100] card) and inverts only the refused-day expectation that grant (e) removes. Confirmed by :807; no other test deleted, none skipped.

## 4. Walks (my own runs; JSON reports byte-equal to the builder's where both exist)
- Seed 20267349: red over dfc4445, 'I2 proven cancellation lost at step 17', RECORD_INVALID target_load after adopt-observed 105 on fx-row, sets 3 -> 4, its Undo (out\w-base-20267349.txt 96b252d7); green on the head (out\w-head-20267349.txt d65e4f68).
- Seeds 20264386 and 20266433: red over dfc4445 (I2, RECORD_INVALID compensates; out\w-base-20264386.json 77e88617 and out\w-base-20266433.json 9adf11d8, byte-equal to the builder's) and green on the head (e0028ed4, 7d4bd1ad, byte-equal).
- Seed 20266884: green on both products (fdc9ff35 both, byte-equal to the builder's): it does not reproduce under the retained class, as disclosed; :807's replacement by 20267349 is right.
- R8 1500 walks from 20266001 on the head with ALL: 0 counterexamples (out\w-head-search.json 55541704, byte-equal to the builder's). R8 3000 from 20261001: 0 (out\r8-3000.json 07abc422; sets 1193, correct-consumed-repair 65). R7 3000 from 20260923: 0 (out\r7-3000.json 4c5b10d4). The 3 x 17,000 and the R7 17,000 were not re-run by me (time); the builder's JSONs exist in nlr19\out and the 1500/3000 prefixes reproduce byte-for-byte, so I take the counts as reported.

## 5. Planted defects (14 of 39 re-run by me, in-memory overlays, no product byte written)
Killed as claimed: M19-guard-after (FIT-GUARD), M19-lh-hold (N29-LATER-HOLD-UNDO), M19-reader-E-old (4 READER rows), M19-fit-validate-part (FIT-MALFORMED), M19-restore-current (RESTORE-COUNT, -BASELINE), M19-lh-every-tq (LATER-HOLD-SCOPE), M19-selected-exact-fc01 (5 rows), M19-fit-fill-cardw (5 rows), M19-guard-drop-fc01 (FIT-GUARD and N29-VECTOR-LAYOUT-R9.10: two rows now, not one, see D-R19L1-4), M19-captured-after-exact (FIT-UNDO), M19-lh-none (4 rows), M19-restore-current-prop and M19-lh-none-prop (R8 walk 3000 from 20266001). Logs out\mut-*.txt, every one "OVERLAY ... applied 1".
The three survivors, each argued from the code (not from the builder's words):
- M19-guard-drop-fc03 (192/192, out\mut-M19-guard-drop-fc03.txt ef238e47): EQUIVALENT. The FC03 copy sits inside `if (judged && ...)` (:1008), and judged requires an offer or an AFTER_STEP_2 refusal; SET_COUNT_BASIS_UNPROVEN is not in AFTER_STEP_2 (:224-225), so whenever FC01's copy fires (same fold.state ex, same original slots of the same entry) the host path is skipped, and whenever it does not fire the predicate is false. No input reaches the FC03 copy with a true predicate. L asks for both copies; both are built; the second is defence in depth.
- M19-reader-plannow-old (192/192, 54a15d58): EQUIVALENT. planNow :1021 is reached only when judged; PLAN_CHANGED is not in AFTER_STEP_2, so FC01's (a) has established originals.length === max(1, ex.sets||1) and (b) has established ex.wSets.length === originals.length (or wSets is not an array): planNow never has a position beyond a non-empty stored vector.
- M19-lh-no-repair (192/192, 306a4fdd): EQUIVALENT over reachable inputs, by the argument in section 2 plus: a pending entry of an earn held under an unprovable order makes the claimed adopt (same recorded base as the earn) meet movedBase at :717 and dispute EFFECT_CONFLICT before refusedAccept; a held-back earn (LEGACY_PENDING or VECTOR_ADOPTION_UNDEFINED) never created its entry. I could not construct a genuine TARGET_QUEUED refusal of a claimed adopt-observed whose pending entries are not all repair-mapped earns. Unpinned by a row (D-R19L1-3).

## 6. The :807 rulings, each tested (F5 and F6 are my own evaluated calls: a probe test appended to a copy of FC12 and compiled in its place, nlr19-style swap; out\fprobe-head.txt ae4f8b1f, out\fprobe-base.txt 0267d8f0; the builder's own probe source fc12-probe.cjs was overwritten by a later walk-trace probe, so its probe1.txt could not be re-derived from its source)
- F5 STOP-R19-1 (L N29-LATER-HOLD-UNDO says with the recorded Undo the fold "issues exactly BASIS_REPAIR_REQUIRED and TARGET_QUEUED"): measured on the head, R1 and R2, typed and host: without the Undo [BRR fx-resp-1, TARGET_QUEUED fx-resp-3], w 100; with it [BRR fx-resp-1] only, w 100, authority compensated. Over dfc4445 the Undo adds RECORD_INVALID [fx-resp-4] (auth undefined): the debt the round pays. The PM's ruling is right: FC03:837 has resolved a held-back issue when its spend's cancellation applies since round 8 (issues.splice), L's own mechanism (held.set, spend kept) puts the 95 yes on that path, and a cancelled yes still reported as TARGET_QUEUED would be a stale issue. L's sentence is a derivation error in the spec, not a build defect; the row pins both measured lists.
- F6 STOP-R19-2 (L N29-LATER-HOLD-C3 says host v1 refuses SOURCE_OVERLAP and typed v2 PLAN_CHANGED "as at dfc4445"): measured on the head: C3 refused NATIVE_LOAD_BASIS_REPAIR_REQUIRED [fx-resp-1], field null, offers [], on v1 and v2, R1 and R2; over dfc4445: v2 the same BRR (so L's "as at dfc4445" was already wrong for typed slots), v1 offers [adopt-baseline] (the defect). Cause read in the code: exit (b) :970-978 evaluates C3 on the held projection, passes through only adoption offers, LEGACY_PENDING, VECTOR_ADOPTION_UNDEFINED and SCALAR_SLICE_ONLY (:971-977) and answers every other FC01 refusal, SOURCE_OVERLAP and PLAN_CHANGED included, with holdRefusal(). Changing that pass-through is an FC03 exit (b) byte that L does not name ("Nothing else in FC01/FC03 changes"). The PM's ruling that the measured refusal stands is right: the property D-R9.9-LATER-HOLD-UNDO needs (C3 spent by the kept yes, nothing offered on C3, C4 offered adopt-baseline) holds, and the athlete sees the lift's actual hold. Spec debt D-R19-C3-CODE; the row pins no code yet (D-R19L1-1).
- F7 the two in-place row updates: confirmed under :803 (e)/:804 (f) at :807; section 3 measures that exactly those two old rows fail on the new product and that N29-VECTOR-LAYOUT-R9.10 keeps every sub-assertion the grant does not invert. Right.
- F8 seed 20267349 for 20266884: section 4. Right; both retained.
- F9 E/progression.cjs as an edited carrier (FG02): it is an S8 key (the builder cites acceptance-s8-real-shape.json:271-275 and S9.json:113-117; pre = post 7031838d at dfc4445); its hash is now 84ca3e53 for one line under grant (f). The NATIVE-LOAD child must declare it edited with this round's red-first proof (READER rows, readerup-fitonly). Right; package work, no byte here.

## 7. Other findings (non-blocking)
- F1 FA02 generic path (L (3) asked the builder to STOP if none): verified at today-entry.mjs:202-234 and :265-292: a check refusal is collected as {lift, code} and never rendered by code; with no offer the panel shows NATIVE_LOAD_COPY.none. So SET_COUNT_BASIS_UNPROVEN needs no approved string today; its display stays PROPOSED as L says. Correct.
- F2 FIT-IDENTITY and RESTORE-NO-EDIT pins (cfbab354..., 00ab2971...) were measured on the dfc4445 product and pass on the R9.10 product: the byte-identity claims hold for the enumerated cases (scalar, equal-length vector, debut vector, configured, baseline ask, held; four compensation bodies). Disclosed by the builder; fine.
- F3 The spec's "capture-level carrier beside W/test/engine-capture.test.cjs" is not a file beside it; the FIT rows exercise engine-capture.cjs through createEngineWorkoutCapture in FC12 (CI-run through FC13). Substantively a capture-level test; placement deviates from L's words (D-R19L1-2).
- F4 Comment-only bytes at FC01:2 and FC03:2 (the spec citation) are outside L's enumerated clauses but semantically empty and bound by the revision; every prior round changed the same line. Not blocking; the PM may want L-style sections to say so next time.
- F10 The walk class widening (`if(!B.wSets&&!held(f,L))B.sets=pick([2,3,4])`, FC12:3482) matches L RESTORE COUNT WALK and subsumes the round-18 class; the I2 'later-hold-undo' exemption is removed (a strengthening); R8 3000 walks show sets 1193 and correct-consumed-repair 65, so both new clauses are exercised.
- F11 Spec branch: origin/rebuild/c-native-load-spec is now d91e4dfd (R9.11, accepted at :807); round 19 is built to d3a3ffa as briefed, and :807 carries R9.11's two rules to round 20. Nothing in round 19 contradicts R9.11's text that I read (:156 (c2) already states the READER rule).

## 8. Named debts
- D-R19L1-1 (row pin, round 20): N29-LATER-HOLD-C3 asserts only status 'refused' and offers [] on C3; a mutant that renames the C3 refusal survives it. Pin NATIVE_LOAD_BASIS_REPAIR_REQUIRED [the Q105 yes Ref], field null, on both slot kinds, as :807 orders; spec text D-R19-C3-CODE to say so (and to drop "PLAN_CHANGED as at dfc4445", which dfc4445 never did).
- D-R19L1-2 (placement, PM decision): no capture-level carrier file beside W/test/engine-capture.test.cjs; the FIT rows live in FC12. Accept the placement in the ledger or order the file in round 20; either way the W/test files still run nowhere in rebuild.yml and engine-capture.test.cjs cannot load locally (Source.baseline pin), which is a pre-existing gap outside this round.
- D-R19L1-3 (spec wording / row): laterHold's "every pending native entry is a repair-mapped earn" reading of L LATER HOLD UNDO; equivalent by argument (sections 2 and 5), unpinned. Spec R9.12 should state the "every" form; a row with two pending entries (one repair-mapped) would pin it if the PM wants the mutant killed rather than argued.
- D-R19L1-4 (report accuracy): Step 3 says M19-guard-drop-fc01 "is killed only by N29-VECTOR-LAYOUT-R9.10"; after the FIT-GUARD strengthening it is killed by FIT-GUARD too (measured, out\mut-M19-guard-drop-fc01.txt bd1dbce5, 2 rows); the builder's out-M19-guard-drop-fc01.txt is from the first run (not in the rerun list). Also the round-18 221-mutant inventory was not re-run (disclosed by the builder; its clauses are covered by the unchanged rows, all green). Fix the sentence in the round-20 report.

## 9. What I could not do
- Not re-run: the 3 x 17,000 R8 walks and the 17,000 R7 walk (10 min each); the 80-file local set. I re-ran the seed-level reds/greens, 1500 + 3000 R8 walks and 3000 R7 walks, all byte-equal to the builder's JSONs where both exist.
- Not run: rebuild/m4/workout/test/engine-capture.test.cjs itself (fails to load locally, CI only); native-next-targets.test.cjs / b-ntc (the builder reports the same pre-existing failures before and after in the 80-file set; not re-verified).
- Not constructed: a counterexample to any of the three survivors; each is argued equivalent from the code paths cited.
- The builder's probe1.txt source (fc12-probe.cjs) no longer holds the LATER HOLD probe; I re-derived its numbers with my own probe (section 6) rather than trusting the log.
