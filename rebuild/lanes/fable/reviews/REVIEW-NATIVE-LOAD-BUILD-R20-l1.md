# REVIEW-NATIVE-LOAD-BUILD-R20-l1 (Fable, independent build reviewer, round 20 parts A + B1 + B2, 2026-09-25)

VERDICT: ACCEPT WITH NAMED DEBTS D-R20L1-1..6 (none blocking). The product change is exactly spec R9.11 M (1) and (3) plus R9.12 revision 3 (1b), with the one anchor and no witness; every section-M and :533-579 row exists, each red-first row's measured red is retained and reproduces, the mutant table and the walk results are true as stated, nothing is weakened, Astra L12 B1-B5 are each paid, nothing is raised without a yes, and only grant (a) bytes moved (FC01's clause (3) is R9.11 M (3), within the same grant). Three STOPs are correctly stopped, not fitted; STOP-R20B2-3 (I6) is the one that matters most and is carried as D-R20L1-6.

Object: C:\Users\joeym\AppData\Local\Temp\earned-nlr, HEAD 189d0767, uncommitted. git diff 189d076 -- FC01, FC03, FC12, FA03, report: 5 files, 900+/23-. Hashes measured here (Get-FileHash, LF, CR 0 in all five, 0 non-ASCII added bytes in every diff): FC01 92a4a0b4..., FC03 373c9b1c..., FC12 243f86b3..., FA03 68c468f6..., report c5d9703b..., all as the builder states. git status names nothing else under rebuild/ but the untracked Astra L11 copy. Spec read whole at earned-astra-96 (cef5ade5..., 579 lines; section M :484-579) with Fable R12-l3 (a04bf36d...) and Astra L12 (e48e1b8e..., :61-76). Report sections "Round 20 part A", "part B1", "part B2" read whole (:753-884).

RUNS (mine; node %TEMP%\pm-run.cjs shared, job fable-r20l1-v1, slot 1; guard preload "protected-in-cache: none; refused: none"; TZ America/New_York, MEASURED_TEST_NOW 2026-09-03; scratch %TEMP%\nlr20-fable; no protected file, no 80-file set): the 20-row set (^N28-, ^N27-B39-, ^R17-ANCHOR-STRUCTURAL, ^R2-REVISION) on the final bytes 20/20; the same over the part-B1 FC03 swapped in memory 11/20 with exactly the builder's nine red (R17 :2657, N28-B5-ASTRA, -NO-UNDO, N28-NEVER-HELD-NULL, N27-B39-RETURN-UNDO, -UNDO-AFTER-RETURN, -RETURN, -REPAIR-REVERT, -INVARIANT); mutant (ii) M (1)-half 19/19 LIVE (overlay applied 1); mutant (vii) 15/19 killed by :2657, ASTRA, NO-UNDO, INVARIANT; R9.11 "dissolved exit disabled" 9/19. Each equals the builder's log (z-rows-b1 e2317b27, z-rows-189 e9b48a75, m-b2-vii 9e75361f, m-b2-iv 5f074c14, z-fc12 742f8e43 210/210, z-fa03 3ec5a8a6 46/46, z-fa-b1 fb89e46d 1/2, z-fa-189 d209c0e9 1/2, fa-xii 78ba2db8 1/2, full-head ff613acb and full-base d5c08627 both 1126/71; the cited walk hashes are the JSONs: 089e89d5, 87155418, da943e6e, 78207190, cac49345, 13b88a19). Walk JSONs re-counted here: the six R8 shards hold 745 counterexamples = 491 I15 BASELINE_UNPROVEN + 225 I15 LOAD_MAPPING_REQUIRED + 29 I6 (250/234/261 per bound), the range 21; every BASELINE_UNPROVEN (508 with the range) has a legacy non-PROPOSED debut over w null and NO native entry; every LOAD_MAPPING_REQUIRED (228) is a legacy scalar debut without newWSets over an array wSets; R7 2 x 8500 = 0. No I1, I2, I3, I14, I16, I17 or CARD-PARITY in any shard.

CHECKS
(1) Product = M (1)/(1b)/(3) + rebind, nothing else. FC03 diff: dissolvedExit (:563-585) is OR-ed into exitB (:692-694); it requires adopt-baseline, holdIssues empty, non-empty refs, facts present, ONE ANCHOR = the consumed root of greatest facts.order rank (rank ?? -1, stable sort, pop) that is an authenticated session-start, and per ref: op present, commitment equal, class plan / kind proposal-response, issuance body of this lineage, provenBefore the anchor, and no isHold issue of the lift naming it whether or not superseded_by is set (the fold's full issues list). Null and numeric captures alike. (1b) at :444-451 adds dissolved(id) and ok = ... && (holding || (dissolved && provenBefore([o], byId.get(latest.start), byId))), byte-for-byte the :538 clause; authentic, kind, shape and lineage tests unchanged. Everything reading exitB (V = projectHeld, behind, the moved-base gate, the exit branch) is untouched. FC01 :561-563 is clause (3) exactly (prior image first). PRODUCER_REVISION 1d7dbe40 from B1. No W, no STOP-R912-2 input, no engine-capture, today, progression, today-bindings or copy byte. MET (see D-R20L1-1 on the rebind).
(2) R17 :2657 re-pin as ruled: the want.push line is a ternary whose else-branch is the original expression, so every other tuple of the :2655 loop is built identically (D-R12L3-4 paid); the comment keeps the 189d076/B1 outputs (R2 RECORD_INVALID base_load w 100, R1 EFFECT_CONFLICT load_basis w 100), names the ruling and the entry state (Q105 done/MISSED; the Undo restores w null; D-R12L3-1 paid); an added R1 fold asserts [[],95] equal to R2. Measured red on B1 (rows-b1: actual RECORD_INVALID:base_load / 100 against [] / 95). MET.
(3) Rows: 16 new FC12 rows (ASTRA both orders + duplicate y2, NO-UNDO, R1R2-PARITY, FORGED-REF, WRONG-LIFT, CLOSE-REF with (c), POST-ANCHOR (a)(b), SUPERSEDED-HOLD, NEVER-HELD-NULL with -ORDINARY, RETURN-UNDO, UNDO-AFTER-RETURN, RETURN, REPAIR-REVERT, ORDINARY, CLAIM-ORDER, INVARIANT) plus B1's CONTROL and NULL-VECTOR, plus FA03 N27-B39-HOST and N28-HOST-NO-NEVER-HELD (a)-(e); each with typed v2 / host v1 (C1) and R1 / R2, and each red-first row's red retained in a log I re-derived. Expected tuples equal the ACCEPTED END STATE and every R912W m1b cell (105 / null, y2 cancelled, no issue, baseline ask; 65 no issue; 95 no issue; 60 / null SUPERSEDED). MET (precision debts D-R20L1-2..4).
(4) Mutants: (i) 18/19 POST-ANCHOR (a) and CLAIM-ORDER (a); (ii) 18/19 SUPERSEDED-HOLD, its M (1)-only half LIVE (STOP-R20B2-2, correct); (iii) 18/19; (iv) 9/19; (v) 18/19; (vi) LIVE against the Close variants (a Close fails the shape test first), killed by the added CLOSE-REF (c); (vii) 15/19; (xi) 17/19 both halves; (xii) FA03 (e) and walk I17 seed 20261007; R17-anchor-holding as built 17/19; the five R9.11 mutants 9/10/14/17/14. Overlay anchors verified (b2-ii-superseded-M1, b2-vii, R17-anchor-holding-asbuilt, b2-iv, b2-vi, r911-dissolved-disabled read). TRUE.
(5) Walk: realCard through W/engine-capture prepare at each Start's own day (dayAt(n-1)), fold just before the Start; cardOf EXTENDED to pickStructural + first unfinished debut/unlock + engine-capture :92-95 loads (STOP-R20B1-3 (i) and (ii) modelled, not suppressed; CARD-PARITY 0); I16 after every step; I17 after every check; classes (b), (b'), (c) from a separate generator; the rename draw restricted to non-blank names ((B2) as ruled). Seeds 12035226 (189/B1 red at I2, final green) and 11301954 (B1 red at I16, final green) verified from the logs. TRUE.
(6) Nothing weakened: every part-A change adds cases or assertions; no row deleted; :2657 is the one ruled re-pin; FIT-MALFORMED, READER-*, RESTORE-COUNT, N29-LATER-HOLD-* only widen; R8-PROPERTY default run green with the new checks. Astra L12: B1 (cropped hole, L12-M03 red/green), B2 (explicit zero, L12-M07), B3 (added set, L12-M08), B4 (keptRoot, L12-M12), B5 (N28-B5 rows, red on 189d076 in both orders) each paid. No silent increase: every applied weight (105, 65, 95, 60) is the athlete's actual load under a recorded yes; the residual raises nothing (base_load null). Grant (a) only. MET.

FINDINGS (none blocking)
F1 (FC03 :1098; spec :500, :538). PRODUCER_REVISION hashes rebuild/engine files only, so the (1b)/M (1) fold semantics share revision 1d7dbe40 with the part-B1 FC03. The builder disclosed it; the spec's "the rebind covers the (1b) bytes" is a paper error. Harmless while the build is unreleased (DECISIONS:790), but a rule question for the PM: whether FC03 fold semantics belong in the revision digest.
F2 (FC12 b5Fixture; spec :541 "typed v2 and host v1 slots"). C2 (and C3) are host v1 in every N28-B5 variant; only C1 varies. The builder read the input's "C2 (host v1)" literally and STOPped the other reading as STOP-R20B2-1: a typed v2 completion that captured a numeric card is refused the exit (EFFECT_CONFLICT 1754, PLAN_CHANGED 44 of 51000 walks) where host v1 gets it. That is a runtime-parity gap (spec B) exposed by class (b'), pre-existing on B1, and the largest open item after I6.
F3 (FC12 N27-B39-RETURN-UNDO). Its measured red on 189d076/B1 is the state assertion (Q105 [false,'DEBUT'], w null), which precedes the row's capture assertion; the spec's named red (BASELINE_UNPROVEN through cardLoads) is retained by FA03 N27-B39-HOST (gym.read blocked, z-fa-b1/z-fa-189) and by B1's seed 11301954 (I15), not by this row's own failure.
F4 (FC12 N27-B39-INVARIANT). It folds (m)-(p), NULL-VECTOR, the B5 variants and NEVER-HELD-NULL, not ORDINARY, CLAIM-ORDER or REPAIR-REVERT (each of which calls b2Invariant itself, so the spec's "every row above" is met in substance, not in the aggregate row).
F5 (walk). R9.11's tenth mutant (the Start taking cardOf's card instead of the capture) is not reported for B2. With realCard the only card path it is a walk-model mutant killed by the I15 counts vanishing (491/225 would read 0), which the builder did not show.
F6 (FA03 N28-HOST-NO-NEVER-HELD (c)). After the return the card is 45 so the later completion yields an adopt-observed; the spec's (c) ("no adopt-baseline offer carries non-empty refs") holds vacuously there. The rule function still checks the adopt-baseline shape wherever one is issued ((a), (b), (d), the walk's I17), so the pin is not empty.
F7 (STOP-R20B2-3, I6; i6d-20262146.out.json). Confirmed: under R1 fx-p-3 (an exit naming [fx-p-1 earn, fx-p-2], both on device B with no causal parents, fx-p-1 named only by DEBUT_BASIS_UNPROVEN) is re-evaluated on the reproduced cut and applied as exitB; under R2 it is corresponded and refused RECORD_INVALID base_load. End w is 60 in both, but the issue lists differ, so I6 is real, pre-existing (identical on B1) and outside R9.12; a correct STOP. It shows the R1 reproducible-cut path admits an exit that correspondence refuses, which is the F5 seam the spec left to "the re-evaluation decides".

ADVISOR REPORT ON STOP-R20B1-1/-2: I AGREE with both rulings and both recommendations. Verified statically: engine-capture.cjs:67 refuses a baselineAsk card with numeric w (the legacy newW put there by today.cjs:98); the B1-2 refusal is at :83 (a legacy entry has no newWSets, so :82 falls through and card.w !== original.w), the advisor's correction stands; FC01:542 dispatches compensate BEFORE the :544 LEGACY_PENDING test, and the unapplied-RESTORE branch :578-586 writes the recorded base_load.fields (w null for an adopt-baseline) when heldTrace is false, so D-R20-RESTORE-OVER-LEGACY is a real path (I did not trace whether FC03's capturedAfter lets that Undo through against the old-app sessions; the advisor's "needs the Undo recorded before the re-admission" is the right qualifier). FC12 FIT-MALFORMED already pins "a scalar debut on a vector lift" as LOAD_MAPPING_REQUIRED, so the walk's B1-2 fixture contradicts an existing characterization: restricting the generator there is stating an invariant, not exempting a trap. Two conditions on option (b): the restriction is stated in the walk as the invariant with the row R20-LEGACY-NEEDS-LOAD (FC01:549 and the exposed reader path only), and one R8 shard is re-run to show the 491/225 classes vanish with no new class. The projectHeld extension (hide an unfinished legacy debut/unlock on a lift whose projected w is null) is FC03-only and within grant (a) but changes the registered card, so it needs a spec revision (R9.4 :159 precedent), not a builder round. The admission-time shape check for B1-2 is outside grant (a): one yes/no to Joe first, as the advisor says. STOP-R20B1-3 (i): agree, cardOf must model it, and B2 did.

NAMED DEBTS
D-R20L1-1: PRODUCER_REVISION does not cover FC03 fold semantics (F1); spec author corrects :500/:538; PM rules whether FC03 enters the digest.
D-R20L1-2: STOP-R20B2-1 to the PM as a runtime-parity ruling (typed v2 exit on a numeric capture); when ruled, N28-B5-* gains the typed-v2 C2 cell (F2).
D-R20L1-3: N27-B39-RETURN-UNDO's comment names where its capture-boundary red lives (FA03 HOST, seed 11301954) (F3); N27-B39-INVARIANT folds ORDINARY, CLAIM-ORDER and REPAIR-REVERT too (F4).
D-R20L1-4: FA03 N28-HOST-NO-NEVER-HELD (c) gains a baseline-ask completion after the return (w null via an Undo, then a later completion) so its adopt-baseline clause is exercised, not vacuous (F6).
D-R20L1-5: the walk-model mutant "Start takes cardOf's card" run once and reported by its I15 counts (F5).
D-R20L1-6: STOP-R20B2-3 (I6, 29 + 1 seeds) carried to the PM with F7's reading; STOP-R20B2-2 (pin D-R9.11-TWO-EXIT's null-capture second exit as EFFECT_CONFLICT load_basis, measured 3455/3455) needs the PM's word, and I recommend pinning it as measured (M: "two exits of one hold keep dfc4445's rule").

Files: this file only (uncommitted, LF, ASCII). Scratch (not deliverables): %TEMP%\nlr20-fable (fc12.diff, fa03.diff, report.diff, run.ps1, v1.txt, v1.cmd, out\*). No commit, no push, no DECISIONS.md, no protected file, no 80-file run.
