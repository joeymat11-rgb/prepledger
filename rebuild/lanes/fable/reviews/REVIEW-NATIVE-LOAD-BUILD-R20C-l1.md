# Fable review: NATIVE-LOAD build round 20 part C (l1)

Reviewer: independent Fable (claude-fable-5-1), 2026-09-25. Object: the uncommitted diff over a42168d in C:\Users\joeym\AppData\Local\Temp\earned-nlr, files rebuild/m4/spec/native-load-options.test.cjs (FC12), rebuild/m3/w7-preview/today/test/native-load-panel.test.mjs (FA03), rebuild/lanes/e/NATIVE-LOAD-BUILD-REPORT.md ("Round 20 part C"). I did not write any of it. Static reads plus my own shared-slot runs (pm-run jobs fable-r20c-v1/-v2; scratch C:\Users\joeym\AppData\Local\Temp\fable20c, never in the worktree). No protected file read or loaded (GUARD: none in cache, none refused).

VERDICT: ACCEPT WITH NAMED DEBTS D-R20C-1..3 (none blocking). Every ruled item is implemented as ruled, the walk restriction is stated as an invariant with citations that I verified line by line and it is pinned by two green-kept rows whose planted defects I re-derived from the logs, no product byte moved, nothing is weakened, and the R8 shard claim is true on a re-count of the JSONs. STOP-R20C-1 is correctly stopped, not fitted; my reading is below.

## What I verified (measurements)
1. Scope. git status: exactly the three files modified; git diff a42168d --stat over rebuild/engine, rebuild/m4/workout, rebuild/m3/w7-preview/today/src, rebuild/coach: empty. Numstat FC12 +131/-3, FA03 +22/-2, report +32/-0. sha256 prefixes ff87313e, aac473ef, 17d43b5b match the report. CR 0 and non-ASCII 0 in all three (my byte count).
2. Independent runs on the current bytes (shared slots): FC12 214/214 (fv-fc12 ae54dc07; rows 211-214 are the four new ones), FA03 46/46 (fv-fa03 374b787c). Walk shard 20261001 x 1200 (fv-r8-20261001): 1 counterexample, I6 at 20262146, the prefix of the builder's shard (its first I15 is 20262341, index 1340).
3. R8 shard re-count (nlr20d\out, hashes as cited: fin-r8-* e1977781 63847eef ae76e8ba a26fa397; w1-r8-* byte-equal to fin-r8-*, so the new rows did not change the walk's output). Found 17/15/13/11 = 56 = I15 BASELINE_UNPROVEN 3 (20264592, 20271211, 20273214) + I15 LOAD_MAPPING_REQUIRED 42 + I6 11. Before (nlr20c\out z-r8-20261001-a e998e0d2, -b 3d0ad9ec; 2 x 8500, the same 17000 seeds): 137 + 113 = 250 = 158 + 81 + 11. The 11 I6 seed sets are identical before and after (20262146 ... 20275167). No other class in any JSON.
4. The 45 remaining I15, my own trace check (fable20c\i15.cjs, over day_lifts): every one has a legacy DEBUT draw on the refusing lift followed by a later 'vector' action on that lift (the 42 LOAD_MAPPING) or a 'base'/'plan' null on it (the 3 BASELINE), and no restore marker (undo-yes-restore / restore-moved-base) after the draw: 45 explained, 0 unexplained. The builder's classification and the "none is D-R20-RESTORE-OVER-LEGACY" claim hold.
5. Citations read at the lines named: E/writers.cjs:383 (earnWalk only over numeric w), E/progression.cjs:365 (null w -> null), E/earn.cjs:63 and :88 (newWSets shifted when wSets is an array and w numeric), FC01:501 (earn over non-numeric w -> bad base_load), :516-517 (candidate newWSets must equal the shifted vector), :542 compensate before :544 LEGACY_PENDING (kind-based, any state), :549 the only native DEBUT push, :578-586 the unapplied-RESTORE branch writing the recorded fields, E/today.cjs:55 (PROPOSED never picked), W/engine-capture.cjs:67 (BASELINE_UNPROVEN), W/plan-edit-commands.cjs:46 CHANGES without w/wSets. All as the comment and rows state.
6. Planted defects (nlr20d\mut, logs re-read): c-fc01-501 (earn dropped from the null-w test) -> NEEDS-LOAD red, field target_load (m-501 4bfb6aef); c-prog-365 (null guard removed) -> the :97 arm queues PROPOSED newW 5 at w null (m-365 45698379); c-earn-88-noshift -> VECTOR-SHIFT red, newWSets null (m-88 bea80a7a); c-fc01-517 (the candidate test removed) -> the scalar body applies (m-517 eabdc07c); b2-ii-superseded-M1 -> TWO-EXIT-NULL red, w 65 with fx-exit-b's authority (m-ii-M1 17d1b442); c-walk-cardof over the shard -> 5 found, I6 only (ad755d29): the I15 counts 12 -> 0 kill it. Mutant (xii) still kills FA03 N28-HOST-NO-NEVER-HELD (m-xii-fa 0d880779).
7. Rulings, item by item. (1) Generator: DEBUT never drawn on a w-null base; newWSets carried exactly as earn.cjs mints it (same Array.isArray(wSets) && typeof w === 'number' test); the state draw kept so seeds keep their action sequences (confirmed by w1 == fin). The comment states the invariants, not an exemption, and names D-R20-RESTORE-OVER-LEGACY as not excluded. (2) R20-RESTORE-OVER-LEGACY builds exactly the advisor's path (adopt-baseline 60 applied, its RESTORE Undo recorded while applied, the base re-admitted at 100 with a pending legacy DEBUT 105) and measures w null under the DEBUT and ENGINE_CAPTURE_BASELINE_UNPROVEN, with both controls; typed v2 and host v1, R1 and R2. (3) N27-B39-TWO-EXIT-NULL pins the second exit as EFFECT_CONFLICT load_basis [fx-exit-b] with the first applied (w 60, Q105 SUPERSEDED, spend kept), as the PM ruled and as the walk measured. (4) D-R20L1-3: the comment names FA03 N27-B39-HOST (d209c0e9, fb89e46d) and seed 11301954; b39MoreLogs rebuilds ORDINARY (A([yes],50)), CLAIM-ORDER (a)(b) (atW(100,[],[copy,exit2])) and REPAIR-REVERT (RR(cs2,[exit]), RR(cs,[exit]), RR(cs,[exit,undo])) exactly as rows 4689/4701/4666 build them; the INVARIANT title now matches its folds. D-R20L1-4: the 'return-undo' variant compensates y2 (the exit's yes) after the return, checks w null with no active hold, then D3 on the baseline ask at 50 gets an adopt-baseline whose read-back body carries []: the clause is exercised, not vacuous. D-R20L1-5: run and reported by I15 counts as asked.
8. Nothing weakened: the diff adds rows, a fold and a variant; the only narrowing is the generator's draw, which is the ruled option (b) and is justified by the two rows that pin the invariant it relies on. No expected value changed in any existing row.

## Findings (debts, none blocking)
- D-R20C-1 (precision, generator comment): the retained PROPOSED draw on a w-null lift is itself a shape no writer queues: writers.cjs:383 gates the whole earnWalk on a numeric w and R20-LEGACY-NEEDS-LOAD proves the :97 and :63 PROPOSED arms queue nothing at w null. It cannot produce an I15 (today.cjs:55 never picks it) but it still fires FC01:544 LEGACY_PENDING (kind-based), so the walk keeps a hold shape that the invariant, as the row proves it, excludes. The comment should say it is kept as the round-15 lone-PROPOSED walk shape (R9.4 :159), not as a writer shape; or the generator should drop the entry on a w-null lift. Next round, comment or generator, builder's choice.
- D-R20C-2 (row scope): R20-LEGACY-NEEDS-LOAD pins progression.cjs:365 and FC01:501; writers.cjs:383 is cited but not on the row's call path (canonical calls earnWalk directly). Acceptable (the row is conservative: it goes red if :365 changes even while :383 still holds); name it in the row's comment.
- D-R20C-3 (convention): R20-RESTORE-OVER-LEGACY is green on the debt's current output. Its title marks it KNOWN-RED CARRIED DEBT and the report says it turns red when R9.13 lands; when it does, the row must be flipped to the fixed expectation, never deleted. FC12 should get a carried-debt convention before another such row is added.

## STOP-R20C-1, my reading for the PM
The 45 are all made illegal by a later walk edit of the base (vector on / w null under a pending scalar legacy DEBUT), never by a RESTORE. The rebuild's plan-edit cannot write w or wSets (plan-edit-commands.cjs:46), so these walk actions model re-admitted bases, i.e. the imported old-app class the advisor named for -2. Option (a) would have the walk assert an admission-time normalization that the product does not perform today; that is Joe's shape-check question, not a walk fact. I recommend (b): they stay counterexamples (reported as the residual class, 42 + 3, with the seeds) until the admission-time shape check is answered or R9.13 lands, and the walk is not reshaped to hide them. If the PM rules that admission normalizes legacy entries, then (a) becomes a spec row first (red-first on the admission path) and the walk follows it.

## Not done / limits
- I did not re-run the full 17000-walk shard (4 x ~400 s); I re-ran 1200 walks of the first shard (prefix-consistent) and re-counted all four JSONs plus the two before-JSONs by hash. The 80-file set was not run (no product byte moved; not in the brief).
- Scratch: C:\Users\joeym\AppData\Local\Temp\fable20c (count*.cjs, i15.cjs, i6.cjs, bytes.cjs, trace.cjs, run.ps1, v1/v2 plans, out\fv-*). Nothing committed; this file is the only worktree write.
