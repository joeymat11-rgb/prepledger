# REVIEW NATIVE-LOAD BUILD round 18 (H11 option 1, spec R9.9) - Fable 5.1, l1

Reviewer: Claude Fable 5.1 (claude-fable-5-1), independent; did not write any byte under review. 2026-09-24.
Object: the UNCOMMITTED changes in worktree C:\Users\joeym\AppData\Local\Temp\earned-nlr on f77eca5 (local branch rebuild/e-native-load-red; product bytes there equal c0695e0's, verified: git show f77eca5 and c0695e0 of FC01 and FC03 by explicit path hash 595305b3... and 07b6f261... respectively, identical pairs).
Spec: NATIVE-LOAD-SPEC.md R9.9 at 679567a, extracted byte-exact with cmd /c git show, sha256 ef0f5dd8bc19fa98be6f1a64a70ba98a71d2fe5b6b4f4ccadfe4e61ae3a13e52 (220637 bytes), matches the task. Sections J and K read whole; :123, :127-128, :150-161 read.
Ledger read: DECISIONS.md :777-:803 (read-only). Grants judged: :796 (c), :801 (1) and (3), :802 (A), :803. Rules file opus55-RULES.txt obeyed: no protected-five file loaded, read or named in output beyond its basename; every test run under my own exclusive %TEMP%\earned-runtime.lock (job text "fable51 NATIVE-LOAD reviewer l1 ...", waited on the builder's lock, removed only my own); no git write; nothing under src/, soak, private, ledger/ or EarnedPort touched.

## VERDICT: ACCEPT WITH NAMED DEBTS

The round implements R9.9 H11 option 1 exactly as K names it, red-first for every product change, retires only the K-named rows with reasons, ports R8's two conventions into R7 red-first at the two ruled seeds, re-binds PRODUCER_REVISION, and reruns everything K asks. No engine byte outside FC01; engine-capture.cjs, FC08, FA02, FC16 unchanged (git status: exactly FC01, FC03, FC12, FA03 and the report modified; no untracked file). I found no counterexample to the product. The debts below are disclosures and spec gaps, none a blocking defect.

## 1. Files reviewed (uncommitted, hashes measured by node crypto; all LF, no U+2013/U+2014)
- rebuild/engine/native-load.cjs (FC01) e61ab73f946029005e717e6c595dd076b806fa2707ac8676be1d0b1e79dcfe3f (+79/-? per git diff --stat 79 lines)
- rebuild/m4/workout/native-load-effects.cjs (FC03) 75494ab5f6fcd20e1b0b5a8ad96998e1037b8d31f86b495b2228f954db09c655
- rebuild/m4/spec/native-load-options.test.cjs (FC12) 4036d18a3750913d07757b35cd6e2385232ad11bbcc5579d0154c5fe77888519 (164 tests)
- rebuild/m3/w7-preview/today/test/native-load-panel.test.mjs (FA03) cf47ea1ad0db58a0193b37cf25bf615ca95afb73e6019398d2b9ceea44b0d5a8 (43 tests)
- rebuild/lanes/e/NATIVE-LOAD-BUILD-REPORT.md 4f6b96d212ea44d0f172db0559a9cdc6059f9dbb1d21b1b28e9b7d77d5814194 (Round 18 + REVIEW INDEX appended; the builder's report lists the four code hashes above and they match).
- The builder's scratch copies nlr-build\r18\fc01-f77eca5.cjs, fc03-f77eca5.cjs, fc12-f77eca5.cjs, fa03-f77eca5.mjs hash equal to my own byte-exact git show extractions of f77eca5 (and c0695e0 for FC01/FC03), so the builder's "red on c0695e0" swap evidence is genuine.

## 2. K clause by clause (static read of the diffs, FC01/FC03 by explicit path)
- FC01 header R9.9/ef0f5dd8: yes. entryTarget (SELECTED ENTRY per slot: newWSets at its layout else newW on every captured slot, null for a vector at another layout): yes.
- FC01 step 2 MISSED CLOSE = fold mark (done, 'MISSED', native_load_missed_by === cur.close, native entry of this lift) AND claim (load_basis.authority_refs exactly [closeRef]); typed captures compared with the missed target; set-count and fork clauses kept; PLAN_CHANGED names closeRef once (claim not repeated) plus fork refs: yes. Mark without claim compares with planNow: yes (N29-CLAIM bare case).
- FC01 step 3: adoption branch unchanged; `if (missedClose) refuse('PLAN_CHANGED', [closeRef])` immediately before earn(): yes. adoptReason carries the missed target and the kept working weight (PROPOSED copy): yes.
- FC01 landing(): :560-578 kept; SELECTED ENTRY (capture non-empty, equal length to originals, every cell = target, typed prescribed_load agrees) else DEBUT_BASIS_UNPROVEN 'completion'; exact = every original slot performed at its target (rep miss lands; vector only at its layout via entryTarget null); `!exact` -> q.done, 'MISSED', native_load_missed_by, effect 'missed' with close_ref, nothing else written, returned before the session/wAt lookup (as K's :582-591 replacement places it); exact keeps :592-600: yes. The old 'missed_target' refusal and the unedited predicate are gone.
- FC03: isMissed deleted, isHold = HOLD_CODES only (activeHolds and heldProjection follow): yes. Adopt-baseline anchor admits holding proposal-responses only, the session-close arm removed: yes. MISSED-DEBUT ANCHOR for adopt-observed with non-empty refs: exactly [latest.close], authentic; last matching spend in fold order among uncancelled earns of this lift with close_ref null or the claimed Close (A2), SELECTED ENTRY against cap, accept ops provenBefore the consumed Start; some S4-bound actual differs at its slot; base scalar/fields.w/fields.wSets equal the earn's recorded base_load; vector equals projectBase(recorded fields, cap.length) (DERIVABLE c2 over the captured count): yes. [] keeps the base-equals-capture rule: yes.
- FC03 Close loop: selectedEntry(got, q) replaces full-vector equality; disputed/held skip and causality kept; spend-index close_ref written on effect.kind 'landed' only; missed_target mapping dropped: yes. holdRefusal and exit (b)'s completion_op_id clause dropped: yes. Check path fills the claim from basis.coverage for intent check only, when the fold marks this Close as this lift's missed debut: yes. Host-slot comparison uses the missed entry's target over cap.length: yes. capturedAfter uses SELECTED ENTRY for earns (candidate), exact vector for other effects (builder reading 1, the only sensible reading of K LAYOUT (3)): yes.
- PRODUCER_REVISION re-bound to earned/native-load/v1+sha256:5b2321b4...; R2-REVISION (which recomputes over the engine files on disk) green on head: yes.
- No engine-capture.cjs byte: yes (git status). FC08/FA02/FC16 untouched: yes.

## 3. Rows: new, rewritten, retired (FC12 diff -U0 on test( lines; FA03 diff)
- New (19): N29-R9.9 (8 variants x v1/v2 x R1/R2), N29-ALL-SKIPPED, N29-NO-HOLD, N29-UNDO-LOWER, N29-NEXT-TIME, N29-EARN, N29-CLAIM (bare claim, ignored claim on an ordinary Close, claim on a later ordinary Close, most-recent pick on the [95,100,105,110] ladder under R2 and rename), N29-ANCHOR-CONTROL, N29-FLIP, N29-FLIP-YES, N29-LATER-HOLD, N29-DISPUTED, N29-DEVICE (i)-(iii) with TWO-CLOSE-YES/LAND, N29-VECTOR, N29-VECTOR-LAYOUT, N29-LAYOUT-LAND/-MISS/-UNDO/-UP. Every K-named new row is present; I read N29-R9.9, N29-CLAIM, ANCHOR-CONTROL, FLIP, FLIP-YES, LATER-HOLD, DISPUTED and the FA03 row in full and each asserts J's stated fields (Q done/'MISSED'/missed_by, effect 'missed' with close_ref, spend index close_ref null, N29_UNCHANGED image, next card 100 never 105, isDebutNow false, COMPENSATION_DESCENDANTS on Q's Undo, the claim [C3 Close Ref], refusal codes per variant).
- Rewritten with an R9.9 suffix (5, as K CHANGE names): N29 MISSED-DEBUT REPLAY AND FORGERY, R17-ANCHOR-STRUCTURAL (plus the round-18 "adopt-baseline naming the Q yes" case that closed mutant R17-anchor-holding), R17-ANCHOR-LEGACY-LATER, R17-EDITED-DEBUT, R17b-L9-M03; fixture-changed (2): R17b-L9-M04, R17b-L9-M05.
- Retired (K RETIRE/REPLACE, one reason each in the report Step 4): N29 MISSED-DEBUT (R9.6 hold/exit expectations), N29 MISSED-DEBUT HOLD NAME, the R9.6 REPLAY AND FORGERY text, R17-EDITED-DEBUT (R9.7), R17b-L9-M03 (R9.7), FA03 N29 MISSED-DEBUT [Y] (R9.6). Nothing else removed; N30, N31, N27, R17-RETIRE-MEETS-APPLIED, R17-DEAD-YES, R17b-B31, L9-M07, R13-EXIT-160/-ORDER, R2-DEVICE, R3-B6 are untouched and green.
- R8 walk oracle: isMissedHold dropped from held(); debut classes land/light/other/heavy/mixed; set-count class ('sets' action and sets-before-debut inside 'train', 2 or 4, once, scalar debut only); I14 rewritten to J (consumed, MISSED or ESTABLISH by SELECTED ENTRY, no hold, w/wSets/wAt/last unchanged, target never re-prescribed, exactly [adopt-observed actual] claiming [Close Ref] / PLAN_CHANGED / VECTOR_ADOPTION_UNDEFINED, yes sets w with the entry MISSED); A1 class; corrections on a debut Close; claimed and A1 yeses never RECORD_INVALID at any step. Faithful to K R8 WALK ORACLE.
- R7 port (DECISIONS:801 (3)): device B's plan ops get device_seq 1000+i ascending along the causal chain; m.restores filled by consentUndo and restoreRooted exempts a w-only edit under a consented RESTORE in I5. Both header comments name R8's rounds 13/16 and the seeds.

## 4. Reproduction (my own runner %TEMP%\nlr-fable-l1\run.ps1 and in-memory swap/mutant preload mut.cjs; every run TZ America/New_York, MEASURED_TEST_NOW 2026-09-03, my guard preload printed "FABLE-GUARD protected-in-cache: none; refused: none" in every log; the worktree was never written by a run)
- GREEN head: FC12 164/164 (g-fc12-head.txt 08483adc), FA03 43/43 (g-fa03-head.txt df0f52f9).
- RED-FIRST, product: round-18 FC12 with the c0695e0 product (= f77eca5) compiled in memory: 140 pass / 24 fail (red-fc12-oldproduct.txt e7bb2e34) = R2-REVISION, R8-PROPERTY and 22 R9.9 rows (122-133, 135, 136, 138-144, 148), the builder's red-final3 count reproduced. FA03 N29 R9.9 row on the old product fails at its first R9.9 assertion, the old missed_target hold present (red-fa03-old.txt b420dd3f).
- RED-FIRST, R7 port: f77eca5's FC12 bytes with the f77eca5 product, one run each: seed 20270133 fails "I3 differs under twoDevice" and seed 20265787 fails "I5 stale offer still fresh after plan edit" (red-r7-*-oldtest.txt 73c7644f, 916b500f); round-18 FC12 at both seeds passes (green-r7-*.txt 50a89dad, 48b92abf).
- RETIRED rows: f77eca5's FC12 compiled over the round-18 product: 147 tests, exactly 5 fail = the five retired rows (retired-oldfc12-newproduct.txt ed3d9765), matching the report's Step 4.
- WALKS on the final bytes, fresh seeds of my own: R8 5000 runs from 7000001 with ALL: 0 counterexamples, coverage missed-consumed 156, effect:missed 105, missed-observed-offered 74, missed-observed-yes 57, missed-plan-changed 58, missed-unequal 24, debut-landed 45, setcount-landed 12, setcount-missed 33, sets-before-debut 41, correct-load 11, missed-corrected-landed 7, a1-setup-yes 22, a1-unclaimed-yes 3, later-hold-undo 0 (r8-5000-7000001.json 19340c47, 179 s). R7 5000 runs from 8000001 with ALL: 0 counterexamples, undo 2564, undo-restore 113 (r7-5000-8000001.json 96c7a612, 72 s).
- MY OWN MUTANTS (11, in memory, each applied exactly once, muts-progress.txt 5b1eb5df): land a miss (21 rows fail), close_ref written on a miss (N29-R9.9, N29-DEVICE), first rather than last pick (N29-CLAIM), mark without claim (N29-CLAIM), drop A2 (N29-FLIP-YES), drop the anchor vector check (N29-LAYOUT-MISS), no native_load_missed_by (16 rows), earn branch allowed on a missed Close (N29-R9.9, N29-EARN, N29-VECTOR), capturedAfter exact vector (N29-LAYOUT-UNDO), claim naming any Close (REPLAY AND FORGERY R9.9, R17-ANCHOR-STRUCTURAL R9.9), Close loop at the accepted count (LAYOUT-LAND/-MISS/-UP). All 11 killed.
- Builder's mutant summary read (mutants-summary-all.txt): 61 M18 variants, every FC12/FA03 one killed; the -prop survivors are exactly the disclosed walk-only ones.

## 5. Counterexamples attempted (none broke the product)
- C1 mark without claim on host v1 slots: FC01 alone (typed false) would pass step 2 and, at 95 on the 105 card, offer an UNCLAIMED adopt-observed (base 100). Not reachable through FC03, which fills the claim on every check of a marked Close; and such a record refuses base_load at replay (base 100 vs capture 105). Observation only.
- C2 two missed entries of one lift (N29-CLAIM ladder): missedQ is selected by native_load_missed_by === this Close, so a check on C6 reads Q2, never Q1. Verified in the row and by my first-pick mutant.
- C3 selectedEntry scalar could match a card the entry did not generate only if w equalled newW while the entry was pending; TARGET_QUEUED refuses every check while it is pending and no plan op writes w, so no such card exists.
- C4 missed transition before the SOURCE_FRONTIER session lookup: a miss writes no wAt, so the lookup is not needed; K places the replacement at :582-591, before :592.
- C5 vector target at another layout: entryTarget null -> DEBUT_BASIS_UNPROVEN completion in FC01, but FC03's selectedEntry never sends such a capture (and D-VECTOR-SETS refuses the day before any Start). Consistent with J (l).
- C6 later hold after a claimed yes (J LATER HOLD): the anchor does not check dispute, so the claim verifies and the record then meets TARGET_QUEUED, exactly as J derives; N29-LATER-HOLD asserts it.

## 6. NAMED DEBTS (non-blocking; owner or PM action named)
- D-L1R18-1 (= builder STOP 2, D-R9.9-LATER-HOLD-UNDO): a recorded Undo of a claimed yes that a later correction turns TARGET_QUEUED folds RECORD_INVALID compensates and holds the lift (exit (b) resolves). Spec gap; the builder did not change the product but EXEMPTED exactly this case in the R8 walk's I2 (target absent from the spend index AND a TARGET_QUEUED issue naming its yes; trace later-hold-undo). The exemption must be removed or replaced by the PM's rule in the round that answers it; until then a walk cannot see a regression in that corner. Rare (0 in my 5000, 0-4 per 17000 in the builder's).
- D-L1R18-2 (= builder STOP 1, D-R18-SETS-RESTORE): a consented Undo of an APPLIED adoption after a set-count edit is refused RECORD_INVALID target_load (FC01's RESTORE target is projected at the new count, the recorded base at the old). Pre-existing (no round-18 hunk touches compensation or DERIVABLE), reachable on the host via a sets-only plan edit, outside :796 (c). The walk's set-count class was narrowed so as not to straddle an adoption and its Undo: correct for this grant, but it is a known reachable defect the walks now avoid by construction; owner/PM decision owed.
- D-L1R18-3 (copy): FC01 adoptReason's missed-Close sentence ("the card said <debut loads>, your first workout at the new weight you agreed; your working weight stayed <working loads>") is PROPOSED, not one of the five strings DECISIONS:798 approved. Needs Joe's word before any host shows it.
- D-L1R18-4 (disclosure): N29-DISPUTED and N29-VECTOR-LAYOUT are green on c0695e0 (both pin behaviour R9.9 leaves unchanged); the report names VECTOR-LAYOUT as green-by-design with its M18-V mutants but does not say N29-DISPUTED is product-green. Its red evidence exists by mutant only (R3-repair-land, M3-spend, etc. in the builder's mut outputs). Report wording, not a test defect.
- D-L1R18-5 (oracle change): the R8 walk's window/cache I5 branch now compares a held offer's base with effShown (seed 5009774). Test-only, narrows only when the branch fires, consistent with the plan-edit branch since round 13; the two 17000-walk runs made before it are claimed valid. My 5000 runs on the final bytes support the final oracle; the claim about the earlier runs rests on the builder's argument (no RNG draw, no write), which I checked in the diff and accept.
- D-L1R18-6 (survivors, as accepted since round 16b): 2 host-only, 1 pin-only (R12-decode-absent, killed only by R2-REVISION's on-disk pin), 15 walk-only -prop variants whose FC12 twin is killed; 11 ESM mutants not run (files unchanged). Unchanged character from D-L10-7.
- D-L1R18-7 (low slip): the report cites `git diff c0695e0 f77eca5 -- rebuild` (a directory pathspec, names only, three files reported). The rules ask for explicit paths; no content of a forbidden path was emitted.
- D-L1R18-8 (K OUTSIDE items, carried, not this round's): (1) :150's "copies target scalar/vector into w/wSets" vs the build's w-only equal-load adoption; (2) a completion proven after every holding record whose Start captured a numeric load is PLAN_CHANGED on typed slots while host v1 slots reach exit (b). Both to the PM as K states.

## 7. Rules compliance observed
- Builder: engine bytes in FC01 only (K); FC03/FC12/FA03 test and fold bytes; no engine-capture.cjs byte (vector-capture mutants in memory only); no commit; report and hygiene as stated; disclosed one Set-Content on a scratch JSON, one lock removal of its own after a process-filter mishap, and waits on other jobs' locks.
- Reviewer: no protected-five load (guard in every log), no src/ or private read, no git write; runs only under my own lock; my scratch is %TEMP%\nlr-fable-l1 (runner, guard, mut.cjs, mkmut.cjs, muts.ps1, walks.ps1, logs, byte-exact extractions of f77eca5/c0695e0 FC01, FC03, FC12, FA03 and the R9.9 spec at %TEMP%\nlr-spec-r99.md).

## 8. Files written by this review
- rebuild/lanes/fable/reviews/REVIEW-NATIVE-LOAD-BUILD-l1.md (this file; sha256 in the job summary).
