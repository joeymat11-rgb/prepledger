# REVIEW-NATIVE-LOAD-SPEC-R10-l1: independent review of NATIVE-LOAD-SPEC R9.10 (uncommitted draft)

Reviewer: Claude Fable 5.1 (claude-fable-5-1), independent reviewer, 2026-09-24. I did not write the paper. Read-only for every reviewed path; this file is the only write.
Subject: rebuild/coach/NATIVE-LOAD-SPEC.md, the UNCOMMITTED working copy in worktree C:\Users\joeym\AppData\Local\Temp\earned-astra-96 at HEAD 679567a (R9.9, blob sha256 ef0f5dd8bc19fa98be6f1a64a70ba98a71d2fe5b6b4f4ccadfe4e61ae3a13e52, 456 lines).
Reviewed bytes: sha256 6bd14f813aec52e5aa90517502a5557df614b2e196469aacc48a6857b1920605 (271910 bytes, 482 lines, LF, no U+2013/U+2014). This is the finished draft after the STOPs; the builder's stopped draft 0ba58aad... is superseded by it and is named in the header. git diff --cached is empty; git status shows only this file modified.
Judged against: the builder task (R9.10 spec author: STOP-R910-1 option (B), STOP-R910-2 grant (e), D-R18-SETS-RESTORE, D-R9.9-LATER-HOLD-UNDO, missed-Close copy, :159-:166 byte-identical, J and K updated, STOP on anything :803/:804 leave open), the rules file %TEMP%\opus55-RULES.txt, DECISIONS:777-:804 read whole (:796, :798, :801-:804 in particular).

## VERDICT: ACCEPT WITH NAMED DEBTS (D-R10L1-1..3). No blocking item.

## 1. What was measured (all read paths evaluated; nothing protected loaded)
- Diff read whole: git diff -- rebuild/coach/NATIVE-LOAD-SPEC.md, 20 lines changed in place plus 26 appended (section L). Changed old-numbered lines measured by node line-by-line compare: 5, 146, 152, 154, 155, 156, 210, 233, 242, 248, 320, 380, 397, 399, 422, 423, 425, 428, 455, 456. This equals the header's own list exactly (this header, :146, :152, :154, :155, :156, :210, :233, :242, :248, :320, :380, :397, :399, J :422/:423/:425/:428, K :455/:456, new L at :458-:482).
- :159-:166 byte-identical to R9.9: TRUE (node compare of the eight lines).
- Base named in the header (R9.9 679567a sha256 ef0f5dd8...) matches the measured HEAD blob.
- READ basis dfc4445 exists locally (commit; round 18 "spec R9.9 H11 option 1"). Every cited file extracted byte-exact with cmd /c git show dfc4445:<explicit path> and hashed with node crypto: FC01 native-load.cjs e61ab73f (matches L), engine-capture.cjs 94685ca4, today.cjs 685f6e1e, progression.cjs 7031838d, performed.cjs 2372e66b, engine-capture.test.cjs 6d19eebb, configuration-capture.test.cjs 839076da (all match L); each of these except FC01 and FC03 is byte-equal at f77eca5, as L states. No protected-five file was extracted, opened, searched or executed.
- Line citations verified at dfc4445 (exact unless noted): W/engine-capture.cjs :19 number, :22-26 loadCell, :52 clone, :57 tgt guard, :64 baseline, :69-71 selection, :72-77 configured, :79 debut arm, :80 ordinary arm, :81 scalar, :82 final guard, :97 mutation guard, :103-110 resolveLayout, :111-130 readLayout. E/progression.cjs :80-82 _prescribedLoads (used at :84 only), :83-87 _rowAtCurrentLoad, :137 first "Copied from frozen src/app.jsx" marker (progressAnchor), :157 anchor's current-load test, :199-200 native tenure loop, :268-273 targetsFor pad/truncate. E/performed.cjs performedLoadMatches is :243-251 (spec says :244-254; the undefined-position "else return false" the spec relies on is :249). E/today.cjs :89 genSession, :96-98 isDebutNow/q/w, :114 pendingThird, :128-129 debut pad. FC01 :109 planVector, :127-129 baseLoad, :164-170 adoptReason (the missed-branch sentence equals L COPY word for word and equals Joe's approved clause at :804), :174-177 entryTarget (null at another count today), :201 compensation dispatch, :203-208 TARGET_QUEUED, :220 planNow, :230 missedTarget, :232-233 count/typed clause, :340 candidate-length refusal, :357-390 compensation (:370 lastConsumed, :371 latest-row requirement, :380 queued/held branch, :381-385 applied branch projecting planVector({...ex,w,wSets}) at the CURRENT ex.sets), :463 DERIVABLE (c2), :471-472 compensates check, :474-478 RESTORE check (T equals x.base_load exactly), :548 compensate (unapplied-restore branch writes w/wSets from base_load.fields; applied branch restores FIELDS = w,wSets,wAt,last,lastMeta,own,std,topAt,topRun, no sets), :582 landing, :610 exact, :615 missed transition. FC03 :16 BLOCKING, :20 HELD_BACK, :133-136 selectedEntry (exact newWSets equality today), :138 entryTargetOf, :142-147 projectBase, :157/:159 capturedAfter, :228 HOLD_CODES, :261 authorityRoot, :455/:459 anchor pick and target, :527 entryOf, :607 repair map, :724-731 refusedAccept, :734-735 BASIS_REPAIR_REQUIRED, :832 Close loop, :997 planNow, :1001 want, :1013 step3Earn, :1044 PRODUCER_REVISION. plan-edit-model.cjs:334 writes only the changed keys; plan-edit-commands.cjs:46 CHANGES includes 'sets'; T/gym-model.mjs:68-76 prescriptionLine uses load.display; native-next-targets.test.cjs:58 and :320-321 set wSets [40,35] on a sets-2 fixture (fixtures.cjs:15); source-delta.cjs:13 pins progression.cjs 7bb7d75a and :142-146 holds the old _prescribedLoads text; acceptance-s8-real-shape.json:271-275 and S9.json:113-117 carry progression.cjs pre = post 7031838d..., role carried.
- Test rows named by STOP-R910-2: engine-capture.test.cjs:68-73 asserts wSets [40] refuses at :69-70 on the 2-slot fixture; configuration-capture.test.cjs:88 lists [40] first (ex.w 40) among five vectors. Their fixtures need PERFORMED_W6_DIR and Source.load of browser-engine.cjs (requires dates, constants, entered-load, performed, plan, progression, sleep, energy, policy, today, volume, earn; none of the protected five by name), so I did not run them; the inversion is reproduced by the probe below.

## 2. Red-first and green reproduced (runtime lock taken as fable-r910-review-l1, released after; no product byte touched; no fixture, no protected five)
Probe %TEMP%\fable-r910-review\probe.cjs loads the byte-exact dfc4445 engine-capture.cjs through a stub engine (genSession returns one card sized to ex.sets, rirPlan a constant plan, prescriptionCapture.prepare the identity) and, beside it, a SCRATCH copy with L's fit(v,n) transcribed and :79/:80 replaced exactly as L "CLAUSES THAT CHANGE" says (anchors matched byte-exact). Results, dfc4445 -> scratch fit:
- FIT-ORDINARY-UP (w 100, wSets [100,95], sets 3): ENGINE_CAPTURE_LOAD_MAPPING_REQUIRED -> [100,95,95], displays '100 lb','95 lb','95 lb'. RED on dfc4445 as L claims.
- FIT-ORDINARY-DOWN (sets 1): refused -> [100]. FIT-DEBUT-UP (debut [105,100], sets 3): refused -> [105,100,100]. FIT-DEBUT-DOWN: refused -> [105]. All red on dfc4445.
- FIT-IDENTITY: sets 2 vector [100,95], debut [105,100] at sets 2, scalar at sets 3: identical cells on both.
- FIT-MALFORMED: [] , [40,35,NaN] and [40,35,-0] on a 2-slot card (bad entry beyond the card), sparse new Array(2) on 3, present-null wSets, newWSets null debut, scalar debut on a vector lift (newW !== card.w): refused on both, same code. L's "validate the whole stored v before fitting" holds in the transcription.
- STOP-R910-2 inversion: wSets [40] on the 2-slot card: refused on dfc4445 (engine-capture.test.cjs:69-70 green today = the R9.10 row's red) -> [40,40] on the fit copy, exactly L's carried expectation ({value:40,unit:'lb'} twice, '40 lb').
- READER: today's _prescribedLoads text vs L READER on {sets 3, wSets [100,95]}: [100,95,100] vs [100,95,95] (the STOP-R910-1 gap, now Joe's (f)); wSets [100,null] at sets 3: [100,100,100] both (null last entry falls back to w, as L says); [], present-null, ABSENT, equal length, longer than sets: identical on both. The rule changes a read only at a position beyond a non-empty vector whose last entry differs from w, as L REACH derives.
Static reproduction of the two paid debts: RESTORE COUNT red is derivable from the bytes: :384 projects the applied-adoption target at the current ex.sets while :474-478 demands T equal x.base_load (recorded at the consumed completion's count), so any count edit between yes and Undo is RECORD_INVALID target_load today; L's n0 = originalSlots(rows[lastConsumed].entry).length restores equality because :232 forced originals.length = ex.sets when the base was projected, and :548's applied branch never writes sets. LATER HOLD UNDO red is derivable: refusedAccept :728 keeps a spend only for HELD_BACK codes, TARGET_QUEUED is in neither set, so :471-472 finds no spend for the recorded Undo (RECORD_INVALID compensates, as round 18's probe showed); L's four-part condition (adopt-observed, non-empty authority_refs, TARGET_QUEUED, causing entry in the repair map at :607/:735) is evaluable in that function's scope and the Undo body is exempt from the held-root conflict at :708 (body.kind !== 'compensate').

## 3. Judgement against the task and the ledger
- STOP-R910-1 option (B), grant (f) (:804): READER paragraph specifies E/progression.cjs:80-82 exactly (k = min(i, len-1), null falls back to w, empty/present-null/ABSENT unchanged), names the three mirrors (FC01:109, FC03:142-147, FC03:997), derives REACH (native rows only through _rowAtCurrentLoad; legacy byte-identical), rows READER-UP/DOWN/IDENTITY/NULL-LAST/HISTORY/PARITY/UNDO-TEXT/EARN and mutants, custody FG02 at :242/:248 with the S8/S9 pins measured, and the source-delta.cjs:142-146 historical copy correctly left alone (its readers take the pinned preimage). Correct and complete. The FIT GUARD is kept, so the reader change moves tenure and the reps anchor only, never an offer: this is the ruling's "counts normally" without a raise.
- STOP-R910-2 grant (e): EXISTING ROWS THAT CHANGE carries exactly the two assertions, '<row> R9.10', everything else byte-identical, original red retained, and adds a PM-confirmation rule for any READER-inverted row found by running; correct reading of :804.
- D-R18-SETS-RESTORE: RESTORE COUNT (:154, J D-L8F-1, L) with seed 20266884 red-first, RESTORE-FORGED keeps the RESTORE check unweakened, sets not restored. Correct.
- D-R9.9-LATER-HOLD-UNDO: LATER HOLD UNDO (:155, J :425, K :455, L) with seeds 20264386/20266433 and the I2 exemption removed; exit (a)/(b) consequences derived from unchanged capturedAfter and SOURCE_OVERLAP. Correct; the "keep the spend for every TARGET_QUEUED refusal" mutant is honestly marked possibly-equivalent.
- COPY: :804's approved sentence equals FC01:164-170 at dfc4445 word for word; H9 (:397), K COPY (:456) and L COPY say no other string is approved; SET_COUNT_BASIS_UNPROVEN display stays PROPOSED with a STOP if FA02 has no generic path. Correct.
- FIT (grant (e)): the capture hunk is minimal (:79/:80 only, :81/:82 untouched), the card is not changed (no E/ byte for the fit), stored values untouched (:52/:97/plan-edit-model:334 cited correctly), landing gains the layout clause (without it my scratch copy would let a fitted debut performed at [105,100,100] pass :610 and land; L (2) and the mutant list catch exactly this), SELECTED ENTRY takes fit in both modules with FIT-PARITY, FIT GUARD placed after the count clause and before the comparison in both slot kinds, Undo/two-device/pre-FC16 consequences derived from unchanged code. J (l), :210, :320 N29 VECTOR LAYOUT and :380 G agree with L on every value I checked ([105,100,100] card, MISSED at any loads, SET_COUNT_BASIS_UNPROVEN check, next card [100,95,95], variants 2->1 and 2->3->2).
- Nothing outside :803/:804 is decided: K's superseded paragraphs are marked and kept; OUTSIDE R9.10 lists the pre-existing refusals and leaves the ledger PAID marks to the PM. The paper writes no ledger line.
- Rules: paper only, LF, no dashes, explicit paths in every git command it cites, no protected five named as read or run, local-run guidance defers protected-fixture files to CI.
Counterexamples tried and answered by the paper: fitted debut performed exactly (landing clause); count restored while a fitted Start is open (judged at its own layout, PLAN_CHANGED count clause after the MISSED consumption); ordinary fitted card equal to the fitted debut card (impossible: newW differs from w at position 0); held vector lift (wSets projected null, no fit); malformed entry beyond the card (whole-vector validation); Undo of an applied adoption after 3->2 and 3->4 (n0); a device without the edit (unfitted capture, first Start wins, both other-Close refusals named); a record with an added position (base_load and RETIRE target replay under the new (c2)).

## 4. Named debts (none blocking; each is a paper fix or an owner-facing name, not a rule change)
- D-R10L1-1 (attribution, header and L GRANT): the sentence quoted as DECISIONS:803 (e) "read verbatim" ("engine-capture may fit a per-set-weight lift's captured vector to the card's set count (fewer sets, capture what was performed; extra sets, repeat the last set's weight), nothing raised without a yes, via spec R9.10 and a NATIVE-LOAD build round only") is the rules file's grant (e) paraphrase, not :803's words. DECISIONS:803 says, verbatim: "Asked to fix D-VECTOR-SETS (fewer sets: save what was done; extra sets: repeat the last set's weight; nothing raised without a yes): "Yes, fix it (Recommended)". This grants that engine-capture change via spec R9.10 and a NATIVE-LOAD build round." The substance is identical; the paper should quote :803's own sentence (or say "as the rules file states grant (e)") in the header, L GRANT and H11 (3). Paper fix.
- D-R10L1-2 (owner-facing consequence to name): under R9.10 a per-set-weight lift whose set count stays different from its stored vector's length never earns or adopts again until the count is restored (FIT GUARD refuses both branches; :144 leaves vector adoption undefined; no rule can re-base the stored vector at the new count without a yes that no grant names). :146 and G (:380) state it in rule and owner language, which is correct under :803/:804, but the paper names no debt or H item for it, so it can be lost after the seal. Add a named item (suggested D-R9.10-VECTOR-COUNT-STUCK) in H or OUTSIDE R9.10 as an owner question for a later round: whether a yes may re-base a vector lift at its new count. Not decided here; nothing raised.
- D-R10L1-3 (nits): (a) header "lines 1-457 keep their numbers": R9.9 has 456 lines; :457 is the new blank line before L. (b) E/performed.cjs performedLoadMatches spans :243-251 at dfc4445, not :244-254. (c) OUTSIDE R9.10 (1) could also name a stored vector with a null ENTRY ([40,null]), which the readers support (position reads w) but :82 refuses at every count, today and after fit (probe); pre-existing, not a set-count case.

## 5. What I could not do
- I did not run W/test/engine-capture.test.cjs or configuration-capture.test.cjs (they need PERFORMED_W6_DIR and the Source loader; the builder's job under L, CI otherwise); the inversion is reproduced on the extracted bytes instead. I did not fold FC01/FC03 (RESTORE COUNT and LATER HOLD UNDO reds are derived statically from the cited lines, and the round-18 probe results the paper cites are not re-run here). No walk seed was run. No product byte was written; the scratch fit copy lives only under %TEMP%\fable-r910-review.

## 6. Files
- Written: rebuild/lanes/fable/reviews/REVIEW-NATIVE-LOAD-SPEC-R10-l1.md (this file; sha256 in the return packet).
- Reviewed, unchanged: rebuild/coach/NATIVE-LOAD-SPEC.md working copy sha256 6bd14f813aec52e5aa90517502a5557df614b2e196469aacc48a6857b1920605.
