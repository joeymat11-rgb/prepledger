# REVIEW-NATIVE-LOAD-SPEC-R11-l1
Reviewer: Claude Fable 5.1 (claude-fable-5-1), independent reviewer, commissioned by the Claude Opus 5.5 PM; 2026-09-24. I did not write the change under review.
Subject: the UNCOMMITTED R9.11 edit of rebuild/coach/NATIVE-LOAD-SPEC.md in worktree C:\Users\joeym\AppData\Local\Temp\earned-astra-96 at HEAD d3a3ffa (R9.10, rebuild/c-native-load-spec).
Measured: git status shows exactly ` M rebuild/coach/NATIVE-LOAD-SPEC.md` and `?? rebuild/lanes/astra/reviews/NATIVE-LOAD-BUILD-REVIEW-L11.md`. HEAD file sha256 6bd14f813aec52e5aa90517502a5557df614b2e196469aacc48a6857b1920605 (271910 bytes, byte-exact via cmd /c git show HEAD:path); working file sha256 5c837f37e1d5620c88f8498bbd01f7ab636d323016219c410ffcabb41688eb34 (293201 bytes). Astra L11 sha256 82d2c1fe1fa875bce6319ecc08ed0d349af89afbc60059ee903f07d614d3eb26 (20488 bytes), read whole.
Judged against: the builder task (spec author R9.11, owned file only; confirm B37/B38 against Astra's inputs; specify the minimal rule for B39 without moving any weight without a yes and without prescribing a debut twice; :159-:166 byte-identical; J rows, K/L builder list, red-first rows incl. seed 11301954 and the host reduction, a real-card-path walk class, mutants; STOP on owner rulings), the rules file opus55-RULES.txt, and DECISIONS:777-:804 (read whole; :784-785 grant (a), :793, :795, :796, :801-:804).
Method: static, byte-exact. Every engine citation re-measured on blobs extracted with cmd /c git show dfc4445:<explicit path> and hashed with node crypto (FC01 rebuild/engine/native-load.cjs e61ab73f... 49029 bytes; FC03 rebuild/m4/workout/native-load-effects.cjs 75494ab5... 82968; rebuild/m4/workout/engine-capture.cjs 94685ca4... 10797; rebuild/engine/today.cjs 685f6e1e... 47806): all four equal the hashes section M states. No protected-five file was extracted, opened or loaded (git archive of rebuild/ at dfc4445 into my scratch excluded the five, rebuild/conform/private, *soak* and *ledger* by pathspec). Nothing committed; no worktree switched; the only file written in the worktree is this review.

VERDICT: ACCEPT WITH NAMED DEBTS (D-R11L1-1 .. D-R11L1-5 below). No blocking item.

## 1. Shape of the edit (measured)
- Line count 482 -> 532 (both files LF, no CR, no U+2013/U+2014). Differing pre-existing lines: exactly :5 (header), :150, :158. Lines 484-533 are new (section M). Every pre-R9.11 line number is kept, as the header claims.
- :159-:166 byte-identical: sha256 of those eight lines (LF-joined) f4ecaa37658253df... before and after, matching the header's f4ecaa37.... :11-:145, :147-:149, :151-:157 identical.
- Header revision paragraph names the base (R9.10 d3a3ffa, sha 6bd14f81...), the review (sha 82d2c1fe...), the two rules (:158 EXIT AFTER ITS HOLDS DISSOLVE, :150 SCALAR ADOPTION OVER A NULL VECTOR), section M, and the named debt D-R9.11-TWO-EXIT. All checked true.

## 2. B37 and B38 (paid by R9.10, confirmed by M) - CONFIRMED
- B37 (Astra: working 100, adopt 105, sets 3 -> 2, Undo [100,100] refused RECORD_INVALID target_load; host adopt 45 over 40, 2 -> 1). R9.10 :154 RESTORE COUNT issues the target at the adoption's recorded base count (3, resp. 2), so target [100,100,100] equals the adoption's recorded base_load and the unchanged DERIVABLE RESTORE check (:156) passes; the applied branch (FC01:569-573 at dfc4445) restores every prior field, sets stays 2 (1). M's derivation matches the code I read (FC01 compensation :357-390, :380 and :384 are the current-count issuance R9.10 replaces; :541 fieldImage). Rows B37-ASTRA / B37-ASTRA-HOST use Astra's numbers exactly. Confirmed; no rule text change needed.
- B38 (Astra: Q105 missed at 95, adopt 95 yes, Undo yes, then a consumed completion corrected; host: a consumed C1 set removed). FC03 evidenceChanged (:474-489) is true for both a correction and a removal, so Q's accept enters the repair map (:733-736) and R9.10 :155 LATER HOLD UNDO holds the claimed yes back with its spend kept (refusedAccept :724-731 extended per L); the Undo names a live spend, takes compensate()'s unapplied RESTORE branch (FC01:557-566) and writes the recorded base w 100 (40). Issues exactly BASIS_REPAIR_REQUIRED [Q ref] + TARGET_QUEUED [yes ref], no RECORD_INVALID, cancellation kept; exit (b) recovers on a later completion. This is what Astra's invariant asks (no dead recorded yes). Confirmed; rows B38-ASTRA / B38-ASTRA-HOST.

## 3. B39 trace at dfc4445 - CONFIRMED against the code, and against Astra's retained red
- Final fold with the base back at 100: Q105's accept meets its recorded base (movedBase :221-226 false) -> queued, not held. Exit accept: activeHolds (:647) empty -> exitB (:655) false -> V = state (:657) -> movedBase(w 100 vs recorded null) -> EFFECT_CONFLICT load_basis, spend kept, unapplied (:708-717). Undo: heldTrace (FC01:351-353) finds no applied exit -> unapplied RESTORE branch (:557-566) writes the exit's recorded base_load.fields, w present-null and wSets present-null (the exit was issued on projectHeld, which sets `{ ...x, w: null, wSets: null }`, FC03:250-254); :816-819 clears the conflict. Result w null, Q105 pending, no issue. genSession (E/today.cjs:96-98, :103-110) then makes baselineAsk true with card.w = q.newW 105, and W/engine-capture.cjs:64 refuses ENGINE_CAPTURE_BASELINE_UNPROVEN for the whole day.
- Astra's retained measurement (C:\Users\joeym\AppData\Local\Temp\earned-nls-L11-dfc4445\diag-output.txt, read as data): seed 11301954, `BEFORE_FAKE_C4 w:null queue:[DEBUT newW 105 done:false] issues:[] card:{w:105,baselineAsk:true,isDebutNow:true} capture:ENGINE_CAPTURE_BASELINE_UNPROVEN`. This is the actual red at dfc4445; the walk's cardOf (FC12:3183-3185) invents `Array(k).fill(q.newW)` where prepare refuses, which is Astra's D-L11-WALK and M's walk (a).
- Second trap (author-found): confirmed. FC03's exitB branch already deletes a null wSets from the programme (:798-799), but the exit's prior image (FC01:541) is taken on V = projectHeld, so its Undo (applied branch :569-573) restores wSets present-null; the next ordinary adopt-baseline (state = t.state, :807) writes w alone (FC01:542) and W/engine-capture.cjs:80 then copies the null wSets and :82 refuses ENGINE_CAPTURE_LOAD_MAPPING_REQUIRED for the whole day. Reachable from ANY hold + exit + Undo + adopt-baseline, not only B39, so :150 is necessary and the B39 rule alone would not restore the invariant. The header's "both rules are needed" is right.

## 4. The rule (:158 EXIT AFTER ITS HOLDS DISSOLVE; :150) - judged sound
- No weight moves without a yes: w 60 (the exit yes) and w null (the Undo yes) are each a recorded yes; the unordered base return has no ordering op and never wins over a later authenticated plan op (:156, :161 "a later base-w change neither conflicts with it"). The exit's Undo restores the baseline ask exactly as :161 already says. No debut is prescribed twice: Q105 is hidden while held and retired SUPERSEDED by the exit; under R9.11 it is never on a captured card.
- Invariant check (constructed): earn records need a numeric w (c3), so a pending native entry over w null can arise only by an Undo writing null; the adoption Undo paths are COMPENSATION_DESCENDANTS-guarded (a later Start captured the adopted load), the LATER HOLD UNDO writes a numeric base, and the exit paths are exactly the ones R9.11 rewrites. Two-exit race (D-R9.11-TWO-EXIT), derived by me: the second device's exit is not dissolved (the superseded hold names its refs) -> EFFECT_CONFLICT -> the lift is held with no entries -> baseline ask captures; its unapplied RESTORE Undo writes w null over the first exit's applied 60 by a consented RESTORE whose displayed target is null; after the first exit's Undo, movedBase is false at the second exit's place and it applies as an ordinary adoption, which :150 keeps capturable. No trap found in that class; it stays a walk class to measure (D-R11L1-2).
- Minimality: an alternative exists (an unapplied RESTORE of an exit-shaped record writes nothing, leaving w 100 with Q105 pending and its 105 card next), but it would change :156's Fable l8 P1 rule (never-applied RESTORE writes the recorded w/wSets; Astra L10-M02 kills its omission) and discard the recorded exit yes. The author's rule keeps :156 and DECISIONS:793 (a yes judged at its original cut). I accept the choice; its one behaviour change outside the trap is named (D-R11L1-3).

## 5. Builder list (M) - citations verified at dfc4445
FC03 :647 holdIssues, :653-658 anchors/exitB/V/behind, :708 movedBase gate, :724 refusedAccept, :733-736 repair, :795-807 exit branch (incl. the null-wSets delete at :798-799), :816-819 compensation clears, :944-950 exit issuance filling authority_refs, :1044 PRODUCER_REVISION, :434-437 the ok test with its holding clause, :250-254 projectHeld, :474-489 evidenceChanged, :157-161 capturedAfter. FC01 :537-546 adoption (:540 array refuse, :541 prior, :542 w), :548-576 compensate, :351-353 heldTrace, :357-390 compensation, :109 planVector, :122-128 fieldImage/baseLoad. E/today.cjs :96-98, :103-110. W/engine-capture.cjs :64, :80-82; it has no require (confirmed), and FC12 guards the protected five at load (Module._load hook, :20-28) and requires engine-capture at :2229 and :2834. FC12 :2227-2234 cardLoads, :2878 R13-EXIT-160, :2891 R13-EXIT-ORDER, :3120 propertySequence8, :3183-3185 cardOf: all true. The clause texts (1)-(3) are implementable as written and touch FC01/FC03 only (grant (a), DECISIONS:784-785); no engine-capture, today or progression byte.
Rows: N27-B39-RETURN-UNDO (Astra's input, both orders, red at :64 through cardLoads), -RETURN, -UNDO-AFTER-RETURN, -CONTROL (second trap), -NULL-VECTOR, -REPAIR-REVERT, -ORDINARY and -CLAIM-ORDER (green-kept, mutant-red), -HOST (FA03, Astra's reduction), -INVARIANT; walk (a) real card path with I15/I16 and CARD-PARITY, (b) exit-undo-return class, (c) two-exit class report-only, (d) Astra's seed range then the R8 17000 and mutation bounds; ten mutants each mapped to a killing row. Complete for the task's list.

## 6. What I could not do
- Red-first reproduction of seed 11301954 at dfc4445 was prepared (scratch tree from git archive of rebuild/ at dfc4445 minus the excluded paths; runner run-seed.ps1 with NATIVE_LOAD_PROPERTY8_SEED=11301954, RUNS=1, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York) but NOT run: %TEMP%\earned-runtime.lock was held for my whole window by "opus55 NATIVE-LOAD builder r19 r8-17k-1000001 (earned-nlr)" (created 15:49:55; re-created as "r8-17k-5000001" by 16:00; still held at every poll through 16:03:08, an exclusive New-Item poll of 38 s three times). Per the rules I did not remove it and never ran with it present. The red cited above is Astra's retained diag-output.txt plus my static derivation on the byte-exact blobs. Green cannot be reproduced: R9.11 is paper only and no build exists. The prepared scratch tree and runner stay in %TEMP%\fable-r911-l1 (run-seed.ps1, poll-run.ps1) for whoever holds the slot next.
- No host (FA03) run; no CI; nothing under rebuild/conform/private, src/, ledger/, *soak* was read.

## NAMED DEBTS (for the PM to carry or the author to pay in a later revision; none blocks R9.11)
- D-R11L1-1 S8 numeric arm vs dissolved hold: :155 ADOPT-BASELINE ANCHOR (FC03:434-437, unchanged) still admits a NUMERIC capture of the consumed Start only when an ACTIVE hold names each ref. A dissolved-hold exit whose capture were numeric would refuse RECORD_INVALID base_load under R2, against :158's "judged on the held projection it was issued on". Bound: the check issues an exit only when the Start is proven after every holding record (FC03:930-943; a numeric capture refuses PLAN_CHANGED), so a genuine exit's capture is the baseline ask (null cells) and S8's numeric arm is skipped; reachable only through Astra's open D-L11-TYPED-HOLD. Pay by one sentence in :158 or M stating this bound, or by extending S8's clause with the same order test; add a green-kept row (dissolved exit, null capture, R2) so the bound is measured.
- D-R11L1-2 two-exit class: M says "not derived here". My derivation (section 4) finds no trap but one consented weight write worth a row: the second exit's unapplied RESTORE Undo writes w null over the first exit's applied 60. The walk class (c) should assert exactly that and the :150 key removal at the second exit's later application.
- D-R11L1-3 behaviour change outside the trap, for the ledger line: J (o) (exit, return, no Undo) at dfc4445 held the lift (exits offered); under R9.11 the recorded exit applies (w 60, Q105 SUPERSEDED). It follows from :161 and moves no weight without a yes, but the PM should accept it consciously when publishing R9.11.
- D-R11L1-4 :150 wording: "A present-null wSets reaches the programme only as the held projection's null (:159), which an exit's Undo restores" omits the unapplied RESTORE branch (FC01:557-566) of an exit-shaped record, which writes the same present-null pair from its recorded base (the B39 path at dfc4445, the second-exit path after R9.11). The rule itself keys on the stored form at adoption time and covers both; the sentence should say so.
- D-R11L1-5 the red-first evidence for seed 11301954 in this review is Astra's, not mine (section 6); the builder's round must show its own red at I15 before the fix, as M (d) already orders.
