# REVIEW-NATIVE-LOAD-SPEC-R12-l1 (Fable, independent static reviewer, 2026-09-25)

VERDICT: REJECT as a build basis. The paper is honest, precise and inside grant (a), but the ruled clause (arm (ii), "no hold names the ref") does not meet precondition (2): it admits one of Astra's L12 section 4 anchor probes (FC12 R17-ANCHOR-STRUCTURAL :2657 at 189d076), which the author found and named STOP-R912-1. Statically I confirm that derivation, and I add that the "re-pin the R17 case" candidate is not a safe way out (F3). The clause needs a witness of the original hold, so the PM ruling as stated ("one clause") is falsified for that probe and the paper should return to the author with the witness decided, not build after a re-pin.

Object: the uncommitted edit in C:\Users\joeym\AppData\Local\Temp\earned-astra-96 (HEAD d91e4df). Measured by node over git show with explicit paths: base blob sha256 5c837f37..., 533 lines; worktree file 123c8c08dd1bf4aaa56d3547f3edd4137a39f68b4f14b1773c747eb829062cd0, 312442 bytes, 556 lines (555 plus the trailing newline). 0 CR, 0 non-ASCII bytes, 0 U+2013/U+2014. Changed lines: 5 (append only), 155, 500, and 533-555 appended; lines 1-4, 6-154, 156-499 and 501-532 are byte-identical; :159-166 hash f4ecaa37 before and after. Inputs read whole: opus55-RULES.txt, Astra L12 (sha256 e48e1b8e...), the precondition list (41c3981e...), FC03 at 189d076 (earned-nlr copy, blob a4687901 = 189d076, sha256 eb3a7617...) at :230-257, :270-290, :340-455, :596-716, :716-816, :926-970, and FC12 :2636-2662 (unchanged by the round-20 working copy, whose diff starts at :3760). Nothing run, no lock taken, no protected path opened.

(1) Fold order traced (FC03 at 189d076): exitB with M (1)'s dissolved test (:651-656) -> V = projectHeld (:657) -> reproducible on V (:679) -> correspondence only when not reproducible (:681-688) -> DERIVABLE (:691) -> exit branch (:816-823, holdIssues superseded_by at :823). For L12-B5 at base 100: under R2, and under R1 when the cut does not reproduce, (1b) at :443-447 admits y1's ref by arm (ii) (authentic, class plan, kind proposal-response, lineage fx-press, dissolved, provenBefore([y1], byId.get(latest.start)) true because the check issued the exit only on a Start proven after every hold ref, :953), M (1) sets exitB, the record applies as the exit (w 105), and its recorded Undo is the Undo of an APPLIED adoption (restores w null). Under R1 when the held cut reproduces, the re-evaluation at :770-773 passes body.basis (with authority_refs) to FC01, which only reads them, so the exit reproduces and applies without (1b). All paths give Astra's 102.5 end state; consistent with :158. MET, subject to F2.
(2) Arm (ii) as written refuses a forged ref (authentic, :392), a wrong-lift ref (lineage test at :446), a non-proposal or Close ref (class 'plan', kind 'proposal-response'; a Close is class 'session'), a post-anchor ref (provenBefore :277 against latest.start :423) and a ref named by a superseded hold (dissolved reads every isHold issue of the lift in `issues`, where :823 keeps superseded ones). NOT MET for a never-held ref: F1.
(3) No silent increase: target_load stays the S4-bound actual loads and base_load the record's own null projection; every write is exit (b)'s or its Undo's; the consented values are the lifted 105 and the Undo's null. MET (see F3 for what a never-held exit bypasses; it is not a raise).
(4) Grant (a) only: FC03 :443-447 plus the PRODUCER_REVISION rebind (:1068 at 189d076); no FC01, W/engine-capture, E/today, E/progression or copy byte. MET.
(5) Rows N28-B5-ASTRA (both orders, red outputs named), -NO-UNDO, -R1R2-PARITY (green equality, stated as such), the five probes with expected issue lists and w, seven mutants each with a killer, walk class (b') with seed 12035226, and (B1)/(B2) are written so a builder can write them red-first. MET, with D-R12L1-5 and D-R12L1-6.
(6) Only :5, :155, :500 and the appended M lines moved; J, K, L and :158 byte-identical; the five approved copy strings untouched; ASCII, LF. MET.

FINDINGS
F1 (blocking; :155 arm (ii), M :538-539). Confirmed statically: R17-ANCHOR-STRUCTURAL's last case (FC12:2657) is an adopt-baseline over C3's numeric capture naming fx-resp-1, an authentic earn yes of fx-press proven before C3 and named by no hold (Q was consumed MISSED, not held). Under (1b) it passes arm (ii) (R2: not reproducible, so correspondence runs at :681); M (1)'s dissolved test then sets exitB at :651-656 (holdIssues empty, ar non-empty, ref proven before an anchor, no hold names it); FC01 adopts 95 on the held projection and the exit branch writes w 95. The pin (want RECORD_INVALID:base_load, w 100) goes red. So arm (ii) cannot tell "the hold dissolved" from "there never was a hold"; the ruled clause is under-determined, exactly as STOP-R912-1 says. The R9.11 M (1) has the same gap for null captures (no ref is checked in correspondence there), so the witness belongs in M (1) too.
F2 (:155 arm (ii) vs :158 and M (1), :500). Two anchors now coexist: (1b) requires the ref proven before the latest consumed Start; :158 and M (1) accept "before the Start ... or before its own response". A record whose ref is proven before y2's response but not before C2's Start sets exitB (M (1)), so V = projectHeld; under R1 its held cut can reproduce and the FC01 re-evaluation echoes body.basis, so it applies, while under R2 (1b) refuses it base_load: an R1/R2 split. Only synthetic records reach it (a genuine exit has every ref proven before the Start, :953), but N28-B5-POST-ANCHOR does not say which sub-case it pins and "R1 and R2 alike" is asserted, not traced. Align the anchor (narrow :158's "or before its own response" and M (1)'s anchor list to the consumed Start, which :953 already guarantees for every genuine exit), or pin the sub-case and say what R1 does.
F3 (STOP-R912-1's candidates, :539). "Rule the R17 case's new outcome" is not a safe candidate: exitB skips the movedBase gate (:716 `!behind && !exitB && ... movedBase`), so an adopt-baseline on a never-held lift that names any old proposal-response of the lift would apply on the held projection instead of being held EFFECT_CONFLICT by :156 UNPROVABLE ORDER, and would retire the lift's pending native entries SUPERSEDED (:816-823). Nothing is raised (target = actual loads, consented), but :156 is weakened for that shape. Of the candidates listed, only ORIGINAL-CUT WITNESS (or an equivalent witness that the record's own cut held the lift by an issue naming r) distinguishes L12-B5 from R17: y2's basis.source is the 102.5 admission where y1 is EFFECT_CONFLICT; the R17 record's cut holds nothing. Its feasibility (S7's "earlier admission recorded in the same generation" must keep that admission's base image for the lift) is the open measurement, and it should be measured before the PM rules.
F4 (minor, :155 arm (ii) text). "the consumed-Start anchor of M (1)" reads as if M (1) had only that anchor; M (1) (:500) lists the response first. Say "the narrower of M (1)'s anchors".
F5 (minor, :537). "(1b) closes that split" is true only on the correspondence path; on the R1 reproducible path the re-evaluation decides. The row already makes the builder print the path; the sentence should say "(1b) closes the split on the correspondence path".

NAMED DEBTS
D-R12L1-4: arm (ii) and M (1)'s dissolved test need a witness that the record's original cut held the lift (F1, F3); STOP-R912-1 stays open and (1b) must not build before it is ruled. The 189d076 output of R17:2657 is retained; it is never re-pinned to w 95.
D-R12L1-5: one anchor for the dissolved test everywhere (F2); N28-B5-POST-ANCHOR must fix the "before the response, not before the Start" sub-case and its R1 path.
D-R12L1-6: the R1 path of every green-kept probe is unstated (a re-digested body keeps the cut digests, so under a present revision the re-evaluation, not correspondence, may decide); the builder reports which path each probe takes under R1, and any probe that applies under R1 STOPs.
D-R12L1-7: M (1)'s null-capture form of F1 (R9.11) has no row; pin it with the witness, or name it in J.
D-R12L1-2 and D-R12L1-3 are recorded in the paper as the author says (:5, :555); D-R12L1-1 is paid (an object exists).

Files: this file only (uncommitted, LF, ASCII). Scratch (not deliverables): C:\Users\joeym\AppData\Local\Temp\fable-r12l1-scratch\probe.cjs, base.cjs, base2.cjs. No commit, no push, no DECISIONS.md.
