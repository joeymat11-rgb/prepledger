# REVIEW-NATIVE-LOAD-SPEC-R12-l2 (Fable, independent static reviewer, R9.12 revision 2, 2026-09-25)

VERDICT: REJECT as a build basis for W. The revision-2 paper is honest, ASCII/LF, moves only :5, :155, :158, :500 and appends :533-:577, keeps every pin, pays F2-F5 and D-R12L1-5/6 and correctly STOPs the witness's inputs to the PM (STOP-R912-2). But the ORIGINAL-CUT WITNESS as defined at :556 does not do what :539, :555 and :556 say it does: it folds over a scope the RECORD ITSELF chooses (basis.order.start_ids, coverage, frontier) and accepts ANY of the four hold codes, so a synthetic never-held exit passes W by claiming a cut that omits the Start its ref's own record consumed (G1). That is the R17 shape W was adopted to refuse, it re-opens F3 (exitB skips the moved-base gate and retires pending entries), and it would ship as a green build because no row in the paper exercises a forged cut. W must be tightened (and rowed) before the PM rules STOP-R912-2, or the PM would be ruling on host and generation bytes for a witness that does not separate L12-B5 from a forgery.

Object: C:\Users\joeym\AppData\Local\Temp\earned-astra-96, HEAD d91e4df, rebuild/coach/NATIVE-LOAD-SPEC.md uncommitted, sha256 57e6f32b29af0bbd198bed4fbe7b2a63839f247d35e76c48da8e8f99823bc1a7 (326621 bytes, 577 lines, 0 CR, 0 non-ASCII, so 0 U+2013/U+2014), matching the author's report. git diff --stat d91e4df: 49 insertions, 4 deletions; hunks at :2-8, :152-161, :497-503, :530-577. Base blob extracted with cmd /c git show d91e4df:<path> and compared line by line: of lines 1-532 exactly :5, :155, :158, :500 differ; :159-:166 hash identically before and after (my own convention, sha256 6fa0a582... over the eight lines joined by LF). Inputs read whole: opus55-RULES.txt, the full diff, R12-l1 (28 lines), the precondition list (27 lines), Astra L12 :55-79, FC03 at 189d076 (earned-nlr HEAD 189d0767, file sha256 eb3a7617... = the paper's) at :80-110, :225-290, :296-318, :321-340, :340-455, :483-533, :545-640, :640-730, :795-830, :938-970, plus Select-String for every cited symbol; FC12 :2636-2662 (its round-20 working diff does not touch that region); today-bindings.mjs:512-530. Nothing run, no lock taken, no protected path opened, nothing committed.

CHECKS
(1) L12-B5 traced through FC03 order at 189d076 (accept :646 -> holdIssues :656 -> exitB :664 -> V :666 -> reproducible :699 -> correspondence :701-707 -> DERIVABLE :710 -> moved-base gate :717 -> exit branch :814-825): with M (1)+W and (1b)+W, y2 at base 100 after y1 re-applies has no active hold; (1b) admits y1's ref by arm (ii) (dissolved, provenBefore([y1], byId.get(latest.start)) true by :953, W true because y2's cut over the 102.5 admission holds y1 EFFECT_CONFLICT load_basis by movedBase :230); M (1) sets exitB; FC01 adopts 105 on projectHeld; the exit branch writes w 105, wSets absent (:818); the recorded Undo is the Undo of an APPLIED adoption and restores w null. Under R1 the cut reproduces at 102.5 (held projection) and not at 100 (y1's wAt/topAt/topRun survive projectHeld :251-258), which is what :537 and the R912W table say. End state :536 consistent. MET ON PAPER, but NOT BUILDABLE: W's four inputs are absent at 189d076 (verified: foldNativeLoad :519 takes base as an argument; today-bindings.mjs:519 nativeNullSource is one constant and :526 passes base: args.state; S7 :419 and :360 test equality only; factsAtCut :86-91 filters the current facts). The author says so (STOP-R912-2) and L12-B5 stays refused until the PM rules. Correct and honest.
(2) FC12 R17-ANCHOR-STRUCTURAL :2657 (read: an adopt-baseline naming fx-resp-1 under fx-revision-2, want RECORD_INVALID:base_load, w 100) stays as pinned under wfull (table :565, measured): its GENUINE cut (start_ids [C1,C2,C3], full coverage) holds nothing. The five section-4 probes: forged (authentic :392), wrong-lift (lineage :446), Close ref (class/kind :445), post-anchor (provenBefore :277 against latest.start :423), superseded-hold (dissolved reads superseded issues, :823 keeps them) each refuse base_load with the R1 path stated at :544 (corresponded, all five modes). MET for the probes as written. NOT MET for the probe class the paper omits: G1.
(3) N28-NEVER-HELD-NULL :574 with its -ORDINARY control closes M (1)'s null form: 60/EFFECT_CONFLICT green on 189d076, 65/no issue on M (1) without W (measured), red-first against mutant (viii). MET, subject to G1 (the same forged-cut record with a null capture passes W the same way).
(4) One anchor: :155 (ii), :158 and :500 all read "the Start of the latest consumed completion"; the response anchor of :662-663 stays exitB's for ACTIVE holds only. :548 pins the before-response/after-Start sub-case and traces its R1 path correctly (exitB not set, V = state with w 105, digests differ, corresponded, order test fails). MET (precision note G3).
(5) :156/:165 UNPROVABLE ORDER: a record that fails W is judged as today (exitB false, moved-base gate :717 runs). The re-pin option was measured and rejected (:565-:566, :571), so F3 is paid for the re-pin. NOT MET for a record that passes W by G1: it skips :717 and retires the lift's pending entries SUPERSEDED at :819-821, exactly F3's shape. Nothing is raised by it (target_load = actual loads), but :165 is weakened for that shape.
(6) Grant (a): the paper builds nothing outside it; it names (1), (2), (4) of STOP-R912-2 as host/generation bytes and stops. MET.
(7) Rows: N28-B5-ASTRA (both orders, red outputs named), -NO-UNDO, -R1R2-PARITY (green equality, said so), five probes with R1 path, -WITNESS-BASE, N28-NEVER-HELD-NULL(+ORDINARY), the R17 killer, N27-B39 rows under W (n,o,p,q derived, STOP on difference), mutants (i)-(xi) each with a killer, walk (b'), (B1)/(B2): precise enough for red-first. W itself is NOT precise enough to build (G2). PARTLY MET.
(8) Nothing else moved (measured above). :158's change is a narrowing (anchor) plus a strengthening (W), inside the precondition list's allowance and the PM's one-anchor ruling. ASCII/LF. MET.

FINDINGS
G1 (blocking; :556 W definition, :539, :555, :574). W folds "the generation's operations restricted to the record's own basis.coverage (basis.order start_ids and frontier)" and holds iff that fold ends with an active hold issue (isHold, any of the four codes) naming every r. The scope is the record's own claim. At 189d076 nothing binds a record's basis.order.start_ids to a prefix of the facts order: S6 (:416-417) checks only that each id is an authenticated Start and that the record's OWN consumed Starts are named; coverage is checked by S5 (:412-414) only for the record's own roots and evidence refs; the prefix/coverage binding that exists (sameCut :623-640) runs only on the R1 reproducible path and is not part of W as written. Construction, R17 :2657 shape (fx-resp-1 consumes C2; the synthetic adopt-baseline consumes C3): set basis.order.start_ids = [C1, C3] (S6 passes). W's inner fold: factsAtCut (:86-91) keeps only sessions C1 and C3; fx-resp-1's accept is not reproducible (evidenceChanged :486-487 returns true) so correspondence runs and S3 (:382-387, sessionOf(facts, C2.close) null) returns 'consumes'; dispute (:548-553) pushes RECORD_INVALID field consumes, lift fx-press (owner = lift at :704 for a non-lineage field), refs [fx-resp-1]. That is an active hold issue of the lift naming r, so W holds. Omitting C2's ops from coverage instead gives the same issue through structural() 'consumed reference absent' (:335, :563). Outer fold: (1b) passes (authentic, lineage, dissolved, proven before C3's Start per R12-l1 F1, W true), M (1) sets exitB, FC01 adopts 95 over projectHeld, the exit branch writes w 95 and retires the lift's pending native entries: the never-held exit that :539 says W refuses. The same record with a null capture (N28-NEVER-HELD-NULL re-shaped with start_ids omitting C1) passes M (1)'s W identically. Every row in the paper keeps the record's genuine cut, so this ships green. Remedy candidates (the author chooses, the PM rules): (a) W's inner cut must be a PREFIX of the issuance admission's facts.order.start_ids (with S6 that forces every Start before the record's own consumed Start into the cut) and its coverage must pass sameCut's coverage clause (:632-640) for that cut; and/or (b) W counts only a hold that r EARNED as a valid record in the inner fold (r in the inner fold's `held` book, :718-719, or spent and held), never a RECORD_INVALID of r itself, which a cut choice can manufacture. Either way add row N28-FORGED-CUT (R17 shape and the null shape, start_ids [C1,C3] and a coverage-omission variant; expected RECORD_INVALID base_load, w 100, R1 and R2) and mutant (xii) W at the record's claimed scope.
G2 (precision; :556-:557). "restricted to the record's own basis.coverage (... basis.order start_ids and frontier)" names two different filters (ops by coverage, facts by start_ids/frontier through factsAtCut) in one clause; the builder needs both stated separately, plus: whether the record's own response and issuance ops are in the inner fold (a genuine coverage predates them, so no), whether the inner fold's own dissolved candidates run W (yes by the memo text; termination follows because a record's coverage cannot name itself, but say so), and how "facts as they stood at issuance" is derived (the paper says nothing records it; STOP-R912-2 (4)). The memo per (spend_id, refs) is sound only because W(body, ar) depends on the record's own admission and facts, not on the outer fold; state that too.
G3 (minor; :500 M (1)). "the anchor (1b) reads as byId.get(latest.start)" is correspondence's local (:423). In the accept transition (:646-667) there is no `latest`; say: the consumed root of greatest rank in facts.order.start_ids, as :422-423 computes it.
G4 (minor; :544). projectHeld is :251-258 at 189d076, not :249-252; the substance (w and wSets nulled, nothing else) is right.

R12-l1 F1-F5 AND D-R12L1-4..7
F1: addressed by W in the right place (both (1b) and M (1), both capture forms) but NOT paid while G1 stands. F2: PAID (one anchor at :155, :158, :500; sub-case pinned :548). F3: PAID for the re-pin (measured :565-:571 and rejected); RE-OPENED by G1 for the forged-cut shape. F4: PAID (the sentence was rewritten; moot under one anchor). F5: PAID (:537 "on the correspondence path"). D-R12L1-4: NOT PAID (G1). D-R12L1-5: PAID. D-R12L1-6: PAID (:544 R1 PATH, measured in five modes, builder prints and STOPs). D-R12L1-7: PAID as a row (:574), subject to G1.

NAMED DEBTS
D-R12L2-1: W must not take its fold scope from the record; bind the inner cut to a prefix of the issuance admission's order with sameCut's coverage clause, and/or count only a hold r earned as a valid record (G1). Row N28-FORGED-CUT and mutant (xii) are required before W is built. Until then :539's "It refuses the R17 case" is true only for the genuine-cut R17 record.
D-R12L2-2: W's build definition (G2, G3): one ops filter, one facts filter, the record's own ops excluded, recursion and memo soundness stated, "facts at issuance" defined once STOP-R912-2 (4) is answered.
D-R12L2-3: STOP-R912-2 stands as the author wrote it; the PM's interim call (build R9.11 M (1)-(3) without W, D-R912-NULL-OPEN) leaves N28-NEVER-HELD-NULL red at 65 and L12-B5 refused, both disclosed. If the PM takes it, the ledger line should say the never-held exit is reachable by name until W ships.
D-R12L2-4: the four derived N27-B39 rows (n), (o), (p), (q) under W and the walk (b') were not measured (the paper says so); the builder measures and STOPs on any difference.

Files: this file only (uncommitted, LF, ASCII). Scratch (not deliverables): C:\Users\joeym\AppData\Local\Temp\fable-r12-l2\diff.txt, base.md. No commit, no push, no DECISIONS.md, no node run, no lock.
