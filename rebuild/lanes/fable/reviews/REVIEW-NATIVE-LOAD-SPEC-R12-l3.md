# REVIEW-NATIVE-LOAD-SPEC-R12-l3 (Fable, independent static reviewer, R9.12 revision 3, 2026-09-25)

VERDICT: ACCEPT as the round-20 build basis, with named debts D-R12L3-1..4 (none blocking). The paper implements PM ruling STOP-R912-1/-2 option (C) faithfully: one anchor everywhere, no witness, the never-held exit as RESIDUAL (iv) at :157 with its cost stated, the R17:2657 re-pin marked as a ruled semantics change with the 189d076 output retained, and every row, probe, mutant and walk clause precise enough for red-first. The ruling is not unsafe beyond what it admits, with two disclosures it does not spell out (D-R12L3-1, D-R12L3-3): the residual exit's recorded Undo restores the held projection (w null), not the pre-exit weight, and the ADMISSION GATE closes imported and foreign sources only; a record written into the local log past the guarded host was already outside the model (R9.1 :157), so (iv) widens nothing there.

Object: C:\Users\joeym\AppData\Local\Temp\earned-astra-96 (HEAD d91e4df), rebuild/coach/NATIVE-LOAD-SPEC.md uncommitted. Measured here (PowerShell over the raw bytes): 340335 bytes, 579 LF, 0 CR, 0 non-ASCII (so 0 U+2013/U+2014), sha256 cef5ade55a257e62037438049192a52e068e2f48693b17867ebc25ecfd861605, matching the author's report. git diff d91e4df -- rebuild/coach/NATIVE-LOAD-SPEC.md read whole (89 lines, 129486 chars; word-level diff of :155, :157, :158, :500): 52 insertions, 5 deletions; hunks at :2-8 (:5), :152-161 (:155, :157, :158), :497-503 (:500), :530-579 (appended). :159-:161 are diff context, :162-:166 outside every hunk, so :159-:166 are byte-identical. FC03 and FC12 extracted byte-exact from earned-nlr with cmd git show 189d076:<path> (sha256 eb3a7617... and 2ff69b3c..., the paper's); read FC03 :250-290, :419-450, :519-533, :645-668, :941-995, :1067-1070 and every authority_refs hit; FC12 :2639-2662 and the anchor-holding hits. Astra L12 :59-76, Fable R12-l1 and R12-l2 whole, R912W out-m1b.json whole. Nothing run, no lock taken, no protected path opened, nothing committed.

CHECKS
(1) L12-B5 admitted, R1 = R2, end state as :536. Trace at 189d076 with (1b) and M (1) as :500/:538 state them: at base 100 after the return y1 re-applies and no hold names its response; correspondence's numeric arm (:441-447) runs because C2's capture is numeric; ok(r) = holding (false) || (dissolved (true) && provenBefore([y1], byId.get(latest.start), byId)), true by the exit's own issuance test (:953); M (1) sets exitB in the accept transition (:656-666 region), V = projectHeld (:251-257), FC01 adopts 105, the exit branch writes w 105 wSets absent; the recorded Undo is the Undo of an APPLIED adoption and restores w null. out-m1b.json shows exactly :536's tuples for no-Undo, order (A), order (B) and duplicate y2, R1 and R2 alike, with the R1 path printed (y2 re-evaluated at 102.5, corresponded at 100). MET.
(2) Residual bounded. base_load stays the record's own null projection (:441 refuses any numeric base), target_load the S4-bound actual loads (:428-431), and the record is itself a recorded proposal-response yes; nothing is raised. Host side: FC03 writes authority_refs at exactly two places, :969 (inside the :947 holds.length gate, after the :953-961 after test, holding-record Refs only) and :989 (a Close Ref, check intent only, MISSED-DEBUT claim); basisOf defaults [] at :314. A Close Ref fails arm (ii)'s kind test, so the guarded host cannot emit the never-held shape; the paper's citations :947-969 and :989 are exact. ADMISSION GATE is real at FC03:525-532 (a generation with sourceImports and any native op is refused NATIVE_LOAD_SOURCE_FRONTIER_UNPROVEN before folding); D-L12-ISSUANCE is Astra L12 :71 as cited. MET; see D-R12L3-1 and D-R12L3-3.
(3) Five probes and the POST-ANCHOR sub-case refused under R1 and R2. out-m1b.json: forged (absent op, wrong commitment), Close Ref (C1, C2), post-anchor (device B copy), wrong-lift and superseded-hold y2b all give RECORD_INVALID base_load [y2p or y2b], w 105 from y1, absent from spent, path corresponded under both revisions. The response-before-Start sub-case (:548) is derived (exitB not set under the one anchor, so V = state, digests differ, corresponded, order test fails), stated as pinned and not claimed measured. MET.
(4) One anchor: :155 (ii), :157, :158, :500 and :538 all read the Start of the latest consumed completion ranked by facts.order.start_ids as :422-423; the response anchor of :662-663 is kept for ACTIVE holds only. MET; precision D-R12L3-2.
(5) R17 re-pin: :575 changes :2657's tuple to [name, [], 95] under the ruling's name, keeps the 189d076 output (RECORD_INVALID base_load w 100 under R2; EFFECT_CONFLICT load_basis w 100 under R1) in the row comment, and :539/:550 say it is ruled, not fitted. FC12 :2640-2660 read: :2657 is that case; the want tuple is built in the :2655 loop, so the builder special-cases it. out-m1b.json R17:2657 gives w 95, issues [], inSpent true, card [95,95,95], corresponded, both revisions. MET.
(6) Rows and mutants: N28-B5-ASTRA (red outputs named per order), -NO-UNDO, -R1R2-PARITY (green equality, said so), five probes plus sub-case, N28-NEVER-HELD-NULL (red 60/EFFECT_CONFLICT, ruled 65/no issue, control -ORDINARY), :2657 re-pin, N28-HOST-NO-NEVER-HELD (a)-(e), mutants (i)-(vii), (xi), (xii), walk (b') seed 12035226 with I17, (B1)/(B2). Each has an expected tuple and a killer. MET.
(7) R12-l1: F1 mooted by the ruling (never-held ref is RESIDUAL (iv), disclosed at :157); F2 PAID (one anchor, :548 sub-case); F3 mooted by the ruling and DISCLOSED (:157 says the moved-base test is not run and entries retire SUPERSEDED); F4 PAID (moot under one anchor); F5 PAID (:537). D-R12L1-4 superseded by the ruling plus the host pin; D-R12L1-5 PAID; D-R12L1-6 PAID (:544, measured in five modes); D-R12L1-7 PAID (:574 at the ruled outcome). R12-l2: G1 moot (no W); G2 moot; G3 PAID (:500 computes the rank over body.consumes in the accept transition); G4 PAID (:544 cites :251-257, which is the function's exact extent; l2's :251-258 was one line long). D-R12L2-1/-2 moot; D-R12L2-3 superseded (D-R912-NULL-OPEN is not named; the residual is permanent, D-R12L3-3); D-R12L2-4 met (:577 measure and STOP).
(8) Nothing else moved: only :5, :155, :157, :158, :500 and the appended :533-:579 differ from d91e4df; J, K, L, the five approved copy strings and :159-:166 untouched. ASCII, LF. MET.
(9) Grant (a): FC03 (:443-447, the accept-transition dissolved test, :1068 rebind) and FC12 only; mutant (xii) is a scratch copy of FC03; no FC01, W/engine-capture, E/today, E/progression, L/today-bindings, generation or copy byte. MET.

FINDINGS (none blocking)
H1 (:157, :575; disclosure). A never-held exit's recorded Undo is the Undo of an APPLIED exit, so it restores the held projection (w null, wSets null; :150 R9.11), not the pre-exit weight (60 in N28-NEVER-HELD-NULL, 100 in R17). Nothing is raised, and only a synthetic record reaches it, but :157 should say so, and the builder's retained comment on :2657 and the N28-NEVER-HELD-NULL row should record the native entry state after the exit (out-m1b.json prints q null for R17; the REPAIR rows print [true, SUPERSEDED]).
H2 (:500, :538; precision). "ranked as FC03:422-423" carries that code's fallbacks: rank ?? -1 when facts is null (foldNativeLoad :522 sets facts null when workoutFacts is absent) and a stable sort then pop when consumes has several roots. Say the anchor is undefined-safe the same way in M (1) (an adopt-baseline consumes one root in every row; if consumes is empty or facts null, exitB stays false).
H3 (:157, :5; wording). "the ADMISSION GATE refuses every external native record" is exact for imported and foreign sources (FC03:531-532; today-bindings' project refusal per the comment at :525-530). A record written into the local log by something other than the guarded host is outside the admission model already (R9.1 :157); the ledger line should say the residual rests on that assumption, and cite FC03:525-532.
H4 (:550, :575; wording). "the existing FC12 mutant R17-anchor-holding" is a scratch mutant named only in :2640's title, not a row in FC12; the builder's naming step is right, the noun is not.

NAMED DEBTS
D-R12L3-1: state the never-held exit's Undo image (w null) at :157 and record the entry state in :2657's retained comment and N28-NEVER-HELD-NULL (H1).
D-R12L3-2: M (1)'s anchor fallbacks stated once (H2); builder-level, no rule change.
D-R12L3-3: the PM's ledger line for STOP-R912-1/-2 carries the residual's cost as :157 states it (moved-base test skipped, entries retired SUPERSEDED, Undo to null) and the gate's scope (H3).
D-R12L3-4: H4 wording; and the :2655 want loop must keep every other case's tuple byte-identical when :2657 is special-cased (the paper says so at :575; the builder shows the diff).

Files: this file only (uncommitted, LF, ASCII). Scratch (not deliverables): C:\Users\joeym\AppData\Local\Temp\r912l3-diff.txt, r912l3-fc03.cjs, r912l3-fc12.cjs, r912l3-grep.ps1, r912l3-bytes.ps1. No commit, no push, no DECISIONS.md, no node run, no lock.
