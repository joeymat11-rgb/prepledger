# REVIEW-NATIVE-LOAD-SPEC-R13-l1 (Fable, claude-fable-5-1, 2026-09-25)

Independent spec review of NATIVE-LOAD spec R9.13, worktree C:\Users\joeym\AppData\Local\Temp\earned-astra-96 (branch rebuild/c-native-load-spec, HEAD e092afa, the spec uncommitted).
Reviewed file: rebuild/coach/NATIVE-LOAD-SPEC.md sha256 085ca3745d6382f2c2c2bb5b1513efa83f1b7fafb4b282a462280e5f65d89323, 381267 bytes, 619 lines, 0 CR, 0 non-ASCII (measured by node over the bytes).
Diff read whole: git diff e092afa -- rebuild/coach/NATIVE-LOAD-SPEC.md, +48/-8, one file. In-place hunks touch only :5, :102, :155, :158, :500, :537, :538 and :555; :580 is blank, the N heading is :581; :159-:166 are untouched by any hunk.
Rulings read at DECISIONS:793, :815, :819 from refs/remotes/origin/rebuild/t2-client-core by explicit path. Astra L13 (earned-nlr rebuild/lanes/astra/reviews/NATIVE-LOAD-BUILD-REVIEW-L13.md) read at its B1-B3 and D-L13-* lines; P/reduce-i6.json read (read-only).
Code read from 40eb702 blobs extracted byte-exact with cmd /c git show in earned-nlr (FC03 373c9b1c..., FC01 92a4a0b4..., FC12 ff87313e..., matching the author's read basis): FC03 :224-263, :277-292, :380-460, :565-590, :685-760, :784-860, :923-1101; FC01 :20, :219-250, :499-518, :542-550; E/earn.cjs :61-98; E/writers.cjs :185-200, :266-271, :336-360, :381-384; E/progression.cjs :363-366; E/today.cjs :50-58, :94-111; W/engine-capture.cjs :58-86; L/source-admission.mjs :386-500; W/plan-edit-commands.cjs :44-47; FC12 :799-801, :1314-1316, :2656-2658, :3165-3175, :3696-3703, :3780-3784, :4758, :4777, :4806, :4820.
git diff --stat a42168d 40eb702 over the nine product paths is empty (part C moved test bytes only, as READ BASIS says); the earned-nlr worktree FC12 is modified by the parallel builder, as the author says. Nothing was run except static reads and the byte checks above. No protected-five file was opened.

## VERDICT: ACCEPT WITH NAMED DEBTS (D-R13L1-1..4)

## Findings

### F1 (i) REFS-ARM-EVERY-REVISION: stated exactly, normative, and it does what the ruling asks. ACCEPT, one required builder clause (D-R13L1-1)
- The DEFINITION is FC03's S8 refs arm verbatim: latest and cap :422-426, ar :432, base-null test :441 left in correspondence, numeric-only :442, holding :443, dissolved :448, authentic :388, kind/lift :449-451, every() :452. FORM (B) at :729 makes a record that fails the arm take the existing :732 branch, so R1 and R2 run the same correspondence on the same fold and give the same tuple (RECORD_INVALID, response Refs, base_load when S1-S7 pass, lift as :735-736).
- WORKED EXAMPLE checked against P/reduce-i6.json: both measured columns match field for field (R1 w 95, shown 95, card [95,95,95], Q105 done SUPERSEDED, exit in spent, DEBUT_BASIS_UNPROVEN causality active + RECORD_INVALID base_load [l13-missed-yes] superseded; R2 w 100, shown null, nulls, Q105 DEBUT pending, exit absent, three active issues) and exitRefs = [fx-resp-1, l13-missed-yes]. The TRACE holds: under R1 the active hold's ref l13-missed-yes precedes l13-exit on fx-device-B (:278), so exitB (:693-695) hides the moved base from sameCut and correspondence is skipped; fx-resp-1 is named by no hold (DEBUT_BASIS_UNPROVEN is not in HOLD_CODES :237) and nothing proves it before C4's Start, so the arm fails at :449-452. After (B) R1 = the R2 column.
- A genuine ordered exit is unchanged: the check issues it only when every hold ref is provenBefore the Start (:983, the same function REFS-ARM (c) uses) and fills authority_refs with exactly the active holds' Refs (:997-999); at the exit's place each ref is holding, or dissolved and proven before that same Start. Null captures are vacuous (:442), compensate bodies pass by definition, so N28-*/N27-* rows keep their results. Rows R913-L13-B1-REDUCE (red-first, R1 path printed), -ORDERED (green-kept control, STOP on a pre-existing R1 != R2) and R913-I6-SEEDS (the three seeds, the I6 list, the three R8 bounds) are adequate; mutants r1-r3 are each killed by a named row.
- D-R13L1-1 (required, builder): REFS-ARM MUST be ONE function that correspondence's S8 arm also calls, never a second copy. If the two drift, a record can fail the gate and then pass correspondence, and FC03:786-835 then applies it from its body with no issue at all (present && !reproducible pushes neither a re-evaluation nor ABSENT_APPLIED): exactly the revision-dependent class the ruling closes. The spec's "exactly the refs half" is a paper claim; the round-21 report states the shared function and its line.

### F2 (ii) REVISION-MOVES: stated as ruled; the corrected claims are true. ACCEPT, mechanism carried (D-R13L1-2)
- PRODUCER_REVISION (FC03:1098, ...1d7dbe40) hashes the rebuild/engine files only (:1090-1097), verified by FC12 R2-REVISION (:800); the build report :839 already discloses that :538 had nothing to rebind. The :538 SUPERSEDED marker, the :537 refs-arm marker, :102 and :500 (2) are each correct as written. The ISSUANCE list (exit projection :993-994, refs fill :997-999 and :1018-1019, missedQ :1016-1017, AFTER_STEP_2 :224-225, held-path refusals :977-1008, the step-2 window :1037-1073) matches the ruling's four heads; FOLD-ONLY is correctly bounded. Round 21's (i) and (v) are fold-only ((v) changes heldProjection :259-263 only; :696 and :993 call projectHeld without legacy).
- D-R13L1-2: the rule has no enforcement until N-Q4 is ruled; the only check is the round report's hunk classification. Carried, named, for the PM.

### F3 (iii) D-L13-TYPED-C2: the carried limit is explicit, its cause is cited truly, its row is pinned. ACCEPT
- FC01:229 makes planNow all null when w is null, :247 compares the typed capture against it and refuses PLAN_CHANGED, FC03:1001 sees no adoption offer and :1008 returns holdRefusal (:940-945, EFFECT_CONFLICT load_basis). R913-TYPED-C2-CARRIED (a)/(b)/(c) follows the D-R20C-3 convention and (c) keeps NO TRAP red-first for typed v2.

### F4 (iv) A-LEGACY-VECTOR: exactly the owner ruling, in the import path only; raises nothing above the old app's card; mints no entry. ACCEPT, with D-R13L1-3
- DECISIONS:819 verbatim: "every set up by the same amount, never above the old app's own card", in L/source-admission.mjs. The conversion writes only q.newWSets on an EXISTING pending non-PROPOSED debut/unlock (the :82 test) with newWSets = wSets.map(x => x + (newW - w)), the E/earn.cjs :63/:88/:97 shape and the vector FC01:516-517 requires; under P every element <= newW, and E/today.cjs:98 prescribes newW, so no set is above the old card; no queue item is created, so no debut the old app did not queue can appear. Placement is true: replay state :416, programme() at :438 before the append :476-485 under the ORDERING comment :473-475, families from :486; the replayed state is not a digest input (:446-449); E/writers.cjs:270 lands the vector; W/engine-capture.cjs:82 fits it where :83 refused. Rows -CONVERT, -KINDS, -P, -IDEMPOTENT, -LAND and the four mutants are red-first and cover the writer invariants.
- D-R13L1-3 (minor, N-Q1's twin): P has no lower bound. When q.newW < ex.w (possible for an 'unlock' in principle) a small trailing set shifts below zero; W/engine-capture.cjs:85 then refuses the day (LOAD_MAPPING_REQUIRED) and nothing is raised, but the spec should either add "every shifted element >= 0" to P or name that case beside N-Q1. The precondition P itself is the author's narrowing of the ruling and is correctly surfaced as N-Q1.
- N-Q3 is real: E/writers.cjs:187-199 promotes PROPOSED to DEBUT without newWSets; the recommendation (establish reachability statically, STOP if reachable) is right.

### F5 (v) LEGACY-OVER-NULL: stated as ruled; raises nothing. ACCEPT, with D-R13L1-4
- heldProjection (:259-263) is the only registered projection; projectHeld's legacy-false calls at :696 and :993 are unchanged, so the check keeps FC01:222 LEGACY_PENDING (STRUCTURAL at FC01:20 is debut, unlock, own, reclaim, ladder). E/today.cjs:55/:96-98/:103-110 give the baseline ask and :67 of engine-capture passes on card.w null. The R20-RESTORE-OVER-LEGACY flip (FC12:4806) keeps the retained red; -UNHELD, -CONTROL, FA03 -HOST and the three mutants are adequate. Nothing durable is written; the card carries null loads.
- D-R13L1-4 (required row assertion, then a carried limit): R913-LEGACY-OVER-NULL-UNHELD MUST also assert, after the baseline-ask completion, that the fold's w for that lift is STILL null and no adoption receipt was written. E/writers.cjs:345-347 (DEBUT LOAD ADOPTION) stamps ex.w from the first completed numeric load when ex.w == null; if any rebuild path lets that legacy writer run on such a completion, a weight is adopted without a yes (I1). If the entered-load design already makes it unreachable, the row states where. N-Q2 (no adoption while the legacy entry pends; w stays null; a new standing state outside :163 SCOPE) is honestly named; option (a) for the trial is the right recommendation, and the PM's grant-(h) check should also answer whether Joe's import holds that shape (a pending legacy debut on a null-w lift).

### F6 (vi) (vii) (viii): true restatements. ACCEPT
- P1-P7 are dissolvedExit :569-586 line for line (every :579, commitment :580, kind :582, lift and order :583, not-named :584, anchor kind :578); the null-capture claim (correspondence's arm is numeric-only, S3 not run on the reproduced path) holds. The R913-NULL-REF rows and R913-ANCHOR-WRONG-KIND match Astra B2 and its mutants; the "under M08 R1 applies 60 while R2 refuses" note is consistent with :578 and S3 :381.
- TWO-EXIT-NOT-DISSOLVED follows :844-856 (superseded_by), P6 and :747-756; it matches Astra B3's input description. No earlier law is weakened: it names as a rule what round 20 measured.
- WALK-I3-PARTIAL-ORDER cites FC12:3166-3173 and :1315 truly; it is a test-model rule and asks for no product change.

### F7 No weakening; hygiene
- Every in-place change is a bracketed marker that adds a precondition ((i) at :155, :537), widens an invariant (:158), or marks a false claim false (:538). No hold, exit, anchor, copy or engine grant is relaxed; grants (a) and the :819 owner approval are the only authorities cited, and every "derived" outcome is left to measurement with STOP on difference. Line numbers, byte hygiene and the header's claims (sha256, 619 lines, N-Q1..N-Q4) are all true.

## Named debts
- D-R13L1-1: REFS-ARM as one shared function with correspondence (required in round 21; the report names its line).
- D-R13L1-2: REVISION-MOVES has no mechanism until N-Q4 is ruled (PM).
- D-R13L1-3: P lacks a lower bound; name the negative-shift case or add >= 0 to P (spec, one sentence).
- D-R13L1-4: R913-LEGACY-OVER-NULL-UNHELD must assert w stays null and no legacy adoption receipt (E/writers.cjs:345-347) after the baseline-ask completion; N-Q2 carried as a named limit for the trial (option (a)).

## What I could not do
- No dynamic check: no test, walk, probe or mutant was run (static review only, as briefed). Seed outcomes (R913-I6-SEEDS, the 45 I15, Astra's interval) are the builder's to measure.
