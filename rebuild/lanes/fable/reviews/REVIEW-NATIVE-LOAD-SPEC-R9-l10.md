# REVIEW NATIVE-LOAD-SPEC R9.6 (l10) - reviewer claude-fable-5-1

Input: earned-astra-96 rebuild/coach/NATIVE-LOAD-SPEC.md uncommitted, sha256 b739c2f8...b73e8e (verified), numstat +13/-6 vs a575692. Diff read blind, then Astra build L8. Paper only; effects.cjs:419 re-read in the public scratch copy.

l9 item: paid. ADOPT-BASELINE ANCHOR compares base_load with the record's own load_basis projection (c2) instead of the capture cells, admits a numeric consumed capture only with load_basis.authority_refs naming authenticated hold records of the lift (missed Close = consumed Close), FC01 fills them at issuance, otherwise field base_load. N29 REPLAY covers R2, a non-reproducible cut and cold replay; forged variants ([] refs, another lift's Close, target 105) refuse by the right field. Both writers.cjs citations now name the measured head and the build head with the w/return lines.

Laundering through forged authority_refs: nothing new. Target stays S4-bound to actual loads, so the most a forged hold claim yields is w = what was really lifted, and adopt-observed (numeric capture, base = capture, target = actual) already permits exactly that shape under S8 without any refs; a forged yes itself is the named RESIDUAL behind the admission gate. Wording debt D-R9-ANCHOR: say that for a MISSED DEBUT the fold verifies the miss structurally (the consumed Start captured the lift's selected native entry and its actual loads differ from that target), so the refs are checked against the ops, not trusted; for the other holds the refs must resolve to records of this lift that the fold itself classifies as holding at that cut.

RESTORE/RETIRE (Astra L8 B28): the text is unambiguous - "classified by the RECORD'S OWN SHAPE, never by replay-time state", "replay restores it", "the fold writes NO w/wSets" for RETIRE, and "a later dispute ... cannot turn its RESTORE into a RETIRE, nor the reverse". So a RESTORE that ends at 42.5 after a yes to 40 is a build defect, as the PM says. One interaction the text leaves implicit, debt D-R9-MOVED: :156 UNPROVABLE ORDER speaks of "an accepted effect's recorded base_load" and could be read to hold a RESTORE whose base moved; the build already exempts compensate bodies from movedBase (effects.cjs:419). Add one clause: the movedBase test never applies to kind compensate; a RESTORE replays its prior image by shape over any base.

## Verdict: ACCEPT WITH NAMED DEBTS
D-R9-ANCHOR and D-R9-MOVED, wording only, no behaviour change. No contradiction found between the anchor, DERIVABLE c2/c3, NO TRAP, N29 and the refusal table; no path raises or lowers a weight beyond what was lifted without a yes.
