# REVIEW NATIVE-LOAD-SPEC R9.9 (l11) - reviewer claude-fable-5-1

Input: earned-astra-96 rebuild/coach/NATIVE-LOAD-SPEC.md uncommitted, sha256 503e0d98...2804ab (verified, node crypto over the raw bytes; 184382 bytes, 452 lines, ASCII, LF, final LF), numstat +55/-12 vs HEAD 105cc28 (the R9.8 blob hashes 28c73fa4...4594d9, verified, as the header says). Diff read whole (git diff 105cc28 -- explicit path), then DECISIONS:777-798 in the PM tree. Static reads at c0695e0 by git show with explicit paths: rebuild/engine/native-load.cjs (FC01), today.cjs, writers.cjs, rebuild/m4/workout/native-load-effects.cjs (FC03), and the row names of native-load-options.test.cjs (FC12) and native-load-panel.test.mjs (FA03) by grep. Nothing executed, no protected file, no src/, no lock taken.

## 1. The ruling (DECISIONS:796, option 1) as R9.9 pays it
Traced on F0/N02c (Q105 accepted at w 100), every J case and variant re-derived from the cited lines. (a) 95 on every set: the Close loop (FC03:787-816, Closes at their Start's rank in the workout order, entries !done only) reaches landing() (FC01:560-601) with a capture equal to the target, exact false -> under K the entry goes done/'MISSED', effect 'missed', nothing else written (w 100; wSets, last, wAt absent); the check passes MISSED CLOSE, step 3 (FC01:222) offers adopt-observed 95 with a yes; Undo of Q105 is COMPENSATION_DESCENDANTS by capturedAfter (FC03:135-139, C3's Start captured 105); Undo of the 95 yes before C4 restores the prior image (D-L8F-1), after C4 descendants; no answer leaves w 100 and C4's 95 goes by N09. (b) 100 on every set: PLAN_CHANGED, no offer, [10,9,8] then counts as a top at 100 and a NEW Q105 needs a new yes. (c) unequal: VECTOR_ADOPTION_UNDEFINED. (d) exact with a rep miss: lands. (e) 110: only by a yes. (f) edited: consumed, offer names the edit. (g) partly skipped: PREFIX_UNRESOLVED. (h) a correction re-decides land/miss. Never 105 again: the done entry fails today.cjs:55 and FC01:191-195. No silent raise or lower found on any path: every w write is a landing (yes to Q105 and 105 lifted), an adoption yes, or an Undo restoring the recorded prior. Unchanged as claimed: :159-:166 (HELD PROJECTION, exits, OTHER COMPLETIONS, SCOPE, UNPROVABLE ORDER, DEAD YES, D-L8F-1, the other-hold anchor arm) are not in the diff; :158 loses only the missed hold; :155 loses only the missed-Close arm and gains the MISSED-DEBUT ANCHOR. Replay: the 95 record's accept sits at C3's rank + 0.5 (cutOf FC03:534, events :563), after the miss has folded, so R1 reproduces the queue digest and re-derives the offer; R2 or a non-reproducible cut passes S8 through the anchor (the missed earn is found in spent by close_ref null, which K's :810-813 keeps null on a miss; the "most recently accepted ... proven before that Start" filter picks the right earn when a later Q105' exists); forged base 102.5, target 105 and an R9.8 exit naming [C3 Close] refuse by the named fields. Two-device (ii) is what the build does (events sort by start_ids rank, not delivery). K's row list is complete: every FC12/FA03 row naming missed, missed_target or DEBUT_BASIS_UNPROVEN is in K (FC12:785, 919-923, 2187, 2214, 2222, 2248, 2294, 2305, 2370, 2378, 2386, 2696-2697, 2809-2863, 2926; FA03:942-973); the engine citations verify (FC01:161-165, 191-195, 213-215, 222-234, 340, 381, 504, 521, 560-601; FC03:205-209, 399-432, 511, 787-816, 852-856, 893-894, 946-966), one slip: heldProjection is :226-230, not :227-231. No engine reader keys on a done entry's state (git grep over today/writers/progression/earn/native-load: only PROPOSED and !done are read), so 'MISSED' is inert outside FC01/FC03.

## 2. B1 (blocking): the same debut IS prescribed twice in the LAYOUT case, statically, today
Trace: yes to Q105 (newW 105, no newWSets, sets 3); the athlete edits sets 3 -> 2 (plan-edit CHANGES carries 'sets', plan-edit-commands.cjs:46 per l6) before the debut Start. pickStructural (today.cjs:55) selects the unfinished entry by lift alone, genSession :97-98 puts newW 105 on the 2-set card, the Start captures [105,105]. The Close loop's same(want [105,105,105], got [105,105]) is false (FC03:794), so the Close neither lands nor consumes; nothing retires the entry (movedBase :197-203 reads w, wSets and forks only; :209 has no set-count arm; the entry is not done, so R9.9's consumption never starts); the next card is 105 x 2 again, every check on those Closes is TARGET_QUEUED (FC01:191-195), and the loop runs until sets return to 3 or the athlete undoes Q105 (capturedAfter is false on a [105,105] capture, so that Undo is offered) - neither is "counts as done on its first completed session". Classic has no such loop: writers.cjs:268-270 lands on q alone, whatever the layout. K names the case ("NAMED, NOT DECIDED HERE") and tells the builder to STOP if a probe shows a second prescription; the STOP condition is met on paper now, so it goes to the PM before round 18, not after it. Until ruled, :152 ("never prescribed twice"), :210, :283, :380 ("is not shown again") and J's last column overclaim, and the ruling's fourth clause is not implemented for this case. Two candidate rules, the PM's choice, neither designed here: (i) identify the debut Close by the entry the Start SELECTED (every captured original load equals newW, or the newWSets prefix), so such a Close consumes as MISSED and its check refuses by step 2's set-count clause; (ii) an authenticated set-count edit ordered after the accept retires the entry under :209 (done, SUPERSEDED, spend kept). Either needs a J row, a D1 variant and a red-first FC12 row; (i) touches the FC03 capture match at :794 and FC01:579, (ii) touches :209 and movedBase.

## 3. Named debts (wording; no behaviour change asked)
- D-R9.9-SKIPPED: a normal Close with EVERY original slot of the lift skipped consumes the entry (:152 "a skipped or unresolved original slot") and spends C1/C2 with nothing lifted. The ruling's "whatever was lifted" does not reach nothing lifted, and classic writers.cjs:237 returns early for an entry without a reps array (whether an untouched lift reaches :268 with reps [0,0,0] or is omitted depends on the entry builder in src/, which I did not read), so "R9.9 builds that classic outcome" is unproven for this Close. State the rule as a decision, row it in J (the check is PREFIX_UNRESOLVED [Close Ref]), and put the boundary to the PM.
- D-R9.9-LATER-HOLD: yes to 95 applied, then C1 corrected. Q105's accept becomes BASIS_REPAIR_REQUIRED (FC03:697-700, repair map, not dispute()), C3's Close is skipped (:798), Q105 is pending again and hidden, and the 95 adoption's accept meets FC01:504 TARGET_QUEUED at its point in the fold: refused, not applied, and not in the spend index (TARGET_QUEUED is neither BLOCKING nor HELD_BACK, FC03:16,20), so its Undo names no live spend. The lift shows the baseline ask; exit (a) on Q105 is COMPENSATION_DESCENDANTS, exit (b) recovers; nothing raises. Under R9.8 the same yes was an adopt-baseline exit and survived the correction. :161 covers only an adopt-observed "on a held effect's weight"; J's OTHER-HOLD row should state this fate.
- D-R9.9-TWO-CLOSE-YES: a yes recorded while Close A was the visible missed Close applies as written after Close B (earlier in workout order) arrives and takes the consumption: the queue digest no longer reproduces, and the anchor's structural miss verifies on either capturing Close (DECISIONS:793 keeps the consent); a NEW check on Close A is then PLAN_CHANGED. J (ii) covers checks only; say the recorded yes stands.
- D-R9.9-LEDGER: the header and K's COPY paragraph cite :796 for open Q3/Q5; DECISIONS:798 approved the five NATIVE_LOAD_PROPOSED_COPY strings as at c0695e0 and "ask first". Only the MISSED CLOSE explanation (adoptReason FC01:163-164, "the card said") is new PROPOSED copy; say that.
- Wording: J's UNDO row "while C3 is the newest completion" should be :154's condition, no later Start captured the adopted load (an open C4 Start already refuses); after that Undo a re-check of C3 is SOURCE_OVERLAP [C3 Close Ref] (FC01:228; cancelled spends stay in 4d) - add it; :380 offers only when the same weight was lifted on every set; :155's "at most one native entry ... unresolved" justifies the anchor's pick only together with the "proven before that Start" filter (missed earns stay close_ref null in spent).

## Verdict: REJECT (one item, B1)
Option 1 is implemented as ruled for every case the text reaches: nothing raises or lowers a weight without a yes, no hold survives a miss, the anchor, NO TRAP, dead-yes and D-L8F-1 text is byte-unchanged, replay and two-device cases are defined, and K's row list is complete. But the set-count case re-prescribes the same debut on paper today, so the owner's "never prescribed twice" needs the PM's word (and one rule) before round 18. The debts ride along.
