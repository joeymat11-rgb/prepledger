# REVIEW NATIVE-LOAD-SPEC R9.9 (l12) - reviewer claude-fable-5-1, independent (did not write the draft)

Input: worktree C:\Users\joeym\AppData\Local\Temp\earned-astra-96, branch rebuild/c-native-load-spec, HEAD 105cc28 (R9.8; blob sha256 28c73fa4b96edf427a3d65bc3b76ab4ae3f2ddaf9b426fdbdeb99236414594d9, 154276 bytes, 409 lines, extracted byte-exact with cmd git show and hashed by node crypto). Reviewed: the UNCOMMITTED rebuild/coach/NATIVE-LOAD-SPEC.md as it stood at review time, sha256 ef0f5dd8bc19fa98be6f1a64a70ba98a71d2fe5b6b4f4ccadfe4e61ae3a13e52 (220637 bytes, 456 lines, ASCII only, LF only, final LF; node crypto over the raw bytes). NOTE: the commission named draft sha256 2eff05d2...8302b7; the working tree now holds a later draft whose header says it answers DECISIONS:802 on top of 2eff05d2 (the K VECTOR CAPTURE STOP resolved as option (A)). This review judges the ef0f5dd8 bytes. Diff read WHOLE: git diff -U3 HEAD -- rebuild/coach/NATIVE-LOAD-SPEC.md (148 lines, +60/-13, one file). Line-by-line node comparison against the HEAD blob: exactly 13 lines differ inside the old range, :5, :123, :127, :152, :155, :158, :206, :210, :283, :320, :380, :397, :399 (the header's list plus :5 itself and :397, which the header discloses); lines :159-:166 are byte-identical to R9.8 (verified by equality of each of the eight lines); the rest is appended after old line 409 (blank :410, sections J :411-:433 and K :434-:456). Ledger read: DECISIONS:777-:802 whole in the PM tree, :801 and :802 verbatim. Prior review REVIEW-NATIVE-LOAD-SPEC-R9-l11.md read whole. Static reads at c0695e0 by git show with explicit paths only: rebuild/m4/workout/engine-capture.cjs :61-95 and :8, rebuild/engine/native-load.cjs (FC01) :161-165, :190-191, :205-235, :340, :381, :504, :521, :556-601, rebuild/m4/workout/native-load-effects.cjs (FC03) :16, :20, :23, :110-139, :206-209, :227, :245, :399-433, :561-568, :761-816, :893-894, :922, :946-947, :992, rebuild/engine/performed.cjs (skipped slots :18, :61), rebuild/engine/today.cjs :55, :97-98, :128-129, rebuild/engine/writers.cjs :268-275, rebuild/m4/workout/plan-edit-commands.cjs :46, plan-edit-model.cjs :334, :347. Nothing executed, no test run, no lock taken, no protected-five file, no src/, no private data. Disclosed slip: one scratch node script in %TEMP% was written with Set-Content (ASCII); it hashed nothing and no cited hash came from it (hashes came from node crypto over bytes extracted by cmd git show or read from the working tree).

## 1. l11's blocking item and debts: all paid or ruled
- B1 LAYOUT (blocking in l11): RULED by DECISIONS:801 (1) and written as the SELECTED ENTRY rule at :152, :155, :210, :283, :320, :380, H11 :399, J (j)-(l), K LAYOUT (six builder changes, each red-first, plus MUTANTS). Re-traced statically (section 2, T1-T3): the set-count loop l11 found (FC03:794 same(want,got) false on [105,105] vs [105,105,105], Q pending, 105 x 2 prescribed again) is closed by making :794, FC01:579 and capturedAfter :137 use the SELECTED ENTRY test. Classic parity claim verified: writers.cjs:268-270 lands on q alone (q.done, ESTABLISH, ex.w = q.newW) whatever the layout. PAID.
- D-R9.9-SKIPPED: RULED DECISIONS:801 (1); rowed J (i), D1 N29 "every set skipped", K N29-ALL-SKIPPED; check outcome PREFIX_UNRESOLVED verified against FC01:222-223 (line.positions 0 on an all-skipped entry, !complete). performed.cjs:61 accepts skipped slots, so landing() :570-574 reaches originals and K's exact is false: consumed as MISSED. PAID.
- D-R9.9-LATER-HOLD: J OTHER-HOLD, LATER HOLD paragraph states the fate (TARGET_QUEUED at FC01:504 at the record's point, no live spend, exit (b) recovers); K N29-LATER-HOLD. PAID.
- D-R9.9-TWO-CLOSE-YES: :155 TWO CLOSES and J SECOND DEVICE (ii): the recorded yes stands; the landed-first variant derived and named D-R9.9-TWO-CLOSE-LAND for the PM (nothing raises). PAID as wording; the derived question stays a named debt (carried below).
- D-R9.9-LEDGER: header, H9 :397, H11 :399 and K COPY cite DECISIONS:798 (five Undo strings approved as at c0695e0, adoption asks first); only the MISSED CLOSE adoptReason clause (FC01:161-165, "the card said") is new PROPOSED copy. PAID.
- Wording items: J UNDO row now uses :154's condition (no later Start captured the adopted load) and adds the SOURCE_OVERLAP re-check (verified: FC01:226-228 is inside the adoption branch, so after the Undo, w 100, C3 at 95 takes that branch and its root is in the tombstoned spend); :380 offers only for one weight on every set; :155 justifies the pick by the two filters. PAID.
- l11's own slip corrected by the author: heldProjection is FC03:227 (function heldProjection(fold) at :227), so K's :227-231 stands; l11's :226-230 was wrong.

## 2. Judged against DECISIONS:796, :801 (1) and :802, exactly
:796 = spec :399 option 1: "a debut counts as done on its first completed session whatever was lifted, and a lower weight lifted next time is then offered to be set. Either way no weight is raised or lowered without a yes and the debut is never prescribed twice." :801 (1): first completed session whose card was generated from the debut entry consumes it whatever its layout; scalar lands iff every performed set is at the target load (a set-count edit alone does not stop it); anything else MISSED, nothing written, equal load offered with a yes; vector at a changed layout MISSED; all-skipped consumes as MISSED, nothing written, no offer. :802: vector capture option (A), no engine-capture change in this seal, the :79-82 refusal stays exactly as today with or without the entry, vector debut pending on no captured card and never prescribed twice, D-VECTOR-SETS to Joe; author readings R1 (a skipped or unresolved original slot never lands) and R2 (an equal higher load is only offered) accepted. Each clause is found in :152 in the ruling's own words and again in :210, :283, :320, :380, :399, J and K. Traces, fixture F0/N02c (w 100, inc 5, sets 3, Q105 accepted, no wSets), every value re-derived from the cited c0695e0 lines plus K's stated changes:
- T1 scalar, fewer sets, lands (J (j)): sets 3 -> 2 by plan edit (CHANGES carries 'sets', plan-edit-commands.cjs:46; put writes sets alone, plan-edit-model.cjs:334, w:null projection :347 never sets w). today.cjs:55 selects the unfinished entry, :97-98 puts newW 105 on the card, :128-129 size it to e.sets = 2; engine-capture.cjs :69 selects the one entry, :81 maps card.w on both slots -> capture [105,105], :82 passes (length 2 = card.tgt.length). Close at 105 x 2: FC03:794 SELECTED ENTRY (every cell = newW) passes, :798 not held, :803 provenBefore; landing(): K's exact (originals.length = capture.length = 2, both performed at 105 lb) -> q done/ESTABLISH, ex.w 105, wAt, last [8,7], wSets untouched (:594-599 as kept). Next card 105 x 2, isDebutNow false. Undo of Q105: COMPENSATION_DESCENDANTS (capturedAfter with the SELECTED test; also landed). No second prescription.
- T2 scalar, more sets, mixed (K N29-LAYOUT-UP): sets 3 -> 4, capture [105 x4], performed 105,105,105,100: SELECTED passes, exact false -> MISSED, nothing written, w 100. Check: MISSED CLOSE step 2 compares the capture with newW on 4 slots (passes), :214 set-count 4 = ex.sets 4; step 3 (FC01:222) adoption branch, :224 unequal -> VECTOR_ADOPTION_UNDEFINED [Close Ref]; no offer, no raise, no lower. Next card 100 x 4, never 105.
- T3 scalar, set count edited AFTER the Start and BEFORE the Close (not rowed in J; see debt D-L12-2): capture [105,105,105] at sets 3, plan now sets 2, Close at 95 x 3. Close loop: SELECTED passes (capture against q, not the plan) -> MISSED, consumed, nothing written. Check: FC01:214 originals.length 3 != ex.sets 2 -> PLAN_CHANGED, so the equal load 95 is NOT offered on this Close; the athlete lifts 95 on the next 100 x 2 card and N09 offers it. Consistent with :801's consumption and with step 2's pre-existing set-count rule; :801's "offered" clause is not met on this one Close. Paper-only gap, named below.
- T4 all skipped (J (i)): the Close carries a typed entry whose slots are 'skipped' (performed.cjs:18, :61); captureOf :114 reads the Start's cells -> [105,105,105]; SELECTED passes; landing() :570-574 accepts the entry (normal Close), K's exact is false -> MISSED, nothing lifted, nothing written, C1/C2 stay spent. Check: :216 does not fire (skipped is not 'unresolved'), :222 line.positions 0 -> adoption branch, :223 !complete -> PREFIX_UNRESOLVED [Close Ref]; nothing offered. Never 105 again (today.cjs:55 !q.done). Exactly :801's all-skipped clause.
- T5 mixed loads [100,95,95] (J (c)): MISSED; check :224 -> VECTOR_ADOPTION_UNDEFINED; nothing offered, w 100. Established vector lift (N11) missed at [100,95] (equal to wSets): MISSED CLOSE passes step 2, step 3 performedLoadMatches true -> earn branch -> K's PLAN_CHANGED before :234; at other loads :224 Array.isArray(ex.wSets) -> VECTOR_ADOPTION_UNDEFINED. As J states.
- T6 two devices (J SECOND DEVICE): (i) unproven order: FC03:803 provenBefore false -> DEBUT_BASIS_UNPROVEN causality, Q pending, w 100 (R8 rule, :165, unchanged). (ii) two proven Starts both capturing 105: events sort by workout rank (:561-568), the first Close meets q !done at :789 and consumes; the second finds no undone entry and is an ordinary Close: with the first missed, its check compares capture 105 with planNow 100 -> PLAN_CHANGED. The landed-first variant of the OTHER Close's check is not stated (debt D-L12-3). A yes recorded on the other Close stands (:155 TWO CLOSES; the verification reads accepted records, not native_load_missed_by). D-R9.9-TWO-CLOSE-LAND named for the PM.
- T7 corrections after a yes: FLIP-YES (A2): C3 lands after the correction (Q ESTABLISH, w 105 before the 95 record's fold point); the 95 record's claim verifies through the "landed by the claimed Close itself" arm; DERIVABLE passes (base 100 != target 95); at its point it meets the moved base (:165) and changed evidence (:166) rules -> held with spend kept, never RECORD_INVALID. Reverse (K N29-FLIP): a landed C3 corrected to 95 is re-decided MISSED at the next projection, w returns to 100 with no yes, and a C4 trained on the 105 card then refuses PLAN_CHANGED. Never records a weight that was not lifted; but it lowers the projected w without a yes (debt D-L12-1, PM word).
- T8 Undo: Undo of Q105 after a miss -> COMPENSATION_DESCENDANTS (capturedAfter :135-139 with the SELECTED test; C3's Start captured it); Undo of the lower-weight yes before any Start captured 95 -> the whole prior image (D-L8F-1, :156 unchanged), Q stays MISSED, C3 stays spent, re-check of C3 -> SOURCE_OVERLAP [C3 Close Ref] (FC01:228 in the adoption branch since w is 100 again); after a C4 Start captured 95 -> COMPENSATION_DESCENDANTS even while C4 is open. Undo of Q105 recorded on a device that had not seen C3, unordered against C3's Start: not applied. No path writes w except a landing (yes + lifted), an adoption yes, or an Undo restoring the recorded prior.
- T9 pre-FC16: a native entry exists only under the seal that ships FC16 with FC01/FC03, so every debut Close is FC16-captured; the MISSED CLOSE's earn branch refuses PLAN_CHANGED before any window rule and its adoption branch is never window-bound (:127 unchanged in that clause); pre-FC16 C1/C2 never earn (D-R9-2), so no Q105 arises from them.
- T10 vector day-capture mapping (J (l), K VECTOR CAPTURE): engine-capture.cjs at c0695e0: :69 selects the unfinished debut/unlock entry only when card.isDebutNow; :79 with q.newWSets defined requires q.newW === card.w (true, today.cjs:98) and takes loads = q.newWSets; :80 with original.wSets takes original.wSets; :82 refuses ENGINE_CAPTURE_LOAD_MAPPING_REQUIRED when loads.length !== card.tgt.length, in either direction (2 -> 3 and 2 -> 1 alike); fail (:8) throws, so prepare aborts for the whole day (every lift). So after a set-count edit a vector lift's day refuses with the entry pending (:79,82) and without any entry (:80,82): pre-existing, exactly as :802 records. The entry stays pending (TARGET_QUEUED, Undo offered since no Start captured it), unprescribed, and captured again only when sets returns to the vector's length. :801's vector MISSED clause is written and has no reachable capture while D-VECTOR-SETS stands; N29-VECTOR-LAYOUT pins the refusal with and without the entry and is red-first by the three VECTOR CAPTURE MUTANTS (scratch copies only). Options (B)/(C) are kept for the record and not built. Correct under :802.
- T11 A1 anchor control: the claim is explicit and keyed: FC01 judges a MISSED CLOSE only with the fold's mark (state.queue entry done MISSED with native_load_missed_by === cur.close) AND load_basis.authority_refs exactly [closeRef], which FC03 fills at :922 on the check path; with the mark and no claim step 2 compares with planNow and refuses PLAN_CHANGED, so no unclaimed offer is issued on a missed Close. At replay an adopt-observed with [] is anchored to the capture (FC03:429 unchanged), so C5 at 100 on the ordinary 105 card (w 105 by C4's yes) applies anchored to 105 and never to Q105's base. A claim naming any other ref refuses base_load. Verified pick: the most recently accepted uncancelled earn of this lift, unlanded or landed by the claimed Close, passing SELECTED ENTRY against the consumed Start's capture and proven before that Start; "most recently accepted" plus "proven before that Start" select the captured entry when an earlier missed Q105 (close_ref null in spent, K :810-813) and a later Q105' both exist. A forged base 102.5, target 105, the R9.8-shaped adopt-baseline naming the Close, the claim dropped, the claim naming the Start or another Close: each refuses by the named field. A2 as T7.
- Never prescribed twice, every layout: scalar entries are consumed by the first normal proven Close of a card generated from them at any set count; vector entries at a changed count are on no captured card (day refused) and at their own count are consumed. Residual re-prescription exists only where :801's "completed session whose card was generated from the debut entry" does not apply: a Close that is not normal, an unproven order (T6 (i), R8 :165), a disputed or held spend (:798, hidden by :159), or a wrong capture (typed slot's prescribed_load unlike the Start cell, FC01:580). All pre-existing, fail-closed, stated at :152 and :210.
- No raise or lower without a yes: every w write traced is a landing (the accepted target, lifted on every performed set), an adoption yes, an Undo restoring a recorded prior, or exit (b)'s yes; the one w movement without a yes is T7's un-landing back to the prior working weight after the athlete's own correction (D-L12-1).
- Unchanged as claimed: :159-:166 byte-identical (HELD PROJECTION, EXIT (a) incl. DEAD YES, EXIT (b), OTHER COMPLETIONS, SCOPE, UNPROVABLE ORDER, later-correction rules); :158 loses only the missed hold; :155 loses only the missed-Close arm of the adopt-baseline anchor and gains MISSED-DEBUT ANCHOR and TWO CLOSES; :156 D-L8F-1 text untouched; the other-hold anchor rule (proposal-response refs named by an active hold) keeps its meaning; FC03:16/20 classes unchanged.
- K's engine and row citations re-verified at c0695e0: FC01 :161-165, :190-191, :207-215 (missedExit :213), :222-234, :340, :381, :504, :521, :560-601 (exact :579-581, missed :586-590); FC03 :23 same, :110-139, :206-209, :227, :245, :399-432 (entries[0] pick :417-419, close arm :422-423, proposal-response arm :424-425, :429 adopt-observed cap equality), :568, :787-816 (:794, :798, :803, :812, :814-815), :893-894, :922, :946-947, :992; engine-capture :69-82, :91 (window_hi). All match K's descriptions.

## 3. Named debts (none blocks; each is a wording row or a PM word, no engine byte beyond K)
- D-L12-1 UN-LANDING BY CORRECTION (PM word wanted): :152 "Land versus miss is a projection of the current facts at every fold: a later correction of that Close's sets re-decides it" and K N29-FLIP make a landed debut un-land when the athlete later corrects a debut set below the target: w returns from 105 to the prior 100 with no yes, wAt/last/native_load_authority revert, and any C4 trained on the 105 card refuses PLAN_CHANGED. It never records a weight that was not lifted (I1) and the reverse correction lands again, but it is the one w movement without a yes under :796, and it is asymmetric with :166, where a correction after a yes to the EARN's evidence is BASIS_REPAIR_REQUIRED with consent kept, never silent. R9.8 kept an edited debut pending (DEBUT_BASIS_UNPROVEN completion). Ask the PM to confirm the projection rule (recommended: keep it, it is fail-safe and the prior weight was consented) or to route a post-landing correction to a hold; state the chosen rule in one sentence at :152 and in J (h).
- D-L12-2 SET COUNT EDITED BETWEEN START AND CLOSE (row): T3 is consumed (lands at the captured count when every performed slot is at the target; else MISSED) but its check refuses PLAN_CHANGED by FC01:214's set-count clause, so :801's "equal load offered with a yes" is not met on that Close and the offer arrives only through N09 on the next card. Add the variant to J (k) and D1 N29 (LAYOUT-MID) with a red-first FC12 row; K's landing() change already covers the landing side ("last at the captured count"). No rule change.
- D-L12-3 SECOND DEVICE, LANDED-FIRST, THE OTHER CLOSE (row): J (ii) states the other debut Close's check only when the first Close missed (PLAN_CHANGED). When the first landed (w 105) the other Close's capture 105 equals planNow, so its check reaches step 3: at 105 on every set it is the earn branch (a completion whose Start precedes the landing Close in order but whose Close follows it; whether it is a top in the new tenure or falls outside it, :206 "fall outside the new tenure", must be stated), at another equal load an ordinary adopt-observed with authority_refs [] anchored to its capture 105, at unequal loads VECTOR_ADOPTION_UNDEFINED. State the three outcomes in J (ii) and add them to N29-DEVICE; nothing raises without a yes on any of them.
- D-R9.9-TWO-CLOSE-LAND (carried, author-named): a genuine yes on Close A refused RECORD_INVALID base_load and holding the lift after Close B landed first; whether a yes lost to the race should hold the lift is for the PM. Named, not changed.
- D-VECTOR-SETS (carried, ruled :802): owner question to Joe as K and :380 put it. The vector MISSED clause of :801 stays unreachable while it stands; a per-set-weight lift with an edited set count refuses its whole day, every lift included. :380 tells Joe so in plain words.
- K "OUTSIDE R9.9" (1) and (2) (carried to the PM, author-named): :150 "copies target scalar/vector into w/wSets" versus the build's w-only write for an equal-load adoption (FC01:521); and the typed-path PLAN_CHANGED (FC01:214) versus host-v1 exit (b) for a numeric-card completion proven after every holding record. Neither is missed-debut semantics.
- Wording (trivial): header "lines 1-410 keep their numbers": R9.8 has 409 lines; :410 is the new blank line before J. The header cannot carry this draft's own hash; the round report should record ef0f5dd8 as the R9.9 bytes reviewed by l12 and 2eff05d2 as the draft :802 ruled on.

## 4. Copy and scope
- Copy: the only new PROPOSED string is the MISSED CLOSE adoptReason clause (FC01:161-165, "the card said" names the captured debut and the sentence names the kept working weight); K says it is not one of the five approved strings and is not approved. The :380 owner paragraph is spec text to Joe, not product copy. The :798 five strings are cited exactly, no other string claimed approved. Correct.
- Scope: every engine change K names is FC01/FC03 and their tests (FC12/FA03), within DECISIONS:796 (c); no W/engine-capture.cjs byte beyond the granted hunks; no protected-five content anywhere in the draft; no src/ read (the author states the all-skipped classic outcome as unproven at writers.cjs:237 and rests on the PM's ruling, correctly). The held projection, NO TRAP, exits, DEAD YES and D-L8F-1 are untouched.

## Verdict: ACCEPT WITH NAMED DEBTS
Option 1 as DECISIONS:796, :801 (1) and :802 rule it is written in the rulings' own words and pays every l11 item: the SELECTED ENTRY rule closes l11 B1 on paper and in K's red-first builder changes; all-skipped is consumed with nothing written and no offer; a scalar target lands at any set count iff every performed original slot is at the target; a vector target at a changed count is on no captured card (the pre-existing :79-82 refusal, unchanged per :802 (A), D-VECTOR-SETS to Joe); the missed-debut anchor is an explicit verified claim (A1) that admits the landed-by-itself arm (A2); no hold survives a miss; :159-:166 are byte-identical; every citation re-verified at c0695e0. Blocking items: none. Debts D-L12-1 (PM word on un-landing by correction), D-L12-2 and D-L12-3 (rows) ride to round 18 with the carried D-R9.9-TWO-CLOSE-LAND, D-VECTOR-SETS and the two OUTSIDE items.
