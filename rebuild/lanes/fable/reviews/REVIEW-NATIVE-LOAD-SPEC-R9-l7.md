# REVIEW NATIVE-LOAD-SPEC R9.2 (l7) - reviewer claude-fable-5-1

Input: earned-astra-96 rebuild/coach/NATIVE-LOAD-SPEC.md uncommitted, sha256 a41c8793...3771b0 (verified), numstat +26/-9 vs b849508 (verified). Diff read blind, then Astra R9 L2 and DECISIONS:794 (remote ref, explicit path). Executed one probe on the 12 public factories (r92-probe.cjs, build-0e4fc74 scratch copy, runtime lock held, invented lift); static reads of native-load.cjs:107-129, native-load-effects.cjs:215-236, engine-capture.cjs:22-25/:64/:91, today-bindings.mjs:528-545/:599-610.

## Rule 1: held lift projects w/wSets null -> baseline-ask card, day never refused
Holds at the engine, executed: genSession on {w:null,wSets:null} gives baselineAsk true, tgt [0,0,0], the DEBUT note; engine-capture.cjs:64 then nulls the loads and :23 emits the not_prescribed cell; a native queue entry left beside it changes nothing on the card. Nothing durable is written by a projection. BUT the built FC08 does the opposite: heldProjection (today-bindings.mjs:606) sets quarantined:true, which exActive removes from the card (executed: the lift is ABSENT), :607 drops its queue entries, and :541-543 refuse the day when every lift on it is held (D-B2-1). The spec never says this projection is replaced; the FC08 row (:222) and :164/:168 still describe the old one. As the paper stands, exit (b) is unreachable in the build and N27's "day not refused" contradicts a projection the same spec cites. Gap G1.

## Rule 2: the projected null is not a step-2 plan change
Holds for completions captured on the held card (not_prescribed against null: nothing changed). For a completion captured at 100 BEFORE the hold, step 2 is told not to refuse, TARGET_QUEUED blocks only earns, and step 3 then sees actual 100 against null - an adopt-baseline offer - while exit (b) is limited to completions "ordered after every holding record". The refusal for that pre-hold completion is unnamed (gap G3, wording). Adopting it would not raise anything (it is what was lifted, with a fresh yes), so no trap and no guard lowered.

## Rule 3: TARGET_QUEUED blocks earns only; an accepted adoption retires held entries
Holds. The held earn cannot land (effects.cjs:527 skips held spends), so its 105 is never written without the adoption's own yes; the adoption is a later authenticated plan op and supersedes under :156; spends and holding records stay. Non-held lifts keep the old full TARGET_QUEUED. Raise without a yes: none. Lower the guard: the DEBUT card is the existing baseline semantics (targets 0, no advised load), not a weakening.

## RETIRE/RESTORE by the record's own shape
Verified against :352-357: RETIRE issues target = current load = its own base_load; RESTORE issues target = prior image, which differs from its own base because adoption needs base != target. Collision under rule 1: on a HELD lift FC01 evaluates the projected state, so a compensation's own base_load is all null (baseLoad(ex) :128 with w null). A disputed applied adopt-baseline (BASIS_REPAIR_REQUIRED, no descendants, Undo offered) issues target = prior image = all null == own base all null, so the shape rule classifies it RETIRE: "the fold writes NO w/wSets", and the adopted 60 stays applied after an Undo the athlete answered yes to. adopt-observed escapes (prior 95 != null). Gap G2, fail-open in one shape: the spec must have FC01 record base_load from the fold's applied base, not the held projection (or add an explicit kind marker to the compensation body), and N27 needs this row.

## Kind-aware correspondence (c1-c3)
Holds against the wire shape: load_basis carries sets/w/inc/steps/wSets wrappers (:232-233), planVector takes wSets[i] when array and non-null else w (:109), loadOf maps null/number/string exactly as (c2) says (:115), fieldImage wrappers (:124). Present-null wSets repeats the scalar and earn's candidate test is Array.isArray, both stated. Genuine null and absent baselines pass (c3); Astra's B-R9-1 remainder and B-R9-5 are paid.

## Verdict: REJECT (two narrow items)
G1: name the FC08 change - heldProjection projects w/wSets null instead of quarantined:true, :541-543 all-held day refusal removed, queue-entry drop kept as projection only - and update the FC08 row, :164/:168 and the D-B2-1 note; without it N27 cannot be built. G2: the shape rule misclassifies a held adopt-baseline restore as RETIRE and leaves the adopted load applied after Undo. G3 (wording): name the refusal for a pre-hold completion checked on a held lift. Everything else asked in this round holds; my l6 C1/C2 and Astra L2 are paid.
