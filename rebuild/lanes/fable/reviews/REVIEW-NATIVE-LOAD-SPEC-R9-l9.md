# REVIEW NATIVE-LOAD-SPEC R9.5 (l9) - reviewer claude-fable-5-1

Input: earned-astra-96 rebuild/coach/NATIVE-LOAD-SPEC.md uncommitted, sha256 e73e4f57...8c518b (verified), numstat +13/-6 vs a575692 (verified). Diff read blind, then REVIEW-NATIVE-LOAD-BUILD-FRESH-l1. Paper only; static reads of writers.cjs:250-300/:334-362 and today.cjs:53-98 in the build-0e4fc74 scratch copy (public engine files, nothing executed).

1. Loop-free: yes. Miss -> hold (DEBUT_BASIS_UNPROVEN missed_target) -> entry hidden, so 105 is never re-prescribed -> exit is adopt-baseline of the missed completion's actual loads or of any later baseline-ask training; a yes marks Q done/SUPERSEDED (spend kept), so it cannot land later; a later 105 can only come from a fresh earn with fresh consumes. No answer leaves the baseline ask and nothing written.
2. No move without a yes: holds. Applied w stays 100 through the hold (N29); 95/100 is written only by the adoption's yes; the hidden entry cannot land (:527 skips held spends); the classic auto-follow (:356) has no native counterpart.
3. NO TRAP / COMPENSATION_DESCENDANTS: consistent. Undo of Q105 stays refused (C3 is a later row, :344); the exit is the missed completion itself, carved as the one named exception to OTHER COMPLETIONS, plus later baseline training; the hold list in :158 names it. One CONTRADICTION, blocking: the exit record is an adopt-baseline whose consumed Start captured NUMERIC 105 cells. S8 (:155) admits a numeric capture only for earn/adopt-observed and says adopt-baseline's base is all null against not_prescribed cells; DERIVABLE (c3) wants w null/vector null. Once the record is not re-evaluated (revision absent after any seal, or cut not reproducible) S8 fails on base_load and the genuine exit record is RECORD_INVALID: the lift is re-held, the athlete adopts again, and every seal repeats it. That breaks N25's promise and the spirit of DECISIONS:793. Fix (one clause): for adopt-baseline S8 compares base_load to the record's own load_basis (null under the held projection) and admits a numeric capture when the basis carries a hold of that lift at that cut; target_load stays anchored to the S4-bound actual loads, so no forgery gains from it. N29 needs an R1/R2 replay row for the exit record.
4. "Exact target with a rep miss still lands": right. :151 requires complete original positions and the exact accepted vector, not reps; the classic lands on any reps too (:268-275). The control [8,7,6] at 105 -> w 105 is consistent with both.
5. Classic reading: correct in substance, citations off. The debut-lands branch is writers.cjs:268-275 (q.done, ESTABLISH, ex.w = q.newW at :270, return at :275, so :345-356 is skipped), not :261-268 (:261-266 is the era standard-retire block). The auto-follow is :345-356 with the w write at :356, not :338-349 (:338-344 is comment). today.cjs:55/:97-98 prescribe the card only while !q.done: correct. H11's two options describe the engines fairly.
Also: SCOPE (:163, VECTOR_ADOPTION_UNDEFINED with [Close Ref], never the hold's code) and S1 lineage (field lift_lineage_id) pay fresh N1/N2 exactly; N30/N31 rows match.

## Verdict: REJECT (one item, section 3)
S8 as written invalidates the R9.5 exit record on any non-re-evaluated replay; one clause plus an N29 replay row repairs it. Fix the two writers.cjs citations while there. Everything else asked holds on paper.
