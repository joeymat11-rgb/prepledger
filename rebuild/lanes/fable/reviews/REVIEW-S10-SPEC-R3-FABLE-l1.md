# REVIEW-S10-SPEC-R3-FABLE-l1 - reviewer Claude Fable 5.1, 2026-09-26

VERDICT: ACCEPT WITH NAMED DEBTS (D-R3F-1, D-R3F-2). Neither debt is in the object; both are in the aid script and in the evidence footprint of run 36214720524.

OBJECT: uncommitted rebuild/lanes/b/tooling/packages/S10.json in C:\Users\joeym\AppData\Local\Temp\earned-s10int at HEAD 9849bc7ffff5543668d3157c997235d108c54e62 (git status: " M" on that path only, nothing else asked). sha256 0f55a704968f89b984c976e3ab07d105f1b1461296c13b7ddf2382ae4fdb7308 = the expected hash. 107310 bytes, LF only, trailing newline, versus 107078 bytes at HEAD.
METHOD: static reads and git reads only; my own read-only node scripts in C:\Users\joeym\AppData\Local\Temp\s10-spec-r3-fable\ (cmp.cjs, ascii.cjs, argv.cjs, skipscan.cjs, ranges.cjs, sim-r4-needles.cjs); no test and no node script run inside the lane; no protected-five path opened (none of the 36 children's argv names one; all argv paths are under rebuild/lanes, rebuild/m3, rebuild/m4, rebuild/engine/test).

## Q1 - exactly which lines moved
git diff -U2 HEAD -- rebuild/lanes/b/tooling/packages/S10.json: 1 file, 3 insertions, 3 deletions, three hunks, nothing else.
- @@ -1900: child ui-pack-pins   "needle": "# pass 121"  ->  "# tests 121"
- @@ -1918: child release-object "needle": "# pass 14"   ->  "# tests 14"
- @@ -2050: notes[0] grows from 1563 to 1793 characters by ONE pure insertion (removed substring is empty), inserted after "s10-engine-files-differential by its terminal line":
  "; ui-pack-pins and release-object keep the S9 precedent's '# tests N' needle, their test counts 121 and 14 from the same table, because a pass count can differ by OS: hosted ubuntu run 36214720524 skips 3 of the 121, DECISIONS:835"
Whole-object check (cmp.cjs): with the two needles and notes[0] masked, the parsed spec is JSON-identical to HEAD; notes[1..9] identical; the only children whose needle differs from HEAD are ui-pack-pins and release-object. Nothing else moved.

## Q2 - S9 needles and argv, byte for byte
git show d7f6540:rebuild/lanes/b/tooling/packages/S9.json (32 children) versus the r3 S10.json (36 children):
- ui-pack-pins: S9 needle "# tests 121", S10 needle "# tests 121", Buffer.equals true, hex 2320746573747320313231; argv identical: --test --test-reporter=tap rebuild/lanes/c/ui-port/pack-pin.test.mjs rebuild/lanes/c/ui-port/approved-pin.test.mjs
- release-object: S9 needle "# tests 14", S10 needle "# tests 14", Buffer.equals true, hex 23207465737473203134; argv identical: --test --test-reporter=tap rebuild/lanes/c/ui-port/release-object.test.mjs
Side observation (not a finding): of the 26 S10 children that also exist in S9, 23 carry the same needle and argv; today-17 (685->726, five gss-annex files added), m4-import (62->63, engine-pins-unprotected added) and sealed-inventory-fence (54->58, same argv) differ as the S10 brief intends.

## Q3 - the needle table, the ubuntu facts, and the other 34 needles
Table C:\Users\joeym\AppData\Local\Temp\s10-t4-needles-e9ff2ca.txt sha256 064ff80ed2cd6d2c7abeb950951065df00814bdbf8d9b8b5ca4f37ccffcdcf3f (matches the 064ff80e cited in notes[0]). 36 data rows, six tab fields each (child, class, exit, tests, fail, needle). Rows: ui-pack-pins PM-RUN-PM-GREEN EXIT=0 "# tests 121" "# fail 0" NEEDLE=# pass 121; release-object PM-RUN-PM-GREEN EXIT=0 "# tests 14" "# fail 0" NEEDLE=# pass 14. So on the PC the tests column is 121 and 14 and pass equals tests (no skip on win32).
Hosted run 36214720524 (node %TEMP%\pm7-joblog-seq.cjs, both jobs): ubuntu b-package step 13 observed children 1-19 then FAIL CHILD-NEEDLE-NOT-A-TERMINAL-LINE at the 20th (ui-pack-pins); ubuntu direct step for pack-pin+approved-pin printed # tests 121 / # pass 118 / # fail 0 / # skipped 3; ubuntu release-object printed # tests 14 / # pass 14 / # skipped 0. windows b-package observed all 36 children; windows direct step for the pair printed # tests 121 / # pass 121 / # skipped 0.
WHY 3 and why it is deterministic (static read): pack-pin.test.mjs:2563-2566 loops location in [ancestor, root] x acl in [false, true] with { skip: acl && process.platform !== "win32" ? "Windows list-directory ACL row; ..." : false } = 2 skipped cells off win32; approved-pin.test.mjs:875-877 is the same construct over acl only = 1 cell. 2 + 1 = 3, keyed purely on process.platform, so Linux pass is always 118 and Windows pass is always 121, while node's "# tests N" counts skipped cells and is 121 on both. "# tests N" is therefore the OS-invariant verdict for this pair, and it still proves green because b-package.cjs asserts CHILD-REQUIRED-EXIT-ZERO (line 2823) before the line-start needle match (line 2839, RegExp('^' + needle, 'm')) - node --test exits non-zero on any failure and zero on skips.
The other two conditional constructs in the pair are not OS-keyed: the 455-character-path t.skip (pack-pin:2429, approved-pin:768) fires only when the temp root is too long (neither runner), and the file-link cells (pack-pin:1895-1924, approved-pin:335-362) branch on EPERM but never skip. "# tests N" is immune to both anyway.
ARE ANY OTHER OF THE 36 AT RISK? No, on the evidence available:
- 19 children (today-17 through sealed-inventory-fence) were OBSERVED by b-package on ubuntu at 9849bc7 with their "# pass N" needles, and by windows: pass counts already proven equal on both OS.
- 7 more have both-OS direct-step evidence at the same run with pass = tests and skipped 0 on ubuntu: reference-closure 5, release-object 14, passphrase-normalize 29, w6-local-import 22, epp-proposed-pick 9, d-epp-2-capture 13, today-split-fence 404 (its t.skip at writer-fence.test.mjs:1051 is on an EPERM/EACCES spawn refusal, not OS; ubuntu ran 404/404).
- 10 have only Windows evidence in this run (today-carry 9, f2-land 81, s10-sup-source-carriers 4, s10-sup-inherited-carriers 3, s10-sup-defect-witnesses 3, s10-sup-writers-differential 3, s10-sup-second-gate 3, s10-engine-files-differential terminal line, s10-copy-lock 6). Static scan of every argv file (skipscan.cjs: \bskip\b, .todo, process.platform, win32/darwin/linux, process.env.CI/GITHUB_*/RUNNER_*/OS, os.platform, os.EOL, path.sep, os.type) finds NO platform-conditional skip or todo in any of them: today-carry and the five s10-sup files have no skip construct at all (the writers-differential hit at :134 is a data literal kind: 'skip'); f2-land's only hit is a comment (guard-coverage.test.mjs:761); s10-engine-files-differential.cjs has none and its needle is a full terminal sentence; s10-copy-lock's three { skip: UNDER_TEST !== 'lock' } cells key on process.env.COPY_LOCK_UNDER_TEST (default 'lock', so never skipped unless the PM sets it), and its header states it runs unchanged on both runners.
- S9 precedent agrees: S9 carried '# tests N' for exactly this pair and '# pass N' for every other shared child, and S9 sealed green on both OS.
So the risk is confined to the pair r3 fixes; see D-R3F-2 for the evidence gap on the 10 Windows-only children, which the next ubuntu run at the r3 spec closes by construction (b-package will now reach child 20 and beyond).

## Q4 - the note text is true and ASCII-safe
Claim by claim: (a) "keep the S9 precedent's '# tests N' needle" - true, Q2 byte-equal to d7f6540's S9.json. (b) "their test counts 121 and 14 from the same table" - true, the tests column of 064ff80e reads # tests 121 and # tests 14. (c) "a pass count can differ by OS: hosted ubuntu run 36214720524 skips 3 of the 121" - true, ubuntu direct step # tests 121 / # pass 118 / # skipped 3 (windows 121/121/0), the 3 traced to the platform-keyed ACL cells above. (d) "DECISIONS:835" - rebuild/DECISIONS.md is 834 lines at HEAD, at chain tip 88a6463 and at refs/remotes/origin/rebuild/t2-client-core (line 834 is the T4 re-observe line the note already cites), so :835 is the next line; it does not exist yet and the note is true only once the PM writes it as described. The note is one line (no CR/LF), 230 characters, pure ASCII (straight apostrophes 0x27). The file's 87 non-ASCII characters (U+00B7, U+2013, U+2014, U+2192, U+2026, U+00A7 at lines 12, 1647, 1653, 1659) are quoted ledger-line text carried unchanged from HEAD, identical count and positions before and after r3; r3 adds none.

## Q5 - T6e-static-r4.cjs versus r3
git diff --no-index -U1 r3 r4: 15905 vs 15862 bytes, r4 sha256 926d78e75f0697dd876a0830e60c2011548dff350b62e7830e7646b9db742bd7, r3 197c939e908f921c69e69f98256e2f6804f43fca4d579927b108c6c5c33dcdf5; r4 is pure ASCII with LF only. Hunks: CHAIN 4ea143a... -> 88a6463e3375507c3b052d66165479783089b82c (constant; CHAIN_REF unchanged); spec sha check 82f60c3a -> 0f55a704 (constant + label); four ck labels @4ea143a -> @88a6463 (labels only); and the needle-table check (lines 119-124). Nothing else differs. (r2 vs r3 differed by one line, for the record.)
The re-pointed check reads %TEMP%\s10-t4-needles-e9ff2ca.txt, drops the '#' header, splits each row on tabs, takes a[3] as the tests string and a[5] minus the NEEDLE=(TERMINAL:)? prefix as the pass/terminal needle, and wants a[3] for the set {ui-pack-pins, release-object} and a[5] for the other 34, requiring m.size===36 and an empty diff. Correct on the table's shape: every row has exactly 6 fields (the differential row has empty tests/fail fields and its terminal needle in a[5], no tab inside the needle); the prefix strip matches the r3 logic. Simulated read-only in sim-r4-needles.cjs against the r3 object: 36 rows, 36 children, diff none, PASS true. Message on failure names each mismatch or the row count. Verdict on the check: correct for its purpose; one weakening named as D-R3F-1.

## DEBTS
- D-R3F-1 (aid script, low): r4's needle check dropped r3's explicit s.children.length===36 assertion; m.size===36 plus "every child found in the table" no longer implies a bijection, so a 35-child spec with correct needles would pass that cell (simulated: r4 logic PASS on children.slice(0,35), r3 logic would refuse). Harmless for this object (I count 36 children; the earlier ck only prints the count) and CI's own runner still counts children at the seal; fix is one clause, need(s.children.length===36, ...), if the PM wants r4 as strict as r3.
- D-R3F-2 (evidence, closes itself): at 9849bc7 ten children (today-carry, f2-land, the five s10-sup-*, s10-engine-files-differential, s10-copy-lock) have Windows-only observation because ubuntu's b-package stopped at child 20; static reads find no OS-conditional skip in any of them, so the r3 needles are expected to hold, and the first ubuntu run at the r3 spec (which passes child 20 by construction) is the proof to record with the seal.

## AAR
1. Read the exact bytes and diff them; the three-hunk diff and the empty "removed" substring settled Q1 before any interpretation.
2. Byte-compare needles across specs with Buffer.equals, not eyes; the same for argv.
3. Trace a CI count difference to the source lines that produce it (2 + 1 platform-keyed ACL cells) before calling it OS-invariant.
4. Check the runner's contract (exit-zero before needle) so a weaker-looking needle is shown to still prove green.
5. Simulate an aid script's changed check read-only rather than trusting its label; that is how the dropped count pin surfaced.
6. Keep the ledger claim honest: :835 is verified as the NEXT line, not as existing.
