# EARNED PM HANDOFF - 2026-09-17 (from the Fable 5.1 PM seat to its successor)

You are the new day-to-day PM of the Earned rebuild. The owner is Joe. He has no coding
experience, works from his iPhone, and his Windows PC runs the Claude desktop app with the
Desktop Commander tools you will use. Read this file first, then rebuild/DECISIONS.md lines
416 to 505 (findstr /n), then the files named below. Everything here is on the chain branch.

## 1. The owner's rules (binding, verbatim spirit)
- End EVERY message to Joe with a PROGRESS footer: done · in flight (whose court) · waiting
  on him (exact action) · queued. Never ask him to paste, run, upload or coordinate from
  the phone. Never apologise for long cycles. One attached file, named for its destination,
  last in the message, if he must move one. Ask only decisions worth asking; decide and
  record the rest. Owner rulings count only in his own words, recorded with provenance.
  High-density, conclusion-first answers, ELI15. He likes frequent updates while work is in
  flight (30-minute two-line status while anything runs; wake-ups at most hourly).
- DECISIONS:416-439 process points 1-19 (PM runs lanes; author != reviewer != integrator;
  Fable high effort for the final review of anything on the path to his data and for
  feature-complete gate audits; timebox mechanical problems 20 min; end the turn after you
  dispatch: one judgment, one dispatch, one ledger line, then end the turn; read his queued
  messages within minutes). :455 Today changes after S4 ride reseal children.

## 2. Safety rules (never break)
Never print or expose the GitHub token (GH_TOKEN is read only inside script headers, never
echoed). Never delete data. Do not break the app. Keep the /ledger lockdown intact. No merge
to main, no deploy, no private import/activation, no purchases, and no real history port
without Joe's own words each time. Never read rebuild/conform/private, src/history.js,
ledger/, C:\Users\joeym\EarnedPort\main-050b356\**, C:\Users\joeym\EarnedPort\out\** (the
sealed bundle and the passphrase file), %TEMP%\port-real.log, or the protected soak before
2026-10-05. Author != reviewer != integrator. Never weaken a law, guard or test to go green.
No U+2013/U+2014 in user-facing UI copy. Production health data stays OUT of AI packs;
verdict-only reporting for the private census and for anything about his ledger.
Measurement data never enters a cloud session, an AI pack, a review artifact or the ledger.
Sub-agents never npm install in shared node_modules (:456); junctions only.

## 3. Where everything is
- Repo joeymat11-rgb/prepledger. Chain branch rebuild/t2-client-core (the tip). Ledger:
  rebuild/DECISIONS.md, append-only, one line per event, LF, no CR; at handoff 505 lines.
- PC worktree of the PM: C:\Users\joeym\Documents\Codex\2026-09-04\read-rebuild-t3-brief-md-and\work\t2-client-core-pm
  (detached HEAD at the tip; it carries an ignored junction rebuild\conform\private ->
  %TEMP%\earned-s4\rebuild\conform\private, the private census: never read it).
- Node: C:\Users\joeym\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe
- Lane worktrees under %TEMP%\earned-* (earned-s6 = the S6 seal worktree with the private
  junction and rebuild/conform/engines bundles; earned-diag = the running diagnosis).
  New worktrees need three junctions: node_modules, rebuild\m3\w6\node_modules,
  rebuild\m3\w5\node_modules from %TEMP%\earned-ci\...
- Helpers in %TEMP%: gh-runs.cjs <sha40> (CI status), gh-wf.cjs <workflow.yml> <n>,
  gh-joblog.cmd <runId> "<regex>", gh-joblog-ctx.cjs <runId> "<regex>" <n> (context before
  a match, failed jobs only), earned-ledger-<n>.cjs (the append pattern: assert HEAD sha, no
  CR, exact line count, last-line regex, append one LF line, post-check count), s6-full{1,2,3}.cmd
  and s6-prep.cmd (the FULL run pattern), port-real.cmd + port-verdict.cjs (the port run and
  its verdict-only parser). Write scripts with the write_file tool (mode rewrite); PowerShell
  Set-Content corrupts UTF-8. Never chain && after find /c.
- Commits: git -c user.name="cowork (Earned PM)" -c user.email="joeymat11@gmail.com", with
  trailers Co-Authored-By: <model> <noreply@anthropic.com> and Claude-Session: <your session url>.
  Push HEAD:rebuild/t2-client-core; verify the DECISIONS line count on origin after every push.
- Slice site https://earned-slice.netlify.app auto-deploys on tip pushes touching today/** or
  slice/pwa/** (slice-host.yml). Joe's phone runs it as a Home-Screen app; a full close and
  reopen picks up a new build.

## 4. State at handoff
- M2-S6-TODAY-CHILD is sealed and merged (:501, tip at 02d3219a then ledger lines). Standing
  CI step --package S6; coach ENGINE_REVISION = M2-S6-TODAY-CHILD@5bff018d56223d98. Live on
  the phone: setup-first fresh install, Import screen on Today and Measure (after setup),
  replay families for every writer, pre-import workouts admitted, B-LOM (the day after an
  import opens), food/machine-note saves (:485), the Windows flake fix.
- THE REAL PORT: PC half done (:502-503): port.cjs run of record all six PASS, ORACLE 10/10
  scope FULL, bundle C:\Users\joeym\EarnedPort\out\earned-port-2026-09-17.json (sha256
  17216d9c3039e7a12b699de8ba0e6ec2a25748ca6fa9d163a49ee9df6cbfb1dc, 643364 B) delivered to
  Joe through the chat; the six words sent separately (they are in the PASSPHRASE.txt beside
  the bundle; Joe already has them). PHONE HALF REFUSED (:504): review refused
  LOCAL_SOURCE_PROGRAMME_UNRESOLVED, the route retracted cleanly (reason label
  review-refused), nothing was written. Joe was told not to retry.
- IN FLIGHT at handoff: P3-PORT-REFUSAL-DIAGNOSIS (lane D Opus high + Opus review), branch
  rebuild/d-p3-port-refusal in %TEMP%\earned-diag, deliverable
  rebuild/lanes/d/P3-PORT-REFUSAL-DECISION-BRIEF.md + rebuild/lanes/d/p3-port-refusal/*.test.mjs.
  It ran as sub-agents of the old session, so it STOPPED when that session ended. Check
  git -C %TEMP%\earned-diag log --oneline -5 and status; if the brief is not committed,
  re-dispatch the ticket (its full text is in the old session's ledger line :504 and the
  workflow script; the gist: enumerate every review path that yields
  LOCAL_SOURCE_PROGRAMME_UNRESOLVED in rebuild/m3/w6/local/source-admission.mjs and the F4
  family in rebuild/m4/import/replay-core.cjs; reproduce Joe's path synthetically: fresh
  install on the S5 page, setup on the phone, days of host writes on a live clock, the S6
  page, a synthetic schema-60 bundle carrying the OLD app's programme, the real route;
  bracket one variable at a time; classify (a) owner-decidable rule, (b) product defect,
  (c) runbook gap; recommend). Then judge it: (a) put the choice to Joe plainly; (b) fix via
  lane D + Opus review + Fable final (his data path), sealed files ride S7, undeclared
  files merge on their own CI; (c) fix the runbook. Then the retry: same bundle, same words.
- Pending scheduled ticks of the old session are dead; schedule your own (send_later).

## 5. The seal chain (for S7 and later), as S6 proved it (:465-467, :495-:501)
brief accepted by name -> THEME + BRIEF-BY-SHA + GATE-SUPERSESSION token lines on the tip ->
author cites their sha256 in packages/<ID>.json and re-proposes the artifact through the
runner's own proposed() (a scratch script compiles b-package.cjs as the tooling tests do;
validate with a scratch PENDING review: ENVELOPE PENDING + PUBLIC CI EVIDENCE PASS, delete
the scratch) -> --ci PASS -> Fable final -> PM --full with the private junction ->
REVIEW-PENDING -> receipt line "- <date> · cowork · POSTFIX-ACCEPTANCE <ID> <reviewed
commit40> <artifact path> <sha64> ACCEPTED" -> rebuild/m4/spec/review-<slug>.json {version:1,
status:"ACCEPTED", receipt:{commit,path,line(verbatim text),lineSha256}} -> MERGE the tip
into the reviewed head (never rebase) -> authorized --full -> SEALED RUN RECORDED
receipts/<ID>.json + VERDICT-<ID>.md naming its sha -> coach constant <ID>@<sha16> -> second
--full BYTE-IDENTITY -> CI both OS -> ff-merge. TWO RULES: the standing CI step names the
new package INSIDE the package before proposed() (never at the ff-merge); revision cells
read the sealing window (parent seal while the standing receipt is absent).

## 6. Queue after the port
Edit My Week screens part 2 (brief rebuild/lanes/d2/BRIEF-EDIT-MY-WEEK.md) -> N3-MACROS-ENTRY
(:471, lane C, rides S7) -> P4b coaching memory Option A -> N3-MACROS-ENGINE (engine tier,
science check, Fable final) -> PWA-SPLIT before wide beta (:480). Carries: TODAY-TREE-CUSTODY
widened to rebuild/m3/w6/test (:482/:487/:501), HOST-DAY-AGREEMENT (:485), lane B tooling
(executedClosure computed specifiers, pinned-unexecuted role, child tail carrying the
failing name, rebuild.yml step NAME), P3-P6 minor pins, fakeRoot temp dirs. Open owner
questions still unanswered: Q3 (Dad's training days) and Q7 (trial first vs fix
D16/D30/D45); Dad's trial starts after Joe's two-day trial.

## 7. Dispatch pattern that worked
Workflow scripts with a COMMON block (tools, node path, cmd shell with logs in %TEMP%, privacy
lines, seal facts, suites) and a TICKET block; author (Opus high for anything on the data
path, Sonnet medium for plumbing) -> Opus review -> one fix round with the same reviewer ->
Fable final for the data path. Reviewers commit review files on a detached worktree; the
integrator carries them with git checkout <review sha> -- <file>, rebases undeclared-file
branches onto the tip (sealed-file branches are merged forward, never rebased once
reviewed), pushes, waits for CI on both OS, ff-merges, appends the ledger line.
Windows-only today-child CI reds were a flake (cause fixed in S6); if one recurs, retrigger
with a one-line note in a lane report that is not product-declared.
