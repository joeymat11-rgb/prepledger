# EARNED — D33/D34/D35 import-guard repair brief — ready for independent review

2026-09-06 · ASTRA published proposal, revision3, on `rebuild/m2-import-guards-brief`. The owner's “Approve Batch A and all seven recommended choices” is recorded in integration `402ab9ef2b57d0f0b1d40425e130e228c69f752c`; D33–D35 are approved FIX directions. Cowork accepted revision2@96e298f C1–C6 and confirmed the authorization mechanism below with seven conditions; this final wording still needs its exact recheck before implementation. No product or test changes were made by preparing it.
Verified integration: `3e02e1bf9bf47267449b3a402e42089b49222b6b`. Engine/frozen source bytes inspected through PR37's checkout match that integration. PR37@`01eff99de9ad9a4c8ff152b546f945785704837a` is independently accepted BASELINE infrastructure; `BRIEF-POSTFIX-GATE.md` is accepted, not pending. Original audit is `614e20315b01543d3b7bbc4fa1fe8a5c20bcb690`; frozen `app.jsx@fe516c1` blob `f98671d823f0d8cd83e730cdd930afe5f5e7b628` remains unchanged.

## 0. Start, scope and authority

Build branch: `rebuild/m2-import-guards`, from the current `rebuild/t2-client-core` tip carrying this accepted brief and the recorded owner ruling. The coordinator publishes the branch claim and actual integration SHA before a builder starts; one package/PR, no merge by builder. Pin the real owner and cowork DECISIONS lines by exact line SHA-256 and verify their presence at the declared candidate base. An annotation or this prose is not the runner's authorization receipt.
All45 directions have now been approved, but every implementation remains PENDING until its actual repair is accepted. The successor manifest records all45 actual dispositions and preserves the fixed15 non-D obligations; selectedApprovedFixIds for this package is exactly D33/D34/D35, with requiredIds unchanged. Never call the other42 repaired, waived or M2 closed.
Product changes: only `dataLossGuard`, `isPristineSeed`, and the initial future-version return in `migrate`, all in `rebuild/engine/migrate.cjs`. Keep `recordCounts`, patch bodies/order, reconcilers, merge/writers, oracle shim and every other product declaration unchanged. Pure helpers nested inside those declarations are permitted within their reviewed full source bytes.
To reuse the actual correction reducer, explicitly allow two instance-local late-bound adapters at the migration factory boundary: `_unionCorrLog=(...a)=>E._unionCorrLog(...a)` and `_replayCorrections=(...a)=>E._replayCorrections(...a)`. Their exports already exist in merge.cjs; no copied reference reducer, new export, global cache or clock/ID/storage read. Pin both bindings as named source deltas, alongside the three changed declarations.
Gate work stays under `rebuild/conform/v4/postfix/`: versioned manifest, concrete `laws/import-guards.cjs`, approved expectations, exact legacy-case carrier and necessary PACKAGE runner/tests. PR37 currently deliberately exits2 for PACKAGE; its schema/inventory is scaffolding, not a completed repaired-engine gate. Implement the accepted §2–5 PACKAGE machinery without removing its fail-closed obligations or building unrelated themes. Preserve the original audit/suite/oracle/tools/goldens.
Report: `rebuild/m2/REPORT-M2-IMPORT-GUARDS-ASTRA.md`. Estimate: D33 8h + D34 3h + D35 2h =13 engineering hours; completing/reviewing PACKAGE infrastructure is separate work to estimate in the task receipt, not hidden inside those13h or claimed as measured duration.

## 1. Exact product behavior

### D33 — identified read days, entries and stored set slots

Preserve every existing guard clause, early return and reason order (`migrate.cjs:2320–2354`, frozen:13328–13361): count losses, permanent feed-op presence and missed-day exceptions all remain. Append new reasons after them; never mutate prev or next. A filed correction cannot erase an already-existing unrelated loss reason.
Protect distinct read-day identities `String(r.d)` from object rows with non-null d, matching the existing day-count convention. Every old day must remain; another day does not compensate. Multiple bodies for the same day may still fold under the existing read winner; this package does not fix equal-day value conflicts.
For ordinary persisted session shapes, identity is sessionLog day key + exact entry.id + occupied own index in entry.reps. Occupied means an own array index whose value is neither null nor undefined: holes and their JSON null representation are empty;0 and strings are occupied. Protect each old entry even with no occupied slots, and each occupied slot on that same entry/day. Moving an entry/day, compensating elsewhere or padding with holes does not preserve identities. IDs and payload values are not coerced; normal IDs are nonempty strings.
This protects presence, not a newly invented per-set immutable-ID model: equal occupied-slot counts with different values keep existing payload/merge rules. A shorter reps array is loss even when remaining values are unchanged. Removing a middle element shifts values and loses a terminal occupied index; do not claim to identify which historical set it was.
Also protect each old skipped placement by day and nonempty string `skipped[].id`; skipped records are objects, not strings (frozen:17891,17900–17914,19758,20032). Only a matching final unskip replay that restores the same entry and removes its skipped placement covers removal; a bare restored entry or an unrelated correction does not. Malformed/duplicate skipped bodies use the ambiguous multiset rule below.
Append unique reasons in this order: sorted `readidentity ${JSON.stringify(day)}`, `entryidentity ${JSON.stringify([day,id])}`, `setidentity ${JSON.stringify([day,id,index])}`, `skippedidentity ${JSON.stringify([day,id])}`, then `sessionidentity ${JSON.stringify(day)}`; compare tuple components deterministically, numeric slot order. An absent whole entry emits only its entry reason, not every former slot. These exact strings are reviewed new observable output, never permission to print private data.
Ambiguous duplicate/id-less entries or non-array reps receive no correction exemption. Match each such old complete body by canonical JSON MULTISET containment in next: object-key order is immaterial, arrays/types/property presence remain exact, and every old multiplicity must survive. Unchanged bodies plus additive entries pass; removal or in-place change appends one sessionidentity reason for that day. Apply the same rule to ambiguous skipped bodies. Unsupported non-JSON bodies must match unchanged by descriptor-safe identity/structure or refuse, never stringify a getter or infer safe from a caught exception. Valid unique entries in the same day keep ordinary checks; no fabricated IDs. The locally regenerated private-blob predicate for ambiguous session entries returned only NOT TRIGGERED; future malformed imports remain an explicit conservative compatibility limit.

### D33 — precise filed-correction coverage

Use the SAME next session's complete corrLog, normalized by actual `_unionCorrLog({},nextRec)` and replayed by actual `_replayCorrections` on a deep scratch copy of prevRec with that normalized log. Source: merge.cjs:158–225,238–284 / frozen:13514–13581,13594–13640. Its sort is String(at)+op, not a replacement wall-clock ordering algorithm.
Require well-formed local evidence for every claimed exception: nonempty string op, recognized kind, parseable at, matching nonempty ID and the actual payload shape below. Duplicate op metadata with contradictory kind/ID cannot prove an exception; do not repair `_unionCorrLog`'s independent identity defect here. A valid correction already present in old history may reconcile a stale body; no freshness/current-time/new-op requirement.
Prove coverage per affected entry and field, not by any correction count or stamp. The final applicable placement/reps effect under unchanged replay must match next; a later unrelated load-only amendment does not cancel a prior reps correction, while a later reps/placement override does. Retain exact payload, receipt and input bytes; checking a replay's unrelated metadata is not the exception test.
- **skip:** matching `kind:'skip',id` actually removes the entry and puts that ID in skipped; final next must have the same absent-entry/present-skipped placement. Real UI: frozen:17891 stamps and files the action and emits an op-less receipt. Do not demand an invented same-op receipt or treat a bare skipped flag/dropped list as evidence.
- **unskip:** matching `kind:'unskip',id,to:{id,...entry}}`, with to.id equal to id and explicit reps array, restores/restates that entry and removes its skipped placement. The live path frozen:17906–17920 obtains hand-entered reps, stamps/files the complete entry, and emits an op-less receipt. Against a stale longer entry, the shorter hand-entered restoration is legitimate and must remain allowed if final replay/payload matches. It is no longer an OPEN shortening case.
- **strike:** `kind:'strike',to:[{id,reps,...}]` requires explicit reps for the affected entry; final replay must exactly reproduce the proposed shorter reps, plus explicit rirSets if supplied. Require a retained next.feed record with exactly that op, as in the shipped correction/migration contract and oracle (`frozen:11523–11538`; `port-oracle.cjs:37–41`). Wrong ID, scalar to:1, missing/op-mismatched receipt, insufficient payload or superseded reps does not cover loss.
- **amend:** same array shape and replayed fields reps/rirSets/w as strike, but the shipped filing located at frozen:11490 is load-only. A load-only amend cannot cover any removed slot. This package does not approve a new reps-shortening amend producer: if such a required existing path is found, publish its exact source/evidence and a reviewed positive/delta case before allowing that exception. Other amendment effects remain unchanged.
Mixed histories are checked together: a genuine strike for one lift cannot cover another lift's missing entry/sets; skip then unskip ends present; unskip then skip ends absent; later reps override must match next; scalar/invalid evidence cannot become valid through presence alone. Replay failures or unsupported shapes yield no exception, never a success inferred from a caught exception.
This establishes consistency with local recorded evidence, not authentication of an imported story or proof a human acted. `_fileCorr` is not a signer. Preserve legacy `_fileKnownCorr` and patch backfills exactly; no broader text matching, invented correction records, W5 attestation or receipt-prose rewrite.

### D33 — legacy corr-only loss is refused by the guard, without changing merge

`_mergeSession`'s corr-without-ops branch (`merge.cjs:338–357`, frozen:13694–13713) remains byte-identical, including its existing winner and dropped provenance. A record-level corr stamp alone cannot prove which unrelated entry was deliberately removed. Therefore a proposed result missing a protected old entry with no applicable filed correction now receives its explicit entryidentity loss reason.
This is the approved lost-fact guard fix, not a request to re-rule the merge winner. No separate ratified requirement was found compelling the guard to exempt that loss. Preserve old history and the exact merge result as evidence; do not fabricate provenance or accumulate phantom entries to bypass the refusal.
Existing host consequences stay unchanged: ghSync refuses the remote write by returning null (`frozen:12366`); ordinary save keeps the durable prior state and displays its guard banner (`22161–22182`). This deterministic legacy loss can refuse EVERY retry in either direction until an existing deliberate force/import resolves it; it is not self-healing on reconnect. Test repeated retries/both directions and an existing force/import positive. `restoreFromCloud` (`13242–13251`) is already UNGUARDED: record that pre-existing OPEN host limitation outside this package, never imply this repair protects it. No new forced-write path, retry or recovery UI; hosts are tested synthetically, not modified.

### D34 — four authored record families, with no field exclusion

Retain existing coarse shape/fingerprint conditions, then compare complete content in exactly reads, sleep.nights, dailyLogs and sessionLog against this instance's unmodified authored SEED. Canonicalize object-key order only; preserve array order/multiplicity, property presence, null, type and exact strings. Invalid/non-JSON projections return false without mutation or exception escape.
Compare every read field INCLUDING pt, weight/date/note/sealed/offWindow/reclassification evidence; all night-row fields; every daily row field; and complete session entries/skipped, reps/rir/rirSets/w, corr/corrLog and dropped provenance. No field exclusion is justified: actual migrated/settled unmodified SEED retains exact four-family equality including pt across the checked injected dates. A pt-only edit must therefore make the predicate false.
Do not compare trend/weekly/exercise caches/feed or broaden the predicate to previously unchecked waist/photos/targets/decisions/queue. Those excluded families are not all derived; the predicate is not proof that the entire ledger is untouched. No SEED rewrite, normalization pass, memoized mutable-state identity or clock/ID/draft call inside isPristineSeed.
Fresh `T.migrate(null)` from an actual engine with unmodified SEED must remain true; the inspected baseline's complete four-family projection matches before and after repeated settle. Also retain the existing synthetic-SEED raw-law fixture and a settled synthetic starter; neither substitutes for the real positive.
Boot/marker host: call actual candidate migrate/predicate in frozen:13270–13272, then preserve the existing marker-OR-predicate at16217. Fresh starter creates marker; an edited family with no marker does not. An existing marker survives later logging. Explicit clear removes the marker, but a still-pristine state remains eligible through the OR predicate; do not falsely test that clear always hides the offer.

### D35 — untouched future schema before healing

Move the existing `if(old && old.v > SCHEMA_V) return old` to the head of migrate, before container healing. Keep its comparison/coercion semantics; do not replace with an integer-only version check. For future input return the same object/aliases/cycles, without examining any non-version field, healing, cloning, settling, patches, IDs, clock or drafts.
Supported null/fresh, v1/v2, every v3…59 entry and v60 retain full frozen return/input/alias/receipt behavior. Explicit future test domain includes numeric61 and coercible string"61", null/missing containers, unknown nested aliases, self-cycle and non-version getters that throw if read. An accessor/Symbol/hostile coercion on v itself is not promised untouched execution: publish any such extra compatibility case before adding it to the allowed delta inventory; never generalize this exception to supported JSON state.

## 2. Required observable cases and exact delta cells

Common synthetic state uses day A=`2030-02-03`, day B=`2030-02-04`, read `{d:A,w:170}`, entry `{id:'synthetic-a',w:100,reps:[10,9,8],rirSets:[2,1,0]}`, plus another lift where needed. Preserve complete original trace in the pinned baseline; compare all return/input/alias cuts, not just the table's changed cell.
| Required case | Old → repaired observable cell; every unlisted cell preserved |
|---|---|
| IG33-READ-REPLACE | guard `{safe:true,lost:[]}` → `{safe:false,lost:['readidentity "2030-02-03"']}` at equal day counts. |
| IG33-SET-SHRINK | guard true/[] → false/`['setidentity ["2030-02-03","synthetic-a",2]']` when only terminal slot disappears. |
| IG33-ENTRY-LOST / LEGACY-CORR | guard true/[] → false/`['entryidentity ["2030-02-03","synthetic-a"]']` for that missing entry, including unchanged corr-only merge output; test both merge directions/preimages. |
| IG33-COMPENSATION / HOLE | Removing the same identity while adding another lift/day/slot cannot erase its exact reason. Separate pinned hole/null/undefined/0/string cases: only0 and string are occupied among these values; entry presence is still protected. |
| IG33-SKIPPED | Removing/moving an old skipped object with id synthetic-a changes safe/[] to false/`['skippedidentity ["2030-02-03","synthetic-a"]']`; real final unskip restoration remains safe. A hand-filed unskip without to.id removes placement in old replay but receives this refusal: pin that explicit negative. Repeated skip/unskip and unrelated corrections cannot erase another skipped fact. |
| IG33-CORRECTION-POSITIVES | Real filed strike+matching op receipt, live skip+op-less receipt and live unskip with shorter typed restoration remain safe when no old reason or unrelated loss exists; actual replay and exact receipts/inputs unchanged. |
| IG33-CORRECTION-NEGATIVES | Wrong/malformed/missing/insufficient/superseded evidence, load-only amend shrink and unrelated real correction each change old safe/[] to the exact identified loss refusal. |
| IG33-UNCHANGED / ADD / FOLD / FEED | Exact old result preserved for unchanged/additive writes, duplicate same-day fold, permanent-op dedup/presence, derived-feed and missed-day/clean-read rules. |
| IG33-AMBIGUOUS | Same malformed bodies plus additive bodies retain the old result; old multiplicity removal or in-place change appends exact `sessionidentity "2030-02-03"`. Pin id-less/duplicate/non-array-reps/skipped cases separately; no wildcard delta. |
| IG34-EDITED-FAMILY | Each synthetic equal-fingerprint content edit changes pristine true→false only; include all four families, explicit null/type/extra field, corrections and pt-only read edits. |
| IG34-POSITIVES / ORDER | Real settled default starter, synthetic starter and object-key reorder remain true; genuine additional record remains false. Array reorder preserving coarse tail/count changes true→false. |
| IG34-MARKER | Edited-record boot's set-marker call true→absent; resulting marker/offer false where initially absent. Existing-marker/explicit-clear/fresh-positive outcomes preserve the exact OR rule above. No migration/state/receipt delta. |
| IG35-FUTURE | Original post-input/result null sleep becomes `{nights:[],needed:3}`, reads becomes[], dailyLogs/sessionLog become{}; repaired retains each original null (and retains absent fields as absent in separately pinned cases). Return===input and unknown aliases remain true; non-version getter is unaccessed. |
| IG35-HEAL-GETTER | Future input with ordinary data v61 and an immediate sleep getter throwing `Error('IG35_FORBIDDEN_FIELD_READ')`: old throws that exact error after one getter call; repaired returns the same input, zero getter calls, identical property descriptors. Separately, a getter only on an unknown payload remains unaccessed on both engines. |
| IG35-SUPPORTED | Exact frozen traces for all supported entry paths and repeated migration; this includes normal required side effects, not a return-identity-for-everything shortcut. |

Every case runs both Date modes with matching injected clock, the nondefault clock, and isolated engine instances. Exact trace deltas name case/mode/assertion/output location, old predicate and new predicate; every delta must execute once as specified, and every observed difference must match one entry. No wildcard lost-list, receipt, input-state or source exception.
PACKAGE trace amendment: version the descriptor profile in target/tests so pre-call snapshots capture data/accessor descriptors, get/set identity and flags without invoking getters, including Symbol.toStringTag. Use intrinsic brand/prototype checks without user property reads; retain ordinary data/cycle/alias traces and distinguish known builtin Error/TypeError/etc classes with equal messages. Unsupported host/custom-prototype/proxy shapes remain HARNESS_ERROR, never engine RED or a mutant kill. Getter-call counters plus named call coverage must prove frozen migrate actually throws and candidate actually executes without reading the getter. Test the old unsupported-accessor path explicitly as a harness failure, and an Error-versus-TypeError trace mismatch.
Missing-container D35 cases also need exact structural add/remove delta cells, beyond PR37's existing replacement-only pointers. Pin the absent/present descriptor, full old/new cell and preserved aliases; unknown additions/removals still fail. No whole-input/result/graph exemption or automatic acceptance of regenerated expectations. These are test-infrastructure changes only, within the already named PACKAGE scope.
The old `migrate-differential.cjs:257–261` “filed strike” case actually contains to:1, which replay ignores. Keep the original test/source immutable; its successor carrier explicitly changes only that guard-result expectation to the named identity refusal and adds a genuine filed-payload positive. Carry later original cases too; an early intentional old assertion failure must not hide D36 or other preservation checks.
Composition: keep remoteBefore/localBefore before all calls; use actual returned `rm=T.migrate(remoteArg)`, merge in both directions, then actual final migration. Verify guards against both original preimages, all intermediate mutations/aliases and receipt bytes. Keep exact restore/ghSync host traces separately named; their original argument order/forced-import policy is unchanged.

## 3. Gates, actual fault mutations and bite

Original pinned audit remains45 RED-frozen/45 RED-candidate/90 GREEN historical repair controls/104 effective mutant executions. Against the repair, call original seeds' run only through actual candidate factories: selected3 GREEN direct, other42 still raw RED with approved preservation deltas only. Historical controls never stand in for a repaired engine.
Strengthened theme claims must be RED on frozen, GREEN on actual candidate. Required actual source mutants: D33 counts-only, missing per-entry/slot protection, accept-any-correction, ignore exact strike receipt/payload, and deny-all; D34 coarse fingerprint, never-pristine, raw key-order and whole-state comparison; D35 heal-before-return and return-all-inputs-unchanged. Positive cases must kill overstrict substitutes.
Mutate bounded declarations in disposable candidate copies before fresh-process load. Require the named behavioral assertion to fail; source-pin refusal, syntax error, missing target or timeout earns no kill. Run one disclosed bite, quote its RED line, restore exact source bytes/hash and prove the restored direct gate GREEN. Also bite one unlisted receipt/input-field delta to prove the comparator fails closed.
Complete required current gates: full three-blob oracle in both Date modes, migrate-full supplement, full second gate including engine-test/source/carrier accounting, sync-laws18/59/0 and surface baseline; preserve merge/writer/migration differentials. No required three-blob census/DTO/receipt delta is anticipated. A discovered conflict needs an exact reviewed successor expectation, never changed frozen tools/goldens or suppressed assertions.
Run unchanged conformance CONSISTENT and SELFTEST PASS with prescribed ENGINE_MAIN/ENGINE_OLD, MEASURED_TEST_NOW and TZ; strict PASS with measured clock unset. Private fixture preparation/execution stays in authorized local custody and reports only verdicts. Preparation did not run these private gates.
PACKAGE additionally verifies actual owner/cowork ledger-line content hashes, source inventories, case×mode×clock assertion occurrence counts, real mutation execution and all45+15 statuses. Missing authorization/cases/inputs remains exit2; failed behavior/extra delta is exit1. Only complete package coverage may print POSTFIX PACKAGE PASS; neither that nor selected3 GREEN means M2 CLOSED/full v4 PASS.

### PACKAGE authorization: exact artifact, separate receipt coordinates

Use `acceptance-import-guards.json` for ALL substantive requirements: phase/package, fixed original code-base anchor, all45+15 statuses/dependencies, governing source/brief and executable/input inventories, exact source postimages, cases/assertion matrix/deltas/mutants/gates and required role/claim/ID authorizations. Hash its exact UTF-8 bytes; no embedded self-hash or future review receipt. The operational manifest has exactly `{version:2,acceptanceFile,acceptanceSha256,candidateBase,receipts}`; no duplicate/substantive override fields. Runner reads substantive requirements ONLY from the artifact. Its own bytes are bound externally by this hash/review; the thin envelope is schema/receipt/ancestry checked; every other executable/input byte remains pinned.
Require raw byte equality with UTF-8 `JSON.stringify(parsed,null,2)+'\n'`, plus an actual duplicate-decoded-key pre-parser before JSON.parse and the closed schemas. Reject invalid UTF-8, duplicate/unknown/missing fields and invalid numbers/types. Ordinary object-key order remains exact reviewed artifact bytes; no new general canonicalization profile. Verify artifact bytes from Git at its reviewed artifact commit and byte-identical on disk.
The dedicated NEW cowork theme/payload acceptance receipts identify the exact reviewed artifact commit/path and full64-hex hash, with an exact terminal `ACCEPTED` verdict field. Reject negated verdicts (`NOT ACCEPTED`, `REVISE`, `REJECT`) rather than substring-matching ACCEPTED; cowork executed that v1 falsely accepts its original NOT ACCEPTED review line. Pin that exact negative control. Previously ratified owner/contract receipts retain their exact independently reviewed complete-line/hash mappings; do not infer new acceptance from their prose or rewrite historical ledger lines.
The operational review status is only PENDING or ACCEPTED: PENDING iff its review receipt is null. PENDING evidence runs exit2, print REVIEW-PENDING and no PASS word; a receipt supplied with PENDING or any third status fails. Once cowork reviews the exact artifact/code/cases, its dedicated ledger receipt names that artifact/hash, not the future operational envelope or its own commit. Integrator commits the receipt; candidate incorporates that real docs-only receipt base, changes only operational coordinates, and reruns the authorized PACKAGE. No self-certification or circular hash is necessary.
Replace v1's merge-base equality with explicit ancestry: immutable codeBaseAnchor and operational candidateBase (receiptBase) are ancestors of HEAD; receiptBase is also an ancestor of/equal to freshly fetched origin/rebuild/t2-client-core. At dispatch record the actual current integration tip as codeBaseAnchor, never choose a convenient substitute. The codeBaseAnchor..receiptBase diff permits only rebuild/DECISIONS.md, rebuild/m2/*.md, rebuild/QUEUE.md and rebuild/ROADMAP.md; any original PIN_PATHS file change fails. Verify all pinned product/input bytes at HEAD from Git and disk. An accepted branch may therefore rerun after integration without falsely treating its new merge-base as changed product; unexpected combined code/input changes require restaging/review.
Named fail-closed cases: envelope substantive override; changed artifact byte; invalid UTF-8/duplicate key; unpublished/unincorporated receipt base; wrong-role/missing full hash/negated receipt verdict; receipt-status contradiction; intervening engine edit; unlisted artifact/delta/source change. Positive: the executed PR37 post-integration false-stale example now passes under the same accepted payload and valid updated receipt coordinates. Report the exact acceptance hash used; every coordinate/base change requires rerun. No private data enters this artifact or its report.

## 4. Report and reviewer checklist

Report the nine established sections: Module map; Executed gates; Clock/ID/storage and mutable ownership; Bite/restoration; SEAMS; RECON disagreements; Not covered; Wall-clock/token accounting; DEFECT LOG/statuses. Include exact base/ruling/brief/manifest pins, five permitted product source changes, every delta, all gate tails and NEXT; no private values/counts/hashes/prose.
Reviewer checks: (1) actual owner receipt and independent theme acceptance; (2) only scoped declarations/bindings changed; (3) real skip/unskip/strike positives and malformed/legacy-loss negatives; (4) real unmodified-SEED positive and exact marker OR; (5) future identity/no-effects with supported migration retained; (6) old scalar-strike carrier accounted without hiding later cases; (7) direct real-engine GREEN and actual fault RED; (8) unchanged three-blob/second gates and all45+15 inventory. Re-execute on Linux before integration.

## 5. Never, limits and preparation evidence

Never edit frozen app, original suite/oracle/audit controls, product merge/writers/canonicalization, known correction backfills, receipt prose, transport or another claim. Never create a generic correction-authentication claim, new set identity scheme, wholesale pristine/reset detector, all-arrays-grow rule or automatic force path. No source-fidelity exception beyond the explicit reviewed five changes.
No new owner ruling is presently identified for these three repairs. The prior draft's legacy-corr blocker is removed: unchanged merge authority plus a stronger loss guard implements approved D33; independent review must assess the exact compatibility delta. A newly found real shortening-amend handler or incompatible required assertion is a concrete review issue; do not silently broaden coverage or request a product ruling merely because a fix changes behavior.
Executed preparation: actual migrated/settled starter matches complete four-family projection INCLUDING pt at four injected dates; real skip/unskip filing restates a shorter typed restoration over a stale longer entry; corr-only merge loses an unrelated entry in both directions while the old guard permits it; future61 returns identity after healing null containers. Private ambiguous-session predicate on the locally regenerated frozen live blob: NOT TRIGGERED. Only that verdict was exposed; no product repair or post-fix gate ran. Reviewer C1–C6 corrections are incorporated, plus executed typed-error/toStringTag and structural-delta harness gaps; this revision awaits the exact recheck.
