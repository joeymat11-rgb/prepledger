# M2 — POST-FIX ACCEPTANCE AMENDMENT, draft for independent review

2026-09-06 · ASTRA · PUBLISHED PROPOSAL, independent review pending. Branch `rebuild/m2-postfix-brief` from integration `811d1c0a34756b12e2ebf56163e3c018672797ed`. No owner disposition, fix, gate implementation or acceptance is claimed.
This is the smallest proposed acceptance-contract addition before the first defect repair. It is not the complete suite-v4 brief and does not close M2.
Reviewed audit source: PR30 at **`614e20315b01543d3b7bbc4fa1fe8a5c20bcb690`**, verified from the actual checkout. Also read the accepted module-7 briefs/report and current master prompt §5. The proposal below still needs independent review and the selected fixes still need their recorded owner disposition.

## 0. Authority, starting point and scope

The owner-activated `rebuild/FINISH-PROMPT.md` §5 requires all D1–D45, recorded rulings, accepted theme briefs, both engine gates and a separately accepted v4 tranche, including the queued non-D findings. It permits neither automatic approval of FIX recommendations nor silently changing extraction expectations.
The intended first repair package is **D33/D34/D35 — import guards**, 13 estimated engineering hours in the accepted register; its owner disposition is still pending. The preparation below makes its future test contract reviewable and adds no training rule.
Before implementation, record: actual current `rebuild/t2-client-core` tip; merged audit/acceptance receipts; the exact owner disposition covering the selected IDs; cowork's acceptance of this amendment and the theme's concrete behavior/delta brief. An unmerged or merely recommended answer is not an approved ID.
Build the gate infrastructure in a separate reviewed change or the authorized theme PR, before modifying product behavior in that worktree. With no approved fixes applied it must report **BASELINE PASS / FIXES PENDING**, not completed repairs. Do not create a third concurrent product stream or claim these proposed filenames already exist.
Proposed new test files live only under `rebuild/conform/v4/postfix/`; the adopted brief/report live under `rebuild/m2/`. The later theme brief alone authorizes product declarations. Do not edit `rebuild/conform/laws/`, `oracle/`, `run.cjs`, its manifest, the original audit seeds/helpers/runner, frozen app/assets/tools/baselines, or another builder's files.

## 1. Why the existing gate needs an explicit successor

| Existing contract | Exact issue after an authorized repair | Narrow amendment required |
|---|---|---|
| `conform/v4/run-defect-laws.cjs:46–70` | `inspect` requires raw RED, patched repair-control GREEN and mutants RED; `main` also requires frozen/candidate trace parity. A fixed candidate correctly violates those requirements. | Preserve that runner at its accepted extraction/audit pin. Add a separately named post-fix runner; do not change the old verdict into GREEN by applying its control to the candidate. |
| `conform/v4/helpers.cjs:39–58` and the law `control`/`mutants` fields | Candidate loading is hardwired to `rebuild/engine`; `patch` stores the loaded function in `__auditOriginal`. After repair that pointer is the repaired function, so restoring it is not reintroducing the defect. | New explicit target loading plus mutations of the actual candidate module in disposable copies. Historical controls remain evidence that the old assertion distinguishes its proposed behavior; they are not implementation. |
| `engine/test/migrate-source.cjs:15–37`; `merge-source.cjs:21–24`; `writers-source.cjs:32–34` | Three altered migration declarations fail exact copied-body comparison; later source checks pin the entire earlier `migrate.cjs`. | A successor fidelity check recognizes only independently accepted declaration deltas, verifies every other byte and retains every original order/export/instance/injection check. No whole-file exemption or rewritten old acceptance report. |
| `engine/test/defect-witnesses-5.cjs` D33/D34/D35; migration/merge/writer differentials | These deliberately preserve old defects or compare complete candidate output to frozen output, including guard results. | Keep historical witnesses runnable on the pinned original. Add approved behavior cases and exact delta-aware comparisons for the current candidate; do not call an old expected defect failure a passed legacy gate. |
| Full oracle and `engine/test/migrate-full.cjs` | Required DTO, raw post-state, receipts/order and input mutation currently compare to immutable frozen goldens/reference. | For this first package **no three-blob DTO or migration-output change is expected**. Require the existing full checks unchanged. Any discovered difference is a blocker for a specific reviewed expectation amendment, not an automatic exemption. |
| `engine/test/second-gate.mjs:40–54,108–144,159–163` | It requires every original assertion site/outcome/count, sync result and exact surface bytes. Fixes in later themes may legitimately change specific runtime expectations. | For D33–D35 require the complete current second gate unchanged first. If a concrete existing assertion conflicts, name its source/hash/site and approved replacement assertion in a separately reviewed amendment; never skip the section or suppress a failure. |

Original acceptance evidence remains: frozen source `fe516c1:src/app.jsx`, blob `f98671d823f0d8cd83e730cdd930afe5f5e7b628`; accepted module-7 engine; PR30's audit source/results. Pin all relevant public source/fixture hashes from Git, and reproduce the unchanged original checks from a clean original checkout. Check the actual current baseline before attributing any failure to a new repair.
The final audit SHA, integration SHA and public test-file hashes belong in the implemented baseline descriptor; never place a private blob/golden hash, count, date or payload in it. Required private fixture preparation remains with authorized local custody and produces only permitted verdicts.

## 2. Proposed minimal files, API and manifest

| New path under `rebuild/conform/v4/postfix/` | Contract |
|---|---|
| `run.cjs` | Explicit baseline/target preflight, all-45 inventory, original-seed raw results, approved direct cases, preservation/delta checks and effective module mutations. Nonzero for any missing or unexpected result. |
| `target.cjs` | Load the explicit candidate `index.cjs` factory in a fresh child process; pass clock/ids/drafts through the accepted boundary. Every runtime call resolves to that candidate's real E graph. Frozen references load only the independently pinned original. No fallback path or reference helper in candidate execution. |
| `manifest.json` | Versioned acceptance/disposition and exact public source/case/delta pins, described below; reviewed changes append a successor version. |
| `laws/import-guards.cjs` | Existing IDs for D33/D34/D35; direct cases and positive cases that exercise the actual repaired implementation, including the relevant host composition. No repair `control`. |
| `legacy-gates.cjs` | Explicit declaration-level successor fidelity checks and only necessary pinned legacy case carriers; preserves every unamended source/runtime/ordering/instance check. Records original-case → unchanged/replaced-by-approved-case accounting. No general expectation filter. |
| `test/runner.test.cjs` | Fail-closed infrastructure tests and the mutation/target-isolation checks in §5. Synthetic data only. |

Proposed command: `node rebuild/conform/v4/postfix/run.cjs --manifest rebuild/conform/v4/postfix/manifest.json --baseline <clean-pinned-audit-checkout> --candidate <current-repo>/rebuild/engine`.
Both paths must resolve to the declared Git tree/code hashes; reject missing engines, stale manifests, missing authorization required by the phase below, extra law files/IDs, duplicate IDs, unknown options and candidate imports outside its permitted module/dependency inventory. Never infer an engine from a developer-machine default. Set and report explicit `ENGINE_MAIN`, `ENGINE_OLD`, date mode and TZ for child gates.
The proposed new-law shape is `{id, defect, cite, expect: 'GREEN', run, mutant}`. `run(target)` obtains `target.engine({clock, ids, drafts})`, invokes real exported functions and returns `{ok, assertions, trace}`. Require the exact reviewed case × mode × clock × assertion inventory and occurrence counts; derive GREEN from every required observed assertion, rejecting missing/duplicate/unknown names and inconsistent `ok`. `mutant` is a nonempty array of `{id, file, declaration, preimageHash, apply, expectedFailures}` specifying a bounded on-disk source change, not a target-method replacement. These are proposed APIs, not claims about the current runner.
Original seeds use a separate legacy `B` adapter: preserve `B.clock(day)`, `B.engine()`, `B.engine(dayString)` and `B.engine(clockObject)`, with the original deterministic ID and empty-draft defaults (`helpers.cjs:31–47`); every call loads the explicit candidate factory. Do not pass these old shapes directly into the new options API. Account each original seed's boolean as its pinned raw-law result plus the required call trace; do not invent an internal assertion inventory it never emitted. Test all adapter forms, mutation of the same clock object after creation, nondefault dates and separate instances, without altering original `run` bodies or supplying a candidate reference helper.
Manifest required fields:
- `version`, `phase: BASELINE | PACKAGE`, `packageId`, nonempty `requiredIds` (this first package exactly D33/D34/D35), `baseline` (frozen source pin, accepted extraction/audit commit, public source/case hashes), `candidateBase`, and `inventory` covering **D1–D45 exactly once**, plus the separate queued non-D IDs below.
- For each D-ID: original law ID/hash; disposition `UNRULED | APPROVED-FIX | KEEP | DEFER`; exact owner ledger/ruling reference where required; theme brief/version and cowork acceptance; desired claim; `implementation: PENDING | PRESENT`; dependency IDs. No disposition is filled from an agent's recommendation.
- For `PRESENT` fixes: approved declarations and their pre/post hashes; concrete synthetic positive/negative cases; actual module mutants; exact approved output delta assertions; all preserved fields/aliases/receipt bytes and compatibility rules. A `PENDING` fix stays raw RED and explicitly pending; it cannot earn repaired/closure PASS.
- Delta records name a law/case, mode/input selector, actual output location, approved old/new assertion and its ruling. For synthetic constants use reviewed exact expected values/bytes; for variable values use an independently specified rule plus concrete examples. Every observed difference must match exactly one reviewed delta; every required delta must execute and satisfy both its old and new predicates. Reject overlapping/unused entries and all unlisted output, error, input-mutation, alias or receipt-byte changes. No wildcard “ignore receipts,” whole DTO deletion, candidate-generated expected value or normalized string comparison.
- Source deltas cover only the named declarations and explicit necessary binding/export changes. The revised full declaration bytes are review-pinned; unchanged declarations, surrounding bytes, declaration order, factory graph, clock/ID/storage boundaries and mutable-instance isolation remain checked. A new helper or changed boundary needs an explicit manifest entry and accepted theme scope, never permission implied by a changed file hash.
BASELINE requires the pinned original engine with no implemented-fix claim; UNRULED rows are permitted and success is only `BASELINE PASS / FIXES PENDING`. PACKAGE requires every `requiredIds` row APPROVED-FIX/PRESENT, complete recorded owner/theme/reviewer receipts and satisfied declared dependencies; a required PENDING/UNRULED/KEEP/DEFER row exits 2. Nonselected unresolved rows remain visible with preservation evidence. PRESENT is legal only with APPROVED-FIX; reject invalid enum values or other contradictory pairs. Pin the reviewed manifest through its review receipt and verify the cited approval/brief content and commit, not merely that a citation file exists; the candidate cannot generate its own authorization.
Before repair, publish a manifest with all 45 actual statuses; do not premark D33/D34/D35 approved or present. Implement only import-guard cases now. Later themes extend this versioned runner by accepted diffs, rather than building every final-v4 law in advance.

## 3. Runtime verdicts: preserve evidence, then test the real repair

1. In the pinned original checkout run the unchanged 45-law audit runner and verify its full original result: **45 RED-frozen / 45 RED-candidate / 90 GREEN repair controls / 104 effective mutant executions / 0 HARNESS_ERROR**. These are the independently accepted PR30 observations, not repair counts. Preserve its raw/trace parity and all source pins there.
2. On the new candidate call each original seed's **`run` only**, against explicit fresh real engine instances. Do not invoke `law.control`, `patch`, `__auditOriginal` or a reference repair. Preserve ordered arguments, return/input mutations, identities/aliases, exceptions and receipt bytes as applicable; do not trust the seed's one-line `detail` string as a complete comparison.
   Use a collision-free typed trace/graph representation, or exact JSON bytes supplemented by explicit identity/alias and non-JSON-value assertions for the declared input domain. The old `run-defect-laws.cjs:27–33` snapshot sorts object keys, loses aliases and has ordinary-object tag collisions; it remains historical evidence, not the successor's sole exact comparator. Unlisted trace differences fail closed.
3. A repaired approved ID must be GREEN directly on candidate and remain RED against original frozen behavior; its new strengthened claim must also be RED-frozen and GREEN-candidate. A seed unexpectedly GREEN at the original baseline is a specification/defect problem and fails preparation, never silently disappears. Original helper supplements needed only for diagnostics retain their exact pinned frozen declaration provenance; they never supply a candidate function.
4. For every unapproved or not-yet-repaired ID, require the original raw RED and the previously accepted observable behavior outside approved cross-ID deltas. An incidental GREEN, changed error/receipt or changed trace is **UNAPPROVED-DELTA**, blocking integration until explicitly explained and reviewed against the actual owner ruling. It is not automatically another repaired ID. KEEP/DEFER requires its own recorded disposition and stays visible as an unresolved desired-law RED with preservation evidence; this amendment grants no deferral.
5. Run each actual fault mutant in a disposable candidate copy **before loading it** in a fresh process. Assert one exact intended source change, unchanged fixture/assertions and actual execution through that declaration, then require the named behavioral assertion to fail. In this explicit mutation phase validate the declared mutation's preimage/diff/postimage, rather than stopping at the intentional source-pin difference; rejection by a source-hash check alone earns no behavioral kill. Syntax/import errors, timeouts, wrong target, missing capability and a mutation that makes no difference are HARNESS_ERROR/INEFFECTIVE, not detected faults. Positive controls must defeat deny-all, return-empty and never-pristine substitutes.
6. Fresh unmutated candidate processes must pass before and after all mutants; verify product/test preimage hashes are restored exactly. Do not monkeypatch only `__test` while internal E calls execute a different function. Run affected cases in frozen-Date and native-Date modes with explicit matching injected clocks, a nondefault clock and two independent engine instances; preserve existing ambient-read traps and per-instance state tests.
7. Print all 45 IDs/statuses plus proposed lines such as `D33 ... RED-frozen / GREEN-candidate-direct / mutants 3/3 EFFECTIVE / preserved PASS`. Totals distinguish approved-present, approved-pending, unruled, KEEP/DEFER and harness errors. `POSTFIX PACKAGE PASS` means only that manifest's implemented package, never M2 CLOSED or full v4 PASS. Exit 1 on failure; exit 2 for missing required authorization/input/custody; no partial-success flag for a required gate.

## 4. First package: D33/D34/D35 and exact preservation boundaries

| ID / existing law | Direct positive and negative acceptance required | Actual fault mutations |
|---|---|---|
| D33 / `V4-guard-record-identities-and-sets` | Unchanged/additive writes pass; replacing a read day at equal counts and removing an unfiled nested set refuse. Duplicate reads on one day may still fold. Run an actual filed/attested correction through the existing correction path, retaining its exact op/evidence/receipt; its permitted set removal passes. An unrelated, malformed, missing or insufficient correction must not excuse the removal. Existing derived-feed, permanent-op and missed-day exceptions remain exactly as specified. | Restore counts-only protection in the actual guard; remove correction coverage check; deny every write. Each must fail the relevant assertion, not only change a summary. |
| D34 / `V4-pristine-compares-record-content` | Changed values in each compared family are non-pristine; object-key order alone is not content. A fresh real engine with **unmodified SEED** must keep `migrate(null) → isPristineSeed === true` (`tools/engine-test.jsx:8388–8391`), alongside the authentic synthetic starter after settling. A genuine added record stays false. Exercise restore eligibility through the declared synthetic boot/marker host below; no actual restore/private data. | Restore coarse fingerprint; return false for all inputs; treat object-key order as content. Direct genuine-starter and edited-content controls must detect all three. |
| D35 / `V4-unknown-schema-return-untouched` | A newer schema returns the same input identity with every supplied field and alias unchanged, including null containers and unknown payloads; no heal/settle/patch/clock/ID/draft effect before return. Supported v1/v2, each patch-era entry, current schema and fresh-seed exits still perform their required migration and remain idempotent. | Move container healing before the version guard; an identity-return-for-all-versions substitute must fail the supported-version positive cases. |

D33 requires a concrete correction-coverage algorithm in the repair brief, derived from the existing filed/attested correction contract, before code is written. A hand-added `corrLog` row is not by itself proof of valid attestation; inspect `_fileCorr`/reconciler semantics and cite the matching records/receipt. `_fileCorr` appends local correction evidence; it does not authenticate an external claim. Do not invent a new correction schema, wire attestation requirement, or “all arrays can only grow” rule.
D34's comparison families and treatment of genuinely derived fields must be enumerated in that brief. Comparing arbitrary whole state or raw SEED blindly may reject a legitimate settled fresh seed; a synthetic fixture alone does not decide this boundary.
D34's existing carrier only reads/removes the marker (`second-gate-carrier.cjs:155–157`); it cannot prove eligibility. The declared synthetic host must use the real candidate fingerprint in the pinned boot condition (`app.jsx:13270–13272`) and marker-or-fingerprint condition (`16217`), with synthetic storage. A fresh starter creates the marker; an edited record with no marker does not; later writes retain the marker; explicit clear removes it, with eligibility still determined by the existing OR condition. The original D34 seed mutates `T.SEED`, so it cannot replace the unmodified-SEED positive.
For synthetic composition, retain independent `remoteBefore` and `localBefore` snapshots **before any call**; use `rm = T.migrate(remoteArg)`, then `merged = T.mergeState(rm, localArg)` and `result = T.migrate(merged)`. Test `T.dataLossGuard(remoteBefore,result)` and `T.dataLossGuard(localBefore,result)` against the case's reviewed expectations, retaining every return/input/alias cut and exact receipt/decision bytes. Repeat in the opposite merge direction and with repeated delivery. Do not discard migration's returned object or use a post-migration argument as the protected preimage.
Keep exact host traces separately identified: restore uses `rm = migrate(remote); mergeState(rm,s); migrate(merged)` with its restore receipt (`app.jsx:13242–13251`); ghSync uses `mergeState(state,migrate(rState))` then guards `rState` (`second-gate-carrier.cjs:46–49`). Exact host behavior and the stronger pre-call guard checks are distinct evidence. This package grants no transport/order change or change to the frozen forced-import policy (`app.jsx:22161–22174`).
For this package, run both full three-blob oracle modes, `migrate-full`, full second gate, sync laws and engine surface unchanged; retain merge/writer/migration differential coverage. The original migration differentials may have a specific changed guard/pristine result: identify exact case and revised assertion before accepting it, preserve all other output/input/alias/prose comparisons, and retain its frozen-run evidence.
Existing defect-witness runners remain original evidence. Where a runner has no case-selection seam, its first intentional defect mismatch must not hide later cases: the successor inventories/copies the pinned original case bodies and helper dependencies into a versioned test carrier, binds every engine call to the candidate, verifies all original case IDs are accounted for, executes unaffected cases unchanged, and dispatches the three selected cases to their approved replacement assertions. This test carrier contains no reference product implementation. A selected old witness failing because it expects the bug is recorded as superseded for that case only, not silently omitted or described as passed.
If any approved delta reaches a required census value, receipt, assertion site, shape/source guard or record DTO, stop and obtain a narrowly scoped expectation amendment identifying that exact output and why. Keep original oracle/tools/goldens unchanged. Future candidate expectation files belong in the reviewed successor area, derived from ratified claims plus synthetic examples; no `golden` regeneration to bless current output.
Run frozen conformance `CONSISTENT`, `--selftest` PASS and `scripts/check.mjs --strict` PASS in their prescribed separate environments. These continue proving the frozen families/package and are not substitutes for current-candidate runtime checks. Preserve every required private fixture gate locally, with no private output in the public report.

## 5. Negative acceptance, full-v4 inventory and handoff

Before accepting the new gate, prove it refuses: a missing/duplicate D-ID; PACKAGE with a required ID downgraded to PENDING or a missing dependency; empty `requiredIds` or PRESENT+UNRULED; absent/nonexistent required approval or an existing citation with wrong content/disposition; stale candidate base; wrong/missing candidate module; altered frozen pin; skipped/duplicate assertion or positive case; unchecked extra output delta or overlapping/unused delta; wildcard source exemption; unchanged/invalid-syntax mutant; a candidate wrapper that runs the audit repair control; and an empty-output candidate that previously satisfied only an equality check. Every preflight failure and behavioral failure must remain distinguishable.
Exercise the trace comparator with alias loss, key-order-only drift, undefined versus an ordinary object resembling its tag, and one extra changed field; each must fail without an exact reviewed delta. Prove actual execution of the mutated declaration; a source-pin rejection or a count of external calls is not a behavioral kill or branch-coverage proof.
Require direct frozen RED/current GREEN plus an actual on-disk fault RED and byte-restored GREEN for each implemented defect. A mutation report containing fewer required mutant IDs, unexecuted sites or a caught harness error fails. Use disposable copies, no private fixtures, no external calls or persistent storage outside the declared scratch directory.
Keep all 45 rows and these separately queued full-v4 obligations visible in the manifest/report inventory: T2 restart-from-serialized-store and raw unsigned-pull trust cases; canonical-v2 decimal compatibility findings; rig185 rename shape; portable explicit engine defaults; sync seeds for every `MERGE_ARR` key; later received Sol/Grok objections. Reconcile their source/ruling/status in the final-v4 brief, keeping unavailable review verdicts OPEN.
The preserved `0c3e7ce:rebuild/t3/REPORT-CLAUDE.md` §6 was retrieved read-only: retain **6a** correctly athlete-bound second-athlete lease fixture plus negative admission; **6b** contiguous client frontier after consented apply/effect; **6c** authoritative log-sequence frontier definition; **6d** parked-child survival through armed crash; **6e** signed/trusted pull receipts before client W advances; **6f** decimal canonical-profile defects; **6g** issued non-protein conflict-domain behavior. These are retained findings, not newly adopted rules or claims that the report's proposed remedies are already accepted. Cross-reference overlapping F4/T2/W5/W6 evidence without losing any ID.
Do not turn overlapping W5/W6 boundary tests into automatic completion of these suite items: cite exact accepted proof and decide the required integration explicitly. The first import-guard package must not change canonicalization, client/authority semantics or unrelated suite law files.
Report: actual base/approval/brief pins; changed declarations; raw original baseline result; all-45 candidate status inventory; direct implemented-law and positive-case output; every actual mutant/bite and restored hash; full oracle/second-gate/current delta verdicts; unperformed/private-only gates; exact allowed output/source changes; residual D/non-D obligations; wall-clock and NEXT. Cowork re-executes the candidate on Linux and checks every allowed delta; the integrator runs the combined accepted revision. Builder does not merge.
Success here enables only the first recorded repair package. M2 closes later through all recorded dispositions, accepted repairs, the separately reviewed full-v4 suite, both current engine gates and an accepting M2 scorecard. No product code, test implementation or post-fix acceptance verdict was produced by this draft.
Same-family challenge: ASTRA's counterreview prompted the five clarifications above; this is **not independent acceptance**. Actual in-memory Node probes confirmed the real `migrate(null)` positive and the audit control's four-family comparison, rejected arbitrary whole-state/SEED equality, observed a distinct return for synthetic v59 migration, and reproduced the old trace's alias/key-order/tag collisions. No private data or product state was printed, no complete engine gate was rerun, and no post-fix runner has been implemented. Owner dispositions and independent review remain pending.
