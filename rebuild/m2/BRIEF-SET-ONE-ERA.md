# EARNED — D30 first-set technique-era repair brief — PROPOSED, NOT ACCEPTED

2026-09-06 · ASTRA · Docs-only publication from integration `213300c6c49f772dcb2ff859e1c1375dd571b837`. D12/D33/D34/D35 are independently accepted and integrated; 41 defects and 15 non-D obligations remain open. This document specifies the next approved repair; it implements nothing and is not an acceptance receipt.
Owner authority: all45 directions at `rebuild/DECISIONS.md:60`; D30 is the plain FIX in `rebuild/m2/AUDIT-REGISTER.md:379–387`, law `P-D30-first-set-trend-respects-the-recorded-technique-era`. The app currently mixes first sets from before and after a recorded technique change when reporting today's improvement. Use its existing comparability rule; no new training choice is needed.
Frozen source: `fe516c1:src/app.jsx`, blob `f98671d823f0d8cd83e730cdd930afe5f5e7b628`; original audit `614e20315b01543d3b7bbc4fa1fe8a5c20bcb690`. Source evidence: frozen8883–8911/3187–3191; extracted `volume.cjs:189–217`, `progression.cjs:561–566`, `plan.cjs:36–56`; original `defect-witnesses-4.cjs:67–80`. Coordinates are on the publication base, not guessed post-edit lines.

## 0. Start, scope and authority

Future build branch `rebuild/m2-set-one-era`, from the then-current verified integration carrying this independently accepted brief and accepted D12. The coordinator records one exact base/claim in the existing M2 stream; keep all retained worktrees. One product PR; C independently executes the exact artifact on Linux; I integrates and checks the combined revision. No builder merge or new dispatcher.
Product allowlist: only `rebuild/engine/volume.cjs`, the `setOneRead` declaration plus two late-bound delegates to existing `E.forksOf`/`E.sameEra`. `plan.cjs:330` exports both; `index.cjs:3–15,27` initializes plan before volume. Keep the factory export name and all other declarations byte-identical. Existing `isoOf`/`todayStart` delegates supply the query day. No direct plan import, duplicated era helper, writer, formatter, threshold, cache, schema, history or frozen-source change.
Let P=`rebuild/conform/v4/postfix/`. New closed profile `M2-SET-ONE-ERA`: `P/acceptance-set-one-era.json`, `manifest-set-one-era.json`, `laws/set-one-era.cjs`, source-only expectation/frozen/mutant helpers, a wholly synthetic delta fixture, necessary exact legacy carrier and focused tests. Enumerate final paths/hashes in its artifact; required runners cannot depend on scratch `work/` files.
Allow only profile-specific extensions of `P/acceptance.cjs`, `source-proof.cjs`, `target.cjs`, `package-runner.cjs` and `ci-second-gate.cjs`, plus directly required tests. Keep `P/run.cjs`'s 19-entry inventory unchanged. The sole workflow change allowed is `.github/workflows/rebuild.yml`'s existing second-gate profile argument to `M2-SET-ONE-ERA`; retain its real dispatcher and fixed public verdict. No generic plugin/profile permissions or skipped legacy checks.
Report: `rebuild/m2/REPORT-M2-SET-ONE-ERA-ASTRA.md`. Any additional source/carrier/expectation scope discovered during construction needs a concrete named delta and independent technical review before it can earn PASS; this is not permission to repair adjacent defects. Register estimate: 3 engineering hours; successor construction and independent verification add work, not a promised elapsed duration.

## 1. Exact product behavior

Preserve the initial exercise lookup and IDLE guard exactly. Immediately after that guard, obtain `forksOf(s, exId)` once; obtain `isoOf(todayStart())` once only when that returned array is nonempty, otherwise use `null` as the unused query-era argument. Inside the existing sorted-date loop, before reading that date's session record, skip it when `sameEra(forks, d, at)` is false. All remaining original code is unchanged.
This is the existing query-day era, not the latest fork in the record. `eraIdx` includes a boundary from its date (`from <= d`). An array-valued `forks`, including `[]`, overrides legacy `fork.from`; preserve unsorted/duplicate entries and existing truthiness/coercion rather than normalizing them. No forks means no extra clock read and no date filtering. IDLE means no new helper/clock calls.
Do not introduce `d <= today`: a future session before the next fork remains eligible if it belongs to today's era; a session across a future fork does not. That historical behavior is outside this correction. Preserve the current interpretation of all fork kinds; neither D31 nor D32 is selected.
After era filtering, preserve same-load string comparison, first-rep numeric coercion, empty/missing-reps handling, hard-session and rushed exclusions, sort order, minimum `TREND_MIN_SESSIONS = 4` (`constants.cjs:104`), regression/confidence arithmetic and three-decimal rounding. Fewer than four qualifying observations returns exactly `{status:'COUNTING', exId, n, need:4}`. Preserve later invalid-fit branches returning the same shape with `n:0`; never borrow old observations to produce LIVE.
No state mutation, ID creation, storage call, history deletion or receipt rewrite. Capture exact new helper/query-clock calls and removed per-date dependency calls in the expectation frames. For excluded dates, deliberately never access their session record or call weather/pace; test that a throwing accessor on an old-era record is bypassed while the same current-era accessor preserves the old exception. Do not claim arbitrary malformed states are now repaired.

## 2. Cases, expectations and observable consequences

Use actual literal frozen exports and actual candidate factories. Run each new direct case in four cells: frozen/native Date modes × injected days `2026-09-03`/`2026-09-07`. Derive dates relative to the selected query day where required, without an ambient clock. Pin complete invented states, including every reached HISTORY/ROLLUPS dependency; cloning authored SEED is not proof of synthetic custody.
First construct and hash source-only expectations from the pinned frozen declaration with only §1's enumerated guard/delegate projection, before inspecting candidate results. Pair that projection with independently specified membership/count assertions and manually checkable fit inputs; a candidate cannot supply its answer key. Record exact expected numeric confidence outputs during construction, not guessed values in this brief. Source projection alone is not repaired-engine evidence.

| Required case family | Exact obligation |
|---|---|
| ERA30-ORIGINAL | Preserve the original D30 raw construction: enough old same-load first sets plus one current-era observation. Frozen is LIVE; new result is the existing COUNTING shape, `n:1`, original minimum. Retain original raw law/case bytes. |
| ERA30-NONE / EMPTY / LEGACY | No exercise fork, explicit empty array overriding legacy fork, and legacy single fork. No-fork complete results/exceptions and underlying clock observations stay exact; legacy filters at its existing boundary. |
| ERA30-BOUNDARY / MULTIPLE | Day before/on/after each of at least two forks; exact inclusivity. Permute and duplicate boundaries without inventing a normalizer; unrelated exercise forks cannot change this exercise. Match existing `sameEra` membership exactly. |
| ERA30-FUTURE | Current query era differs from latest stored fork. Future session inside query era stays eligible; across the next fork is excluded. Move the injected day across that fork and prove the new query era is used. |
| ERA30-COUNT | Zero, one, minimum-minus-one and exactly minimum qualifying current-era observations, each with enough old observations to tempt borrowing. Exact counts/status/absent properties; no old sample fills a shortage. |
| ERA30-FIT | Wholly invented flat/increasing/decreasing positive current-era first sets with old-era extremes. Independently pin complete pct/lo/hi/from/to/count outputs and original rounding; same current-era input without old points gives the identical fit. |
| ERA30-FILTERS | Wrong load, string-equivalent load, empty/missing reps, numeric-string/zero/negative first reps, hard-session and rushed exclusions; all-eligible mean nonpositive; IDLE for absent exercise or `typeof ex.w !== 'number'` (do not newly reject NaN). Preserve original exceptions/coercions outside the era cut. |
| ERA30-READ-CUT | Old-era session accessor throws: no access after the repair; current-era counterpart still throws. Weather/pace call traces omit only excluded dates. Input descriptors/aliases remain exact; no catch-all recovery. |
| ERA30-SET-COUNT | A set-count change with no technique boundary does not add filtering: this reads the first set, not `volumeConversion`'s separate D32 behavior. Fork-kind behavior continues to follow existing `sameEra`. |
| ERA30-CLOCK | Counting/LIVE with forks samples the injected query day exactly once, including two calls straddling a fork day; no retained snapshot/cache. No-fork and IDLE add zero query-day samples. Ambient-Date trap and divergent injected/native dates detect leakage. |
| ERA30-LAB | Execute the actual `labAnalytics2` set-one card (`sleep.cjs:1283–1290`) with entirely invented dependencies. LIVE may become its existing ARMED/counting card or a new existing LIVE value. Compare the complete card/string and aggregate shape/order; preserve every other card unless a separate exact source consequence is proved and reviewed. |

Pin each concrete case ID, multiplicity and full typed return/input/error/alias/dependency frames before artifact acceptance. New observable permissions are only exact add/remove/primitive cells or explicit bijective alias mappings at enumerated cases. No whole-card/string/state wildcard, ignored clock family, missing-case credit or HARNESS_ERROR-as-RED. The table is a construction inventory, not an executed verdict or an empty fixture allowance.
The known direct consumer is the laboratory card; no actionable Today/writer/private exposure is established by that fact alone. Existing full census/second-gate/differential checks remain mandatory to find indirect effects. Protected census/surface consequences, if any, must be derived in authorized local custody against the SAME input, reviewed as exact successor cells, and published verdict-only. Keep frozen goldens unchanged; an unexpected delta remains RED until specified.

## 3. Cumulative gates, lineage and effective faults

Immutable accepted parent: `P/acceptance-step-efficacy.json` at `904d35ddfb81e1a9b4cfc1d6ccbb50b58651149c`, SHA256 `ff164b8620ee0ab7851e7d9283b32d1b330b261fad1f02178d4266131cfcabb1`. Its operational envelope at `0a7abebc71b861a10334f70782d2bea2f729cc4b` has SHA256 `7c78a3b85cb8001bd9cf3c0076faa410a203e66cfdd9cde6598e3617a9c648ec`; actual artifact receipt at `348993f71c448f612a6c462a408e56ebea0b6afa`, DECISIONS line71 SHA256 `911906f078f116d13d337654f98c71c1a83c7fa026c019c64c18fcfbf2cd8edf`. Integration/authorized-run acceptance is DECISIONS72 at `213300c`. Recursively verify its immutable import parent and receipt too.
New profile selects only D30 and carries accepted D12/D33/D34/D35. Re-run all inherited 295×4 direct cases, 68 effective fault executions and 180 raw cells against the actual composed candidate, adding the new exact inventory. Carry the parent's six source changes and expectations verbatim; extend proof only for this one volume declaration/two delegates. Parent bytes, original operational manifests and all45 original audit identities/dispositions remain immutable. A changed candidate needs composed proof; the old candidate-pinned envelope cannot certify new volume bytes by itself.
All five selected/carryforward raw laws must be direct GREEN-candidate, D30 RED-frozen. The other40 gate-date rows remain their exact approved pending RED behavior. Preserve D27's dated GREEN-BY-FIXTURE-DATE and D44's exact nondefault THROWS/UNDEFINED masking pins, without repair credit. D45 remains consequential RED/open with the accepted runtime-only D12 custody. All15 non-D obligations and the stale-offer follow-ons remain OPEN. No `__auditOriginal` substitution for selected/carryforward cases.
The new substantive artifact closes source/executable/input pins, cases, typed deltas and source faults. The separate operational envelope contains only coordinates/real receipts. Preserve strict UTF8/duplicate-key/closed-schema validation, actual Git/disk hashes and ancestry, owner/theme/artifact acceptance checks, and PENDING iff null review receipt. Review-pending execution may report observations, never PACKAGE PASS/exit0. Only a real dedicated acceptance receipt authorizes the envelope-only transition and required authorized rerun; never re-author substantive expectations at that cut.

| Inherited gate identity group (unchanged runner paths) | Required execution and verdict |
|---|---|
| `migrate-source`, `merge-source`, `writers-source` | `rebuild/engine/test/*-source.cjs`, frozen/native each: 6 invocations, exact closed source successor PASS. |
| `witnesses-1`…`witnesses-7` | Original `defect-witnesses[ -2…-7].cjs` files; 2/5/7 frozen/native, others one original dispatch: 10 invocations. Closed source/expectation carriers PASS; preserve every later assertion. |
| `migrate-differential`, `merge-differential`, `writers-differential` | Original test paths; migrate frozen/native, merge one dispatch, writers frozen/native/**trap**: 6 invocations. Full states/returns/aliases/receipt comparisons PASS under exact approved deltas only. |
| `merge-laws`, `migrate-full`, `second-gate` | One original dispatch each. Sync-laws18/59/0; full oracle10/10 on all3 blobs/both Date modes plus exact migration supplement; all3072 assertion sites and fixed-clock second gate, all4 surface cells and6 legacy sites, PASS. |
| `conformance`, `selftest`, `strict` | One dispatch each. CONSISTENT99/99/29/70 plus rig185 W1/W2 PASS; SELFTEST PASS; strict All checks passed with test clock unset. |

Total remains19 inherited identities/28 outer invocations; inner mode coverage is preserved. Run from the repo root with AGENTS private preparation kept local, explicit ENGINE_MAIN/ENGINE_OLD, `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`; strict unsets MEASURED_TEST_NOW/PL_ENGINE/PL_LAWS_LIB. Publish exact entry command including new manifest, original baseline and candidate path. Public CI runs its actual profile dispatcher with and without private setup and on both operating systems. Keep all357 current infrastructure tests, adding focused cases; execute full required checks at candidate and integration cuts as prescribed, not after every drafting change.
Legacy `defect-witnesses-4` and source gates need a pinned successor rather than editing their original files. Verify complete original helper/test bytes, actual candidate source and exact replacements before executing. Frozen source carriers may project only §1 plus inherited accepted changes; no fs interception, frozen-engine substitution, skipped assertions or global volume exception. Existing second-gate protected expectations, D45 custody and unchanged assertion4822 continue exactly; additional D30 effects are unresolved until the source-only construction proves them.
Real source faults in disposable candidates: remove era guard; invert predicate; replace query era with latest fork; exclude the inclusive boundary; borrow pre-era points to meet minimum; break legacy-fork dispatch; add a future-date cutoff; use an ambient clock. Each must fail its named behavioral case in all applicable matrix cells. A syntax failure, missing export, pin refusal or timeout is not a kill. Retain all parent faults, restore exact bytes, and prove direct GREEN after restoration.
One bite removes the actual guard and quotes ERA30-ORIGINAL's failing assertion/verdict; restore volume bytes and quote the restored SHA and direct GREEN. Separately alter an unlisted output/receipt field and prove exact comparison refuses it. Preserve cold CLI and PENDING→accepted-envelope lifecycle tests; a mock receipt proves harness behavior, never real package authority.

## 4. Report and independent reviewer checklist

Use the nine established sections: Module map; Executed gates; Clock/ID/storage and ownership; Bite/restoration; SEAMS; RECON disagreements; Not covered; Wall-clock/token accounting; DEFECT LOG/statuses. Include exact base/owner/theme/parent/new-artifact pins, three allowed source edits, complete assertion/cell/call inventory, all gate tails, 45/15 accounting and NEXT. Report attributable cost as unavailable when unavailable; the workflow savings target is a pilot, not an acceptance claim.
C checks the existing era rule, no-fork/IDLE clock preservation, boundary/future/legacy behavior, exact insufficient-data counts, all consumer consequences and source faults; independently executes the final exact package on Linux under approved private custody. I merges only that accepted revision and verifies the combined scope/strict/infrastructure/actualCI plus both operating-system jobs. Neither a source-only expectation run, a local green case nor this brief closes D30.

## 5. Never, seams and NEXT

Never change frozen app/tools/suite/oracle/goldens, original witnesses/laws, accepted artifacts/envelopes, training thresholds, fork semantics, stored history, receipt templates, writers, private inputs or the seeded soak. Never expand this into D31/D32, date cutoff, cache/as-of redesign, stale-offer repair or full v4 closure. No protected literals/derived state hashes in public fixtures, reports or tool output.
Behavior specification used a reviewed comment-suppressing exact-function reader and synthetic canary tests, without reading a private blob. Mandatory unchanged-suite preparation subsequently regenerated private fixtures only under AGENTS local verdict-only custody. An earlier source-comment exposure was disclosed separately and its contents are omitted from every draft/commit. No D30 product repair or acceptance rig was run. Exact-function extraction is a custody precaution, not an independent reviewer or replacement for complete source proof. Do not print adjacent authored-history comments when verifying these coordinates.
Still to construct: concrete fixture IDs/counts, independent fit/prose/call expectations, full safe consumer reachability, exact source/carrier replacements and any protected census/surface deltas. Build them under the tracked profile before artifact review, never infer zero consequences. If existing E exports are insufficient, stop that dependency and propose the exact additional binding; do not widen product scope silently.
NEXT: C reviews this brief and the queue's evidenced D12/R1 status corrections. After acceptance is recorded, the coordinator may claim D30's implementation in the existing M2 stream. R1 retains its separate W6 slot, scoped-read checkpoint accepted but RESOURCE FAIL; no metric/cap waiver or private-port readiness follows from this PR.
