# M2 import guards — ASTRA implementation report

IN PROGRESS. One coordinated package on `rebuild/m2-import-guards`; three repairs are implemented and initial preservation gates pass. The complete PACKAGE and independent acceptance remain pending.

## 1. Module map

Actual dispatch base: `947369caeb9fea2d6bcb04b0447857de35ba887b`, the fetched integration carrying accepted PR38 and the owner ruling. Root verified its exact three-file documentation diff, accepted-brief ancestry and 62-line decision ledger.
Scope: `rebuild/engine/migrate.cjs` — dataLossGuard, isPristineSeed, the initial future-version return, and the two explicitly permitted late-bound correction adapters. PACKAGE infrastructure and cases stay under `rebuild/conform/v4/postfix/`; this is the sole report.
Root owns product/report and private gate custody. Three bounded agents own disjoint PACKAGE infrastructure, concrete theme cases, and legacy test carriers. One aggregate branch and PR; builders do not merge.
Product changes are the two late bindings at migrate.cjs:6–7, future return at2219, pristine predicate at2270 and loss guard at2357. The recordCounts declaration and every patch/reconciler/merge/writer/shim remain unchanged. Nested pure helpers canonicalize only the guarded content; ambiguous non-JSON bodies use descriptor-safe equality or refusal, not getter execution.

## 2. Executed gates

Start verification: exact ledger/prefix/append and accepted brief hashes PASS; only the two brief/report files and ledger changed from the prior integration. Cowork independently compared947369c with its dry-run tree and confirmed equality. Root and integrator observed both OS CI and strict green.
Before product edits, the exact frozen-source reference (with frozen bundle dependencies) and unchanged extracted engine each returned4 GREEN/11 RED on the initial15 direct cases; all intended failures occurred, including actual future-input getter execution. After the five permitted changes the candidate returned15 GREEN/0 RED. The initial helper incorrectly described its source supplements as only missing exports: it also overrode exported migration/guard functions. This was corrected before final comparison: the literal bundle's migrate/isPristineSeed/dataLossGuard and mutable SEED now take precedence, with source supplements only for missing functions. The resulting208-case/761-assertion inventory ran at2026-09-03 and2026-09-07 in frozen/native Date modes: literal bundle104 GREEN/104 RED and actual candidate208 GREEN/0 RED in EACH cell, with0 unexpected results. Positive preservation cases must remain GREEN on both; requiring every individual positive to be RED would remove the tests that reject overstrict repairs.
Executed current product preservation gates (unmodified runners, explicit verified frozen engines, prescribed date/zone; local private details withheld):

```text
PASS M2-5 actual full oracle Date=frozen: all ten required law IDs; all three blobs
PASS M2-5 actual full oracle Date=unfrozen: all ten required law IDs; all three blobs
PASS M2-5 full migration gate: actual full oracle and exact post-state supplement; all three raw blobs; both Date modes
SECOND GATE candidate FINAL108: 3072 passed, 0 failed
SECOND GATE candidate surface: byte-identical to committed baseline (123077 bytes)
SECOND GATE candidate: PASS; exact original harnesses; July lazy-clock bridge; closed synthetic I/O; manifest .tmp/m2-second-gate/candidate-manifest.json
M6 SYNTHETIC native: PASS — 152 exact whole-state differential cases; 17 two-device scenarios, both directions, repeat batch twice, two injected clocks; complete returns/inputs/aliases/receipts.
M7 WRITERS native: PASS 84 scenarios / 314 call cuts / 27 methods; two clocks; full returns/errors/arguments/state/aliases/receipts.
PASS exact sync-laws source: tools/sync-laws.mjs:37 via PL_ENGINE; 18 named laws GREEN in both candidate Date modes
INFO 9 engine-track rig185: W1 PASS, W2 PASS
SUITE CONSISTENT — 99 reference GREEN · 99 STRONG · 29 RED-first against absent families · 70 GREEN against present families
SELFTEST PASS
All checks passed. Safe to ship.
```
The last line is the original strict gate's exact output, not permission to ship this incomplete package. Merge/writer runners also executed their frozen/trap modes. Legacy-carrier unit tests16/16 PASS; original baseline migration differential126 cases in each Date mode and D33–D36 witnesses4/4 were independently re-executed before successor adaptation.
Required results remain the entire accepted brief's gate inventory; missing receipt or private preparation is BLOCKED, never a partial PASS. The successor artifact is a draft; its independent artifact acceptance receipt does not exist yet. Exact case×mode×clock traces, actual mutations, final successor carriers and authorized PACKAGE remain pending.

## 3. Clock, ID, storage and mutable ownership

No new ambient clock, ID, storage or global cache is introduced. Correction evidence uses the actual existing instance-local union/replay boundary on scratch state. Product edits began only after runnable infrastructure and meaningful actual frozen-RED evidence. D35 preserves the exact existing future-version comparison/coercion before any non-version access; supported migrations remain covered by the original full oracle and second gate.

## 4. Bite and restoration

NOT RUN. Actual scoped source mutations, a behavioral RED line, exact restoration and an unlisted-delta comparator bite are required before acceptance.

## 5. SEAMS

Authority: owner DECISIONS line60, SHA256 `ebb565c65ba1a33dbc5b007d8854e6c203f1f8ae218bef7521c2dc07ffd596c8`; theme acceptance line62, SHA256 `5bc569c669b2ce2722128f512bb53488f5b306aa2b1329d8cd345e4a7ef6f53d`; both at the dispatch base. Accepted post-fix contract line49: `f14f5e9280bc4a4316757ccfb5ed68e57152f8913bbe6979d6c1ad4f2cefac52`.
Governing brief: `rebuild/m2/BRIEF-IMPORT-GUARDS.md` at `df5bd1b8682cc5ec2720da67976e8931e19b2cf8`, SHA256 `3229d916bcf013c1db9f91e86f174f09c13effdbeb1624f805ecbb9204ef7c6d`.
The exact artifact/receipt split, descriptor-safe trace and source/case successor rules are mandatory. Existing unguarded restoreFromCloud and deterministic corr-only retry refusal remain explicit limits. Actual ghSync guards its remote preimage only: a sparse remote winner can still omit richer local entries without tripping that host call; the direct guard against the retained local preimage refuses. Ordinary save and explicit force use their actual unchanged argument/override rules. Exact frozen host compositions cover both directions, repeated retries, live skip/unskip controls and restore. No symmetric host guard, automatic force path or imported-evidence authentication is claimed.

## 6. RECON disagreements

No new product ruling is proposed. The three directions and detailed C1–C6 behavior are accepted. During full raw-law date testing, the unchanged D27 seed is naturally GREEN on09-07 because an earlier logging instruction masks its bug; D44's dated sample makes sweepVolume return null and its seed then throws TypeError. Both are RED on09-03. Root's team and Cowork independently reproduced exactly these two exceptions across all45 laws. Cowork's2026-09-06 Message32 accepts narrow per-cell accounting with six conditions: default cells retain45 original RED/42 unselected candidate RED/3 selected GREEN; selected rows remain RED→GREEN on both dates; nondefault unselected outcomes AND exact traces match separately pinned frozen outcomes; only the exact unselected D44 nondefault law throw may be an explicit THROWS outcome, never a harness-success/repair; every non-RED row carries its masking reason and remains PENDING; all four cells are immutable, with negative tests for missing/contradictory expectations and changed D27/D44 outcomes. Default, selected or unpinned throws and all infrastructure errors still fail. This fixture-date limitation is recorded for later v4 work; original laws/dates remain unchanged.

## 7. Not covered

Other42 repairs, fixed15 non-D obligations, complete suite v4 and M2 closure remain pending. No frozen app/original audit/suite/oracle/goldens/seeded-soak change, provider action, private import, release, phone verdict or CLOCK acceptance is authorized by this package.

## 8. Wall-clock and token accounting

Implementation dispatch: 2026-09-06 15:21 UTC. Completion and active gate intervals will be recorded from actual execution. Estimates are13 engineering hours for the product repairs plus18–26 for PACKAGE infrastructure; these are not measured agent duration. No separate token budget was requested.

## 9. DEFECT LOG and NEXT

D33, D34, D35: APPROVED-FIX / implemented, acceptance PENDING. All45 owner dispositions remain recorded; this report does not mark any accepted repair. No additional product repair is included.
NEXT: finish the complete direct/legacy trace inventory and effective mutants; publish one reviewable repair PR and exact acceptance artifact. Cowork executes independently; the integrator records the dedicated artifact receipt, then the final authorized PACKAGE reruns. No repeated owner approval is needed.
