# M2 SET-ONE-ERA (D30) — ASTRA builder report

WORK IN PROGRESS. This report records preparation and current construction; no D30 package or product acceptance is claimed.

## 1. Module map

Claim: `rebuild/m2-set-one-era`, from verified integration `28ff3be3a0c47fa76b642015ac3757da5c76548c`. The base carries the accepted D30 brief, D12/D33/D34/D35, and DECISIONS:82/84. I integrated the brief in `e0337bea92c2e1347453d1cc1683e7480a02a2c4`, then recorded the queue acceptance in the base commit. Root verified the expected tree, all three accepted document hashes, the exact 84-line ledger and both-OS CI34185573123. These are brief-integration results, not this candidate’s gate results.

Product changes are exactly the `volume.cjs` / `setOneRead` query-era filter and two late-bound delegates, `E.forksOf` and `E.sameEra`. Full postimage SHA256: `23e9528bff84cf83086c77c7c26f990f13589dde748382f5e98d35b6206a9719`. No other product file changed. Ptolemy owns direct source expectations/product cases, Copernicus the closed cumulative profile, Planck exact legacy carriers, and the coordinator the complete package/report. Their checks are author evidence, not independent acceptance.

## 2. Executed gates

Preparation PASS: real dependency directory from an identical-lockfile retained checkout; complete fixed-reference hashes verified; the unchanged oracle regenerated the private fixture/golden in an isolated local mirror; public goldens match after the permitted package-stamp normalization, and committed public pins stayed unchanged. No private values, hashes, counts or text are reported. The original baseline checkout was verified at `614e20315b01543d3b7bbc4fa1fe8a5c20bcb690`, with tracked files clean.

Setup disclosure: `npm ci --ignore-scripts` could not start because npm was absent from the shell (native exit1, no dependency or lockfile mutation). The already installed real `node_modules` directory was copied from the same-lockfile accepted brief checkout. Previously commit-verified frozen reference artifacts were rehashed and reused; no fresh `build-engines.mjs` invocation is claimed. Scratch preparation logs/metadata remain local.

Focused author checks:146/146 infrastructure assertions; actual target-worker parity24/24 cells over six selected D30 cases; unlisted output mutation detected; all8 real source faults effective in4 cells each (32/32); protected carrier11/11. These are bounded construction checks. Initial coherent commit `78db27b`: full infrastructure451 tests,442 PASS/9 FAIL; complete package stopped before raw/direct execution with `POSTFIX FAIL ENOBUFS`. The unchanged Git reader’s16MiB buffer could not read the44MiB fixture. The infrastructure failures exposed historical STEP test inventories/eval bindings and one protected-read failure under focused diagnosis. The historical test setup now selects the verified actual ERA candidate inventory/carrier while retaining immutable STEP expectations and every original assertion; focused custody checks11/11 passed. The new large-byte reader and historical callback bindings passed25/25 focused checks. The protected prototype passes unchanged in isolation (1/1,14.985 seconds); its original failure preceded candidate loading. No concrete shared-path cause was established, and discarded child diagnostics prevent retrospective diagnosis. Full reruns use serial infrastructure followed by PACKAGE for isolation, without altering the parent helper or treating the original failure as product evidence. Original logs/receipts are preserved as `.tmp/coordinator/*-at-78db27b.*`. No failing run earns acceptance; final reruns remain pending. The required command is `node rebuild/conform/v4/postfix/run.cjs --manifest rebuild/conform/v4/postfix/manifest-set-one-era.json --baseline C:/Users/joeym/.codex/worktrees/9339/prepledger-dev --candidate rebuild/engine`, from the repository root with explicit local ENGINE_MAIN/ENGINE_OLD, `MEASURED_TEST_NOW=2026-09-03`, `TZ=America/New_York`; strict removes the test-clock/PL overrides. Pending observations must not be called PACKAGE PASS.

## 3. Clock/ID/storage and ownership

The accepted rule obtains the query day once only when the exercise has nonempty forks; no-fork and IDLE add no query-day read. No ID creation, storage write, state mutation, date cutoff or receipt change is permitted. Source-only expectations are constructed before edited candidate results are inspected. The existing law control is not a clock-trace oracle.

The35 direct cases were constructed in140 source-only cells before any edited candidate inspection. Original fixture seal: `d6b477e2790ede69b2e2442215296ebac50b8ffce37916af1c7e57c37b94d0b2`. The later mechanical permutation into the inherited matrix order preserved all140 per-cell serialized hashes; new fixture seal: `f4c72b029485a8a31fac5d24461f022a6dd8f54a4bf17a4423bb2d05254af352`. The raw four-cell serializer was authored after the first candidate smoke check, using the already pinned full frozen source/projection and unchanged original raw law/input. It never loaded the candidate or used candidate values. This narrower raw chronology requires explicit C review; it is not described as fully pre-inspection serialization.

The pre-existing frozen clock provider and extracted `dates.cjs` provider use different injected method names. Exact source and candidate provider frames, query values and call counts must remain visible in the pinned expectations; this difference is not an ignored clock family or a new clock contract.

## 4. Bite/restoration

Executed literal-restoration bite in a disposable candidate: `ERA30 BITE RED: ERA30-ORIGINAL/pace, ERA30-ORIGINAL/result, ERA30-ORIGINAL/weather`. The mutated file was overwritten with its exact original bytes, followed by `ERA30 BITE RESTORED GREEN: 23e9528bff84cf83086c77c7c26f990f13589dde748382f5e98d35b6206a9719`. The main candidate inventory remained unchanged. All8 prescribed source faults earned32 behavioral kills across the matrix; pin/syntax/timeout failures earn no kill. The full package will repeat these fault checks.

## 5. SEAMS

Named technical scope addition for C review: the existing JSON parser accepts canonical pretty JSON only. The sealed synthetic fixture is44,181,946 bytes compact, but353,282,869 bytes in that canonical pretty form. The implemented ERA-only helper carries a hash-verified literal copy of the unchanged strict parser, substituting only `JSON.stringify(parsed,null,2)` with `JSON.stringify(parsed)` and proving exact reversal. It is selected only for the new ERA artifact/fixture, preserving every decoded-key/UTF8/syntax/canonical check and every typed cell. Original `strict-json.cjs` SHA5722f233e704149f03f2898bf739c3cc42fe323a0c91e0eee55eb82cc57a3478 and all parent formats stay unchanged. The unchanged legacy Git transport also has a16MiB buffer, so a second fixed carrier reads only the two named ERA artifact/fixture paths, verifies Git blob type and exact size, caps the complete read at64MiB/60seconds, and preserves every caller byte/hash check. Neither original reader is changed. These added carriers need explicit independent technical acceptance before final PACKAGE PASS; construction observations alone do not authorize them.

The artifact/envelope remain separate. Parent artifact `ff164b8620ee0ab7851e7d9283b32d1b330b261fad1f02178d4266131cfcabb1` and operational envelope `7c78a3b85cb8001bd9cf3c0076faa410a203e66cfdd9cde6598e3617a9c648ec` stay immutable. The exact inherited 19 identities/28 outer invocations, 295×4 direct cases,68 effective faults and180 raw cells remain mandatory on the composed candidate.

Author-side review caught a missing boundary: the first seeded-card helper compared only frozen and source-projected results. That proved the expected consequence, not candidate parity. The helper now prepares all four original/projected expectations before loading the verified actual candidate, then compares the complete set-one card, input and HISTORY/ROLLUPS against the same captured seed/clocks in memory. Actual candidate: UNCHANGED in4 cells; a correctly pinned executable altered candidate: RED in4 cells. Its final11 focused tests passed; log `.tmp/postfix/era-carrier-focused.log`. The full package must still re-execute this boundary.

Protected P6 sites8790–8793 and the seeded set-one card require verdict-only execution. A difference must stay RED pending its reviewed exact successor, never golden regeneration. D27/D44 masks and runtime-only D45 custody remain unchanged.

## 6. RECON disagreements

No new training-rule decision is authorized. D30 uses the existing query-era rule. Future dates inside that era remain eligible; D31/D32 and other adjacent behaviors remain outside this repair. Any additional binding/carrier/expectation scope will be reported as a named blocker before it receives PASS.

## 7. Not covered

No private port, production or phone qualification, seeded-soak interaction, broader workout/schema acceptance, suite-v4 closure or M2 closure. No C artifact acceptance or authorized-envelope rerun has occurred. The integrated brief’s acceptance does not certify the future product.

## 8. Wall-clock/token accounting

The single implementation branch was created at2026-09-08T04:02:30Z from28ff3be; final elapsed build/test time will be recorded from the run receipts. Attributable model cost/token usage is unavailable. Account-wide credit movements from preceding review/integration assignments are not this repair’s engineering cost. The workflow savings target remains a pilot, not a demonstrated result or acceptance gate.

## 9. DEFECT LOG/statuses

D12/D33/D34/D35 remain the four accepted repairs. D30 construction is in progress;41 preserved defects and15 non-D obligations remain open until independent acceptance. This profile selects only D30. Its eventual acceptance reduces the remaining defects to40; it does not close the15 other obligations or the stale-offer follow-ons.

NEXT: finish the exact source-derived D30 cases and cumulative profile, execute the complete mandatory package, publish one product PR, obtain C’s exact-artifact acceptance, then follow the dedicated receipt/envelope-only authorized rerun and designated I integration route. No builder merge.
