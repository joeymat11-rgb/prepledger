# ASTRA — D30 brief and evidenced queue correction

2026-09-06 · Branch `rebuild/m2-set-one-era-brief`; base/integration `213300c6c49f772dcb2ff859e1c1375dd571b837`. Docs only; proposed contract, not accepted. C owns independent review and I integration. Retained D12/R1/W6 worktrees and model settings remain unchanged.

## 1. Module map

`BRIEF-SET-ONE-ERA.md` specifies D30's already-approved first-set era correction, three narrowly allowed source edits, exact era/clock/call behavior and the cumulative acceptance package. No product code or fixtures changed. `QUEUE.md` records D12 accepted/integrated, R1's scoped checkpoint accepted/resource failure retained, and the coordinator's D30 docs-only preparation claim.
Authority: DECISIONS60/66–67/69–74; D30 register379–387. Metadata verified plan exports/initialization before volume, minimum4, current delegates and the laboratory consumer. Eight new effective source faults are required; existing19 gate identities/28 invocations,180 raw cells,1180 carried direct executions and68 carried faults remain mandatory for the future repair.

## 2. Executed gates

Executed on this docs-only worktree with unchanged product bytes:

```text
FROZEN-PATHS PASS — pinned authorized base; committed and working copy
OLD-PACKAGE PASS — 18 allowlisted files; actual ZIP entries and bytes verified
SCOPE-FREEZE PENDING — new PWA archive, full private suite and final M3 implementation evidence remain release gates
INFO 9 engine-track rig185: W1 PASS, W2 PASS
SUITE CONSISTENT — 99 reference GREEN · 99 STRONG · 29 RED-first against absent families · 70 GREEN against present families
SELFTEST PASS
PASS engine suite — 3072 assertions passed
All checks passed. Safe to ship.
```

The strict tail concerns the frozen app's existing checks, not readiness to ship the rebuild. `git diff --check` PASS. CI/independent review were not yet executed at report authorship; their later exact-SHA receipts are required before integration.
Reproduction: use a **real node_modules directory** with the pinned root dependencies (this checkout copied the existing D12 directory; no install or lockfile change). Run `node rebuild/m3/w0/scope-package.mjs`; strict with MEASURED_TEST_NOW/PL_ENGINE/PL_LAWS_LIB unset. Build references through accepted `buildPublicEngines()` exported by `rebuild/m3/w0/public-oracle.mjs`; set ENGINE_MAIN/ENGINE_OLD explicitly to `.tmp/m3-w0-engines/engine-main.cjs`/`engine-old.cjs`, MEASURED_TEST_NOW=2026-09-03, TZ=America/New_York and EARNED_CLIENT_DIR to this checkout's `rebuild/client`; run original `rebuild/conform/run.cjs` and `--selftest` after AGENTS private preparation.
Setup failures retained: initial `npm` lookup was unavailable (no install ran); the old engine builder with relative `.` failed its Windows module-specifier import before building. The accepted W0 builder succeeded without editing that original tool. First full suite run lacked the ignored private fixture and returned INCONSISTENT at the privacy gate; after local regeneration/unchanged golden-pin verification it returned the PASS lines above. No failure was relabeled or hidden.

## 3. Clock/ID/storage and ownership

No clock/ID/storage implementation changes here. Proposed D30 reads the injected query day once for a nonempty fork list after the original IDLE guard; empty/no-fork cases add no clock dependency. No ambient Date, new state, writer or historical mutation. Same coordinator and two streams; no new schedule or model switch.

## 4. Bite/restoration

No product bite is claimed for a documentation PR. The brief requires an effective removal-of-era-guard bite, exact source restoration and direct GREEN for the future repair, plus eight case-specific source faults. The scratch reader's three synthetic canary tests passed; they test output custody, not D30 correctness.

## 5. SEAMS

Current source results and E wiring were inspected through comment-suppressing exact-function/metadata readers. A same-family challenge found no blocking behavior/scope error; it corrected an overbroad privacy statement, now explicitly distinguishing source-comment exposure from private fixture custody. This is not C's independent acceptance.
An earlier targeted source read reached an authored-history comment; it was disclosed to the owner, raw reads stopped, and its contents were omitted from all drafts/commits. Behavior planning used no private blob. Required conformance preparation later regenerated the frozen private blob/golden locally using the existing isolated-mirror procedure; only PASS/FAIL was emitted, committed public manifests/goldens stayed identical. No protected content or new protected hash is in this PR.

## 6. RECON disagreements

None proposed. D30 reuses `forksOf`/`sameEra`; future-date eligibility, fork-kind behavior, legacy precedence, D31/D32 and training thresholds stay unchanged. The new reviewed output contract changes only the incorrect mixture of technique eras and its exact proved consequences.

## 7. Not covered

No D30 implementation, repaired-law GREEN, source-only expectation artifact, full D30 package acceptance or private LIVE re-evaluation. Concrete fit/card/call fixtures and any protected successor deltas remain construction obligations; they cannot be waived or guessed. D45, stale offers,41 unimplemented defects and15 non-D obligations remain open. R1 resourcev1 still fails; v2 is rejected; no replacement metric, cap change, remote-only acceptance, W6 completion, owner port or phone/soak claim.

## 8. Wall-clock/token accounting

This focused publication slice began approximately22:24UTC; local checks completed22:42UTC, with drafting/PR/review work continuing afterwards. It is not a repair-build or completion forecast. Attributable tokens/cost and a comparable baseline are unavailable. D12 integrated during the workflow pilot but was carryover work; no measured20% saving is claimed. Retained settings and all mandatory checks/independent review are preserved.

## 9. DEFECT LOG/statuses

D12/D33/D34/D35 are accepted and integrated at213300c:4/45 repaired. D30 remains approved FIX but unimplemented; its new brief and artifact require their own review. D45 remains consequential RED/open under the accepted D12 custody; all other dispositions remain unchanged. R1's scoped-read checkpoint is accepted at9f08c01/docs successor003c816, but R1-COMPLETE remains FAIL on resources. No new product defect is asserted by this documentation check.

NEXT: C reviews the D30 brief and exact queue diff, then supplies the existing integrator route with the acceptance/ledger append if accepted. Only then claim the D30 product build in the retained M2 stream. R1 retains its separate slot; no owner action is needed for this publication.
