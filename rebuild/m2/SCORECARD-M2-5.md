# M2 MODULE 5 — SCORECARD (cowork, 2026-09-05; rebuild/m2-engine-5 @ aba8ad6, re-run on a fresh checkout with the locally regenerated private live blob + golden)
| # | Item | Result |
|---|------|--------|
| 1 | GATE (must) | The FULL port-oracle `check` (the real CLI, not a partial mode) exits 0 with all TEN required law IDs GREEN on all THREE blobs (preimage, synthetic, PRIVATE live) in BOTH Date modes (frozen preload and native Date); 0 FAIL · 0 DEFECT · 0 HARNESS_ERROR. Reproduced here independently of Astra's run. |
| 2 | BITE (must) | Astra's bite (_settleExit feed text) failed the live census law. cowork's OWN, different bite (reconcileSightings: topRun+1) → 3 FAIL (census law on preimage + synthetic + live), exit 1; a second, malformed bite (ReferenceError) → 4 HARNESS_ERROR, exit 1 — the gate fails closed either way. migrate.cjs restored byte-for-byte (sha 73a0082a…). |
| 3 | FIDELITY (must) | Independent diff of the 93 copied declarations against `src/app.jsx` @ fe516c1 (cowork's own script, not the tracked runner): only the three authorised substitutions (_freshId → ids.fresh; the v1 hack3 stamp and _stampCorr → clock.nowISO()) plus two dropped trailing comments and the export block. Nothing else differs. Tracked `migrate-source.cjs` agrees (93 declarations / 2,291 lines; modules 1–4 + oracle-shim byte-identical to the accepted base). |
| 4 | SUPPLEMENTS | `migrate-full.cjs` PASS (both modes, exact JSON.stringify post-state + post-call input on all three raw blobs); `migrate-differential.cjs` PASS (126 cases × 2 modes; 64 migration exits/repeats; rename .from witness); `census-partial.cjs` PASS (regression only, 829/141 + volume); `defect-witnesses-5.cjs` 4/4 reproduced on frozen AND candidate. |
| 5 | SUITE | run.cjs CONSISTENT (99 GREEN · 99 STRONG · 10 port ids · rig185 W1/W2 PASS); selftest PASS; check.mjs --strict PASS without MEASURED_TEST_NOW. Scope: 8 files, all under rebuild/engine + rebuild/m2; frozen app, suite, client, authority, M3, soak stub untouched; no lock-file change; nothing private tracked. |
| 6 | SEAMS (disclosed) | drafts facade (length/key) instead of RECON's drafts.dates() — brief-ruled; T.records stays absent (oracle's own legacy DTO); renames[].at vs nameAt .from remains a known oracle-projection seam, not a new defect; boot does NOT mint (mergeState's {mint:true} is module 6); earnWalk + correction writers are callable closure additions, not modules 6/7. |
| 7 | DEFECT LOG | D33–D36 preserved, nothing fixed: D33 dataLossGuard counts session days, not sets (a silent set loss passes); D34 isPristineSeed is a coarse fingerprint (an edited weight still reads "pristine"); D35 the future-schema "untouched" exit heals containers first; D36 the curl migration receipt prints a fixed "60·60·55" line regardless of the actual vector. Next number: D37. |
| 8 | HARNESS NOTE (suite v4) | run.cjs step 0 defaults ENGINE_MAIN to a hard-coded machine path (`/tmp/rig174/engine-main.cjs`) while ENGINE_OLD defaults beside the suite; on a machine with a stale file there the port ids drop to 7 and the run is INCONSISTENT until ENGINE_MAIN is set explicitly. Not a PR defect — recorded for suite v4 (default both engines under rebuild/conform/engines/). |
| 9 | COST | Astra: ≈9 min recorded between checkpoints (setup excluded); cowork verification ≈20 min. |
Verdict: module 5 (migrate) ACCEPTED for merge. The engine's MIGRATE is ready six days before the 2026-09-11 checkpoint; MERGE (module 6, mergeState) is next and is the remaining half of the checkpoint question.
