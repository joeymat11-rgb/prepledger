# M2 MODULE 6 — SCORECARD (cowork, 2026-09-05; rebuild/m2-engine-6 @ cd043f5, base f2f7458; re-run on a fresh checkout with the locally regenerated private live blob + golden)
| # | Item | Result |
|---|------|--------|
| 1 | GATE a (must) | FULL port-oracle `check` 10/10 GREEN on all THREE blobs (incl. PRIVATE live) in BOTH Date modes; exit 0; 0 FAIL/DEFECT/HARNESS_ERROR. Merge did not disturb migrate. Reproduced here. |
| 2 | GATE b (must) | The unchanged `tools/sync-laws.mjs` re-pointed through its own PL_ENGINE seam at the candidate: 18 laws / 59 committed seeds / 0 capability skips, GREEN on the frozen reference AND on the candidate in both Date modes; identical law inventory and carve-outs (8 superset exemptions, same seeds). Reproduced here. |
| 3 | GATE c (must) | Merge differential vs the real frozen bundle: 152 exact whole-state cases per mode (17 two-device scenarios, both directions, same batch replayed twice, two injected clocks; returns + post-call inputs + aliases + receipts). PASS in both modes. Reproduced here. |
| 4 | BITE (must) | Astra's bite (sessionLog routed through _richer) broke 4 named laws (convergence, correction-survival, non-shrink, session-superset; 34 violations). cowork's OWN, different bite — the keyed-array union takes LOCAL only, dropping remote-only records — was NOT caught by the 18 sync laws (all GREEN) and NOT by the port census (10/10; the census never calls merge); it WAS caught by the merge differential (first case: "two-device disjoint records AB"). The gate as a whole fails closed; see row 8. merge.cjs restored byte-for-byte (sha b69dd11f…). |
| 5 | FIDELITY (must) | cowork's own line-by-line diff of the contiguous range app.jsx 13371–14452 (1,082 lines) against the candidate: exactly ONE differing line — _stampCorr's `new Date().toISOString()` → `clock.nowISO()` (the module-5-accepted seam). 14 header lines (E forwarders) + 4 footer lines (export) outside the range. Tracked merge-source.cjs agrees (42 declarations; modules 1–5 + oracle-shim byte-identical to base). |
| 6 | SUITE + REGRESSIONS | run.cjs CONSISTENT (99/99, 10 port ids, rig185 PASS/PASS); selftest PASS; check.mjs --strict PASS (no MEASURED_TEST_NOW); migrate-full, migrate-source, census-partial, defect-witnesses-5 all exit 0. Scope: 10 files under rebuild/engine + rebuild/m2 only; index.cjs registers merge LAST; frozen app, suite, client, authority, M3, soak untouched. |
| 7 | DEFECT LOG | D37–D40 preserved, 4/4 reproduced on frozen AND candidate in both modes: D37 a record-dated merge receipt prints different ± prose by wall day (the mint reaches typicalError's default as-of); D38 merge drops writer-shaped trial accept/decline rows (no union key); D39 a stale replica resurrects a dismissed volume offer; D40 equal-richness same-day dailyLogs tie resolves by direction (documented local-wins). Next number: D41. |
| 8 | COVERAGE FINDING (cowork) | The frozen sync laws' 59 seeds never populate waist / photos / caffLog / medsLog (the MERGE_ARR keys other than reads, which mergeState re-unions separately), so a merge that drops remote-only entries in those arrays is invisible to the law gate. Consequence for BRIEF-7 and the second gate: the merge differential stays MANDATORY (it is), and suite v4 gets an item "seed every MERGE_ARR key in sync-laws". Not a candidate defect — a harness gap, proven by execution. |
| 9 | BRIEF-7 REVIEW | Read in full. ACCEPTED with the annotation in row 8. Boundary rulings accepted as proposed: (i) the ghSync/snapshotMaybe legacy transport carrier is TEST-ONLY, mocked I/O with a hard-failing tripwire, bound to candidate E, never exported from index.cjs/oracle-shim, never touching real credentials; (ii) the pure draft/preference/ask/day helpers engine-test calls (askContext, writeDaily, captureAsk, mergeSessionDrafts, …) are copied exactly and DISCLOSED as an expansion — no React, storage or network; (iii) the lazy-Date clock bridge in the second-gate adapter is a harness bridge, not the native-Date proof (writers-differential carries that proof); (iv) acceptance count = the freshly executed reference's FINAL108 N with M=0, not RECON's estimate; zero capability skips in sync-laws; surface baseline byte-compared, never re-written. |
| 10 | COST | Astra: ≈10.5 min recorded between checkpoints (setup excluded); cowork verification ≈25 min. |
Verdict: module 6 (merge) ACCEPTED for merge. THE 2026-09-11 CHECKPOINT QUESTION ("is the engine's migrate/merge ready?") is answered YES on 2026-09-05, six days early — both halves extracted and proven identical on the owner's live ledger. Per PLAN-M3-v1 the real port goes in week 2; M2 itself still needs module 7 + the second gate + the post-extraction audit before the port runs on real data.
