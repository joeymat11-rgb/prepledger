# M2 MODULE 7 — SCORECARD (cowork, 2026-09-05; rebuild/m2-engine-7 @ ba92a2c, base 7bbc510; re-run on a fresh checkout with the locally regenerated private live blob + golden)
| # | Item | Result |
|---|------|--------|
| 1 | SECOND GATE (must) | `second-gate.mjs --reference` and `--candidate` both run here: engine-test FINAL108 **3072 passed, 0 failed** on the frozen reference AND on the candidate (same N, same executed-site multiset); vacuity 9 known hits, baseline matched; sync-laws 18/59/0 skips on both; engine-surface **byte-identical to the committed baseline (123,077 bytes)** on both; I/O tripwire PASS (zero delegated requests). Proven from the build artifacts, not the report: the candidate bundle's source map lists ONLY rebuild/engine/*.cjs + the test adapter/carrier (no src/app.jsx); the reference bundle lists ONLY src/app.jsx. |
| 2 | FULL ORACLE (must) | Port-oracle `check` 10/10 GREEN on all three blobs (incl. PRIVATE live) in both Date modes. Writers did not disturb migrate. |
| 3 | WRITERS DIFFERENTIAL (must) | 84 scenarios / 314 call cuts / 27 methods, two clocks, in frozen, native AND trap modes; NATIVE TRAP PASS — zero ambient now/random/storage attempts, including swallowed catches. Reproduced here. |
| 4 | BITE (must) | Astra's bite (runAdaptive note title) → engine-test 196/199 RED. cowork's OWN, different bite — a landed debut no longer advances the working weight (completeSession, app.jsx 2619) — → second gate RED at engine-test.jsx:176 ("rows debut banked, weight advanced") AND writers-differential RED; the census stayed 10/10 GREEN as expected (it never calls writers — exactly why the second gate exists, audit F5). writers.cjs restored byte-for-byte (sha 8b4cc404…); clean candidate gate re-run PASS. |
| 5 | FIDELITY (must) | cowork's own block-by-block diff of all 90 copied declarations (2,594 source lines) against app.jsx @ fe516c1: 29 lines differ and every one is a clock substitution of the six disclosed shapes (nowISO / nowMs / hour / dow / today) — 30 occurrences, line 10120 carries two; ZERO other differences. Tracked writers-source.cjs agrees; modules 1–6 + oracle-shim byte-identical to base; merge-source's only edit is the index expectation (writers registered last). |
| 6 | CUMULATIVE + SUITE | merge-laws, merge-differential, migrate-full, migrate-differential, census-partial, defect-witnesses 5/6/7 all exit 0; run.cjs CONSISTENT (99/99, 10 port ids, rig185 PASS/PASS); selftest PASS; check.mjs --strict PASS (no MEASURED_TEST_NOW / PL_ENGINE / PL_LAWS_LIB). Scope: 14 files under rebuild/engine + rebuild/m2; frozen app, tools, suite, client, authority, M3, soak untouched; nothing private tracked. |
| 7 | BOUNDARIES (as accepted in BRIEF-7) | Test-only legacy carrier (ghSync/snapshotMaybe/agentToolExec/restore markers) covers 17 carrier-or-mixed assertions with mocked I/O and an external tripwire — never exported from index.cjs/oracle-shim; pure helper expansion (askContext, writeDaily, captureAsk, draft/preference helpers) copied exactly and disclosed; the July lazy-clock bridge is a harness bridge only (the trap mode is the native-Date proof). Assertion classification: 2,629 runtime-derived · 69 capability/mixed · 337 source/asset inspections · 9 mixed · 17 carrier · 11 harness = 3,072 — "3,072 assertions" ≠ 3,072 candidate paths (disclosed). |
| 8 | DEFECT LOG | D41–D45 preserved, 5/5 reproduced on frozen AND candidate in both modes: D41 a queued debut/reset changes the scalar load without advancing the per-set vector; D42 undoing a break restores the plan but leaves the scale seal active for three more days; D43 an own/std completion stores the CONFIGURED load, not the ENTERED one (two records of one performance disagree); D44 a one-set give-back prints "VOLUME −1" and spends the budget while no set was removed; D45 the analyst context text says terminal RIR 0 blocks every earn while the writer awards one. Next number: D46. |
| 9 | COST | Astra: ≈10 min recorded between checkpoints (setup excluded); cowork verification ≈40 min (two full second-gate runs + bite + diff). |
Verdict: module 7 ACCEPTED for merge. **THE M2 EXTRACTION STEP IS COMPLETE**: all seven modules extracted, both gates GREEN (the census on three blobs × two clocks AND the complete second gate re-pointed at the extracted engine), every module's copy independently diffed by cowork, every module bitten by both builder and cowork. M2 CLOSES only after the post-extraction audit: D1–D45 → red-first laws → owner rulings → fixes in the modules, never in the frozen app.
