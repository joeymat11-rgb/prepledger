# Issued task text (verbatim, as pasted into Codex on 2026-09-05; committed for OPEN-SOURCES) — branch rebuild/m2-audit-register

You are ASTRA. The M2 EXTRACTION STEP is COMPLETE (modules 1–7 accepted; rebuild/m2/SCORECARD-M2-7.md). M2 now CLOSES only through the POST-EXTRACTION AUDIT: every preserved defect → a red-first law → an owner ruling → a fix in the module (never in the frozen app). Your task is the technical half of that audit. Branch: rebuild/m2-audit-register from the rebuild/t2-client-core tip that carries the module-7 merge (record that SHA). Open ONE PR. Do not merge. No product code changes in this PR.

Deliver TWO things:

1. rebuild/m2/AUDIT-REGISTER.md — one entry per preserved defect D1–D45 (sources: the defect logs in REPORT-M2-1…7 and the tracked defect-witnesses 1–7 runners), grouped by THEME (e.g. clock/as-of staleness; cache identity; counts-only guards; merge tie/identity; receipt prose vs actual effect; scalar vs per-set load; undo half-effects; analyst text vs writer). For EACH defect, exactly these fields:
   (a) PLAIN — one sentence, no jargon, that a non-programmer reads as "the app does X when it should do Y" (or "the app does X; whether that is right is the owner's call").
   (b) EVIDENCE — app.jsx lines @ fe516c1, the module file + lines now, the witness file/case name.
   (c) BAR — which of the four-part bar it meets, if any: lost fact / applied without consent / untrue value or receipt the athlete sees / hidden commit — or "none: cosmetic / internal / performance". Be strict; a receipt that prints a number that is not what happened is UNTRUE VALUE.
   (d) LIVE — does the owner's private live ledger actually trigger this defect TODAY? Run the witness's predicate against the locally regenerated private blob (AGENTS.md preparation) and report ONLY "TRIGGERED / NOT TRIGGERED / NOT APPLICABLE (needs two devices | a future write | …)" — no values, counts, dates or receipt text, ever.
   (e) LAW — the proposed red-first law for suite v4: id in the existing style, family (policy / progression / authority / client / engine), the one-sentence claim, and what its mutant is. It must be RED on the frozen engine AND on the extracted engine today (identical behaviour) and GREEN only after the fix.
   (f) FIX — where (module file + function), what changes in one sentence, estimated hours, and any risk of touching a frozen receipt string or the port oracle's golden.
   (g) RECOMMENDATION — exactly one of: FIX (a plain bug; no training-rule judgement involved) · OWNER RULING (a training or product rule is at stake — state the question in ONE sentence with the two answers and your recommended one) · KEEP (documented behaviour, write it into the sheet/appendix as is) · DEFER-M4 (real but not needed for the owner's port or beta). Justify in one line. Do not soften a FIX into a RULING to avoid a decision, and do not turn a rule into a FIX to avoid asking.
   End with a SUMMARY TABLE: id · theme · bar · live · recommendation · fix hours — and totals per recommendation.

2. rebuild/conform/v4/ — the red-first LAW SEEDS as executable code, NOT wired into run.cjs and touching nothing under rebuild/conform/laws/, oracle/, run.cjs or the manifest: one law file per theme in the existing law-file shape (id, cite, expect, run, mutant), plus rebuild/conform/v4/run-defect-laws.cjs that runs every law against ENGINE_MAIN (the frozen bundle) AND against rebuild/engine (the candidate) and prints, per law, RED-frozen / RED-candidate / mutant-DETECTED — the proof that each law is red-first and strong. Every one of the 45 must be RED on both engines today (identical behaviour is the whole point of the extraction); a law that is GREEN today is either mis-specified or the defect is not real — say which, do not delete it silently. Synthetic states only; the private blob never enters a committed law or fixture.

Report rebuild/m2/REPORT-AUDIT-ASTRA.md: what/why, the runner's full output (45 lines + totals), the summary table copied, seams (which laws needed a synthetic that the frozen engine only reaches through a writer; which "LIVE" answers are NOT APPLICABLE and why), what is unsure, and the wall-clock. cowork verifies by execution (every law RED on both engines, every mutant detected, the LIVE column re-run locally), then turns the register into the owner's ruling pack. Nothing private in chat, reports or commits. No suite edits, no product-rule changes, no fixes in this PR.
