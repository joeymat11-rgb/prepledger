# BRIEF-W5-R1 — complete reconciliation, lease history and device recovery

## 0. Assignment, authority and boundaries
ASTRA technical brief v1, published as a PROPOSED contract for independent review; no implementation/acceptance or new owner ruling. This docs-only branch is `rebuild/m3-r1-brief`, created from verified integration base `947369caeb9fea2d6bcb04b0447857de35ba887b`. The coordinator retains dispatch ownership; this publication starts no implementation stream.
Reference W5 is accepted `9dd8dae3e0955dee079e91a2d0f426cdaac95a9a`, integrated at `cb5580a3c3b778e614127026a3769d383f07611b`; both are ancestors of this publication base. Source and rule claims below were rechecked against that base on 2026-09-06, including accepted BRIEF-W6 v1.1 integrated at `df09f438a93cb9548ef3f66b28b39deecc7fb347`. A later implementation records its actual accepted base and rechecks W5 compatibility.
Read W5 WIRE/bridge/worker/auth/crypto/public-client/schema, authority admit/index/store/README, runtime sheet v1.7.38 A2:114–149 and B11/12/17–20, PLAN-M3-v1 §3/§6 PHONE-RESTORE/KEYS-RECOVERY, and accepted BRIEF-W6 v1.1. The earlier W5 scratch proposal and the two R1 preparation notes are evidence, not extra authority.
Deliver ONE useful R1 implementation PR: immutable issued-lease history, actual enrollment/renewal, complete guarded reconciliation, original-device recovery query/replay, public verification and real local D1/HTTP tests. A retained-state diagnostic alone does not finish R1. No merge; cowork verifies by execution.
R1 produces authenticated server evidence. Client checkpoint C, CLOCK, state-loss fence K1, finite time bounds T1, P1 custody, remote deployment, phone recovery and private use remain separate gates; no R1 result alone grants permission, refills counters, proves elapsed time or clears learned negative state.
Allowed implementation paths after this brief/amendment is independently accepted: `rebuild/m3/w5/` (new reconciliation code/migration/fixtures/tests and narrow bridge/worker/crypto changes), `rebuild/m3/rigs/rig-r1.cjs`, `rebuild/m3/REPORT-R1-ASTRA.md`; ONLY the explicit optional resolver in `rebuild/authority/index.cjs` and `admit.cjs` below. No W6/client, existing laws/oracle/manifest/shared runner, frozen app, private fixture or seeded soak edits. Use a separately coordinated registration change if the shared runner is later needed.

## 1. Required behavior and the narrow T3 amendment
R1-D1: every retained account fact needed to reconstruct authority state, accepted 1..W, original requested outcomes, device/range/lease lineage and current app standing comes from ONE guarded global-revision snapshot. No cross-account data, mixed pages, invented history or fake completeness.
R1-D2: issued leases are immutable and resolve by original athlete/device/lease ID for admission AND WAITING drain; renewal never revokes older capabilities or changes existing operations/receipts. New writes retain gap-free device counters; original late delivery still admits under the unchanged rules.
R1-D3: a new same-account phone enrolls with a fresh device identity, restores retained history and can query/relay surviving old-device envelopes without modifying identity, HMAC, lease, sequence or predecessor. Missing unsynced bytes remain lost/unknown, never reconstructed; revocation barriers still apply.
R1-D4: enrollment/renewal intent retries produce exactly one durable issuance, including lost replies and epoch races; current standing is separately checked before activation. Default T3/W5 behavior and original signing/canonical contracts stay exact outside the explicitly added capability.
REVIEW-REQUIRED two-file core seam: `createAuthority(config)` forwards optional `config.resolveIssuedLease` into `makeAdmission`; signature `(tx, athleteId, deviceId, leaseId) -> lease | undefined`, synchronous/read-only. Omission retains EXACT `device && device.lease` behavior at admit.cjs:42–43. With it, require the original historical metadata device, resolve the requested lease inside that transaction, then execute every existing verification/range/schema/barrier/admission step unchanged. Promise/invalid resolver return is unavailable, never a fallback to an unrelated lease.
W5 resolver reads `tx.get("issuedLeases", JSON.stringify([deviceId,leaseId]))?.lease`; absent is LEASE_UNKNOWN. Both initial `decide` and all `drainWaiting` reconsiderations use it. No temporary metadata lease swap: automatic drain can contain several leases for the same device, and core metadata writes could persist the wrong pointer. No generated-source rewrite, async I/O or writer-order change.
W5 crypto currently accepts one signing kid. Add default-compatible `{activeSigningKey,verificationKeys}` input at its existing platform boundary; single-key input remains exact. Sign with active only; verify exact signature kid against unique pinned public P-256 keys, rejecting duplicate/missing pins and malformed inputs. Old public pins remain while historical leases/receipts need verification; old private keys are unnecessary. Preserve low-S P1363, old domains and canonical/HMAC bytes. Account identity-HMAC rotation remains P1's separate seam.
Every old signed surface, including pull/snapshot/time, obtains key_epoch from the actual active signer, never a stale authorityKey.kid alias. Test those routes after a signing-key rotation while historical leases and dispositions still verify under retained public pins; signing-key rotation alone changes no device issuance counter or lease.
Source constraints: operations/history include rejected slot contenders outside `dispositionHistory`'s incumbent lookup; identity-collision replies can be unretained; a returned WAITING may precede later drain completion. Query final stored records directly without admission/drain. Current `/enrol` and `/lease` are lookups; the new handlers below are explicit additions.

## 2. Database, issuer and account/device lifecycle
Migration `rebuild/m3/w5/migrations/0002_reconciliation.sql`: add collections within authority_rows, scoped uniqueness and JSON/integer constraints; immutable-row UPDATE/DELETE guards for issuedLeases, issuanceIntents, enrollmentIntents and standingEvents. Existing global batch therefore loads all dependencies. Keep subject→athlete uniqueness; no new secret material in rows, signing responses or logs.
`accountRegistry/state = {profile,account_epoch,state:ACTIVE|CLOSED,history_origin}`. This is the APP account's authoritative lifecycle, not a claim about unseen Clerk session changes. Trusted W4 provisioning creates only authorized subject mappings; unknown subject is denied, never auto-creates an empty replacement account after missing/restored state. Closure is an authenticated administrative/application action, tested synthetically; no new public deletion UI is required here. Token/session expiry alone never closes an app account; fresh authentication alone never reopens CLOSED.
Bridge deployment option `reconciliationProfile:"earned/r1/v1"` requires a valid account registry/issuance profile on EVERY old and new scoped route; missing/corrupt registry refuses UNAVAILABLE, CLOSED refuses state17. Default option absent preserves the old W5 fixture contract; production R1 deployment pins it ON and tests that a removed registry cannot fall back to legacy admission. This is a server configuration, never request-selectable. Legacy incomplete-history accounts do not pass complete proof and cannot silently become PROFILE_GENESIS.
`deviceIssuance/<deviceId> = {device_id,creation_epoch,current_lease_id,issue_ordinal}`. Historical device IDs never disappear/recur. Metadata devices retain the current creation lease for default callers. Core revocations remain the source of device barriers; a wrapped revoke appends standing evidence in the SAME guarded commit, preserving the exact existing warning/barrier. Never revoke an old device automatically when another enrolls.
Device counter profile PROPOSED: first enrollment sets creation_epoch=1 and issue_ordinal=1; each genuinely new successful renewal advances both by exactly1 in the same guarded issuance commit. An exact intent retry advances neither; two different intents with the same expected epoch have one winner. Neither replay/query nor signing-key rotation advances them. Counters are safe positive integers; a new issuance that would exceed either counter ceiling refuses with409 ISSUANCE_EXHAUSTED, never wraps or silently restarts. Exact durable intent replay still returns its original result without consuming counters. These are per-device issuer counters, not local checkpoint C or a permission-reset signal.
`issuedLeases/[deviceId,leaseId] = {lease,lease_bytes_b64,issuer_profile,issue_ordinal,issuance_intent_digest,account_epoch,creation_epoch}`; exact signed lease bytes and parsed object must agree. `issuanceIntents/[deviceId,intentId] = {stable_request_digest,lease_id,result_creation_epoch}`; `enrollmentIntents/<intentId> = {stable_request_digest,device_id,lease_id}`. Immutable `standingEvents/<eventId>` binds event kind, account/device, epoch and original revocation/closure evidence; unknown kinds refuse profile parsing.
Create production mapping/account registry/device/lease/history together under this profile BEFORE any production op: history_origin=PROFILE_GENESIS gives complete issued history. Legacy metadata-only accounts require independently verified complete imported issuance evidence; otherwise HISTORY_INCOMPLETE. An OBSERVED_CURRENT backfill cannot prove no earlier leases existed. Do not manufacture history or seek an automatic owner waiver.
Issuer engineering profile PROPOSED for review: schema1, first range `[1,1048576]`, each successful new renewal intent extends hi by1048576, capped at Number.MAX_SAFE_INTEGER; lower bound remains1. Calculate the capped increase without unsafe intermediate integer arithmetic; if hi is already at the ceiling, new issuance refuses with409 ISSUANCE_EXHAUSTED without wrap or changes to any durable intent/device/lease row. These are finite authorization capacities, not 64, operation allocation, training rules or a loss bound.
Ranges for successive leases on ONE device may overlap. Disjoint reservations would jump unused device_seq slots, contradicting A2:116–117's gap-free acknowledged sequence. Slot identity uniqueness remains unchanged; the sanctioned client preserves nextSeq/predecessor. Never derive nextSeq from range start/end, max accepted, max slot or W. After detected restore/conflict with unprovable allocation, enroll a fresh device for future writes and preserve old envelopes separately; no renumbering.
The sheet explicitly requires `not_after ≥ the C3 interval, 30 days` (A2:132–134); the accepted W6 brief preserves that minimum issued term. Issuer samples its configured server clock once per new issuance, sets not_before to that sample and not_after=max(sample+30days, prior issued not_after); nonfinite/unrepresentable issuer samples or computed endpoint overflow return500 CLOCK_UNAVAILABLE with no issuance/domain-row effects. Emit the canonical UTC format accepted by W5's lease verifier; an exact durable intent replay needs no new issuance sample. Never shorten an old lease. This defines issuance bytes ONLY, not actual UTC error/rate/freshness or client [Tlo,Thi]; T1 remains OPEN.
All enrollment/renewal/closure/revocation and reconciliation use the existing global-revision guard. Stale work retries fresh, duplicate intents return the durable result, and no issuance response precedes the atomic commit. Historical delivery never checks arrival expiry; currently revoked source operations still meet the original barrier.

## 3. Closed HTTP contract and authenticated byte profiles
Every route is POST JSON, no query; retain original pinned issuer/audience/azp/Origin/JWKS auth and no-store/CDN/Vary headers. No caller keys/discovery or request-selected athlete/subject. Original `/op`, `/enrol`, `/lease`, `/pull`, `/snapshot`, `/time` stay backward-compatible; keep import/restore typed NOT_IMPLEMENTED. Actor and account derive from verified subject plus the applicable enrolled-device gate below.
NEW route controls use strict UTF-8/JSON: reject duplicate decoded keys, unknown fields, unsafe control integers and noncanonical unpadded base64url. Do not silently alter old route parsing/default behavior. Original envelope numeric/shape/HMAC rules remain T3's. Old routes retain262144-byte request maximum; proposed new R1 routes allow1048576 encoded bytes, streamed and bounded, so base64 wrapping can relay an original maximum-size W5 operation. A larger combined claim request returns413, never truncates or claims complete coverage. Control identifiers use W5's exact regex `^[A-Za-z0-9][A-Za-z0-9_.:-]{0,127}$`; nonce/context IDs are independently generated32 random bytes base64url; supported schemas/profiles are pinned, not client assertions.
| New route | Exact request fields | Scope and result |
|---|---|---|
| `/enrol/create` | `{intent_id,schema_version,nonce}` | Verified existing ACTIVE account, no pre-existing actor required; generate never-reused server device ID and first lease. Persist intent/device/history atomically; return signed historical enrollment result plus current standing. |
| `/lease/renew` | `{device_id,intent_id,expected_creation_epoch,expected_lease_id,schema_version,nonce}` | Current ACTIVE account/non-revoked enrolled actor. Existing exact intent lookup precedes now-advanced creation preconditions; new intent requires expected epoch/pointer and atomically appends lease/switches pointer. |
| `/reconcile` | `{device_id,request_b64,continuation,page_index}` | Current non-revoked enrolled actor; decoded request below selects CURRENT_DEVICE or ACCOUNT_RECOVERY. Initial continuation=null/page_index0; later requests repeat exact request bytes and original signed manifest. |
| `/recovery/replay` | `{device_id,envelope_b64,nonce}` | Current ACTIVE actor may relay unchanged historical same-account source envelope; ordinary `/op` remains strict same-device. Core evaluates original source lease/HMAC/range/barrier, no privileged bypass. |
Decoded reconcile request exactly `{version:"earned/reconcile-request/v1",mode:"CURRENT_DEVICE"|"ACCOUNT_RECOVERY",nonce,context_id,claims,requested_lease_ids}`. Claim `{claim_id,envelope_b64}` retains original operation UTF-8 bytes. Lease request `{source_device_id,lease_id}` is a lookup, not issuance evidence. Reject duplicate claim IDs/identical-envelope duplicates/identical lease pairs; distinct competing envelopes remain separate claims even when op_id/slot collide.
CURRENT_DEVICE requires every claim/lease source equal actor; ACCOUNT_RECOVERY permits historical source devices within the same mapped athlete, even revoked ones. Mixed/foreign account source rejects the whole request. Every envelope's athlete is checked against auth-derived scope; actor authority never edits embedded source identity. Proofs bind actor, mode and each original claim/source; no unretained outbox is invented.
Stable enrollment/renewal intent digests encode all fixed request fields EXCEPT nonce in the route-table field order using UTF-8 JSON.stringify of the reconstructed closed control DTO; retry uses a fresh challenge with unchanged fixed fields. Same intent/different content refuses. Authenticate disclosure first; after an intervening revoke/closure, original issuance remains immutable but current standing prevents activation. Two distinct renewal intents racing one epoch produce one winner and typed STALE_CREATION, not two fresh leases. A retry must not fail merely because its OWN success advanced the epoch.
New signature domains: `earned/enrollment-result/v1`, `earned/lease-renewal-result/v1`, `earned/recovery-replay-result/v1`, `earned/reconcile-manifest/v1`, `earned/reconcile-page/v1`; retain every old domain. All signatures use existing P-256 low-S profile and pinned public verifier, no phone private authority key. The alternate `reconciliation-manifest/page` spellings are not accepted aliases; fixtures prove wrong-domain verification fails.
Nonpaged result envelope unsigned fields exactly `{profile,key_epoch,scope_digest,nonce,intent_digest,payload_digest,payload_bytes,data_b64}` then authority_signature; profile equals that route's enrollment/renewal/replay signature domain above. Intent digest for replay is the exact original-envelope byte hash; it is not a new operation identity. Payload enrollment/renewal is `{issuance,current_standing}`, issuance being the exact issuedLeases record schema above; replay is `{original_envelope_digest,outcome,disposition_bytes_b64,current_standing}`. Replay outcome is DISPOSITION or ENVELOPE_MISMATCH; the latter requires null disposition bytes and remains unresolved, never a drain-eligible reply. Current standing exactly `{account_epoch,account_state,actor_device_id,actor_revoked,creation_epoch,current_lease_id}`; no external-provider-active assertion. Serialize fixed field order in fixtures.
A stored issuance replay may return an old lease while the separate current pointer differs. Treat historical issuance and current activation as different claims. Any issuance time/error bound, recovered key availability and local Saved acknowledgment are deliberately absent from these response meanings.
Byte hash `D(tag,bytes)=SHA256(UTF8(tag+"\0") || bytes)`, encoded canonical unpadded base64url. Tags: `earned/reconcile-request-bytes/v1`, `earned/reconcile-payload-bytes/v1`, `earned/reconcile-page-bytes/v1`, `earned/reconcile-manifest-bytes/v1`, `earned/reconcile-coverage/v1`, `earned/r1-intent-bytes/v1`, `earned/r1-result-bytes/v1`, `earned/r1-operation-bytes/v1`. Digests bind exact bytes without old canonical number/string normalization.
scope_digest=D(`earned/reconcile-scope/v1`, tuple), where tuple is issuer, subject, origin, athlete, actor-device in that order, each uint32 big-endian UTF-8 byte length then exact bytes; reject overflow. For enroll, actor-device is the newly issued device. New signed outer envelopes contain only closed ASCII strings/base64url, null where declared, booleans where declared and safe integers; original records travel as exact byte payloads. Preserve original HMAC/canonical/receipt/signature bytes inside them.

### Complete guarded read and paging
Add `bridge.reconcileScoped(subject,device,request,expectedPayloadDigest?)`: in ONE existing read/stage/guard loop load global revision, all rows and subject mapping, establish current scope, project complete account data and guard revision before returning. No separate scope/log/lease/history calls, no writers/drain during read. Assert empty authority/subject domain delta; the existing revision-control increment is permitted. Never publish global revision or foreign owner/row metadata.
Fixed payload order `{profile,scope,coverage,claims,accepted,devices,leases,standing,retained_rows}`. scope binds actor/athlete/mode and scope_digest. accepted contains W plus all original log rows1..W. claims are in request order. devices includes historical identities, creation pointers, lastAccepted maxima and exact revocation barriers as separate fields. leases is the complete immutable issued inventory plus one lookup answer per requested pair. standing includes app account epoch/state and device event lineage; no secrets/raw subject/token.
retained_rows is EVERY same-account row in the CLOSED collection profile: metadata (including initialPlan), operations, slots, history, ownership, log, lastAccepted, revocations, transactions, undo, suspensions, issuances, instances, applies, accountRegistry, deviceIssuance, issuedLeases, issuanceIntents, enrollmentIntents, standingEvents. Unknown collection refuses PROFILE_UNSUPPORTED rather than silently omitting it or exposing an unforeseen secret. Subject mapping is not dumped.
Row DTO `{collection,row_id,value_b64}` carries exact persisted value JSON-text UTF-8 bytes, in unsigned UTF-8 collection/row-ID order. All derived DTO objects have fixture-pinned field order; arrays have declared numeric/byte order; encode using UTF-8 JSON.stringify, never canonicalEncode on payload bytes. Verify row shape/parsed values; malformed persisted data refuses instead of repair. Include initial plan and transaction/issuance state so a complete log is not mistaken for complete reconstruction inputs.
coverage exactly `{authority_facts:"retained-at-one-snapshot",accepted:"complete-1-through-W",retained_rows:"complete-profile-collections",issued_leases:"complete-from-profile-genesis"|"verified-import"|"incomplete",history_origin}`. HISTORY_INCOMPLETE cannot produce successful R1-COMPLETE proof. Original historical collision replies never retained are outside retained coverage, explicitly named; no claim of all messages ever sent.
Each claim result contains `{claim_id,requested_envelope_digest,source_device_id,device_seq,op_id,commitment,outcome,envelope_match,stored_operation_row,history_rows,slot_incumbent}` with null where absent. Outcomes KNOWN_TERMINAL/KNOWN_WAITING carry original stored disposition/history; IDENTITY_CONFLICT is derived evidence, not a recreated missing reply; UNKNOWN_AT_SNAPSHOT is not rejection or permission to discard/recreate. Include rejected contenders outside incumbent slot history.
Match original identity/commitment AND full parsed JSON values (string code points/array order exact; object key order irrelevant). Unequal full values with matching commitment yield ENVELOPE_MISMATCH and stay unresolved; do not “fix” T3 canonical aliases. Do not claim original transport whitespace/key order was stored. Current disposition comes from the snapshot after any prior drain, never stale cached admission return.
Validate contiguous accepted1..W, historyCount/history membership, operation/disposition/commitment links, slot incumbents and complete issued lineage/current pointer; reject omissions/inconsistencies as RETAINED_INTEGRITY. No nextSeq is inferred from W/max slot/max accepted/range. Client restores original pending bytes or reports known unknowns; a fresh device starts its OWN seq1/predecessor null.
Manifest unsigned fields exactly `{profile:"earned/reconcile-manifest/v1",key_epoch,scope_digest,nonce,context_id,request_digest,snapshot_id,payload_digest,payload_bytes,page_bytes:32768,page_count,coverage_digest}` then authority_signature. snapshot_id is32 random bytes, not revision/date; request digest uses decoded request bytes; coverage digest uses exact encoded coverage bytes.
Page unsigned fields `{profile:"earned/reconcile-page/v1",key_epoch,manifest_digest,index,offset,bytes,page_digest,data_b64}` then authority_signature. manifest_digest hashes EXACT original signed manifest JSON bytes. 200 result `{manifest_b64,page}`; continuation is original manifest_b64. page_count=ceil(payload_bytes/32768), offset=index*32768, bytes=min(32768,payload_bytes-offset); zero-based indexes and exact lengths.
Transport cap PROPOSED for engineering review: complete serialized payload≤16777216 bytes (16MiB), hence≤512 pages, page bytes32768; streamed request/response readers reject above declared bounds before unbounded accumulation. This limits one account-proof assembly's raw allocation/CPU and is not an account/history limit, truncation permission or loss bound; decoded/runtime memory can exceed raw size and must be measured. Above cap returns413 RECONCILE_LIMIT, no complete proof. A larger/paged-storage profile requires review; do not quietly raise or split a checkpoint.
Stateless content-pinned reread/restart: each page reauthenticates, verifies continuation binding, takes one fresh guarded complete snapshot and re-encodes it. Identical full payload digest permits indexed bytes from the original content; changed own-account content yields409 SNAPSHOT_CHANGED/no page. This proves stable content, not identical observation time/revision across pages. Actor revocation refuses even if old content verifies; unrelated global changes do not expose revision or force change if scoped bytes remain identical.
Client verifies pinned manifest/page signatures, signed key_epoch equal actual signature kid, scope/nonce/context/mode/basis/session epoch, exact offsets/length/count/claim correspondence, all page indexes and final whole-payload digest before positive completion or any frontier/outbox sink. Identical duplicate page is idempotent; conflicting duplicate, missing/skipped/truncated page or mixed manifest refuses completion. Out-of-order arrival may be buffered by index; missing indexes never count complete. Consume one outstanding nonce once; restart uses a fresh nonce and discards the incomplete positive assembly.
Public API proposed `createR1Verifier({keys,subtle})` returns async pure `verifyResult(resultBytes,expected)`, `verifyManifest(manifestBytes,expected)`, `verifyPage(pageBytes,manifestBytes,expected)` and `assemble(manifestBytes,pageBytesByIndex,requestBytes,expected)`; expected closes route/profile/scope/nonce/context with captured structural basis digest/session epoch, not JavaScript object identity. Return `{verified:true,value}` or `{verified:false,code}`, no storage/UI/transport effects. Independently verified byte copies are owned; no object-identity grants or truthy Promise acceptance. W6 owns durable K1 observation, nonce consumption, final current-basis check and atomic sink; this interface does not pretend to implement them.
First enrollment has no preexisting device identity: capture trusted issuer, authenticated subject, allowed origin, nonce and intent before sending. Verify the outer response with already pinned authority keys and exact payload framing/digest, then derive athlete/device IDs from its signed issuance lease and verify that lease. Require issuance/embedded-lease IDs and all repeated athlete/actor-device identities to agree; recompute scope_digest from captured issuer/subject/origin plus returned athlete/device. Any already-known athlete must match. Current standing may carry a later creation epoch/current lease than an exact historical enrollment-intent replay; validate that distinction without changing the immutable issuance or mistaking its old epoch for activation. Exact nonce/intent correspondence remains mandatory. A signed mixed-device response fails. Only the server's existing ACTIVE subject→athlete mapping authorizes this assignment; verification establishes neither recovered keys, checkpoint C nor activation through unresolved standing/K1/T1/P1.
Authenticated terminal negatives from a page/status/header cannot be discarded just because a positive assembly's basis became stale. Production ingress must use published K1 capture/replay; opaque/partial bytes are not defined as unobserved by this brief. A fresh actor nonce binds correspondence only, not UTC freshness or C. After relaunch, lost challenge state invalidates that attempt; a valid proof never clears independently learned17/19/20 or unproven integrity18.

### Original-source recovery and errors
New phone B's actor identity authorizes account recovery; old A envelopes remain A's operations. `/recovery/replay` is a REVIEW-REQUIRED narrow HTTP authorization addition: a dedicated internal bridge method verifies B's current enrollment/account and A's historical membership, then calls `authority.admit(authDerivedAthlete,originalEnvelope)` inside the same guarded staged snapshot. Do not pass a caller override into ordinary invokeScoped/admit or weaken `/op`'s same-device check. Preserve inner source device/schema/lease/HMAC and every stored receipt byte; never “admin accept.”
Before admission within that same snapshot, use the reconciliation full-value matcher against a known source operation. Matching identity/commitment with a different full parsed envelope returns signed unresolved ENVELOPE_MISMATCH with null disposition bytes; do not call admission or return the cached terminal result. The unchanged core returns a known terminal result before full-envelope revalidation, so query-only checks are insufficient: a synthetic composed/decomposed Unicode note has the same canonical commitment but different parsed values. Add this exact negative to recovery replay, plus byte-exact legitimate replay, while preserving ordinary `/op` behavior and original canonical rules.
A non-revoked A envelope may arrive late; revoked A beyond its barrier is rejected even through B; exact existing terminal replay is unchanged. B's creation counter/chain never absorbs A slots; W6 must keep any surviving old-source replay inventory separate. Without surviving unknown A bytes, B restores only authority-retained facts and reports loss/uncertainty per B12. No promised rejected-item vault beyond actually retained data (B19).
Typed errors `{error:{code,state?,retryable}}`: existing auth401/state11 and scope403/state17 retained; new400 INVALID_R1_REQUEST,409 INTENT_CONFLICT|STALE_CREATION|SNAPSHOT_CHANGED|ISSUANCE_EXHAUSTED,413 RECONCILE_LIMIT,503 UNAVAILABLE,409 HISTORY_INCOMPLETE,500 RETAINED_INTEGRITY|CLOCK_UNAVAILABLE,409 PROFILE_UNSUPPORTED. ISSUANCE_EXHAUSTED has retryable:false until a separately reviewed issuer profile changes the exhausted capacity; CLOCK_UNAVAILABLE has retryable:true with bounded backoff and the same fixed intent. Both omit state: they are server issuance refusals, not evidence of state20, expiry, client clock continuity or server freshness. No response drains outbox; only authenticated core terminal dispositions govern terminal removal. New409/413 codes do not invent a product state or override known17/18/19/20. Preserve original route error shapes for old callers.

## 4. Executable gate, mutations and report
Publish `reconciliation/{contract-v1.json,codec.cjs,project.cjs,verify.cjs,issuer.cjs}`, migration0002, `fixtures/reconciliation-v1.json`, focused tests and standalone `rebuild/m3/rigs/rig-r1.cjs --env local`. Contract JSON closes every field/enum/byte order/limit; fixtures contain synthetic byte vectors, no committed keys. Generated-per-run issuer/auth keys; existing pinned Wrangler4.129.0 local D1 and actual loopback HTTP on Windows/Linux, no account/token/external service after dependency installation.
All new tests are NOT RUN. Required output `R1-COMPLETE PASS` covers R1-D1–D4 on fresh profile-genesis synthetic accounts and negative legacy-incomplete fixtures. Owner-phone remains PENDING; synthetic-remote is BLOCKED exit2 while no actual remote database exists, with its actual recorded reason. No local simulation earns either gate.
T01 default core exact parity/34 authority laws; optional resolver old leaseA→renewB→lateA, WAITING A→currentB/C→parent→drain; interleaved devices/leases; unknown/forged/schema/range/revoked leases retain original dispositions. Rotate signing key, verify old capabilities/receipts; original operation HMAC and bytes unchanged.
T02 enroll/renew atomicity at every read/guard/row/commit/reply cut; lost reply same intent returns one original issuance; conflicting intent content refuses; two intents one expected epoch yield one winner; revoked/closed retry never reactivates historical result. Corrupt/duplicate immutable rows refuse. Exercise the final valid counter/range endpoints and first invalid issuance:409 ISSUANCE_EXHAUSTED; nonfinite/unrepresentable issuer samples and computed endpoint overflow:500 CLOCK_UNAVAILABLE. Require exact error/retryable fields, absent state, no issuance/domain-row effect and successful replay of a previously durable intent at exhaustion. Default range tests prove no jump, unsafe arithmetic, wrap or counter reset.
T03 actual new-device recovery: A accepted+unsynced/WAITING work; B authenticates/enrolls fresh, restores complete accepted/initial-plan/transaction history, queries and relays surviving exact A bytes, then commits B:1/null predecessor. Repeat with A revoked, B revoked mid-pages, no surviving A copy and missing local keys. Replay B's original enrollment intent after a later renewal: original issuance bytes remain valid historical evidence, current standing names the later epoch/lease, and neither is silently substituted for the other. Preserve barrier; no foreign data, invented missing fact or false local recovery/permission result.
T04 one guarded complete snapshot under100 independent invocations, including ownership changes and WAITING drain; no mixed basis; read domain delta empty. Global revision increments are control only. Same-account mid-page change restarts; unrelated-account change does not reveal foreign state. Actor/source swaps, foreign references, forged issuer/audience/origin/key and body athlete selectors all refuse.
T05 full retained operations/history including rejected contenders outside slot history; known WAITING→terminal, UNKNOWN, commitment collision with unretained prior reply, canonical alias ENVELOPE_MISMATCH. Missing/duplicated accepted position/history/lease/pointer or omitted initialPlan/transaction inventory fails. Never infer complete issued history from observed current metadata.
T06 exact byte/signature/hash vectors, Unicode/number/duplicate-decoded-key boundaries; duplicate identical page retry, conflicting duplicate, skipped/omitted/truncated/out-of-order assembly, mixed request/manifest/context/nonce and consumed nonce. Only complete verified bytes can call a fake positive sink; negative evidence is surfaced to the K1 seam instead of swallowed as stale data.
T07 boundary payload exactly16MiB accepted if otherwise valid; +1 byte typed413/no truncation/no fake complete proof; new1MiB/old256KiB request caps and streaming limits; an originally valid maximum-size W5 operation fits recovery's encoded wrapper. Measure maximum temporary server/browser allocation separately. Infinite retry/accumulation is forbidden. Existing history remains stored/readable through its original APIs when this proof cap blocks.
T08 fake CLOSED/ACTIVE, lease-pointer, nonce or token refresh cannot create C or reset allowance; no issuer date is treated as T1 finite accuracy. No P1 key availability is inferred from lease success. Remote/phone/current knowledge-loss blockers stay separately visible.
Bites in disposable copies: disable resolver during WAITING drain; make intent include fresh nonce; omit retained rejected contender/issued lease/initialPlan; skip actor/source account binding; disable page completeness. Each named negative must fail independently, not merely when server and verifier share the same omission. Restore each source byte-for-byte and quote RED plus SHA256. No inert mutation counts.
Regression: existing AUTH-D1 (34 mapped laws/10 effective breaks/100 races), HTTP-190 (5 real HTTP scenarios), frozen conformance CONSISTENT and SELFTEST under documented pinned ENGINE_MAIN/ENGINE_OLD, MEASURED_TEST_NOW=2026-09-03/TZ=America/New_York; strict with MEASURED_TEST_NOW unset. Include default single-key byte vectors and unchanged default-core trace witnesses; no edits to original laws or golden data.
Report `REPORT-R1-ASTRA.md`: what/why; actual base/source pins; exact schemas/wire/API table; full gate and effective-bite/restored hashes; immutable-history and old-device recovery evidence; source semantics changed only at accepted optional resolver/new auth routes; resource measurements; local/remote/phone distinctions; K1/T1/P1/D2 remaining blocks; wall-clock; NEXT. PR ends with R1-COMPLETE, AUTH-D1, HTTP-190, suite SUMMARY and strict tail; no merge.

## 5. Estimates, missing policy and dispatch
Engineering estimates are separate from observed agent wall-time and exclude remote setup/phone/K1/T1/P1 implementation. No calendar promise is implied by these ranges; review/test findings may change them.
| Component | Owner | Hours | Depends on |
|---|---|---:|---|
| Contract/fixtures and reviewed allowlist | A, C checks | 2–3 | Acceptance of this brief/resolver/recovery binding |
| Core optional resolver/default parity + W5 keyring | A | 2–4 | Contract |
| D1 immutable history, enrollment and renewal | A | 5–8 | Resolver/keyring, migration |
| Complete projection, byte codec, public verifier and paging | A | 4–6 | Contract; joins history before completion |
| Actor/source recovery relay and new-device scenarios | A | 2–4 | Enrollment, history, full proof |
| Actual D1/HTTP races, crashes, limits and effective bites | A | 5–8 | Combined implementation |
| Independent execution/reviewer response/report | C + A | 3–4 | Candidate gates |
Total23–37 engineering hours; do not add a second diagnostic-only PR to that path. Independent pure codec/projection work can overlap issuer work if the coordinator later assigns separate paths; currently no new implementation claim is issued.
Targeted tracked search rechecked at `947369caeb9fea2d6bcb04b0447857de35ba887b` found runtime:599 and PLAN-M3-v1:68 referring to a D2 recovery/erasure contract, client README's generic `clear(collection)` erasure capability, and knowledge-fence discussions. It found NO accepted sign-out retention/erasure matrix or owner choice in the ledger/plan/roadmap/backend packet/client-authority READMEs/GOALS/NEXT. M2's approved defect D2 is unrelated to privacy D2 and is not that ruling. This is a scoped repository finding, not proof that no off-repo ruling exists.
Coordinator checks for an already recorded external ruling first. If none, the smallest owner choice is: keep encrypted local history/pending entries locked for same-account recovery after sign-out (recommended), or erase the local copy after an explicit unsynced-loss warning. Do not silently choose or ask twice; account-deletion/erasure obligations need the same supplied matrix. This blocks the corresponding private sign-out behavior, not R1 construction/tests under synthetic fixtures; no question is being sent by this brief.
This publication adds only this brief and `REPORT-R1-BRIEF-ASTRA.md` on the recorded docs branch. Independent review/acceptance and any merge follow the current M2 receipt sequence so this branch cannot contaminate that package's pinned receipt-base allowlist. Following acceptance of this exact combined contract and two-file amendment, the coordinator issues one implementation claim/branch from a freshly verified integration tip and arranges W6 consumption of the published API. Do not claim C/CLOCK or private readiness merely because R1-COMPLETE passes.
