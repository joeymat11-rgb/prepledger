# W6 v1.1 — browser storage, recovery and bounded offline saving

## Message65 shared edit/history adoption — pre-edit boundary, September10

APM affected-boundary acceptance received September10 in the existing task01a077e8: exact amendment and complete Message65 readback read; implementation scope accepted as written, carrying all eight relevant limits. This is not runtime qualification or new issuance. Product has implemented the declared candidate and its affected checks below; coherent independent runtime/integrator acceptance remains required.

Base W6 48ddeeba, M4 3ecb8fc, R1 98c1405. Manager's CORRECTED-REVIEW-READBACK.md in APM acceleration-trial/WORKOUT-EDIT-SEMANTICS-01 records scoped ACCEPT of9e14919's four corrections and WORKOUT-HISTORY-JOINv0.2. Adopt those semantics in the actual retained history path; complete schema issuance/private activation remain forbidden until their existing gates close. This amendment requires the existing APM affected-boundary readback before runtime edits.

Exact source allowlist: new workout/edit-history.cjs and workout/edit-values.cjs; existing workout/schema.cjs, authority-profile.cjs, commands.cjs, stored-history.mjs, project-history.mjs, continuation.mjs; M4 engine-history.cjs and engine-order.cjs where corrected Start context/liveness affects ordering and correspondence; affected current public-client.mjs history/edit/prepare consumers only if they assume direct Set targets or old projection fields; existing focused tests, actual client/R1/native recovery composition and exact shared-module source inventories. No T2/authority-core/issuer/source-intent/codec/SQL/engine/law/seed change. Reached extra functions require an explicit amendment rather than scope inference.

edit-values.cjs provides one closed effective-time and recording/replacement domain implementation for shape, target-specific relations and interpretation. Preserve old recorded numeric/reserve domains. Only replacements permit exact {clear:true} for optional reserve/reason/logical_set_slot. Validate nested replacement_fields against the actual original target kind, and final Skip scope/slot after all live patches. Reject an edit's own non-session class. Traverse target chains through the existing known-operation dependency reader, enforcing athlete/schema identity, cycles/missing references and conditional lift lineage: required exactly for rooted Set/Skip, forbidden for Start/Close. Shape may validate the union of closed replacements; relations must reject variants invalid for the actual target. No caller target-kind hint or recorded-reserve relaxation.

edit-history.cjs consumes typed operation rows with explicit accepted receipt positions and local/rejected/unresolved status assembled from the SAME generation; it has no authentication flag or raw-wire-string adapter. stored-history keeps existing sameRecordedValue proof comparisons and complete all-class prefix verification before normalization. Unique IDs/positions and causal/target dependencies are checked; actual original objects/strings remain distinct from derived current values. Identity equality remains the existing typed comparator, never JSON.stringify described as wire custody. Non-session originals and applicable legacy-class target chains are retained; unknown legacy capture/lineage/context remain explicit unresolved mapping, not guessed eligibility or global corrupt-storage labels.

One shared fold interprets accepted corrections/removals, nested edit replacement and restoration, per-root containment, accepted concurrent ordering and immutable original capture. A separately labelled local view uses that same fold only where pending dependencies/order are proved; incomparable pending effects stay unresolved, with the accepted view independently available. Never assign pending athlete-log positions. Unknown status/rejected originals stay visible and cannot establish current guidance. The adapter supplies original/effective Start, Set, Skip and Close data plus all effect identities to display/resume and the M4 factual reader. M4 compares to the same generation's entire membership/status and shared interpretation; no independent patch fold. Original prescription/layout stays immutable; corrected Start context informs existing candidate-component rules while source order remains causal/accepted. Removed Start children remain reachable without usable live session context. Ambiguous completion/slot relationships cannot become completed work or permission.

Required evidence: actual commands and durable W6 reopening for Start date/slot correction, Close correction/removal, Skip correction/removal, reserve clearing without recorded-clear acceptance, nested correction/removal/restoration, accepted concurrent ties versus unresolved pending ties, rejected edits, immutable original capture and unrelated later records; actual relation/admission WAITING and invalid target/class/schema/lineage cases. Exercise actual source-generation comparison and existing async revision/token/standing refusals, same shared M4 factual projection, signed recovery where supported, old schema1 bytes and explicit unqualified legacy mapping. Preserve existing synthetic/unissued limits until genuine full-schema issuance exists. Full conformance/selftest/strict and affected independent/integrator gates apply. Current UI/font source is included in the next coherent candidate, not a separate repeat review.

## Approved local typography — September10, base05c38cf

Extend the retained presentation cut with the exact owner-approved Instrument Sans/Serif fonts, loaded from pinned public upstream files and embedded into the actual compiled browser bundle with OFL notices. Source scope: workout/fonts public files/manifest, workout/typography.mjs, mount calls in the two existing panels, build-browser.mjs's exact asset loader and existing native prepared-panel checks. Install static font CSS once per document; no account, data, permission, storage or engine state. Build only the explicitly pinned font/license paths; no general arbitrary binary/text importer or runtime CDN request. Verify font decoding with networking disabled, actual mounted rendering and existing 320/390px/enlarged-text behavior. Keep font bytes/licensing/source metadata reviewable and preserve the unchanged current edit-review packet. This is approved design integration, not complete offline-app, private/phone or product acceptance.

## Approved workout presentation integration — September10, base667e34d

Owner-approved Refinement A and additions C, as assigned in M4 BRIEF-OWNER-WORKOUT and CURRENT-PRODUCT-DECISIONS, authorize this bounded retained-host presentation work. Source scope: workout/prepared-panel.mjs, command-panel.mjs and their existing browser tests/reports; no T2, public-client, repository, schema, authority, engine, issuance or currentness logic change. Preserve the exact original capture/current assessment, action permissions, actual-acknowledgment-only save transitions and pending/refused inputs. Show current-lift set positions and exact recorded/skipped versus pending state from the host's interpreted continuation or acknowledged action; a temporary display copy is not a history or permission reducer. Keep original per-set instructions separate from entered results, leave inputs/effort unrecorded until chosen, and reveal saved feedback/next action after a real commit. Relevant current safety differences remain foreground; optional original/current detail stays available. No invented previous-performance comparisons, timer, Undo, arithmetic, defaults or personal qualification. Synthetic disclosure remains visible in this not-yet-qualified host. Existing full workout round-trip and native save/failure/lifecycle checks plus matching phone-width/enlarged-text review apply. The separate9e14919 edit-review packet and its fixed inputs remain unchanged.

## Prior selected-source inspection — local lineage join

The current source/native engine reproduction needs the previous selected material at the same verified cut. Add only guarded profile.sourceSelection(intentId) and candidate.inspectSelectedSource(intentId,visitor): read the already validated source-selection row and its exact source material from the SAME complete signed inventory, with the original inventory/local revision/token/current-context checks before and after every asynchronous access and visitor. An uploaded but unselected source or unknown selection is not selectable through this API. Return copies, no mutation, selection change, activation, clock or permission. Existing current-source and v1/v2 archive semantics remain unchanged; no T2/R1/issuer/wire/schema change.

M4 source-reading lineage reproduces a prior native local_json from its predecessor source and the authenticated accepted checkpoint cut, then compares exact bytes. Later corrections/removals of inherited facts reconstruct the earlier effects before replaying the recorded merge; new native facts after that cut follow it. Rollback uses the target activation's original material/checkpoint, never the rollback request's later before. Existing full-current producer/consent/atomic/private/science/device/resource gates remain; this inactive accessor grants none of them. Actual second-import/accepted-edit/rollback/pending/reopen evidence and retained guard/default checks are required in the same candidate.

## Factual-reading T2 join — pre-edit amendment reviewed; local candidate

APM bounded amendment review, September9: the exact seam is justified and implementation may proceed within this disclosed scope; this is not full candidate acceptance. Required added condition: face.missingDates consumes readings and affects machine board/paceCurrent and exposure of old snapshot values. Its new projection-enabled path must use qualified accepted dates; pending presence/removal/correction may suppress but MUST NOT restore stale/re-entry machine output. Exercise this through actual face code. Default seam-absent behavior stays exact. This receipt resolves pre-edit amendment review, not the larger independent/integrator gates.

Current base76b714d, R1 98c1405, M4 source-projection witness. Actual signed source recovery/reopen retains a phone-B reading but the current face drops it because client/index.cjs reads() enumerates ownOps(). PLAN-M3:70 requires the full remote projection. M4 IMPORT-CONTROLLER-v1.md v1.4 records the reached witness and the distinct source/accepted/pending layers. The narrow core seam below was independently reviewed by APM before its implementation, as recorded above.

Proposed touched core: only index.cjs reading projection dependency/read APIs and face.cjs factual-reading display. Retain the entire old path when the trusted integration is absent. A static synchronous reading projector, supplied by the configured T2/W6 adapter rather than renderer input, receives copies of the ACTUAL model's operations, dispositions, receipts/frontier, outbox and rejected records. It returns a closed factual read view with original identity/provenance, resolved observations or explicit unresolved state, plus accepted versus local-pending status. It cannot supply plan/lease/currentness/clock/coverage/permission or machine outputs. Malformed projection refuses the staged view before durable publication; it never silently falls back to ownOps and drops remote facts.

W6 supplies this projector only after authenticating the same generation's signed remote originals and all remaining local identities. New local operations are still constructed by the original committer and inspected at the final W6 batch/schema/durability checks. Do not accept a caller DTO or verified:true flag. Synchronous projection uses current model copies, not a precomputed view that misses the just-created operation. Async signature/IDB work remains outside T2. Existing late context/revision/token and final transaction checks remain mandatory.

Every original reading/edit/removal remains accessible; same-date or concurrent-edit ambiguity is explicit and supplies no invented winning quantity. Pending changes remain Layer1 and may suppress dependent machine outputs only. The accepted engine input set excludes pending edits/effects until actual admission. No date-only deduplication, own-device-only filtering, receipt-order-as-edit-causality, historical-source targets as consent or new duplicate advice store. Source-to-engine coverage and current policy remain separate actual joins, never an extra erasable marker that clears recovery.

Required scoped evidence: current remote-reading RED becomes GREEN through actual workerd/D1/P1 source recovery and public reopen; original/remote/local correction and removal provenance, unresolved concurrent and same-date states, failed identity/signature/current-generation checks, newly saved local input immediately visible after durable commit, pending suppression without new machine calculations, default T2 path byte/behavior parity, and original conformance. Keep the source-current/rollback/qualified app objective whole. No issuer/schema/admission/installed engine/frozen law change is implied. APM completed that pre-edit review through the existing coordination lane. Subsequent bounded reading review also required effects on earlier dates to withdraw dependent old computations even when a newer date remains. The factual-only projector cannot extend snapshot freshness: without source/effect coverage, accepted new dates do not prove the old engine snapshot current. Pending facts and accepted/pending effects suppress direct trend/rate/maintenance/pace and use the existing fact:reading dependency filter; unrelated declared outputs remain. This is an interim truthful boundary, not completion of the qualified producer.

## Local signed source recovery amendment — September9

Compose retained R1 source320b899 and its source codec successor with W6's existing protocol-injected encrypted recovery stage. New rows-v4 MUST receive the explicit configured source codec and execute its shared indexed source rules; sourceImports cannot pass as opaque rows. Preserve original source/candidate/checkpoint/context bytes, full accepted prefix and selection/rollback binding. Expose source material only through the same checked inactive recovery handle, after signed complete inventory, relational validation and local comparison; no activation/permission or current engine projection is implied.

Use a versioned local archive proof for rows-v4 that records its inventory profile. Retained v1/rows-v3 archives keep their original schema/meaning and are read through an explicitly configured protocol registry; never reinterpret an old proof as v4 or silently omit an unsupported archive. Original recovery-source/plan/receipt behavior, exact original bodies, pending operations and final revision/token/context fencing remain. No T2 client, issuer, current-state producer or engine-policy edits in this amendment. Existing W6-local staging seams may compose source evidence without changing accepted core semantics. No private port/merge/deploy/science/K1/full IMPORT-ROLLBACK acceptance; independent review remains for the coherent candidate.

Actual mixed-version execution found the live v3 page head cannot be decoded as v4. Version only the v4 live-head key inside the SAME generations store; retain immutable archive record/AAD format and the ORIGINAL SHARED transport-budget control. A protocol transition must not erase history or reset the recovery attempt allowance.

## Active local import custody amendment — September9

Under the current owner-workout/controller brief and M4 spec/IMPORT-CONTROLLER-v1.md atcf56b90, the W6 candidate now adds named inactive encrypted source/checkpoint custody to the existing generations store. See REPORT-W6-ASTRA.md's current section for exact API, local profile, source pins and executed evidence. This is an implementation candidate awaiting the coherent independent review, not an accepted wire/storage/recovery profile. No T2, issuer, authority inventory, P1 wire encoding or active-state format changes are included. The local custody record is separate from rotating active/previous and provides no activation, consent, export coverage, K1 or rollback verdict. All original broader gates remain; actual accepted source/activation and full recovery coverage are next. This section narrows the current edit scope; it grants no private port, merge or release permission.

## 0. Authority, purpose and scope

For the owner: this work makes “Saved” mean the entry and its sending queue finished saving together. After the app restarts without internet, the allowance remains **24 observed hours and 64 new operation slots since its last reconciled connection**, whichever runs out first. A weigh-in uses one slot; a multi-part action can use several. Reopening does not refill the allowance. At the limit, or after a detected clock rollback invalidates that allowance, the app keeps the entered values and asks you to reconnect. The recorded rollback default is refusal until reconnect; the owner may override it.
Version 1.1 implements Sol's recorded **ADOPT-WITH-CHANGES** verdict, [SOL-W6-REFINEMENT-2026-09-06.md](SOL-W6-REFINEMENT-2026-09-06.md) and DECISIONS at `cd99918`; cowork must verify this diff before marking W6-REFINEMENT DONE. The retained 64 slots are a **client-policy cap only, never a loss bound**. The owner already chose bounded offline saving over blanket refusal after restart (DECISIONS, 2026-09-05, accepted at `0a13921`). Do not ask that choice again. This is not a guarantee of 24 real hours or at most 64 lost entries: restoring an old, internally consistent copy can restore its old allowance too. That restore exposure is the accepted residual, not an open strict-versus-bounded decision.

Queue item W6-BRIEF publishes the instructions; **W6 implementation is not built here**. Builder ASTRA, future branch `rebuild/m3-w6-browser-bridge` from the then-current `rebuild/t2-client-core`; record its full base SHA and every published dependency SHA. One implementation PR, no merge. Version 1.0's authoring branch was `rebuild/m3-w6-brief`, starting from independently accepted W0 `b00057e` while its integration was being published. Version 1.1 is published on `rebuild/m3-w6-refinement-v1-1` from recorded verdict base `cd99918bea6e6a5b9c4d58a91e9985f7e345e71a`.
Inputs: PLAN-M3-v1 §§1–3/6 W6 (18 engineering hours including brief/review shares, not an additional estimate); accepted T2/T3 and W3 scorecards; `w0/CONTRACTS.md`, `w0/RIG-INVENTORY.md`; DECISIONS' bounded/acceleration/role rulings; runtime sheet v1.7.38 A2 and B11/17/18/19/20; the published W5 wire table when available. Newer recorded owner rulings govern the earlier plan's unconditional restart refusal.
Allowed future implementation: `rebuild/m3/w6/`, W6 cases/wrappers under `rebuild/m3/rigs/`, one `REPORT-W6-ASTRA.md`. Reuse the accepted T2 client through an explicit browser integration boundary; any necessary T2 product edit must first be narrowly disclosed in an amendment to this brief and independently reviewed. Do not silently duplicate the committer, change T3 semantics, edit the frozen suite or implement W7's screen/port here. Leave the seeded soak and W3 witness files unchanged.
Independent storage/staging implementation may continue from v1.0's published requirements; bind transport only to W5's published, commit-pinned contract. Final CLOCK acceptance binds to v1.1 and requires cowork's W6-REFINEMENT verification, sufficient published W5 time bounds and a proven knowledge-loss fence/recovery contract. W4 supplies real keys/account configuration for remote use; synthetic local keys are sufficient for local work. A local pass never completes W9's phone gates or W8's recovery drills.

### Narrow T2 integration amendment — conditional acceptance; four corrections published

[The concrete W6 amendment](w6/T2-AMENDMENT-PROPOSAL.md) pins W5's published callable API at `d44706123d4be8844db6f919a8238255b73e3fb2`. Cowork independently reviewed exact `2f789734d906fb3864ca747abfe6351455b732cc`: **ACCEPTS WITH FOUR CONCRETE CHANGES before T2 edits**. The published corrections require (1) invalid optional permission samples to refuse20 before Lease.check, (2) actual numeric/object/throw T2 sink handling with durable result/reason normalization, (3) structural candidate-scoped grants that allow repeated checks within one candidate, and (4) explicit reject-only/presealed-metadata limits with runtime epoch invalidation, never a CLOCK solution. The coordinator checks this correction diff against that conditional acceptance; cowork may confirm it in parallel, with no second owner approval.

The exact T2 allowlist remains `rebuild/client/index.cjs`, `rebuild/client/lease.cjs`, `rebuild/client/sync.cjs`, retaining all default behavior. W6-local dependencies are `@noble/hashes` 2.2.0 and esbuild 0.28.1, with only the declared browser crypto importers replaced. Implementation waits for the four corrections to be checked and the actual accepted W5 integration SHA; no T2 or dependency change has occurred in this docs update. Storage checkpoint `e934f9b1787fbe01dc241081594254b5caeb1d8a` remains unchanged review evidence. This amendment authorizes no product-rule change, new storage/key/privacy format, final-time authentication frame or fence profile; all CLOCK blockers remain.

## 1. Contracts to implement and fields still OPEN

| Boundary | Required W6 behavior / publication dependency |
|---|---|
| T2 committer | `client/store.cjs:68–76` and `client/index.cjs:133–148` are synchronous: returning a Promise from backend.commit still publishes Saved early. Load a revision, run the real client on an isolated candidate snapshot, finish async work, then atomically publish that candidate only after the durable transaction completes. |
| Durable transaction | Operations, outbox, device sequence/parent, inbound receipts, terminal/rejected ledger, frontier, keys/epochs, lease/checkpoint, budget/high-water and relevant standing/recovery metadata share one consistency boundary. Assert the loaded revision in that same transaction; stale work retries from a fresh snapshot without duplicate acknowledgement. |
| Time separation | Permission uses capability/budget evidence; athlete effective date/time/offset remains the athlete's attested value. `client/index.cjs:112,132,143` currently couples permission and timestamps through clock.now. Do not inject a conservative permission bound into every timestamp or silently alter operation identity/canonical bytes. Publish the minimal integration seam before implementing it. |
| Authority verification | P-256/SHA-256 public verification for dispositions, receipts/pulls, snapshots, leases and fresh challenge-bound time before invoking trusted T2 APIs. Preserve operation HMAC, identity/domain/canonical rules. Browser receives no authority signing secret. Exact encoding, signed domains, key epochs, paging and failure schemas are OPEN until W5 publishes them. |
| HTTP/auth | Use W5's actual op/pull/time/lease/enrol contract, authenticated Clerk subject + enrolled device, and its retry/error semantics. Endpoint spelling, bodies, proof fields, versioning and test vectors are OPEN, not specified by this document. Never treat a retryable transport refusal or import/restore NOT-IMPLEMENTED as terminal success. |
| Reconciliation/renewal | Fresh signed time alone does not reconcile device sequence/head or standing. W5 must publish compatible history/lease renewal with outstanding immutable operations, including WAITING causal children; an empty outbox is not a prerequisite. Current `authority/admit.cjs:42–49` resolves one device lease: replacing it naively can strand old envelopes. This seam blocks renewal integration until proven. |
| Sealing/recovery | W6 publishes its local cipher/nonce/AAD/key-storage/migration profile and browser vectors; P1 payload keys stay separate from identity keys and authority private keys. Require fresh nonces, account/device/schema binding, old-epoch recovery and authenticated corruption detection. W4/W5 must supply the actual production custody/recovery contract before private use. A seal authenticates contents, not recency. |
| Projection | Preserve verified operations from other devices in the durable history/projection interface; `ownOps` alone is incomplete. W7 consumes this interface. Equal frontiers without the second device's fact are not a pass. |
| Browser durability | Prepare crypto/network outside an active IndexedDB transaction. Request strict durability where supported, record actual support; wait for transaction complete, never a request success callback. Reopen from disk in fault tests. [IndexedDB transaction lifecycle and durability](https://w3c.github.io/IndexedDB/#transaction-lifecycle) define the boundary, not proof against eviction or every device failure. |

W5-CONTRACT must include immutable commit/version, canonical/encoding fixtures, all signed bindings, challenge issue/consume/timeout behavior, explicit freshness/server-error/server-clock-rate assumptions sufficient for the conditional lease-time mapping below, history/sequence reconciliation, lease history, and typed retry/terminal errors. Copy that reference into REPORT-W6 before transport work; list missing fields rather than inventing a parallel wire protocol. W6 must not modify W5-owned runner/rig190/191 concurrently: add cases against its published registration contract in an integration-reviewed change.

### Bounded restart refinement v1.1 (retained numbers; four recorded corrections)

**Scope:** for an intact, enrolled, decryptable installation after unproved continuity, apply this allowance in addition to every issued lease/schema/sequence and standing constraint. Preserve the sheet's original lease term (A2:136–149, including the minimum 30-day issued capability); this shorter client restart allowance is a separate refinement, not an early server revocation. Qualified continuous-execution behavior retains its existing lease constraints. Count all new committed slots since the checkpoint, including those written before the restart, so restarting cannot create another pool.
**Checkpoint C:** after an authenticated, reconciled exchange, atomically store its identity/version, signed time/lease proofs, enrolled device and key/schema epochs, verified frontier/head, local pending lineage, next sequence, wall sample W0 and last persisted sample W_last=W0, nondecreasing observed-time high-water H=0, and used slots U=0. Define reconciliation against W5's published proof; it may preserve outstanding WAITING/unacknowledged operations with their original identities. A token refresh, successful GET /time alone, page load, install, clock edit or local snapshot restore never issues C.
**Observed elapsed:** on a bounded-path decision, sample UTC wall milliseconds W; reject nonfinite readings. A detected wall rollback `W < surviving W_last` explicitly INVALIDATES/EXHAUSTS the restart allowance: refuse state 20 by definition of that allowance, even when its numeric time/slot counters remain below their limits. This is not proof of lease expiry or an additional unstated refusal. The recorded default requires authenticated reconciled reconnect before a new allowance; the owner may override that default. Charge `H = max(previous H, W - W0, 0)` in milliseconds. After an unproved restart H is observed wall advance and can undercount elapsed/server-clock advance. A separately qualified same-execution elapsed upper bound or authenticated newer evidence may only tighten permission; it must not be claimed from an unqualified performance timer. Persist H and W_last=W with each acknowledged update atomically; preserve detected allowance invalidation through the same fence/recovery requirements as other learned refusal knowledge. Record/check proven expiry and recovery fencing as below, including failures to persist that knowledge.
**Lease-time mapping — CONDITIONAL:** checkpoint C also records the authenticated server-time interval [Tlo, Thi]. Only under W5's published freshness, server-error and server-clock-rate assumptions sufficient to justify this mapping may `max(surviving lease-time high-water, Thi + H)` be used as a bound against lease.not_after; those assumptions remain OPEN. At checkpoint creation require Tlo ≥ lease.not_before under that authenticated interval profile; persist the justified high-water with commits. After an unproved restart H is merely observed wall advance and can undercount; max preserves a surviving local bound but cannot repair undercount, server-clock steps/drift or stale/error-bound ambiguity. Raw Date.now is never compared directly with server lease UTC: a constant ±24h client skew cancels in W−W0; a later jump is handled below. Do not call Thi + H a certified real-time bound after restart. Independent authenticated proof of expiry still refuses20. **No CLOCK PASS until W5 states sufficient bounds**; retaining 24h/64 does not supply them or reopen the settled bounded-rule exposure below.
**Last valid / first invalid:** an allowance that has not been invalidated, with H ≤ 86,400,000 and U + N ≤ 64, permits a batch of N new operation slots only if every underlying lease endpoint also permits it. Exactly the final millisecond and slot 64 are valid; the following millisecond or slot 65 refuses state 20. Both ends of the issued sequence/time interval remain inclusive. A batch crossing any end refuses in full, consumes no sequence/budget, keeps entered values and says no Saved.
**What consumes the budget:** newly durably committed athlete operations, including explicit correction/replacement operations, consume slots. Receipt/pull processing, exact resend, rendering and token refresh do not. A multi-op session consumes its full operation count atomically; a declared durable draft follows its own mechanism and is not silently converted into an operation. A commit abort consumes nothing. Never reserve a smaller slot count by splitting an atomic action.
**Renewal:** reconcile acknowledged and pending history, device sequence/head, standing and valid issued capabilities; then activate a fresh checkpoint in one transaction. Preserve every original outbox envelope and previous valid lease proof needed for delivery. Do not renumber/re-sign pending operations or turn a WAITING parent relationship into a transport-sequence gap. After detected restore/conflict, reconcile or re-enrol under the published recovery contract before resetting counters.
**Why these numbers:** 24 observed hours permits an ordinary day between connections; 64 slots permits a modest Today workload without using private usage statistics. Neither is measured optimal. A smaller allowance reconnects more often; a larger allowance permits more unreconciled work. Sol retained both numbers; 64 is only a client-policy cap, not a server/global-loss bound. Cowork verifies v1.1 against the four recorded corrections before marking the refinement DONE. Do not silently relabel this operating allowance as a stronger security or loss bound.

### Limits that must remain visible in the refinement

- **W6-RESTORE-BOUND (existing W3 red witness):** a coherent old restore can replay the entire sealed checkpoint, budget and sequence. It can be indistinguishable from an ordinary restart offline and produce repeated conflicting Saved acknowledgements. A server-enforced finite range could limit distinct accepted slots, but cannot cap those repeated acknowledgements or all possible loss. This brief does not claim W5 implements such a range or that 64 is a server cap. Preserve the witness as the expressly accepted residual; this refinement does not reopen it.
- **Sol point 1 — settled bounded-rule exposure, verbatim:**

  > 1. Yes. BRIEF-W6 §1 "Observed elapsed" + "Lease-time mapping"; sheet A2:136–149 / C11. Take C with a fresh 30-day lease, U=0, H=0. Kill the process for 31 real days; before relaunch, wall time reads only W0+1h, still above surviving W_last=W0. The proposal derives H=1h, Thi+H < not_after, and permits Saved although the real lease expired. This is the settled bounded-rule exposure, not an argument for strict restart refusal.

- **W6-LATE-CREATION:** A2:138–149 admits legitimate work delivered after expiry. The authority rejects specified identity/binding/range/schema/revocation violations, and returns an original disposition for an identical accepted retry. It cannot use arrival time to prove an operation was created late. “Reject stale/replayed work” is not blanket arrival-time expiry. Previously valid saved work is never made invalid by shortening a client allowance.
- **W6-KNOWLEDGE-LOSS — HARD CLOCK BLOCKER (B17/B19/B20):** quota failure, then kill/restore, can erase newly learned expiry/revocation/rejection if it existed only in RAM. Once that knowledge was actually learned, losing it on failed persistence and later resuming is **not** the owner's “unproved elapsed time” exception. Atomic successful commits do not solve failed knowledge persistence. Require a demonstrated fence/recovery contract for authenticated responses and sign-out intent; separately retain an executed red witness for any locally observed expiry knowledge that cannot survive failure. CLOCK stays BLOCKED until that contract proves the required behavior; adoption of this bounded refinement cannot waive the blocker, and omitting the case cannot earn PASS.
- **Clock evidence:** performance.timeOrigin is not a trusted boot identity; persisted high-water is not elapsed time during process absence. [High Resolution Time](https://w3c.github.io/hr-time/#sec-monotonic-clock) does not provide this app an authenticated cross-reboot clock. W3's 1,000ms RTT and zero server error are synthetic premises, not production limits. W5 must publish sufficient freshness/server-error/rate assumptions for the conditional lease-time mapping; H can undercount after restart. A guessed tolerance cannot turn the original strict C11 witness green or earn CLOCK PASS.

## 2. Build sequence, recovery and state table

1. Pin sources and W5 fields; run unchanged W3 tests and a red-first early-Saved witness against the real synchronous T2 committer. Publish the proposed integration/time split and storage/sealing schema before adapting the boundary. Independent storage, signature and standing work may continue from v1.0's published requirements; CLOCK acceptance binds to v1.1, cowork's refinement verification and its remaining hard blockers.
2. Implement async staging and revision-guarded IndexedDB commit. Recheck standing, schema, both budget/lease endpoints and full batch range at the final transaction cut after crypto; callbacks/events may invalidate a candidate before completion. Publish model/acknowledgement only on complete. Tab/PWA races use stored revisions, not a same-process mutex. Document the unavoidable physical commit interval and its tested clock assumptions; do not claim a button-time check proves them.
3. Implement inbound validation and recovery fence: authenticated rejection removes optimistic contribution immediately in the current presentation, but failure to persist its disposition/rejected-ledger/outbox removal must keep truth blocked through relaunch. Current `client/sync.cjs:20,60–68` pendingRecovery is RAM; `index.cjs:192` revoke can return durable:false. A candidate mechanism durably arms a recovery-intent marker before an exchange/sign-out that can reveal loss of standing or rejection; disarm only with its authenticated outcome atomically committed. If arming fails, do not initiate that transition; handle unsolicited loss as an explicit fault. A lost response leaves the marker unresolved: retain entered values and require the specified recovery, never silently clear on boot.
   Explicit sign-out intent itself immediately establishes state17 and refuses new local writes, even if persisting its marker fails; withholding an SDK call must not leave the session writable. The marker's failed-persistence/relaunch problem still needs the demonstrated recovery contract, not a claim that this RAM refusal survives a crash.
4. Prove marker write failure, response loss, abort, complete-before-reply, repeated restart and offline relaunch behavior. State 18 applies when stored truth/integrity cannot be established, including unavailable decryption/recovery; state 17 applies when standing loss is independently known. Authenticated rejection is state 19 in the current process; after failed persistence and relaunch, the unresolved integrity/fence path is 18 until re-proven, not durable 19 recreated from lost RAM. No rejected contribution may resurface. No request, SDK event or local clock sample may claim its new knowledge survives a crash without evidence. If fencing every offline restart would reinstate blanket C1 refusal, record that conflict rather than implementing it unnoticed; unresolved W6-KNOWLEDGE-LOSS remains a HARD CLOCK blocker.
5. Implement migration and recovery using synthetic histories: atomic schema/key/checkpoint transitions with previous valid generation retained until success, no first-use inference from an empty store, no network/crypto awaits in upgrade transactions. Unsupported schema refuses truth/write as specified rather than resetting data. An old tab must close or refuse operations during an upgrade; race it in the rig.
6. Integrate the published public-signature/time/auth contract and two-device history. Run local faults first; remote fixtures only against the actual deployed W5; W9 uses a separate physical test origin with synthetic data. Never touch the idle soak or private installation. W6 adds storage/lifecycle harnesses, not the Gym UI.

| Case | Required state / visible and durable outcome |
|---|---|
| Valid original continuous evidence, or permitted bounded restart | Normal offline state 1 / existing online face; Saved only after complete; no claim of sync until authenticated durable outcome |
| Last valid / first invalid observed time AND slot | Inclusive valid edge; first invalid →20, no op/outbox/sequence advance; face and committer agree |
| Lease not-before/not-after or sequence lo/hi violated | 20 even if restart allowance remains; no retroactive rejection of previously valid saved work |
| Process kill/reboot with intact checkpoint, standing and allowance | Bounded refinement can permit; this is trusted local evidence under the refinement, not proved monotonic continuity; conditional lease-time mapping still applies |
| Whole coherent old restore | Offline may look valid; retain restore/collision red witness and disclose exposure; authenticated conflict/recovery on reconnect, never silent rewriting |
| Known stale lineage; missing/corrupt checkpoint/store | Recovery required; failed integrity →18 before athlete-data/plan paint, never automatic reseed or first use |
| Detected wall rollback W < surviving W_last, including a later −24h jump; roll-forward beyond cutoff | Rollback INVALIDATES/EXHAUSTS the allowance →20 until reconciled reconnect by its definition, even below numeric limits; not proof of expiry. Forward movement beyond the allowance cutoff exhausts it →20; inside-budget movement consumes observed time. Never lower surviving H or replenish budget; constant ±24h skew is distinct from a later jump |
| DST forward/backward; timezone change only | No change to UTC elapsed budget/sequence; preserve athlete local date/time/offset separately |
| Mere session expiry with intact enrollment/decryption and permission | 11: pause sync, allow durable leased/budgeted logging; fresh token does not reset allowance |
| Unavailable decryption/recovery; stored truth/integrity cannot be established | 18 before truth paint; never infer standing loss from this alone, reseed or call it first use; independently known standing loss follows the next row |
| Independently known standing loss: sign-out, revocation or closed account, including when decryption/recovery is also unavailable | 17 before20 for writes: refuse new writes, keep typed values, name resolution; renewed token alone insufficient; existing data follows recovery/erasure contract and unproved integrity still blocks truth paint |
| Authenticated rejection; its persistence fails, current process | 19 plus recovery fence in this process; no unverified frontier advance or outbox drain |
| Failed rejection persistence followed by relaunch | Unresolved integrity/fence path →18 until re-proven; cannot recreate durable 19 from lost RAM; no rejected fact resurfacing. Failure to prove this fence is the HARD W6-KNOWLEDGE-LOSS CLOCK blocker |
| Quota/abort with no higher-priority fault | 3; no Saved or ghost op; keep input; abort leaves prior durable generation, sequence and allowance intact |
| Standing/revision/expiry changes during async staging | Revalidate at commit; abort/retry correctly, no early acknowledgement; test changes while commit is pending |
| Late reconnect, renewed lease, lost acceptance reply, repeated batch | Original bytes/IDs replay; one effect/original disposition; retain immutable old capability proofs and valid pending work |
| Declared revocation barrier | Preserve accepted prefix; later barred work follows authority disposition; known loss of standing refuses17 regardless of remaining allowance |

Read precedence: integrity18 before standing17. Write precedence:17 before20 before3. Rejection19 is orthogonal; pending state7 can coexist with20. An invalid signature advances nothing and drains nothing; it is not an authenticated rejection. An unavailable network/SDK response alone proves neither explicit sign-out nor revocation.

## 3. Gate, existing assertions and required bite

Future commands in this section are **acceptance requirements, NOT implemented/passed by W6-BRIEF**. Use W5's shared runner `node rebuild/m3/rigs/run.cjs --case NAME --env ENV`; ENV is local, synthetic-remote, owner-phone or isolated-restore. Emit `NAME PASS/FAIL/BLOCKED/PENDING`; nonzero for FAIL, exit2 for BLOCKED/PENDING. Until W5 publishes the runner contract, entry registration stays OPEN. Name every unrun environment, rather than substituting a memory model.

| Case / done-line | Required executable evidence |
|---|---|
| IDB-187 → D2, C1/C2/C9 | Reuse `rebuild/t2/rig187.cjs` restart assertions through the async wrapper and real browser IndexedDB, not backend-env redirection alone. Seed/reopen full records/envelopes/outbox/rejection/history/keys/frontier/checkpoint; inject every save cut, missing store, quota and two-tab revision races. A Node fake-IDB/model pass is preliminary; browser and physical runs remain distinct. |
| MIGRATE → D2, C4 | Kill before/during/after schema and key migration; reopen fresh process, old tab races new tab, partial/old/empty generation. Preserve exact previous or complete new state; loss/corruption refuses18. |
| STANDING → D2, B11/17/18/19 | Every row above including arm-fence failure, sign-out, revoked account/device, decryption/recovery loss, session-only expiry control, rejection with failed persistence, renewed token with unresolved fence, and late response after account switch. Compare displayed permission and durable effects after full relaunch. |
| CLOCK → D2, C11 refinement v1.1 | Last/first time and sequence, entire multi-op batch, replay consuming no slots, both ±24h directions including a small legitimate rollback below numeric limits (allowance invalidation →20, not proof of expiry), DST both directions, TZ, kill/reboot/coherent restore, range exhaustion, late reconnect, renewal with outstanding WAITING/old lease, revocation, and expiry → persistence failure → kill → rollback. Report bounded cases separately from retained strict/restore red witnesses; pending cowork refinement verification, insufficient published W5 freshness/error/rate bounds, or unsolved W6-KNOWLEDGE-LOSS fence/recovery contract → BLOCKED. |
| SESSION-RESUME → D2, C8 | Harness starts session, saves set and declared draft; kill/background for one real hour, resume same durable session/op/outbox. No Gym screen required. Physical elapsed test cannot be accelerated to earn PASS. |
| HTTP/signature integration → D1/D2 foundations | Reuse W5 HTTP-190/rig190 vectors; forge each signed surface, wrong account/device/key epoch/challenge, stale/duplicate challenge and incompatible wire version. Before verified commit assert frontier/outbox unchanged. Prove second device's fact persists, not merely matching counters. |

One required disposable bite in the implementation: change the bridge to publish Saved before IndexedDB complete, delay then abort the real transaction, and require `IDB-187 FAIL` with the early-ack assertion. Restore byte-for-byte, record restored SHA and exact RED line, rerun the affected gate. Add a second narrowly focused mutant only if the primary witness does not exercise a different claimed safety boundary; inert mutations earn nothing.
Unchanged regression gates from repo root: `node --test rebuild/m3/clock-spike/test/continuity.test.mjs rebuild/m3/clock-spike/test/core-witness.test.mjs` (39 current assertions, red witnesses retained); `node rebuild/conform/run.cjs` → SUITE CONSISTENT and `--selftest` → SELFTEST PASS with explicit ENGINE_MAIN/ENGINE_OLD, MEASURED_TEST_NOW=2026-09-03 and TZ=America/New_York after AGENTS preparation; `node scripts/check.mjs --strict` → PASS with MEASURED_TEST_NOW unset. Run the accepted public rebuild CI on Linux and Windows using a **real node_modules directory** (`npm ci --include=dev`), not a symlink that changes package membership.
Before remote/phone acceptance W5/W4 integration must actually exist; no simulated remote PASS. The owner target is iPhone17Pro/iOS26.6.1 installed from Safari, separate test origin; record actual model/OS. WebKit's [storage policy](https://webkit.org/blog/14403/updates-to-storage-policy/) makes persistence a requested storage mode, not proof of survival. W9 must execute real disk/lifecycle cases; any survival-relevant new store needs its own qualifying evidence. Never transfer the seeded stub's future C3 verdict automatically to different storage.

## 4. Implementation report and handoff

Commit ONE `rebuild/m3/REPORT-W6-ASTRA.md`: what/why; full base/dependency SHAs and exact adopted refinement; local store/crypto/W5 wire compatibility table; gate verdicts by real boundary/environment; bite and restored SHA; seams/red witnesses; what remains unsure; wall-clock. Publish no private values, counts, dates, receipts or private hashes. Cite public code/fixtures and synthetic evidence only.
Keep W6-RESTORE-BOUND, W6-LATE-CREATION and W6-KNOWLEDGE-LOSS visible even when their witness assertions pass. Report which integration seams require approved core edits; original W3 tests and frozen laws stay unchanged. An acceptance matrix with a missing critical case is incomplete, not GREEN.
End report and PR with NEXT: what W6 actually unblocks (W7 integration, W9 physical testing), what remains dependent on W5/W4/refinement, and next proposed versus actually claimed queue ID. Cowork verifies by execution; Claude Code integrates only the verified commit and confirms both OS jobs on the integration commit. Owner attendance follows a prepared checklist; this brief itself needs no account/device action.

## 5. Never

- Never change the frozen app, engine rules, current conformance laws/reference/oracle/runner/manifest, accepted W3 witnesses, or `rebuild/m3/soak-stub/`.
- Never silently reseed, manufacture consent, rename/re-sign old operations, discard rejected-history knowledge, refill budgets locally, or use arrival-time expiry to reject legitimate late work.
- Never ship authority signing secrets to a phone; put tokens/private records in chat/git; use the owner's installation for destructive tests; claim a signature/checksum proves freshness.
- Never label a Promise, fake IndexedDB, uninterrupted-process test, copied soak receipt or simulated remote service as physical durability/continuity evidence.
- Never treat v1.1 publication as cowork's completed refinement verification, sufficient W5 time bounds or a proven knowledge-loss fence; bypass owner rule/spend/private-release decisions, merge this PR, duplicate W5/audit work, or change an unmet gate to meet a date.
