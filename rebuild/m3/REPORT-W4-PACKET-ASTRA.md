# W4 setup packet — ASTRA

Prepared 2026-09-06 on `rebuild/m3-w4-ready-packet`, base `df09f438a93cb9548ef3f66b28b39deecc7fb347`. This executes the accepted queue's W4-PACKET preparation; it implements no setup or product code and makes no new owner ruling.

The packet removes already-completed purchases and the seeded soak from the owner's work, identifies the remaining account and recovery choices, and gives the integrator a concrete setup sequence. It distinguishes public cloud integration from local private-secret custody, and database creation from deployment and remote acceptance. It records the actual helper execution refusal and expired local CLI session; neither was bypassed. The existing browser verifier and cloud integrator are separately working.

Read-only inspection found an unpinned Wrangler range behind version-pinned instructions, a nonportable `.bin` invocation, and a helper self-test that can overwrite an existing test variable. Proposed corrections require implementation and independent execution review. No token value, provider account, private ledger or phone was read for this packet.

## Executed checks

```text
FROZEN-PATHS PASS — pinned authorized base; committed and working copy
OLD-PACKAGE PASS — 18 allowlisted files; actual ZIP entries and bytes verified
SCOPE-FREEZE PENDING — new PWA archive, full private suite and final M3 implementation evidence remain release gates
```

`git diff --check` passed. The changed scope is two Markdown files. Runtime/private regression preparation was not repeated for a documentation-only change; the normal public Windows/Linux and strict CI remain required on the candidate. This report does not claim W4, secret handoff, US-CONFIG, deployed auth, billing or a phone gate passed.

## Seams and uncertainty

The packet contains proposed recovery objectives and a bounded billing-rehearsal option, not owner approval. The actual deployment credential route remains blocked pending ordinary authorized setup and a dummy-data test. The cloud integrator's successful public preflight does not authorize moving private credentials or data there. Production crypto configuration waits for W5/W6's published and verified contracts. No account provisioning or helper policy change occurred. Vendor documentation describes capabilities; it does not prove entitlement or successful setup on the owner's account.

Same-family preparation review corrected appointment ordering, loss-objective wording, billing-delay disclosure and route status. Cowork independently accepted `4f7ade2` as preparation after scope-package and source/vendor checks, with one correction: distinguish the designed owner-run helper from the denied direct agent invocation. This revision applies that distinction, removes presumed administrator remediation and leaves the untested owner-session result NOT RUN. Exact-diff acceptance is requested; no setup success is inferred.

## NEXT

Unblocks: W4-PACKET execution review, then the narrow setup correction and remaining O-PC/O-RECOVERY choices. W4 remains BLOCKED until its real prerequisites pass. Astra continues the already-claimed W6 storage slice and coordinates W5; this documentation creates no third implementation stream. Actual private import still requires all accepted pre-port gates and the owner's specific account/import approval.
