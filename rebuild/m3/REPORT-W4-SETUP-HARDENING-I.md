# REPORT — W4 SETUP HARDENING (integrator, "I") — 2026-09-06 (revision 3: R6/R7 + NAME-only fresh proof + local-auth prerequisite)

Executes `rebuild/m3/W4-READY-PACKET.md` §1 "Prepare and review the narrow setup correction" (and records the §2 authentication-reality prerequisite). Integrator-owned setup tooling only.
**Base:** `095ddf12f6aea70d6625a4ca6ac3c38bd0ec4084` (the accepted integration tip of `rebuild/t2-client-core` at branch time).
**Branch / PR:** `rebuild/m3-w4-setup-hardening` → PR #36 against `rebuild/t2-client-core`. **Not merged; cowork verifies the exact PR head.**
**Nothing here creates an account, a credential, a database or a deployment, or touches the frozen app, the suite, the workflows, the root lockfile, W5/W6 or the seeded soak.**

## 0. Review history (what each revision fixed)
**Revision 2 (Astra's five findings on head 435f145):** (1) cleanup ownership by EXACT value (`-ceq`), not marker prefix; same-prefix/different-value negative; (2) `$attempted` set before `Set`, `finally` cleanup; write-then-throw / cleanup-throw / read-back-failure tests; (3) fixed messages, no caller string echoed, sentinel tests; (4) fresh intended-process dummy proof; (5) built-from-scratch child env for the launcher e2e tests, disposable profile, no credential-shaped key, proxy variables named a tripwire (not interdiction).
**Revision 3 (Astra R6/R7 + cowork's protocol; this head):**
- **R6** — `store-secret-selftest.cmd` and `store-secret-freshproof-arm.cmd` now `pause` after the helper returns so the PASS/FAIL line stays visible on a double-click, and still return the helper's real exit code (`exit /b %RC%`).
- **R7** — `store-secret.cmd` no longer uses `set ARGS=%*` and a quoted comparison (broken by quoted arguments); the bare-double-click test is `if "%~1"=="" pause`.
- **NAME-only fresh proof (cowork's simpler protocol):** the dummy value of `EARNED_FRESHPROOF_<8 hex>` is now DERIVED from the name (`EARNED-FRESHPROOF-OWNED-<suffix>-<24 hex of sha256("earned-freshproof:"+suffix)>`), so the owner relays the NAME only; `--verify-fresh-proof [NAME]` and `--cleanup-fresh-proof [NAME]` recompute the exact expected value. Ownership stays EXACT (`-ceq` the derived value; a same-prefix value that is not the derived one is `foreign-value` and is never removed). NAME optional: verify auto-detects a single armed variable (two → `ambiguous`, pass the NAME); cleanup without NAME sweeps every owned variable and names anything foreign it kept. New double-click cleanup route `store-secret-freshproof-cleanup.cmd`. No raw argument is ever echoed (fixed messages; sentinel tests). Tested outcomes: fresh OK (NAME and auto), stale process (`not-visible-in-this-process`), differing process value, foreign value (verify / cleanup(NAME) / sweep), absent (`not-armed`), two armed (`ambiguous` + sweep), collision, exhaustion, arm write-then-throw cleanup, arm read-back mismatch.
- **Local-auth prerequisite recorded (W4-READY-PACKET §2):** the LOCAL Claude Code on the PC is 2.1.221; on 2026-09-06 `claude auth status --json` returned exit 1 / `loggedIn:false` (no credential file inspected, no account detail printed, no login attempted, no harmless request possible yet). SETUP-TOKEN §3 now names the fresh LOCAL integrator process as the one that runs the fresh-process proof, `whoami` and D1, requires the owner's ordinary sign-in plus one harmless real request BEFORE token creation / fresh-proof acceptance, and states that this cloud session cannot inherit the Windows USER environment and must never receive it. Bundled into the one PC appointment; no new owner request now.

## 1. Scope (exact files, all under `rebuild/m3`)
| file | change |
|---|---|
| `tooling/package.json` | `wrangler` pinned EXACTLY `4.129.0` (was `^4`). `.npmrc` unchanged (`package-lock=false`): a direct pin is not a transitive lock; only the wrangler package identity/version is guaranteed and checked. |
| `setup/wrangler.cjs` | NEW portable launcher: resolves `rebuild/m3/tooling/node_modules/wrangler/package.json` → its own `bin.wrangler` file (must stay inside the package dir), requires name `wrangler` and version exactly `4.129.0`, runs it via `process.execPath` with an argument ARRAY and `shell:false`, forwards exit codes, sets `WRANGLER_SEND_METRICS=false`, refuses `login`/`logout`. Never installs, fetches, discovers accounts, reads or prints credentials, or changes policy. `--launcher-check` resolves and spawns nothing. Exit codes: 2 usage · 3 not installed/malformed · 4 wrong version · 5 refused subcommand. **Unchanged since revision 2 (byte-identical to a6db2fa).** |
| `setup/store-secret.ps1` | Core behind a facade (`Get`/`Set`/`GetProcess`/`List`): two-name allowlist retained; masked entry retained; **no value, length or caller argument is ever printed**; no silent overwrite (Yes/No, value not shown); truthful outcomes `stored` / `unverified-readback-mismatch` / `unverified-readback-failed` / `set-failed`; exit codes 0/1/2/3/4/5; `--help`. Self-test: collision-checked owned `EARNED_SELFTEST_<random>`, `attempted` before write, `finally` exact-value cleanup. Fresh proof: `--arm-fresh-proof` (prints NAME), `--verify-fresh-proof [NAME]`, `--cleanup-fresh-proof [NAME]` with NAME-derived expected values (§0). `-AsLibrary` for tests. |
| `setup/store-secret.cmd` | Forwards `%*` unchanged and returns the helper's exit code; pauses on non-zero exit or when `"%~1"==""` (bare double-click); documents modes and exit codes; process-scoped `-ExecutionPolicy Bypass` = owner-run design (unchanged). |
| `setup/store-secret-selftest.cmd` | NEW double-click → `--selftest`; pauses so the line stays visible; returns the real exit code. |
| `setup/store-secret-freshproof-arm.cmd` | NEW double-click → `--arm-fresh-proof`; pauses; returns the real exit code. |
| `setup/store-secret-freshproof-cleanup.cmd` | NEW double-click → `--cleanup-fresh-proof` (sweep of exactly-owned dummies); pauses; returns the real exit code. |
| `setup/store-secret-tests.cmd` | NEW double-click → the PowerShell unit tests (in-memory facades only). |
| `setup/test/wrangler-launcher.test.cjs` | NEW `node --test` suite: core on dummy package facades; child-environment assertions; e2e on the REAL pinned install in the minimal isolated env + proxy tripwire. **Unchanged since revision 2.** |
| `setup/test/store-secret.tests.ps1` | NEW PowerShell tests (Pester-free) dot-sourcing the helper in library mode: parsing (incl. NAME-optional forms and no-echo sentinels); allowlist; derived value; store outcomes; self-test (clean, collision, exhaustion, same-prefix guard, hijack guard, throw-without-write, write-then-throw, cleanup-throw, read-back failure, read-back mismatch); fresh proof (fresh OK by NAME and auto, second cleanup, empty sweep, stale, differing, foreign ×3 + auto with only foreign, absent ×2, two armed ambiguous + sweep, collision, exhaustion, arm write-then-throw, arm read-back mismatch). |
| `SETUP-TOKEN.md` | §3: self-test FIRST → fresh-process DUMMY proof (NAME only; cleanup wrapper) → **local sign-in + one harmless request in the fresh LOCAL Claude Code** → real hand-over; fresh-process rule; "User environment is your Windows profile, NOT an encrypted vault"; no-echo statement; exact-value cleanup statement; honest test status. §4: launcher invocation; `whoami` = separate AUTHENTICATION proof. §2 permissions and TTL/rotation unchanged. |
| `SETUP-D1.md` | §0 exact pin + launcher + "not a transitive lock"; §2 `--launcher-check` step 0 and inspect/reuse-before-create; §4 abort conditions; `--jurisdiction=us` retained. **Unchanged since revision 1.** |
| `REPORT-W4-SETUP-HARDENING-I.md` | this file |

## 2. Executed here (Linux, Node 22, real `node_modules` directory, no credential in the environment)
| check | result |
|---|---|
| `npm install --prefix rebuild/m3/tooling --no-package-lock --include=dev` from the pinned manifest | wrangler **4.129.0** (38 packages; no lockfile written; root `package-lock.json` untouched) |
| `node --test rebuild/m3/setup/test/wrangler-launcher.test.cjs` | **17 pass, 0 fail, 0 skipped** (revision 2; the launcher and its test file are byte-identical in revision 3 — verified by `git diff a6db2fa HEAD` on those paths) |
| real launcher in the minimal env + proxy tripwire | `--launcher-check` → `wrangler-launcher OK 4.129.0 rebuild/m3/tooling/node_modules/wrangler/bin/wrangler.js` · `--version` → `4.129.0` · `--help` lists `d1` · `login` → refused, exit 5 |
| static checks on the helper (revision 3) | no `Length`/`chars`/`$len` output; no caller-string interpolation in messages; ownership by exact value only |
| `node rebuild/conform/run.cjs` (AGENTS preparation) · `--selftest` · `check.mjs --strict` | run on revisions 1–2 of this branch: `SUITE CONSISTENT — 99 reference GREEN · 99 STRONG · 29 RED-first against absent families · 70 GREEN against present families` · rig185 W1/W2 PASS · `SELFTEST PASS` · `All checks passed. Safe to ship.` Revision 3 changes only `rebuild/m3/setup/**`, `SETUP-TOKEN.md` and this report, none of which those gates read; per Astra they were NOT re-run for documentation. |
| `node rebuild/m3/w0/scope-package.mjs` · `git diff --check` (revision 3) | `FROZEN-PATHS PASS` · `OLD-PACKAGE PASS — 18 allowlisted files` · clean |
| GitHub `rebuild` workflow | head 435f145 run #40 and head a6db2fa run #51: ubuntu SUCCESS + windows SUCCESS (the workflow's public checks; it does not run this launcher or the PowerShell tests). Revision-3 head: recorded in the PR description / integrator report after the push. |

### Independent receipts (reported to the integrator; not re-verified here)
- **Astra, head a6db2fa (revision 2):** Windows Node 24.19.0 launcher tests **17/17 PASS, 0 skipped**, real wrangler 4.129.0, explicit minimal parent env / disposable profile / proxy tripwire; PowerShell 7.6.5 AST-only parse of both `.ps1` files PASS; no script or `.cmd` execution. The Node receipt carries to revision 3 because `setup/wrangler.cjs`, `setup/test/wrangler-launcher.test.cjs` and `tooling/package.json` are byte-identical to a6db2fa. The PowerShell files changed in revision 3 and need a fresh AST parse.
- **Astra, head 435f145 (revision 1):** Windows Node 24.19.0 launcher 16/16; PowerShell 7.6.5 AST parse PASS.
- **Cowork, original head (revision 1), Linux PowerShell 7.4.6:** facade tests **38/38 PASS**; function probes then confirmed defects 1–3 → NOT ACCEPTED; those defects are fixed in revisions 2–3. The revision-3 test file has not been run anywhere yet.

## 3. NOT executed here — honest boundaries
- **PowerShell is not available in this Linux session.** `store-secret.ps1`, `store-secret.tests.ps1` and the four `.cmd` wrappers (argument/exit forwarding, pauses) are **NOT RUN** here; revision 3 of the `.ps1` files has not been parsed anywhere yet.
- **The owner-run wrapper proof is NOT RUN.** A prior agent invocation of the `.ps1` was refused by the PC's script policy; it was not retried through `-ExecutionPolicy Bypass`, encoded commands, another runtime or any policy change, and it says nothing about the owner double-click route, which has not been tried in the owner's normal session. No administrator action is sought or assumed.
- **Windows User-environment persistence and the fresh-process hand-off are NOT proven** — that is exactly what the dummy self-test and fresh-process proof will show, and both have NOT RUN.
- **The LOCAL Claude Code is not signed in** (2.1.221; `auth status --json` exit 1 / `loggedIn:false` on 2026-09-06). Its ordinary owner sign-in and one harmless real request are prerequisites inside the PC appointment; this cloud session cannot stand in for it and must not receive User-environment or private data.
- **Real token creation waits** for: helper accepted → owner self-test OK line → fresh-process proof OK line (in the signed-in fresh LOCAL process) → then the token. `whoami` afterwards is a separate authentication proof.
- The "not a transitive lock" residual stands; registering the launcher tests in CI would be a separate reviewed change.

## 4. Remaining owner-session actions (ONE PC appointment; in order; no terminal work for the owner; nothing before step 1 succeeds)
1. `rebuild\m3\setup\` → double-click **`store-secret-tests.cmd`** (optional, in-memory) and **`store-secret-selftest.cmd`**. Send the one summary/OK line each prints, verbatim. If a window says scripts are disabled, send that sentence instead and stop — no policy change.
2. Sign the LOCAL Claude Code in the ordinary way (existing account; no new plan or purchase). The integrator then makes one harmless real request in a FRESH local session and reports only success/failure + client version.
3. Double-click **`store-secret-freshproof-arm.cmd`**; note the printed NAME. Close every Claude Code window and terminal; open the LOCAL Claude Code FRESH from the Start menu. The integrator runs `store-secret.cmd --verify-fresh-proof NAME` there (expects `fresh-proof OK`). Then double-click **`store-secret-freshproof-cleanup.cmd`**.
4. Only after steps 1–3: create the Cloudflare custom token per `SETUP-TOKEN.md` §2 (unchanged permissions, 90-day TTL) → double-click **`store-secret.cmd`** → choose `CLOUDFLARE_API_TOKEN` → paste → Store.
5. Close and reopen FRESH again. The integrator runs `node rebuild/m3/setup/wrangler.cjs whoami` (authentication proof; verdict + client version only), then `SETUP-D1.md` §2 (inspect/reuse before create; `--jurisdiction=us`).

## 5. Queue and NEXT
- Queue: **W4-SETUP-HARDENING** — PR #36 revision 3; awaiting cowork's execution review (not DONE by this report). W4-PACKET stays DONE-as-preparation; **W4 / O-PC stay BLOCKED** on: accepted helper → owner dummy self-test → local sign-in → fresh-process proof.
- NEXT for the integrator after acceptance: the owner's steps 1–3 above → 4–5 → `US-CONFIG` per `SETUP-D1.md` §2 → `D1-CREATED.md` (verdict only).
