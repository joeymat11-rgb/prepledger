# REPORT — W4 SETUP HARDENING (integrator, "I") — 2026-09-06

Executes `rebuild/m3/W4-READY-PACKET.md` §1 "Prepare and review the narrow setup correction". Integrator-owned setup tooling only.
**Base:** `095ddf12f6aea70d6625a4ca6ac3c38bd0ec4084` (the accepted integration tip of `rebuild/t2-client-core` at branch time; PR #34 docs merged, ledger 49 lines).
**Branch:** `rebuild/m3-w4-setup-hardening` (own worktree; the integration checkout was not touched). One PR against `rebuild/t2-client-core`. **Not merged; cowork verifies the exact PR.**
**Nothing here creates an account, a credential, a database, a deployment, or touches the frozen app, the suite, the workflows, the root lockfile, W5/W6 or the seeded soak.**

## 1. Scope (exact files)
| file | change |
|---|---|
| `rebuild/m3/tooling/package.json` | `wrangler` pinned EXACTLY `4.129.0` (was `^4`). `.npmrc` unchanged (`package-lock=false`): a direct pin is not a transitive lock; only the wrangler package identity/version is guaranteed and checked. |
| `rebuild/m3/setup/wrangler.cjs` | NEW portable launcher: resolves `rebuild/m3/tooling/node_modules/wrangler/package.json` → its own `bin.wrangler` file (must stay inside the package dir), requires name `wrangler` and version exactly `4.129.0`, runs it via `process.execPath` with an argument ARRAY and `shell:false`, forwards exit codes, sets `WRANGLER_SEND_METRICS=false`, refuses `login`/`logout`. It never installs, fetches, discovers accounts, reads or prints credentials, or changes policy. `--launcher-check` resolves and spawns nothing. Exit codes: 2 usage · 3 not installed/malformed · 4 wrong version · 5 refused subcommand. |
| `rebuild/m3/setup/store-secret.ps1` | Rewritten core + unchanged UI intent: two-name allowlist retained (`CLOUDFLARE_API_TOKEN`, `CLERK_SECRET_KEY`); masked entry retained; **no length or value is ever printed** (only NAME + outcome word); an existing value is never overwritten silently (Yes/No prompt, value not shown); truthful outcomes `stored` / `unverified-readback-mismatch` / `unverified-readback-failed` / `set-failed`; explicit exit codes 0/1/2 cancelled/3 refused/4 empty/5 unknown argument; `--help`. **Self-test** no longer touches a fixed variable: it picks a collision-checked temporary name `EARNED_SELFTEST_<random>` (up to 8 tries, fails closed), writes an OWNED marker value, reads back, and in a `finally` block removes ONLY a value still carrying its marker — a foreign value is left in place and reported; cleanup failure is reported, never hidden. Core functions take a **facade** (`Get`/`Set` script blocks) so tests exercise the real code with in-memory stand-ins. `-AsLibrary` loads the functions without running. |
| `rebuild/m3/setup/store-secret.cmd` | Forwards every argument (`%*`) unchanged and returns the helper's exit code (`exit /b %RC%`); pauses only on a non-zero exit or when double-clicked with no arguments; documents exit codes and that the process-scoped `-ExecutionPolicy Bypass` is the owner-run design (unchanged). |
| `rebuild/m3/setup/store-secret-selftest.cmd` | NEW double-click wrapper → `store-secret.cmd --selftest` (dummy-only; a double-click cannot pass arguments, so the owner needs this file). |
| `rebuild/m3/setup/store-secret-tests.cmd` | NEW double-click wrapper → the PowerShell unit tests (in-memory facades only; nothing written to the environment). |
| `rebuild/m3/setup/test/wrangler-launcher.test.cjs` | NEW `node --test` suite: core against dummy package facades + end-to-end against the REAL pinned install under a network tripwire (all proxy variables → `http://127.0.0.1:9`, every `CLOUDFLARE_*`/`CF_*`/`WRANGLER_*` variable removed from the child env). |
| `rebuild/m3/setup/test/store-secret.tests.ps1` | NEW PowerShell tests (Pester-free) dot-sourcing the helper in library mode: argument parsing, allowlist, store outcomes (ok / pre-existing / read-back mismatch / setter throws / refused / empty), self-test (clean pass, collision skip, collision exhaustion, hijacked-value ownership guard, setter exception cleanup, read-back mismatch). |
| `rebuild/m3/SETUP-TOKEN.md` | §3 rewritten: dummy self-test FIRST via double-click `store-secret-selftest.cmd` (exact path, exact OK line), then the real hand-over; "fresh process" rule spelled out (a child of a stale parent is not refreshed); "User environment is your Windows profile, NOT an encrypted vault"; honest test status. §4: launcher invocation replaces `node_modules/.bin/wrangler`; fresh-process `whoami` proof; report verdict + client version only. §2 permissions and TTL/rotation retained unchanged. |
| `rebuild/m3/SETUP-D1.md` | §0: exact pin + launcher + "direct pin is not a transitive lock". §2: `--launcher-check` step 0; **inspect/reuse-before-create** rule (reuse only the intended `us` database; collision/unknown/wrong jurisdiction = named blocker; never delete/recreate; verify jurisdiction via API if `d1 info` omits it). §4 abort conditions adjusted accordingly. `--jurisdiction=us` retained. |
| `rebuild/m3/REPORT-W4-SETUP-HARDENING-I.md` | this file |

## 2. What was executed here (Linux, Node 22, real `node_modules` directory, no credential in the environment)
| check | result |
|---|---|
| `npm install --prefix rebuild/m3/tooling --no-package-lock --include=dev` from the pinned manifest | wrangler **4.129.0** installed (38 packages; no lockfile written; root `package-lock.json` untouched) |
| `node --test rebuild/m3/setup/test/wrangler-launcher.test.cjs` | **16 pass, 0 fail, 0 skipped** — 9 core cases on dummy facades (exact version accepted; missing install → 3 and no `node_modules` created; 4.128.0 / 4.130.0 / 5.0.0 / prerelease → 4; squatting package → 3; bin escaping the package → 3; declared-but-missing bin → 3; string-form bin; arg checks) + planSpawn shape (execPath, argument array incl. a hostile string kept as ONE argument, `shell:false`, telemetry off, caller env forwarded) + 6 end-to-end cases on the REAL pinned install under the tripwire (`--launcher-check` → `wrangler-launcher OK 4.129.0 rebuild/m3/tooling/node_modules/wrangler/bin/wrangler.js`; `--version` → `4.129.0`; `--help` lists `d1`; unknown subcommand forwarded and its non-zero exit forwarded; `login` refused with exit 5 before any spawn; no arguments → usage exit 2; output free of token-shaped strings and environment dumps) |
| real launcher smoke, `env -i` + proxy tripwire | `--launcher-check` OK · `--version` → `4.129.0` exit 0 (wrangler printed its own "proxy environment variables detected" warning and made no request) · `--help` shows the `d1` group · `login` → exit 5 |
| static check of the helper | no `Length`/`chars`/`$len` output remains; the fixed name `EARNED_SELFTEST_VAR` no longer appears |
| `node rebuild/conform/run.cjs` (AGENTS preparation; `MEASURED_TEST_NOW=2026-09-03 TZ=America/New_York`, engines explicit) | `SUITE CONSISTENT — 99 reference GREEN · 99 STRONG · 29 RED-first against absent families · 70 GREEN against present families` · `rig185: W1 PASS, W2 PASS` (a first run in the fresh worktree was INCONSISTENT only because the private golden had not been copied in yet — the documented missing-fixture symptom, not a defect; re-run after the full preparation: CONSISTENT) |
| `run.cjs --selftest` | `SELFTEST PASS` |
| `node scripts/check.mjs --strict` (`MEASURED_TEST_NOW`/`PL_ENGINE`/`PL_LAWS_LIB` unset) | 3072 assertions · APP_V 7.56.0 · 18 files ship · `All checks passed. Safe to ship.` |
| `node rebuild/m3/w0/scope-package.mjs` | `FROZEN-PATHS PASS` · `OLD-PACKAGE PASS — 18 allowlisted files` |
| GitHub `rebuild` workflow (ubuntu + windows) on the branch head | recorded in the PR description and the integrator report after the push (the workflow runs on every branch push) |

## 3. What could NOT be executed here — honest boundaries
- **PowerShell is not available in this Linux session** (`pwsh`/`powershell` absent). `store-secret.ps1`, `store-secret.tests.ps1`, both `.cmd` wrappers and their argument/exit forwarding are **NOT RUN**. They were reviewed by reading; the tests were written to the helper's real function signatures, but a syntax error in either file would only surface on Windows. First Windows run of `store-secret-tests.cmd` (in-memory only) is the cheap check; its summary line belongs in the next report.
- **The owner-run wrapper proof is NOT RUN.** A prior agent invocation of the `.ps1` was refused by the PC's script policy. That result was not retried here through `-ExecutionPolicy Bypass`, encoded commands, another runtime or any policy change, and it says nothing about the designed owner double-click route, which has simply not been tried in the owner's normal session. No administrator action is sought or assumed.
- **Windows User-environment persistence and the fresh-process hand-off are NOT proven.** The in-memory facades prove the helper's logic, not `[Environment]::SetEnvironmentVariable(…,'User')` on the owner's machine, and not that a freshly started Claude Code / terminal sees the variable. Real token creation waits for both.
- **The launcher was not run on Windows** in this session (the branch CI's windows job runs the repository's public checks, not this launcher — registering the launcher test in CI is a separate reviewed change, not made here).
- The "not a transitive lock" residual stands: `npm install` resolved wrangler's dependencies fresh; only wrangler's identity and version are checked.

## 4. Remaining owner-session actions (in order; no terminal work; nothing before step 1 succeeds)
1. Open the repo folder → `rebuild\m3\setup\` → double-click **`store-secret-tests.cmd`** (optional, in-memory) and **`store-secret-selftest.cmd`** (dummy round trip in the real USER environment). Send the integrator the one summary/OK line each prints, verbatim. If the window says scripts are disabled, send that sentence instead and stop — no policy change.
2. Only after the OK line: create the Cloudflare custom token per `SETUP-TOKEN.md` §2 (unchanged permissions, 90-day TTL) → double-click **`store-secret.cmd`** → choose `CLOUDFLARE_API_TOKEN` → paste → Store.
3. Close Claude Code and every terminal; reopen them FRESH from the Start menu. The integrator then runs `node rebuild/m3/setup/wrangler.cjs whoami` in that fresh session and reports only success/failure + client version. Then `SETUP-D1.md` §2 (inspect/reuse before create; `--jurisdiction=us`).

## 5. Queue and NEXT
- Queue: **W4-SETUP-HARDENING** — PR published, awaiting cowork's execution review (not DONE by this report). W4-PACKET stays DONE-as-preparation; **W4 / O-PC stay BLOCKED** until the owner-session self-test and the fresh-process proof exist.
- NEXT for the integrator after acceptance: the owner's step 1 above → step 2/3 → `US-CONFIG` per `SETUP-D1.md` §2 → `D1-CREATED.md` (verdict only).
