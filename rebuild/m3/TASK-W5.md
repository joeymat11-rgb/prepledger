# Issued task text (verbatim, as pasted into Codex on 2026-09-05; committed for OPEN-SOURCES) — branch rebuild/m3-w5-authority-bridge

You are ASTRA. M3 work item W5 from rebuild/m3/PLAN-M3-v1.md (§2 row W5; §3 "Authority bridge" and "Crypto/auth"; §6 rows AUTH-D1 and HTTP-190; the rig section "Publish/re-create tracked rebuild/m3/rigs/rig190.cjs and rig191.cjs"). The M2 extraction is complete (rebuild/m2/SCORECARD-M2-7.md); the repo is quiet. Branch: rebuild/m3-w5-authority-bridge from the rebuild/t2-client-core tip that carries the module-7 merge (record that SHA). Open ONE PR. Do not merge. Nothing private, no secrets, no product-rule changes, no edits to the frozen app, rebuild/conform/ laws, rebuild/authority's synchronous core semantics, or the seeded soak.

Deliver under rebuild/m3/w5/ and rebuild/m3/rigs/ ONLY (plus the report):
1. D1 STAGED BRIDGE for the accepted T3 authority core (rebuild/authority): one consistent D1 read batch loads the athlete and every ownership/dependency/drain input with a REVISION; the synchronous core runs on a staged cache; one atomic write batch first asserts that revision through a failing constraint, publishes all effects and increments it (global revision first, per the plan); unique keys on athlete/device/sequence, athlete/op identity and log position; NO successful disposition is returned before the durable commit; stale work retries from a fresh snapshot; a crash at any cut leaves no partial rows. Schema as a tracked SQL migration. Local D1 via the pinned wrangler (4.129.0) local mode / miniflare — must run on Windows AND Linux from the repo root with no account, token or network.
2. WORKER auth/HTTP: routes for op / pull / time / lease / enrol (import/restore may be stubs that return a typed NOT-IMPLEMENTED, never a fake success); Clerk token verification of issuer, audience and origin against a PINNED test issuer with locally generated keys (no live Clerk call in tests); scope = Clerk subject + enrolled device, NEVER a request-supplied athlete id; shared response caching blocked; exact retry of an accepted request yields ONE effect (C6 cuts before acceptance AND after durable acceptance before the reply).
3. PUBLIC-SIGNATURE BOUNDARY: replace the authority's shared-HMAC test signatures at the declared boundary with pinned P-256 / SHA-256 public verification — canonical bytes and signature-encoding vectors committed as fixtures; the phone side receives verification keys only; receipts, pulls, snapshots, leases and the challenge-bound fresh server time are all signed and verified BEFORE any frontier advances or an outbox drains. Preserve operation HMAC, identity, domain and canonical contracts exactly as T3 defines them. Key material in tests is generated per run; nothing resembling a real key is committed.
4. RIGS: re-create tracked rebuild/m3/rigs/rig190.cjs (five interop scenarios: two same-athlete devices, a separate athlete, a forged verifier, an identical replay, a lost acknowledgement) and rig191.cjs (TEN EFFECTIVE breaks in disposable copies: replay append, identity collision, sequence reuse, foreign reference, rejected-parent waiting, lineage ordering, stale basis, unsigned disposition, unpinned export, ignored lease expiry — an inert mutation earns nothing) from rebuild/t3/RIGS-PREREGISTERED.md:9–18, plus rebuild/m3/rigs/run.cjs --case NAME --env ENV (ENV = local | synthetic-remote | owner-phone | isolated-restore) emitting "NAME PASS/FAIL/BLOCKED/PENDING", nonzero on failure, exit 2 on blocked/pending. Map the 34 §A laws (rebuild/conform/laws/sheet-A-authority.cjs) onto the ASYNC D1/HTTP path through tracked wrappers — a backend environment variable alone does not test the boundary. Race 100 independent invocations including an ownership change and a waiting drain.
GATE (all must hold; quote the lines): run.cjs --env local: AUTH-D1 PASS (34/34 mapped laws GREEN on local D1 + rig191 10/10 EFFECTIVE breaks) and HTTP-190 PASS (5/5 over real local HTTP with the C6 cuts); --env synthetic-remote: BLOCKED (exit 2) with the reason "W4 remote database not yet created" — never simulated; the frozen suite unchanged and CONSISTENT (node rebuild/conform/run.cjs; authority family still 34 GREEN; rig185 PASS/PASS) with MEASURED_TEST_NOW=2026-09-03 TZ=America/New_York and ENGINE_MAIN/ENGINE_OLD set explicitly; --selftest PASS; node scripts/check.mjs --strict PASS with MEASURED_TEST_NOW unset. One bite: break the revision assertion (or the P-256 verification) in the bridge only and show AUTH-D1 / HTTP-190 go RED; restore byte-for-byte; quote the RED line and the restored sha.
Report rebuild/m3/REPORT-W5-ASTRA.md: what/why; the exact wire contract W6 must implement (routes, bodies, signatures, error/state codes, the challenge-bound time exchange) as a table — W6 is blocked on this; the gate output verbatim; the bite; SEAMS (what stays synchronous-memory, what needs the real D1, what the owner's Clerk production activation changes); what is unsure; wall-clock. Report at the end of the PR description: the AUTH-D1 line, the HTTP-190 line, the run.cjs SUMMARY line and the strict tail. cowork verifies by execution on Linux before integration.
