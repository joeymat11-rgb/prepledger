# R1 resource-local/v2 — REJECTED proposal, not adopted

Independent disposition2026-09-06: cowork executed scoped9f08c01 on Linux and rejected this proposed acceptance metric. Own-only used-based128604204B and foreign133795156B still exceed96MiB; the proposal fails its own both-host requirement. Original reviewed proposal SHA791575c64578f42d6d0ec9bfe000ec773127100be6e22a8fa64b6be5a7e6a6ef remains available at9f08c01. The text below is preserved as the rejected proposal. No metric/cap/gate or remote-only replacement is adopted; original FAILs and R1-COMPLETE FAIL remain. The report distinguishes observed results from unproved runtime/hosted-equivalence hypotheses.

2026-09-06; scoped product `ed71de2e7406bd83dee8eeec358c8bd163cb2f80`. This is the bounded OPEN-RESOURCE-V2 technical-review question already recorded in SCOPED-READ.md. No implementation, metric substitution, cap acceptance or resource PASS follows from this document. All six original resource-local/v1 FAILs remain authoritative until an explicit independently reviewed successor contract is recorded. W6 remains paused.

## Evidence and question

The scoped original workload still fails v1 at162,050,118 bytes. Its current A/B pair is161,795,629 /178,667,024 bytes under v1; the diagnostic used-based sums are83,715,877 /86,555,640 bytes. Earlier independent Linux own-only also failed the used-based view at136,203,486 bytes, so changing the formula is not a promised fix. Population scoping addresses a demonstrated foreign-data allocation source; no next micro-optimization has an established peak-memory effect. Instrumentation review found no concrete retained-payload/remote-object leak.
[CDP Runtime.getHeapUsage](https://chromedevtools.github.io/devtools-protocol/tot/Runtime/#method-getHeapUsage) distinguishes used JavaScript heap, allocated JavaScript heap, used embedder heap, and ArrayBuffer/external-string backing storage. [Cloudflare's memory limit](https://developers.cloudflare.com/workers/platform/limits/#memory) covers the isolate across concurrent requests, including JavaScript and WebAssembly; its page does not equate that limit to either CDP sum. Therefore the proposal below is an explicitly local sampled occupancy check, not proof of the hosted limit or universal peak. The whole-isolate allocation vector remains reported.

Question for independent technical review: retain allocated-capacity v1 as the local acceptance metric, or adopt the precisely limited v2 surrogate below with its negative controls and separate remote requirement? Recommendation: review v2 now; do not select it merely because the current Windows occupied-memory observations are lower. If the reviewer cannot justify its meaning/omissions, reject it and keep R1 resource acceptance FAIL.

## Exact proposed contract

1. New profile `earned/r1/resource-local/v2`; original runner/profile/results remain unchanged. Candidate evidence pins the exact source, runtime, workload, operating system and formula. Adoption requires C's executed review and an integrator ledger entry; a builder cannot turn the current v1 FAIL into PASS.
2. For each simultaneous sample of the verified application isolate, define occupied = usedSize + embedderHeapUsedSize + backingStorageSize. Every component must be finite/nonnegative; usedSize must not exceed totalSize. Ceiling remains96MiB =100,663,296 bytes. Do not add totalSize to occupied or sum component maxima taken at different times. Also retain and print the original total-based metric and its independent verdict.
3. This metric includes observable occupied heap, embedder heap and backing storage; it does not measure all native allocation, allocator metadata, stacks, unreported WebAssembly allocation or unsampled transients. No claim of a complete resident-memory bound, guaranteed production margin, or reclaimability. The hosted128MB limit is not increased or reinterpreted.
4. Keep the exact full32 sequential pages, two complete32-page overlapping attempts, and +1-byte413 refusal; original own scope/claims/retained-byte/signature checks and all CPU/SQL/batch/domain-write limits remain. Also repeat the existing exact A/B populations in three fresh default-runtime pairs on each required host, report every arm/vector/verdict; any occupied-limit failure fails v2. No best-of selection, forced collection, heap flags, altered workload, per-page isolate restart or idle wait to obtain a lower result.
5. Actual application-isolate sampling must cover the full calibration/workload intervals, with source-pinned begin/end samples and recorded cadence/gaps. Missing target, components, calibration, a phase or a completed workload is BLOCKED. An observed excess, invalid proof, request failure or timeout is FAIL. A versioned sampling-gap rule must be specified and tested before implementation; choosing that rule from a favorable run is forbidden.
6. In separate disposable test isolates, hold a128MiB ArrayBuffer until measurement and separately hold a large ordinary JavaScript double array until measurement. Each must exceed96MiB in its corresponding backing/used component and be rejected, or calibration is BLOCKED. These are instrument controls, never product or acceptance-workload modifications. No exact JavaScript object-overhead estimate substitutes for an observed vector.
7. Effective negative controls: drop backingStorageSize, drop usedSize, select the wrong isolate, omit a phase, or discard an over-budget sample; each mutation must fail the independent control/assertion that would otherwise detect it. An inert mutation earns no credit. Restore instrument bytes before every workload. Test malformed, stale, missing and non-simultaneous vectors with fixed synthetic samples too.
8. Full unchanged functional/crypto/concurrency/guard-bite/original-W5/conformance/SELFTEST/strict gates remain mandatory on final code. C independently runs the exact candidate on Linux; root runs Windows. No phone/remote/production result is simulated.
9. Local v2 PASS, if adopted and actually observed, would establish only the named sampled local acceptance check. A separate real deployed synthetic resource run must verify provider-side resource outcomes and the declared available isolate-replacement evidence before private import; successful HTTP responses alone are insufficient because Cloudflare may complete in-flight requests before replacing an oversized isolate. The remote contract must name the required provider evidence, treat exceededMemory/resource termination as FAIL, and treat unavailable required evidence as BLOCKED; ordinary unrelated isolate replacement is not by itself proof of a memory failure. Pending remote work retains its existing W4 dependency. Do not use local v2 or HTTP200 as a claim that Cloudflare's memory ceiling was proved.

## Separate unresolved safety issue

The own-account payload cap is currently checked after complete materialization (`project.cjs`), so arbitrary oversized retained input can exhaust resources before typed413. OPEN-BYTE-PREFLIGHT remains a separate contract: a size/count preflight must be in the same revision snapshot, prevent large values from crossing into the application isolate, preserve authentication/integrity/error precedence, and prove an exact no-false-refusal bound for every valid1MiB proof. This proposal neither implements nor closes it. A resource formula cannot repair that path.

## Review disposition

PENDING independent technical decision. Required unresolved definitions: sampling-gap rule; reliable application-isolate identity across target reconnects; demonstrated component treatment of the pinned runtime; exact placement of the deployed synthetic gate in R1/M3 acceptance. Until resolved, no v2 runner is built and no existing gate changes. Estimated implementation after an accepted contract:2–4 engineering hours plus independent execution, not a calendar promise.
