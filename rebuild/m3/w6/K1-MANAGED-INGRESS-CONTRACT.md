# K1 managed ingress and durable obligations — proposed implementation contract

2026-09-07 · Astra · retained W6 claim / PR32. **PROPOSED; independent review pending; implementation and acceptance cases NOT RUN.** This authors a concrete missing lifecycle/schema artifact. It does not declare all ingress closed, authorize product edits, establish CLOCK, choose training behavior or create another stream.
Original source checkpoint: 1a5098d44e7cc4202bf3387f22681491bc13714d; W5 binding addendum read at8233f892fe8547deedce3a79c9940b519b042505; integration213300c6c49f772dcb2ff859e1c1375dd571b837. Exact public file SHA-256 pins follow; newer source requires applicability review.

| Source under rebuild/m3/ | SHA-256 | Governing locations |
|---|---|---|
| BRIEF-W6.md | 4c15f73b39e59994e1d25d480c1491dabd393efd09a34ef67f78a78d851e9a79 | 37–43 allowance/checkpoint;54 knowledge loss;62–63 sign-out/restart;69–83 outcomes |
| w6/public-client.mjs | 43395ee9244c27357c492d0fc909d64bfcf09526e0089f623203bf239b49f3ec | 22–35 contexts/completion;64–97 staging/sample;118–128 response guard;139–177 execute/reopen/time |
| w6/FRAME-AMENDMENT-PROPOSAL.md | cd9fa3c20b1e901a756c7035a8427d423e3e86f790fcb96917c7c407a9ffbb7a | 21–28 encrypted proof ownership;39–46 frame fields;80–91 final cut/failed control;105–106 limits |
| w6/CLERK-INGRESS-RESEARCH.md | bbea9cb4d8a00ccee3348754a544f92860fac9ad0069813c22fc6fa4b4f9c03d | 9,28 vendor source pins;15–22 independent SDK channels;36–46 controlled candidate/unknowns |
| w6/PRODUCTION-DEPENDENCIES.md | b5e7f138a1b54882af046983a5ace9e8327a0c349c077f68058e535560f976e4 | 12–15 dependencies;21–35 initial lifecycle sketch;46–48 custody |
| w5/WIRE.md | a6f652972b6c24af947da4e296874464850ca426eec1f5e8484f890dd250106c | 5–23 requests/results;25–35 refusals;37–60 signatures/time/limits |
| w5/fixtures/contract-v1.json | 57e6774494b348225e711ed21b16569dd15870390de398fecd172b8e2ac56de1 | 1–23 encoding/domains;24–89 canonical vectors;90–119 time profile |
| w5/public-client.cjs | 4cb894d706edaad845ba682ff27643f0a8882e0a4ffd9b7090be732f6ff8c8de | 28–74 signatures;86–151 response verification;153–174 challenge lifetime |
| w5/worker.cjs | aaa26bec36dc7c1e25119543b64dc659eb25c09ff10d9f89e1e640f48b961941 | 12–27 body limit;34–41 headers/errors;45–135 actual route handling |
| w5/crypto.cjs | eb2d1721ca69e23ca9d8fd8df2e7fbde398b5591b36045f3e51f5f94258671ee | Actual P-256 signing/verification and domain bindings |
| ../authority/canonical.cjs | c8888a3907d543371400e75ccfcb11bec2c3e3e7a40e74bd9d471b21a26e08cc | Existing canonicalEncode used by W5; no encoding repair here |

## 1. Scope, ownership and earliest observation

Current acceptResponse receives a caller-owned response before guard.run (public-client118–128,154–157); execute/reopen reach permissionNowIso at82–85 without that guard. exchangeServerTime surrounds request creation at163–177. A guard around the trusted sink alone cannot protect earlier status/header/parser/UI/clock knowledge.
**Proposed owner profile:** one same-origin Web Lock semantic owner per authenticated installation namespace. It alone holds the private durable-client instance, verifier, permission evaluator, token/response parser and transport callbacks. Lock naming uses the exact namespace through a reviewed collision-free encoding; that encoding and supported-browser behavior remain binding-review items.
Other enrolled contexts forward bound intents through owned MessagePorts; they cannot directly invoke these private dependencies. Each intent carries immutable scope/session references, a fresh intentId and its captured input. Responses bind the same intent/scope; stale delivery cannot paint or clear another account's draft. This is a proposed coordination profile, not an accepted restriction of tab/PWA support.
No wall-time lease, heartbeat silence or timeout steals a lock. A paused owner is not a dead owner. After actual lock acquisition, a successor authenticates the stored pair and resolves inherited obligations before exposing truth. Lack of an owner/answer preserves the draft and supplies no success; its exact waiting UI must satisfy the existing concurrency contract before adoption.
The owner keeps network waits outside the short local stage/commit queue. A pending nonnegative network child does not itself forbid a separately permitted local write. Such a write preserves that child's OPEN root. Avoiding a permanent OPEN-per-session guard, mandatory lifetime network connection and automatic bounded-restart refusal is a REQUIRED BUT UNPROVED property: pre-arming continuously available UI controls can violate it, as the crash comparison below explains.
**Proposed auth profile:** no Clerk JS import in the enrolled offline context; use explicitly owned native-fetch requests for the ruled email-code flow and token/session actions. This excludes that SDK's timers/channels by composition, not by polling:false/touchSession:false/off. Actual FAPI request/cookie/challenge and W5 JWT bindings remain OPEN; this document does not implement them or assume a quiet SDK mode.

| Producer enum | Owner-controlled ingress cut | Evidence/closure requirement |
|---|---|---|
| W5_DISPOSITION | Before dispatching the original operation/retry and before handling response status/headers/body | Original operation bytes and published disposition binding; each pending attempt remains identifiable |
| W5_PULL / W5_SNAPSHOT / W5_LEASE | Before the producing request, cache delivery or supplied-proof verification | Exact published route/version/scope/request; full signed result and relevant expected basis |
| W5_TIME | Before challenge creation/request and any response-age/time evaluation | Existing single outstanding challenge; no timeout makes an unresolved request safe; T1 qualification separate |
| AUTH_EXCHANGE | Before explicit auth/token/session request or informative callback | Actual versioned FAPI profile; classify expiry/absence separately from proven standing loss |
| SIGN_OUT | Before enabling the actual final native Sign out button | Intent itself immediately refuses17; later SDK/network call is not the intent cut |
| PERMISSION_SAMPLE | Before any local sample/evaluation capable of learning expiry or allowance invalidation, including face/reopen | Inventory every actual caller; local-arm/C1 and final-sample treatment remain explicit review blockers |
| CONTEXT_SWITCH | Before enabling an account switch or ownership handoff | Retire old presentation and intent sources; scope retirement is not fabricated global account revocation |

No unspecified producer is permitted to invoke the private evaluator or sink. Online/offline/focus/visibility, storage events, worker messages and cookie changes may trigger owned intents; they are not authenticated revocation by themselves. Include every actual handler/import in the final producer manifest; unknown informative ingress prevents profile acceptance.
The first informative cut includes status/headers, body/error parsing, verification, cached/queued delivery and deliberate UI intent, not only application subscribers. Before any such cut the matching root/child must already be durable. Full raw data may be held opaque, but withholding a comparator does not erase knowledge already delivered to application code.

## 2. Proposed closed proof shapes

All new K1 wrapper/descriptor/capture shapes below are proposed version1 validators, not currently accepted production validators. Their unknown/missing fields, duplicate decoded keys, unexpected types, nonfinite/unsafe numbers and unsupported versions refuse18 before truth. These new field allowlists do NOT replace inherited W5/T3 signed-record validators: retain every original signed field and use the pinned existing contract, including its permitted extras. No object coercion. Full proof bytes remain encrypted in the existing frame body; its existing kind/version/digest/bytes envelope and strict parser remain unchanged.
Serialization proposal: UTF-8 JSON.stringify of closed objects constructed in the field order shown, no whitespace/newline, strings restricted to Unicode scalar sequences. IDs are32 lowercase hex characters from16 fresh random bytes, checked for collision in retained scope; they are identities, not cryptographic security budgets. References are lowercase64-hex digests or explicit null where allowed. Revision fields are positive safe integers. Arrays are dense; child IDs are unique and sorted by ASCII ID; duplicate memberships refuse.
Root proof: kind=guard, version=1; exact fields {protocol,rootId,namespace,athleteId,deviceId,ownerRef,sessionRef,basisRevision,predecessorGuardRef,phase,children,closureRef}. protocol is earned/k1/root/v1; namespace/athlete/device obey their existing authenticated contracts; ownerRef/sessionRef are nonnull immutable proof references; predecessorGuardRef is null only for verified initial absence. phase is OPEN, CLOSING or RESOLVED. closureRef is null unless phase=RESOLVED. children is the complete array of child records below, never only recently active children.
Child record: exact fields {childId,producer,phase,binding,rawRef,outcomeRef}. producer is one enum in §1. phase is ARMED, DISPATCHED, CAPTURED, RESOLVED or CANCELLED_BEFORE_DISPATCH. rawRef/outcomeRef are null until their full matching proofs exist. A durable ARMED record does not prove dispatch never happened after it; after restart it remains ambiguous until recovered.
Binding: exact fields {profile,requestId,method,route,requestDigest,opId,challengeRef,intentId,basisRef,ruleVersion}. profile/ruleVersion name exact reviewed source/schema versions; method is POST or LOCAL. route is the exact registered producer route/action. requestId/intentId are scoped IDs; requestDigest commits the exact request descriptor/body bytes below. Null is allowed only by the producer table, including its nondispatchable W5_TIME pre-challenge phase. basisRef is the immutable evaluation basis. §2.1 specifies proposed W5 combinations; AUTH_EXCHANGE/UI/local-sample combinations remain OPEN and cannot dispatch through this profile.
Raw proof: kind=recovery, version=1; exact fields {protocol,rootId,childId,scopeRef,producer,requestId,profile,capture}. protocol is earned/k1/raw/v1; capture is the closed producer union. §2.1 authors W5 capture shapes only. **AUTH_EXCHANGE/UI/local-sample capture schemas and sensitive auth custody remain OPEN; arbitrary JSON or silent redaction is not valid evidence.** No bearer token is copied into a proof merely because it arrived in a response.
Outcome proof: kind=recovery, version=1; exact fields {protocol,rootId,childId,rawRef,basisRef,ruleVersion,classifications,effectsRef}. protocol is earned/k1/outcome/v1. classifications is a nonempty duplicate-free array in this fixed order: STANDING_LOST, AUTH_REJECTION, ALLOWANCE_INVALIDATED, INTEGRITY_UNPROVEN, AUTH_EXPIRED_ONLY, NO_NEW_NEGATIVE, UNAVAILABLE; incompatible combinations are rejected by the producer rule. NO_NEW_NEGATIVE cannot coexist with a negative or UNAVAILABLE. effectsRef retains the complete immutable disposition/effect evidence, not a guessed success boolean.
Positive permission depends on the exact current basis and selected rule. Negative knowledge cannot be discarded merely because its observed basis became stale; validate scope/authenticity and preserve every independently applicable negative dimension from every unresolved raw proof. A changed token or successful unrelated request cannot discharge it.
Closure proof: kind=recovery, version=1; exact fields {protocol,rootId,ownerRef,sessionRef,childrenDigest,outcomeRefs,barrierProfile,barrierEvidenceRef,resolution}. protocol is earned/k1/closure/v1; childrenDigest commits the exact complete child array; outcomeRefs list every required resolution in child order. resolution is CLEAN or RETAINED_NEGATIVE. The exact barrier profile must prove stopped creation, retired delivery and each child's resolution; no caller assertion alone satisfies it.
Each changed root/proof is a new immutable entry; the frame selects its digest. Frame guard=OPEN for OPEN/CLOSING, resolved only for a validated RESOLVED root. H/W_last/U, invalidation, observationCounter and selected references remain authoritative only in the frame. Never spread historical retainedMetadata into permission or duplicate current counters into a root as alternative authority.
Root/proof growth cannot silently trim unresolved children, old signed operations, rejection knowledge or recovery evidence. Retention/size bounds and reclamation need an explicit reviewed rule tied to complete closure and P1/R1 requirements; unavailable space refuses with retained obligations.

### 2.1 W5 v1 bindings and captures — proposed manager contract

This table binds only the published W5 v1 routes; it does not amend their HTTP bodies, signatures, operation HMAC, canonicalization or lease semantics. Binding profile=earned/k1/w5-http/v1 and ruleVersion=earned/k1/w5-capture/v1 select these pinned rules. method=POST; requestId, intentId and basisRef are nonnull; those local identifiers are never added to the HTTP body. Authenticated root scope and the selected verifier bind athlete/device/schema and public-key pins; callers cannot select another scope through response fields.
Request descriptor has exact fields {serviceOrigin,origin,method,route,contentType,bodyB64}, in that order. serviceOrigin is the configured HTTPS service origin; origin is the permitted frontend origin; method=POST, contentType=application/json, and route is an exact table literal with no query/fragment. bodyB64 is canonical unpadded base64url of the exact UTF-8 JSON body bytes. requestDigest=SHA-256 of this descriptor's §2 serialization; it commits both endpoint/context and untouched body bytes. The owner captures this descriptor before durable arm and dispatches those same bytes. Authorization/Cookie values are not descriptor fields or evidence. Invalid configuration cannot be repaired by trusting response-declared origin.
Construct the listed body keys in order and serialize once; operation is the full original immutable T3 envelope, not a reconstruction from its canonical commitment. An operation retry retains that exact envelope and request bytes; its separately recorded attempt has a fresh local requestId. JSON transport must preserve the existing parsed operation/canonical bytes; it cannot silently coerce unsupported values. Worker request limit remains262144 bytes, not a newly selected history/capture limit.

| Producer / route | Exact request body; binding nonnull/null | Required expected-response join before the trusted sink |
|---|---|---|
| W5_DISPOSITION /op | {device_id,operation}; opId=operation.op_id; challengeRef=null | Full original operation retained; disposition op_id/device_id/device_seq/canonical_content_commitment match that operation; operation athlete/device match root; status and signature verified |
| W5_PULL /pull | {device_id,after}; after is a safe integer>=0; opId=null, challengeRef=null | Signed athlete/device/version/key epoch; envelope.after equals this request.after; every receipt after+1..through present once, in order, fully verified before one sink |
| W5_SNAPSHOT /snapshot | {device_id,watermark}; watermark is a safe integer>=0; opId=null, challengeRef=null | Pass the exact requested watermark to acceptSnapshot; signed W=watermark, partial=false, pending=0, records=W, entries exactly1..W; no omitted expected-watermark argument |
| W5_LEASE /lease or /enrol | {device_id}; opId=null, challengeRef=null | Verify full lease, root athlete/device and selected schema; /enrol is lookup-only for an already provisioned binding, /lease returns the current stored lease; neither creates or renews one |
| W5_TIME /time | {device_id,challenge}; opId=null; challengeRef points to the complete W5_CHALLENGE proof below | Exact pending challenge from the same live public boundary, scope/version/key epoch/time profile, canonical UTC and signature; consume according to existing single-use verifier before its sink |

All dispatched table rows require nonnull requestDigest; no unlisted route/producer pairing is admitted. The table adds the explicit requested-after check around acceptPull, which currently validates the envelope's own range without receiving the request. It always supplies acceptSnapshot's existing expectedWatermark argument. These are proposed manager joins, not claims that the current public-client already implements this manager. Existing W6 lease-change refusal remains LEASE_RENEWAL_UNIMPLEMENTED18; do not install a new capability through this table.
Time has two durable cuts: before beginTimeChallenge (which itself samples monotonic time at public-client153–159), commit ARMED W5_TIME with route=/time, opId=null, requestDigest=null and challengeRef=null. This is the sole POST null-pair exception and cannot dispatch. After that completes, call the existing owned boundary once, retain its returned challenge in a raw proof whose capture is exactly {tag,challenge,wireVersion,timeProfile,boundaryId,observedTimeoutMs}: tag=W5_CHALLENGE, wireVersion=earned/w5-http/v1, timeProfile=earned/challenge-time/v1, boundaryId is a fresh scoped ID for this live boundary, and observedTimeoutMs equals its configured finite positive timeout<=30000. challenge decodes canonically to32 bytes (43 base64url characters).
Before fetch, durably replace that child's binding with the complete requestDigest/challengeRef and retain the full immutable challenge proof. Either persistence failure prevents dispatch and preserves OPEN; challenge generation/sampling is not retrospectively declared unobserved. The challenge proof cannot restore the verifier's private pendingTime after restart or reproduce its unexposed started sample. The original live boundary still verifies elapsed time inclusively against its configured observed timeout and consumes before callback; failed persistence/replay needs a fresh challenge plus outstanding-obligation recovery. This supplies no UTC-error/rate/RTT bound or checkpoint C.

HTTP capture is exactly {tag,request,response}, with tag=W5_HTTP and request the descriptor above. response has exact fields {url,type,status,redirected,headers,bodyState,bodyB64}. url/type/status/redirected come directly from the owned browser Response before parsing; redirected is its actual boolean. The proposed owned fetch uses explicit redirect:'error': type is one of basic,cors,default,error,opaque,opaqueredirect; status is an integer0..599. Only a proven owned basic/cors response at serviceOrigin+route with redirected===false can supply this profile's authenticated transport observation. Any redirect failure or redirected response refuses and retains OPEN. The live source-owned dispatch under the reviewed origin/cache/service-worker profile supplies origin evidence; a captured boolean, constructed Response or copied fields cannot.
headers is a fixed ordered array of [name,value] pairs: content-type, cache-control, cdn-cache-control, cloudflare-cdn-cache-control, pragma, vary, x-content-type-options, earned-wire-version. Each lowercase name occurs once; value is its exact browser Headers.get string or null. This captures browser-visible normalized headers, not unavailable original wire-header bytes. Missing/CORS-hidden values are not synthesized. The configured origin/CORS/cache/service-worker composition must prove this observed source; production exposure/transport authenticity remains a binding gate, not a caller boolean.
bodyState is UNREAD, COMPLETE or READ_FAILED. bodyB64 is canonical unpadded base64url of exactly the observed body bytes: empty for UNREAD, complete for COMPLETE, the received prefix for READ_FAILED. Each saved capture is immutable; a later capture retains the earlier proof and is linked through the updated child. Only COMPLETE may reach parsing/verification. The alternative no-response capture is exactly {tag,request,failure}, tag=W5_TRANSPORT_FAILURE, failure=NETWORK, ABORT or UNEXPECTED_RESPONSE; it contains no exception text, stack, token or invented HTTP status. It cannot prove that no informative event previously occurred.
Parse COMPLETE bodies with fatal UTF-8 decoding and duplicate-key refusal, retain exact bytes and the complete decoded signed record; never replace raw bytes with canonical re-encoding. Relevant malformed/error bytes stay encrypted under existing proof custody and are never output. W5 v1 emits no bearer tokens in its response schema; do not save credentials/request headers under this profile. Unexpected sensitive auth material requires the separately closed auth-custody profile, not silent redaction followed by successful replay. Capture size/retention remains §2's OPEN bounded-storage question.

| Successful body (HTTP200 plus earned-wire-version=earned/w5-http/v1) | Retained signed record / exact verification source |
|---|---|
| /op: {disposition} | {op_id,canonical_content_commitment,device_id,device_seq,status,decided_at,authority_signature}; ACCEPTED includes athlete_log_seq/accepted_at, rejection includes rejection_code, and all actual core extras remain signed/retained. status is ACCEPTED, WAITING, REJECTED or REJECTED_DEPENDENCY; do not drop extensions to force a new wire schema |
| /pull: signed envelope | {athlete_id,device_id,after,through,receipts,wire_version,key_epoch,authority_signature}; each receipt is {seq,op_id,canonical_content_commitment,accepted_at,op,authority_signature}. Full unchanged op is retained; another device's receipt is valid only within the signed same-athlete range |
| /snapshot: signed envelope | {athlete_id,device_id,W,partial,pending,records,entries,label,rejectedAppendix,otherDeviceNote,wire_version,key_epoch,authority_signature}; preserve label/note exactly, rejectedAppendix=[], partial=false,pending=0. Accepted history only, never proof of all pending/rejected/local records |
| /lease or /enrol: {lease} | {lease_id,athlete_id,device_id,schema_version,range,not_before,not_after,signature}, retaining optional issued_server_time/actual signed fields; range=[lo,hi], safe1<=lo<=hi, canonical UTC bounds with not_before<=not_after. Optional issued time is not freshness |
| /time: signed envelope | {athlete_id,device_id,challenge,server_time,wire_version,key_epoch,time_profile,authority_signature}; time_profile=earned/challenge-time/v1; server_time is canonical UTC seconds or three fractional digits |

Use the actual pinned W5 verifier, not the fixture's illustrative encoding-only signature. Signed-message bytes are UTF-8(domain + canonicalEncode(full record minus its signature field)), with no delimiter. Domains are earned/disposition/v1, earned/receipt/v1, earned/pull/v1, earned/snapshot/v1, earned/lease/v1 and earned/server-time/v1 respectively. Wire signature is ES256.kid.base64url(r32||s32):64-byte big-endian P1363, canonical unpadded base64url, 0<r<n and0<s<=n/2 on P-256; verify SHA-256 ECDSA against the exact pinned public-only key. Lease excludes signature; other records exclude authority_signature. Signed outer time/pull/snapshot key_epoch equals the actual signature kid. Unknown key, private key, DER, high-S, malformed or mismatched bytes refuse before any sink; preserve canonical-v1's published quirks and original operation HMAC/identity unchanged.
HTTP errors are unsigned {error:{code,state?}}. A genuine owner-controlled response from the configured authenticated service preserves WIRE25–35: 401 UNAUTHENTICATED→11 (sync authentication only), 403 SCOPE_FORBIDDEN→17 for the requested scope (including unknown /enrol binding), and409 FRONTIER_AHEAD→18. A403 is not a signed terminal operation rejection or proof of global account closure. Arbitrary/cached/caller-supplied error JSON, a forged state field, wrong-origin response, opaque response or missing wire version cannot establish standing; preserve prior knowledge and the unresolved child while refusing protocol use.
400 MALFORMED_REQUEST/413 REQUEST_TOO_LARGE retain the exact immutable queued envelope/op_id/sequence/HMAC and entered values; repair request construction/configuration only, never rewrite that operation. A changed entered fact uses its separately governed append/correction route;404/405 are protocol/configuration refusals;501 NOT_IMPLEMENTED supplies no restore proof;503 UNAVAILABLE/network failure retries only the unchanged original operation. None drains an outbox or resolves its terminal outcome. HTTP200 with invalid signature/body is not success; retain W5's presentation refusal and independently known17/18/19/20. Only the full verified signed disposition can establish its terminal result; WAITING remains waiting. Durable sinks publish success only after complete and honor stored/durable/confirmed refusals. No HTTP failure, elapsed timeout or completed fetch clears an unresolved operation/root or establishes clean reconciliation, renewal, lease history or checkpoint C.

## 3. Proposed API and transition requirements

The manager privately exposes beginObservation(binding), dispatch(handle,producer), capture(handle,raw), resolve(handle), beginClose(rootId), finalize(context) and releaseOwner(). Handles are scoped, candidate-bound capabilities; the same request may retry only under its original immutable identity and a separately recorded attempt. No public alreadyArmed flag or unowned callback can bypass dispatch.
beginObservation first authenticates the pair/current scope and builds the full root/child proof. Commit OPEN with full-pair CAS and await transaction complete before enabling the producer. Arm failure triggers zero controlled requests/samples/intent controls; retain input and return3 unless an independent stronger fault is already known. No knowledge-survival claim is made before that commit.
Adding a child requires its durable membership before dispatch; concurrent membership changes require restaging from the new complete basis. ARMED and DISPATCHED phases describe durable evidence, not proof of a maximum runtime phase. Children never allocate operations or allowance slots merely by existing.
capture preserves full relevant evidence before positive semantic use, while immediately latching independently learned17/19/20 and invalidating affected candidates. Capture/outcome-write failure leaves the already durable OPEN obligation. A failed raw write is not permission to declare the event unobserved or fall back to a positive old view.
resolve evaluates the exact retained raw/basis/rule version and persists complete consequences atomically. Authenticated rejection must commit rejected ledger, disposition and outbox change together and idempotently; accepted facts cannot be removed because a reply was lost. No ordinary trusted T2 sink may be called before its matching verified producer and guard authorize it.
CANCELLED_BEFORE_DISPATCH is allowed only in the same live owner with a proven never-dispatched capability and disabled delivery source. A successor cannot infer it from ARMED. After dispatch, AbortController, timeout, rejected fetch, unsubscribe or an empty callback queue alone cannot resolve a child. Recovery needs the actual producer-specific proof or a demonstrated no-delivery barrier plus the required outcome reconciliation.
beginClose synchronously closes child admission and disables genuine action sources, then resolves all owned obligations. A queued genuine click, accessibility activation, pending parser, redirect/challenge or callback must be accounted for. Removing a node alone is not quiescence. Native disabled-button behavior and ownership handoff require actual browser tests; no fictional event-loop flush.
Sign-out intent arriving in the active owner immediately establishes17, even if every later write fails. It cannot be recast as an unpressed confirmation. A not-yet-dispatched disabled button is different from an already delivered intent. Account switch retires old scope and prevents old account painting without inventing authenticated account closure.
Before bulk sealing, capture the complete proof set and close basis. A new child/evidence/negative or owner/session change invalidates that candidate; newly required immutable bytes require restaging and resealing. A digest/frame flag cannot stand in for absent full evidence. Existing final frame fields may carry the permitted later sample only under the separately closed T1/local-sample contract.
finalize is synchronous at the existing serialized, same-IDB-transaction validation cut. Recheck full durable basis and live owner/session/observation state; no external await inside this final cut. This is an engineering constraint, not a theorem that every Promise continuation is inactive. A known negative arriving before commit processing begins must abort the affected operation batch; deliberate task-delayed delivery is a required witness.
After commit processing begins, success remains conditional on persistence. On successful complete preserve the actual disk facts, but a changed context receives no stale Saved/paint; on abort there is no operation/outbox/sequence/U advance. A new late negative stays known and starts/continues its own obligation; crossing the cut never licenses ignoring it.
Control-only refusal retains the exact prior body and U/sequence. It may preserve OPEN and known refusal but cannot invent a new proof, clear OPEN or install staged operations. Closure requiring new immutable proof bytes uses a newly sealed proof-bearing candidate, never the control-only shortcut. Clearing OPEN/checkpoint transitions require the reviewed semantic amendment; this file does not enable them.
releaseOwner requires no newly enabled producer and validated durable closure or explicit transfer of complete unresolved obligations to a successor that remains recovery-blocked. Transfer does not retire the old callbacks: their children stay unresolved until the actual delivery barrier is proved, and any late learned negative still belongs to that obligation. No release merely because the local command queue is empty. Lock loss/termination has no assumed finally/cleanup execution.

## 4. Restart, concurrency and failure outcomes

| Observed cut | Current process | Fresh process / durable result |
|---|---|---|
| Valid clean checkpoint, no unresolved obligation | Existing continuous/bounded permission rules | Bounded restart may work; no refill and no claimed monotonic continuity |
| Arm fails before informative ingress |3 unless stronger knowledge; no producer action; retain input | Prior authenticated generation unchanged; no invented OPEN or new knowledge |
| OPEN committed, kill before/after possible dispatch | Live owner alone knows its runtime phase |18 before truth until child-specific recovery; ARMED does not mean never dispatched |
| Learned sign-out/revoke/closed account; outcome write fails |17 refuses writes, preserving other integrity dimensions | Durable OPEN →18 until re-proven; no renewed-token escape |
| Authenticated rejection; raw/outcome write fails |19/recovery, no resurfaced rejected contribution | OPEN →18; cannot recreate durable19 from lost RAM |
| Learned rollback/expiry/allowance invalidation; write fails |20, no operation or allowance advance | OPEN →18 until re-proven; unchanged budget is not permission to forget the event |
| Successful retained rejection/standing proof | Apply verified negative dimensions and atomic effects | Re-evaluate authenticated proof; no automatic clean restart or erased negative |
| Token expiry/network absence alone |11 where standing/lease/budget permit logging; pending network child need not stall a local commit | Unresolved exchange after restart still follows OPEN recovery, not a fabricated standing17 |
| Local commit abort/quota; no stronger fault |3, no Saved/ghost operation, retain input | Complete previous pair unchanged; pre-existing OPEN remains OPEN |
| Child added or known negative before final cut | Restage/abort; no partial batch or stale grant | Complete prior operation body/U retained; obligations survive |
| Complete succeeds, context changes before result delivery | Committed but unconfirmed; suppress stale Saved/paint | Preserve durable facts and new negative obligations; never call success rolled back |
| Missing key/invalid proof/unknown validator |18 before truth; independently known17/19/20 retained separately | No reseed, previous promotion, default CLEAN or disappearance of rejected facts |

These are dimensions, not a universal scalar ordering: preserve BRIEF-W6's17 >20 >3 write precedence and independent18 integrity/19 rejection consequences. Namespace changes cannot leak the prior account's typed values; same-scope refusal retains them.
**C1 interrupted-arm review blocker:** compare kill immediately after durable local arm but before sampling with kill after observed negative and failed capture. Also compare kill after enabling the genuine SIGN_OUT/CONTEXT_SWITCH control WITHOUT activating it against kill after intent delivery and failed persistence. Each pair may leave the same durable OPEN. This proposal uses18 for unresolved OPEN; continuously enabled controls could therefore make every idle kill recovery-blocked. It must prove clean ordinary offline restart remains available and obtain technical review against BRIEF-W6:54,63,69–72. Do not silently extend an unresolved-exchange exception to every sample/control, treat an unpressed action as known intent, or evade learned knowledge. A genuine unresolved rule conflict needs its exact red witness; bounded24h/64 is not reopened by default.

## 5. Named acceptance cuts — requirements, NOT RUN

| Case | Required actual boundary / positive and negative controls |
|---|---|
| K1-FIRST-INGRESS | Real manager + T2/format2 + owned producer; hold OPEN transaction before complete: zero request/status/header/parser/sample/signout activation; release completes, then the intended producer runs once |
| K1-ARM-FAIL | Quota/abort at every arm await/request/complete cut; zero dispatched action, exact prior pair/ops/outbox/U, retained input; stronger-state controls |
| K1-RAW-NEGATIVE | Actual signed rejected response and sign-out intent; fail raw/outcome persistence; new instance sees OPEN18 and cannot repaint contribution; successful recovery idempotent with exact original bytes |
| K1-W5-BINDINGS | Actual generated P-256 responses for every table route; wrong op/after/watermark/schema/challenge/key/version/bytes and reordered or missing receipts refuse before sink; valid full originals deliver once; fail each /time arm/bind cut and prove zero premature fetch |
| K1-W5-ERRORS | Authentic scoped403 retains published17; forged/foreign/opaque/unversioned error cannot manufacture standing;401 does not revoke offline lease; malformed200/partial body/503 never drain or clean OPEN; stronger known states survive failure/restart |
| K1-LOCAL-SAMPLE | Actual permission/face/reopen callers; rollback/expiry + failed persistence/relaunch, kill-before-sample comparison, clean completed-root bounded restart; retain any C1 conflict as RED |
| K1-PENDING-LOCAL | Hold real network child unresolved while permitted local operation durably completes; retain child/full outbox; no queue starvation, false sync or OPEN clearing |
| K1-MEMBERSHIP-CAS | Two child registrations/close attempts from same basis; stale candidate restages, no missing child or lost evidence; fresh retry succeeds without duplicate effect |
| K1-HANDOFF-UI | Two actual same-origin pages; native pointer/Enter/Space/assistive sign-out; kill with an enabled but UNPRESSED sign-out/switch versus delivered intent plus failed persistence, including clean idle-restart control; queued action versus disabling; paused owner versus lock release/termination; no timeout stealing or old-scope paint; preserve any C1 conflict as RED |
| K1-FINAL-CUT | New negative during bulk crypto, deliberately delayed before commit processing, and complete-before-reply; full pair/op/sequence/U comparison and committed-unconfirmed result |
| K1-REPLAY-CLOSE | Every await/kill in capture/resolve/close; malformed/missing/foreign proof, duplicate child and stale positive basis fail; negative from stale basis remains considered; correct complete closure permits clean restart |
| K1-REAL-BINDINGS | Actual selected FAPI/Clerk/W5 config, request/header/challenge and callback inventory; production key/recovery/phone checks separately required, never passed by loopback fixtures |

Fault injection must cross real tested boundaries and preserve exact restored source bytes; source-pin/syntax failure alone is not an effective safety witness. Expected answers derive from the contract, not candidate output. Synthetic local keys/session resources are permitted for local proof; no private records or credentials in outputs.

## 6. What is resolved by authoring; what still blocks implementation/adoption

This document supplies the missing finite producer inventory, proposed ownership model, root/child schema, guard placement, closure/failure rules and named executable cuts. Review may now challenge a concrete artifact instead of another lifecycle summary. It has not proved completeness or replaced an existing accepted contract.
§2.1 now supplies explicit W5 v1 producer/null, request, challenge, capture and signed-result joins from published sources. It remains a proposal, not an implemented manager or accepted ingress proof. **Author/review bindings still OPEN:** AUTH_EXCHANGE/UI/local-sample schemas; all local clock/face entry points and the C1 crash comparison; actual Web Lock/MessagePort ownership and native UI barrier; complete replay/closure and origin-evidence implementation; safe proof retention/reclamation; complete source/function allowlist. Proposed implementation locations are w6/managed-ingress.mjs plus narrowly reviewed public-client and frame-repository joins; no client-core, W5 route or frame schema edit is authorized here.
**External/profile evidence still OPEN:** FAPI headers/version/cookies/CORS/challenges and JWT acceptance; provider-specific cancellation/delivery guarantees; T1 finite bounds and exact final local-sample treatment; R1 complete outcome/recovery/resource evidence; P1 auth-material/body/frame/old-key custody and security budget; actual installed-phone and recovery/soak applicability. A missing remote credential does not prevent authoring local schemas/tests, but a mock cannot certify those bindings.
NEXT: same W6 claim, publish this proposal with a separate K1 verdict request. Close the listed authorable producer/capture/source bindings, then independently review the exact implementation profile before production code; run local cases first and keep remaining remote/phone/CLOCK obligations explicit. No new owner purchase, reviewer promotion, parallel product stream, permanent guard or protocol waiver.
