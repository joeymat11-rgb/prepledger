# W7-PREVIEW — published implementation slice

## 0. Source and boundary

Execute QUEUE W7-PREVIEW from9a42b10: PLAN-M3-v1 W7 read face and m1/MOCK.md's public artifact. One Today screen and its weigh-in/Why interactions, explicitly synthetic and memory-only. Use the actual accepted engine to project synthetic facts; its rules remain unchanged. The original private mock and live ledger are not preview inputs.

## 1. Deliver

Under this directory: runnable browser view with ratified typography/colors/layout, replaceable synthetic model, a local build/serve path using existing pinned dependencies, focused tests, README with one owner-facing opening instruction and technical reproduction for the integrator. Bundle only allowed preview assets; no repository-root server or source/ledger exposure. Fonts may use the mock's declared system fallbacks; document any fidelity limit.
All model-driven numbers/claims come from the engine at a pinned synthetic date. An in-memory weigh-in updates the preview only, says so, and never emits durable Saved/Synced. Unsupported Gym/plan-choice/history actions remain visibly unavailable in this slice; do not construct unapproved screens or imply their behavior works. Preserve the ratified product face, with clear separate preview controls/status.

## 2. Gate

Synthetic projection parity against direct engine reads, input validation/no mutation on invalid input, no persisted or network athlete operation, reset/reload behavior, usable weigh/Why/back keyboard flows, no stale hardcoded numerical claims, ratified template/style provenance. Browser smoke on narrow and desktop viewports; physical iPhone/VoiceOver/performance gates remain NOT RUN. Reuse unchanged public checks, full suite/selftest and strict under AGENTS preparation. No suite edits.

## 3. Report

One `rebuild/m3/REPORT-W7-PREVIEW-ASTRA.md`: base/claim; what/why; exact mock mapping and disclosed preview substitutions; model parity/browser/test outputs; scope/bundle check; seams; uncertainty; elapsed work; NEXT. Publish synthetic evidence only. Preserve private gate custody/verdict-only rules.

## 4. Review and NEXT

Cowork independently verifies; Claude Code integrates accepted changes. W6 and W5 contracts gate later storage/server binding, while W8/W9 and M2 audit gate the private port. This preview alone earns no I1, PHONE-2, OWNER-TODAY or durable-storage PASS. Open one PR, do not merge.

## 5. Never

Never change frozen app/engine rules, conformance laws/oracle/runner/reference/manifest, seeded soak or original public mock. Never read the private original for this preview, persist entered athlete data, introduce a fake server receipt, copy secret-bearing config, change dependencies/lockfiles, duplicate audit/W5 or quietly add Gym/consent/product-rule work.
