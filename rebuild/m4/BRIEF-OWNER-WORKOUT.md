# EARNED — complete owner workout slice — proposed technical brief v0.29

ASTRA · 2026-09-08 · Original publication base `213300c6c49f772dcb2ff859e1c1375dd571b837`; v0.16 follows retained PR46 `01cb2edf8eaa3bde64f838b95fcfd433b2c4e971`. Its reader received scoped independent acceptance; this successor adds the actual signed legacy-observation projection candidate, documentary corrections and the owner's full coordinated-plan goal. Stage A as a whole remains unaccepted. Prior scoped technical-text verdicts remain attached to their actual revisions and do not close M3/M4/M5, authorize a private import, waive a gate or change a training rule. The document itself does not ship app behavior.
Owner instruction, adopted at the retained checkpoint: prioritize the earliest qualified complete workout-logging app. Today and check-ins support that delivery; they are not its substitute. The existing coordinator, two implementation streams, retained worktrees, model settings, reviewer and integrator remain. This is an executable slice brief, not another maintained plan or work queue.
Sources: runtime sheet v1.7.38 at `rebuild/conform/gates/inputs/EARNED-RUNTIME-SHEET-v1.7.38.txt` (R below); public mock `rebuild/m1/earned-mock.public.html` (H below), pinned by `rebuild/m1/MOCK.md`; ROADMAP M4; `PLAN-M3-v1.md` §§1/2/6; `FINISH-PROMPT.md` §§5/9; accepted W5/W6 refinements and existing M2 repair contracts. Source coordinates below are on the publication base unless explicitly identified as the retained W6 draft.

**PR47 process amendment and factual correction PR48 accepted/integrated.** Accepted PR47 source `2dd1bfe489833677237f62185275f6ea2a175eaf` merged at `927728a66d7f759c091339d743cec59bc911bdcb`, with record `db044538f63ebc85dd34432e7a4d7ffa8991c959`. PR48 source `647ce0461f6d75474d7268ea163d082ab0752972` merged at `ee3de147a668e7b0867b988734a3088b9a29b008`, with record `58c2fc9ad18038aad5c4be34920fd20dcb978cdb`; verified in integration `28ff3be3a0c47fa76b642015ac3757da5c76548c` DECISIONS. The accepted process brief §§1–4 governs; its two first-owner deferrals still require the exact executed prerequisites in §4 and never award D8/D9/HANDOFF/full M3/beta PASS. This is status reconciliation, not new PR46 acceptance, schema/spending/import/model or C3 authority.

## 0. Claim, delivery and prerequisites

v0.28 publishes the next ACTUAL client-command boundary in spec/WORKOUT-BASIC-SCHEMA.md §6, based on retained W6cdf698d and R1bec056d. Existing encrypted-client schema mismatch refusal already works; keep it. Add explicit selected-version workout commands through the real T2 committer and W6 atomic bridge, preserving legacy methods and all gate precedence. This implements the needed command component, not the complete immutable schema or activation: non-workout schema2, richer edits, nonnumeric loads, partitions, capture and legacy qualification remain required before first use, not removed from scope. Applicable boundary review precedes adoption of accepted client/W6 source.

v0.27 records actual retained R1 workout admission atbec056d6b8f86069c500d958e86f212bd6e5f392: Opus Message82 ACCEPTED the implementation and the exact two-binding source-pin exception, with no required corrections. Author mandatory gates/corrected179/179 and CI7SUCCESS/3SKIP both OS pass; reviewer executed core34/34, actual HTTP9/9, effective/restored bite, source-pin9/9 and18 own challenges. This is a bounded component, not complete schema, activation, corrected-state fold, client Saved, recovery or private use. Source/acceptance details and the next cross-version question are in spec/WORKOUT-BASIC-SCHEMA.md §5. Full goal and all remaining Stage A/first-use gates are unchanged.

v0.26 incorporates Opus80 C-1–C-5 before actual source adoption: lease-granted gating after existing version/revocation/range/reuse checks; scrub callback rejectionCode; explicit boolean results; stable reference union order; disclose workout-only version2 limitations. Permission to implement is bounded to this amendment, not full schema/activation. v0.25 published the exact next authority-boundary amendment in spec/WORKOUT-BASIC-SCHEMA.md §5: use retained R1d267, preserve its issued-lease resolver, and apply the accepted new shape and typed start/target dependencies inside actual admission/staged D1. It requires affected technical review before adoption. This is the next part of the real command path; complete manifest, all semantics, client activation and capture recovery remain required, not silently deferred from first use.

v0.24 closes nonblocking Opus Message76 F-3: the new count/effort representation rejects negative zero while preserving ordinary zero and all legacy bytes. Message76 independently ACCEPTED the v0.23 module with the86-test successor review pending; the correction changes no training prescription. v0.23 implemented the shared basic-workout shape module after Opus Message74's interface permission at66658cc and its two incorporated corrections: accurate reference-guard coverage and deterministic diagnostic precedence. Actual primitive/byte tests and an effective restored missing-reps fault cover this pure boundary. The module is not yet wired into client/authority/W6 or an issued schema; it does not repair live saving by its presence. Capture still needs its P1 storage/recovery mapping, without changing the empty start payload or substituting local-only history. Full Stage A, private/phone and scientific qualification remain open; the full product goal is unchanged.

Owner reaffirmation, September8: deliver the smallest dependable private app for Joe and Dad as soon as reasonably possible, including accurate transfer of Joe's existing data, reliable workouts and justified supported prescriptions. Full goal v2 below remains unchanged. v0.17 follows PR46 `3b88b52db33f43fafcb56de7cfba266c681de4f5` and adds the read-only history UI consumer; its dependency's scoped review remains pinned to that revision. The subsequent scoped review found three consumer gaps. This successor repairs only the nonshipping view/projection issue and reference joins; original reviewed bytes/verdict remain preserved. No accepted reader, writer, rule or private fixture changes.

Owner amendment23 progress reporting is adopted here and in the existing compact checkpoint. Reuse these five first-use outcomes as a stable reporting baseline, not another queue or gate: (1) faithful isolated records and approved import — M3 TODAY-PORT/IMPORT-ROLLBACK; (2) complete correct, justified workout/plan actions — WORKOUT-SCHEMA/ENGINE/JOURNEY/CHOICE/SAFETY; (3) durable logging and verified recovery — W6 and WORKOUT-RESUME; (4) qualified real authority/account isolation — AUTH-D1/HTTP-190 and applicable W4/W8; (5) independent integrated acceptance and actual Joe/Dad phones — OWNER-WORKOUT/PHONE-2/PHONE-RESTORE. All five contain remaining work; accepted components are not completed outcomes. No defensible overall percentage is calibrated yet, and historical3/10 is not30% of this app. Report implementation, independent acceptance and each person's phone readiness separately, about every30minutes during active work and at material checkpoints; percentages never imply time remaining. No new scheduler or funding authority.

### Full product purpose retained — owner amendments 21–22, September 8

Owner amendments24–25 adopted: assistant PM owns bounded helper dispatch/capacity; Astra owns product integration and independent acceptance. EFFORT-03 and HEAD-IDB-01 completed in parallel, then stopped; zero active now. Reasonable added usage for useful acceleration is permitted within existing credit limits; net savings is not a prerequisite or a demonstrated result. Their evidence below is author preparation, not independent acceptance. No recursive swarm, paid fallback, private/reviewer edits or changed gates.

Canonical owner-approved purpose: [PRODUCT-GOAL.md](C:/Users/joeym/Documents/Codex/2026-09-06/earned-process-proportionality-audit/PRODUCT-GOAL.md), version2, SHA256 `43ebdeed5986e238969c9cdf759a40f02f7066b630c2eb6af54bc1c6fe5ad5c5`. This local owner record is the reference, not a shipped asset or private fixture. Earned's complete goal includes individualized training, nutrition, recovery/lifestyle, agreed phases, adaptation to response, useful explanations and reliable private records. These serve one coherent plan: what to do today, when to maintain/change course, and how agreed changes reach actual plan actions with measurable evaluation criteria and less daily planning. More inputs/automation are not inherently better. The present workout slice serves that goal; logging-only delivery does not satisfy it. No completed scope below is implied by a plan or a green unit test.

| Goal area | First-pilot disposition and existing work | Later or unresolved scope retained |
|---|---|---|
| Training, exercise selection and progression | Required intended pilot support: correct complete workouts and justified individualized load/reps/sets/effort; WORKOUT-ENGINE/JOURNEY/CHOICE and the rule inventory below. Implementation/acceptance remain open. | Broader selection/onboarding breadth follows demonstrated need; Dad's goals, capabilities, equipment and preferences are not supplied by Joe's seed. |
| Nutrition/body-composition priorities | Workout pilot does not claim a qualified nutrition coach; Today/check-in and extracted energy reads are existing work, not scientific validation or private availability. | Nutrition intake/energy/macronutrient prescriptions and body-composition interpretation remain later work under ROADMAP M4/M5 and the energy rule review; exact pilot exposure unresolved before shipping affected advice. |
| Recovery and relevant lifestyle | Reachable pain/illness/interruption/re-entry safeguards remain required for pilot prescriptions (WORKOUT-SAFETY/RESUME). | Broader activity/sleep/recovery prescriptions remain later work; unsupported advice is not presented as qualified, and no personal health facts are inferred. |
| Agreed phase and justified transitions | Individual goals/phase used by the pilot must be explicit rather than inferred from scale/performance; legacy engine labels are not proof of physiology. | Phase transitions and their adjustment rules require the existing evidence/applicability review; exact first-pilot phase UI remains unresolved, not a new automatic release prerequisite. |
| Adaptation from individual response | Supported workout progression/hold decisions must use faithfully recorded per-set performance and reviewed rules, justify maintaining/changing course, and carry agreed changes into the actual plan under required consent. | Wider coordinated nutrition/activity/recovery adaptation and phase-response evaluation remain later; observed trends do not establish causes and no automatic cross-domain rule is invented. |
| Explanations, confidence and measurable criteria | Supported meaningful workout choices must agree with actual plan state: what changed, why, next action and measurable response criteria, with missing evidence visible (WORKOUT-CHOICE and ratified Why flow). | Coherent follow-through across later prescriptions follows their feature work, reducing separate recommendations the person must coordinate; no mathematically optimal or superior-result claim. |
| Private, reliable records and useful ongoing use | Required pilot scope: isolated people, correct port, truthful saving, recovery and actual-phone workout proof; existing M3 and WORKOUT gates remain. | Stable pilot updates, privacy-safe issue reporting and broader public scope continue through the retained process; no new service or monitoring before authorized live evidence. |

This disposition stages the approved purpose without erasing any area or turning the complete vision into day-one scope. Material contradictions or scope changes use the existing decision record and applicable acceptance; ordinary implementation sequencing continues. The current authenticated-history work supports reliable records and later individualized adaptation. Prescription correctness and rationale stay separate within the same necessary feature review. This new owner-purpose reference does not change the currently pinned reader-review source or imply reviewer acceptance of this documentary addition.

### First-use route decision — owner amendment 17, 2026-09-08 (proposed delivery slice)

PM selects **B: finish the smallest useful rebuild, reusing the working app's workout interaction patterns**. This does not select a new architecture or authorize copying its bootstrap, persistence or training engine. The immediate target is correct complete workouts for two isolated private users; a synthetic screen or Today-only release is intermediate evidence. Existing scoped verdicts remain attached to their original bytes. This new section is preparation pending its applicable technical acceptance, not full Stage A acceptance or a release deferral.

Evidence boundary: reuse the supplied source inventory at `9dd8dae` compared with locally recorded main `864846c`; this is not deployed-app verification. Additional focused inspection used PR46 `87366ae`, whose unchanged `src/app.jsx` SHA256 is `d1ac52b547beb023df2dc31b1b081487e3ed2e19699377252eb115e0d5faee5c`. Do not read or distribute embedded history. Source coordinates below refer to that file; no private state was accessed for this comparison.

| Route | Implemented reusable behavior | Exact additional work and first-use failure if omitted | Route-specific engineering estimate |
|---|---|---|---|
| A: separately isolated adaptation | GymMode, draft resume, finish recap, history/export/recovery controls | Remove personal bootstrap/configuration and all owner-bound sync/restore/diagnostics; isolate every storage/credential/backup path; repair commit-before-close and per-set input flow; reconnect the accepted repaired engine; prove the applicable consent, recovery and phone contracts. Separate origins alone do not fix copied data or a wrong backup destination. | 110–195 h, low confidence, **excluding** common defect/obligation closure below. A simpler local-only fork could be less work only after a concrete accepted replacement for the current recovery/authority contracts; no such exclusion is assumed. |
| B: retained rebuild + reused UI patterns | Extracted engine and accepted repairs; scoped authority/client/bridge work; existing workout presentation and decoder preparation | Close supported history/provenance/command encoding and producer/consumer joins; finish durable storage and qualified provider path; wire a full workout rather than a Today preview; perform recovery/isolation/phone proof. | 82–142 h, low confidence, **excluding** common closure. This is a work estimate, not measured agent time or a delivery date. |

Why B wins under the same present bar: A does not avoid the known engine findings and would add an independently maintained personalization, save and remote-recovery boundary. Retrofitting the already required operation/storage contracts into that monolith converges on B while duplicating work. B keeps reviewed repairs in one engine and lets us reuse the useful interactions without inheriting the old persistence/identity paths. This is a code-grounded planning judgment, not a benchmark proving B is faster; revisit only if the next bounded implementation exposes a route-changing fact.

The focused source witness extracts **actual** `gymEntries`, GymMode `finish` and App `save` callbacks with a synthetic engine-result stub and synthetic storage. Normal, quota and guard cases all reproduced as expected: on the two refusal paths the old ledger survives and an error banner is set, but both drafts have already been removed and `onClose(lines)` still runs (`20034–20042`, `22131–22186`). This is not a silent-error claim, React/device test or proof of engine output. It demonstrates a concrete recovery/recap repair required by A. The same completion path carries one override per lift (`20040`); the new app's actual per-set loads cannot be assumed implemented by copying it. D43 independently witnesses the existing writer's early-return load error. No new D number, frozen edit or private witness was introduced.

| Remaining B work | Existing role and dependency | Required evidence | Estimated engineering h |
|---|---|---|---:|
| Supported history/legacy provenance and minimal workout command mapping | A; Stage A, existing Q1 only for answer reuse; C acceptance | Retained 21/21 decoder preparation extended at the actual boundary; unknown remains unknown; original bytes and accepted-history scope preserved; no inferred plan consent | 16–28 |
| Historical capability issuer, schema transition and current-head producer | retained R1 A; published reviewed contracts, sufficient issuance bounds | Authenticated immutable lease registry, atomic activation, late old operation/replay and failure cuts; real transport verification | 16–24 |
| Complete durable consumer and key/knowledge recovery joins | retained W6 A; producer contract, I custody | Failed informative persistence/relaunch fence, actual IndexedDB and signature-before-drain tests; T1/K1/P1 and required existing gates | 12–24 |
| Complete synthetic workout UI using actual engine | A; closed minimal command/projection contract, accepted repairs | Start, actual per-set load/effort/unknown, skip/early finish, correction, interruption, history and every reachable advice/consent action; no fake save | 18–30 |
| Qualified deployed provider and fault probes | I plus retained R1 A; W4/identity/secrets, resource/byte-preflight | Real US deployment, scoped two-athlete failures, bounded resource evidence and request/reply loss; unavailable evidence stays BLOCKED | 8–16 |
| Independent composition, recovery and target-phone acceptance | C/I/O; preceding synthetic proof, actual two devices | Existing mandatory package, restore/rotation/account recovery/import rollback, phone accessibility/lifecycle and applicable C1–C11; approved private import only afterwards | 12–20 |

Common work is deliberately separate: the original register totals 131 h; subtracting the four accepted repairs' original 16 h leaves a **historical 115 h estimate**, not a fresh remaining-work forecast. D30 public review is not full acceptance. The 15 non-D obligations and exact full artifact/review requirements still need accounted closure; their overlaps and cost are not quantified here. Neither route is promised complete within its route-specific range, and no unchanged full suite was rerun to manufacture route evidence. Excluding a finding requires actual transitive isolation and the accepted change process, not merely hiding a screen or a non-triggering old ledger.

External wait is separate and unestimated: actual designated C/I capacity/custody, provider provisioning, Dad's supplied profile and phone, owner physical actions, and storage qualification. The seeded soak's earliest readback is October 5; that date proves neither the production store nor Gym qualification. No broad signup, extra platform, unrelated visual expansion or new scheduler is introduced. No existing applicable gate is dropped. Confidence in B's reuse decision is moderate; timing confidence is low until the first implementation slice and common closure are measured.

**Next bounded Stage A slice (6–12 h within the first row, not additional scope):** connect retained version-specific history interpretation to the existing R1 verifier, then close its product consumer contract. Actual-source reconciliation at retained R1 `003c816` changes the next action: `authority/admit.cjs:45–53` ALREADY has `resolveIssuedLease`, wired by `w5/bridge.cjs:176–187` to `issuedLeases` and existing issuer machinery. The separately reviewed one-line legacy proposal at87366ae is unnecessary on this path; do not add another `leases` registry or overwrite the resolver. Its verdict remains evidence only for its actual older source. Existing R1 schema/activation/resource qualifications still apply; discovery of implemented code does not close them.
New nonshipping history-reader candidate calls the actual R1 projector/public-signature verifier and retains exact original log/operation/disposition row bytes. It does not manufacture a W5 receipt to fit the old decoder. A captured authenticated prefix is not current-frontier authority or a progression-qualified workout. Legacy slot extraction is preserved, missing basis/eligibility/liveness remains explicit, and forged/foreign/incomplete proofs return no history. Enumerated future product scope remains `rebuild/m4/workout/history-decode.cjs` plus focused test and this report/brief; actual R1 imports require the reviewed integration dependency. No production boundary is edited now. The actual R1 source fixture creates signed synthetic rows, not an issuer/D1/HTTP/IDB/phone proof. New16/16 observations and one effective invented-basis fault supplement the retained21/21 diagnostic; they do not rename it implementation or make the complete app done. Publish this exact consumer contract for applicable C execution before promotion. Preserve Q1, actual correction/liveness fold, currentness, storage, prescription and independent gates.

NEXT: preserve the in-flight historical-lease review and its separate process verdict. Close this first-use slice's exact input/output contract through the retained stream, then build the eligible boundary; continue common accepted repairs when their actual C/I conditions permit. Do not let the process amendment become a pilot prerequisite, start a second fork or ask the owner to manage tasks.

### Owner amendments 18–19: prescription quality and proportionate private use

The paramount pilot is individualized, well-supported workout and progression guidance, based on faithful performance/context, serving Joe's and Dad's own goals. It is not logging-only, nor a claim of mathematically optimal coaching. The owner permits tracked minor bugs during useful private use while public development continues. This supersedes a flat open-defect count as the pilot release decision; M2 CLOSED and public milestones retain their separate accounting. No known material prescription error, false workout fact/Saved, cross-user leakage or unrecoverable loss is transferred to users as routine discovery. Any actual exclusion from an existing technical release clause is documented with the shipped dependency evidence and receives applicable technical acceptance. The frozen laws/results remain truthful; no exclusion is accepted merely by this paragraph.

| Current slice rule | Actual source/call path | Basis and applicability | Existing evidence; material gap; status |
|---|---|---|---|
| Held-lift instructions and projection agree with the effort plan | `writers.cjs@499d2aa:756–781 rirPlan`, `2728–2729 effortWords`, `153–183 sessionFromDraft`; `today.cjs:166 genSession`; Gym `src/app.jsx:19931–19934,20268` | Existing opener-only hold policy; correct carry-through/copy is separable from changing failure frequency | EFFORT-01 direct held3-set2/1/0 versus all2 wording is real but NOT proved on normal Gym. EFFORT-02 real public engine reports fresh/resumed cards omit holdFlag: synthetic held single-set becomes0 instead of direct2; multi-set loses hold explanation. APM replayed19 outputs; Astra checked the source join. Confirm actual supported pilot producer/configuration and plan-to-render coverage before classifying its first-use consequence. No user-live trigger, mounted UI, full-writer or clinical qualification claimed; no engine fix made. |
| Preserve authenticated facts without inventing missing plan/consent | Runtime A1/A3/B14; actual R1 `verify.assemble → project.payloadState`; retained decoder → proposed reader | Explicit data/consent policy, not a physiological claim; both people | Existing21-case diagnostic plus new actual-signature16-case candidate; original rows retained, forged proof refused. Currentness/eligibility fold/durable/UI join absent. IMPLEMENTATION PREPARATION, no prescription acceptance. |
| Actual load and effort/unknown mean what the athlete entered | Runtime A3; mock first/last effort question; old Gym `gymEntries → finish → completeSession` | Data fidelity and explicit effort vocabulary; not evidence that the requested effort is optimal | D43 entered-load witness and new save-wiring witness remain relevant. Mixed-load sets/unknown effort require actual new writer/UI proof; no inferred zero or exact-three. FIRST-USE MATERIAL when reachable. |
| Prescribed effort follows an individually justified policy | `src/app.jsx@87366ae:6672–6695 rirPlan`; affected callers `18387`, `18400`, `19932` (GymMode), normal default2/1/0, alarm floors0 to1 | Existing coaching/product policy; research informs proximity-to-failure but does not prove this exact taper for every user | Frozen tests establish compatibility, not efficacy. Default terminal0 and comment6693's occasional all-out last set differ; comment2496 says terminal0 BY DESIGN. Resolve both comments and all callers, retaining the alarm exception. Targeted primary-source triage below finds no universal optimum; rationale/applicability must be reviewed before copying to Dad or claiming science mandates it. OPEN MATERIAL PRESCRIPTION QUESTION, no rule edit. |
| Each person gets their own goal/profile/accepted plan | Owner amendments17–19, runtime scope/consent, actual retained account separation | Owner/athlete choices plus applicable evidence; never inherited personal bootstrap | Existing identity tests do not supply Dad's goals/settings or prove his plan appropriate. Obtain already authorized/supplied inputs through existing owner setup, then scoped prescription review; initialization and actual two-phone proof remain OPEN. |

Targeted effort evidence (abstract-level triage, not a completed clinical or individualized review): [Robinson et al., 2024](https://pubmed.ncbi.nlm.nih.gov/38970765/) associates closer-to-failure sets with greater hypertrophy, while reporting a negligible estimated-RIR/strength association; the authors caution that estimated RIR and exploratory models leave the exact dose-response uncertain. [Grgic et al., 2022](https://pubmed.ncbi.nlm.nih.gov/33497853/) reports no significant overall failure/non-failure difference and a small trained-subgroup hypertrophy advantage; all included samples were young adults, limiting generalization. These findings support investigating the goal/user/tradeoff, not asserting every last set must reach failure. The specific plan remains an explicit policy pending its individual rationale, not a silently changed rule. Next necessary feature review examines that material issue with full section16 evidence guidance; no extra review stage or broad audit.

Corpus reconciliation: `research-brief.md@87366ae:274–279` already records Refalo2023's failure/non-failure distinctions and a “Refalo2024” meta-regression summary. The latter has no identifier and resembles the Robinson record above; its original source identity remains **UNRESOLVED**, so do not count it as independent corroboration or silently rename the frozen corpus. Keep the explicit PMID attribution for the newly checked record and resolve the older citation during the same material effort-policy work. The scoped reviewer independently confirmed the source/caller/comment evidence and the abstract limits; its literal default-ladder check is documentary evidence, not execution of the complete `rirPlan` guards. Its blanket “every session” wording must be read as the normal default, given the expressly recorded alarm floor.

| Pilot classification | Concrete current application | Minimum closure / tolerated consequence |
|---|---|---|
| Release blocker | Reachable wrong actual loads (D43), unsafe save/restore paths, cross-user bootstrap/backup, unsupported prescription rationale | Relevant red-first correction/proof, exact owner-policy resolution where needed, independent implementation and integrated phone/recovery evidence. No broad percentage or old non-trigger verdict substitutes. |
| Minor, tolerable with recorded workaround | No specific known issue has yet been demonstrated minor in the shipped workout path; do not automatically classify all internal/cosmetic BAR rows | Before first use record actual symptom, bounded consequence, workaround and version. A spacing glitch can qualify only when it does not hide warnings or prevent input/recovery; no claim that such a glitch was observed. |
| Deferred public-only | Public acquisition/signup breadth and unrelated visual expansion, outside the named two-person configuration | Preserve isolated accounts, all reachable prescription/privacy/recovery behavior and required technical acceptance. Do not equate public-only with auth isolation or physical qualification. |

Pilot feedback uses the existing owner/assistant-PM conversation: build/version, expected versus actual behavior, short reproduction steps and impact. Approved privacy-safe diagnostics only; no raw health logs, credentials or new service. Assistant PM may reproduce with synthetic input; Astra fixes with relevant regression checks and reviewed versioned updates. Do not invent automated monitoring before actual deployment/authorization. Dates in New York: September9–10 initial material rule inventory/triage; September10–12 first relevant independent prescription/feature review, conditional on eligible included capacity; September11 revised FIRST-USE FORECAST based on observed work/overlap/blockers. These are provisional review/forecast targets, not release dates. No new scheduler; creditsOFF and heartbeatPAUSED.

V0.14 adds a NON-SHIPPING publication of the accepted proposed current-head text, `spec/current-head-contract-v2.md`, and four review-evidence files under `spec/current-head-review/`: README, original AMENDMENT and two unchanged synthetic reviewer scripts. The evidence README pins their bytes, commands and limits. These supplement the four pre-existing specification/diagnostic attachments; no product file or prior diagnostic changes. Opus5/High's verdict is **ACCEPT AS PROPOSED CONTRACT TEXT**, not implementation, schema, full Stage A, CLOCK, private-use or integration acceptance. The prior scoped C1/SET/Q3 review remains separate. Currentness wire-text design is now reviewed; implementing and proving the joint producer/durable-consumer/issuance behavior remains OPEN with normalizer, legacy-basis and Q1 dependencies. No new task, queue, schedule or model setting is introduced.

Publish this brief/report on `rebuild/owner-workout-brief`, with four NON-SHIPPING SPECIFICATION/DIAGNOSTIC attachments: `rebuild/m4/spec/nonconcurrent-projection.cjs`, `nonconcurrent-projection.test.cjs`, `partition-generation-diagnostic.cjs` and `partition-generation-pins.json`. V0.10 added the last two public files so the actual-client regression runs directly from the PR, without a separate attachment relay; the scratch default root and self-pinned diagnostic exclusion from product-module enumeration are the only adaptations. V0.11 added the proposed generation-history binding; v0.12 maps its accepted-history inputs. V0.13 makes the pure START relevance classifier executable against the unchanged pinned relation and clarifies ask-time/offline answer preservation, with normalization/currentness/schema joins and owner Q1 still OPEN. This amends the builder's earlier docs-only/two-attachment proposal to execute Stage A preparation; it does not authorize product integration. C reviews the contract by execution of its concrete witnesses when available; I records acceptance. No shared QUEUE/DECISIONS edits in this proposal, no duplicate R1/W6 builder and no replacement schedule. The owner is not asked to relay another task.
PR46 retains its separate exact-commit verdict; PR47 acceptance/integration does not accept this workout contract. Its conditional first-owner route remains subject to the accepted process brief §4's actual prerequisite proofs. PR48's outstanding factual reconciliation and I receipt are recorded separately, without inventing a new workout dependency or waiving technical acceptance/private-use gates.
After acceptance, the coordinator assigns the first eligible bounded stage inside an existing stream, on the current accepted integration. Each stage has a published contract, explicit path allowlist, synthetic input inventory and one PR/report. Dependent work may start from a sufficiently closed published contract while C verifies in parallel; it need not wait for the preceding implementation PR's acceptance unless an unresolved result determines that contract. New schema/rule authority still needs its designated acceptance. I integrates only accepted revisions and rechecks the combined result. Local preparation may continue during reviewer unavailability; nothing earns acceptance from that alone.
Stage A — workout command/schema and projection contract: make the existing runtime requirements below executable, with red witnesses against current shortcuts, closed canonical fixtures, required-field validation and exact legacy compatibility rules. Candidate paths are `rebuild/m4/workout/` and narrowly enumerated client/authority/W5/W6 schema or command boundaries. Publish the exact source-function/path delta before changing those accepted boundaries; this document is not a blanket synchronous-core rewrite allowance.
Stage B — data-free actual-engine composition and synthetic whole-workout UI: reuse accepted rules and mock variants, implement the complete facts/projection/interaction path against Stage A. Candidate UI/projection/assets/tests live under `rebuild/m4/workout/`; extend existing package/rig dispatch only by an enumerated amendment. No seed import, private literal, surrogate training engine or fake remote success.
Stage C — integrate qualified W5/W6 storage/HTTP, real account/phone proofs and the approved private port. Reuse the retained work; dependencies below must pass. Synthetic Stage B is useful progress, never OWNER-WORKOUT PASS. Bring supporting proposal/Review/Re-entry/conflict/recovery actions into this slice; unrelated visual expansion can wait.

| Dependency / existing owner | Current evidence and exact completion join |
|---|---|
| M2 close-out — A/C/I | D12/D33/D34/D35 integrated at this base; all45 outcomes,15 non-D obligations, full v4 and BOTH engine gates remain tracked for M2 CLOSED. Owner amendment19 supersedes treating41 unresolved outcomes as a flat private-pilot prerequisite. Classify each actual shipped dependency as material blocker, demonstrated tolerable minor issue or technically accepted exclusion; retain its public/M2 outcome and truthful failing/pending verdict. The old PR47 map supplied no accepted exclusion; this new owner policy alone is not proof of one. D30 PR49 has scoped public review, full artifact acceptance pending. Never copy unrepaired actual-load or other reachable material behavior into the client. |
| R1 — retained A/C | Scoped-read checkpoint accepted, local resource v1 FAIL and v2 proposal rejected (DECISIONS73–74). Preserve the six failed local observations and required local functional/regression/error-order checks. Ledger74 assigns resource closure to deployed synthetic provider-side evidence: exceeded memory/termination FAIL, unavailable evidence BLOCKED; successful HTTP alone is insufficient. No local metric/cap substitution or repeated unsupported whole-isolate-limit experiment. Byte preflight remains its separate reviewed contract. |
| T1/K1/P1 — A/C/I | Publish sufficient time/error/rate assumptions; prove the knowledge-loss fence at each first informative observation; establish real payload/key/old-epoch/account recovery custody. Accepted storage mechanics do not close these three joins. |
| W4/W8/W10a — I, with O only for prepared actions | Actual intended US database/production identity/secrets, deployed probes, isolation/restore/rotation/account recovery/import rollback and privacy-safe telemetry. After the amendment is technically accepted, only actual charged-threshold email and the alternate operator’s full rotate/deploy/rollback qualification may remain DEFERRED/PENDING for first-owner use, subject to the exact prerequisites in §4. Full TELEMETRY-COST/HANDOFF and M3 completion are not claimed; broader beta still requires full HANDOFF. I prepares appointments; no extra spend/import authorization. |
| Physical/C3/independent — A/C/I/O | Actual target iPhone and second phone, applicable C1–C11 and independent milestone review. Today-only C3-PENDING allowance does not authorize Gym/beta. C must map qualification to the production storage configuration; no automatic inheritance from the stub. |
| Remaining owner scope — C/O | Resolve any still-open source-ingestion-for-beta ruling from existing records before assigning that protocol. Preserve M5 and OPEN-M1 completion requirements. Do not repeat an already completed owner decision. |

## 1. Workout behavior and closed command contract

The slice is done only when the owner can open the correct accepted prescription, start its workout, record what actually happened, recover an interruption, finish and inspect truthful history, and handle every offered decision or blocking state without returning to the frozen app. Each action retains typed values on refusal and states the applicable recovery action. No placeholder toast may claim a write.
Preserve the existing operation identity, athlete/device binding, sequence, predecessor/causal parents, effective date/time/offset, schema version, lease, canonical commitment and signed disposition contracts (R150–164). Request-supplied athlete IDs never establish scope. Replays preserve the full original operation under the accepted R1 typed-value/alias/recovery contract, as well as canonical commitment bytes; canonical equality alone is insufficient. Preserve R1's ENVELOPE_MISMATCH recovery path. Display edits never change accepted facts in place.

| Command / field contract | Required mapping and invariants |
|---|---|
| Start prescribed or ad-hoc workout | Envelope carries planned split slot or explicit AD_HOC discriminator and `plan_basis`; `session-start` payload is `{}` (R159,213,232). Allocate one stable start operation identity. Merely starting/logging never accepts a plan or proposal. Preserve the governing basis through interruption and cross-device reconciliation. |
| Log a set | `session-set` envelope carries `session_start_op_id`, `logical_set_slot`, `lift_lineage_id`; payload carries actual load with its immutable unit and reps (R159–164,213,233). Preserve each entered load independently, including mixed-load sets. A fixed pounds schema is permitted; no kilogram-entry feature is added here. Reject unsupported units rather than acknowledging a different unit. Never snap, average or substitute the prescribed scalar to fit an old writer. Slot allocation must remain stable on retry; concurrent attempts remain distinct until a recorded resolution. |
| Reserve / effort follow-up | Preserve the mock's exact meanings: 0, 1, 2, 3+, Not sure; asked first/last only; skipping assumes nothing (H409–421,468–476). Zero is an answer, unknown is not zero, and 3+ is a lower bound, not an exact three. A later answer to an already Saved set is a targeted append-only correction; do not delay or silently rewrite the acknowledged set. |
| Skip set or lift | Envelope identifies start, lineage, `skip_scope: lift | set`, and slot for set scope; optional reason in payload (R160–161,213,233–234). Skipped and not logged are not zero-rep observations. Correcting a skip preserves the original operation and its disposition. |
| Finish normally or early | `session-close` envelope names the start, never a set slot; payload includes `completion_kind` (R160,214,234–235). Early finish preserves every logged fact and leaves the rest not logged. No synthetic completed set, implicit progression acceptance or blind all-lifts-complete summary. Identical operation retry has one effect; distinct concurrent close operations remain recorded facts governed by the ratified resolution contract, never deduplicated solely by start ID. |
| Correct / remove / undo | A fact correction names its target and complete validated replacement fields; removal appends a reasoned tombstone (R96–110,206–210). A plan undo uses the separate guarded compensating transaction with target transaction, seen basis, target-effect digest and compensating-group commitment (R252–271). Do not conflate deleting a local form draft, correcting a set and undoing an applied plan. |
| Choose an offer | Bind the authenticated issued instance, chosen outcome and exact consent digest to its displayed basis. Preserve APPLY/KEEP/NO semantics, equal unselected initial choices, stale/withdrawn suppression, suspended-transaction handling and receipt truth (R272–291; B1/B5–B8/B14). No direct training-plan write from a presentation button. |
| Resolve workout/set ambiguity | Record the complete session partition, component generation, keyed answer commitment and chosen governing basis; record targeted set IDs, duplicate/distinct partition, canonical choices and replacement slots for set collisions (R162–163,214–216,235–236). Incomplete/stale answers cannot silently choose a workout or earn an advance. |
| Pain / illness / return | Reachable session-only or open-interval pain attestation, targeted interval close/correction, illness interval and applicable Re-entry choice (R203–205; B1/B4/B14). Withdraw affected machine advice immediately under existing safety/dependency rules; preserve unaffected acknowledged facts. No diagnosis, guessed recovery date or invented training adjustment. |
| Read / resume / history | Project authenticated accepted operations through contiguous W plus separately identified permitted local pending facts, with corrections/tombstones/rejections and workout partitions applied. Preserve original IDs, units, actual per-set values and governing plan provenance. History is real workout history across enrolled devices, not the current `face().history` suspension list or a date-only grouping. |

Stage A must publish the exact envelope schema before product implementation. The following are narrow **proposed technical encodings**, not claimed existing rulings: reserve is a tagged value `exact` with value 0/1/2 and unit rep; `at_least` with value 3 and unit rep; or `unknown`, `skipped`, `not_asked` without a numeric value. Here `skipped` means the effort question was skipped, not a set/lift skip; `not_asked` requires actual prompt context, never inference from missing legacy data. Completion kinds are `normal` and `early`. An optional field absent in old accepted operations stays absent; never rewrite old signed bytes to add these tags. Canonical absent/null/value remain distinct.
C reviews those technical spellings, unknown/absence preservation and legacy mappings before they are normative; C does not decide a new early-close training eligibility rule. A must prove mapping to existing engine opener/terminal effort and completion rules without treating 3+ as an exact measurement or filling unknown effort. If the existing training contract cannot determine a result, preserve the fact and existing uncertainty/suppression behavior; put the precise unresolved product question and red witness before the owner, rather than inventing a rule.
The schema publication also closes planned/ad-hoc discriminator representation, slot allocation, all required/forbidden fields, quantity units, correction compatibility, action-to-envelope placement, plan undo prerequisites, conflict/offer commitments and error codes. Test wrong/missing fields and legacy-version behavior at both client and authority boundaries. Never reinterpret already signed payload aliases or allow an old permissive schema to admit new malformed operations.
Current implementation gaps are concrete: `client/index.cjs:151–168` constructs pound-only set payloads without reserve, start payload slot aliases and an unqualified close; `ops.cjs:51–76` lacks the required workout envelope mapping; the retained W6 draft at `6f62455` stages only weighIn/logSet/logSession/finishSession. Authority generic validation and 34/35 family GREEN do not prove this complete mapping. `client/session.cjs:52–87` has useful partition calculations but not a complete durable set-resolution command.

### Stage A preparation addendum — basic session contract, 2026-09-07

EFFORT-01/02 helper adoption, September8: source-grounded direct mismatch and its corrected generated-session applicability are incorporated, with no product-code rework requested. EFFORT-02 used12 pinned public sources, the actual pulse/bodyAlarm path and real genSession/sessionFromDraft/rirPlan/effortWords; APM replayed all19 outputs. It tested synthetic no-alarm/AMBER/RED and explicitly configured holds, not actual users, hold creation/release or mounted UI. RED's numeric effort floor is not permission to train; the old screen separately recommends delaying or walking. Astra checked the omitted card fields and consumers. Fixing only the sentence would miss the synthetic projection loss. The next affected implementation must preserve the intended hold and selected per-set quantities through the actual plan projection, then test fresh/resumed/single/multi-set/alarm cases and displayed instructions, under existing engine/receipt/review gates. No failure-frequency rule is selected here. The helper's4m27s then4m40s overlapped integration; useful incorporation is established, not saved hours/cost/speedup. Same helper now idle; no second assignment needed while results are integrated. These local documentary changes arrived after UI review dispatch and do not change its499d2aa pin or count as independent acceptance.

New primary records spot-checked by Astra: [Refalo2024 trial, PMID38393985](https://pubmed.ncbi.nlm.nih.gov/38393985/) is a different paper from the already verified Robinson2024 meta-regression. Its abstract reports similar average quadriceps growth with1–2RIR versus failure in18 trained adults over8weeks, with greater acute fatigue at failure; it does not establish a universal or individual optimum. [Lasevicius, PMID31895290](https://pubmed.ncbi.nlm.nih.gov/31895290/) compared markedly different repetition counts at low load in25 untrained men, not a clean1–2RIR-versus-failure comparison. These bounded checks support retaining uncertainty, not changing the default. Corpus author-intent/provenance remains unresolved; do not count the ambiguous old attribution as separate corroboration. Other helper full-text claims remain its attributed preparation until needed in the existing affected review. No new literature sweep, safety guarantee or personal prescription.

This closes a **proposed representation**, not Stage A acceptance or the complete workout schema. Local preparation is authorized by the owner's handoff §4 while C is unavailable. Original product, schemas, leases and training rules remain unchanged. The previous seven required-field RED witnesses remain evidence; the separate executable proposal below tests additional concrete representations, not a newly implemented client. No repeat owner decision is requested.

**Version and ownership.** Reserve a new workout schema revision through the existing client/authority/W5 lease contract; its number and activation/compatibility handshake are still OPEN. The fixtures deliberately use existing `schema_version:1` to measure current permissiveness; that is NOT the proposed production admission route. Never make v1 accept new malformed workout operations merely because its generic validator already does. Preserve exact already signed/accepted v1 bytes, disposition and replay behavior. New-version validation occurs before local persistence/acknowledgement and independently at admission. `commitBatch` must forward the selected version to `Ops.build`; the current method does not. Common identity/lease/effective/commitment fields stay under their existing owners; arbitrary `extra` input must not override them. No change to `earned/op/v1`, canonical profile, HMAC domain or identity key is proposed.

| Basic action | Proposed additional envelope fields | Exact proposed payload and meaning |
|---|---|---|
| Start | `planned_split_slot_id`: nonempty stable identifier, or reserved literal `AD_HOC`; `plan_basis`: explicit authenticated basis identifier or existing `NO_ACCEPTED_PLAN` | `{}`. No payload slot alias; no default accepted plan. Both sentinel spellings and their collision exclusion require technical acceptance. |
| Set | `session_start_op_id`, `logical_set_slot`, `lift_lineage_id`: nonempty stable strings | `{load:{value,unit:"lb"},reps:{value,unit:"rep"},reserve?}`. Load is finite and greater than zero (R245–247); reps finite. The full reps integer/range domain is still OPEN, not decided by this structural fixture. The pounds-only form introduces no kg-entry feature. |
| Skip set | `session_start_op_id`, `lift_lineage_id`, `skip_scope:"set"`, `logical_set_slot` | `{}` or `{reason:nonemptyString}`; no load/reps fabricated. |
| Skip lift | `session_start_op_id`, `lift_lineage_id`, `skip_scope:"lift"`; no set slot | Same optional reason. Skipping the effort question is a different action. |
| Close | `session_start_op_id`; no slot | `{completion_kind:"normal"|"early"}`. These are proposed spellings; neither chooses a new training-eligibility rule. Preserve distinct concurrent close identities and all unlogged work. |

Reserve variants are closed: `{tag:"exact",value:0|1|2,unit:"rep"}`, `{tag:"at_least",value:3,unit:"rep"}`, or `{tag:"unknown"|"skipped"|"not_asked"}` with no numeric fields. No other keys, tags, null, numeric strings or exact-three synonym. Explicit zero, absence, null and a lower bound never collapse. Reserve remains optional because R232–235 does not require it; missing old reserve is not inferred `unknown`, `not_asked` or zero. `not_asked` requires the UI's real prompt context. Prompt order/first-last eligibility is a separate semantic check; the structural fixture cannot certify it. A later answer appends a targeted correction rather than modifying the saved set.
The six kind-specific envelope keys in this table are forbidden on unrelated basic actions; payload keys are closed per row. Common A3 fields, metadata, lineage ownership, dependency existence, plan-basis validity, lease/time/sequence, causal parents and all numeric domain checks still require the actual boundaries. The executable predicate is deliberately not their substitute. Wrong/missing locally decidable input must return existing state3 without operation/outbox/sequence advancement, retain typed input, and preserve standing/integrity/lease refusal precedence. At admission retain existing signed `MALFORMED`/dependency semantics, full R1 typed-value/identity rules and existing state19 handling; do not invent a new transport success or alter error order. Unsupported-version handling and its exact wire code are an OPEN W5 negotiation join.

**Stable identity and lossless history.** Allocate a start identity once at durable start; allocate a stable logical slot within its workout and lineage before a set retry can become a second user action. Slot representation is an opaque string, not a displayed row number; the allocation/partition collision policy still needs a canonical fixture. Reuse the complete saved envelope after a lost acknowledgement. Explicitly select the session being closed; a close for a different session cannot erase its active draft. Group history by stable starts and accepted complete workout partitions, never date alone. Scope accepted records to contiguous authenticated W and identify permitted local pending records separately; apply correction/tombstone/rejection and collision decisions without deleting original operations. Partial or stale partitions remain unresolved. These are proposed implementation obligations from the existing truth/identity contracts, not an alternate reducer or implicit acceptance of new projection semantics.

| Legacy input / engine seam | Required compatibility handling before any production mapping |
|---|---|
| v1 start `payload.slot`, set `payload.session_start_id`/`slot`/`lift`, close `payload.closed`/`session_start_id` | Preserve original envelope bytes and commitment. A separate read view may label the observed alias, with original op ID and legacy provenance. Do not promote aliases into signed envelope fields or infer missing basis, lineage identity or normal/early completion from display names or flags. Missing identity needed to compute a result stays unresolved under the existing truth contract; the recovery UI is still to be specified. |
| Explicit old numeric effort / absent effort | Retain the actual observation and its source; absence stays absent. New tags are not retroactively added to accepted history. No automatic `3`→`3+` rewrite. |
| Bounded/unknown new effort into engine | `progression.cjs:388–396,415–422,438,441` copies/slices/pads values but has no bound discriminator; `deriveLastMeta:399–412` copies scalar load/reps/effort. No verified `3+`→exact3 transform exists. Preserve the rich operation; stop short of assigning an engine transform until numeric consumers and resulting receipts are proved. |
| Mixed loads, partial or skipped work | Preserve each fact, unit, operation and unlogged status. Do not flatten into a single prescribed `w` or fill missing reps to satisfy a writer. Existing M2 repairs and full engine projection gate remain mandatory. |

**Prospective source amendment, precise scope; not yet permission to edit.** `client/ops.cjs` `validate/build` owns selected-version required fields and closed variants; `client/index.cjs` `commitBatch`, `setPayload`, `logSession/logSet/finishSession`, `activeSession/sessionFacts`, and session-targeted `correction/tombstone` need explicit mapping (current generic corrections hardcode class reading). `authority/validate.cjs` `validShape` owns version-dispatched workout validation; generic `quantitiesValid` need not change just for the proposed tags. Retained W6 `t2-stage.cjs` command allowlist/staging must enumerate actual added commands. No broad `extra` escape, synchronous-core rewrite, new endpoint or engine edit is authorized. Shared schema ownership, new command names, exact version negotiation, correction placement, offer/undo/partition commitments and the complete transitive projection allowlist remain Stage A joins.

**Executed preparation.** The public review attachment contains `proposed-session-contract.cjs`: 45 synthetic shape cases, actual pinned client builder and both canonical encoders, actual authority shape validator, absent/null/zero and bound/exact commitment examples, plus original-byte/alias-rewrite example. Verdict: `SESSION-CONTRACT-PROPOSAL 45/45 PASS | proposed valid 14 | current/proposed differences 29 (20 sheet-required; 9 proposed-profile) | canonical parity 45/45 | synthetic compatibility examples 3/3`. These cases are not the 45-defect register:20 isolate required-field/shape checks and9 concern proposed spellings/restrictions. The examples do not implement a legacy reader. No admission/HTTP/D1/IDB/phone, full required-schema proof, product mutant or Stage A PASS follows. The existing seven-red full-memory-admission diagnostic remains separately labelled. This addendum materially narrows the eventual implementation review while leaving its authority with C/I.

### Stage A continuation — conflict decisions and schema transition

The updated owner objective is the earliest qualified working workout app with his data imported. These remaining contracts prevent recorded decisions being rejected and old pending entries being stranded during that delivery. They remain technical proposals; nothing here authorizes a private import or waives existing gates.
Executed on unchanged pinned synchronous T2/T3: actual `resolveAmbiguity` saves one operation/outbox entry, but its bare numeric `payload.generation` fails authority validation and receives a signed `MALFORMED` rejection. Deleting only that number changes shape validation, but is not a valid fix: R162 requires generation in the envelope. Separately, a schema2 lease with unchanged T2 still creates a schema1 operation and receives signed `MALFORMED`. A deliberately naive fixture overwrite of the only stored device lease strands an older pending operation as `LEASE_UNKNOWN`, while replay of an already accepted operation remains exact. Restoring the old lease cannot undo that terminal rejection. There is no renewal API being exercised; this is a rejected migration design, not an existing deployed upgrade claim. Five controls PASS; two current requirement witnesses RED; one naive design REJECTED. No D1/HTTP/phone proof follows.

| Decision kind | Proposed kind-specific envelope | Proposed sealed payload |
|---|---|---|
| `session-relationship-resolution` | `candidate_start_op_ids`, `component_generation`, `covered_decision_op_ids`, `answer_commitment` | `{workouts:[{start_op_ids,plan_basis}]}` |
| `set-slot-resolution` | `combined_workout_id`, `targeted_set_op_ids`, `component_generation`, `covered_decision_op_ids`, `decision_commitment` | `{attempts:[{set_op_ids,canonical_set_op_id,logical_set_slot}]}` |

All spellings, generation representation and token domains/preimages require technical acceptance before implementation. Each target-family list is a complete, duplicate-free current membership set; each partition is nonempty and covers every member exactly once, with no foreign/additional ID. Every workout block carries its governing basis; disagreements require the athlete's actual selected/attested complete plan, without changing the accepted plan. Every attempt block chooses a member as canonical and owns a unique resulting slot; singleton blocks select themselves. Never replace that choice with the smallest operation ID. Completion/safety/lineage/basis/evidence gates still follow the selection. Facts are never deleted or rewritten.
`covered_decision_op_ids` must cover the current contradictory maximal decisions. Identical concurrent answers canonicalize; contradictory answers leave the affected component unresolved; a later answer covers the entire current decision antichain. All targeted starts/sets/prior decisions must have complete causal dependency coverage so current `admit.cjs:53–61` cannot bypass waiting/ownership checks merely because an ID lives in an extra field. Candidate-edge semantics remain unchanged. Durable generation advancement, combined identity and identical-answer aliasing still require executable reducer fixtures; corrected display time cannot redefine identity (R552–585).
Sort unordered new memberships/partitions explicitly before building the immutable new operation; use complete canonical arrays/tuples, never ambiguous `join("|")` keys. Do not change global canonical `SET_FIELDS` or reorder already signed records. Tokens must be keyed and domain-separated, binding kind, chosen schema, full family/generation, covered decisions and complete selected payload. Exact domains, normalization-collision handling and preimages are OPEN; the existing operation identity/HMAC domain is unchanged. No unkeyed low-entropy answer hash is proposed (R302–305).
Prospective delta, still review pending: `client/session.cjs` partition/canonical/basis/collision helpers; `client/index.cjs` ambiguity/resolution/generation persistence; `client/ops.cjs` selected-version checks; `authority/validate.cjs` and `admit.cjs` relationship validation/reference coverage; one named pure resolution reducer under the eventual approved workout path. Existing helpers are partial: generation is currently in the wrong payload location; one resolution is retained per component; generation advancement is memory-only; collision helpers pick lexicographic IDs/generated slots. They are not a complete concurrent-decision oracle.

**Actual automatic-generation join, now witnessed:** tracked `rebuild/m4/spec/partition-generation-diagnostic.cjs` calls the unchanged receive/resolve/face/store/reconstruction APIs, never `advanceGeneration`. After resolving two compatible starts, receiving a third and then its already-folded tombstoned view, the original two-member answer silently revives, also after a fresh Node reconstruction. A separate initial unresolved-component case exposes a supplied session-dependent output despite state14. Result:6/6 controls PASS;3/3 current requirement witnesses RED. The locally saved answer remains pending; it still suppresses the supplied output after revival, so the witness does NOT claim a post-revival next-load value. Incoming accepted views are explicit seam assumptions; exact source observations, decision/outbox and plan are retained. R557–570 requires suppression and supersession, but a per-batch counter is not an implementation contract: current mutable start views have no shared change/W identity and cannot distinguish stale replay from replacement. Close the deterministic family-generation/history basis, split/rejoin lineage, pending/accepted decision binding, reconstruction and missing-history refusal before changing the named source. Effective-time→membership mapping remains separate; do not invent a time policy or let manually advanced table tests stand in for this receive/restart lifecycle.

#### Proposed generation-history binding — technical acceptance OPEN

Use the accepted receipt of the last relevant component transition as a provenance/version label in the proposed internal tuple `["SESSION-GENERATION-1", ruleVersion, normalizerVersion, athleteId, sortedStartIds, changeSeq, changeOpId, changeCommitment]`. This selects no incrementing numeric generation or released wire spelling. Compare the complete canonical tuple, including both immutable version bindings; a future selected-schema, keyed/domain-separated answer commitment must bind all of it. No delimiter-joined identity, unkeyed low-entropy answer hash, operation re-HMAC or rewrite of old signed bytes is authorized (R96–105,162; the decision table above).

Input is one verified complete athlete history through contiguous W, retaining original signed operations/receipts where T2 strips signature metadata. Published W5 `WIRE.md:12–13,19,23` supplies complete signed pull/snapshot history and requires verification before the awaited sink; T2 `sync.cjs:71–79` supplies a separate contiguous receipt/operation path. A requested-W snapshot proves that prefix, not that it is the current head. The current mutable `receiveSessionStarts` list is not sufficient input. Proposed derived state is `{reducedThroughW, ruleVersion, normalizerVersion, activeComponents:[{sortedStartIds,generationTuple}]}` alongside the start projection and immutable original/decision history; it cannot replace the history needed for reconstruction or a reviewed equivalent authenticated checkpoint.

Under one immutable candidate-rule/normalizer version, classify every ruled relevant live-component change and its affected components/lineages. **Unchanged sorted members do not mean an unchanged component:** a relevant relation, eligible-start or normalizer transition may leave ABC connected with the same vertices and still invalidate its generation. Unresolved relevance classification blocks qualification. Only components classified unchanged retain their tuple; retired components leave the active map; new or relevantly changed components take the triggering receipt identity. Singletons may be tracked internally, as current `client/session.cjs:32–42` already does; ambiguity UI concerns multi-start components. This supplies no missing effective-time, reclassification or concurrent field winner rule.

For the already-ruled example, AB forms at receipt eB; compatible C creates ABC at eC; removing C creates AB again at eRemoveC. The old `[rule,AB,eB]` answer remains History and cannot match `[rule,AB,eRemoveC]`. A split can label each resulting component with the same trigger receipt, distinguished by its complete membership; a later rejoin uses its own relevant trigger. An explicitly approved candidate-rule version change rebuilds under its new version and rejects old-version answers even if membership is unchanged; do not manufacture a receipt for deployment. Rule activation/currentness still needs consistent published binding.

Receipt sequence labels the accepted transition; it never establishes observed dependency or creates a causal edge. Keep semantic parents and only the already-declared tie-break where applicable (R114–121,586–590); no wall/device clock, delivery count or new receipt-order winner policy. Agreement is for the same signed history, not counterfactual authority logs assigning different receipts to concurrent operations. Buffer holes, process the same complete prefix independently of delivery order/batching, and make exact receipt replay a no-op. Checkpoint replay resumes strictly after its verified W; fresh reconstruction yields exact tuple equality. Commit start projection, generation map and basis advance together before publishing eligibility (`client/store.cjs:56–76`). Missing/unprovable history cannot reset generation to1. W6 migration/integrity remains a join: default T2 integrity only counts ops/outbox (`store.cjs:49,82–84`). Affected session outputs must consult the resulting eligibility gate, not just state14 copy or incidental pending-operation withdrawal; unrelated outputs are preserved.

**Pending lifecycle remains explicitly OPEN.** Proposed local membership-changing operations retain their original bytes in a separate guard set bound to the accepted basis; they do not mint accepted tuples. Known effects immediately withdraw affected machine eligibility (R323–326,342–344). Acceptance folds once at the signed receipt and retires its pending guard atomically; WAITING remains pending. But accepted AB/answer → local C withdrawal → C REJECTED does not itself establish that the old answer is current: re-evaluation cannot silently revalidate it. Do not select that rule by implementing a counter or by refreshing a token/sync.

The next technical acceptance must settle: (1) the recommended receipt-labelled tuple versus a canonical content-derived history token, including selected-schema/keyed binding; (2) the accepted start/tombstone normalizer, with the executable relevance sub-contract below reviewed separately; (3) answer issuance/currentness and accepted/pending applicability. Required controls cover accepted AB/answer → local C → ACCEPTED (new relevant tuple, old answer retained), the same prefix → REJECTED (C excluded without automatic answer revalidation), and rejected/rolled-back persistence (prior complete durable view/generation/guard state retained, no new eligibility published). **Owner Q1 remains OPEN:** whether a genuinely ACCEPTED prior answer may be reused after verified current-frontier reconstruction establishes byte-identical complete generation tuple and all required applicability bindings, or fresh athlete confirmation is required. The existing diagnostic's locally pending answer cannot decide that question. No answer-applicability implementation is authorized while it remains unresolved.

**Q3 clarified offline lifecycle; technical contract, not a new owner rule.** If a question's required ask-time verified basis was never established, preserve uncommitted typed input and refuse creating the immutable resolution operation until it can be established. If the question was already issued on that established frontier, it remains offline-answerable subject to the existing lease, standing, durable-save and known-dependency guards; do not demand a new network read immediately before every commit. Recheck the issued immutable bindings against current local known-transition state atomically before commit. A changed tuple supersedes the question; unrelated accepted history with the same tuple does not. Once an immutable answer exists, preserve its exact bytes across pending/WAITING/accepted/rejected reconciliation, even when applicability is withdrawn and a new question is required. No input deletion, invented pending wire kind or generic offline refusal follows (R342–344,557–570,610–617).

The unimplemented information join is specific: a successful authenticated *current-head* complete sync plus reviewed workout-history reduction must establish the component/full generation and rule/normalizer versions, governing bases, complete decision antichain and relevant local-change guard for question issuance. Bind those facts to an issuance record and recheck them against one atomic local state before committing. These are information requirements, not invented deployed fields or a normative record schema. An old requested-W snapshot, empty outbox or `W === authorityW` shortcut cannot prove currentness. Existing T2 `sync.cjs:90–103` reduces plan transactions, not qualified workout history. Retained W6 `public-client.mjs:32,39,104–106` retains wire proofs and reports durable revision with `confirmed:false` when context changed; those mechanisms do not provide a question-issuance predicate. Its current adapter has no resolution command. A reviewed producer/consumer record, reconstruction and atomic check remain required; the pure classifier below does not close them.

#### Current-head wire-text review — v0.14

Use `spec/current-head-contract-v2.md` and `spec/current-head-review/README.md` as the exact reviewed technical proposal plus evidenced publication clarifications. The new proposed pull mode binds its fresh challenge, purpose/profile, scope and complete range/head under `earned/current-head/v1`, reuses the existing server revision assertion and fences the separate client durable commit. It supports the captured issuance attempt; it is neither perpetual currentness nor a new network requirement for already-issued offline Q3 answers. The selected profile/domain is public contract preparation only: no method, record or error code has been implemented by this publication. Keep existing `409 FRONTIER_AHEAD / state18`. The server and client revisions are never compared to each other. §3's nine future cases in that attachment remain NOT RUN; the published eight consumer and four bridge-stub challenges prove only their stated existing-code observations. Apply all source-delta/independent acceptance and actual D1/HTTP/storage/resource gates before any producer/consumer change. Schema/normalizer/legacy-basis/Q1 and the complete Stage A activation remain OPEN.

#### Proposed live-start normalizer mapping — technical acceptance OPEN

This closes the *input inventory* for the proposed binding, not its schema or behavior decisions. Coordinates here are in `rebuild/client/` at retained PR46 `413a3d54d800f160da0f7c7fb0d6dcc9e010f5ac`; all14 product files still match `spec/partition-generation-pins.json`. Preserve each original envelope and signed receipt unchanged beside the derived view. The table names information requirements, not new wire keys or a released validator.

| Information | Existing boundary | Proposed binding and remaining limit |
|---|---|---|
| Start identity and scope | `ops.cjs:54–57`; view `start,device` | Exactly the authenticated session-start op_id and athlete/device; include every accepted device. `index.cjs:80` ownOps is insufficient. Never use a display/date key. |
| Accepted transition provenance | Contiguous receipt path `sync.cjs:71–79` | Full op/commitment/signed receipt, prefix W, source schema and normalizer version. Receipt projections strip signature metadata and may omit operations; they alone cannot qualify this input. |
| Slot/classification | Legacy `payload.slot` (`index.cjs:168`); proposed `planned_split_slot_id` above | Version-specific mapping retains the exact legacy alias/provenance; no missing/empty→AD_HOC inference. New spelling/sentinel and classification remain OPEN. |
| Date/time/offset | Immutable `op.effective` (`ops.cjs:58`); view `date,time` | Retain all original effective fields and exact relation-input source; no receipt/current-clock defaults. Correction/reclassification→relation normalization remains OPEN; corrected display time cannot redefine identity. |
| Governing plan basis | Proposed `plan_basis`; current view drops it (`index.cjs:213`) | Retain authenticated basis or explicit NO_ACCEPTED_PLAN; expose missing legacy basis as missing. Do not infer the current accepted plan or hide valid history. Basis is separate from candidateEdge but required for partition/basis qualification (`session.cjs:52–75`). |
| Liveness and target lineage | View `tombstoned`; generic targets (`index.cjs:80–81`); current edit/removal writers fix class to reading (`:161–162`) | Derive only from a verified version-valid session-target effect with exact same-athlete target/class and semantic-parent coverage; retain originals and removal evidence. A supplied flag, close, or reading-class writer is insufficient. Session-start removal/reclassification, including whole-workout deletion, remains an OPEN schema/fold join outside the per-set profile. |
| Sets/completion | View `sets`; set/close paths (`index.cjs:151,159,164,168`) | Neither controls current candidateEdge/live filtering. Keep separate collision/completion/progression evidence; a closed workout is not automatically removed from ambiguity, and a set correction is not a start tombstone. |
| Decision applicability | Resolution op/derived record (`index.cjs:219–222`), dispositions/outbox | Distinguish accepted-through-W, own pending, WAITING and terminal rejection. Neither a matching derived record nor an empty outbox proves a current accepted answer. Preserve complete antichain/history; no receipt-order winner. |

Current `receiveSessionStarts` (`index.cjs:213`) upserts mutable `{start,device,slot,date,time,sets,tombstoned}` views: omitted starts survive, defaults can use the receiver's clock, and provenance/basis are discarded. Current `session.cjs:19–42` filters Boolean tombstones and relates slot/date/time, not device/sets/basis; its adjacent-date23:00–01:00 rule does not require different devices. These observations are not decoder permissions. Missing required provenance/basis/normalizer information must prevent qualified machine eligibility while retaining visible facts; no new error/presentation enum is chosen. `sync.cjs:90–103` currently folds plan transactions, not this history; boot and generation helpers (`index.cjs:44,71–72,116–122,215–225`) do not reconstruct it.

Under the reviewed immutable version, retain before/after normalized starts, the complete undirected edge set and components for each verified accepted transition. The relevant-transition classifier identifies affected lineages from both sides, including changed eligibility, ruled relation inputs and edges; it cannot compare only members or advance all workouts on an unrelated reading. Executed against unchanged `candidateEdge`, AD_HOC A(day1,23:30), B(day2,00:30), C(day2,00:45) has AB/AC/BC; C at02:00 removes AC but keeps ABC connected through AB/BC. This illustrates why member equality is insufficient; it does NOT authorize a time correction to alter relation inputs. Unsupported classification blocks qualification. Exact replay is a no-op; incompatible bytes under one identity are an integrity conflict.

**C1 executable relevance sub-contract, still proposed/nonshipping.** The existing spec pair exports `createStartClassifier(candidateEdge)`. Its tests pin and import the actual unchanged `client/session.cjs` SHA256 `dcc1c0c77fa42fbd6a8751b9d118164e2b24ff54560802bf10315983fb8b43c3` before execution; every live normalized START participates regardless of its eligibility flag. The classifier derives every undirected candidate edge itself, rejects caller-supplied edge subsets, and computes transitive overlapping component/lineage unions across BOTH snapshots. A lineage is affected by any added live start, liveness/eligibility/relation input or edge change, or version-binding change among its members. Only explicitly unchanged components are returned as unchanged. Members and complete array identities use deterministic code-unit ordering; field insertion order cannot create a transition, and original operation/receipt/plan-basis bytes remain exact.

Its input explicitly assumes authenticated, complete, supported normalized views with immutable source identity/scope, original bytes/receipt/basis, source schema, rule and normalizer versions. Missing/unsupported provenance, partial graphs, identity conflicts, missing retained originals or mutation of their bytes refuse this model domain; that is not an authority disposition. The classifier does not decode signed history, infer legacy aliases, validate actual removals/reclassification, allocate generation tuples, or decide Q1/Q3 applicability. SET-display and unrelated-event controls require unchanged normalized START inputs; they are not executed SET decoders. Both snapshots use ONE pinned relation: version tests prove label invalidation, not migration across different candidate algorithms. The assumed live-view rejoin fixture grants no permission to resurrect a tombstoned immutable start. Those input/effect/decoder joins remain OPEN.

Handwritten expected components, full edges and affected lineages cover same-members/different-edges, unrelated changes, live eligibility retention, late/split/rejoin, transitive cross-snapshot lineage unions, version changes, property/start order, delimiter-collision identities and missing input. New result: `START-RELEVANCE CLASSIFIER PREPARATION: 33/33 checks PASS; 576 input permutations; 1 fresh process; 1/1 effective classifier fault; PINNED RELATION / NORMALIZATION ASSUMED`. The member-only source fault actually preserves the full derived edges while wrongly reporting no affected lineage; expected-output comparison fails, then exact source restoration passes. This is a reviewable classifier contract, not a repaired current client or permission to skip any existing actual-boundary RED.

Pending acceptance/rejection is a separate control, not the diagnostic's supplied tombstoned-view case. `sync.cjs:38–59` processes dispositions without retiring the separate resolution record. A local C guard cannot mint an accepted generation; ACCEPTED folds once at its actual receipt, WAITING stays pending, and terminal rejection excludes its effective contribution while retaining original bytes, signed negative evidence and state19 semantics. **Owner Q1 old-answer revalidation after rejected C remains OPEN.** The Q3 offline/input-preservation distinction above is clarified; its exact currentness/issuance predicate remains unimplemented. Required future comparisons cover accepted C, rejected C, failed/rolled-back rejection persistence, exact retry, fresh reconstruction, delayed/reordered receipts with holes and current-frontier refresh: compare complete durable history/generation/guard state and affected versus unrelated eligibility. No pending-lifecycle run, rejected-effect policy or schema adoption is claimed. The smallest implementation prerequisite remains a reviewed version-specific history decoder and decision-issuance/applicability contract; accepting the pure classifier alone cannot close it.

### Selected-schema activation — source-reconciled next implementation boundary

**Proposed scope, not an issued new capability.** Retained R1 is published at `d26795a47d638ec1e67840455273cc05eeca9926`; W6 now publishes `cdf698d43bc027ab6f1c075e179b42b133d9cfcf` (consumer product pins unchanged fromffaeaad). Its current-head addition does not change the schema/lease boundary inspected at0d7f5e0. The issuer already retains immutable `issuedLeases`, binds renewal intent digests to the requested schema, and resolves an old operation's original lease. Do not rebuild that registry or intent mechanism. Correct the earlier blanket range wording: current R1 renewals intentionally extend overlapping `[1, high]` ranges; the requirement is never to reuse an operation's device sequence or rewrite its lease/identity, not to require disjoint issued ranges. A schema transition must preserve the accepted sequence/epoch rules and valid old entries, including late arrival and exact replay.

Tracked diagnostic: `node rebuild/m4/spec/schema-transition-diagnostic.cjs <R1-at-d26795a> <synthetic-output-directory>`. Six actual local-D1/core/P-256 cases confirm: version2 enrollment refuses without domain writes; version1 enrolls; version2 renewal refuses without domain writes; low-level Ops.build retains an explicit version; old version1 admission/exact replay survives the refused upgrade; ordinary version1 renewal retains that replay. `SCHEMA TRANSITION DIAGNOSTIC: 6/6 CONFIRMED; v2 activation NOT IMPLEMENTED`. Version2 is a diagnostic input, not an adopted schema number or lease. Seven named input files are pinned; this is not a claim that every transitive dependency is individually pinned. No private fixture, simulated authority, W6 activation or phone evidence enters this diagnostic.

| Exact retained source boundary | Proposed narrow change and invariant |
|---|---|
| w5/reconciliation/codec.cjs validateRouteRequest/intentDigest | Select only a published, accepted schema registry entry. Keep the existing schema-bound intent digest and all request/scope/error-order rules. An unsupported version still refuses; merely accepting any positive integer is insufficient. |
| w5/reconciliation/issuer.cjs validLease/makeIssuance | Carry that selected registry version into the newly signed issuance; validate every historical capability using its own retained version. Keep immutable original lease bytes, epochs, ranges, request retry and standing guards. Old schema support is not removed when a new writer activates. |
| authority/validate.cjs validShape and admit.cjs existing lease comparison | Retain legacy validation/behavior for old signed operations; dispatch the accepted new workout validator for its selected version, then preserve existing identity, dependency, capability and rejection ordering. No new schema may bypass validation through a non-HTTP admission path. This extension needs the applicable boundary review; it is not authorized by W5's old core-freeze scope alone. |
| client/index.cjs commitBatch and ops.cjs build/validate | Pass the activated verified schema into the actual batch builder, together with the closed workout envelope mapping. Ops.build already accepts an explicit version; commitBatch currently omits it. Preserve operation HMAC/canonical contracts, original retry bytes, per-set values and typed input on refusal. |
| w6/public-client.mjs stage/lease admission and repository transaction | First reconcile original pending identities/status/sequence and validate the signed new issuance under existing recovery/knowledge guards. Activate selected writer version, capability and sequence metadata in one actual encrypted transaction, retaining old operations/outbox/proofs. Its current mismatch guard already refuses20 before durability; keep it until activation is implemented. A renewed server lease alone never activates the local writer or repairs unknown standing. |

The closed workout schema must precede issuance: required/forbidden fields, fixed supported units, stable logical slots, effort tags, corrections/removals and supported completion semantics. Preserve zero effort, unknown, skipped question, skipped set and not-logged as distinct values. The sheet does not itself settle every repetition-domain edge (including zero/fractional reps); retain that exact OPEN validation question until its existing rule/source is established, rather than silently making a training decision. No set/effort data may be coerced into an old writer's scalar input.

Required transition cases: unsupported enrollment/renewal has no effect; identical intent retries retain one issuance; simultaneous renewals obey the existing epoch guard; both old and new valid operations admit with their own capabilities; new malformed fields refuse locally and at authority; abort before/during local activation leaves the full old generation; lost reply/restart replays the original request safely; restored older local state cannot reset sequence or adopt a new capability implicitly. Include a retained old pending entry and subsequent new entry together, not an empty-outbox-only success. Compare complete signed rows, original operation bytes, dispositions and encrypted generation, not just counts. These new transition cases remain NOT IMPLEMENTED/NOT RUN; the six existing-behavior cases above do not satisfy them.

**Current-head dependency:** Opus Message60's bounded producer acceptance atd26795a is now joined by Message66's bounded mechanical consumer ACCEPT atffaeaad and Message68's browser5/5 atcdf698d, with identical product pins. Actual W6 now retains complete signed envelopes, compares the ORIGINAL client revision/attempt in its final transaction, waits for durability and verifies historical reopen without fresh permission. Independent10/10 challenges include real local D1/HTTP; author and reviewer reproduce149/149 plus effective/restored revision faults. Desktop Chromium5/5 adds real IndexedDB/WebCrypto, with synthetic signer/transport/observation guard. HEAD-IDB-01 remains attributed preparation, not the product implementation. A nonblocking diagnostic-code propagation limitation is recorded. Whole-PR resourceFAIL, persistent knowledge-loss, production normalizer/currentness/issuance, Q1, CLOCK/T1/P1, contract cases6/7, private/integrator and physical qualification remain open.

**Prescription carry-through:** EFFORT-03's disposable candidate gives76 focused passes after25 intended original failures, but the whole patch remains BLOCKED for integration. Adding a hold flag to fresh genSession cards also changes the shared sessionFromDraft path: both captured-ID and legacy reps-only drafts can inherit TODAY's hold when their historical hold was never captured. Do not apply this repair as fresh-only without separating those paths or resolving their capture contract. Existing rirPlan/alarm/sessionFromDraft function bytes are unchanged; that does not prove joined behavior unchanged. Resume must distinguish captured prescription evidence from current configuration; missing historical evidence remains unknown. The actual numeric effort policy and scientific/individual applicability remain the existing material review, not a conclusion from these code tests.

Research check: [RFC8785](https://www.rfc-editor.org/rfc/rfc8785.html) explains stable serialization for signatures and preserves strings without Unicode normalization. EARNED's accepted canonical-v1 explicitly normalizes strings and has its own decimal profile; adopting JCS as a drop-in serializer would change signed identities. This is an interoperability distinction, not a new EARNED rule. [RFC6902](https://www.rfc-editor.org/rfc/rfc6902.html) describes ordered JSON Patch and error/HTTP atomicity; it does not supply EARNED's append-only correction provenance or concurrent workout semantics. Retain the application contracts rather than replacing them with generic patch operations. These two targeted primary-source checks close those library-selection questions; no broader research or dependency adoption is needed.

#### Prescription capture and resume — proposed implementation obligations, v0.21

`node rebuild/m4/spec/prescription-resume-diagnostic.cjs` runs the actual unchanged public engine composition and `sessionFromDraft`, with twelve source files pinned to075f37c. Both captured-ID and legacy reps-only drafts reuse current load, targets, setup, label, alarm or opener history in twelve synthetic cases. Entered repetitions, slot count and inputs remain unchanged. These are twelve source dependencies, not twelve established bugs: current safety can correctly change a permitted action. The initial hypothesis that changing `last` alone changes this fixture's targets was refuted by the real anchor path; that negative control remains. No private seed/index, engine writes, mounted workout, durable capture or physiological qualification is exercised.

New-session capture must be part of the SAME durable generation as its start and outbox, under the existing W6 scope/permission contract. The following are information requirements, not adopted wire keys or a new identity scheme:

| Information to preserve | Required meaning and boundary |
|---|---|
| Original start, athlete, originating device and selected source/schema/plan/rule basis | Bind to the actual start and producer version; a caller's self-verified boolean is insufficient. Preserve original operation/receipt/capability and pending status separately. This does not forbid authenticated same-athlete recovery on another enrolled device; origin identity and the reading device are different facts. |
| Ordered lift/lineage identities and logical set slots | Labels and array positions are not identity. Later additions follow the existing explicit append-only workout contract; do not label today's remaining lifts as captured history. |
| Instructions actually displayed: per-set load/unit, rep targets, effort, reason and setup | Preserve resolved instructions and their basis, including unknown/not-prescribed values. Do not reconstruct historical instructions from current `genSession`, `rirPlan`, a scalar load or a default hold=false. Retaining a hold flag alone is insufficient. |
| Observations and draft boundary, separately | Preserve completed values, skipped/not-logged distinctions and the declared durable draft boundary. Corrections remain append-only; never fill observations from prescriptions or treat unlogged work as zero. No claim that an uncommitted draft survives B13/C8. |

Capture is NOT prescription qualification. The fresh producer still needs the held-card repair, reviewed policy/applicability and accepted schema/projection ownership. First recover historical truth, then evaluate current safety/permission: old captured instructions cannot bypass standing, integrity, rejection, lease, pain/illness or re-entry checks. Current safety may MODIFY instructions even when it permits training: the actual alarm branch raises the effort floor without invalidating the session (`writers.cjs:774`, unchanged source at4907407). Keep the captured ladder as displayed historical evidence; it alone is never permission to perform that ladder on resume. Derive the currently permitted instruction through the qualified current safety policy, present any change and its reason separately, and retain original instructions/entered facts unchanged. A withdrawal follows the applicable existing state and never promises Saved. This preserves existing modifier and refusal rules, not a new intensity policy or state.

Legacy drafts with incomplete prescription evidence retain their values, identities and slots, with missing historical instructions explicitly unknown. Today's regeneration is not evidence of yesterday's prescription. Replacement/current-plan instructions need the existing authenticated basis and any resolution/consent required by that contract. This is no permission to discard drafts, guess values, impose a new blanket consent rule, or require online access for an already valid offline Q3 capability. Q1/Q3 obligations are unchanged.

Required implementation evidence: atomic start/capture/outbox; failed commit leaves the complete prior generation; lost acknowledgment/restart replays the same identities; changed plan/load/targets/setup/alarm/history cannot overwrite historical instructions; a newly active alarm modifies the CURRENT permitted instruction without rewriting the captured ladder, and blocking safety cases still refuse; same-label/different-lineage is not substituted; another session's close cannot erase this draft; cancel/correction/rollback preserve completed facts; incomplete legacy history remains explicit. Exercise the real W6/T2 command path after schema acceptance. A clone or memory-only test is not that gate.

Next technical closure is the exact field encoding, authenticated basis join, atomic start/correction version and existing custody/retention mapping. This grants no additional private server retention. WORKOUT-RESUME, CLOCK, K1, normalizer and actual phones remain unaccepted. This is an existing complete-workout dependency, not another implementation stream.

### Stage A continuation — edits must change the workout projection

The actual legacy T2/T3 path now has executed whole-boundary witnesses: accepted load/reps corrections leave `acceptInitialPlan("from-session")` using old values; an accepted set tombstone still contributes to that choice, its offer and restart summary. Empty replacement maps and empty removal reasons are locally acknowledged before signed `MALFORMED` rejection. This is a legacy-bootstrap diagnostic, not a conforming new workout fixture or remote/phone result. Reading correction/removal controls work on the same core. New diagnostic: `WORKOUT-EDIT DIAGNOSTIC: 3/3 controls PASS; 5/5 existing requirement witnesses RED; signed exact retries and immutable originals checked`. These are missing WORKOUT-CORRECT/SCHEMA obligations, not renumbered M2 defects.

| Action | Proposed selected-version command and sealed fields | Projection/admission obligation |
|---|---|---|
| Correct load, reps or effort | Explicit `correctSessionSet(target_op_id,replacement_fields)`; `kind:"correction"`, `class:"session"`, target's `lift_lineage_id`; payload `{replacement_fields}` with a nonempty closed subset of `load`, `reps`, `reserve` using the basic-session types above | Preserve original start/slot/lineage, all untouched observations and exact original bytes. Check the projected complete target's field domains, not just that a replacement map exists. Missing replacement keys mean unchanged; clearing optional effort needs its own defined operation and is not implied by null. |
| Correct displayed effective time | Same SET-targeted append with `replacement_fields.effective:{local_date,local_time,utc_offset}`, all three required together; permit alongside the above fields | This nested spelling is proposed. Change the target set's display ordering, never immutable start/set/combined identity. SET time never remaps the parent START's slot/date/time. Invalidate only a generation whose reviewed relation/membership inputs actually changed; display reordering alone does not establish that change. A future START-time correction/normalizer mapping remains outside this one-set profile. No device-clock ordering shortcut. |
| Remove one mistaken set | Explicit `removeSessionSet(target_op_id,reason)`; `kind:"tombstone"`, `class:"session"`, target's `lift_lineage_id`; payload `{reason:nonemptyString}` | Exclude that fact's current contribution while retaining original/removal records and later independent facts. Complete-workout deletion/cascade is not authorized by this one-set operation. |
| Replace a rejected set | New standalone `session-set` with complete selected-version fields; optional old/new link stays local | No correction target or causal dependency on the rejected operation; retry of the old rejection remains identical. Preserve the rejected history under state19. |

This profile is a proposal for technical acceptance, not an implemented schema or a new training rule (R106–108,155–164,206–210,229–247,583–588,616–617). Targets may belong to another device of the same authenticated athlete; never add a same-device restriction. Include targets in causal dependency coverage. Unknown targets wait, foreign ownership rejects, and rejected parents retain existing `REJECTED_DEPENDENCY` behavior. Client checks all locally decidable required/domain fields before acknowledging; unchanged typed input survives state3. Admission checks the same selected-version schema plus target ownership, target kind/domain/lineage and complete projected required fields before acceptance. The published error precedence, capability and identity contracts still apply.
Prospective source delta: `client/index.cjs` edit commands plus `sessionFacts`, `resumeAfterKill` and initial-plan offer inputs; `client/ops.cjs` selected-version validation/build; `authority/validate.cjs` target-aware correction validation called from `admit.cjs` after existing ownership/dependency checks; existing W6 command allowlist/staging; one pure workout fact projection under the eventual approved M4 path. Current `correction/tombstone` hardcode reading at161–162, `sessionFacts:106` ignores them, and `authority/reduce.cjs` exports log/frontier rather than supplying a missing workout projection. Merely changing the class or editor screen cannot close this gap.
Publish executable correction-fold fixtures before implementing the reducer: retained original + overlay provenance; correction before/after arrival of target; causal correction chains and concurrent changes under B15's declared ordering; same-athlete second-device edits; correction versus tombstone; correction of effective time and invalidated partitions; restart and exact retry. Do not invent delete cascades or use device sequence/wall time as semantic order. The exact concurrent field-fold/clearing policy and generation effects remain a named schema/reducer join where not already settled by the sheet.
Fact corrections may change history, evidence and a newly derived offer. They must not silently alter an already accepted load/set/rep plan: plan changes still require their own complete consented atomic transaction. The diagnostic's explicit local plan choice is not evidence of a remotely committed plan or permission to change existing prescriptions.

**Executable nonconcurrent specification, v0.9 retained plus v0.13 C1:** run `node rebuild/m4/spec/nonconcurrent-projection.test.cjs` from the repository root. Handwritten views cover all seven nonempty load/reps/reserve subsets and causal correction chains targeting the ORIGINAL set, then removal. Each later edit must causally cover every prior edit of that target; transitive parent ancestry counts, while receipt order or a transport predecessor alone does not. Later same-field and disjoint-field edits, transitive removal, untouched observations, absent/bounded/unknown effort, original operation/receipt bytes, unrelated set and opaque accepted-plan bytes are checked. Concurrent same-target edits from distinct valid model device chains refuse in BOTH receipt orders; no winner rule is invented. Three exact chain fixtures add6480 delivery permutations to the existing240; nine fresh processes and four effective/restored model faults must match. That unchanged projection coverage remains101/101 checks PASS; the separate C1 counters above import only the pinned candidateEdge relation. No schema or product activation.
The model explicitly ASSUMES an authenticated, complete, accepted synthetic graph. Its IDs, commitments, receipts, schema and lease are illustrative placeholders; there is no canonical/signature/admission/transport/storage/engine proof. It refuses incomplete/foreign inputs, unsupported concurrent changes, clearing, effective-time/other fields and correction-of-correction. Refusal is a model-domain limit, not an authority disposition or a replacement for dependency waiting. These observation overlays use the explicitly proposed structural types only: reps' full integer/range domain and the real prompt context permitting not_asked remain OPEN. So do effective-time/generation rules, concurrent folds, production validation, schema/legacy activation, numeric engine-carrier mapping and current actual-boundary RED witnesses.

### Read-only presentation preparation — 2026-09-07

The existing data-free `w7-preview/browser-engine.cjs` can already run actual `genSession(state, day)` (`today.cjs:61–173`) for explicit synthetic states; a full writer composition is not needed just to inspect that output. The review attachment contains reusable `projectPrescription`/`renderPrescription` code and cases, outside this docs-only branch and outside W7. It clones the pinned G14 template, replaces all mock rows/values, removes fabricated completion/current-set navigation, preserves literal explanations as text, shows baseline targets as open and retains the whole engine result. No workout command, storage or plan-write interface is invented.
Prospective placement, after the required stage/contract acceptance: `rebuild/m4/workout/presentation.cjs` for the two functions and `rebuild/m4/workout/test/presentation.test.cjs` for focused tests; an explicitly reviewed G14 asset composition is also required. Do not expand W7's existing T01/T02/T08 allowlist. The attached static HTML is a generated review artifact, not a replacement application build or an installable logger. Its inputs use the existing nine read factories, the pinned public G14 template and invented public fixtures; no runtime import of seed/migrate/merge/writers. Full engine is used only as a local test oracle, with verdict-only failures and source pins.
Two stronger cases limit the result. **Per-set loads:** G14 supports a load vector; `genSession` consumes `wSets/newWSets` but returns scalar `w`. Equality with that DTO cannot prove lossless display. A synthetic mixed-load case demonstrates the omission, and this preparation marks that load unavailable rather than flattening it. This is an explicit unimplemented mapping, not permission to omit per-set loads from OWNER-WORKOUT. The actual projection must carry the full chosen vector and its provenance. **Queued targets:** a real synthetic debut queue entry changes the generated load; this is derived engine output, not proof of accepted-plan authority. Do not classify or filter a queue state as consent based on this viewer. The final accepted projection must establish that provenance independently.
Evidence: `WORKOUT-PRESENTATION PREPARATION: 22/22 exact genSession parity; 22/22 DOM cases; native+frozen Date; read composition excludes seed`. Eleven scenarios cover U/L/rest/empty/held/baseline, different targets and loads between lifts, order, different loads within a lift, a queued debut, and text escaping. Three behavioral bites (scalar flatten, baseline zero, unsafe markup) all fail ordinary assertions; literal restoration and rerun pass. This is local Node/jsdom evidence, not a browser, accessibility, bundle, phone, storage, workout-command or acceptance verdict.
Browser access to the temporary localhost preview was explicitly denied by the browser permission system; no alternate browser, raw protocol or renderer was used to bypass it. Visual/responsive/keyboard verification remains NOT RUN. The temporary tab and server were closed; the saved static HTML remains available for authorized later review. Production build/asset audits and current required full/regression/independent gates remain pending for actual integration. This preparation does not close Stage A/B, per-set mapping, privacy-safe full composition or any owner-use gate.


### Current-head producer and durable consumer — September 8 checkpoint

Retained draft PR43 now publishes d26795a47d638ec1e67840455273cc05eeca9926 from
003c816e695fce7e77e17665f25d8cdcc2435211. Implements the accepted proposed wire
text through actual R1 scoped D1 reads and the public verifier:28/28 focused,
four effective/restored faults, existing R1 focused179/179 and original W5/full
frozen gates PASS. Initial177/178 failure and owned compatibility-test correction
are disclosed in REPORT-R1-ASTRA; no frozen suite edits. Both-OS CI green. Scoped
independent implementation review is ACCEPT for the bounded capability only; R1 resource FAIL remains.

W6's actual consumer is now published on its development branch atcdf698d with product
pins fromffaeaad. Opus66/68 independently accepted the scoped mechanics/browser
evidence. The following table preserves the original0d7f5e0 implementation checklist;
its first four rows now have bounded product evidence, while its K1 row remains open:

| Actual retained source | Required affected work and proving case |
|---|---|
| public-client.mjs verifiedHistory and boundary callbacks | Add a separately verified current-head proof family and receiveCurrentHead only with reviewed scope/context ownership; original six signed families retain their checks. Reopen must verify all retained bytes and cannot revive a consumed volatile attempt. |
| t2-stage.cjs command and inbound-proof allowlists | Stage the full current-head envelope plus receipts through actual T2, preserving originals and proof. It is not a projected T2 snapshot or an invented normalized workout basis. Unsupported normalizer/legacy fields stay unqualified. |
| bridge.mjs perform/validateCommit and repository commit | Existing CAS restages on conflict; this new observation cannot silently update its captured client revision/attempt. Compare captured revision and standing/knowledge in the same final transaction; test real IndexedDB races, failed completion and retained input. Server revision is never compared to client revision. |
| public-client.mjs sink/completedOutcome | Ordinary sink success currently has no confirmed:true; late context change explicitly returns confirmed:false. New wrapper requires durable:true AND confirmed:true. Map only a completed qualified observation for the captured attempt; do not blanket-add confirmation to every callback or call it issuance. |
| K1-MANAGED-INGRESS-CONTRACT.md and observationGuard | The injected guard remains proposed/unsatisfied production knowledge-loss machinery. Arm before informative ingress; failed persistence/relaunch cannot erase learned negatives. An in-memory epoch or signed response is not the missing persistent fence. Keep its required acceptance and actual-store cases. |

The coordinator resumed the retained W6 claim under its published CURRENT-HEAD-CONSUMER.md,
without a new stream or dependency merge. Exact consumer CI is7SUCCESS/3SKIP on
Windows/Linux; the PR remains draft and unmerged. Original product/reviewer pins
and initial test failures remain in REPORT-W6-ASTRA.md and the reviewer attachments.
No normalizer, schema activation, Q1, T1/K1, CLOCK, phone or private-use acceptance
is inferred. Preserve offline answer Q3; require a new challenged observation for
a new issuance attempt. Next work remains the closed workout schema/activation
and captured prescriptions, followed by actual integrated workout/recovery/phone proof.

### Read-only signed-history UI consumer — v0.17

Enumerated delta: `rebuild/m4/workout/history-panel.cjs`, `history.css`, `README.md` and `test/history-panel-check.cjs`, `test/history-panel.test.cjs`, `test/history-browser.cjs`, plus this existing brief/report. Related spec delta: legacy-workout-view.cjs, legacy-workout-projection.cjs, its test and README repair reviewed F1–F3: retain class-mixed session-kind facts and expose per-set context/association. No frozen/core/authority/client/W5/W6 changes. This is an unconnected presentation component, not production activation or an additional stream. `createHistoryPanel` calls its trusted authenticated projection with raw proof input, then shows supported corrected per-set observations and separate original/change details; it never interprets a user-supplied verified flag as credentials. Captured-prefix uncertainty and missing context remain visible. No day/device workout grouping or inferred plan/progression readiness. Removed and unresolved entries never present an active numeric result.

The consumer clears previous content before each async read, rejects late superseded responses, and exposes clear/dispose for the lifecycle owner. Its request generation is a display fence only; the future account owner must call it on context loss and supply the new account's trusted reader. This does not detect sign-out or close W6 knowledge-loss/durable recovery. Source strings render only as text; no raw errors or active record markup. No save/finish/apply control is offered before its actual producer/storage contract exists.

Executed on synthetic data through the actual retained writer/core/signature/projection path: 23 history checks (all20 originals retained) plus15 DOM checks PASS. Three added desired consumer assertions separately fail on exact3b88b52 and pass after the repair. Four desktop Chromium checks at390/320px and200% text, including keyboard details, PASS after fixing an observed12px text overflow without clipping. Lifecycle and hostile-text stubs are labeled separately from signed-path evidence. Physical Safari/VoiceOver, qualified account switching and full workout joins remain open. README specifies exact reproduction and scope; applicable publication and independent acceptance are separate. The prototype supports the reliable-records pilot outcome; it does not turn partial logging or green unit tests into first use.

## 2. Actual engine, projection and public package

`m3/w7-preview/browser-engine.cjs:3–5` deliberately excludes seed/migrate/merge/writers because the seed module embeds personal history. Stage B must not import the full engine entry point or solve missing dependencies by bundling authored seed/history. Preserve this boundary in browser assets, source maps, server static files, test fixtures and error output.
Use the actual accepted engine factories with injected clock/IDs/drafts and explicit authenticated athlete history/provenance. Publish an exact transitive dependency inventory and a data-free engine assembly. Any helper movement needed to separate code from authored data requires a narrow source-equivalence proof and reviewed path/function delta; no copied approximation of writer arithmetic. Empty HISTORY/ROLLUPS/SEED is acceptable only for fully synthetic cases that explicitly supply every reached dependency, never an owner fallback.
Accepted operations are the durable facts. Build a deterministic projection into the engine's required representation without discarding per-set loads, bounded/unknown effort, skips, correction identities or consent. Compare full synthetic inputs/outputs/receipts and public composition with the actual accepted engine at the same basis and injected time. If the engine's legacy representation loses a ratified fact, preserve the operation and expose a red compatibility witness; do not flatten facts or manufacture readiness.
Source closure narrows this join: `writers.cjs completeSession:202,230,400` rebuilds `lastMeta` and session history from scalar load and opener/terminal effort; it does not ingest performed load vectors, per-set identities or a supplied middle-set effort vector. Ordinary adoption at336–354 translates a prescription vector, not performed facts. Repeated entries for one exercise are not a lossless workaround because history takes the final exercise weight and some readers select the first entry. `progression.cjs buildRirSets:388–396` pads middle effort with null; `sessionScore:540–550` multiplies scalar weight by total reps. Preserve the rich immutable facts independently of that derived legacy view. Any lossless engine representation/eligibility repair needs the existing accepted M2 amendment and full gates; do not silently average loads, convert a lower bound to exact3, invent zeroes or label `sessionLog` the complete factual record. D41/D43 remain their existing assigned defects, not new achievements of this diagnostic.
Applying a plan is an authority-accepted atomic plan transaction; an optimistic client projection cannot authorize it. Reload/replay/import rollback must not execute a writer twice or re-mint a receipt. Preserve current accepted targets separately from derived guidance and imported legacy provenance. Engine repairs continue only through the approved M2 process and immutable parent lineage; this brief supplies no golden rewrite permission.
Build/server output uses an explicit asset/input allowlist and synthetic canaries; verify actual deployed archives and transitive imports, not a filename denylist alone. No historical literals, secrets, authority private signing keys or default owner data in public assets. Local preview remains clearly synthetic; production headers block shared athlete response caching and private responses never enter static caches.

## 3. Reachable UI and evidence-producing cases

Reuse the public mock's active-workout surface and variants: G01/G03/G04 for set/effort, G05–G10 for progression/debut/holds, G11 for lift actions, G12 early finish, G13 review/finish and G14 overview. Keep the ratified hierarchy/wording with factual projections; retain 16px-or-larger inputs, keyboard/focus behavior, 200% text and VoiceOver. Add only the supporting actions required by the runtime where the mock has no completed flow.
Mock toasts and static transitions are not an implementation oracle: History/Undo/skip/pain are partly illustrative, and G12 currently routes to G13's all-complete summary even after early finish (H784–815,948–949). Render the true completed/skipped/unlogged breakdown. The current Today preview implements only T01/T02/T08 and may supply presentation utilities, not workout persistence or plan authority.
Proposed tracked entry: `node rebuild/m4/workout/rigs/run.cjs --case NAME --env ENV`. Reuse existing env names local/synthetic-remote/owner-phone/isolated-restore and PASS/FAIL/BLOCKED/PENDING with exit2 for blocked/pending. This runner is **not built**. It must dispatch real implementations and preserve the existing mandatory runners; no new global replacement suite or simulated physical/remote PASS.

| Proposed case → required proof | Concrete red-first/control/fault requirement |
|---|---|
| WORKOUT-SCHEMA | Full closed command matrix through actual T2/W6 staging and authority HTTP: exact bytes/units/IDs, reserve variants, normal/early close, skip scopes, corrections, undo, partitions and consent. Missing fields/foreign references/stale generations refuse correctly; old accepted bytes remain unchanged. Current shortcuts produce red witnesses, not an assumed answer key. |
| WORKOUT-ENGINE | Actual data-free composition and operation projection versus accepted engine on wholly synthetic varied loads/effort/skips/corrections/technique eras, both Date modes and injected clocks. Full state/return/receipt/provenance comparisons; no seed literals, average-load substitution or UI arithmetic. |
| WORKOUT-JOURNEY | Prescribed workout → start → actual mixed-load sets → first/last effort including unknown and 3+ → skip → early finish → exact history; normal-finish control. Exercise actual UI, durable store and signed HTTP together, then two physical phones. No unlogged zeroes, duplicate effects or implicit plan acceptance. |
| WORKOUT-CORRECT | Correct load/reps/effort, remove a mistaken fact and exercise guarded plan undo separately; replay twice, restart, lose acknowledgement and deliver another device's later operation. Originals/targets and later valid facts survive; derived view/receipt matches the real effect. |
| WORKOUT-RESUME | Kill before/during/after durable commit, quota failure and ≥1 real hour mid-workout; new process reopens full records/outbox/start/slots/typed-draft boundary. Saved only after durable completion, never Sent/Synced/Applied by inference. A real Gym journey is required in addition to the existing C8 harness. |
| WORKOUT-CHOICE | Correct prescription and fresh offer with APPLY/KEEP/NO; independent stale-basis/generation changes, declined recurrence and undo/conflict cases. No preselection, silent policy change or stale offer; full consent digest and deferred A6 halves implemented when this machinery is brought forward. |
| WORKOUT-CONFLICT | Two starts, same/distinct workout partitions and duplicate/distinct set attempts across two devices, old/partial answers and reversed arrival. Unresolved ambiguity blocks the affected earned claim; resolving preserves every fact and truthful governing basis. |
| WORKOUT-SAFETY | Pain scope/interval, illness, stale dependencies and Re-entry; immediate suppression and reachable explicit resolution, not a toast. Correctly distinguish integrity18, known standing17, current-process rejection19 and exhausted allowance20 under accepted W6 refinements; no leaked previous-account truth. |
| WORKOUT-PACKAGE | Actual built/deployed bytes, transitive input inventory, private canaries, source-map/static-cache checks and no authority signing material. Physical 200% text/VoiceOver, ten truthful cold paints within the existing limit and complete usable flows; desktop tests earn no phone verdict. |

Use effective disposable product mutations for each claimed new law: drop actual load/effort/identity; early acknowledgement; double replay; ignore correction; fabricate unlogged zero; accept stale offer; omit safety withdrawal; leak bundled seed. A missing module/syntax error/pin refusal does not count as a behavioral kill. Record a real bite's failing case, exact restoration hash and restored GREEN. Original frozen suite files stay unchanged; extend the authorized v4 successor and product-boundary tests through reviewed amendments.

## 4. Mandatory joins, report and independent acceptance

Until the proportionate amendment is independently accepted, preserve full original conformance/selftest/strict and scope/CI at each currently required candidate/integration cut. After acceptance, classify risk in the existing report: routine UI needs one independent peer, focused regressions/affected journey and fast CI; engine/schema/storage/sync/privacy/auth/recovery/migration and acceptance-contract changes retain semantic/adversarial and relevant reference/backend/device evidence. Ambiguity is substantive; keep cheap protections. Evidence reuse requires independently validated complete accepted execution, transitive inputs and actual environment; unknown/changed behavior/composition means affected reruns, never self-declared PASS. No package skip is wired by these proposals. AGENTS private preparation, explicit ENGINE_MAIN/ENGINE_OLD, fixed gate date/TZ and strict’s unset test clock remain. Reuse focused tests while debugging; run complete mandatory packages when the candidate is ready. Existing local 99/99/70/29 status is a baseline, not a workout release gate: actual required policy/progression adapters and applicable A6 halves remain necessary for their shipped behavior. All45/15 outcomes remain accounted in the authorized v4/M2/public closure process; under owner amendment19, pilot exclusions or minor-issue acceptance require concrete consequence/dependency evidence and applicable technical acceptance, rather than blanket closure of every public item.
Any engine change still requires the accepted postfix source/delta/mutant/legacy package, full port oracle all3 blobs/both Date modes, the current complete inherited second-gate/surface/sync-laws inventory (including the original3072 assertion sites, not capped at that tally), every inherited assertion and independent exact-artifact acceptance. A UI-only change does not authorize an engine delta; its projection/public-composition cases and all existing mandatory regressions remain required.
Retain AUTH-D134 mapped laws, rig191 ten effective breaks, HTTP-190 five scenarios and100-invocation race, local/deployed separation, R1 resource contract, W6 actual IDB/T1/K1/P1, all applicable C1–C11 and physical PHONE-2/PHONE-RESTORE. Preserve synthetic F2 isolation/restore/keys/account recovery/import rollback and privacy-safe telemetry, independent milestone review and exact correct-account/private-import approval. First-owner cost/HANDOFF exceptions are only the following accepted-and-executed conditional joins; all other requirements remain. Private results are verdict-only.

For billing, verify the real alert/recipient, supported enforceable workload/request/retry controls across actual ingress/background paths, an assigned actual-billable-usage check with frequency/latency/stop threshold within existing authorization, and a rehearsed concrete ingress/work/retry stop and recovery procedure preserving pending operations. Record rejected traffic/delayed billing/remaining service charges: these controls are not a universal hard cap. Actual charged-email proof remains DEFERRED/PENDING until ordinary authorized use permits observation; no charges solely for a test and no TELEMETRY-COST full PASS. Material uncovered exposure or missing controls blocks this exception.
For alternate operation, verify owner-controlled accounts/credentials, independently available recovery material, an executed complete data-and-key restore away from the primary installation into usable authenticated history, and all first-deploy/import checkpoint/rollback protections preserving subsequent writes. Measure approved loss/time objectives including pending local operations. Backup/decryption alone is insufficient. The unexecuted alternate operator’s rotate/deploy/rollback qualification and full HANDOFF remain DEFERRED/PENDING until before broader beta; restore and first-release protections never defer.
**OWNER-WORKOUT PASS** requires every applicable join above (including verified replacement prerequisites for either independently accepted conditional deferral) plus a complete real workout and correct history on the owner's qualified installation, witnessed acceptance and evidence pinned to the released code/storage/key/schema configuration. A synthetic journey, Today-only acceptance, review-pending artifact, unavailable remote environment or calendar-shortened soak cannot earn it. M5/OPEN-M1 remain open until their own accepted closure.
C3: never open, poll, update or reseed `rebuild/m3/soak-stub/` or its origin. Use off-device records to verify the last interaction and booked safe-pressure/readback protocol; ≥30days is measured from the last interaction, not assumed from a seed date. Resolve conflicting older pressure wording before owner instructions. Qualify the tested storage configuration explicitly; a different production origin/key/migration/workout path is not automatically covered.
Each implementation report records base/claim/source amendments; exact command/schema/legacy mapping; synthetic witness and candidate output; package/private custody; full mandatory gate tails; effective bites/restoration; seams/not-run/unknowns; wall-clock and NEXT. Give no private state values/counts/dates/prose/hashes. Attribute any cost savings only when measured; the operational pilot is not a promise.
C, or an explicitly qualified independent fallback under accepted PR47 §3, verifies contract and product on the exact submitted revision, executes Linux and relevant boundary/phone tests, and records remaining blockers. The owner separately authorized one retained Opus5/High PR46 public contract review using included capacity; this is not general fallback qualification or product/phone/private-custody/merge authority. Its initial CHANGES-REQUIRED verdict on `6d6f95454b8fca87edeb01b6166f5cdc5c193fcb` is not acceptance; each successor needs its own exact-revision receipt. I integrates accepted revisions and verifies combined checks plus both OS jobs. Sol retains the existing blind milestone role; same-family helper review is preparation, never independent acceptance. No repeated unchanged peer polling during an external availability block.

## 5. Never and NEXT

Never alter the frozen app, original suite/reference/oracle/goldens, accepted artifact bytes, authored history, seeded soak, training thresholds or owner rulings. Never hardcode workout arithmetic, convert unknown/3+ to invented exact effort, flatten actual loads, remove a fact to hide conflict, treat Saved as Synced/Applied, ship private seed, or turn a skipped test into a completion claim.
Unresolved before Stage A acceptance: precise technical schema/legacy compatibility, engine representation of bounded effort without information loss, complete command/projection source allowlist, actual error/commitment mapping and the intended storage/configuration contract. Executed C3 qualification remains a Stage C/OWNER-WORKOUT prerequisite; it does not block Stage A schema acceptance. T1/K1/P1/resource/production/private and review availability remain separate blockers; this brief does not solve them by prose. No broad process redesign or new owner purchase/setup request follows.
OPEN scope clarification: if a whole-workout removal action is required, what exact facts does it target and what remains live? The single-set tombstone profile neither defines a cascade nor adds a new deletion feature or completion gate.
NEXT: C reviews this proposed workout brief and its concrete basic-session attachment alongside the already delivered D30 brief when its existing task has capacity. The coordinator retains R1 and M2 ownership; further narrow preparation may close the named Stage A schema/version/correction/projection joins while review is unavailable, without product-rule changes or acceptance claims. Implementation starts from the sufficiently closed published contract and required acceptance. I continues its existing account/device/recovery preparation request when available. Keep the same compact checkpoint and continuation; do not create another schedule or competing queue.
