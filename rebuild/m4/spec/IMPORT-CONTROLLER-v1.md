# Import preparation and controller boundary v1.5 — September 9, 2026

## Reproducible reading-engine candidate — v1.5

import/reading-replay.cjs now executes actual preparation reproduction and accepted reading/effect reconstruction against the same authenticated recovered source/generation. The caller owns authentication and current revision/context binding. Exact immutable source/local preparation must reproduce candidate_json; a saved candidate/coverage list cannot skip operations. Rebuild from the immutable basis each time, never call undoRead against a current state and assume later effects survived. Pending remains a separate local factual view. Actual installed applyRead/currentRate execution produces inspectable input/output coverage and qualified:false/activated:false; this is not active state or consent.

Legacy source rows retain their original source member and position, not fake operation correspondence. Native accepted reading IDs/effects retain authenticated original identity. Different native local images, source/pending overlap, same-date collisions, backdated order, unmapped context and unsupported effective-time input remain unresolved calculation prerequisites. Original-day/hour is explicitly injected for the existing writer; runtime timezone/Node and producer are bound to the returned candidate. A later publication must revalidate the source/generation/producer/context and actual applicable policy, never merely trust these fields. First imports, subsequent native source checkpoints and rollback require the full lineage rules; below-W/list membership or an empty engine context never proves application.

9 focused actual-engine checks,8 effective faults/restored9 and the actual signed W6 source→calculation→admitted correction/removal→five pending/reopen witness PASS; exact receipts in REPORT-OWNER-WORKOUT-BRIEF-ASTRA.md. This Node-only calculation candidate is not a second advice engine and is not mounted in the browser. Next extend the SAME qualified producer to complete accepted context and source/native lineage, then accepted-plan/consent reconciliation and guarded atomic publication. All full-app/P1–P6/private/science/device/resource gates remain.

## Current projection requirements — reached implementation witnesses

The v1.3 recovery join at W6 76b714d retained remote originals but its ownOps-only face dropped them. The original actual signed recovery/reopen witness reached SOURCE_CURRENT_PROJECTION_REMOTE_READING RED. Its receipt is preserved at C:/Users/joeym/AppData/Local/Temp/earned-projection-boundary-oHWBGE/evidence.json (SHA c0b4a7118a1bad42963ab67a6329024f5fdcee7b728d7c533ecc7be21964e1c5). After APM's required pre-edit W6 amendment review, the factual reading seam makes this witness GREEN. spec/source-projection-witness.cjs now expects that current GREEN while still demonstrating the unchanged engine/context limitations below. Current receipt C:/Users/joeym/AppData/Local/Temp/earned-projection-boundary-uQpJUd/evidence.json SHA ddb207aefd9ad54ec2753ee6d33392ea914c2696043e2acc6e57b78dc1a8d6e0.

The actual installed applyRead also returns the state unchanged when any read already has that date. Passing every reading through it cannot provide a complete factual view. Source validation accepts engine_context_json:{}; immutable custody does not prove which T2 operations local_json/candidate_json already represent. A signed manifest binds submitted bytes, not engine meaning. Do not silently change the accepted writer or infer operation coverage from date/frontier alone.

The same authenticated generation must keep three meanings distinct:
1. Exact original imported material and provenance. Historical targets/accepted/proposals are not current consent.
2. Qualified source plus complete accepted facts at contiguous W, from every device, with corrections/removals and unresolved collisions explicit. Only this accepted projection may produce machine-derived numbers/recommendations. Same-date collision requires its governing interpretation; silent first/last insertion wins or dropping it through applyRead is not a resolution.
3. Pending local originals and their factual/authored overlay, including those held at the import checkpoint and those saved later. Runtime sheet B Layer1/Layer2 still governs: pending facts may withdraw/suppress dependent machine output, never generate new machine guidance. Replaying all queued work means retaining its exact local effect/disposition, not adding it to accepted machine state.

Coverage must be derived from actual source/checkpoint and authenticated identities before skipping/replaying an operation. Merely listing it below W or in checkpoint.ops does not prove a legacy engine effect. A typed coverage assertion alone is not evidence. The first imported legacy source has no invented Earned operation correspondence; a subsequent machine checkpoint needs reproducible source/effect coverage from the same qualified producer. Rollback selects its prior accepted projection basis, reconciles every later accepted/pending original under the same layer rules, and never rewinds the active database or revives old guidance/permission.

The factual reading join is now implemented in W6: all-device immutable originals and edits, separate accepted/local folds, explicit concurrency and same-date ambiguity, upstream signatures/local identity/current-context checks, current-model local visibility and dependency suppression. It is not imported-source engine projection. Even accepted new dates cannot freshen a snapshot without actual coverage; earlier effects can invalidate reading-dependent computations while a newer date remains. Existing recoveredPlan guard remains first. See W6 REPORT-W6-ASTRA.md for12 focused/395 default suite/6 effective faults/36 native Chrome and conformance evidence. Next demonstrate source/effect coverage through the SAME actual engine producer, then qualified accepted calculations, pending suppression, consent and guarded publication. Never replace the full app objective with this viewer or a second advice store.

## Current indexed W6 source recovery join — local candidate

R1 source320b899 plus shared indexed-codec successor98c140514db7aee3c175d1a7ec1ce6d73f14a046 now joins W6's existing encrypted recovery stage and actual T2 historical assembly at76b714dc7e2a00ca21849f8e3e1af4ec9ede06fb. The browser verifies the COMPLETE signed rows-v4 cut and original authority relations, then runs the SAME source-record/material/selection-chain rules as R1 through indexed reads. Streaming accepted-prefix hashing preserves exact earlier JSON-array/domain bytes. Source/checkpoint/material inspection is isolated and tied to the same inventory and local revision/token/current context; it cannot grant activation or fresh permission.

W6 explicitly configures the source reader and versioned archive decoder registry. Old local-recovery-proof/v1 remains exact and decodes rows-v3 only; new v2 identifies rows-v4. Both survive later saves/reopen. Only the v4 live page HEAD is namespaced, because the actual mixed-version witness showed an old-head decoder failure. The original transport control/budget remains shared; protocol change cannot replenish attempts. Named source custody and immutable archive pages remain outside rotating active/previous storage.

Executed with actual M4 preparation, W6 custody, workerd/D1/P1 admission and complete signed recovery: activation binding → later remote accepted fact → three queued local saves → recovery → rollback selection → inactive historical candidate storage → two further saves/reopen. The exact original material and checkpoint, remote original and all five pending bodies survive. Native Chrome separately passes28 checks through full close/relaunch. Old/new archive coexistence, corrupted source/prefix, caller mutation and changed-cut refusal are covered. See current W6 REPORT-W6-ASTRA.md and R1 REPORT-R1-ASTRA.md for exact sources,15 source/167 recovery/383 W6 checks, four effective faults and retained initial failures.

Remaining controller boundary: application source upload/intent issuance is still driven by synthetic harnesses; T2 has no generic public source event committer. The recovered source selection/material has NOT yet been projected through actual current engine/history/policy or atomically published as qualified active state. No private import, current guidance, consent, physical journey, independent acceptance, numeric integrator receipt, K1/schema2 closure or full IMPORT-ROLLBACK is claimed. The later factual-reading index/face seam has its required pre-edit review in BRIEF-W6; the source recovery join itself changes no T2 core. The single execution NEXT remains EARNED-DELIVERY-STATUS.md.

## Authority source transaction amendment — local candidate, not accepted or deployed

Use an explicit `earned/source-import/v1` R1 transaction. Existing schema1 `fact/event` records only the athlete's import or rollback intent; its unchanged admission receipt ALONE never activates anything. An immutable source selection binding, written in the SAME staged database/CAS publication as real admission, binds the exact intent, commitment, accepted sequence, chosen source material and before/after accepted frontier. An already recorded ordinary `/op` intent without the exact binding refuses promotion. Exact lost-response retry returns the retained binding; a changed request conflicts. Original schema1 leases, canonical/HMAC bytes, WAITING behavior and schema2 workout limitations remain unchanged.

The named `sourceImports` collection contains closed manifest, chunk and selection records. Source material preserves exact source/candidate/local JSON text, complete W6 checkpoint JSON and engine context JSON. Chunks are at most32768 bytes; total material at most16MiB. These are candidate transport bounds, not a resource qualification. Manifest and chunks are immutable; uploads are inactive and resumable. Activation checks completeness, lengths and domain-separated digests, original checkpoint/frontier and prior selection. Rollback appends a new selection of an earlier activation's ORIGINAL checkpoint; no operation, source or database row is erased or rewound.

Actual `admit()` drains WAITING operations. Bind its actual resulting complete frontier, and require the client to replay ALL accepted records after the source checkpoint plus ALL retained local pending records before current projection publication. Never assume the intent alone advances the frontier. A stale starting frontier/selection requires reconciliation before a new preparation; a storage/key/context/CAS failure publishes no partial binding.

Signed inventory uses an explicit `earned/r1/rows-v4/*` extension with21 collections, including `sourceImports`; rows-v3 remains exactly20 and refuses unsupported source rows. The existing P1 encrypted-row algorithm and collection-bound AAD stay unchanged; a trusted, explicit source-profile capability permits this new collection. Default P1 source support stays off. Recovery must verify complete signed inventory AND retained semantic binding/chunk/history coherence before source use. Neither an inventory chunk nor a source receipt grants a fresh clock/lease, current prescription, consent, K1 closure or private activation. Legacy complete-proof/replay APIs must refuse unsupported source inventories until explicitly joined; they cannot silently omit them.

This amendment authorizes local candidate code under existing R1 paths and this spec, not a T2 edit, provider/deployment configuration change, private import or independent acceptance. The next actual vertical witness must use real authority admission, encrypted durable custody, complete signed recovery and later-operation-preserving rollback. Qualified current-state publication and the physical/private/integrator/scientific/resource gates remain open.

Status: local preparation, named encrypted local custody, R1 source selection/rollback and W6 signed source recovery with actual T2 pending-work replay are implemented as LOCAL CANDIDATES. Durable qualified active source/current-state publication remains unimplemented. This is not an accepted wire/schema amendment, port authorization or IMPORT-ROLLBACK PASS. The single execution queue remains the retained `outputs/EARNED-DELIVERY-STATUS.md`.

## Existing authority

`rebuild/m3/PLAN-M3-v1.md:70` requires an immutable chosen source generation, checkpoint/frontier and private source hash; generated guidance remains separate from accepted targets/provenance. Activation is atomic. Rollback restores a projection checkpoint and replays ALL later accepted and queued operations without erasing imported history or rewinding the live database.

`rebuild/m2/BRIEF-POSTFIX-GATE.md` §4 supplies the actual composition: capture independent remoteBefore/localBefore before any call; consume `migrate(remoteArg)`, then `mergeState(rm, localArg)`, then `migrate(merged)`; guard BOTH pre-call images against the final result. The accepted D33/D35 implementation and its original gates remain unchanged. An imported correction log is not external authentication.

R1 `rebuild/m3/w5/worker.cjs` answers `/import` with501 unless explicit source capability is configured, when its new local candidate handles the transaction. `/restore` remains501. W6 `rebuild/m3/w6/repository.mjs` atomically seals/publishes a complete active generation against the loaded revision/token, but that primitive does not authenticate an import binding. M4 `workout/engine-order.cjs` checks anchor causality only; the now-verified W6 source binding still must enter the actual engine/current projection and final publisher.

## Implemented preparation

`rebuild/m4/import/prepare.cjs` is a Node-only, in-memory preparation adapter. Inject the actual installed engine and W6 `parseStrictJson`. It imports no engine entry point itself. Its synthetic test is local only and uses the installed engine; neither it nor the full seeded engine is a browser asset. No app/bundle allowlist changes are made.

`prepare(sourceBytes, {localBytes})` copies both byte buffers before parsing or engine calls. `localBytes` is optional for a source-only preview; a replacement/merge of an existing installation MUST supply the real independently captured local engine snapshot through the future qualified controller. Absence never means an existing account is empty. It returns isolated accessors for original bytes/states, candidate bytes/state and a frozen local summary. It makes no storage call and no promise that preparation survives a process exit.

The supported-version migration factory is reused, not copied. Current-schema migration calls the mutating `_settleExit`; earlier migration may return a different object. The adapter retains the actual return, uses separate working copies, performs the real merge/settle when local input is supplied, and runs actual `dataLossGuard` on independent copies of both original states. JSON output must round-trip without silent NaN/Infinity/undefined/hole/negative-zero conversion. Exceptions expose fixed codes rather than private engine prose or guard details.

Unsupported/future input is refused before any engine call. v1/v2 require a separately qualified seed-template/profile path; there is no implicit Joe or Dad default. v3 through the installed schema may enter the existing migration, but any migration/schema/serialization/guard failure refuses preparation. Focused execution covers v59 and v60; it does not qualify all historical private profiles. The underlying engine's required future-schema identity return remains unchanged.

The summary's source/candidate/local digests identify local bytes only. They are private once used with private data. Do not log, publish or treat them as signatures, source-generation IDs, accepted frontier receipts or consent. Historical `accepted`, `targets`, adjustments and other decision records remain verbatim in the original source. Reconciliation of those fields in the candidate does not grant current plan authority. `activation: pending` is a local disposition, not a new durable knowledge fence.

The guard is stronger than recordCounts but is not whole-value parity. Its D33 protections include record/date/exercise/occupied-set identity and correction coverage. A same-date measurement value change can pass it. A focused explicit scope test preserves this fact. Do not turn guard success into “every value preserved,” scientific acceptance or qualified carryover. Exact original bytes remain available for the required full port/parity review.

## Required controller transaction, not implemented by this adapter

1. Capture the authenticated account/device context, complete accepted frontier/prefix, active-generation revision/token, selected immutable source bytes and local projection checkpoint before preparation. Include the independent pending operations/outbox; engine state alone cannot represent their custody. No local enumeration/sequence or cached view substitutes for authenticated source coverage.
2. Prepare using the adapter. Store exact original bytes and the selected candidate as an inactive encrypted source generation through a reviewed custody representation. Separate historical source decisions, derived candidate guidance, and authority-accepted current targets/consent. Give retries stable source identity from this custody layer; do not re-mint IDs or re-run writers to manufacture acceptance.
3. Before activation, verify the same source, account, checkpoint/frontier and current repository revision/token, plus the actual applicable issuer/permission/schema/clock/standing conditions. A changed source or intervening operation requires fresh preparation/reconciliation. The existing accepted wire contract must explicitly authorize and bind the activation; preparation supplies no fake `activation_op_id`, metadata token or alternate proof channel.
4. Publish the chosen source/checkpoint, accepted current-plan provenance and complete resulting projection atomically. Keep the earlier source and checkpoint. No success paint, current Start permission or import anchor before the actual accepted binding and durable publish. A crash must leave either the previous active state or the entire new state recoverable, never a mixed generation.
5. Rollback selects the prior projection checkpoint and replays the complete subsequent accepted log plus all local pending operations through the existing projectors, preserving identities/bytes/dispositions. It does not restore an old database over current facts. Imported originals remain retained. Repeated delivery or rollback must not execute a writer or consent transaction twice.

The exact issuer/authority representation, encrypted inactive-generation custody and activation/recovery encoding need a coherent amendment in the existing R1/W6 contract before their implementation can claim acceptance. In particular, the current closed operation registry and P1 coverage cannot be extended by placing extra keys in metadata. K1 baseline-only deletion/recovery and schema2 old-pending compatibility remain real prerequisites, not solved by this document.

### Named custody and recovery amendment — proposed implementation constraints

APM's bounded read-only review of a60f5a2 found no preparation defect in its reviewed scope and identified the actual repository constraint: `readRecords` reads only active/previous; `publish` overwrites previous on EVERY commit. Therefore that rotating slot is not the import rollback checkpoint. Two ordinary saves after activation would otherwise erase the selected pre-activation baseline. This is a controller dependency, not a new engine defect or independent acceptance receipt.

The controller must retain an immutable, named custody record independently of active/previous, using the existing encrypted repository/storage boundaries. Its required contents are the exact selected source bytes and candidate bytes, their local identifiers/digests and migration/build context, the complete pre-activation projection checkpoint, accepted frontier/prefix identity, and every pending operation/outbox identity and exact stored body at that checkpoint. The source and checkpoint record must remain readable after later commits and repeated rollback. A local name or digest does not confer authority; the signed activation must bind that retained identity and the actual checked account/source frontier.

Reconciliation, encrypted export and isolated restore must cover the source record, named checkpoint, pending work and signed activation binding together. At present R1 `project.cjs` enumerates exactly20 collections, enforces exact metadata/state fields, and `storage/row-codec.cjs` restricts sealing to that inventory. `VERIFIED_IMPORT` in accountRegistry only describes retained authority-history origin; it is not an implemented legacy-engine importer or a source/checkpoint receipt. Consequently, neither an extra metadata key nor changing that label supplies custody/recovery coverage. Any new retained representation must be added coherently to the existing versioned inventory, sealing, signed coverage and client verification; old pending operations/replay remain valid under their original profile.

Final local activation reuses the existing revision/token-guarded atomic `repository.commit`; a separate store or parallel authority is unnecessary. The import extension must validate the named record and actual signed binding against the same active revision/context immediately before that commit. On recovery, a missing named record or source binding remains unresolved integrity; do not silently activate cached guidance or reinterpret a remaining marker as proof. This amendment defines required custody contents and transaction behavior, not a new accepted wire operation name.

The first full vertical case is staged import → genuinely accepted activation → a later remote accepted fact and a local pending fact → at least two later durable commits → rollback → close/reopen. Assert every original source byte, prior and current capture, later accepted fact, pending operation body/identity, and truthful disposition remains available. Repeat rollback and delivery; no duplicate writer effect or deleted imported record. Exercise interruption before and after publication. This case must run through actual authority/HTTP and repository implementations when their contract extension exists; the preparation tests do not stand in for it.

### Implemented local custody at W6 0b281870fa6ed51686257afa70f738be704b4902

`rebuild/m3/w6/import-custody.mjs`, exposed through `repository.importCustody({parseStrictJson,validateContext})`, stores named inactive records in the existing generations object store, independently of active/previous. `stage(id, expected, {sourceBytes,candidateBytes,localBytes,engineContextJson})` captures input before asynchronous work and reads the checkpoint from the actual encrypted active generation. It ignores caller-supplied expected.generation. localBytes is explicitly nullable for source-only preparation, never evidence that an existing installation is empty. Stored source/candidate/local JSON text retains exact validated UTF-8 spelling and bytes; the complete original repository generation preserves pending operations/outbox and metadata.

The local-only encryption domain is earned/local-import-custody/v1 with AES-GCM additional data binding profile, namespace and source name. This differs from the active-generation format and cannot be promoted to active by moving the record. Atomic staging compares the active revision AND token in its final transaction and checks current context there. It publishes no active generation. Only completed durable transactions return staged; abort/quota/stale/context failure retain the original active state. Names cannot replace bytes or retarget a checkpoint. An identical retry returns the original checkpoint receipt after later saves; it does not freshen that receipt into activation permission. load(id) rechecks current context and returns independent copies.

This API preserves bytes, not provenance acceptance. The migration context is supplied and unverified, the source name is a local custody identifier, and all results say activation pending. There is no rollback or activate method and no automatic active-state restore. At that checkpoint the existing R1/P1 signed inventory did not export/reconcile this local profile. The v1.3 join above recovers explicitly uploaded source material/checkpoint; automatic application upload and qualified recovery after complete local loss remain open. No K1 fence or private carryover qualification is claimed.

Executed:12 focused checks (11 custody and the actual M4 preparation→W6 encrypted stage→reopen),8 effective product faults with original/restored11 PASS,383 complete W6 checks,11 native Chrome custody checks through relaunch, required conformance/selftest and strict PASS. The local vertical custody case stages after a real T2 pending fact, makes two more actual T2 saves, proves previous rotated, then reopens the exact named checkpoint/source while all3 current pending operations survive. That case deliberately does not claim the not-yet-implemented accepted-activation/remote-fact/rollback sequence above. Exact logs, source hashes and initial test-copy/native-JSON setup failures are retained in W6 REPORT-W6-ASTRA.md.

## Remaining evidence at the actual join

Use synthetic data first: both merge directions and repeat delivery; source/local changes between preparation and activation; interruption at source chunks/checkpoint/activation; missing/edited source and wrong account; accepted remote-device and queued local operations after activation; rollback twice with every later fact and original capture retained; current-plan consent/withdrawal separated from historical source records; complete source/candidate/private parity at the authorized port. Verify actual Worker/HTTP, encrypted repository and reopened history, not an in-memory substitute. Physical/private/independent/integrator/scientific/resource gates remain separate.

Current focused evidence is only the preparation adapter: 14 tests and 10 effective faults in disposable product copies, original/restored GREEN. Cases include actual source mutation, both protected preimages, lost migration return, local-only loss, equal-count identity substitution, unsupported schema, strict parser, isolated outputs, lossy JSON and private error text. Existing installed engine files are unchanged. No private source was imported and no new deployment/merge/activation was performed.
