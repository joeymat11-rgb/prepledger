# Performed workout facts → actual engine — proposed compatibility amendment v1.11 (working revision)

September9 status: the bounded numeric D41/D43 repairs, canonical earn module, closed source/evidence profile and exact build entry are independently accepted by Review149 at3e908d2 (artifactd21fb7fa31264c49058e31b58f695a45640f7f2ee61e7103a3277f411dee290c). Real ledger receipt integration remains pending. The internal actual-history mapping is now implemented and tested as described in §5e. The rich reader remains nonshipping source construction; its production caller, original-layout resolver, import/controller join, qualification and complete app joins are not thereby implemented or accepted.

ASTRA · 2026-09-09 · Part of the retained owner-workout brief/PR46, not a new app, stream or training rule. The rich-input amendment remains PROPOSED. Independent146 found the bounded D41/D43 construction and canonical earn move SOUND; Review149 then accepted their actual implementation and completed evidence at3e908d2. PACKAGE receipt integration remains pending. Rich-fact construction follows PR32's same-workout resume/Finish/correction journey, accepted through13946a1/Review144. Full PRODUCT-GOALv3 and CURRENT-PRODUCT-DECISIONSrev3 govern.

The original20-case compatibility and20-case source-proposal scripts below reproduce on published f146db69, where their preimage pins hold. The candidate uses load-write.test.cjs against actual static modules:3/3 blocks PASS, with full oracle3-blobs×2Date modes,18 merge laws and the existing integrator3072/3072 runtime diagnostic also PASS. Subsequent evidence covers180 exact complete original-law comparisons (all45IDs×four date/mode cells),44 new direct comparisons/20 effective module mutants,1180 inherited direct comparisons/68 effective mutants, full original writer differential in three modes and exact source/witness carriers. Expected new outputs derive from pinned frozen source before candidate inspection. Reportv0.47 distinguishes these executed checks from the new cumulative profile and independent receipt still pending. No personal, scientific or phone acceptance is inferred.

## 0. Authority, scope and exact evidence

The owner approved all45 M2 repair directions at DECISIONS.md:60; D41/D43 are already approved FIX directions, not questions to ask again. That does not approve an arbitrary implementation. Their accepted theme/delta gate remains required. D41 repairs prescribed per-set load translation. D43's law targets the owned-standard branch, while its FIX explicitly requires **each persisted session entry** to use validated performed load; its EVIDENCE already includes the unconditional `writers.cjs:400` write. The repeated-entry flatten is an additional regression case for that same D43 write-site repair, not evidence that D43's original law covered repeated entries. The theme must retain the original law and add this positive/negative case with exact changed-output accounting. This does not authorize replaying the writer or define the richer representation below.
The legacy engine closure in `performed-engine-compatibility.cjs` is byte-pinned to075f37c0e0cbe51ade41f42a667469cc4684cd0e; actual workout projector/schema are pinned tof7e7e42. Run `node rebuild/m4/spec/performed-engine-compatibility.cjs <retained-W6-tree>` from the repo root. Working result:20/20 diagnostics CONFIRMED in native/frozen ambient Date modes with an injected clock. Distinguish three kinds of evidence: (1) valid legacy repeated entries with entered loads40/30 are stored30/30 at the unconditional writer site; (2) proposed extra fields for per-slot vectors, middle effort and source IDs are ignored, proving a representation gap rather than a defect within the old input contract; (3) tagged effort passed into a numeric reader is an invalid-adapter hazard, not a currently stored schema defect. The actual factual projector preserves corrected/current/original/removed values; it grants no eligibility.
The repeated-entry regression now reaches actual `typicalError`: four entirely synthetic sessions produce seven spurious same-load residuals and select its own-repeat estimate, although their alternating entered loads satisfy no adjacent same-load pair. This proves an effect on the noise instrument under this synthetic input; it proves neither a live trigger nor personal scientific validity. A lower-bound object also produces false failure prose in the hypothetical numeric adapter, whereas missing effort produces the existing unrated explanation. Thus both numeric results and receipt meaning need assertions.
Those rows pass the actual basic shape validator but their accepted statuses are declared synthetic inputs. This is not a signature, HTTP, lease, production bootstrap or scientific qualification test. Proposed extra writer fields deliberately expose ignored input; they are not an adopted interface. Weight×reps exposes information loss, not a validated muscle-growth metric.
No edits to frozen app, original laws/oracle/manifest, old goldens, seeded soak or canonical operation bytes. This amendment selects an engine-side read representation; no new authority schema or transport format. Default legacy input behavior stays exact except separately reviewed D41/D43 deltas.

## 1. Factual representation and trust boundary

Build one private derived workout view from the SAME authenticated generation and already-resolved session/slot/edit interpretation used by the retained host. The producer obtains this view through its existing scoped integration, never from renderer-supplied values or a caller's `eligible:true`. Preserve its exact source revision/token, known frontier, account/device/context and current accepted plan basis through the existing preparation/final-commit checks. A local known prefix is not proof of an unseen remote head; existing offline standing/lease rules remain applicable.
The smallest representation is a versioned, ephemeral `workoutFacts` member on the engine input, separate from the immutable legacy `sessionLog`. It contains sessions keyed by Start operation identity, not date. Each session retains its effective date/time/offset, captured plan basis and ordered captured slot identities; actual Close/Skip references; unresolved interpretation reasons; and all included/current fact references. Original payloads remain in the authoritative store; the view carries source IDs and exact current values needed by readers, not a second mutable history database.
Each slot retains its lift lineage and logical slot identity, original Set operation ID, ordered effective edit IDs, source/current disposition status, inclusion/removal state, and exact current `load`, `reps`, and optional `reserve` variants. A skipped or unlogged slot has no fabricated performed payload. Removed originals remain addressable in the source and history UI; their old values do not contribute as current performance. Two sessions on one day stay distinct; no date-key overwrite, repeated-exercise workaround or arrival-order interpretation of concurrency.
Keep prescribed loads/targets/effort and actual performance in separate fields. `exercise.wSets` is a prescribed vector, never an alias for performed weights. Do not add a vector to a legacy entry and assume the old writer consumed it: the executable collision witness disproves that. Do not average weights or compress skipped slots so a different set becomes the opener/terminal slot.
This view is rebuildable read state. Reopening, syncing, correcting or replaying facts MUST NOT call `completeSession` again to mint receipts, mutate the current plan or apply an adjustment. Any engine transition/proposal is evaluated once against its declared basis and committed only through the existing authorized operation/consent/plan-transaction path. An acknowledged workout fact is not consent to a plan change. Derived caches may be discarded/rebuilt; immutable originals and agreed plan effects may not.

## 2. Reader contract and bounded source changes

Use actual extracted factories with explicit clock/IDs/drafts and explicit athlete inputs. No import of seed/migrate/merge into the browser, seed literals, empty-owner fallback, copied scoring algorithm or parallel prescription engine. Reuse the accepted legacy readers on legacy entries and add a selected rich-fact branch at their real access points. A tagged value must never reach arithmetic through JavaScript object coercion.
Add a small `rebuild/engine/performed.cjs` factory for the versioned view's validation, session/slot enumeration and typed effort predicates/formatting; bind it through the existing data-free assembly. Keep the projection into that factory under `rebuild/m4/workout/engine-history.cjs`, called inside the retained producer boundary. The internal history module now exists (§5e); the performed factory still exists only in nonshipping source construction. Neither grants authority to ship an unqualified producer.

| Consumer / actual source | Required rich-fact behavior; legacy branch remains exact |
|---|---|
| `writers.completeSession:230,400`; `applyAgentProposal` D41 | Never use the writer's lossy history return as the factual source for a new workout. Its proposal/effect evaluation must retain the supplied fact basis; factual projection is independent of replaying its side effects. Implement D41/D43 under their approved exact source/expectation amendments, not by substituting prescribed weights for actual records. |
| `progression.sessionScore:540`; `liftTrend:555`; `progressionTrend`; `volume._blockSlope:170` | For rich entries, compute the existing load×repetition accounting from each included performed set in the supported same unit, preserving distinct sessions and correction effects. Do not substitute first/last/mean load. Keep technique-era, quality, window, minimum evidence and noise rules; changes in comparability or interpretation need their existing rule qualification. The total alone is not proof of hypertrophy or readiness. |
| `progression.typicalError:445`; `beatsNoise:477`; `writers.sessionDebrief:624–640` | Enumerate qualified comparable rich records with explicit performed-slot/load identity before invoking existing residual/banked arithmetic. A falsely flattened common scalar must not create a repeat. Keep same-era checks, minimum-six residuals, own→pooled→published fallback and existing error-band arithmetic; these are implementation contracts, not a fresh scientific endorsement. `beatsNoise` currently receives rep arrays and does not check loads itself; establish eligibility at its caller rather than claiming that function already pairs load vectors. |
| `progression.rirSetsOf:415`, `openerRir:438`, `terminalRir:441`, `deriveLastMeta:398` | Query actual captured slot identity. Return typed facts through the rich branch, preserving absent/unknown/skipped/not_asked and lower bounds. Legacy numeric arrays stay numeric and byte-compatible. A missing planned terminal set is not the last performed set. |
| `progression.progressStep:18`; `volume.volumeConversion:240`; `writers.sessionDebrief:650` | Replace only their rich-input comparisons with typed predicates; never turn `at_least3` into `exact3`. Existing predicates `>=3`, `>=1`, `<=1`, `===0` are decidable for the supported bound; absent/unknown/skipped/not_asked give unknown, not false or zero. Preserve each reader's existing missing-evidence branch and denominator semantics; do not introduce a blanket hold. Any other requested comparison must have an explicit truth table before use. |
| `progression.rirReceipt:425` and affected athlete-facing why text | Format exact values versus “at least3” truthfully; distinguish missing/unknown/skipped when relevant. Do not print a known number as a measurement if it is only a bound. Preserve old receipt strings for old inputs; list exact new-profile text/output changes for review. |
| `progression.progressAnchor:57`, `targetsFor`, `_deriveSightingFull`; `volume.setOneRead`; `plan` exposure/era readers; `volume.muscleVolume` | Read the same resolved facts, original slot identities, technique era and factual set counts. A skip is not zero reps; a tombstone is not another set. Current prescribed-vector versus performed-vector comparability must be explicit. Do not silently feed the scalar legacy cache when the rich record disagrees. Preserve existing era/exposure rules; partial-slot anchoring and unresolved partitions remain the named qualification question in §5. |

Before product edits, publish the exact reached declarations, late-bound delegates, public composition imports and closed source-delta carrier against the implementation base. The table is a semantic boundary inventory, not a claim that every transitive caller has already been enumerated. `sleep` delegates to volume/scoring; its body-alarm consequences belong in the affected output comparison, not a separate reimplementation.
The factory's typed predicate is three-valued: true/false/unknown. For supported exact0/1/2 and at_least3, evaluate each registered predicate from its full represented set of possibilities. Unknown variants stay distinct facts even where a particular legacy decision maps them to the same existing missing-data branch. No unregistered threshold, scalar surrogate, mean effort or invented confidence.

| Supported recorded fact | >=3 | >=2 | >=1 | <=2 | <=1 | ===0 | ===1 | ===2 |
|---|---|---|---|---|---|---|---|---|
| exact0 | F | F | F | T | T | T | F | F |
| exact1 | F | F | T | T | T | F | T | F |
| exact2 | F | T | T | T | F | F | F | T |
| at_least3 | T | T | T | F | F | F | F | F |
| absent / unknown / skipped / not_asked | U | U | U | U | U | U | U | U |

This table also closes the exact1/exact2 branches observed in `progressStep`, and the <=2 stall comparison; it does not invent a threshold. An unregistered recorded variant must retain its source fact and receive explicit interpretation before a decision, never be numerically coerced. Legacy numeric3 is not retrospectively declared an exact measurement: its original input contract/provenance belongs to the rich-to-legacy mapping review.

### Existing comparison rules and the proposed rich-record extension

`typicalError:461` compares adjacent entries with equal scalar-load strings and equal performed-set count, within the technique era. Equal count alone does not identify which planned slots were done, and two equally short records can meet it. `atTopOfWindow:375–385` requires the complete **progression-bearing prefix**, not necessarily every currently configured set. `progressAnchor:55–79` has no corresponding complete-prefix check: the working diagnostic proves that a two-set record can anchor while the three-set top-window test is false. Keep these distinct rules; Review145's statement that a partial record cannot anchor was too broad.
Proposed rich noise-pair key: the same lift/technique with equal ordered effective slot identities and equal exact performed load values/units, with current included numeric reps at the paired positions. Do not compare only equal vector lengths, sort slots by weight, average loads, or borrow the prescribed vector as actual work. Explicitly resolved partial records with the same performed slots are not automatically rejected solely for being partial; their use still follows the relevant existing measurement/anchor/top-window rule. The top-window rule requires its own declared prefix identities. Proposed comparisons between rich and legacy records require a proven slot/load mapping; absence of that mapping cannot silently become a made-up scalar match or reconstruction of lost values.
This is a proposed meaning-preserving extension for technical/affected scientific review, not an owner-approved physiology change. It can change pairing, sample count, error estimate, record confirmation and explanation; the implementation brief must enumerate those outcomes. D40's owner ruling concerns **same-day calorie-conflict tie breaking** and supplies no authority for workout comparisons. The applicable route is the general accepted theme behavior/delta review plus existing prescription qualification. No fresh owner question is needed for preserving exact entered facts; a genuinely new material training choice still uses the existing ruling route.

## 3. Coherent implementation and acceptance chunk

One retained root integration chunk: actual host correction → authenticated factual view → actual engine's affected readers → qualified prepared next instruction at the exact current basis. Ship no detached refusal-only helper as the completed milestone. Keep current safety, captured old instructions and candidate new recommendations visibly separate. The same typed facts must drive displayed actual history and the next decision, so a correction cannot update one but leave the other stale.
Use entirely synthetic, shape-valid signed operations through the actual T2/W6/D1 path for the positive integration cases; the pure diagnostic is only the starting witness. Compare: uniform loads/legacy parity; varied loads on three slots; exact0/1/2 and at_least3; middle effort; absent/unknown/skipped/not_asked; reasoned Skip/early Close/normal Close; correction/removal followed by new work; two resolved same-day sessions; current versus old technique; and current accepted plan versus captured historical instructions. No invented Joe/Dad profile.
Every case checks exact original/current facts, source IDs/statuses/units, final input/output/why/receipt bytes, applicable source basis and permitted plan effects. Repeat the same history and reopen the real host: no additional Set/Close/receipt/plan transaction; a later correction deterministically changes only its authorized derived consequences. Revoked/stale/ambiguous source cannot obtain a fresh permitted handle by copying an old result. A valid qualified case must succeed, not merely refuse every input.
Sensitivity: independently remove one performed weight, collapse a lower bound to exact, drop a middle effort/identity, treat a skip as zero, ignore a correction, replay the completion writer, overwrite a same-day session and bypass the current plan basis. Each must fail a named behavioral assertion after reaching the relevant product path. No syntax/pin/timeout failure earns a bite. Restore exact product hashes and re-run affected GREEN cases.
Existing M2 requirements apply to every extracted-engine edit: full three-blob migration/port checks in both Date modes, complete second gate/surface/sync laws, exact reviewed source/expectation carriers and approved delta accounting, v4 red-first laws and PACKAGE provenance, public package/frozen-path tests, conformance/SELFTEST and strict with test clock unset. Do not replace an old expectation merely because it fails. Use the established retained post-fix gate; if a required changed expectation is not yet specified, complete that concrete amendment before claiming implementation acceptance. Private inputs/results stay under existing custody and publish verdict only.
One independent review covers the coherent facts/readers/integration change plus its necessary source/delta carrier. Integrator acceptance and combined-revision gates remain. Original96MiB/default-runtime capacity failure, K1/negative-knowledge/CLOCK/keys, production custody/restore/port and actual-phone gates are neither solved nor waived by this representation. No repeated resource variants or enlarged limit.

## 4. Report, seams and successful next demonstration

Keep the existing nine-section report: exact base/ownership/source map; executed gate lines; clock/ID/storage/trust boundaries; effective bites/restores; seams; rule/source disagreements; not-covered; elapsed/accounting; defects/status/NEXT. Distinguish data correctness, implementation correctness, scientific applicability and actual private-device evidence. No whole-app percentage, calendar promise or training superiority claim.
The next successful demonstration is the SAME app showing a corrected three-set workout with different actual weights/effort and deriving a next instruction from the real engine at that exact factual/plan basis. The displayed explanation must preserve bounds and identify applicable uncertainty; agreeing an adjustment uses the existing consent/effect path. Synthetic success precedes the required separately qualified personal case. Broader individualized nutrition/recovery/lifestyle/phases and daily-plan U1–U5 remain planned in their approved delivery order.

## 5. Open implementation questions — resolve inside the existing work, not by silent defaults

1. Review the explicit rich pairing/mapping extension above against each existing caller, with exact outcome accounting. Existing scalar-load/count/era, common-prefix noise arithmetic and progression-bearing-window rules are located; they are not one universal eligibility rule. Still close rich-to-legacy mapping, missing/changed planned slots and current prescribed-vector anchoring in the bounded implementation brief. Do not choose a convenient scalar or add an unapproved blanket restriction.
2. Close the complete transitive source/expectation inventory and data-free assembly on the actual integrated repair base. This proposal names the observed access points; permission to build must be bounded to a reviewed exact delta, not “all engine consumers.” Do not drop accepted D12/D30/import repairs by building from the older diagnostic pin.
3. The qualified prescription/current-safety producer and effect protocol remain required. Preserve the existing EFFORT-03/applicability findings rather than declaring numerical legacy parity to be scientific validation. No newly delivered prescription is accepted merely because this representation is lossless.

Independent145 judged a4a6126's direction SOUND and reproduced16/16; that is direction acceptance only. The numeric dependency is now independently accepted as stated above; the following rich correspondence selection remains a proposal for the same affected review. No additional owner setup, new dispatcher, agent, model, credits or schedule is requested.

### 5a. Concrete first-use correspondence proposal — not yet an adopted producer

Preserve the full approved goal while selecting the smallest faithful supported mapping. The initial qualified producer may issue one occurrence of a lift per captured workout, with ordered set positions determined by the actual governing exercise definition. Preserve each original logical_set_slot verbatim, including arbitrary valid IDs. Resolve its one-based position from the recognized producer's ORIGINAL captured plan inputs, with explicit versioned correspondence meaning. A canonical JSON tuple `[lift_lineage_id, one_based_planned_position]` remains a supported older proposal-fixture convention, not a schema requirement or permission to rename real IDs. A string that merely resembles a tuple is not proof. The registered resolver binds exact original producer and basis, complete captured membership, lift lineage and contiguous within-lift positions. Do not infer correspondence from labels, present-day configuration or a caller-supplied eligibility bit. This uses the existing capture/producer fields; it proposes no new authority record or schema field.

Cross-session identity is then the same authenticated athlete, lift lineage, applicable technique era and explicitly resolved planned position. Different Start/Set operation IDs preserve distinct facts without preventing a comparable repeat. Different plan bases do not automatically invalidate a pair: the reviewed producer must prove unchanged position meaning across them. A changed/unknown producer convention, repeated lift occurrence, unrecognized historical capture or unproved legacy mapping remains explicitly uninterpreted for the affected comparison. All its original facts still appear in history. This is an input-meaning proposal; it does not itself approve the physiological pairing or grant a new prescription.

Retain the complete captured slot list before selecting performed records. For a captured A/B/C session with A and B performed and C skipped, C has no terminal measurement; neither B's exact0 nor A's at_least3 can fill it. Removing B leaves A, a removed B fact and the C skip; positions never compact. A known performed prefix may still follow the existing partial-anchor branch when its load/era/caller requirements hold. It does not thereby meet the complete progression-bearing-window test. Non-prefix holes need an explicit affected-reader mapping before advice: retain the actual agreed plan and explain unavailable evidence for that dependent decision, without inventing a zero, globally discarding the session or installing a blanket hold rule. Exact positive/negative output and science applicability still require the existing review.

Consumer-specific comparison stays separate: repeat-noise pairing uses corresponding included positions and each actual load/unit; the opener reader uses the captured opener only; whole-session accounting uses every included performed load×repetition; effort predicates use the typed truth table. A middle correction can change whole-session accounting without changing the opener measurement. Same-day chronological ties require the existing resolved-history interpretation, not Start-ID sorting or D40's calorie rule. Mixed rich/legacy comparisons require a witnessed mapping rather than reconstruction of missing old per-set weights.

**Executed source preparation.** `node rebuild/m4/spec/performed-reader-reach.cjs <LOCAL_JSON>` pins thirteen public source files to accepted3e908d2 and records actual V8 declaration/late-delegate reach from the static browser+writer assembly. Ten probes (five in native/frozen Date) exercise fresh workout, effort/hold distinction, terminal-rating denominators, opener isolation and the existing read consumers. They reached96 copied declarations and78 late delegates, with identical outputs across Date modes. The JSON contains exact source/declaration hashes, current/frozen lines and per-case reach. This replaces no gate and is not exhaustive branch coverage or authorization to modify all reached functions; the earlier218-node static estimate remains an over-approximation. Combine this evidence with the selected literal source/delta construction before engine edits.

**Actual stored-source control.** Retained W6's new prepared-workout test sends a bounded opener, exact-zero middle, reasoned terminal skip and Close through the actual client/repository, reopens, then removes the middle through the prepared correction path and reads a fresh client again. Original capture/operations, bound, skip identity and removed fact remain exact; the history explicitly grants no progression eligibility. Focused83/83 pass. This closes the factual half of PROGRESSION-CASES-01 M2, not its qualified-reader half, K1, real HTTP, scientific or physical-phone acceptance. No lossy legacy-writer replay is used as input.

## 6. Historical source construction — implemented and accepted at3e908d2

Inspection/attempted execution found a missing runtime dependency: `writers.completeSession` calls `E.earnWalk`, but the data-free browser composition excludes `migrate.cjs`, where `earnWalk` is currently defined. The first load-write probe stopped with `TypeError: E.earnWalk is not a function`; this was an assembly error, not a behavioral RED and not counted as a mutation. Earlier20 compatibility cases never entered the two-sighting earn branch, so they did not establish that closure.
`spec/load-write-proposal.cjs` resolves that diagnostic boundary by compiling **only** the exact `earnWalk` declaration from integrated base28ff3be (SHA256888af1dc693a4a0929b6883a7ce5393f908890cb633059e193fe92fd1bb212c3), with the four actual late delegates `loadRungs`, `nextLoad`, `typicalError`, `beatsNoise`. It loads no migration factory or seed. This is source construction evidence, not a shipped dynamic compiler; production must use static modules.
The mechanical module change required by the real browser join is a verbatim move into `rebuild/engine/earn.cjs`, retaining the same callable signature and explicit inputs. The old migration factory delegates to the one canonical E.earnWalk; assemble the canonical factory after that delegate has been registered so it cannot overwrite itself with a recursive alias. The browser-safe composition imports that same factory, without seed/migrate/merge. No copied second progression algorithm, fallback implementation, new clock or private seed import. Verify direct-call, migration/merge callers, repeated engine instances and browser composition; a no-op or recursive delegate must fail a reached assertion. The module move needs a narrowly reviewed mechanical source carrier, not a whole-file exemption.

| Exact source construction at integrated28ff3be | Expected changed result; controls |
|---|---|
| D41 `completeSession:262`: after adopting the queued scalar, copy `q.newWSets.slice()` only when it is an array. | Actual writer creates a queued105/100 vector from100/95; its completed debut now retains105/100. No caller-supplied replacement queue. No vector is manufactured when the queue has none. |
| D41 `applyAgentProposal:2295`: preserve old scalar, set new scalar, translate an existing vector by new−old when old scalar is numeric. | Actual consented reset100→90 moves100/95→90/85. Retain unchanged proposal removal, plan/queue semantics and receipt text. No extra reset or volume effect. |
| D43 `completeSession:400`: prefer the entry's numeric `e.w`; otherwise retain the existing configured-load fallback. | Owned-standard entry110 is stored110 rather than100; repeated110/90 entries remain110/90 rather than90/90. Ordinary numeric and omitted-weight legacy calls keep their entire output. The validated typed-fact route remains separate; this fallback is not authority to turn an unknown new-profile load into a known measurement. |

The executable script contains each exact before/after literal, requires one occurrence, and compiles the complete proposed writer in memory. Before SHA2568b4cc4048d00e36845e2652ee939583d55ee06132e166701945cc64fde136d36; proposed SHA256008d92961d210ed07850ea881bd3a9f01ee873c1958f8b7d3a47506342c96bf5. Other eleven data-free source/fixture/composition files are checked byte-identical to28ff3be. These constructions implement the approved numeric D41/D43 directions only; neither typed rich facts nor scientific qualification is inferred.
Executed in both ambient Date modes with the same explicit clock: four desired changes, three entire-output/receipt controls, and three individual source reversions each detected. `LOAD WRITE SOURCE PROPOSAL: 20/20 checks PASS; 3 exact proposed source replacements; product UNCHANGED; full gates NOT RUN`. Reversions are in-memory proposed-source mutations, not mutations of an already delivered product. Old laws remain RED on the original source and must be GREEN on the eventual installed modules; no control wrapper may repair the candidate after execution.

The successor acceptance profile must carry the integrated import/D12 artifact, exact baseline module hashes and owner/theme receipts. Authorize only the verbatim earn declaration move, its four delegates/assembly imports and the three literal writer replacements for this construction. Preserve every other module byte, export name, migration pass and original expectation. Run actual full oracle3×2, second gate/sync/surface, current PACKAGE and mandatory publication gates; old source check conflicts need exact mechanical carriers that reconstruct and compare the original bytes, never skip checks. The original owned-standard audit law remains, with repeated-entry/alias/no-vector/omitted-weight controls added. No private census/output difference is assumed; an observed difference requires the existing protected reviewed expectation route.

Full rich-input consumer implementation still needs the bounded source/outcome closure in §5; the numeric repair and module move do not silently grant it. Static preparation inventories416 marked declarations across ten data-free modules and over-approximates218 potential dependencies including bare callbacks. This is not an AST/runtime-coverage proof. In particular, `_stateAsOf` needs historical rich-view filtering, recovery counts/effort must use the same qualified records, and `genSession`/`rirPlan` must preserve captured versus current instructions. Reuse the completed EFFORT-03 candidate/findings when defining that exact carry-through change; its old joined-resume hold was a real constraint, and the accepted W6 captured-instruction route must now be proved rather than presumed to resolve it. Do not import unrelated lab analytics or silently change training-day/energy accounting just to fill an input.

### 5b. Executable selected reader construction — nonshipping, September9

`spec/performed-proposal/source.cjs` pins thirteen public inputs to accepted3e908d2 and constructs literal replacements for progressStep, rirSetsOf, rirReceipt, sessionScore and typicalError; it adds the proposed performed factory to the disposable static browser composition. Every old byte is reconstructible from the recorded replacements. The retained product, accepted source artifact and frozen inputs stay unchanged. The test-only compiler does not ship. This selected closure does not authorize all downstream consumers or replace the full assembly/source/expectation gates.

`check.cjs` uses the actual retained capture/schema/projector/strict parser pinned tof7e7e42. Synthetic Start/Set/Skip/Close/correction/tombstone records pass their actual shape checks; the factual projection is source-preserving. Each completed rich entry retains explicit Close identity/status, full captured positions, current and removed originals, lower-bound effort and the separately recorded effort target. Synthetic statuses are declared, not authenticated; this converter is not the qualified producer. An open session is not inferred complete from its sets. Live active-session presentation must use its existing factual path, not this completed-performance entry.

Executed in EACH native/frozen Date mode:208 exact legacy whole-output controls;90 rich checks;8/8 named source sensitivities detected and exact candidate reconstruction restored PASS. Checks include weighted score/current correction, actual repeat-noise arithmetic, comparable partials, unequal performed positions/loads, preserved terminal skips, unknown/bounded effort, captured-target prose and explicit completion. Candidate prose first failed `PERFORMED_ORIGINAL_TARGET_PROSE`; it now states the saved result and saved target separately, preserving the original numeric step rules and legacy strings. The earlier null-prototype comparison failure was a harness issue and earns no behavioral-red credit.

Limits: source sensitivities are in-memory proposal mutations, not a delivered-product bite. Same-day ordering has only an explicit unresolved negative case; a supported resolved positive case, rich/legacy correspondence, full as-of filtering, other effort/history consumers, complete source carriers, qualified producer/current plan/consent and UI joins remain required. The typed helper does not grant progression eligibility, scientific applicability or authorization. No conservative fallback is silently promoted to a new product rule. Reproduce from this tree with `PERFORMED_W6_DIR` explicitly set to the retained W6 checkout, then `node rebuild/m4/spec/performed-proposal/check.cjs .tmp/performed-reader-proposal.json`. Only synthetic/code evidence is written.

### 5c. Internal semantic-order step and selected reader join

`workout/engine-order.cjs` implements B15, sheet586–590: traverse causal_parents through the same authenticated generation, reduce to Start dependencies, and use recorded athlete-log sequence only among ready concurrent Starts. Device predecessor/sequence, timestamps, Start spelling and object insertion never supply semantic order. Check exact frontier/complete prefix/Start status, reached scope, missing parents and cycles. Concurrent local Starts without an authoritative tie remain unresolved; an offline causal chain can succeed. Return ordered immutable Start IDs and source frontier, never a partition, progression-bearing flag or write permission. Its precondition is the existing host's authentication of storedWorkoutHistory and the SAME generation; this function does not replace signature verification or establish an unseen remote head.

The selected reader now consumes the producer's internal order and verifies exact membership instead of sorting rich sessions by date. This transports internal derived evidence, not a trusted renderer-supplied order. Positive same-day and date-skew cases exercise actual typicalError/performedHistoryRows using the real ordering function; they are conditional reader tests, not state14 partition acceptance. A resolved physical-workout interpretation and downstream scientific eligibility remain separate requirements. Mixed legacy/native order now explicitly requires the import mapping, instead of pretending legacy dates establish B15 causality; this is an unfinished first-use dependency, not an approved feature deferral or permanent ban.

`node --test rebuild/m4/workout/test/engine-order.test.cjs`:16PASS/0FAIL/0skip, including three named effective in-memory source faults (ID sort, absent concurrent-local guard, removed causal dependency). Then the proposal runner:208 legacy/92 rich/9 source sensitivities EACH native/frozen, restoredPASS. The new date-sort fault fails the earlier same-day semantic-order assertion; the initial harness expected the later skew assertion and failed without earning sensitivity credit, then was corrected to the actual first failure. No retained engine bytes, original gates or accepted artifact changed. The helper is tracked ordinary code, but the production host/engine/UI does not yet invoke it. No network/private input or independent acceptance is implied.

### 5d. Imported baseline plus post-activation facts — internal mapping implemented, multi-import composition still required

#### C2 amendment: native workouts spanning a later import or rollback

**Bounded opener consumer implemented, accepted scope by APM September10.** Common factual enumeration/validation now exposes internal performedHistoryMembers only to the nonshipping rirPlan known/hot opener count. Native membership/order integrity, shared immutable baseline.session_log identity and source/activation identifiers remain required. The count needs membership and original slot identity but no relative legacy/native chronology; it does not grant whole-prescription equivalence or qualify hold/alarm/sleep inputs. performedHistoryRows and typicalError retain their existing mixed chronology requirement and adjacent-pair behavior; formats are not regrouped. The actual registered replay→capture case passes: with fixed other context, two imported hot ratings plus an accepted native3+ rating meet the existing minimum/majority, while rollback removes the later imported rating and restores the previous target. Original capture bytes are unchanged.77 affected checks,208 exact legacy/153 rich checks and14 named effective sensitivities EACH native/frozen Date PASS. Missing/replaced baseline and unchanged mixed noise refusal are checked. The APM read-only matrix is acceleration-trial/NATIVE-READER-ORDER-01/RESULT.md under the authority root; it changes no product rule. Root's actual synthetic preparation/merge probe shows one same-date output combining both sources while preserving source bytes, so neither date nor whole-row equality resolves cross-format provenance. Exact logs and the explicit current W6 test-dependency pins are in the report.

**September10 accepted-membership implementation (candidate on M479a0c823).** The existing engine-history projector now provides projectAccepted and projectAcceptedWithSources over the complete authenticated inventory supplied by the host. First validate the complete current receipt prefix, then derive the requested accepted cut; pending originals/effects are not accepted inputs. Original membership is bounded by each root's original accepted position. Reconstructing a later import's original local image uses its historical effect cut; rebuilding the inherited state uses current accepted effects on those same original roots. Later original records are recognized but not falsely represented by the earlier checkpoint. The existing shared normalization/edit fold and B15 order remain authoritative; no completeSession replay or plan/receipt write is performed.

The guarded reading-replay path exposes this as workout_history separate from accepted_state and immutable local_json. Its private registered workoutInput returns the SAME frozen derived facts and imported baseline; the existing single engine input clone retains baseline.session_log === input.sessionLog. Import may preserve an unknown top-level workoutFacts field. Source-aware capture excludes that unproved field only from its ephemeral input, then attaches only the registered derived facts, including the case with no accepted native sessions. Original source/candidate bytes are preserved. This binding proves input correspondence, not rich policy/current guard qualification.77 affected checks, the actual import-field counterexample RED before/GREEN after, and original conformance/selftest are recorded in REPORT-OWNER-WORKOUT-BRIEF-ASTRA.md.

**Remaining signed journey boundary.** A/workout/B/workout/rollback/reopen including delayed A-Start is not yet proved in the actual signed recovery composition. Current R1 issuer validLease requires schema1; scoped admission checks the actor through I.device. The existing signed-but-unissued schema2 workout-http fixture intentionally fails recovery integrity, so it is not reusable as a passing recovery witness. APM permits a disclosed test-only selected profile/capability derivative only if unchanged whole-inventory, actor, shape/reference, signature and P1 checks all hold. No derivative or runtime issuer/core relaxation was made. Shape-only bridge selection would also leave sourceImports relation validation incomplete. Preserve these separate full-schema and authority requirements, the existing mixed chronology refusal, and all current-policy/consent/review/private gates.

September10, M4bc4d239/W62141c86/R192327ad. The single-anchor mapping below applies only to its proven post-activation case. It must not be applied to every retained Start when the selected import changes. APM read the actual order/history/capture/source consumers and the bounded reproduction, confirming that B15 supplies native order but no already-ratified resolver identifies each Start's original selected source from its capture. APM accepted this affected implementation boundary and subsequently the precise capture-v2 scope below. Candidate implementation and qualification remain distinct.

Keep four independent inputs in the same guarded composition: (1) the selected imported baseline, with original activation behind rollback and the same reconstructed sessionLog; (2) each original Start/capture and its registered layout, preserving original plan/input-basis identifiers; (3) the complete native B15 order and shared accepted/pending factual interpretation; (4) any original-source relationship actually established by authenticated retained producer/basis/source/checkpoint evidence. The current selection is not each Start's source. Original source provenance and whether a fact was already represented in a replayed state are separate questions.

The actual capture contains plan_basis/input_basis/local source_revision. Public-client supplies the revision and token fence, but resolveWorkoutBasis is an injected installation dependency; current retained examples use explicitly synthetic basis identifiers. Source validateSelectionChain proves the selected source at an accepted cut. Neither that cut nor the local revision proves the source used by a delayed/offline Start. Causal descent is a dependency, not by itself a unique source selection. readLayout proves captured format/layout, not historical source selection. No source identity may be inferred from the nearest activation, accepted position, display date or opaque input-basis spelling. Missing historical evidence remains unknown while exact facts and captures stay available.

The necessary prospective qualified-basis integration must bind the actual selected source/selection and complete relevant input membership when instructions are prepared, preserve that relationship through the existing atomic Start/token boundary, and later resolve it from authenticated retained evidence. The inspected retained client/authority plan actions bind plan transactions through seen_plan_basis, not a captured input_basis/source selection; the current workout authority profile returns true for Start relations and supplies no such resolver. These inspected paths do not close historical source association. Define the minimal prospective binding in this SAME C2/qualified-producer contract for affected technical acceptance. Do not introduce an arbitrary resolver result, new wire field, digest formula, metadata flag or second historical engine-state store solely to make a fixture pass. This is explicit remaining implementation work, not a claim that capture already carries the binding.

Exact affected consumers: import/reading-replay.cjs must reconcile native original/effect membership through each source checkpoint without dropping, double-applying or converting rich facts to legacy scalars; workout/engine-order.cjs retains full-prefix checks and B15 native order but must scope the old import-descent assertion only to an actually proven relationship; workout/engine-history.cjs retains full membership/status/shared-normalizer/original-layout checks and separates capture facts from original-source proof; spec/performed-proposal/factory.cjs may not turn selected legacy + all native concatenation into an unsupported global chronology. Preserve the existing one-import successful case. A qualified mixed-reader order requires its own proved source/replay relationship; absence must remain an explicit guidance limitation, not hide factual history. No installed engine, common schema, issuer, source wire codec or W6 core edit is authorized by this paragraph. Any reached additional consumer or prospective binding change must be named before its affected pre-edit acceptance.

The coherent witness is actual source A → captured Start/sets/Close → source B → second captured workout → rollback A → reopen, preserving every original capture/native ID, selected original baseline and pending body once. Add a delayed/offline Start prepared under A but accepted after B: its later receipt must not relabel its source. Correcting/removing an earlier native record must use the shared interpretation without changing its original capture or allowing the newer import to erase it. Keep unknown legacy context explicit. Synthetic unissued schema2 capability may support an attributed intermediate mechanism witness only; it cannot count as genuine issuance or complete recovery qualification. Full C1–C4 and existing currentness/science/independent/integrator/private/device boundaries remain.

Retained-function reproduction: coordinator outputs/NATIVE-SOURCE-ORDER-DIAGNOSTIC.json, SHAd341f3f133f4d50b0c978ba30da6f5fa96b97a8981fd957215c7536d399d5729. Declared synthetic host graph; selected B rejects old Start with WORKOUT_ORDER_IMPORT_DESCENT_UNPROVEN, reusing A or omitting the anchor passes both Starts without establishing correct source composition. Four actual source hashes and unchanged input/source are recorded. This is a specific falsifier of the universal-anchor assumption, not an authenticated journey or an approved replacement ordering rule.

APM subsequently read the complete two-document delta and ACCEPTED the minimal C2 scope clarification. No runtime or prospective representation acceptance was implied. The following is the exact additional boundary now submitted for readback; it is not implemented.

**Accepted prospective binding scope.** Inspection found no retained relationship sufficient to reconstruct the preparation source generally. Reuse the existing source codec's closed frontier value `{W,log_digest,selection_id}` as a `source_basis` member of a NEW explicitly selected `earned/workout-prescription/v2` capture. Keep the original v1 capture unchanged and readable; it has no fabricated source basis. Keep plan_basis/input_basis/source_revision and their existing meanings unchanged. This is one necessary additional captured datum, not a new digest formula, source record, operation kind or detached historical store. The existing source-import/v1 prefix hash/selection semantics are reused exactly. Source ID, original activation and rollback target are derived from the authenticated selection chain, not copied as redundant assertions. `selection_id:null` records a proven no-selection cut, not an unknown source or a permission to synthesize a default athlete.

**Preparation and commit.** The actual qualified controller obtains that frontier from the same authenticated source inventory and generation used for instructions, checking its W against that generation. The statically configured basis resolver must return this exact value alongside its existing basis/causal inputs; the public prepare/start APIs accept no caller-supplied source basis. A non-null selection becomes an explicit relied-on operation reference/causal parent, but the selected-source proof comes from the frontier/chain check, not ancestry alone. The v2 capture validator includes source_basis in descriptor-safe copy/freeze and exact expected-context comparison. Existing W6 revision/token/context/retirement fences still guard preparation and atomic Start. No source-only binding completes the unresolved full qualified input-basis, safety, consent or C1–C4 requirements.

**Historical resolution.** Extend the existing source codec's guarded indexed reader with a bounded historical selection-at-cut read that uses the SAME validateSelectionChain generator after authenticating/validating the complete current inventory. Validate the requested original W, recompute its existing prefix digest and selected ID, and compare the complete captured source_basis. Read original source/activation through that chain. A cut inside an incomplete activation/drain cannot become a valid captured basis. Use the existing final assertStable/currentness guards; never build a parallel source normalizer or trust caller-supplied rows. For an accepted Start, captured W must be strictly less than its original accepted sequence; do not require seq-1. Validate the same-athlete inventory once and batch historical cuts through the existing chain in the same reader, reusing same-generation material validation. A Start prepared at A's cut still resolves A when accepted after B; no comparison of capture local revision with authority sequence. A v1 capture remains factual with unknown original source. Authentication of the recorded source relationship is not execution/scientific attestation or current permission.

**Genuinely additional consumers.** Shared workout/capture.cjs and its configured validators need the closed v2 representation; existing schema.cjs reference extraction must include a non-null source selection for that profile; public-client.mjs prepareWorkout and resume assessment need the static resolved/expected source basis while startPreparedWorkout preserves its existing atomic capture path. Engine-capture's registered layout reader must distinguish v1/v2 without regenerating old captures. Source/codec.cjs validateIndexedSource and the existing W6 recovery-local guarded source-reader exposure need the historical cut operation. Final authority selected-manifest integration must validate the referenced source selection against actual same-athlete sourceImports, not merely the current Start-relations true return; its exact bridge/profile wiring must be inspected before that additional runtime edit. No issuer/core/lease transition is enabled by this proposal. The original four C2 consumers and joined witness remain the implementation purpose, with full schema/authority installation deferred only until its explicitly required complete manifest acceptance.

Implemented prerequisite, September10: guarded selectionsAt/inspectSourceCuts now resolve whole historical frontiers. The actual reading replay registers the same immutable consumed state and baseline in a private WeakMap; engine-capture requires that exact registered projection plus matching source_basis and refuses copied/substituted state. This retains no second engine-state image and supplies no current permission. The v2 capture validator, shared reference extraction, original-version historical read, command expected context and recovery forwarding are candidate edits. Actual source/producer join1/1, historical cuts9/9, capture format63/63 and M4 affected69/69 PASS; exact pinned evidence is in the existing delivery checkpoint and CAPTURE-SOURCE-PREREQUISITE-CHECKPOINT.json. The source/producer test uses declared unqualified producer identities, so it is not the full native workout/issuance witness. W6 public-client prepare/resume is now adopted from the APM-reviewed isolated helper with exact source/log pin verification; PM retains native source/order/replay work. No issuer/core/capability switch is authorized.

This binding can establish original-source identification for prospectively qualified captures. It does not establish an arbitrary global chronology for imported rows and older native workouts, duplicate resolution, rich/legacy correspondence or current prescription eligibility. Those consumers must use the executed source/replay membership and the common interpretation rather than unconditionally concatenate selected legacy plus all native rows. The first implementation acceptance must retain those limits and the original one-import case; it cannot claim full C2 from the new field alone.

September10 source-binding update: actual import/reading-replay.projectLineage now supplies earned/imported-engine-history/v1 from the guarded selected source. source_generation_id is the immutable source_id; activation_op_id is the ORIGINAL activation resolved behind a rollback. session_log references the same returned reconstructed accepted_state.sessionLog; a whole-result clone preserves the relationship. Existing coverage separately records current selected intent/action, original checkpoint and material identity. The actual R1/W6 signed source/second-import/rollback/reopen path now verifies this binding and exact imported row enumeration through the existing performed reader. This supersedes the synthetic-only source-binding status below; it does not imply native captured-workout/source composition, currentness, scientific qualification or active publication. Native operation context remains unmapped in the current source replay. IMPORT-CONTROLLER-v1.9 and the current report state the exact22/three-fault/actual-join evidence and limitations.

The optional internal importAnchor has source_generation_id and activation_op_id, supplied by the real import controller from its chosen immutable source generation. It is not an arbitrary renderer assertion. `engine-order` verifies the activation is accepted within the same complete prefix and every new Start causally descends from it, directly or through verified ancestor Starts. Later dates/log positions alone fail. This does NOT invent a new wire operation kind or prove that an arbitrary accepted operation is an import activation: the actual controller's source/activation binding, durable activation/rollback protocol and same-snapshot custody remain required production work.

The candidate workoutFacts.legacy_baseline holds profile earned/imported-engine-history/v1, matching source_generation_id/activation_op_id, and session_log pointing to the SAME immutable imported log used by the input state. Checkpoint identities must match engine-order's returned import_anchor. This shared reference avoids another history copy; it is a trusted internal composition invariant, not cryptographic proof or a mutation detector. The controller must own/freeze the snapshot and exclude renderer mutation. A whole-view structured clone preserves the shared reference. Replacing only sessionLog or the generation identity fails the mapping check.

Keep the original engine's date-key order and exact record objects WITHIN its imported baseline, followed by the causally post-activation native records in B15 order. This neither reinterprets legacy dates as authority causality nor labels display dates as semantic order. The existing actual repeat-noise reader keeps comparisons within each supported format; rich/legacy pairs remain unproved and are not fabricated by expanding a legacy scalar into a per-set vector. Positive mixed data retains all eight synthetic records and18 actual residuals; one new record after a legacy segment retains the old9 residuals without adding a cross-format pair. These are arithmetic/representation results, not scientific acceptance of the resulting recommendation.

Latest execution: engine-order20/20 tests,0skip, four named source faults detected; candidate208 legacy/97 rich/12 source sensitivities EACH native/frozen, restoredPASS. New failures cover missing causal import descent, dropped imported rows, replaced generation and replaced shared snapshot. Original records and accepted engine bytes remain untouched. Activation references/receipt positions are declared synthetic kernel inputs; no signature, real import, private census or physical phone verdict is earned. The output JSON now records the exact ordinary order-module hash as well as candidate/retained source pins.

### 5e. Actual stored-source mapping — implemented kernel, production binding pending

workout/engine-history.cjs now consumes actual storedWorkoutHistory and its SAME supplied generation through the existing scoped producer seam. It calls the actual injected projectWorkoutRecords again and verifies exact operations, status/frontier, athlete, source membership, captured originals and projected current values. Authentication, source revision/token custody and current-context/final-publication fencing stay with the actual host. These structural checks do not authenticate an arbitrary generation or establish an unseen remote head.

The trusted resolveCapturedLayout returns earned/captured-lift-layout/v1, the exact original producer/basis, a nonempty correspondence_profile and the complete original slot list with explicit lift positions and prescribed effort. It must be bound to the authenticated original capture and its registered format, not renderer claims or a current plan. Earlier executable tests use a declared synthetic registry; §5h now implements interpretation for the exact configured engine-capture producer. Non-supported specified effort target shapes refuse explicitly instead of becoming unknown.

Each completed session reaches the proposed real reader with per-slot performed/unlogged/skipped/removed/unresolved state. Every removed original remains in removed_facts; rejected source IDs and competing source identities remain addressable. Corrections change accounting while preserving original/current values and edit IDs. An absent final slot never promotes the middle set to terminal. Different explicit correspondence profiles do not pair, and arbitrary IDs without proven layout meaning cannot enter the selected reader. Cross-session positional/load checks remain conditional input mapping, not scientific eligibility.

The full causal source_order includes open records. sessions and its order.start_ids contain only the completed subsequence; incomplete_sessions retains open/ambiguous completion records and exact facts with completion:null. A completed workout is therefore not erased when another is open, nor is the open workout invented as completed. The completed reader refuses a directly mispassed incomplete entry. This is consumer-specific factual separation, not a new global hold or progression-bearing partition. progression_eligible remains false.

Executed32/32 source-mapping/order tests include actual reopened W6 corrections/removals, arbitrary IDs, bounded/zero/unknown/absent/not-asked/skipped-question effort, multiple removed originals, competing set records, same-day completed/open records and source/layout/status/scope disagreement. Existing nonshipping reader regression remains208legacy/97rich/12 named effective sensitivities each native/frozen Date, restoredPASS. Original conformance99/99/29/70 plus rig185 and selftestPASS; retained accepted static engine unchanged. No signature/schema2 admission, production import, personal/scientific or actual-phone verdict follows from these synthetic cases.

### 5f. Actual preparation/resume connection — implemented seam, production policy not qualified

W6 local4eba747 adds the optional static projectWorkoutHistory callback. The public client obtains the history privately associated with its authenticated candidate, passes it with the SAME generation/source revision into this mapper, and gives isolated workoutFacts copies to resolveWorkoutBasis, workoutProducer and workoutResumePolicy. A public request cannot replace it. A missing/mismatched projection yields no capture or handle. Preparation rechecks the actual repository revision and authenticated token before returning instructions, then checks current context and retirement; final prepared-Start transaction fences still bind the same source. Resume uses its existing final read with the new factual input. This connects source custody to the configured callback without granting scientific eligibility.

Actual M4/W6 tests now correct a stored set, reopen it, map inside preparation, call the proposed real reader inside workoutProducer, save the resulting capture on the next Start and reopen exact equality beside preserved prior facts. The basis callback receives the same factual content in a separate copy. The test uses causal ancestry to the original Close and correction, not dates or new ordering rules. Its output explanation is explicitly synthetic accounting/typed effort; no advice qualification follows. A second actual path delivers open facts to the resume policy while its completed reader remains empty.34mapping/order tests and372composed tests plus44native-host checks and conformance/selftest pass. The accepted static engine stays unchanged.

This hook does not itself supply a registered layout resolver, import source/activation binding, qualified production policy or complete current/recovery publication. Section5h supplies the engine-capture format resolver; other dependencies remain. Actual execution now confirms archive-proven positions already join M4 ordering: the upstream reader authenticates the SAME generation's existing receipt index before this internal mapper uses it (§5g). No metadata/renderer claims or new proof channel are needed. No schema2 issuer, accepted recovery, K1, private import, science or phone gate closes here.

### 5g. Recovered index compatibility — executed, no new runtime join needed

spec/recovered-engine-history.cjs extends only a disposable copy of the exact W6 archive-history fixture. The real Worker/D1/P1 schema1 prefix joins an actual retained pending workout, correction8→9, factual early Close and fresh repository read. The unchanged engine mapper receives that authenticated history and SAME generation. Its existing receipt index supplies frontier1 and causal order; the proposed real reader returns accounting360 and absent terminal, preserving original8/current9/edit ID/bounded3+/unlogged final slot.31/31 tests pass, including existing archive/original/index/forged-claim refusals. Historical recovered-plan profile remains present/inactive throughout; no fresh prescription or eligibility is granted.

This disproves the previously inferred missing archive-receipt→M4-ordering plumbing. No receipt field, marker, runtime fallback or extra authentication abstraction is warranted. The original-layout resolver in this test is still the fixture's explicitly declared synthetic two-position producer, not the production resolver. Local schema2 capability and generation placement are synthetic; accepted-workout recovery, production import/current policy/K1/science/phone requirements remain. Candidate static engine and W6/M4 runtime bytes are unchanged. Exact source pins and generated module are preserved with the run evidence.

### 5h. Engine capture and registered original-layout interpretation — implemented candidate

Current integration state, September9: workout/engine-capture.cjs prepares immutable targets through the actual genSession/rirPlan readers. Its new readLayout interprets the authenticated original capture for the exact configured producer/build/profile. Whole-capture validation, canonical tuple IDs, contiguous per-lift positions and strict typed effort establish this registered format's meaning. It preserves original bytes/order and rejects foreign producers, malformed positions/effort and accessors. It does not authenticate arbitrary input or attest engine execution. Other producers/arbitrary IDs require their own supported resolvers.

Historical factual interpretation no longer requires a second full original engine-state store. resolveLayout retains whole-engine replay as a diagnostic comparison: original clock yields effort[2,1], current clock three days later yields[2,0], so replay through the current engine disagrees while readLayout correctly preserves the captured original. That difference is not a new historical snapshot-retention requirement. Authentic capture custody, source revision/current permission and actual import/current-state binding remain mandatory. APM agreed this technical direction; that is not independent acceptance.

Actual retained W6 Start→Set→early Close→correction→fresh reopen now uses readLayout without the previous test-owned original-input Map. Corrected9reps retains original8/edit IDs and accounting360. The actual authenticated-history preparation hook then passes those facts into the actual genSession/rirPlan capture adapter for a changed current60lb/3set plan. Next Start and another fresh reopen preserve both original40lb/2set and new captures exactly. This is an explicitly native-only synthetic engine-state/identity/lease/store fixture, not production import, qualified current-state projection or proof that every policy consumes rich facts. At the v1.8 cut rirPlan's opener-history reader was legacy-only; §5i now connects that consumer in the nonshipping candidate.

Final45/45 focused checks (11capture,13history,21order),0fail/skip: .tmp/engine-capture-read-layout-final.log. Original conformance99reference/99strong/29RED-first/70adapter plus rig185 and SELFTEST PASS: R1 .tmp/captured-layout-{conform,selftest}.log. Adapter SHAdac2d28104cc05dabaf72289e96ba0d90503c183969b38df314397dc62b16709. Accepted static engine and W6/R1 runtime bytes unchanged. Prior held-opener guard, exact per-set/debut vectors, zero/missing distinction and unknown confidence remain preserved. No physiological rule or scientific/private/device verdict is added.

Next is actual immutable import/source activation and qualified current engine-state/history/policy, permission and publication binding; reuse the existing shared legacy-baseline/import-anchor mapping without inventing legacy per-set values or new capture protocols. Existing recovery31PASS already proves archive-index compatibility. W6 stays4eba747; R1 stays02de388 over reviewed342. Accepted numeric3e908/artifactd21 Review149 still awaits the actual integrator ledger receipt; no selfreceipt or unchanged package rerun. APM owns capacity: the original frozen-lock Linux witness failed149180269B against100663296, with minimal Linux diagnostic preparation assigned to the existing High helper. No root memory experiment.

Full PRODUCT-GOALv3/CURRENT-PRODUCT-DECISIONSrev3/P1–P6/all eight first-use groups and approved design remain. No logger-only substitution, new review campaign, merge/private activation/release or demand to finish every future automation/Coach feature first. The sole ordered NEXT is outputs/EARNED-DELIVERY-STATUS.md. Prior v0.56 brief/report are preserved byte-for-byte in work/delivery-history/M4-{BRIEF,REPORT}-before-v057.md and retained4a08201. Historical queues below are superseded.

### 5i. Actual corrected opener history into current capture — implemented candidate

Current integration state, September9: the nonshipping performed-proposal source construction now connects the actual rirPlan opener-history consumer to the existing validated performedHistoryRows. Native completed workouts contribute their original first slot's CURRENT corrected reserve. Exact0 is hot; exact1/2 and at_least3 are known nonzero. Missing, unknown, not_asked, skipped, removed and unavailable first slots supply no rating; a later slot never becomes the opener. Other lifts do not contribute. Legacy-only inputs retain the original code path exactly. Mixed imported/native rows use the existing same-snapshot/import-anchor checks and retain legacy scalar observations without inventing per-set values.

The standing rule is unchanged: at least3 known opener ratings and upper median<=0. For supported nonnegative typed ratings, that is precisely a strict majority of known zero ratings. The implementation counts the threshold result rather than turning3+ into an exact measurement. It retains the actual existing target increment, reason, hold and alarm behavior. Arithmetic/representation parity is not scientific applicability or eligibility; no private prescription is qualified here.

Actual retained W6/IndexedDB integration created3 workouts with zero-reserve openers, then prepared the next actual engine capture. Before the source connection the reached assertion failed2 versus3; after it passed3. Two real prepare/commit edits correct zero to3+, the old prepared Start refuses, fresh reopened preparation gives2, and next Start/reopen preserves all previous captures plus the new capture exactly. Identity/lease/current engine state remain explicitly native-only synthetic fixtures. Registered readLayout retains authenticated original capture semantics without a second historical state store; current clock replay remains diagnostic comparison only.

Final46/46 capture/history/order tests,0fail/skip in .tmp/opener-host-final.log. Reader checks each native/frozen:208 exact legacy cases,151 rich checks (54new),14/14 effective named source sensitivities including wrong median and terminal substitution, restoredPASS. New matrix compares whole rirPlan output against the actual unchanged engine for every zero/nonzero count through8 ratings; additional cases cover unknown/absent/removed/skipped first slots, lift scope, imported rows and replaced imported snapshot. .tmp/opener-reader.log and .tmp/opener-reader-evidence.json SHAe66ef1a402d0984dccf37532fb49245e946eed8e6da55b59fcf016a180666714. Source construction SHA9e95c2364e4fd2d39d057272ad6c9985246bf1e235924d2cd1f8e0cdfc254bd4; virtual writer candidate51255306fc877c322501e75727ff7b5ab3bc966899037fee8bd56f3ea171a3d8 versus unchanged installed008d92961d210ed07850ea881bd3a9f01ee873c1958f8b7d3a47506342c96bf5. Required conformance99/99/29/70+rig185 and SELFTEST PASS, R1 .tmp/opener-history-{conform,selftest}.log. Source restoration/pins prove accepted static engine unchanged; W6/R1 runtime unchanged.

Setup failures are retained separately: initial host fixture gave Close an unsupported reason instead of its causal parent list; corrected to the existing API before the reached2vs3 failure (.tmp/opener-capture-red.log and opener-capture-reached-red.log). Initial reader additions reused a local variable name, then an added assertion preempted an older mutant's named assertion; scoped the variables and ordered new checks after the existing checks. No product rule or old expected verdict changed. Final logs above are the claimed evidence.

Targeted source inspection found no actual immutable import activation controller in the retained product yet; PLAN-M3 requires source-generation/checkpoint/atomic activation and later-operation replay/rollback. Current source ownership cannot be replaced with a metadata token or fabricated activation operation. That actual controller/current engine-state projection and qualified policy/permission/publication remain next. This removes one named consumer gap; it does not close complete history-to-plan qualification or activate a recovered plan. The candidate's source construction stays nonshipping until coherent independent/integrator review and required acceptance. Accepted numeric3e908/artifactd21 Review149 still awaits its actual integrated ledger receipt; do not rerun unchanged numeric gates or self-author the receipt.

W6 remains4eba747; R1 remains02de388 over reviewed342. APM owns open Linux capacity and its existing preparation/reviewer lane; root remains High with no memory experiment. Full PRODUCT-GOALv3/CURRENT-PRODUCT-DECISIONSrev3/P1–P6/all eight first-use groups, approved design and qualified Joe/Dad use remain ACTIVE/unfulfilled. No merge/private activation/release or new tiny review campaign. The sole ordered NEXT remains outputs/EARNED-DELIVERY-STATUS.md. Prior v0.57 brief/report are preserved byte-for-byte in work/delivery-history/M4-{BRIEF,REPORT}-before-v058.md and300640f.

Current continuation is the single ordered NEXT in the retained outputs/EARNED-DELIVERY-STATUS.md. Bind actual import/source/current-state policy and qualified caller publication; obtain the actual numeric integrator receipt and coherent independent acceptance.
