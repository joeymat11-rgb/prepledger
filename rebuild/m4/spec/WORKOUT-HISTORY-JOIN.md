# Shared workout-history integration — pre-edit source amendment v0.2

Message63 correction: use WORKOUT-EDIT-NORMALIZATION.md v0.2 for proposed per-root interpretation containment, original editable-value validation, final-result Skip validity and explicit inert unsupported session records/effects. Caller/proof/prefix failures remain distinct global refusals. Preserve original typed values and all named issues at this actual source boundary. A last-effect position is provenance only, not the full current generation. Pending separation does not yet implement a pending overlay. The corrected model requires affected acceptance; none of these requirements authorizes a runtime edit. The following source diagnostic and bases remain unchanged historical evidence.

September10, M4 base87277ff, W6 base667e34d, R1 base98c1405. This pins the actual input/output boundary needed to implement the edit contract after its affected acceptance. It is not acceptance of the model, an installed normalizer, a full schema or permission to edit the client core. The active cd543b7 review packet remains unchanged. No new normalizer or alternative authentication layer is implemented here.

## Actual source and representation

W6 `rebuild/m3/w6/public-client.mjs:readWorkoutHistory` loads one repository snapshot and invokes `stageVerified` with authenticateWorkoutHistory. `verifiedHistory` authenticates the SAME recovery archives and wire-proof families, collecting compact recovery receipt tuples. `stageVerified` then calls `storedWorkoutHistory` before T2 consumes indexes or receives a candidate grant. The eventual public read requires T2 standing/view success, unchanged session/observation context, and a second repository load matching BOTH revision and opaque token. Preserve that order. A successful historical read never satisfies requireCurrentProjection; the existing recovered-plan guard remains.

W6 `rebuild/m4/workout/stored-history.mjs` currently reconstructs the complete accepted prefix using `collections.receipts`, `collections.ops`, wire proofs and private recovery receipts. It then filters to session class, refuses legacy schema, and associates only original-set correction/removal targets. `project-history.mjs` separately folds nonconcurrent Set edits. M4 `engine-history.cjs` compares those rows against the complete generation and reruns that old projector. These are the exact loops to converge; a detached renderer view cannot become the normalizer input.

The actual stored operation input is a parsed typed object in `collections.ops`. `history-proof.mjs:sameRecordedValue` compares original typed content while ignoring object insertion order. It distinguishes original Unicode strings and literal source_json strings. The cd543b7 review model's operationBytes field must NOT be populated with JSON.stringify(op) and described as the original wire bytes. Decimal spelling/property-order outside a retained string may already be absent from that parsed representation. Raw encrypted row/archive evidence remains in existing custody and must not be overwritten, discarded or regenerated to fit the model. Literal capture source_json and other recorded string values remain exact within the typed original.

Executed source diagnostic against actual W6 sameRecordedValue: three representation checks PASS. Different object insertion order and numeric JSON spelling decode to the same recorded value; different source_json spellings remain different; NFD/NFC label strings remain different. Artifact `C:/Users/joeym/Documents/Codex/2026-09-09/earned-active-development/outputs/EARNED-HISTORY-INPUT-DIAGNOSTIC.json`, SHA256 `4e7842998063f360fd6f3d64d51c3671bea8fa7ba00e634925cb4f421b620b72`, pins the actual representation/source-boundary modules. Reproduction is retained root `work/inspect-history-input.cjs` (writes that artifact once; do not blindly rerun). This establishes the production input representation only. It does not authenticate any record or prove raw-recovery preservation. The existing CAPTURE-START commitment/NFC distinction and P1 raw-row recovery evidence retain their separate scopes.

## One private assembly and one shared interpretation

1. Preserve the existing complete-prefix proof checks in storedWorkoutHistory, applied to every accepted record, including non-session facts. Do not filter the prefix before checking positions1..W, original identities, full typed values and receipt commitments. Proofs already authenticated by verifiedHistory are private call data; renderer/index claims cannot replace them. A transport predecessor is not a semantic parent.
2. From that SAME owned generation assemble a private normalizer input containing original operations by ID, accepted positions through W, the explicit local device/outbox/rejected/unresolved classification, and the original-schema registry. Do not introduce a public verified flag, hand-supplied accepted order, alternate repository, detached raw-JSON import or caller-selected decoder. Preserve the already enforced disposition/standing consistency; accepted and rejected cannot coexist for an operation.
3. Follow all relevant target chains before class filtering. Legacy effects can have a historical class different from their session target. Original Start/Set/Skip/Close, nested effects and proposed extensions/coverage must be associated by their validated original references and schema meaning. A label called lift is not a lineage ID. Unknown legacy basis/capture/lineage is an explicit mapping need, not invented provenance or corrupt-storage evidence. The full legacy bridge must close; this amendment does not adopt blanket rejection as a delivered solution.
4. Normalize the accepted layer using the reviewed causal/accepted-sequence semantics. Keep pending local operations separate, preserving original identities and explicit causality without fake athlete_log_seq. A later local overlay may apply a fully causal supported chain; unresolved concurrent pending effects remain identified. No array arrival order, timestamp, frontier number alone or outbox order settles a semantic conflict.
5. Produce typed originals and separate effective values/liveness/provenance, plus explicit unresolved records and associations. Keep original prescription capture/plan basis/slot identities immutable. Preserve removed/rejected originals and applicable nested effect identities. Fixed Skip coverage and immutable extension definitions require their additional accepted variants; the active edit-model verdict cannot implicitly authorize them. Session/slot partitions and generation/answer retirement remain separate required reductions.
6. Both public history and M4 engine projection consume that one normalized result. M4 verifies its correspondence with the SAME source generation and full membership/status/partition/layout before constructing performed input. Do not retain two competing edit loops or treat matching field values as proof that an old generation/answer is current. Configured loads need the separately reviewed typed layout/performed mapping; numeric fallback is forbidden.
7. Expose only a copied display result after the existing context/revision/token checks. Keep internal source references private and read-only during synchronous assembly; do not copy every full archive or attach duplicate operation graphs merely to create a new facade. Any changed allocation/resource behavior needs affected evidence. No capability, correction acknowledgment, next prescription or active plan is published by normalization alone.

## Exact prospective runtime edit boundary

| Source | Required affected delta after acceptance |
|---|---|
| NEW shared `rebuild/m4/workout/edit-history.cjs`, composed identically in W6/M4 | Implement accepted semantics against the typed private input above. Do not transplant the review model's assumed-authentication/raw-byte API. Use the closed original-schema interpretation; no runtime-pluggable arbitrary callback. |
| W6 `rebuild/m4/workout/stored-history.mjs:storedWorkoutHistory` | Preserve complete-prefix binding; assemble all target chains/classes, invoke shared normalization and associate original/effective records. Keep the existing public-client verification/candidate boundary. |
| W6 `rebuild/m4/workout/project-history.mjs:projectWorkoutRecords` | Replace the independent nonconcurrent Set fold with the shared result/compatibility adapter. Do not silently broaden the old interpretation label or omit unresolved edits. |
| W6 `rebuild/m4/workout/continuation.mjs:workoutContinuation` | Consume normalized Start/Close/Skip/Set liveness and, after their acceptance, explicit extension identities and coverage. Preserve actual component/current-safety/normal-finish gates; old capture is never regenerated. |
| M4 `rebuild/m4/workout/engine-history.cjs:createEngineHistoryProjector` | Compare source membership/status to the same normalized result, including rooted nested effects and effective Start/Close/Skip context. Preserve immutable capture and actual source ordering. |
| Existing shared `schema.cjs`, `authority-profile.cjs`, `commands.cjs` in W6/R1/M4 composition | Add only accepted closed variants and exact references; validate actual target-chain roots/ownership/lineage and coherent corrected results. Existing WORKOUT-SCHEMA-V2 names the necessary issuer/versioned Ops/client/authority amendments separately. |
| Existing W6 public-client.mjs/read/edit/prepare/continuation test paths and M4 history/capture/order tests; actual R1 admission/recovery joins | Update only reached compatibility plumbing and required executable cases. Preserve actual verifier/standing/grant/atomic-commit/context/token ordering; no new application core command or missing-guard fallback is authorized here. |

Before runtime adoption, copy this exact boundary and any reached extra function into the applicable `rebuild/m3/BRIEF-W6.md` / R1 brief amendment for the existing affected review. The table is a proposed allowlist, not standing permission. No installed engine, original laws/oracles/goldens, private fixtures, source intent profile, P1 format, extra collection/route or protected soak change follows from it. Full immutable schema and real issuance still require C1–C4 in WORKOUT-SCHEMA-V2.

## Required coherent proof

Use actual W6 commands/storage and current R1 admission/recovery when the amendment is accepted. Exercise accepted and local pending edits in the same history; complete prefix including non-session entries; corrected Start date; normal/early Close changes; Skip correction/removal; nested edit/removal/restoration; original capture and raw custody preserved; applicable legacy-class target chains; concurrent accepted ties and unresolved pending ties; explicit later-added-slot coverage after acceptance; rejection/conflicting proof; and changed account/context/revision/token after asynchronous verification. A missing/foreign/WAITING dependency cannot become Saved. Reopen and signed recovery must produce the same factual interpretation while keeping currentness, source rollback and next-prescription permission separate. No standalone assumed-prefix model or forged version2 lease substitutes for the required genuine-issued full journey.

Reuse unchanged evidence where its scope still applies. Required conformance/selftest/strict and affected real-client/native-browser/recovery gates follow the actual edited files; original numeric integrator and capacity gates remain separate. Reviewer acceptance and the named integrator receipt remain mandatory. This document supplies a concrete integration boundary to reduce another design round after the active semantic verdict; it does not start another review assignment.
