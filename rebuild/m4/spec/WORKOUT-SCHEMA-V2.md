# Complete app schema 2 and capability transition — candidate v0.9

v0.9: SOURCE-CONTROL-V2-01 supplies the exact two reserved source-control variants and domain-compatibility proposal in §3a. PM read the helper's actual one-file codec delta and terminal19 component checks; APM independently checked14 committed/copied pins and read the evidence. No codec patch is installed. The changed intent function is shared by transaction prevalidation and both historical readers; history-only testing cannot enable real admission. C1 temporal semantics, full dispatcher/reference extraction, issuer/standing, immutable manifest and all C1–C4 acceptance remain required. Current M4 native membership/opener/supported-baseline joins are recorded in PERFORMED-ENGINE-v1.12 and the canonical checkpoint; they do not complete schema issuance.

v0.8: C2's imported-engine baseline now derives from the actual guarded source selection/reconstruction, including original activation behind rollback, exact shared sessionLog, checkpoint/material evidence and final currentness guard. Actual R1/W6 source/second-import/rollback/reopen reaches the existing performed reader; IMPORT-CONTROLLER-v1.9 records22 focused/three faults/restored22 and the actual joined witness. This is distinct from schema1 operation observations and does not fabricate their original capture/lineage. Native captured-workout context through source replay, permitted v2 relations to old Starts, partition/currentness and all C1–C4/manifest/issuance conditions remain open. Rev162 independently ACCEPTED the preceding shared runtime at f2e5594/9524515/2e2ac5f only, with caller/layer/provenance/capture limits;rev163 separately ACCEPTED1752068/739bcfc legacy interpretation/host with all original limitations. Neither acceptance qualifies this new source binding or complete schema issuance. Older status paragraphs remain history.

v0.7: Message65 scoped correction/amendment ACCEPT and subsequent APM pre-edit scope acceptance now have a retained runtime candidate. Shared shape/relations implement actual-target replacement variants, optional clear restricted to replacements, effective-time parity and conditional rooted lift lineage. Shared typed history drives W6 saved/reopened interpretation and M4 factual reader, preserving original captures and accepted/local separation. This closes the named implementation gap only to its executed author evidence; independent runtime acceptance is pending. C1–C4, complete registries/manifest, configured-load/extension/legacy/source/currentness and genuine issuance remain open. No six-action partial rollout or private activation is authorized. Older paragraphs below retain historical status.

v0.6 over M4fbdb698 records the Message63 bounded REJECT and corrected edit proposal: contain unsupported post-admission interpretation per original root, validate final Skip after all live patches, enforce original editable-value domains without interpreting clear as an observation, and expose unhandled session records/effects. WORKOUT-EDIT-NORMALIZATIONv0.2 and WORKOUT-HISTORY-JOINv0.2 carry those requirements into the proposed common history boundary.25 author checks/12 effective faults/restored25 pass; independent correction verdict and all original C1–C4 conditions remain open. The existing shared workout shape already refuses the malformed Set recording examples; generic Ops.build is not full admission. No runtime or issuance changed. Older version notes below retain their dated input/review state.

September10, 2026. **PRE-EDIT PROPOSAL; NOT REGISTERED, ISSUABLE OR ACCEPTED.** This is the complete scope and transition contract for closing the already-required workout capability. It is not a claim that all wire forms or interpretation rules are closed. The explicit closure items in §6 prevent publication of the final immutable manifest. They are prerequisites inside the existing Earned delivery task, not another queue. No issuer, T2, accepted engine or runtime is changed by this document.

v0.5 over M487277ff pins WORKOUT-HISTORY-JOIN.md's exact private assembly, typed-original representation, shared normalization and existing standing/context/token boundary. Three actual source representation checks establish why JSON.stringify(collections.ops[id]) cannot be labelled original wire bytes. No new normalizer implementation or review dispatch. The inventory below now explicitly includes the proposed17th extension kind and fixed Skip coverage, rather than leaving them only in a preceding version note. All C1–C4 and affected pre-edit acceptance remain.

v0.4 over M4ce56b67 makes the added-slot representation executable: fixed within-extension capture indices, domain-tagged IDs derived after actual operation allocation, exact definition references and closed lift-Skip slot_coverage that cannot consume future slots.14 checks/seven effective faults/restored14 pass; actual builder/command refuse the proposed kind. LOAD-AND-SLOT-EXTENSION.md v0.2 records the exact fields, additional edit/partition and adoption boundaries. This is a nonshipping representation model, not complete envelope/authentication/normalizer, durable command or accepted17th kind. The ongoing cd543b7 edit review is unchanged; its verdict cannot implicitly accept extension/coverage additions.

v0.3 over M4cd543b7 adds LOAD-AND-SLOT-EXTENSION.md: the actual engine's exact wKey representation, an opt-in M4 mechanical capture/layoutv2 candidate (10 focused/7 effective faults/restored10; combined56) and the proposed durable session-extension/slot-definition reference. The latter would explicitly ADD a17th kind to the existing16-kind inventory below; it is not accepted, issued or implicitly included by a legacy generic variant. Its edit/partition/order closure remains required. Configured performed facts/rich-reader mapping and all prior C1–C4/manifest conditions remain open. No issuer/T2 or default profile adoption follows from the capture candidate.

v0.2 over M4425528d adds the concrete WORKOUT-EDIT-NORMALIZATION.md candidate and executable model for C1/C2: clearing, dates, all four workout fact variants, nested edits/removals, original logical position and protected legacy interpretation.19 checks/six effective faults/restored19 pass, including actual legacy commands and candidate relation. These choices still require affected technical acceptance; the full legacy bridge, remaining field families and all complete-manifest conditions remain open. The original source-boundary evidence below is unchanged.

## 1. Source authority and reached boundary

The governing sources are PRODUCT-GOALv3/CURRENT-PRODUCT-DECISIONSrev3 and later owner amendments, BRIEF-OWNER-WORKOUT §1–2, the runtime sheet A1–A5/B1 and workout-resolution rules, WORKOUT-BASIC-SCHEMA §§1/5/6, CAPTURE-START-v1, and the current source/current-plan checkpoint. Preserve the accepted offline-answer contract and every scientific/private/integrator gate. This document proposes technical assembly; it cannot decide an unruled training or owner-consent question.

Exact preparation bases, all under `C:/Users/joeym/Documents/Codex/2026-09-04/read-rebuild-t3-brief-md-and/work/`:

| Role / directory | Branch | HEAD |
|---|---|---|
| M4 / owner-workout-brief | rebuild/owner-workout-brief | 5c8aadd5d76bcb20b65ee1260e30608bb0274008 |
| W6 / m3-w6-browser-bridge | rebuild/m3-w6-browser-bridge | 667e34d990c7fcdd91f748da08f29e52b2773ad0 |
| R1 / m3-w5-r1 | rebuild/m3-w5-r1 | 98c140514db7aee3c175d1a7ec1ce6d73f14a046 |

`schema2-boundary.cjs` executes the retained modules; `.tmp/schema2-boundary-final.log` records nine successful diagnostic assertions. Evidence: `C:/Users/joeym/AppData/Local/Temp/earned-schema2-boundary-9ic9Lk/evidence.json`, SHA256 `f64c3432bad0db41b1e104a80700f726f391523339f0b880f4bd9bc9f8f40c8d`. It pins the selected boundary sources and preserves its wholly synthetic inputs. These are shape/command/request observations, not genuine issuance or admission:

| Input | M4 basic | R1 basic | W6 basic | W6 capture-configured |
|---|---|---|---|---|
| Start without capture | accepts | accepts | accepts | rejects |
| Start with original capture | rejects | rejects | rejects | accepts |
| Existing numeric set, explicit zero reps, unknown reserve | accepts | accepts | accepts | accepts |
| Ordinary reading, declared version 2 | rejects | rejects | rejects | rejects |

The actual W6 command preserves the complete capture and literal empty Start payload. The generic authority validator accepts the synthetic reading shape, but the selected workout component rejects it. The actual R1 typed relation also accepts a reference to a shape-valid legacy Start with no new plan basis/capture. That does not qualify missing historical instructions. The current route codec accepts an enrollment request for version1 and rejects version2. Actual `issuer.cjs:validLease` and `makeIssuance` independently restrict issuance/history to version1; changing only the request codec would not implement the transition.

The captured W6 and basic R1 implementations must converge on ONE final schema before any genuine version2 lease. Hand-signed old component fixtures are unissued synthetic experiments; they cannot reserve two different production meanings for number2. Preserve them as attributed evidence, never populate a production registry from them.

## 2. Immutable manifest and dispatch contract

The final manifest is a static, reviewed artifact binding the schema number to: the exact operation class/kind matrix; every common and variant field; unit and presence domains; canonical profile; reference extraction; version-specific normalization and domain mapping; capture profile; command mappings; and source/module hashes for both client and authority. Its final digest is computed only after §6 closes and the shared implementation is pinned. This draft has no made-up final digest and no `enabled:true` substitute.

Keep the original `earned/op/v1` commitment domain and canonical encoders. The signed lease's schema number selects one immutable registered meaning; requests cannot supply a validator, profile object or alternative manifest. A future changed meaning needs a new schema number. No extra lease field, new signing key, capture registry or payload-encryption format is required solely to dispatch this schema. The existing signed application/build contract must identify the installed registry; a same-number different-manifest installation must refuse before writes.

Version1 retains its original validator, signed bytes, canonical behavior, original issuance records and exact terminal replay. Do not revalidate an already terminal legacy operation against version2. A late pending/WAITING operation uses its ORIGINAL issued capability and original schema, including after the device's current pointer advances. New version2 operations go through the complete version2 dispatcher at both local commit and admission. No unknown kind/class, missing module or unsupported version falls through to the permissive version1 validator.

The six-action component and the capture component are internal parts of that dispatcher. They are not separately switchable interpretations of a genuinely issued version2 capability. A version2 installation must also construct the ordinary daily facts and plan/decision actions below. Routing the old commands to version1 under a current version2 lease, or disabling all ordinary commands after upgrade, does not fulfill this contract.

Common envelope fields remain exactly `op_id, athlete_id, device_id, device_seq, device_predecessor_op_id, causal_parents, class, kind, effective, schema_version, lease_id, payload, canonical_content_commitment`, with `target_op_id` only for correction/reclassification/tombstone and the declared kind-specific additions. No UI-provided identity, schema, lease, sequence or arbitrary `extra`. Descriptor-safe closed JSON validation precedes common ownership/commitment and semantic checks at the appropriate existing boundary. Calendar validity and explicit athlete-attested offset must be defined once for new writes; preserve original historical dates, not reinterpret them through the host timezone.

Keep all units explicit/immutable, absent/null/value distinct, and recorded zero only where valid. Existing positive finite pound loads and nonnegative integer repetition counts keep their exact representation. Reserve remains optional `exact`0/1/2, `at_least`3, `unknown`, `skipped`, or `not_asked` with the existing closed shapes. No text-to-number coercion, scalar flattening or fabricated observation. Nonnumeric/bodyweight modes and a clear-field operation are closure items, not excuses to approximate a required workout.

## 3. Complete operation-family inventory

The matrix accounts for all16 existing A3 kinds, all11 A4 classes, and the explicitly proposed17th session-extension kind. The added kind and coverage forms require affected acceptance; they are not registered by appearing here. A cell below describes a REQUIRED final component or named pending amendment, not retained implementation. Exact new spellings beyond already-reviewed components require affected technical acceptance before implementation. Unknown class/kind combinations are invalid in version2, never legacy fallthrough.

| Kind / proposed class placement | Complete required representation and relationship behavior |
|---|---|
| `fact` / reading, food-day, steps, sleep, event, illness, pain-attestation, setup-note, body-composition-source | Close each A4 map, required/optional fields and units. Reading lb; food kcal/protein/missing status with at least one observation or explicit status; integer steps; sleep hours and optional quality; typed event interval; illness start/optional end/note; session-only pain referencing Start or open interval later closed by a targeted edit; nonempty setup text; body composition kind/quantity/unit/endpoints/optional point/provenance/effective date and linked weight unless attested total mass. Session-only pain and linked weight are real same-athlete dependencies. No `fact` under plan/session as a generic escape. |
| `session-start` / session | Existing slot and plan basis plus REQUIRED inline `prescription_capture` from CAPTURE-START-v1; payload exactly `{}`. A registry excludes reserved AD_HOC/NO_ACCEPTED_PLAN spellings from real IDs. Explicit ad-hoc choice does not grant arbitrary basis. Resolve the actual governing plan/source and its references; validate ownership, original capture profile and immutable input relation. Receipt is not scientific/current-consent evidence. |
| `session-set` / session | Existing Start, logical slot and lift lineage envelope; independent actual load/reps/optional typed reserve. Bind to the captured or explicitly appended slot in the SAME workout lineage, preserving each mixed load. Added slots need an append-only representation (§6). No set-slot collision is silently deduplicated. |
| `session-extension` / session — proposed17th kind | Start reference and immutable payload.extension_capture per LOAD-AND-SLOT-EXTENSIONv0.2, with fixed within-extension indices and derived domain-tagged slot IDs. Exact ownership/source/definition/lineage and edit/partition joins must close. No global legacy kind-list expansion or fake Set to define an unperformed slot. |
| `session-skip` / session | Start/lineage/scope, optional reason. Set scope requires one logical slot and the definition reference for an added slot. Proposed lift scope requires exact nonempty unique slot_coverage of known original/added slots; later additions cannot inherit the Skip. Same reference/slot rules; coherent corrected variant. Correct/remove a mistaken skip append-only; skipping is never a zero-repetition set. |
| `session-close` / session | Start reference, no slot; normal/early completion. Preserve unlogged slots and distinct concurrent Close records. Removing/correcting a Close changes the effective history under the reviewed reducer, not the original capture or prior facts. No implicit plan approval. |
| `correction` / original factual class or session | Original target plus nonempty validated replacements, including explicit effective date/time/offset where applicable. Replace only named fields; preserve all others. A targeted edit is validated against the target's real variant and lineage, not just the caller's class. Optional-field clearing needs an explicit representation distinct from unknown/null (§6). Corrections to skips/Close/pain interval ends must work. Corrections-of-corrections are not silently treated as original facts; settle their exact original-target semantics before release. |
| `tombstone` / original factual class or session | Original target and nonempty reason; append-only removal with originals/effects still recoverable. Define removal effects on Start, Set, Skip and Close, and corresponding descendants. A plan transaction uses guarded Undo, never this branch. No broad cascade deletion. |
| `reclassification` / original factual class | Original target and declared compatible replacement class; otherwise complete validated destination payload and effective fields. No reinterpretation of old payload units. Classification/effective-time changes enter the reviewed common normalizer and invalidate affected generations; arrival order cannot define them. |
| `lineage` / session (proposed placement) | A3 lift lineage, optional parent and event kind, with sealed rename/technique-fork/context-fork payload. Rename retains identity; technique fork supplies child/parent/carry rule; context fork retains identity and actual exposure boundary. These are source identity/comparability operations, not mutable labels that rewrite history. Exact placement/carry-rule registry remains §6. |
| `session-relationship-resolution` / session | Previously proposed envelope `candidate_start_op_ids, component_generation, covered_decision_op_ids, answer_commitment`; sealed `{workouts:[{start_op_ids,plan_basis}]}`. Complete partition, each member exactly once, chosen basis per block. Complete declared Start/prior-decision/basis dependencies, no smallest-ID default. |
| `set-slot-resolution` / session | Previously proposed envelope `combined_workout_id, targeted_set_op_ids, component_generation, covered_decision_op_ids, decision_commitment`; sealed `{attempts:[{set_op_ids,canonical_set_op_id,logical_set_slot}]}`. Complete duplicate/distinct partition, canonical choice belongs to its block, resulting distinct slots unique. Same original operations remain. Normalizer, keyed preimages and generation applicability must close first. |
| `plan-mutation` / plan | Existing domain/lineage/requested transaction identity/seen basis/member-set commitment; complete declared disjoint-domain group, values/units and each member's athlete_edited/inherited provenance. Preserve the existing one-commit admission + sole plan transaction outcome. No arbitrary unknown plan field or partial group; old/new domain mappings remain atomic. |
| `undo-request` / plan | Target transaction, seen plan basis, target effect digest and exact compensating-group commitment. Existing unique maximum/unsuspended/current-effect CAS; one applied compensation or durable non-applied history, never blind value restoration. |
| `conflict-selection` / plan | Domain, lineage, one-to-one requested transaction ID, seen conflict basis, chosen alternative commitment and complete sealed alternative. Cover current alternatives and preserve atomic result/exact retry. |
| `proposal-response` / plan | ALL nine A3 response envelope fields: proposal_issuance_id, proposal_family_id, evidence_generation, decline_fingerprint_digest, seen_dependency_basis, seen_engine_version, offer_digest, chosen_outcome_id, consent_digest. Sealed selection mirrors the exact chosen outcome. Validate the real authenticated issuance, allowed outcome and exact delta; preserve APPLY/KEEP/NO. Existing legacy payload aliases do not satisfy this new form automatically. |
| `conflict-resolution` / plan | Same nine response bindings PLUS complete covered decision antichain and sealed exact resolution snapshot/complete affected group/chosen values. Never confuse this proposal-decision resolution with plan conflict-selection or workout partition. |

Read/resume/history, draft discard and returning to a screen create no new operation simply to navigate. A corrected rejected entry is a new independently validated operation with a local replacement link; it cannot reuse the rejected identity or receive an implicit correction relationship. Import activation/rollback remains the explicitly bound source profile, with its own accepted intent/source/checkpoint verification; an arbitrary event matching its general shape is not a source activation. Actual `source/codec.cjs:intent` permits ONLY version1. Its reserved event variants are `source-import-intent` with exact interval/source_id/material_digest and `source-rollback-intent` adding target_activation_id. Version2 must define an explicit compatible reserved control variant and reviewed source-profile/version interpretation before issuance; merely accepting general event fields or retaining an old expiring local lease cannot supply a complete upgraded app. The existing material, immutable selection and original before/after-prefix checks remain. Do not alter historical version1 intent bytes or claim this document already closes the profile-version question.

All declared references from every variant join the existing dependency cut in a deterministic, documented order: causal parents first in their retained order, original target next, then variant references in the final manifest's order, keeping first occurrence. A transport predecessor is not a semantic parent. Unknown/unaccepted dependency WAITs; rejected/foreign references retain existing terminal outcomes. Extracting references is not proof of basis authenticity or generation applicability. Missing static implementation is unavailable, not a fabricated rejection carrying an injected `rejectionCode`.

### 3a. Reserved source-control compatibility — precise prospective C1 amendment

Both proposed version2 forms retain the complete common envelope, class:event and kind:fact. Import payload is exactly {type:"source-import-intent",interval:{start,end},source_id,material_digest}. Rollback payload is exactly {type:"source-rollback-intent",interval:{start,end},source_id,material_digest,target_activation_id}. No new envelope field, operation kind or top-level target_op_id is introduced. The source_id names immutable material, not an operation. Rollback's target names an earlier ORIGINAL activation for the same source; request, payload, material and retained binding must agree. Preserve the staging-device/original-basis checks and exact complete before/after-prefix/selection-chain verification.

The complete v2 dispatcher must add payload.target_activation_id to rollback's operation references after retained causal parents, deduplicating first occurrence under §3. Import adds no operation reference. Do not require the caller to duplicate the rollback target in causal_parents to compensate for absent extraction. Request expected.selection_id and manifest basis.selection_id remain source-chain bindings, not invented payload fields. The current session-only shape/profile rejects these fact/event variants and extracts no such reference; source transaction's separate target lookup does not substitute for the complete dispatcher.

Proposed domains remain earned/source-import/v1 and its existing material/chunk/component/request/prefix tags, sourceImports fields/keys, earned/r1/v1, rows-v4 source inventory and P1 collection-bound format. The original earned/op/v1 commitment domain/canonical encoders remain. This means compatibility of these unchanged payload/source shapes, not identical v1/v2 operation bytes or digests: schema is inside the envelope, so commitments/accepted prefixes differ, and later checkpoint/material digests can also differ. Existing schema1 original bytes and old-cut behavior stay exact; unknown versions refuse. The final installed complete profile must govern new version2 interpretation. No final manifest digest or new source/storage format is selected here.

The actual source/codec.cjs:intent currently permits schema1 only and validates interval endpoints merely as strings. The proposed isolated delta permits exactly1 or2 at this subset check. It is called by source/transaction.cjs BEFORE authority admission and by the shared synchronous/indexed history generators; therefore it cannot be installed independently as a history-only feature. Complete descriptor-safe closed JSON/common-envelope/effective/interval validity, authenticated athlete/lease/commitment and declared-reference checks remain required at their actual boundaries. In particular this amendment does not define the remaining C1 temporal normalizer or weaken it to string-only endpoints.

Evidence is APM authority-root acceleration-trial/SOURCE-CONTROL-V2-01/RESULT.md SHA0b2faadaff138e7768349880c58cc30cd89012ad30795cddc1f43f99af270e48, candidate.patch SHA43d359bc73da70e037fbbedfd5f27ab48c8e9fc93440fb4ae5d903288abbf93d and terminal-final.log SHA731fe60a9935120d38625f14a51a685c4a370735a03d05e7d0a6865e850e2b0b.19 actual-codec component checks PASS: original v2 refusal; candidate v1/v2 import/rollback and sync/indexed historical cuts; exact old v1 raw/canonical bytes/digests; unknown versions and wrong target/material/device/chunk/prefix/extra fields. These use minimal synthetic semantic rows and unauthenticated commitment placeholders, not a complete signed inventory. The rollback fixture includes its target as a causal parent and therefore does not prove future automatic reference extraction. The wrong-athlete upstream negative was NOT run; a context-free bare intent has no authenticated athlete input, recorded as LIMIT rather than PASS. Genuine issuer/lease/schema mismatch, WAITING/reference and signed mixed recovery remain unproved. The helper is stopped; no product file was changed by it.

## 4. Real issuance, renewal and atomic local transition

Reuse `earned/r1/v1` issued history and existing rows rather than inventing a second capability registry. The static schema manifest maps supported versions; original signed lease bytes remain in `issuedLeases`. Default version1 behavior remains unchanged. Enrollment/renewal requests choose a supported installed version explicitly; version2 is allowed only after its COMPLETE manifest and implementations are accepted/installed. No network request can opt into a partial component.

1. Preserve authentication, account/device standing and current-pointer validation. Exact intent identity includes the requested schema (already present in the digest input); nonce renewal alone does not change the stable intent. An exact previous intent returns its ORIGINAL issuance even after a later successful renewal. Reusing an intent with a different schema conflicts. Do not regenerate an old issuance using the new current schema.
2. Pass the validated requested schema into the actual issuer. Preserve capacity1048576, minimum30-day term, lower range1, monotonic upper range/creation epoch/issue ordinal and exhaustion checks. One existing guarded D1 batch appends the original issued row + intent + standing event and advances the metadata/current pointer. Renewal never resets device sequence or shortens an old capability. A version2 grant is not a new device.
3. Retain every previous issued lease. Original v1 pending operations and WAITING reconsideration resolve their own `(device_id,lease_id)` and original schema after the current pointer changes. Late arrival after not_after remains admissible under the existing authority law; revocation/range/reused-slot rules retain precedence. Exact terminal replay occurs before new-profile interpretation.
4. The client first verifies the complete signed issuance response, identity, original intent, schema manifest support, standing/current-creation relation, clock and range. A verified future lease is a PREPARED transition candidate, not permission to change the active configuration. Copy it privately; asynchronous verification cannot publish after account/session/revision changes.
5. In the existing W6 repository transaction, compare the original revision/token plus current standing/context. Preserve all original operations, outbox/pending/WAITING/rejected entries, captures, drafts, source archives, clock high-water state and next sequence. Store the verified lease and selected immutable manifest identity in the SAME complete generation; do not merely swap `cfg.lease` or a metadata pointer outside persistence. Release the new configured writer only after transaction completion. Existing prepared Start/decision handles are invalidated, not silently rebuilt under the new grant.
6. Abort/quota/definite precommit failure retains the old entire generation. Lost acknowledgement/uncertain commit locks transition resolution to the actual committed generation; exact retry reads the retained intent/issuance instead of minting a second device or Start. Reopen uses the persisted manifest/capability together; an installation lacking the new profile preserves data and requires update/recovery before new writes. A schema upgrade cannot replenish offline clock budget or restore lost standing knowledge.
7. Remote recovery authenticates the COMPLETE existing rows-v3/v4 inventory and every original lease/operation before interpretation. It requires installed version-specific interpretation for each encountered schema; merely verifying an ES256 signature or a positive schema integer is insufficient. Keep raw historical archives independently retrievable on unsupported interpretation, with no fabricated integrity-corruption or current-permission verdict. Continue preserving old archive domains/head separation and shared transport budget. No new collection/page format is proposed solely for this transition.
8. The final joined evidence must use genuinely issued v1 and v2 capabilities through actual Worker/D1/HTTP/P1 and the real IndexedDB generation transaction. Hand-signing a v2 lease or editing metadata bypasses the very invariant being proved. Pending originals from both schemas survive recovery, correction and rollback. Recovery remains inactive until the existing full source/current-plan/consent/publication guards pass; capability transition is not private activation.

## 5. Precise prospective source amendment

This is a pre-edit boundary for existing affected review. **It does not itself authorize accepted T2/issuer edits.** The governing BRIEF-W6 and BRIEF-W5-R1 need the accepted amendment/receipt before those edits. Preserve old pinned source allowances and tests; enumerate any additional file before changing it. Use the same three retained worktrees/PRs, one product writer, no new stream.

| Path / role | Required delta after contract closure and affected acceptance |
|---|---|
| `rebuild/m4/workout/` shared version2 validator/profile/commands/capture, composed into R1/W6 | Single closed manifest and variant dispatch, capture-required Start, exact references/relations; no request-selected partial profile. Existing basic-only exports may remain explicit diagnostic fixtures, never genuine grant dispatch. Final module names/hashes must be pinned before adoption. |
| R1 `rebuild/authority/index.cjs`, `admit.cjs`, `validate.cjs` | Trusted complete version dispatcher selected from ORIGINAL authenticated lease after existing range/revocation/occupied-slot order; preserve v1 and exact terminal replay. Full type/reference validation and immutable registry availability. No new canonical domain or bypass of existing plan atomicity. |
| R1 `rebuild/m3/w5/bridge.cjs` | Wire the SAME trusted complete dispatcher in ordinary/staged constructors and historical resolver. Preserve consistent snapshot, revision CAS, ownership, durable WAITING drain and reply cuts. |
| R1 `rebuild/m3/w5/reconciliation/{codec,issuer,project,verify}.cjs` | Explicit supported-version request/history validation; forward requested version to issuer; original intent/lease replay and version-aware recovery. Codec intent bytes for v1 remain exact. Existing synchronous/projected paths must agree with W6 indexed interpretation. No mutable enrollment schema override or weakened integrity check. |
| R1 `rebuild/m3/w5/source/codec.cjs` and its exact composed W6 source dependency | After the reserved source-control profile/version amendment closes, explicit version-specific intent validation in the SAME shared synchronous/indexed source interpreter. Preserve original source/profile bytes, immutable history and full material/prefix validation. Any required protocol/storage profile addition needs its own exact pre-edit amendment; none is implicitly authorized here. |
| W6 `rebuild/client/index.cjs`, `rebuild/m3/w6/t2-stage.cjs` | At the ONE actual Ops.build call select the installed lease-granted version for ALL supported commands; static closed command dispatch. Keep entered values, face/standing/lease precedence, full batch validation, onPreparedBatch and one actual committer. Broader commands need their exact allowlist/mapping, not generic `extra`. `client/ops.cjs` changes only if a reached witness requires one; its current explicit schema field already works. |
| W6 `rebuild/m3/w5/public-client.cjs`, `rebuild/m3/w6/public-client.mjs`, `bridge.mjs`, `repository.mjs`, `recovery-profile.mjs` | Authenticated prepared grant and manifest support, final existing token/revision/context checks, atomic complete-generation transition/reopen, mixed original-schema recovery. Use the current signed-verifier and storage infrastructure. No second repository or direct active metadata replacement. |
| Existing R1/W6 tests/composition runners and affected reports; M4 specification/witnesses | Exact real-issued mixed journey, effective failures and source pins. Runners must include current R1 plus actual W6 client, not replace it with R1's older copy. No seeded/private engine in public browser assets. |

This allowlist describes the prospective join, not permission to rewrite all listed files. If complete decision/lineage implementation needs extra reducers or authority plan sources, name those exact files/functions in the closure amendment before editing them. No SQL migration, new Worker route, P1 format, engine policy change, original laws/oracle, frozen app or soak edit follows from this proposal.

## 6. Conditions still required to freeze the complete manifest

These are concrete unresolved contracts, rather than empty `TODO` validators or optional features to drop:

- **C1 — full field/variant schema:** close the typed interval and sleep-date/quality normalizer, optional-field clear representation, all effective-time changes, correction/removal target rules, nonnumeric/bodyweight load representation, added-slot record, lineage placement/carry registry, actual plan-domain field registry and reserved source-control version compatibility. The current context witness demonstrates why raw native intervals cannot be fed to legacy `events`; no ratified detailed spine was found by product/APM. Preserve unknowns; a generic `additionalProperties` rule is not closure.
- **C2 — shared historical interpretation:** pin deterministic original-v1 up-conversion into the same domain/partition model; define which legacy Start/Set forms can support new operations and how real source/capture/basis is proved. Missing basis stays unknown. Neither blanket legacy rejection nor automatic qualification is adopted. Same-date/backdated/correction, split/rejoin, concurrent edits and contradictory decisions need a common normalizer and executable reducer fixtures.
- **C3 — complete decisions and currentness:** finish the already-proposed keyed generation/answer preimages, normalization-collision behavior, identical-answer aliasing, covered antichain, offer bindings and exact authority effect mapping. Retain the accepted ask-time/offline-answer contract. Owner Q1 about reuse of a previously accepted answer remains open where no later explicit ruling exists; do not infer an answer from a pending-answer diagnostic. A receipt frontier alone does not establish new consent.
- **C4 — end-to-end availability:** every required action in §3 must be constructible, validated, admitted/deferred and recovered under genuine version2, with its full current read model and error/recovery path. Publishing a numbered manifest before these contracts are closed makes future fixes an irreversible schema change. An unpublished candidate may evolve locally; an issued immutable schema may not.

Next concrete work is to close C1/C2 against actual retained semantics and the existing affected review, then freeze the machine-readable manifest and exact module list. The independently reviewable transition design in §4 can be assessed with that complete scope visible; approval of its issuer mechanics cannot waive C1–C4 or enable partial issuance. APM owns reviewer coordination, memory qualification and the designated integrator lane. Product stays High and remains the sole writer.

## 7. Required executed acceptance, when implementation is authorized

One coherent journey must include real v1 enrollment → accepted v1 facts + queued old fact + WAITING child → genuine renewal to v2 → atomic local capability/configuration publication → captured Start/sets/skip/normal or early Close and all wider commands → late v1 arrival/drain → signed full mixed-schema recovery → reopen/correction/isolated source rollback with every original and pending byte retained. Exercise separate devices and both delivery orders; do not use a false same-device relation or transport parent. Compare raw source bytes independently of NFC-equivalent commitments.

Named failure evidence must cover unavailable/malformed/throwing/Promise profile; schema/lease mismatch; unknown registry; intent collision and exact lost-reply retry; old lease removed from resolver; concurrent renewal/stale creation; revocation/range/reused-slot precedence; captured Start profile disagreement; each hidden reference omitted; v2 daily command accidentally stamped1; a metadata-only lease swap; failures before/during/after real IDB commit; old/new reader restart; unsupported old-history interpretation; changed session/token after asynchronous verify; and recovery dropping queued operations/capture/source archive. Effective failures must reach their behavioral assertion and restore exact copied source, not merely fail loading.

Retain original authority34/client35/full56 vectors and affected actual D1/HTTP/browser recovery tests, conformance99 reference/99 strong/29 RED-first/70 adapters +rig185, SELFTEST and strict with test clock unset, applicable scope and both-OS checks. Reuse unchanged numeric acceptance; obtain the actual designated-integrator receipt separately. Keep original96MiB memory failure and APM-owned hosted proposal distinct. No merge/deploy, private import/activation, phone/science qualification or final-app PASS is awarded here.
