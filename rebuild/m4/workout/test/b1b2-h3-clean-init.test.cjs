'use strict';
// Executes all original 14 tests, including seven original mutations and the
// first-read cells. Only the declared eight S1 assertion sites are transformed.
// Native and original fixture dependencies: authoring only until B-only PC.
require('./b1b2-evidence.cjs').loadOriginal('h3-clean-init.test.cjs',true);
