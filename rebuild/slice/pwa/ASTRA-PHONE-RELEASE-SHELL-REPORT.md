# Phone release shell — candidate for independent review

September 11, 2026. Branch codex/astra-phone-release-shell; base 55063f89a553af777a702f218b47b649cc11bb9b (first-use setup, accepted separately under 90c0263). Product/test/screenshots commit 4d570024cd485aed53cdd695a8c998e15cdb9292. FILE ASTRA-PHONE-RELEASE-SHELL.md at PM d2f0a28. Read NEXT/ROADMAP, A5 plan/report, approved design handoffs/references, first-use report/review and actual Today/status producers. Builder evidence only; distinct review required.

The existing PWA shell transformation now adds a release body and device viewport metadata, removes the fixed handset layout through release-only CSS, and uses one naturally scrolling document. The approved paper/ink/green and Instrument styles remain intact. The page fills narrow devices and uses the approved 420px reading-width cap on wider screens. Safe-area insets pad the release surface; the existing weight sheet is constrained to the device's dynamic height and remains scrollable. The stylesheet is appended to the existing hashed preflight asset: no extra asset, framework, app entry or cache policy. Today development source, approved reference files and its original emitted app/styles remain unchanged; the existing package check verifies those emitted app/style bytes.

Status inventory was traced before moving markup. today-status receives setup/save/read-adoption/refusal/restore/open-failure messages and remains a visible role=status strip before the app. today-identity carries owner/demo identity; today-storage carries local-store/setup availability. Both remain visible in the device footer, with their existing IDs and producers. The existing explanatory caption is retained. today-calendar is still inserted before the app and retains its dated-draft/Return to Today controls. PWA cache state/detail and install guidance remain visible in normal flow; no status area is display:none or overlaid on workout actions. No new save, backup, sync, restore or update guarantee was authored. Existing preflight/service-worker JavaScript is byte-unchanged; there was no existing explicit update button to relocate.

Source-before built PWA reproduction at 390x844: document width398, inner view388, frame top126.53125, fixed height844. Screenshot evidence/release-before.png. Corrected fresh built PWA: viewport/document/view390, top43.84375 occupied by the live status strip, no border radius/shadow, content-driven height983.65625. Screenshot evidence/release-after.png. This is a scrolling long form, not a forced single-screen form. Both committed screenshots were inspected. Additional inspected screenshots and measurements remain in .tmp/release-shots/ (03-review-320.png, 04-workout-small.png, 05-offline-reopened.png, 07-restore.png, measurements.json).

Executed with existing Node24.19.0 and installed Chrome through retained playwright-core; no installation. Commands:
- node --test rebuild/slice/pwa/test/pwa.test.cjs rebuild/slice/pwa/test/package.test.cjs rebuild/slice/pwa/test/workflow.test.cjs: 54/54, no failures/skips. Original 53 checks retained, one new release-consumer preservation check; upstream missing status/viewport also fails the existing shell-drift refusal control. Manifest, hashed assets, complete precache, response policies, no-private and no-remote-origin checks pass. Build remains13 files,11 precached,13 exact header rules. Original private-guard cases use planted synthetic strings, not private files.
- W7_BROWSER_BIN selecting existing Chrome, node rebuild/slice/pwa/browser-release-check.mjs: PASS against buildSite output and the supported serve-pwa server reading emitted response headers. Entire journey uses actual default owner boot and visible synthetic user entry. Public boot is only reused after enrollment for stored-authority observation; no setup/model/calendar injection or alternative writer.

The built-browser journey enters all seven supported exercises, reviews their actual loads, focuses validation errors, and reaches/focuses the final save control at390x844,320x568 and844x390. All viewport/document widths match; inner content has no horizontal overflow. Real saved setup has no workout operations and reopens identically without reenrollment. It then starts the actual upper session, types40x10, and checks reachable inputs/save at320x390. Four actual upper sets save, one Start and one normal Close are recorded. A170.5 weight is saved through the relocated sheet at320x390; input and submit are uncovered and reachable. Input text is at least16px. Offline reload and a new offline page preserve the complete stored setup/device/collections. A separate worker-blocked offline context cannot load the app.

Actual update control: a second complete local build changes a comment in a copied stylesheet, deriving a new asset URL/cache. The real worker takes control and reports all11 new assets ready while40x10 remains typed; logging continues. Offline reload selects the new stylesheet hash and preserves saved records. Observed immediately after takeover: both old/new cache containers still hold11 assets. This is reported without a cleanup claim or cause assignment; service-worker source is unchanged and PM explicitly retained cleanup outside this correction. Preflight state and install guidance remain present, and the journey has no off-origin requests or page exceptions.

Actual failure control: a native IndexedDB enrolled-marker put throws after commit through the real visible setup save. The focused form says reopen/restore, the live status repeats it, and save is disabled. Reload shows Restore required (ENROLLMENT_MARKER_MISSING), no enrollment form, with the storage refusal still visible/reachable. The error and device information were inspected in actual screenshots.

Execution notes: an initial wider cap640px was rejected by the retained approved-spacing check; replaced with the approved420px cap, leaving that check intact. A weight-input locator initially used a nonexistent accessible name; corrected to the real Weight in pounds and reran the full focused browser journey. Initial update assertions expected immediate removal of all old caches; observed the retained old populated cache, removed that unsupported cleanup claim, and retained verified new-controller/current-asset/offline checks. None is negative product acceptance credit.

A5 tests here describe the exact55063f8 base plus this shell delta. They do not claim the separate publication-guard integration's CI is green: PM routed its workflow.test.cjs blanket-regex correction to another author/tree. This candidate leaves workflow.test.cjs and all workflows unchanged. The separate older lifecycle assertion correction, final BNTC/source-policy qualification and complete nutrition/recovery/progression/routine coverage remain separate. Physical installed iOS safe-area, real keyboard/zoom and Home Screen behavior are not executed; desktop Chromium reduced-height checks are not relabeled as those tests. No engine/setup/storage/Today changes, private input, install/lock churn, broad engine campaign, source requalification, remote publication, push, PR, merge or deployment. git diff --check passes.
